.claude
.claire

hints/benches/*.bincode
hints/benches/committee_1024.json
hints/benches/committee.json
hints/benches/committee_128.json

extension/dist
site/dist
site/assets/dregg.pdf
site/paper/
paper/*.pdf

target
node_modules
__pycache__
.playwright-mcp

# Devnet secrets
docker/devnet-config/*
!docker/devnet-config/.gitkeep
!docker/devnet-config/README.md
deploy/genesis/keys/
deploy/genesis/*.key
deploy/genesis/secrets/
demo/two-ai-handoff/state/
demo/multi-node-devnet/state/

CLAUDIT.md

states/
spec/states

*.zip
*.xpi

metatheory/.lake
lean-objs

libdregg_lean.a*
# Starbridge Lean-as-web-citizen workspace: 3.7GB lean4-src checkout + WASM
# build spikes; a build/scratch area, never committed.
/web
/target-swaplane
/site/pkg
/sdk-ts/dist
sel4/dregg-pd/executor-microkit-pd/microkit-patch/microkit-2.2.0-patched

# build artifacts — never checkpoint compiled output into history
*.wasm
*.rlib
*.rmeta
*.dylib

# macOS finder junk
.DS_Store
discord-values

# night-shift render frames — artifacts, not source (ember: do not check PNGs into git)
night-shift-woven.png
*.woven.png
deos-*-woven*.png

# session working notes + product planning — NOT substrate (kept private, out of the public repo)
DEOS-NIGHT-SHIFT.md
PLANS-BREADCRUMBS.md
DREGG-COMPUTER.md
DREGG*-MIGRATION.md

# regenerable artifacts (stitch)
# NOTE: circuit/descriptors/*staged*.tsv are Lean-emitted but include_str!'d at
# compile time, so a fresh clone cannot build dregg-circuit without them. They are
# tracked via Git LFS (see .gitattributes) rather than ignored. Regenerate with
# scripts/emit-descriptors.sh on the Lean host; they change only on VK-epoch flips.
demo/run/*.png
orb/conformance/h2c-host/h2c-host
*.png.skip
wasm/pkg-mwt/
orb/conformance/h2c-host/h2c-host

# session / agent scratch + logs — ephemeral, never codebase (2026-07-17 sweep)
scratchpad/
tmp/
.codex/
*.log
cosmos-settlement/artifacts/
chain/gnark/emitted
rhlp-web

.hol
.spw/_workbench
.ignore/

# Build-lane OWNERSHIP LEASE (scripts/lane-lease.sh, read by scripts/sweep-build-lanes.sh).
# Remote per-lane runtime state, never source — but this line is also LOAD-BEARING, not
# hygiene. `scripts/pbuild` syncs with `rsync -az --delete --filter=':- .gitignore'`, and
# the lease lives at the ROOT of that destination (`<lane>/.pbuild-lease`). An rsync
# exclude is a receiver-side PROTECT rule as well as a sender-side hide, so this entry is
# the ONLY thing standing between a live lease and the next pbuild call. MEASURED
# 2026-07-26 with this line absent, on the real tree and the real command:
#   rsync -az --delete --dry-run -i --exclude='.git/' --filter=':- .gitignore' ./ persvati:dregg-build/crewbraid/
#   → *deleting   .pbuild-lease
# i.e. agent B's build would silently strip agent A's claim and the next sweep would then
# see an unleased lane and delete its artifacts. Do not remove this line.
.pbuild-lease

# scripts/feature-t3-sweep.sh's per-pair record. A measurement artifact, never source: the
# COMMITTED half of that sweep is scripts/feature-t3-known-red.tsv (the ratchet).
/feature-t3-sweep.tsv
/t3-shard*.tsv

# Atlas screenshots are large, regenerable-by-hand UI captures. They were 40 MB of
# LFS-tracked binary that every checkout re-pulled — which is what exhausted the LFS
# BANDWIDTH budget and made the entire site undeployable. They are not shipped; the
# site build generates a labeled placeholder wherever one is referenced.
dregg-atlas/site/screenshots/
