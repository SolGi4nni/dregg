# mina-finalize-scalars (::v2, gate-linearization included): the descriptor and the four
# 2048-row traces (tens of MB each) are UNTRACKED — deterministically re-emitted by
#   cd metatheory && lake build mina_finalize_scalars_emit
#   ./.lake/build/bin/mina_finalize_scalars_emit ../circuit/tests/fixtures
# The PI vectors are small and tracked. Same discipline as pasta-fq-chainlink/'s witnesses.
mina-finalize-scalars.json
mina-finalize-scalars-trace.txt
mina-finalize-scalars-forged-eval-trace.txt
mina-finalize-scalars-forged-perm-trace.txt
mina-finalize-scalars-lct-shift-trace.txt

# mina-wrap-opening-sched: the three 2048x581 traces (~5 MB each) are UNTRACKED —
# deterministically re-emitted by
#   cd metatheory && lake build mina_wrap_opening_sched_emit
#   ./.lake/build/bin/mina_wrap_opening_sched_emit ../circuit/tests/fixtures
# The descriptor JSON and PI vectors are small and tracked.
mina-wrap-opening-sched-trace.txt
mina-wrap-opening-sched-forged-sg-trace.txt
mina-wrap-opening-sched-forged-fold-trace.txt
