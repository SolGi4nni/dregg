# PICKLES-SYNTHESIS-EPOCH-SCOPE.md — porting Pickles' circuit SYNTHESIS to Lean

**Status:** RIGOROUS GAP READ + DESIGN. No implementation. First rung of the epoch
`project-pickles-in-lean-epoch` (ember greenlit 2026-07-31). This doc is the buildable rung plan +
swarmcycle-costed estimate that the epoch's first rung is defined to produce.

**Companion:** `docs/PICKLES-VERIFIER-SCOPE.md` is the CHECKER side (P0–P10 — "does this Pickles
proof verify"). **This doc is the SYNTHESIS side** — "emit the step/wrap circuit that, when proven,
IS a Pickles proof." They are different constructions that share the same arithmetic floor. Read
that doc's §A/§B first for the Step/Wrap cycle; this doc does not restate it.

## ⚑ SUBSTRATE — House Law #1, said out loud at constraint #1

The whole point of this epoch is **Lean-authored synthesis**. Every gate this epoch emits is authored
in Lean; Rust (proof-systems for VK/prove, mina-rust for witness) only ever RUNS the Lean-emitted
artifact. There is no Rust AIR here and none is to be written. The reference implementation
(`~/dev/mina/src/lib/pickles/`, OCaml) is READ-ONLY; it is **not built** (it is a March-2024 vendored
snapshot with no git rev — every OCaml pin below is path/line-checked against that snapshot, marked
`[read@2024-snapshot]`, and is [UNVERIFIED] against today's upstream, exactly as
`PICKLES-VERIFIER-SCOPE.md` §Provenance discloses).

**Verb discipline.** Every claim below is marked `[read]` (I read the source, file:line given),
`[exists]` (a Lean artifact is on disk, path given), or `[ASSUMED]` (my estimate/judgement, not
measured). Swarmcycle numbers are `[ASSUMED]`.

---

## §0 — The novelty, VERIFIED (not repeated)

The epoch memo claims Lean Pickles would be "the first independent SYNTHESIS + first machine-checked."
The brief said verify this, don't repeat it. I verified the three legs:

1. **OCaml (Mina/o1js) is the only synthesis.** `[read]` `step_main.ml` (581 lines) and `wrap_main.ml`
   (443 lines) build the step/wrap constraint systems via the `Impls.Step`/`Impls.Wrap` Snarky DSL.
   This is the canonical and (as far as this read reaches) sole first-class synthesizer.

2. **openmina/mina-rust is NOT independent synthesis — CONFIRMED against a partial contest.** A first
   read of the tree found no `*_gates.json` and Rust `step.rs`/`wrap.rs`, which looked like Rust-native
   synthesis. The deciding evidence is `[read]`
   `mina-rust/crates/ledger/src/proofs/circuit_blobs.rs:1-30`: openmina **sources circuit constraints
   from o1-labs' `circuit-blobs` repository — "pre-compiled circuit data generated by the OCaml
   implementation."** Three files per circuit: `*_gates.json` (the gate list), `*_internal_vars.bin`
   (variable→expression mappings for witness gen), `*_rows_rev` (row order). They are **fetched at
   runtime** (`provers.rs:55,327,330` `circuit_blobs::fetch_blocking`), which is why the local `find`
   was empty. `step.rs`/`wrap.rs` are the **witness generator + prover**, not the synthesizer — they
   consume the blobs. So openmina re-implements the PROVER, and LOADS the OCaml-synthesized gates.

3. ⇒ **The defensible novelty claim, stated precisely.** A Lean Pickles is the **first independent
   synthesis** (openmina loads OCaml gates; it does not synthesize them) **and the first that is
   machine-CHECKABLE** (a Lean `def` that elaborates and emits, over which fidelity/soundness theorems
   can be stated about the ACTUAL emitted object). ⚑ **Do not call the Phase-A deliverable
   "machine-checked Pickles"** — the synthesis is machine-checkABLE (Lean elaborates + emits), and its
   FIDELITY is established DIFFERENTIALLY against o1js (§4), but the recursion SOUNDNESS is not proven
   until Phase B. The honest artifact name for Phase A is **"an independent, differentially-verified
   Lean implementation of Pickles synthesis."**

**The consequence that de-risks the whole epoch (a favorable coupling, found this read).** Because the
recurring prover (mina-rust) consumes the `circuit-blobs` triple, and the dregg interim already emits
"dregg's own circuit-blobs" from o1js (memo), **the Lean emission target is a concrete, existing file
format**: replace "o1js compiles → circuit-blobs" with "Lean emits → circuit-blobs," and mina-rust
proves UNCHANGED. This is the literal endpoint of House Law #1 for the settlement path — and it means
Phase A does not need to touch the prover at all.

---

## §1 — The emission target, concretely: the `circuit-blobs` triple

`[read]` `circuit_blobs.rs:17-40`, `provers.rs:55,327-330`. For one circuit (a step or wrap branch),
Lean must emit three artifacts:

| artifact | what it is | byte-diff oracle | Lean status |
|---|---|---|---|
| `*_gates.json` | the gate list: `{public_input_size, gates:[{typ, wires:[{row,col}×7], coeffs:[hex]}]}` — kimchi `Circuit`/`CircuitGate` serde `[read]` `proof-systems .../kimchi/src/circuits/gate.rs:128-137,421-423`, `wires.rs:21-24` | **o1js** `Provable.constraintSystem(f).digest` / `Snarky.run.constraintSystem(f).json` `[read]` `o1js/src/snarky.d.ts:561-566,213-217` | `KRow` models `CircuitGate` `[exists]` `KimchiTarget.lean:166-169` — but pre-placement (variable indices, ℤ coeffs), NO row/col placement, NO hex render |
| `*_internal_vars.bin` | internal variable → linear-combination recipe used by witness gen | the witness must satisfy the gates; wrong mapping ⇒ no satisfying witness | **NONE** — dregg has no Snarky-style internal-variable model |
| `*_rows_rev` | row ordering | implied by gate order | **NONE** |

⚑ **`*_gates.json` is the visible half; `*_internal_vars.bin` is the load-bearing half.** The gates
can be byte-diffed against o1js directly. The internal-vars mapping is what makes mina-rust's witness
generator produce a witness that SATISFIES those gates — and it must be co-emitted consistently with
the placement in `*_gates.json`. This pairing is the byte-exactness crux (§4, §8).

---

## §2 — Catalog: dregg's existing Lean assets vs OCaml synthesis (checker-vs-synthesizer)

dregg built the Pickles VERIFIER (checker), never a synthesizer. The distinction that matters for
every row: dregg's asset is a **decision over abstract field values** (a checker: `f(evals, chals) :
Prop/Bool`) or a **data model**; synthesis needs the **gate ROWS that COMPUTE that decision over
witness columns** (an emitter). The checker is the SPEC the emitted gates must meet — a large head
start on the Phase-B correctness proof — but the EMISSION is essentially all new.

### §2.1 — The arithmetic floor (HAVE the math; the emitters are NEW)

Both Pasta fields and both curves are already modelled `[read]` `PICKLES-VERIFIER-SCOPE.md:169-178`:
`[exists]` `PastaField` (K1, `pN`+`qN`), `PastaCurve`/`PastaCurveComplete`/`PastaScalarMul` (K2/K4a/K4b,
both curves + endo + GLV ladder), `PastaPoseidon`+`PastaPoseidonFq` (K3, both sponge states),
`PastaIPA` (K4c). **These are verifier-side functions** (`Ref.hash : … → Fp`, `completeAdd : Pt →
Pt → Pt`), not gate emitters.

### §2.2 — The step/wrap circuit body, piece by piece

Column key: **HAVE-math** = a dregg checker/function computes the same value; **NEW-emit** = the gate
rows are new work; **transform** = what turns the checker into the synthesizer.

| OCaml synthesis piece | dregg checker asset (`[exists]`) | checker or synth? | transform needed |
|---|---|---|---|
| **In-circuit kimchi verify** — `incrementally_verify_proof` `[read]` `step_verifier.ml:498-649`, `wrap_verifier.ml:501-732` | `kimchiVerifyDecision(Gates/Field)` — all six gate bodies, Fiat–Shamir order, ft_eval0, cip, IPA `[exists]` `KimchiVerify.lean:539-575,977-1046` | **CHECKER** (decision over `CommRing R`) | in-circuit-ify: allocate witness cols for every intermediate, emit gates that constrain them to the decision. Biggest single transform. |
| **`finalize_other_proof`** — deferred-value discharge `[read]` `step_verifier.ml:887-1147`, `wrap_verifier.ml:820-1049` | P3 `finalizeOtherProof` (4 arms) `[exists]` `PicklesFinalize.lean`; `derive_plonk` reality-gated `[exists]` `PicklesDerivePlonkRealGate.lean` | **CHECKER** | same in-circuit-ify; 3 of 4 sub-check cores reuse K4c/K5 math |
| **Poseidon sponge** absorb/squeeze → `poseidon` gate `[read]` `step_verifier.ml:75-84,264`; `step_main_inputs.ml:57-90` | permutation math `Ref.hash` `[exists]` `PastaPoseidon.lean`; the poseidon BODY (verifier side) `[exists]` `KimchiVerify.lean:§9c.1` | **CHECKER** (permutation as a function) | emit the 11-rows-per-perm `poseidon` gate layout o1js produces. **dregg has NEVER emitted a poseidon gate row** (§2.3). |
| **complete_add** (`add_fast`) → `completeAdd` gate `[read]` `step_verifier.ml:110,479-492`; `plonk_curve_ops.ml:7` | group law `[exists]` `PastaCurveComplete.lean`; the complete_add BODY `[exists]` `KimchiVerify.lean` (7 constraints) | **CHECKER** | emit the `completeAdd` gate row + witnessed inverses |
| **`scale_fast`/`scale_fast2`** (varbasemul) → `varBaseMul` gate `[read]` `step_verifier.ml:241-245`; `plonk_curve_ops.ml:220,251` | GLV `[k]P` ladder `[exists]` `PastaScalarMul.lean`; the varbasemul BODY (21 constraints) `[exists]` `KimchiVerify.lean` | **CHECKER** | emit the multi-row `varBaseMul` bit-ladder gate layout |
| **`Scalar_challenge.endo`** → `endoMul` gate `[read]` `step_verifier.ml:204,282`; `scalar_challenge.ml` | `endoMap` (injective, proven) `[exists]` `PicklesTranscriptBinding.lean`; endomul BODY (12) `[exists]` `KimchiVerify.lean` | **CHECKER** | emit the `endoMul` gate rows |
| **`SC.to_field_checked`** (endomul_scalar) → `endoMulScalar` gate `[read]` `step_verifier.ml:87-97` | endomul_scalar BODY (11) `[exists]` `KimchiVerify.lean` | **CHECKER** | emit the `endoMulScalar` gate row + witnessed ring quotients |
| **`lowest_128_bits`/`assert_n_bits`** range checks `[read]` `step_verifier.ml:89-101` | bit-decomposition range checks `[exists]` `AirBuilder.lean` (generic-gate) | **partial synth** (generic) | match o1js's exact bit-split gate sequence |
| **`group_map`** (hash-to-curve) `[read]` `step_verifier.ml:214-237` | — | **NONE** | new emitter (Bw19 map) |
| **`check_bulletproof`** IPA group eq `[read]` `step_verifier.ml:247-340`, `wrap_verifier.ml:383-437` | K4c IPA identity + `msm==0` `[exists]` `PastaIPA.lean`, P1 `[exists]` `PicklesRecursion.lean:§P1` | **CHECKER** | emit the MSM/curve-op gate sequence |
| **`multiscale_known`/`public_input_commitment_dynamic`** (public-input MSM) `[read]` `step_verifier.ml:125-184,378-496` | Lagrange-MSM layout (consumer side) `[exists]` `MinaWrapPublicInput.lean` | **CHECKER** (consumer) | emit the producer-side MSM gates |
| **`ft_comm`/`combined_evaluation`** `[read]` `common.ml:238-282` | `ftEval0R`/`cipR` `[exists]` `KimchiVerify.lean`, `MinaWrapFtEval0.lean` | **CHECKER** | in-circuit-ify (mostly generic gates) |

**The pattern, stated once:** for EVERY circuit piece dregg has the verifier MATH and lacks the gate
EMITTER. The math is the correctness SPEC (Phase B); the emitter is Phase A. The one structural fact
that sizes Phase A: **dregg emits NO custom-gate rows anywhere** (§2.3).

### §2.3 — ⚑ The structural gap: dregg emits ONLY generic gates

`[read]` `KimchiTarget.lean:286-303`: `KRow.holds` models `zero`/`generic`/`rangeCheck0` only; every
other gate is `False` (fail-closed). `[read]` `KimchiDag.lean` header + `:2381` and `KimchiLower.lean`:
dregg's kimchi circuits are emitted by lowering a DAG/`Head` to **generic gates** (`packGen`/`dagGens`,
one `Gen1` per node). Even dregg's "native Pasta hash" (Route B) `[read]` `KimchiCellCommit.lean:1-15`
lowers Poseidon2 to generic gates via `KimchiPoseidon2.permGen` — **it does not emit a `poseidon`
custom gate row.** Grep for any `{ gate := .poseidon/.completeAdd/.varBaseMul/.endoMul }` constructor
returns **zero**. So the poseidon + four curve custom-gate emitters, the Snarky-style builder that
places cells and threads the copy-permutation, and the `internal_vars` model are all **new
foundations**, not extensions.

### §2.4 — The `Bridge/Mina*` assets (a related but DIFFERENT effort)

`[exists]` `Dregg2/Bridge/MinaWrap{Challenges,Deferred,FtEval0,PublicInput,Sg}.lean` are the **real
Mina light client** — they DERIVE a real Mina Wrap proof's challenges/deferred-values/public-input
from wire bytes and check the opening `[read]` `MinaWrapDeferred.lean:1-20`, `MinaWrapPublicInput.lean`
census. They instantiate `KimchiVerify`'s `[CommRing R]` bodies at `pN`/`qN` and add the wire grammar.
For SYNTHESIS they are **consumer-side references** (they show the byte layout the synthesizer must
PRODUCE — e.g. the 40-word Wrap public-input order `[read]` `MinaWrapPublicInput.lean` census), but
none emits a gate. They save Phase A the layout SPEC, not the emitter.

---

## §3 — The protocol scaffolding (byte-exact; the Phase-A crux)

`[read]` all of `composition_types/`. The step/wrap STATEMENT is a nested record whose flat packing IS
the public input; a byte off here means the VK is self-consistent but the proof will not chain. Full
field enumeration (`composition_types.ml`, `basename:line`):

### §3.1 — Wrap statement (native Fq; carries the STEP proof's Fp scalars)
- `Wrap.Statement.t` `:677-706` = `{ proof_state; messages_for_next_step_proof }`.
- `Wrap.Proof_state.t` `:427-460` = `{ deferred_values; sponge_digest_before_evaluations;
  messages_for_next_wrap_proof }`.
- `Wrap.…Deferred_values.t` `:211-248` = `{ plonk; combined_inner_product:'fp; b:'fp; xi; bulletproof_challenges;
  branch_data }` (**6 fields, incl. branch_data**).
- `Plonk.In_circuit.t` `:102-123` = `{ alpha; beta; gamma; zeta; zeta_to_srs_length:'fp;
  zeta_to_domain_size:'fp; perm:'fp; feature_flags; lookup }` (**9 fields**); `Plonk.Minimal.t` `:45-61`
  (6, adds `joint_combiner`).
- `Messages_for_next_wrap_proof.t` `:397-407` (the wrap "me-only") = `{ challenge_polynomial_commitment;
  old_bulletproof_challenges }`.
- `Messages_for_next_step_proof.t` `:566-577` (the step "me-only", received) = `{ app_state;
  dlog_plonk_index (the wrap VK evals: sigma_comm×7, coefficients_comm×15, generic/psm/complete_add/
  mul/emul/endomul_scalar `[read]` `plonk_verification_key_evals.ml:7-16`); challenge_polynomial_commitments;
  old_bulletproof_challenges }`.

### §3.2 — Step statement (native Fp; carries the WRAP proof's Fq scalars)
- `Step.Statement.t` `:1414-1425` = `{ proof_state; messages_for_next_wrap_proof }`.
- `Step.Proof_state.t` `:1362-1370` = `{ unfinalized_proofs (vector of Per_proof);
  messages_for_next_step_proof }`.
- `Step.…Per_proof.t_` `:1205-1226` = `{ deferred_values; should_finalize; sponge_digest_before_evaluations }`.
- `Step.…Deferred_values.t_` `:1152-1165` = `{ plonk; combined_inner_product:'fq; xi;
  bulletproof_challenges; b:'fq }` (**5 fields, NO branch_data, different order from Wrap**).
- `Step.Plonk.In_circuit.t` `:1019-1031` (**7 fields**, no feature_flags/lookup); `.Minimal.t` `:986-991` (4).

### §3.3 — The packing / "typ" layer (`spec.ml`) — the byte-exactness engine
`[read]` `spec.ml:9-76` (`basic` + `T` GADTs), `:360-401` (`pack_basic`: `Bool→Packed_bits(_,1)`,
`Digest→Packed_bits(_,255)`, `Challenge→Packed_bits(_,128)`, `Branch_data→Packed_bits(_,10)`,
`Bulletproof_challenge→Packed_bits(_,128)`). The CONCRETE layouts fix the byte order:
- Wrap flat spec + `to_data` `[read]` `composition_types.ml:785-882`: `fp=[cip;b;zeta_to_srs_length;
  zeta_to_domain_size;perm]`, `challenge=[beta;gamma]`, `scalar_challenge=[alpha;zeta;xi]`,
  `digest=[sponge_digest; messages_for_next_wrap_proof; messages_for_next_step_proof]`, `[branch_data]`,
  8 feature flags, lookup-opt.
- Step per-proof flat spec `[read]` `:1268-1318`; step statement flat spec `[read]` `:1453-1459`.
- `branch_data` packs `{2-bit proofs_verified PREFIX-MASK, 8-bit domain_log2}` into one field
  `[read]` `branch_data.ml:45-74,135-136` (⚠ prefix mask `[read]` `proofs_verified.ml:75-81`, NOT the
  3-bit one-hot).

### §3.4 — What dregg already models of the scaffolding (P2)
`[exists]` `PicklesRecursion.lean:1017-1165`: `PlonkMinimal`, `PlonkInCircuit`, `WrapDeferredValues`,
`StepDeferredValues` records; the `Shifted_value` Type1/Type2 bridge (`type1_eq_iff`/`type2_eq_iff`,
`pasta_shift_constants` `2^255+1`/`2^255`); `branchDataPack`/`Unpack` + `branchData_roundtrip`;
`wrapData_roundtrip`. **This is the DATA model** — the records and the shifted-value algebra.
**What is NOT modelled:** the `spec.ml` `pack`/`typ` byte-layout (the `Packed_bits` widths, the flat
field ORDER, the digest-hashing of the two me-only fields), and the `internal_vars` witness mapping.
dregg's P2 is ~40% of §3: the records and shifts, not the packing.

---

## §4 — Byte-exactness: what "exactly right" means, and the o1js-diff oracle

**"Exactly right" (Phase A) = the emitted `*_gates.json` is byte-identical to o1js's for the same
inductive rule, AND the co-emitted `*_internal_vars.bin`/`*_rows_rev` let mina-rust's witness
generator produce a witness that satisfies those gates and verifies against the VK proof-systems
derives from them.** dregg's OWN VK (not Mina's tx VK) is the target; the requirement is
compile→VK→prove internal consistency, established DIFFERENTIALLY (not from first principles).

**The oracle — cheapest path, zero build** `[read]` o1js-oracle sub-read:
- o1js is prebuilt at `~/dev/o1js` and `~/dev/breadstuffs/bridge/mina-zkapp/node_modules/o1js`.
  `Provable.constraintSystem(f)` `[read]` `o1js/src/lib/provable-context.ts:91-123` returns
  `{ gates, digest, rows, publicInputSize, json }`. `.digest` is a single scalar that flips on ANY
  gate-list divergence (it equals kimchi's `Circuit.digest()`). `Snarky.run.constraintSystem(f).json`
  `[read]` `snarky.d.ts:213-217` is the byte-identical raw `{typ, wires, coeffs(hex)}` form.
- **The harness is ~90% built.** `bridge/mina-zkapp/scripts/*.ts` already call
  `Provable.constraintSystem(f)` against the local o1js, but every one reads only `cs.rows` `[read]`
  o1js-oracle sub-read (e.g. `root-air-rows.ts:208`). **Flipping `.rows` → `.gates`/`.digest` mints the
  goldens** — a one-line change per script.
- ⚠ **Nuance for the RECURSION circuit.** `Provable.constraintSystem(f)` dumps a PLAIN circuit's
  gates — perfect for R1 (a trivial circuit) and the app circuit. The full STEP circuit is
  `ZkProgram.analyzeMethods()` (step = app logic + recursion verify); the **WRAP** circuit's gate
  list is internal to `Pickles.compile` and may need a small o1js/Pickles binding tweak to dump. This
  is a named R4 sub-task, not a blocker (the step circuit — the bulk — is analyzable today).

**Why the oracle is sound as a fidelity check (and its limit).** A byte-identical `*_gates.json`
means the Lean synthesizer produced the SAME circuit o1js does, for that input. It does NOT prove the
circuit is a correct Pickles verifier from first principles — that is Phase B. It proves FIDELITY to
the one reference implementation that is known-correct-in-practice, which is exactly the standard
openmina holds itself to (`ocaml_aux`), only stricter (byte-diff vs replay). The oracle can only test
the inputs you run it on; a divergence on an unrun feature (e.g. a gate a bigger circuit triggers) is
invisible until run — so the plan diffs one representative circuit per gadget family (§5), the same
discipline `MINA-GATE-TIERS.md` uses.

---

## §5 — PHASE A: the rung ladder (formalization; each rung a falsifiable gate)

Adjusted from the brief's R1–R5 "to what the read reveals": the brief's R1 (in-circuit-verify body)
depends on a Snarky-style builder + the custom-gate emitters that **do not exist yet** (§2.3), so those
become R1/R2; the scaffolding (brief R2) is pulled EARLY as its own rung because it is the epoch-killer
(§8). Phase A = R1–R4 = **a Lean-synthesized Pickles proof that verifies.**

### R1 — The builder + placement + the generic-gate diff (foundation + oracle)
Author the Lean **Snarky-style circuit builder**: internal-variable allocation, gate accumulation,
**cell placement (row,col)**, and the copy-constraint permutation — plus the `KRow → JsonGate` render
(row/col placement + hex coeffs, the adapter §1 names missing) and the `internal_vars` model. Flip one
bridge script `.rows`→`.gates`/`.digest`.
- **Gate:** for a TRIVIAL all-generic circuit (`x + y == 8`, o1js example `[read]`
  `o1js/src/examples/constraint-system.ts`), Lean's emitted `*_gates.json` **byte-diffs clean against
  o1js's `.json`, and the digests match**; a deliberately mis-placed cell flips the digest. Falsifiable:
  wrong placement ⇒ digest mismatch, RED.
- **Reuses:** `KRow`/`KimchiLower` generic-gate model `[exists]`; `KimchiPartition` placement remainder.
- **New:** the builder monad, placement, permutation, `internal_vars`, the JSON render.

#### R1 — EARLIEST FALSIFIABLE SIGNAL, MEASURED 2026-08-01 (the placement pass EMITS byte-exact `{row,col}` wires)
`[exists]` `metatheory/Dregg2/Circuit/Emit/KimchiPlacement.lean` (built in-graph through lake, 3033
jobs; `#assert_axioms` ⊆ {propext, Classical.choice, Quot.sound}; no `sorry`/`native_decide`) +
`bridge/mina-zkapp/scripts/pickles-placement-oracle.mjs` (green-or-bust, exit 0). The NEW FOUNDATION
`KimchiCustomGates` named as the ONE structural divergence — the Snarky-style **cell placement +
copy-permutation** — is BUILT and byte-diffed against o1js 2.15.0 `Provable.constraintSystem(f).gates[i].wires`:
- **`place` is authored in Lean, transcribed from `plonk_constraint_system.ml`** (o1js's Snarky backend,
  READ-ONLY oracle) `[read]` `:917-943` (`equivalence_classes_to_hashtbl`: SORT each class by `(row,col)`,
  ROTATE-LEFT, cell → next-in-cycle), `:1131-1150` (`add_row` wires cols 0..6), `:1156-1253`
  (`finalize_and_get_gates`: public rows first, `wired_to = init 7 (fun col => permutation {row;col})`,
  unwired cells self-wire).
- **`wires` MATCHES, byte-exact — the divergence is CLOSED.** `caseB_wires_match_o1js` (`by decide`):
  Lean `place` of the smallest cross-row COPY — `x` witnessed once, used in two separate gate rows, a
  3-cell equivalence class `{(0,3),(0,4),(1,3)}` spanning two rows — reproduces o1js's exact sigma
  wiring `(0,4)↦(1,3)↦(0,3)↦(0,4)` cell-for-cell, all 14 cells over 2 gates. `caseA_wires_match_o1js`
  (x+y===8, o1js example) pins the base case. The oracle re-confirms live o1js == the Lean goldens.
- **Falsifiable and it BITES:** `caseB_misplaced_diverges` / `caseB_noCopy_diverges` (`by decide`) — a
  mis-placed input cell, or dropping the copy, makes the wires ≠ o1js's; the adversarial oracle run
  confirms a corrupted golden exits non-zero. `caseB_sigma_is_permutation` (`by decide`): σ is a
  BIJECTION on the circuit's cells (sorted emitted wires = sorted full cell set) — the PLONK copy-arg
  soundness shape, concretely.
- ⚑ **R3's `internal_vars` residual — MODEL half CLOSED.** `InternalVarDef`
  (`{terms : List (Int × PVar), const : Option Int}`) and `PGate.permVars` ARE the mina-rust
  circuit-blobs shapes `*_internal_vars.bin` (`HashMap<u32,(Vec<(BigInt,VRaw)>,Option<BigInt>)>`) and
  `*_rows_rev.bin` (`Vec<Vec<Option<VRaw>>>`) `[read]` `mina-rust crates/ledger/src/proofs/provers.rs:284-293`,
  consumed by `compute_witness` `[read]` `transaction.rs:3822-3866`. So the "no dregg model" half R3
  named is closed. **Sub-residual, NAMED:** o1js's `constraintSystem` JSON does NOT expose internal_vars,
  so its BYTE-diff oracle is the circuit-blobs binprot dump, not zero-build. The PLACEMENT (`wires`) IS
  zero-build-diffable and is the gate above.
- **Cost, REVISED (R4 proximity).** The placement algorithm is now a proven, byte-exact Lean object;
  extending case B's 2 generic rows to the completeAdd/poseidon rows the sibling emitted (`KimchiCustomGates`)
  is the SAME pass fed those gates' `permVars` — placement adds no rows and is gate-agnostic, so the
  custom-gate rows' `wires` become diffable the moment their var-in-cell grids are supplied (an R2/witness
  question, not new placement work). R4 (VK + a verifying proof) needs: the JSON render (`KRow`/`PlacedGate`
  → `*_gates.json` with hex coeffs, mechanical), the internal_vars/rows_rev binprot emit (shapes now
  modelled), and the body assembly. **R1's two live unknowns from §8 — placement fidelity (risk #2) — is
  RETIRED for the generic family and the mechanism is validated;** the remaining R4 variance is body
  emission surface, not the copy-permutation. Phase-A estimate unchanged (R1 3–5; the placement mechanism
  de-risks toward the low end, like R2/R3's packing halves).
- **Honest label:** *Lean's placement pass emits `{row,col}` wires byte-identical to o1js's for a fixed
  copy circuit (MEASURED)* — FIDELITY, NOT "machine-checked Pickles"; asserts nothing about recursion
  soundness.

### R2 — The custom-gate emitter library (the gadgets)
Emit `poseidon` (11 rows/perm), `completeAdd`, `varBaseMul`, `endoMul`, `endoMulScalar`, `group_map`,
and the `lowest_128_bits`/`assert_n_bits` range gadgets — each a Lean emitter over the K1–K4c math.
- **Gate:** for one representative circuit per gadget (a poseidon-only circuit, a scale_fast circuit,
  …), Lean's `*_gates.json` byte-diffs clean against o1js's, per gadget family. A bent round-constant
  or a swapped wire flips the digest.
- **Reuses:** the gate BODIES + Pasta math `[exists]` `KimchiVerify.lean §9c.1`, `PastaPoseidon`,
  `PastaCurveComplete`, `PastaScalarMul` — the emitters are checked FOR FREE against the bodies dregg
  already transcribed (a body a satisfying witness must meet).
- **New:** every emitter's exact row/wire layout, matched to o1js.

#### R2 — EARLIEST FALSIFIABLE SIGNAL, MEASURED 2026-08-01 (the FIRST Lean-emitted custom-gate rows)
`[exists]` `metatheory/Dregg2/Circuit/Emit/KimchiCustomGates.lean` (kernel-clean, `#assert_axioms` ⊆
{propext, choice, Quot.sound}; no `sorry`/`native_decide`) + `bridge/mina-zkapp/scripts/custom-gate-diff.mjs`
(exit 0). The two smallest custom gates were emitted from Lean (`mkCompleteAdd`, `mkPoseidonRow`) and
byte-diffed against o1js 2.15.0 `Provable.constraintSystem(f).gates`:
- **`typ` MATCHES.** `KGateType.completeAdd`/`poseidon` ordinals `3`/`2` (`gate.rs` `#[repr(C)]`) ==
  o1js `GateType` `'CompleteAdd'`/`'Poseidon'` (`mkCompleteAdd_ordinal`, `mkPoseidonRow_ordinal`, `rfl`).
- **`coeffs` MATCHES, byte-exact.** `complete_add` emits `[]` == o1js `[]`. `poseidon` row `j` emits
  `rcsN[5j..5j+4]` flattened (round-major, lane-minor) — **all 11 rows / 165 coefficients byte-exact**
  against o1js's emitted `coeffs` (checked programmatically, o1js OCaml/wasm bindings vs `PastaPoseidon.rcsN`
  from the pinned `proof-systems` crate — two INDEPENDENT sources). **No emission-side permutation:** the
  `STATE_ORDER = [0,2,3,4,1]` shuffle is a VERIFIER-side READ in `KimchiVerify.poseidonConstraints`, NOT a
  reorder of the emitted row. `poseidonRow0_matches_o1js` (`decide +kernel`) pins row 0; **red-proved** —
  a one-digit corruption of any coefficient makes `decide` fail.
- **`wires` DIVERGES, structurally and by design** — the ONE divergence. o1js emits `PERMUTS = 7`
  placement cells `{row,col}` (the wiring pass' copy targets); a `KRow` holds VARIABLE INDICES for the
  columns the gate constrains (11 for `completeAdd`), PRE-placement. The map var-index → placed `{row,col}`
  is `KimchiPartition`'s named remainder — **an R1 (builder/placement) obligation, not an R2 defect.**
- **Cost, REVISED (the byte target for the five is smaller than R2 assumed):** of the five custom gates,
  **only `poseidon` carries coefficients** — `complete_add`, `varBaseMul`, `endoMul`, `endoMulScalar` all
  emit `coeffs = []` (checker headers `KimchiVerify.lean:§9c.1`: "no coefficients"). So the coeff-fidelity
  half of R2 is (a) DONE and byte-exact for the only coeff-bearing gate, and (b) trivially `[]` for the
  other four. **R2's residual is therefore NOT coeff bytes** but: the multi-row LAYOUT of the accumulating
  gates (`varBaseMul`/`endoMul` span several rows with `wNext` reads) and the WIRE placement — the latter
  shared with R1. The four coeff-free gates are the same emitter shape as `complete_add` (typ + `[]` + the
  column variables), differing only in which columns and row count. **R2 de-risks toward the low end**
  (like R3's packing half); the variance now lives in R1's placement pass, which is where `wires` becomes
  byte-comparable. Phase-A total estimate unchanged.
- **Reuses:** `KRow` `[exists]` `KimchiTarget.lean`; `completeAddConstraints`/`poseidonConstraints`
  `[exists]` `KimchiVerify.lean §9c.1` (the emitted `complete_add` row's satisfaction answers to the
  imported checker: `mkCompleteAdd_checked`); `rcsN` `[exists]` `PastaPoseidon.lean`.
- ⚠ **Phase A, NOT soundness.** This is byte-differential fidelity of the row (typ+coeffs) — NOT a proof
  that the emitted gate is sound and NOT "machine-checked Pickles" (Phase B). `wires` is un-diffed until R1.

### R3 — ⚑ The byte-exact statement scaffolding (THE EPOCH-KILLER, §8)
Emit the `spec.ml` packing (§3.3): the flat field order, the `Packed_bits` widths, the two me-only
DIGEST fields, `branch_data`, the deferred-values + Per_proof + Step/Wrap statement public-input
layout — AND the corresponding `internal_vars`. Build on P2's data model `[exists]`.
- **Gate:** Lean's public-input packing of a fixed Step statement and a fixed Wrap statement
  **byte-matches o1js's `Spec.pack` output** (dump o1js's packed public input for the same record;
  diff). A permuted field or a wrong `Packed_bits` width RED. This is the rung whose failure means the
  proof will not chain at R4.
- **Reuses:** P2 records + Type1/Type2 + `branchData_roundtrip` `[exists]`; the consumer-side layout
  `[exists]` `MinaWrapPublicInput.lean` census (the SAME 40 words, from the other side).
- **New:** the producer-side packing + `internal_vars` for the statement I/O.

#### R3 — EARLIEST FALSIFIABLE SIGNAL, MEASURED 2026-07-31 (the packing crux CLEARS)
`[exists]` `metatheory/Dregg2/Bridge/PicklesR3BranchDataDiff.lean` (kernel-clean, no axioms) +
`bridge/mina-zkapp/scripts/pickles-r3-branchdata-oracle.ts` (exit 0). The smallest scaffolding piece
that requires real *packing* logic — `branch_data`, the ONE Wrap-statement field whose packed value
differs from its raw inputs — was emitted from Lean and DIFFED byte-exact against the real chain:
- **Lean emit** `branchDataPack {proofs_verified = N2, domain_log2 = 16} = 67` (via `prefix_mask`
  + LSB-first `project` + `4·domain_log2`, authored from reading `branch_data.ml:63-74`).
- **Oracle** `wrap_public_input[29] = 67` of devnet block 539508 (`mina_real_block_proof.json`) —
  openmina's own `PreparedStatement::to_public_input`, which **o1-labs' `kimchi::verifier::verify`
  ACCEPTS** (extractor ground-truth #3, so slot 29 is validated not transcribed). o1js is js_of_ocaml
  of the same Pickles (`o1js_node.bc.cjs` carries `branch_data`/`Prefix_mask`/`proofs_verified` —
  79/1/266 hits), so `67` is an emission of exactly the `Branch_data.pack` o1js runs.
- **MATCH, byte-exact.** FOUR-way agreement: deployed OCaml chain · openmina Rust · kimchi acceptance
  · Lean synthesis. **Falsifiable and it bites:** a naive tag-not-mask packer emits `66` (`prefix_mask
  N2 = 3`, not `to_int N2 = 2`, is load-bearing); the gate goes RED on exactly that mistake.
- **Verdict — the packing half of R3 is TRACTABLE, not blocked.** The crux field is byte-confirmed
  against a live consensus artifact, and `MinaWrapPublicInput.lean` already pins the full 40-word flat
  ORDER (slots 13–28 = the 16 bulletproof challenges = the block's `step_prechallenges_raw`, verified
  here; padding 30–39 = 0) against the same block. **Residual, unmoved:** `internal_vars` (the witness
  mapping) has no dregg model and NO zero-build oracle at this rung — the real block gives packed
  VALUES, not the witness recipe; `internal_vars` is emitted BY the R1 builder, so it is tested when
  R1 exists. R3 does NOT kill the epoch on the packing evidence; the `internal_vars` residual is real,
  bounded, and coupled to R1. Phase-A cost estimate UNCHANGED (R3 2–4; the packing sub-half is now
  de-risked toward the low end, `internal_vars` still carries the variance).

#### R3 — FULL Wrap-STATEMENT diff, MEASURED 2026-08-01 (the whole packing, not one field)
`[exists]` `metatheory/Dregg2/Bridge/PicklesStatementDiff.lean` (kernel-clean, `[propext, Quot.sound]`,
built through the full lake graph) + `bridge/mina-zkapp/scripts/pickles-statement-oracle.ts` (exit 0).
Extends the R3 branch_data signal from ONE Wrap-statement field to the WHOLE flat statement, diffed
byte-exact against the SAME kimchi-verified devnet block 539508. The map, by slot:
- **The two NON-identity packings, both now emit-and-diffed from Lean:**
  - **slots 0–4, the `fp` block** `[cip, b, zeta_to_srs_length, zeta_to_domain_size, perm]` — carried as
    `Shifted_value.Type1` over **Fp**. `FP_UNSHIFTED.map (type1OfField shift1Fp) = FP_WIRE` **by decide**:
    all five reproduce `wrap_public_input[0..4]` from the block's own decoded UNSHIFTED
    `step_deferred_values` (a second, independent decoded field). ⚑ **Value-field finding:** the shift
    is over **Fp** (the deferred VALUE's field), not the Wrap circuit's native **Fq** — Type1-over-Fq
    diverges on the real data (oracle-checked), so a port that keyed the shift on the circuit's native
    field would misencode all five. The `·2⁻¹` halving and the shift are load-bearing (identity and a
    subtract-only Type2-style pack both REJECTED).
  - **slot 29, branch_data** — the prefix-mask pack, re-affirmed (`= 67`; naive tag emits `66`).
- **The identity remainder, ORDER-pinned against the DECODED record (not a projection):** slots 5–9 =
  `{beta, gamma, alpha, zeta, xi}` in `to_data` order (`composition_types.ml:863-864`, the 07-30
  correction confirmed on real bytes); slot 10 = `sponge_digest_before_evaluations`; slots 13–28 = the
  16 `bulletproof_challenges`; slots 30–39 = 0. The oracle diffs each against `step_deferred_values`,
  a DIFFERENT json path than `wrap_public_input`, so the layout is validated not projected.
- **Capstone (`lean_wrap_to_data_reproduces_wire_head`, decide):** Lean's `wrapFpBlock` (Type1-shifted)
  `++ wrapChallengeBlock ++ wrapScalarChallengeBlock ++ [sponge]` of the decoded record reproduces
  `wrap_public_input[0..10]` byte-exact, and `branchDataPack` reproduces slot 29 — the field ORDER and
  BOTH packings tied to the live block in one object. Oracle reconstructs all 40 words from the decoded
  fields and matches.
- **Coverage: 38/40 Wrap slots diffed byte-exact against decoded fields (PACK 6 · identity 22 · const
  10); 2/40 (me-only digests, slots 11–12) CITED to `MinaWrapPublicInput.word11Gold/word12Gold`
  (Poseidon digests, not recomputed here). NO divergence found** — the full Wrap statement layout is
  byte-confirmed from Lean.
- **Two honest residuals, named not chased:** (1) `internal_vars` UNMOVED (R1's, as above). (2) The
  **Step** statement has NO independent public-input oracle in this fixture — the block is a Wrap-proof
  decode, and its `step_deferred_values` IS the Wrap statement's payload ABOUT the step proof, not a
  step proof's own 40-word public input. The Step per-proof layout is ENUMERATED
  (`composition_types.ml:1268-1318`: `fq(5)+digest(1)+challenge(2)+scalar(3)+bp(15)+bool(1)`, a
  DIFFERENT order — no branch_data, digest early, trailing `should_finalize`, `fq` block Type2/Fq by the
  value-field rule) and handed to a step-proof-decode lane. (3) ⚑ **Layout gap found:** Lean's
  `wrapStatementLayout` sums to `WPUB = 38`; the wire is **40** — the 2-word delta is the
  `Lookup_parameters.opt_spec` block (slots 38–39, both 0 with no lookup), which the Lean layout
  under-counts. Recorded so a Step/Wrap assembler does not size the public input at 38.

#### R3 — STEP-statement layout, ENUMERATED + MODELED + diffed vs o1js/openmina, 2026-08-01
`[exists]` `metatheory/Dregg2/Bridge/PicklesStepStatementDiff.lean` (kernel-clean, `[propext,
Quot.sound]`, elaborates via `lake env lean` against HEAD-clean `PicklesRecursion` oleans; every
keystone `#assert_axioms`-gated) + `bridge/mina-zkapp/scripts/pickles-step-statement-oracle.ts` (exit 0,
green-or-bust). Closes the Step hand-off the Wrap lane named above. ⚑ **The Step statement has NO wire
in this fixture** (it is a Wrap-proof decode — the oracle MEASURES this: `combined_inner_product_shifted
== Type1/Fp(cip)`, not Type2/Fq), so this is a LAYOUT/ORDER + FIELD-KEY result, not a byte-diff of
packed values against the chain.
- **The 27-slot per-proof layout, from TWO independent implementations that AGREE:** OCaml
  `composition_types.ml:1298-1318` `to_data` == openmina Rust `unfinalized.rs:397-434`
  `Unfinalized::to_field_elements`, block-for-block: `fq(5) · digest(1) · challenge(2) ·
  scalar_challenge(3) · bulletproof(15) · bool(1)`. `stepToDataFlat_positions` (decide, axiom-free) lays
  distinct sentinels `0..26` at exactly those offsets. bp = `Backend.Tock.Rounds.n = 15` (tied to
  `deferredRounds .step`, not a bare literal): a Step proof defers a WRAP proof's 15 IPA rounds — mirror
  of the Wrap statement's 16 (= Step rounds).
- **Step ≠ Wrap layout** (`step_and_wrap_layouts_differ`, decide): digest EARLY (block #2, right after
  the field block) vs LATE (Wrap #4); a trailing `should_finalize` bool (Wrap has none); NO `branch_data`
  (Wrap carries it); field block `fq` vs `fp`; bp 15 vs 16. A step assembler cannot reuse the Wrap order.
- **FIELD-KEY `fq = Type2/Fq` — SOURCE-DEFINITIVE** (the mirror of Wrap's Type1/Fp): OCaml `impls.ml:135`
  binds the Step statement `typ`'s `Field` spec leaf to `Shifted_value.Type2.typ Other_field`
  (Other_field = Tock = **Fq**); openmina `unfinalized.rs:105` types `combined_inner_product` as `<Fq as
  FieldWitness>::Shifting = ShiftedValue<Fq>` = Type2 (`plonk_checks.rs:113`, subtract-only `s − 2^255`,
  no halving). ⚠ This is NOT `PicklesRecursion.shiftKindFor` (which keys the IN-CIRCUIT verify on the
  NATIVE field: `.step ↦ Type1`); the STATEMENT deferred block keys on the deferred VALUE's field
  (Fq → Type2), exactly `F::Shifting`.
- **The diverging control** (`step_fq_key_is_observable`, decide): on a concrete Fq scalar, identity /
  Type1-over-Fq (wrong KIND) / Type2-over-Fq are pairwise distinct — so a wrong key WOULD diverge if a
  wire existed, the mirror of Wrap's `fp_shift_is_load_bearing`. The wrong-FIELD control (Type2/Fp vs
  Type2/Fq) is the oracle's cross-modulus bigint check. A **two-sided TS↔Lean pin**
  (`step_fq_sample_shift_pins_oracle`) fixes `type2OfField shift2Fq SAMPLE_FQ` to the exact digits the
  oracle computes with `(s − 2^255) mod q` — kernel `ZMod qN` == bigint, a byte-diff not an eyeball.
- **o1js carries the path** (oracle, green-or-bust): `o1js_node.bc.cjs` hits `should_finalize` 6,
  `shifted_value` 68, `unfinalized_proofs` 36, `per_proof` 7, `composition_types` 194 — same emitter.
- **Residual, named + MEASURED (not chased):** (1) no step wire here → packed VALUES not byte-diffed;
  closed by a decoded STEP statement (a step proof's `unfinalized_proofs.(i)` per-proof public input,
  openmina `Unfinalized`, giving real Fq `cip/b/perm` with their Type2/Fq `.shifted` forms — the analog
  of the Wrap `_shifted` pairing this fixture has for the Fp block). (2) ⚑ **New Step-specific subtlety:
  the Fq→Fp slot embedding.** Unlike Wrap (p<q, so a Type1/Fp value drops into an Fq slot directly), the
  Step statement is Fp-native but its fq scalars are Fq with **q>p** — Type2 EXISTS for exactly this
  larger-scalar-field case (`shifted_value.ml:140-150`); the exact embedding into Fp slots is a
  value-level detail modeled here only as the subtract-only shift over Fq. **Honest label:** *the Step
  per-proof layout ORDER + the `fq=Type2/Fq` field-key are confirmed from Lean against two agreeing
  implementations (MEASURED); packed values await a step wire* — FIDELITY, NOT "machine-checked Pickles".

### R4 — The circuit bodies + VK + a proof that VERIFIES
Assemble R1–R3 into the step and wrap circuit bodies: emit `incrementally_verify_proof` +
`finalize_other_proof` + me-only hashing + `step_main`/`wrap_main` orchestration `[read]`
`step_main.ml:276-581`, `wrap_main.ml:135-441`. Derive the VK via proof-systems from the emitted
gates. Prove via mina-rust (fed Lean's circuit-blobs).
- **Gate:** ① Lean's full step `*_gates.json` byte-diffs clean against o1js's `analyzeMethods` for a
  trivial rule; ② proof-systems derives a VK; ③ **mina-rust produces a proof that
  `kimchi::verifier::verify` (and the light-client) ACCEPTS**; ④ a tamper (bend one emitted coeff) is
  REJECTED. This is the "first independent Lean-synthesized Pickles proof that verifies" milestone.
- **Reuses:** the in-circuit-verify MATH `[exists]` `KimchiVerify`/`PicklesFinalize`; the SRS `[exists]`
  `MinaWrapSrsG.lean`; mina-rust prover UNCHANGED; the o1js `Pickles.compile` reference.
- **New:** the body emission (the largest single surface), the VK plumbing, the blob hand-off, the
  wrap-circuit gate-dump binding tweak (§4 nuance).

---

## §6 — PHASE B (OPTIONAL, EPIC): the recursion soundness theorem (R5)

*A verifying wrapped proof ⟹ the wrapped statement holds.* NOT a prerequisite for Phase A; costed
separately. This is the "first MACHINE-CHECKED Pickles" leg and possibly the deepest formal-methods
work in the project.

### R5 — Two sub-theorems
- **R5a — synthesis FIDELITY as a THEOREM (not just a diff):** the emitted step/wrap gates REFINE the
  `KimchiVerify` decision — "the gates accept iff the kimchi verifier accepts." dregg's checker
  (`kimchiVerifyDecision`, all six bodies, ft_eval0, cip) is the SPEC; this proves the R1–R4 emitter
  meets it, over the ACTUAL emitted rows (the `dagGens_forces`/`lowerHead_sound` discipline `[exists]`
  `KimchiDag.lean`, `KimchiLower.lean`, scaled to custom gates). Upgrades the o1js diff from evidence to
  proof.
- **R5b — recursion soundness:** reuses the verifier-side P0–P6 + the P4 transcript-equality binding
  `[exists]` `PicklesRecursion.lean` (P0–P2), `KimchiRecursionGate.lean` (P6), `PicklesFinalize.lean`
  (P3), `PicklesTranscriptBinding.lean` (P4: `endoMap` injectivity PROVEN, digest binding priced to a
  ROM residual). ⚑ **Rests on the IPA/FRI opening-soundness floor (P10) AS AN ASSUMPTION** — exactly as
  Mina, openmina, and every implementation do (`PICKLES-VERIFIER-SCOPE.md:479-481`). Phase B does NOT
  re-prove IPA; it connects the emitted synthesis to the (already substantially built) verifier-side
  soundness ladder.

The verifier-side ladder (P0–P6, P4) is the reason R5 is "epic but not from zero": the soundness SPEC
and its hardest sub-lemma (P4) are `[exists]` on disk. What is new in R5b is binding the SYNTHESIZED
object (the emitted circuit) to that verifier ladder — R5a is the bridge.

---

## §7 — Swarmcycle estimates `[ASSUMED]`

Reference class: the wgpu prover port = 3–4 swarmcycles; the K1–K5 + P0–P6 verifier ladder (a
comparable arithmetic surface) was built over ~1 intense week of multi-lane work. These numbers are my
estimate, not measured, and the range reflects the two live unknowns (§8).

### Phase A — to the first Lean-synthesized proof that VERIFIES (R1–R4): **~13–20 swarmcycles**
| rung | estimate | driver |
|---|---|---|
| R1 builder + placement + generic diff | 3–5 | the Snarky builder + permutation + `internal_vars` are new foundations |
| R2 custom-gate emitter library | 3–4 | 5 custom gates + range/group_map, each o1js-diffed; math is HAVE |
| R3 byte-exact scaffolding | 2–4 | P2 gives records+shifts; packing + `internal_vars` are new |
| R4 bodies + VK + verifying proof | 4–6 | largest emission surface; VK/blob plumbing; wrap-dump tweak |

**What dregg's existing verifier logic saves vs a true from-scratch port:** the ENTIRE arithmetic floor
(K1–K4c, both fields/curves) `[exists]`; all six gate constraint BODIES as the free correctness check
on the R2 emitters `[exists]` `KimchiVerify §9c.1`; the P2 statement records + shifted-value bridge for
R3 `[exists]`; the in-circuit-verify MATH as the R4 spec `[exists]`; the SRS + VK-evals model
`[exists]`; the o1js differential harness ~90% wired `[exists]` `bridge/mina-zkapp/scripts`; and the
prover REUSED wholesale (mina-rust consumes Lean's blobs, §0). **A from-scratch port would additionally
carry the Pasta field/curve/poseidon/IPA library and the prover — my rough `[ASSUMED]` sense is dregg's
assets cut Phase A by roughly half** versus starting from o1js with no Lean crypto floor.

### Phase B — to the machine-checked version (R5, on top of A): **~8–15+ swarmcycles, research-grade, HIGH variance**
R5a (fidelity theorem) ~3–6; R5b (recursion soundness bound to the emitted object) ~5–10+, the deepest
formal work in the project, gated by how cleanly the P0–P6/P4 verifier ladder attaches to the
synthesized circuit. Flagged **optional epic** — its failure downgrades the deliverable to "independent,
differentially-verified synthesis" (still the novelty milestone), it does not kill the epoch.

---

## §8 — Risk ranking + the epoch-killer

Ranked most-to-least likely to sink Phase A:

1. ⚑ **THE EPOCH-KILLER — R3, the byte-exact scaffolding (deferred-values / me-you passing +
   `internal_vars`), tested for real at R4.** Why it kills: a Pickles proof chains ONLY if the exposed
   public-input `Deferred_values` and the two me-only digests are laid out and hashed EXACTLY as
   `spec.ml` prescribes `[read]` `composition_types.ml:785-882`, AND the `internal_vars` mapping makes
   the witness satisfy the gates. It is **all-or-nothing** (no partial credit — the VK is self-consistent
   but `verify` returns false), the layout is intricate (nested records, `Packed_bits` widths, prefix-mask
   branch_data, digest-hashed me-only fields), and the `internal_vars` half has NO existing dregg model
   (§1). If R3 is wrong, R4 produces a beautiful circuit whose proof silently does not verify. This is the
   rung to build FIRST and gate hardest.
2. **The `internal_vars`/placement fidelity (R1).** Reproducing Snarky's exact cell placement + copy
   permutation is undocumented and byte-critical; the o1js digest diff catches divergence but
   reverse-engineering the placement to MATCH it is the deepest R1 risk.
3. **The R4 wrap-circuit gate-dump binding (§4 nuance).** If o1js will not dump the wrap gate list
   without a Pickles-binding tweak, the wrap-side oracle is degraded to the step-side + the digest —
   workable, but a named friction.
4. **OCaml snapshot staleness.** Every OCaml pin is `[read@2024-snapshot]`; if upstream Pickles drifted,
   the target moved. Mitigated because the OPERATIVE oracle is o1js (whatever o1js emits IS the target),
   not the OCaml prose.

**Phase B is explicitly NOT a Phase-A risk** (ember, this session): the soundness theorem is the optional
epic; A ships without it.

---

## §9 — The plain answer

**How far is Lean from synthesizing a Pickles proof that VERIFIES (Phase A)?** A bounded epoch, not
research: **~13–20 swarmcycles** across four rungs. The construction is fully READ and the reference is
a live, zero-build differential oracle (o1js `constraintSystem`). Nothing is open-ended — the gap is a
known quantity of emitter engineering.

**What does dregg's existing verifier logic save?** Roughly half the port. dregg already has the entire
Pasta arithmetic floor for both cycle sides `[exists]`, all six kimchi gate constraint bodies as the
free correctness check on the emitters `[exists]`, the statement records + shifted-value bridge
`[exists]`, the in-circuit-verify math as the assembly spec `[exists]`, the SRS/VK model `[exists]`, a
~90%-wired o1js diff harness `[exists]`, and — the biggest single save, found this read — **the prover
is reused unchanged**: mina-rust consumes Lean's `circuit-blobs`, so Phase A never touches proving. dregg
has built the CHECKER for every piece; Phase A adds the EMITTER, and the checker is the emitter's spec.

**Which Phase-A rung could kill it?** **R3 — the byte-exact statement scaffolding** (deferred-values /
me-you passing + `internal_vars`), realized at R4. It is all-or-nothing, intricate, and its
`internal_vars` half has no existing dregg model. Build and gate it first.

**Phase B (recursion soundness) costed separately:** **~8–15+ swarmcycles, research-grade, OPTIONAL.**
It turns the differentially-verified synthesis into the first machine-checked Pickles, reusing the
verifier-side P0–P6/P4 ladder that is already on disk, resting on the inherited IPA floor as every
implementation does. Its absence downgrades the artifact to "independent, differentially-verified Lean
Pickles synthesis" — which is itself the first-of-its-kind milestone.
