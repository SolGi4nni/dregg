# Twin-Deletion Campaign Map — 2026-07-23

> 11 Rust twins of Lean-authored AIR. 10 delete-and-route-today (@[export] exists); #1 conservation needs a Lean evaluator authored first (it is the ONLY twin with no runtime export — the deployed node has zero runtime link to the conservation proof). Reference done-right: dregg-automatafl (House Law #1). Sequencing: the deletion is PHASE 2 — after the current bug-fix lanes land (the conservation twin overlaps the executor-conservation lane; the finality-tau twin overlaps the tau lane).

# Rust Twins of Lean-Authored AIR/Constraints — Twin-Deletion Map\n\nRead-only hunt across `/Users/ember/dev/breadstuffs` (excluding `dreggnet-market`/`dark-amm`/`fhegg`). A \"twin\" = a Rust function that decides accept/admit/conserve/finalize where a Lean `@[export]` (or Lean-authored AIR) proves the same property, so the two can drift. I classified every candidate; genuine route-through-or-fail-closed code and already-Lean-authored code are listed under KEEP so they are not mistaken for twins.\n\nThe FFI export surface that route-through relies on (`dregg-lean-ffi`): `verified_2pc_decide`, `verified_happened_before`, `shadow_coord_shared_budget`, `verified_handoff_non_amplifying`, `verified_admits` (strand), `verified_tau_order`, `shadow_decide_refines`, `shadow_record_kernel_step`, `verified_finalization_quorum`, `shadow_constraint_admits`, `shadow_holding_grant_weight`, `shadow_grain_r3_verify`, `shadow_interchain_reached_consensus`. There is **NO** conservation export.\n\n## The map\n\n| # | Twin (Rust site) | Lean AIR it shadows | @[export] exists? | Delete + route plan | No-lean-link fallback | Drift severity |\n|---|---|---|---|---|---|---|\n| 1 | **Per-asset conservation** — `turn/src/executor/atomic.rs:321` `check_per_asset_conservation` + `:439` `check_per_asset_conservation_by_asset` (Σδ=0 boundary via Rust `dregg_circuit::block_conservation::BlockConservation`); live callers `:923`, `:1460` | `metatheory/Dregg2/Circuit/CrossCellConservation.lean` (+ `EmitCrossCellConservation.lean`) — an **emitted JSON descriptor**, not a runtime evaluator | **NO** (descriptor only; no `dregg_cross_cell*` symbol) | AUTHOR a Lean runtime evaluator `@[export] dregg_cross_cell_conserves` over the `(asset,δ)` rows + `declared_supply`, prove `= crossCellConservationAir.accepts` (refinement), then route both `check_*` fns through it | **fail-CLOSED** refuse (`PerAssetConservationViolation`) if export absent | **CRITICAL** — already caused the inflation bug (execute.rs asset-blind); economic mint-from-nothing boundary; zero runtime link to the proof today |\n| 2 | **Cell StateConstraint eval** — `cell/src/program/eval.rs:264` `evaluate_constraint_full` match arms (WriteOnce/Monotonic/StrictMonotonic/AffineLe/AllowedTransitions/BoundedBy/… the \"quorum AffineLe\" + \"cell-program gate\" class) | `metatheory/Dregg2/Exec/DeployedConstraint.lean` `admitsTop` (parses `WO/MO/SM/ALE/AT/BB/…`) | **YES** `shadow_constraint_admits` / `constraint_admits_available()` | Native node already routes the subset via installed oracle (`eval.rs:284`). Make the native path **fail-closed / assert oracle installed** for subset arms; keep the Rust arms ONLY for wasm32/SP1-guest (can't link Lean) | Rust arms retained for no-Lean targets (wasm/zkVM); native w/o oracle = fail-closed, NOT silent Rust | **HIGH** — ~40 arms; today a native node that forgot `install_constraint_oracle` silently runs the Rust twin (`installed_oracle()==None` → Rust decides), fail-OPEN |\n| 3 | **coord 2PC decide** — `coord/src/atomic.rs:294` `verified_decision` → `.unwrap` fallback to `evaluate_votes_native` (`:827`), via `evaluate_votes` (`:815`) | `Dregg2.Coord.TwoPhaseCommit.evaluate` (`Exec/DistributedExports.lean`) | **YES** `verified_2pc_decide` / `dregg_coord_2pc_decide` | Delete `evaluate_votes_native` as the live decider; `evaluate_votes` = export-or-fail-closed | fail-closed Abort (never silent native) | MEDIUM |\n| 4 | **coord causal happened-before** — `coord/src/causal.rs:33` `verified_happened_before` → falls back to native `dag.happened_before` (`:38,42`) | `Dregg2` `CausalOrder.happenedBefore` via `hbBool` | **YES** `verified_happened_before` / `dregg_coord_causal_order` | Route through export; drop native fallback on the live path | fail-closed (treat as not-ordered) | MEDIUM |\n| 5 | **coord shared-budget resolve** — `coord/src/shared_budget.rs:831` `verified_resolve_ordering` → fallback `resolve_ordered_native` | `Dregg2.Coord.SharedBudgetDynamics.resolveOrdered` (`resolveOrdered_accepted_le_balance`) | **YES** `shadow_coord_shared_budget` / `dregg_coord_shared_budget` | Route through export; drop native fold on live path | fail-closed (reject all debits) | MEDIUM |\n| 6 | **captp handoff non-amplification** — `captp/src/handoff.rs:847` `verified_non_amplifying(...).unwrap_or(rust_non_amplifying)`; Rust lattice at `:838` | `Dregg2.Exec.CapTPConcrete.handoffNonAmplifyingC` (§6 granted⊆held; `captp_validate_handoff_eq`) | **YES** `verified_handoff_non_amplifying` / `dregg_captp_validate_handoff` | Replace `.unwrap_or(rust_lattice)` with export-or-fail-closed | fail-closed = amplifies⇒REFUSE (already fail-closed-on-amplification) | MEDIUM |\n| 7 | **strand admission (F-4)** — `federation/src/admission.rs:319` `admitted` → fallback `admitted_rust` (`:305`); node wrapper `node/src/strand_admission_gate.rs:78` | `Dregg2.Distributed.StrandAdmission.admitted` (`strand_admit_eq_admitted`) | **YES** `verified_admits` / `dregg_strand_admit` | Route through export; drop `admitted_rust` from live path | fail-closed (non-seed rejected) | MEDIUM |\n| 8 | **finality tau order** — `node/src/finality_gate.rs:150` `compute_order` (routes `verified_tau_order`) → node fallback to Rust `dregg_blocklace::ordering::tau` (blocklace_sync/execution_cursor/committee_replay) | `Dregg2.Distributed.BlocklaceFinality.tauOrder` (`tau_order_export_eq`) | **YES** `verified_tau_order` / `dregg_tau_order` | Startup hard-check (`node/src/lib.rs:1681`) already refuses full-mode w/o export; extend fail-closed to the per-poll `ordering::tau` fallback so the twin never runs live | labeled-unaudited `DREGG_ALLOW_UNVERIFIED_CONSENSUS=1` (already exists) else refuse | MEDIUM (guarded at startup; twin still live under escape hatch + non-full modes) |\n| 9 | **deploy refinement** — `dregg-deploy/src/refine.rs:225` `decide_refines` → fallback `decide_refines_mirror` | `Dregg2.Deos.FlowRefine.decideRefines` (`decideRefines_iff`) | **YES** `shadow_decide_refines` / `dregg_decide_refines` | Route through export; drop mirror on live deploy gate | fail-closed (refuse deploy) | LOW-MED (deploy-time, not consensus) |\n| 10 | **intent per-leg settle** — `intent/src/verified_settle.rs:314` `rec_exec_asset` is AUTHORITATIVE; `ffi::cross_check_leg` is SKIPPED when no gate (`:311` doc) | `Dregg2.Intent.RingFFI` / `Exec.recKExecAsset` (`ffi_export_realises_settleRing_leg`) | **YES** `shadow_record_kernel_step` / `dregg_record_kernel_step` | INVERT: make the FFI the authoritative decider (or fail-closed when no gate) instead of a skippable cross-check | fail-closed (`FfiDivergence`/reject leg) | MEDIUM (Rust decides; Lean only cross-checks, skippable) |\n| 11 | **finalization quorum** — `node/src/finalization_votes.rs:416` `distinct_votes >= self.quorum_threshold` (live Rust decision) | `Dregg2.Distributed.FinalizationQuorum` `quorumRoot`/`quorum_no_conflict` (the safety theorem) | **YES** `verified_finalization_quorum` / `dregg_finalization_quorum` — but wired ONLY in `#[cfg(test)]` (`:425` mod tests, `quorum_decision_matches_verified_lean_gate`) | Wire the LIVE `record()` decision through `verified_finalization_quorum`, not only the differential test | fail-closed (no quorum) | LOW-MED (Rust `>=` is trivially correct; but `quorum_no_conflict` safety not enforced on live path) |\n\n## Twins we can delete + route TODAY (@[export] already exists)\n\nThese need no new Lean — just delete the Rust decider from the live path and route to the existing proven export, with a fail-closed (or labeled-unaudited) branch when the archive is absent:\n\n- **#3 coord 2PC** (`evaluate_votes_native`), **#4 coord causal** (`dag.happened_before` live fallback), **#5 coord shared-budget** (`resolve_ordered_native`) — the coord trio, all behind `distributed_exports_available()`.\n- **#6 captp handoff** (`.unwrap_or(rust_non_amplifying)` → export-or-refuse).\n- **#7 strand admission** (`admitted_rust` → drop from live `admitted`).\n- **#8 finality tau** (extend the startup fail-closed to the per-poll `ordering::tau` fallback).\n- **#9 deploy refinement** (`decide_refines_mirror` → drop from live gate).\n- **#10 intent settle** (invert so `dregg_record_kernel_step` is authoritative, not a skippable cross-check).\n- **#11 finalization quorum** (wire `verified_finalization_quorum` into live `record()`, not just the test).\n- **#2 constraint oracle subset** (native path: assert oracle installed / fail-closed instead of silent Rust `match`; the export is `shadow_constraint_admits`).\n\nAll of these carry a documented \"the two agree by a differential test\" claim — which is unit-test content, not a theorem — so the drift they guard against is real; the differential goes green while a fallback silently diverges in a config where the gate is absent.\n\n## Twins needing a Lean evaluator authored FIRST\n\n- **#1 per-asset conservation** — the ONLY seed with no runtime-callable export. `CrossCellConservation.lean` is an emitted AIR **descriptor** (JSON), not an `@[export]` decision procedure. The deployed node ALWAYS decides conservation in Rust (`atomic.rs check_per_asset_conservation` + `dregg_circuit::block_conservation`). Lean work: author `@[export] dregg_cross_cell_conserves` (Nat/Int over the `(asset, δ)` rows + declared mint/burn), prove it equals the committed `crossCellConservationAir.accepts` boundary (`balance[last]==0`), then route `atomic.rs` through it, fail-closed. This is Lean-authored AIR per House Law #1 — do NOT extend the Rust `BlockConservation` as \"the check.\"\n\n## The single highest-drift-risk twin\n\n**#1 — per-asset conservation (`turn/src/executor/atomic.rs:321` / `:439`).** It is worst on every axis: (a) it is the asset-**inflation** boundary (Σδ=0 per asset) — drift = mint-from-nothing, and it has ALREADY drifted once (the asset-blind `execute.rs` inflation CRITICAL); (b) unlike every other twin it has **no `@[export]` to route to** — the Lean side exists only as an emitted JSON descriptor, so there is zero runtime linkage between the deployed check and the proven AIR, and it can never be a route-through until a Lean evaluator is authored; (c) the live executor decides it in Rust on **every turn** with no fallback guard. Every other twin at least has a proven export the runtime can be pointed at today.\n\n## KEEP — not twins (do not flag)\n\n- **holdings weight** — `dregg-governance/src/holding_weight.rs:704/754` route through `shadow_holding_grant_weight`, **fail closed** (`GrantError::LeanCoreUnavailable`). Correct reality-gate.\n- **grain R3 verify** — `grain-verify/src/r3.rs:171` routes through `shadow_grain_r3_verify`, **fail closed** (`R3Error::LeanCoreUnavailable`). Correct.\n- **interchain adapter** — `bridge/src/interchain_adapter.rs:331` routes through `interchain_reached_consensus`, fails closed to `false`. Correct.\n- **dregg-automatafl** — the AIR is now **Lean-authored**; `step_layout.rs`/`resolve_layout.rs`/`witness.rs` explicitly \"author ZERO constraints — House law #1\"; the old hand-Rust AIR/`Builder` was deleted. This is the pattern DONE RIGHT (reference), not a twin. (`Dregg2/Games/AutomataflAir.lean` still notes an older deployed hand-Rust AIR with no export as debt — worth a separate check, but the current `dregg-automatafl/` crate is clean.)\n  - ⚑ **CORRECTION, 2026-07-25 — this KEEP was half right, and the half it missed was a twin.** The AIR was indeed Lean-authored, but `dregg-automatafl/src/reference.rs` was a Rust twin of the Lean **SPEC** rather than of a Lean AIR: a hand transcription of `~/dev/automatafl/logic/src/game.rs` that computed `apply_turn` off-circuit and was consulted as the witness oracle by the very generators that fill the Lean-emitted descriptors. This map hunted for twins of Lean-authored *AIR/constraints*, so a twin of a Lean-authored *game semantics* fell outside its net — and this one had actually DRIFTED: the conformance audit found the transcribed lineage divergent from the Creator-Approved ruleset on nine clauses (three destroy material), and `resolve_witness.rs` carried an in-tree comment admitting the descriptor "deliberately diverges from `reference::resolve_mid`" on a 2-cycle. DELETED; the crate now routes every transition through `@[export] dregg_automatafl_rules` (`Dregg2.Games.AutomataflFFI` over `AutomataflRules`), with no fallback. **The generalisation for the next sweep: a twin is any Rust object that DECIDES what a Lean object specifies — the Lean side does not have to be an AIR.** `dregg-multiway-tug/src/reference.rs` is the same shape and is still live.\n  - ⚑ **SECOND MEMBER CONFIRMED, 2026-07-25 — `dregg-multiway-tug/src/reference.rs` IS a spec-twin, a PROVEN Lean spec exists, and this one had ALSO already drifted.** What it decides: action and response legality, the offer interlock and the anti-self-deal refusal, the escrow split, per-row control, the charm/row tallies, and the round winner — plus (with no Lean counterpart, see below) the deal, the per-turn draw, distinct card ids, and one example policy. Who consults it: it is the MOVER for the whole crate — `surface.rs` (the playable Offering commits its projection), `game.rs` (the real world-cell driver), `hidden_hand.rs`, `fold.rs` (the recursive membership/win leaves), `state.rs`/`packs.rs` (types + the influence table), and `tests/{round,decisions,fold_real_cell}.rs`; externally `dreggnet-web/src/tug_web.rs` and `dreggnet-catalog/src/seated.rs`. The Lean spec: `metatheory/Dregg2/Games/MultiwayTug.lean` — `legalB`/`legalRespB`, `applyAction`/`applyResponse`/`applyMove`, `takerShare`/`cutterShare`, `control`, `charmScore`/`geishaScore`, `Won`, `roundWinner`, with conservation, one-action-per-round, offer-interlock, scoring-monotonicity and win-safety all proven and `#assert_axioms`-clean. **THE DRIFT:** `reference.rs::winner_of` is `roundWinner` TRUNCATED to its two absolute-threshold branches — it has neither the charm tie-break nor the row tie-break — so on every round where neither seat clears the bar it answers "no winner" where the model ADJUDICATES a seat. The model carries the witness itself: `undecidedState_not_Won` (no threshold win) beside `undecidedState_adjudicates` (`roundWinner = some .p1`), the §7B fix that took the draw rate from 66.1% to 5.1%. **LANDED:** `@[export] dregg_multiway_tug_rules` (`metatheory/Dregg2/Games/MultiwayTugFFI.lean` — a wire codec adding NO semantics, 14 verbs, ~60 `#guard` teeth including the divergence, green), the full `dregg-lean-ffi` plumbing (`Dregg2/FFI.lean` closure import, `lean_init.c` extern + explicit initializer + `_str` bridge, `build.rs` check-cfg + archive probe + shim define + a `REQUIRED_DECISION_EXPORTS` entry), and `dregg-lean-ffi/tests/multiway_tug_oracle_probe.rs`, whose provenance test transcribes the deleted-twin `winner_of` verbatim and asserts the two objects DISAGREE on the same input. \u26d1 **CLOSED 2026-07-25 (later the same day): `winner_of` IS DELETED and the decision routes through the Lean.** `Engine::score` now calls `@[export] dregg_multiway_tug_rules` for row control, both tallies, the winner AND the CLAUSE of `roundWinner` that decided (a new `branch` verb over a new `roundWinnerBranch`), fail-closed with no fallback. The blocker below was real and had to be fixed FIRST, not worked around: the deployed win-gate WAS the absolute bar `Won` (`winTooth_admits_iff_Won`), so an adjudicated winner was refused by the referee and deleting the twin alone would have BRICKED scoring. The teeth changed with it — `MultiwayTugProgram` now emits ONE TRANSITION CASE PER CLAUSE of the terminal rule (`score_charm_a` \u2026 `score_draw`, the bare `score` method retired), because the deployed `SimpleStateConstraint` has no cross-register comparison and so a disjunction over `b_charm < a_charm` cannot live inside one case; `scoreBranchTeeth_admits_iff_branch` proves the deployed gate IS `roundWinner`. Lean + regenerated artifact + Rust landed in ONE commit. \u26a0 STILL OPEN, and it is the rest of the same disease: `legal_decisions`, `apply`, `apply_action` and `apply_response` in the same file still re-decide `legalB`/`applyAction`/`applyResponse` in Rust. The FFI verbs for them EXIST (`legal`, `legalresp`, `kinds`, `act`, `respond`, `split`); what blocks the routing is per-call FFI cost inside the maximin search (thousands of legality queries per round), which needs a batched verb, not a bigger encoder. The original reasons, as they stood before this closure: **(the winner half is now DONE; (a) and (b) below applied to the whole file)** (a) unlike automatafl's, this twin is NOT wholly re-decidable in Lean today — `MultiwayTug.lean` models cards at guild-row resolution (`Geisha := Fin 7`, zones are `Multiset Geisha`) and has NO deal, shuffle, draw or distinct-card-id notion, so the deal/draw/id-bookkeeping half of `reference.rs` has no spec to route to and a Lean-side extension is required before the file can go; (b) a sibling lane held ~2,600 uncommitted lines across exactly these files at the time (`reference.rs` +620/-216, `MultiwayTug.lean` +975, `MultiwayTugProgram.lean` +592, `surface.rs` +406, plus new `tests/{balance,decisions}.rs`), so rewiring the consumers would have clobbered in-flight work. **Remaining to close it:** route `surface.rs`/`game.rs`/`fold.rs` at the oracle, reduce `reference.rs` to a card-identity marshaller with zero rules (the automatafl `board.rs` + `rules.rs` split), and author the deal/draw in Lean — or state plainly that they stay Rust and why.\n  - ⚑ **THE SWEEP FOR MORE MEMBERS OF THIS CLASS (2026-07-25).** Two further Rust objects decide what a Lean object specifies. **`param-compose/src/reference.rs`** — self-described "THE REFERENCE ORACLE", computes the composition OFF-circuit while `crate::air` re-checks it; its own header was corrected the same day to admit the differential "proves NOTHING about all inputs" and that the real refinement is `Dregg2/Circuit/Emit/ParamComposeRefine.lean::paramCompose_refines_law` over the emitted descriptor. There is NO `@[export]`, so the composition, the canonical subject ordering and the absent-slot rule are decided in Rust. Same shape, one tier weaker (a Lean refinement theorem exists; a runtime oracle does not). **`dungeon-on-dregg/src/descent.rs::Sim`** (`delve`/`lunge`/`flee`, `Result<Sim, &str>`) — a Rust re-computation of the descent transition that `Dregg2/Games/Dungeon.lean` + `DungeonDeployed.lean` specify. Weaker still, and deliberately so: the deployed teeth ARE the Lean-emitted `dungeon_program.json` and the executor re-checks every post-state, so Rust PROPOSES and Lean CHECKS. That is the exact computed-vs-checked line `Dregg2/FFI.lean` §2 draws — worth revisiting only if `Sim` ever becomes an answer source rather than a candidate generator. Everything else with a `Lean`-mirroring header in the tree is either a marshaller (`exec-lean/src/constraint_oracle.rs`), a differential test, or already export-routed and fail-closed.\n- **PQ crypto** — `dregg-pq` mldsa/hybrid_kem install cores ONLY when `export_available()`; these are crypto primitives (EverCrypt/Lean-extracted), gated fail-closed, not constraint twins.\n- **constraint-oracle marshaller** — `exec-lean/src/constraint_oracle.rs` `encode_constraint` is the wire encoder (exhaustive, no wildcard, differential-pinned trusted-Rust slot), not a decision twin. KEEP.\n\nOne structural note for the campaign: the coord trio (#3-#5), captp (#6), and strand (#7) all gate on the same `crate::verified_gate::gate()` + `distributed_exports_available()`/`strand_admit_available()` seam and share the identical `Some(verified).unwrap_or(native)` shape — a single sweep can convert all of them from `unwrap_or(native)` to `expect-or-fail-closed`, and delete the `*_native`/`*_rust` siblings from the live paths in one pass."}],"stop_reason":null,"stop_sequence":null,"stop_details":null,"usage":{"input_tokens":2,"cache_creation_input_tokens":2047,"cache_read_input_tokens":125683,"cache_creation":{"ephemeral_5m_input_tokens":2047,"ephemeral_1h_input_tokens":0},"output_tokens":3,"service_tier":"standard","inference_geo":"not_available"},"diagnostics":null},"requestId":"req_011CdL1N6H67zgnSGStx6Qta","attributionAgent":"general-purpose","type":"assistant","uuid":"35f11e15-22a2-40f8-8641-4ff332059ddd","timestamp":"2026-07-24T01:24:39.092Z","effort":"xhigh","userType":"external","entrypoint":"cli","cwd":"/Users/ember/dev/breadstuffs","sessionId":"fb2ce7d2-e34d-47d3-b774-0368f821eecd","version":"2.1.217","gitBranch":"main","slug":"merry-sprouting-backus"}
