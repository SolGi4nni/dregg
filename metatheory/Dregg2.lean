-- Dregg2: the dregg2 vat model — candidate-independent core + coinductive boundary.
--
-- Candidate-independent modules (shared across all dregg2 candidates):
--   * Dregg2.Core              — symmetric-monoidal cells/turns + Σ_k conservation
--   * Dregg2.Laws              — Predicate ⊣ Witness Galois connection + verify/find seam
--   * Dregg2.Authority.Positional — the l4v integrity lift / vat-boundary law template
--   * Dregg2.Confluence        — I-confluence / tier-1 eligibility (third judgement)
--
-- The Boundary/Soundness module is candidate-dependent: dregg2 uses the coinductive
-- (▶-guarded bisimulation) shape (see .docs-history-noclaude/rebuild/dregg2-design/dregg2.md §1.3/§8).
import Dregg2.Tactics       -- shared proof automation
import Dregg2.Core
import Dregg2.Resource
import Dregg2.Resource.TokenAutonomy   -- idempotent resources (capability lattice / GSet) admit no sound retract into a cancellative foreign token: no_substitution_of_idempotent + gset_not_substitutable_by_int; cancellative payment does substitute (honest discriminator); #assert_axioms-clean, #eval non-vacuity
import Dregg2.Laws
import Dregg2.Authority.AffineBridge  -- THE ONE AFFINE VOCABULARY (apps language-unification gap 1): proves Exec.Program.affineSum (Value.scalar, fail-closed Option) and Authority.RelationalClosure.affineSum (fieldOf, total absent->0) COINCIDE on the common (all-fields-present) domain — programAffineSum_eq_relClosure + the affineLe comparison transports BOTH ways (programAffineLe_admits_relPredAffineLe / relPredAffineLe_admits_programAffineLe), so an affine fact in either layer lifts with no re-proof; non-vacuity pins WHY the qualifier is load-bearing (bridge_conventions_differ_off_domain). A light bridge module by design (keeps the foundational Program.lean free of RelationalClosure's heavy EffectsState->TurnExecutor chain). 8 keystones #assert_all_clean.
import Dregg2.Authority.ImportBinding  -- FIRST-CLASS PROVENANCED IMPORT BINDING (gap 2): ImportedEq fuses CrossCellImport.importValid (the provenance obligation, same Import object) + the Program affineEq atom (enforcement) into ONE construct; keystone importedEq_binds_provenanced_value discharges the cross-cell contract Apps/GovernedParameters re-derives by hand (a valid import + admitted binding => local field = the source's committed truth); inherits anti-lie (importedEq_lying_import_rejected) + i-confluence (importedEq_stable_under_source_advance) directly. New module (no cycle — neither Program nor CrossCellImport imports the other). 8 keystones.
import Dregg2.Apps.RealAppsRound2  -- ROUND 2 apps-beyond-toys on the session's new expressiveness (the weakest *Gated toys rebuilt as real policy-layer cell programs): SubscriptionMetered (metered plan — balanceDeltaGe spend-rate FLOOR + affineDeltaLe TWO-METER budget [what no single-field counter sees] + tier memberOf + strictMono period + senderInField; impostor/replay/over_drain/meter_blowout/forged_tier UNSAT vs honest_consume_admits) + GovernedParameters (a constitution-citing cell — the userspace home of CROSS-CELL VERIFIED IMPORTS: affineEq bound to CrossCellImport.importValid provenance + senderMemberOf board + actor-bound approval + immutable constitution pin; non_board/version_replay/constitution_capture/unprovenanced/approval_impostor/LYING_IMPORT UNSAT vs honest_update_admits + param_binding_matches_constitution) + ComputeMarketDesk (rate-limited desk + ASYNC NOTIFY settlement — balanceDeltaLe payout CEILING + inRangeTwoSided price band + senderMemberOf pool + balanceLe drain + NotifyCap settlement wake; non_provider/off_band/over_payout/stranded/out_of_mask_wake UNSAT + settle_wake_attenuation_no_amplify vs honest_settle_admits + settle_wake_commits). 23 keystones #assert_all_clean.
import Dregg2.Firmament.SeL4Composition  -- THE dregg⊗seL4 COMPOSITION (l4v, extends the f4ce6c7e5 grounding): a dregg cap-delegation run AS the kernel `mint` inside a PD's CNode JOINTLY preserves (a) the seL4 PD cap-space invariant — MintedChildrenAttenuated ∧ WellFormed (allocator-freshness + no-dangling mintedFrom, the cdt discipline; mint_preserves_invariant is inductive, NOT a per-step tautology) AND (b) dregg non-amplification (granted⊆held), with the SAME grantOk/authNarrowerOrEqual witness discharging BOTH — the kernel mint-gate decision IS dregg's non-amp decision. dregg_turn_in_pd_preserves_both + dregg_amplifying_delegation_refused_in_pd (fail-closed) + dregg_subdelegation_chain_preserves (survives a chain, rights ⊆ transitively). notify rides free (its rights ride the unchanged AuthReq). 11 keystones #assert_all_clean.
import Dregg2.Authority.CrossCellImport  -- CROSS-CELL READS as VERIFIED PAST-SNAPSHOT imports (gap 6, docs/CELL-PROGRAM-LANGUAGE §8, "the deepest naturalness gap"): an import cites a receipt in the source cell's append-only chain + the value its field held there. CROWN importValid_stable_under_source_advance: a past-snapshot import is I-CONFLUENT (stable as the source advances) where a live read is NOT (liveRead_changes_under_source_advance) — that immutable-past stability is what dissolves the coordination cost cross-cell reads otherwise pay. Anti-lie uniqueness + provenance tamper-evidence inherited from Exec.Receipt.chain_tamper_evident (HInj/HFresh stay NAMED hyps, never Lean axioms) + staleness faithful-but-visible + composes into the closure algebra as RelPred.affineEq (importBindingPred_iff). #print axioms clean (propext [+Quot.sound])
import Dregg2.Apps.IntegratorWedge  -- THE INTEGRATOR-WEDGE real apps (refuting the lamesauce "i am a toy" critique): AgentOrchestrationBudget (the six primitives buildr/builders/sig/simbi hand-roll UNGATED, as ONE dispatch-board cell program — cap-gated authority + signed provenance via senderInField + atomic budget via affineLe + actor-bound baton handoff + no-replay strictMono + attenuated-notify surfaces + capped async wake; teeth both polarities: forged_dispatch/over_budget/stolen_baton/replayed_dispatch/worker_cannot_widen_reach/uncapped_wake all UNSAT vs honest_dispatch_admits+capped_wake_commits) + EscrowDeskCouncil (M-of-N council escrow: duplicate-padded quorum COLLAPSES via eraseDups + wrong-reveal/stranded-value/non-operator all UNSAT vs faithful_release_admits). Both #assert_all_clean.
import Dregg2.Authority.Positional
import Dregg2.Authority.Caveat   -- keys-as-caps token layer (biscuit/macaroon/caveat/discharge): attenuation chain + attenuate_narrows + token-as-Verify bridge + #eval
import Dregg2.Confluence
import Dregg2.Confluence.CRDT   -- CRDT instance catalog: G/PN-counter, G/OR-set, LWW register (each a MergeState + invariant), bounded-counter non-confluence + escrow/quota-partition refinement; #assert_axioms-clean
import Dregg2.Boundary
import Dregg2.StepCamera     -- step-indexed Iris camera (higher-order resources; shares Boundary's ▶)
import Dregg2.JointTurn      -- cross-cell ⊗ : equalizer + CG-2 pullback + CG-5 aggregate, binding-as-hypothesis
import Dregg2.Finality       -- judgement 2: the 4-tier finality lattice + cross-tier join
import Dregg2.Privacy        -- field / value (Pedersen) / graph (stealth+nullifier) privacy tiers
import Dregg2.Coordination   -- MPST global type G → projection → protocol-cell; deadlock-freedom
import Dregg2.Projection     -- cand-D choreography front-end: blue/red split + epp_correspondence (= boundary_law at two altitudes)
import Dregg2.Await          -- algebraic effects + one-shot continuations; turn-as-rollback-handler
import Dregg2.Liveness       -- GC-as-cell-liveness; lease-expiry; cross-vat cycle leak (impossibility)
import Dregg2.Upgrade        -- anti-brick set_program: AIR_VERSION pin + signature fallback
import Dregg2.Execution      -- userspace programs: configurations, runs, invariant-preservation
import Dregg2.CryptoKernel    -- §8 portal: crypto ops as an uninterpreted interface (Lean⟷Rust); verify/find seam instantiated; cross-vat bridge closed
import Dregg2.PrivacyKernel   -- privacy realized over the portal: committed_conservation + nullifier anti-double-spend via the interface laws
import Dregg2.Privacy.Metadata -- pillar #5: the METADATA-privacy boundary — observer DAG-view (coordination-graph+timing+volume) vs shielded payload; two-sided tooth (payload hidden ⊥ partner/timing/volume LEAK) + k-anonymity as a derived cardinality + metadata-ceiling residual
-- ─── The 3-layer CryptoKernel split (.docs-history-noclaude/rebuild/dregg2-design/PHASE-CRYPTOKERNEL.md) ───
import Dregg2.Crypto.Primitives      -- layer A: CryptoPrimitives (Poseidon2, Pedersen commit+commit_hom, nullifier); collisionHard/binding/unlinkable as Prop carriers (not idealized hash_inj)
import Dregg2.Crypto.Merkle          -- §8 discharge #1: CircuitIR + merkleCircuit + merkle_bridge (Satisfies ↔ MerkleMembers, both directions; path-recomposition sound and complete, no primitive seam)
import Dregg2.Crypto.VerifierKernel  -- layer B: verify defined as circuit-satisfiable; merkle_verify_sound derived (accept ⇒ membership, given the STARK extractable carrier) — the §8 verify-law, derived not assumed
import Dregg2.Crypto.PredicateKernel -- layer C: per-kind KindObligation (statement+relation+Dial floor); merkle_registry_cascade (registry_sound ∘ verify_sound) + merkle_dial_wired (EpistemicDial pinned to the Merkle verifier at the acceptanceOnly ZK floor)
import Dregg2.Crypto.PortalFloor      -- 8 §8 @[extern] crypto-portal floor (post-cutover TCB): SignatureKernel(ed25519)/VerifierKernel(STARK)/PedersenKernel(DLog+commit_hom)/Poseidon2Kernel/Blake3Kernel/NullifierKernel/SealKernel(AEAD+X25519)/MacKernelE(HMAC); each = @[extern] oracle + soundness Prop carrier + *_floor_sound taking the carrier as an explicit hypothesis; Reference instances discharge carriers True only in toy ℤ/ℕ (non-vacuity); #assert_axioms-clean
import Dregg2.Crypto.Ed25519Reduction -- the ed25519 EUF-CMA reduction made EXPLICIT: Ed25519EufCma (the IRREDUCIBLE primitive, EUF-CMA game stated as a first-class predicate SigForgery=accept∧¬Signed) + the REAL reduction protocol_forgery_to_sig_forgery (handoff/blocklace-insert/agent-auth forgery ⇒ ed25519 SigForgery) ⇒ eufCma_repels_all_surfaces closes all 3 surfaces under the named carrier. eufCma_iff_sound ties it to PortalFloor's sigVerify_sound (no new assumption, sharper framing). Teeth BOTH directions: ref_eufCma HOLDS on the honest oracle, forge_not_eufCma + forge_{handoff,block,auth}_forgery CONCRETE forgeries on instSignatureForge (strip carrier ⇒ protocol breaks). #assert_axioms-clean
import Dregg2.Crypto.SchnorrCurveField -- the SOUNDNESS STRUCTURE under the in-circuit Schnorr confidential-value curve (RETIRED-ANCHOR NOTE 2026-07-16: its §3 AIR pillar mirrored circuit/src/schnorr_air.rs, deleted f7d09d5f5 as dead pre-law; the field/curve primitives babybear8.rs+schnorr_curve.rs remain; a deployed value-path sig circuit must be EMITTED, law #1) (circuit/src/{babybear8,schnorr_curve,schnorr_air}.rs, the real-field rebuild). Splits each of 3 pillars into a PROVED theorem + a NAMED finite/PARI seed: (1) "it's a field" — babybear_tower_no_zero_divisors grounds the REAL tower (ZMod p)[X]/(z^8-11)=BabyBear^8 as a field via Fact p.Prime ⊳ Fact(Irreducible f) (ZMod.instField⊳AdjoinRoot.instField), so babybear8.rs::inverse always succeeds on nonzero; the old reused-non-residue BUG modeled as the product ring F×F with the literal zero divisor (1,0)·(0,1)=0 (product_ring_has_zero_divisor) — non-vacuous; (2) "prime order N" — orderOf_eq_of_prime_pow_eq_one (g^N=1 ∧ N prime ∧ g≠1 ⇒ ord g=N, no Pohlig–Hellman subgroup), the old composite 2013191319 modeled by composite_annihilator_proper_suborder (ZMod 6: 3•2=0<6 — primality LOAD-BEARING); (3) the AIR relation SchnorrVerifies (s·G+e·pk=R) with schnorr_pins_R/schnorr_rejects_wrong_R (forgery tooth) — PLUS §3a daa_correct/daa_from_origin PROVING the double-and-add scan computes n·G (closing the abstract-smul gap; daa_flip_lsb_changes = flipped_bit_breaks_transition). Bottom of the stack = SchnorrDLHard (the curve DL assumption, the Ed25519EufCma discipline: a Prop carrier whose negation is a concrete DL solver, never := True; toy_dl_not_hard FALSIFIES it on a degenerate curve). #assert_axioms-clean
import Dregg2.Crypto.TurnAuthSignature -- the turn-AUTH signature FORCING rung (additive; circuit/src/turn_auth_signature_air.rs): closes the light-client gap above SchnorrCurveField — a verifying signature-bearing turn-auth descriptor (boundary BINDS agentPk + the turn-hash-reading challenge e=chal R pk turnHash) IMPLIES the rightful agent authorized THIS turn (turnauth_forces_authorization), under the named curve DL assumption SchnorrDLHard + the Schnorr forking-lemma ForkingExtractor. turnauth_pins_boundary extracts R=s·G+e·pk; turnauth_rejects_tampered_hash = a sig can't be re-pointed at another turn; turnauth_forgery_to_dl REDUCES a forgery (verifies but ¬Authorized) to a curve-DL break ⇒ turnauth_no_forgery closes it. Teeth on toyCurve (DL easy): toy_turnauth_verifies HONEST, toy_turnauth_rejects_tampered, toy_forking_extractor_inhabited (strip DL-hardness ⇒ forgery exists). The deployed turn sig is ed25519; RETIRED-ANCHOR NOTE 2026-07-16: circuit/src/turn_auth_signature_air.rs was deleted (f7d09d5f5, dead pre-law, never live-wired), so in-circuit turn-auth forcing is a NAMED OPEN obligation to be EMITTED from Lean (law #1) — NOT a closed hole; deployed turn-auth today is OFF-circuit native ed25519. #assert_all_clean
import Dregg2.Crypto.DualSchemeAuthority -- the DUAL-SCHEME agent-authority model (#3, BOTH-KEYED; additive): two parallel modes anchored in the cell — Ed25519 = OFF-circuit RECEIPT path (named opaque predicate Ed25519ReceiptVerifies, NEVER circuit-ified), Curve = the in-circuit PROVEN path. Scheme-tagged committed authority AuthScheme=Ed25519|Curve|Both; dualscheme_proven_forces_authorization lifts turnauth_forces_authorization (forced cpk==committed Curve key ⇒ rightful key authorized THIS turn); dualscheme_tag_is_committed = dispatch on the COMMITTED tag (downgrade-proof). The THREE anti-laundering teeth (ember's razor): no_downgrade (forced cpk≠committed ⇒ rejected), no_cross_scheme_confusion (EdPubKey/CurvePubKey distinct non-coercible shapes + modes_dont_cross), both_binding_is_committed (changing either leg moves the commitment). dualscheme_ed25519_is_off_circuit witnesses Authorized derived with NO ed25519 premise. #guard non-vacuity both polarities on toyCurve. #assert_all_clean
import Dregg2.Crypto.QuantumOracle -- a REAL quantum-adversary / QROM model from Mathlib linear algebra (no quantum-specific infra): QState = EuclideanSpace ℂ B, unitary = LinearIsometryEquiv, the random-oracle unitary O_H |x,y⟩↦|x,y+Hx⟩ built as a basis-permutation LinearIsometryEquiv via piLpCongrLeft (proved isometry: oracleUnitary_norm), projSet S = projection onto x∈S (idempotent + norm-nonincreasing). OracleDiffData packages the two reprogrammed oracles + the FACTORIZATION (O_H−O_{H'})=(O_H−O_{H'})∘P_S, discharged CONCRETELY by oracleDiff from the coordinate action (agree off S). Toy: toy_oracle_unitary/toy_oracle_permutes on Bool×ZMod 2. #assert_all_clean
import Dregg2.Crypto.OneWayToHiding -- the One-Way-to-Hiding (O2H) lemma PROVED from primitives, riding the QuantumOracle model (foundation for FO-QROM ML-KEM IND-CCA): q-query adversary (Adversary.state/run interleaving unitaries with q oracle calls) + Born-rule amplitude/probability. THREE steps: oracle_diff_on_S (‖O_H v−O_{H'} v‖≤2‖P_S v‖ from the factorization), hybrid_telescope (Adversary.propagate: a one-query oracle swap propagates norm-preserved through the surrounding unitaries ⇒ ‖A^{O_H}−A^{O_{H'}}‖≤Σ‖(O_H−O_{H'})ψₖ‖ via Finset.sum_range_sub), cauchy_schwarz_queries (Σaₖ≤√(q·Σaₖ²) = Mathlib sq_sum_le_card_mul_sum_sq, same core as HermineTSUF.forking). HEADLINE o2h_bound: |amp_H−amp_{H'}|≤2·√(q·Pfind). Toy on Bool×ZMod 2 fires non-vacuously (separating measurement ⇒ advantage 1 ≤ 2·√(1·1)). #assert_all_clean
import Dregg2.World           -- sibling portal: network/clock/randomness oracle for consensus; quorum finality derived + Byzantine/GST OPEN
import Dregg2.Exec.Value      -- Preserves data substrate (dregg2 §5): name-keyed records over a Schema; type-directed flatten with proved fixed width (circuit-over-records foundation)
import Dregg2.Exec.Program    -- RecordProgram structure-map over records (mirrors dregg1 cell/program.rs): StateConstraint catalog + Cases/TransitionGuard dispatch + default-deny; Heyting fragment + #eval
import Dregg2.Exec.Collections -- the data-model rung (2026-06-14, language uplift): first-class named COLLECTIONS (Collection := List Value, Value.collectionField) + AGGREGATE/quantified predicates (countSatGe/sumOfLe/sumOfGe/allMembers/existsMember) + THE COUNCIL LIFT mOfNDistinct = arbitrary-N M-of-N via eraseDups distinctness (lifts the polis N<=3 fixed-slot cap; a duplicate-pad forge AND an unbound-pad forge both REFUSE); 22 keystones #assert_all_clean
import Dregg2.Exec.Cell        -- the living coinductive cell: cexec as a Boundary.TurnCoalg; livingCell_sound = bisimulation-to-golden-oracle; checkpoint/replay as theorems
import Dregg2.Exec.CellReal    -- coinductive living cell over the real per-asset executor: livingCellA_sound = bisimilar to the per-asset conservation oracle forever; livingCellA_obs_invariant = no drift along the unbounded trajectory; livingCellA_supply_disclosed = mint/burn boundary is disclosed (no hidden leak)
import Dregg2.Exec.CellConfine   -- livingCellA_confinement: with a fixed authority ceiling U and initially-confined kernel, every held cap's authority stays ⊆ U forever — coinductive lift of Authority.Positional.confinement_preserved onto the real executor
import Dregg2.Exec.CellNullifier -- livingCellA_no_double_spend: any spent nullifier stays spent at every index forever (registry grow-only); a non-conservation safety on the spent-note set, dual to conservation's equality
import Dregg2.Exec.CellCommit    -- livingCellA_commitments_persist: published commitments persist at every index of the unbounded trajectory (grow-only registry); structural dual of the conservation frame
import Dregg2.Exec.Durability    -- write-ahead log over the real executor: wal_crash_recovery_sound (recover∘crash = live) + wal_torn_write_no_lost_commit + durTraj_recoverable (recovery succeeds along any unbounded turn stream)
import Dregg2.Exec.CellCarry    -- livingCellA_carries: any predicate `Good` preserved by one step holds along the entire unbounded trajectory; app authors supply a one-step proof and get "holds forever" free. Conservation is the trivial instance; livingCellA_logMono (log is append-only forever) is the demo instance. #assert_axioms-clean; #eval non-vacuity
import Dregg2.Exec.VatBoundary -- vat-boundary authority law on the living cell: intra-vat=authorizedB (Integrity.intra) / cross-vat=token discharges (Integrity.cross); unifies Cell+Caveat+Authority + #eval
import Dregg2.Exec.Kernel     -- executable kernel (Design-Spec layer): exec checks conservation+authority, fail-closed + #eval
import Dregg2.Exec.Generators -- mint/burn conservation generators (mint_delta/burn_delta)
import Dregg2.Exec.Caps        -- capability ops (grant/attenuate/derive/revoke/invoke) + integrity bridge
import Dregg2.Exec.Unified      -- unified KernelOp + step: conservation (step_delta) + ledger
import Dregg2.Exec.StepComplete -- step-completeness spine: cexec attests all 4 StepInv conjuncts; conservation_step realized; end-to-end soundness
import Dregg2.Exec.RecordKernel -- concrete cell: KernelState's bal:ℤ lifted to a content-addressed Value record; recKExec_conserves/recKExec_authorized/recCexec_attests proved over the named `balance` field (#assert_axioms-clean)
import Dregg2.Exec.FFI         -- @[export] scalar + record-cell entries → the Lean⟷Rust cascade beachhead (Rust hosts the compiled kernel; PoC round-trips)
import Dregg2.Exec.DeployedConstraint -- byte-exact deployed StateConstraint decision surface with executable agreement/refusal canaries for the host wire
import Dregg2.Exec.CustomAppWrite -- ATOMIC CUSTOM-PROOF APPLICATION ROOT: a Custom effect must be followed immediately by the canonical bounded SetFields write carrying the proved app root; proves shape/bounds/contiguity and mismatch refusal before the Rust executor weld.
import Dregg2.Exec.CodecRoundtrip -- parse∘encode roundtrip theorem (number/digest/string/lit/flag/enum/scalar-value/bal-entry); removes those codec primitives from the Lean-side TCB; #assert_axioms-clean
import Dregg2.Exec.FFI.Refine  -- §5 Stage-1 (CRITICAL-2): the String→String export REFINES the model — execFullForestAuthStep = encodeResult ∘ runModel ∘ parseWWire (export_refines_on_parseable); composed with CodecRoundtrip.parseWWire_encode ⇒ export∘encode = encode∘model end-to-end (export_refines_endToEnd), bringing the turn/effect wire codec INSIDE the proof (no longer TCB); runModel_state ties the export bytes to runGatedForestTurn's keystones; #assert_axioms-clean
import Dregg2.Circuit          -- circuit-from-Lean: CCS IR + bridge (satisfied kernelCircuit ↔ fullStepInv); verify-law derived, not assumed
import Dregg2.Exec.CellProgram -- CellProgram DSL = executable coalgebra structure-map; denote_conserves
import Dregg2.Proof.Refine    -- Exec ⊑ Abstract: conservation + integrity-intra refinement, simulation diagram OPEN
-- Executable protocols (concrete, computable):
import Dregg2.Protocol.Transfer  -- two-cell atomic token transfer + payment-channel run; conservation/CG-5/atomicity proved + #eval
import Dregg2.Protocol.Workflow  -- authenticated, capability-gated, attested multi-party workflow; all guarantees proved + #eval
-- ─── Living-cell growth: additional safety and authority slices ───
import Dregg2.Authority.Discharge   -- discharge = the await authority-face: admits_mono_discharge (resolves forward, never un-resolves)
import Dregg2.Authority.CDT          -- capability-derivation-tree spine: path_attenuates (authority shrinks down any derivation chain) + CDT≡biscuit bridge to Caveat
import Dregg2.Authority.BiscuitGraph -- Track F: the biscuit PUBLIC-KEY delegation chain (what CDT abstracts away) — offline attenuation via a signed block chain; biscuit_narrows (leaf rights ⊆ root, mirrors path_attenuates) + amplifying/forged-block teeth; SigChecker = §8 SignatureKernel carrier (unforgeability stays §8, never a Lean law)
import Dregg2.Authority.Intent       -- intent = the ∃-resolver / inverse-vat-boundary await face: intent_fill_verifies (soundness-by-verification vs adversarial matcher); FIND stays undecidable/OPEN
import Dregg2.Exec.MultiAsset        -- multi-asset conservation: maExec_conserves_per_asset (per-asset Σ_k) + camera-FPU bridge
import Dregg2.Exec.RecordCell        -- RecordProgram as the structure-map on a record cell: recExec_admitted (nothing commits the program rejects)
import Dregg2.Exec.JointCell         -- executable bilateral JointTurn: joint_cg5_conserves (cross-side conservation, no global ledger) + binding-as-hypothesis
import Dregg2.Exec.CrossCaveat       -- cross-cell state caveats as a further equalizer on the joint turn: crossCaveat_sound = CG-5 ∧ CG-2-equalizer (SharedBinding) ∧ φ; caveated_check_eq_use = no TOCTOU; crossCaveat_rejects + covenant_rejects_high (teeth); cost is the Agreement dial (free single-machine, blocking distributed)
import Dregg2.Exec.CellFinality      -- finality tier on the cell: commit_at_join (max) + I-confluence tier-1 eligibility gate (enforced, not prose)
import Dregg2.Exec.CellPrivacy       -- value-rib: committed_transfer_conserves (conservation over Pedersen commitments / hidden amounts)
import Dregg2.Exec.CellUpgrade       -- anti-brick upgrade turn: execUpgrade_never_bricks + stale→owner-signature fallback
import Dregg2.Exec.NullifierCell     -- nullifier set as an I-confluent tier-1-safe cell: spend_no_double_spend + balance≥0 NOT tier-1
import Dregg2.Exec.CellLiveness      -- GC = cell-liveness: death_is_timed_out (lease-expiry; death never decided) + cross-vat-cycle impossibility
-- ─── Storage-as-cell-programs + verification key layer ───
import Dregg2.Exec.Factory          -- FactoryDescriptor: constructor_transparency (a factory mints cells carrying exactly its contract)
import Dregg2.Authority.VerificationKey -- content-addressed circuit identity (mirrors dregg1's canonical_vk_v2): proof_binds_current_vk + proved_state + ChildVkStrategy; §8 collision-resistance kept as an oracle hypothesis
import Dregg2.Exec.CapInbox          -- CapInbox cell-program pattern: inbox_fifo (tail≤head, cursors monotone) + sender-auth via the token layer
import Dregg2.Exec.PubSubTopic       -- PubSubTopic cell-program pattern: pubsub_append_only (log grows, cursors only forward)
import Dregg2.Exec.BlindedQueue      -- BlindedQueue (commitments-in/nullifiers-out): blinded_no_double_spend + consume_needs_verify (§8 oracle)
import Dregg2.Exec.RelayOperator     -- RelayOperator (bonded relay): bond_floor_held + bond_decrease_needs_dispute + quota_enforced
import Dregg2.Exec.CustodyReceipt    -- CUSTODY ACCOUNTABILITY: closes the store-and-forward gap that CapTPStoreForward/RelayOperator left — the relay's sanctioned DROP power now leaves a SIGNED CustodyReceipt of liability. A signed receipt binds (relay, contentHash, inboxOwner, oldRoot→newRoot, acceptBy); EvidenceOfDrop = the relay's OWN signed receipt + a verified `dropped` outcome past the deadline. KEYSTONE (a) accepted_and_dropped_is_convictable (sig=true ∧ outcome dropped ⇒ adjudicate=slash — the relay convicts itself by its own promise) + KEYSTONE (b) honest_relay_not_slashable (delivered-to-promised-root ∨ refunded ⇒ NO evidence convicts, for ANY claimant — the no-false-conviction half that makes it accountability not a slashing gun). Binding teeth: forged_receipt_no_conviction (sig=false convicts nobody), premature_dispute_inadmissible (deadline must pass), conviction_binds_the_signer (under §8 receiptUnforgeable, exactly one accountable party). Rides RelayOperator: conviction_drives_slash (the conviction IS the dispute the BoundedBy anti-drain requires) + no_conviction_no_silent_slash (re-exports bond_decrease_needs_dispute). NON-VACUITY: #guards accept a real conviction AND reject honest delivery/refund/wrong-root/forged/premature. The §8 seam: sig unforgeability (Ed25519 EUF-CMA, carried as receiptUnforgeable), never Lean-proved. #assert_axioms-clean ×8. Rust keystone = POST /relay/send returns a signed receipt (named, ~50 LOC).
-- ─── Predicate registry / algebraic effects / receipt chain / verifiable credentials ───
import Dregg2.Authority.Predicate     -- verify/find predicate registry: registry_sound + adversarial_find_cannot_forge (matcher untrusted, verifier TCB)
import Dregg2.Exec.Effect             -- algebraic effects as turns: conservation_of_effects + disclosed_non_conservation + exhaustive linearity
import Dregg2.Exec.Receipt            -- per-cell WitnessedReceipt chain: chain_tamper_evident (HInj/HFresh hyps) + cexec_appends_receipt
import Dregg2.Authority.Credential    -- verifiable credentials as keys-as-caps: credential_verifies_iff_issued_and_not_revoked + revoke_blocks_verify + revocation no-loss I-confluence; §8 attestation = oracle
import Dregg2.Distributed.Revocation  -- topology-parametrized revocation (dregg4 single-machine principle): eventual_bounded_revocation (distributed ⇒ honored at most until τ+delay, NEVER after) + immediate_revocation (n=1/instantaneous ⇒ honored never after τ, COROLLARY) + tightness_tooth (stale-window honor is real) + single_machine_collapse; reuses Credential.RevocationSet + verify; #print axioms-clean
import Dregg2.Distributed.Fibration    -- Titanium Phase 1: distributed-adversarial semantics as ONE fibration over B = Topology × FaultModel(Sridhar 4-dim) × CryptoStrength; Fibre/lift/lift_collapse(definitional terminal collapse) + lift_from_apex(single-machine principle as reindexing); 3 fibres through the SAME lift — revocation(proven, reuses Revocation.lean) + conservation/attenuation(named-open DistConservationBound/DistAttenuationBound, never an open hole or := True); negative tooth fibre_weakens_offApex + fibration_nonvacuous; #assert_axioms-clean
import Dregg2.Exec.RecordCellLive     -- Value/RecordProgram cell as a real Boundary.TurnCoalg: recCexec_attests (4-conjunct step-completeness) + recReplay_preserves_sumEquals (conservation over name-keyed records) + stepComplete_preserves instance; #assert_axioms-clean
import Dregg2.Exec.CellRuntime        -- OS-level cell runtime theorems: checkpoint_restore_roundtrip + replay_deterministic_run + replayFrom_conserves + time_travel_fork (two admissible diverging suffixes from one snapshot) over both the ℤ-ledger and name-keyed record cell; #assert_axioms-clean; #eval non-vacuity
-- ─── Proof infrastructure: conservation library, tactics, consensus bridge ───
import Dregg2.Conserve                 -- shared conservation lemma library + `conserve`/`commit_cases` tactics (fail-loud, structural; #assert_axioms-clean)
import Dregg2.Catalog                  -- catalog code-gen (`catalog … where` → smart-ctor + admits-char + auto-#assert_axioms triple) + `discharge` guard-seam tactic + `Dregg2` aesop rule-set + `#assert_namespace_axioms` (all fail-loud; #assert_axioms-clean)
import Dregg2.Exec.Consensus          -- quorum→finality-tier bridge: committedByQuorum→bft tier, net_no_downgrade, finality_monotone_on_net; axiom-clean
-- ─── Atomic hyperedge: turn = wide pullback over shared TurnId ───
import Dregg2.Hyperedge               -- Hyperedge = wide pullback over TurnId + N-ary CG-5; hyperedge_sound proved; SharedTurnId/ring are special cases
-- ─── Abstract spec layer (Dregg2.Spec): the factored dregg2 semantics ───
import Dregg2.Spec.Guard              -- unified verify/find seam (demand⊣supply, first-party|witnessed, meet-semilattice attenuate_narrows, OneOf coproduct); legacy constraints/auths as derived instances
import Dregg2.Spec.Conservation       -- multi-domain LinearityClass-typed conservation: committed_iff_cleartext (hidden-yet-conserved) + multi_domain_independent; range-rib is §8 portal
import Dregg2.Spec.GlobalValueConservation -- Value_a over FOUR channels (balance/cleartext-note/committed-note/shielded), no-overlap by construction (type separation for shielded, noteMode totality for clear/committed) + Value_conserved_of_step (the composition theorem the six local invariants were missing); (balance,shielded)/(balance,supply) CLOSED, (cleartext,committed) closed FOR ADMITTED TURNS ONLY (a refusal, not a proof), (balance,notes) has NO LIVE counterexample today but a named DEFINITIONAL gap (no AssetId↔AssetType corr exists)
import Dregg2.Spec.Authority          -- generative capability graph: introduce/amplify/mint + "only connectivity begets connectivity" (gen_step_traces proved); whole-history closure OPEN
import Dregg2.Spec.Lifecycle          -- lifecycle as the attested dual of creation: creation_and_death_are_dual + archival_is_fold(IVC) + reclaim_by_lease + creation_provable_death_temporal; co-witnessability OPEN
-- ─── Spec cross-links: choreography↔hyperedge, await, vat-boundary Φ ───
import Dregg2.Spec.JointViaHyper      -- N-ary joint soundness derived from hyperedge_sound; hyperedge_is_validity_not_canonicity (validity=proof-check, canonicity=consensus)
import Dregg2.Spec.Choreography       -- blue/red projection-split: red_projects_to_hyperedge, blue_needs_no_hyperedge; operational refinement OPEN
import Dregg2.Spec.Await              -- await family = temporal Guard (conditional_is_temporal_guard + resolve_monotone) ⊕ dataflow DAG (Promise); zkpromise/zkawait unification proved; topo-sort OPEN
import Dregg2.Spec.VatBoundary        -- Φ the named-lossy caps↔keys functor: phi_drops_confinement (permission survives, authority doesn't) + forwarded_cap_is_revocable + biscuit/macaroon=Φ-domain; functoriality OPEN
import Dregg2.Spec.Coherence          -- coherence web: guard_is_authority_conferral, conservation_is_hyperedge_cg5, lifecycle_revoke_is_authority_restrictive, choreography_red_conserves, guard_attenuate_narrows_is_meet ⇄ authority_confers_narrows_is_meet
import Dregg2.Spec.ExecRefinement     -- Exec ⊑ Spec beachhead: exec_refines_conservation (toy ℤ ledger = balance domain) + exec_authz_refines_guard (authorizedB = Guard.firstParty); operational LTS OPEN
-- ─── Program logic, §8 discharges, app Spec layer, operational LTS ───
import Dregg2.Proof.WP                 -- VCG/WP calculus over the Option-monad transition: wp/Triple + vcg + vcg_run_sound (reduces to stepComplete_preserves); monotonic-counter + single-ledger escrow proved; cross-vat escrow OPEN
import Dregg2.Exec.AuthTurn            -- authority-mutating turn (dual of the balance turn): recKDelegate/recKRevokeTarget edit `caps`; cap-edit matches Spec.addEdge/removeEdge (Endow/Revoke); dual frame with recTotal fixed
import Dregg2.Proof.LTS                -- single-cell operational LTS: recAbsStep_forward + authAbsStep_forward united in absStep'_forward; cross-cell whole-history closure residual OPEN
import Dregg2.Crypto.NonMembership     -- §8 discharge #3: nonmembership_bridge (Satisfies ↔ NonMember, both directions; sorted_gap_excludes the combinatorial heart, no seam) + nonmembership_verify_sound (derived) + dial at acceptanceOnly floor
-- ─── §8 discharges #4–5, BFT adversary model, cross-cell LTS, closed userspace-verification loop ───
import Dregg2.Crypto.Temporal          -- §8 discharge #4: temporal_bridge (Satisfies ↔ InWindow, both directions; no primitive seam) + temporal_verify_sound (derived) + dial at `selective`
import Dregg2.Crypto.RangeProof        -- §8 privacy ladder RANGE rung (Bulletproofs): range_bridge (Satisfies ↔ InRange, both directions; bit-decomp gadget, no seam) + range_verify_sound (derived, extractable+binding) + committed_inequality_via_range (LADDER: verifyRange (commit v) lo hi proof ⇒ v∈[lo,hi], reading only the commitment) + selective-disclosure + dial at `selective`; carriers = Bulletproofs soundness ← DLog (same family as Pedersen binding), no trusted setup
import Dregg2.Crypto.Dfa               -- §8 discharge #5: dfa_bridge (Satisfies ↔ DfaAccepts, both directions; no seam) + dfa_verify_sound (derived) + dial at `fullDisclosure`; reference kernel OPEN
import Dregg2.Crypto.DfaAcceptanceAir  -- the REAL `dregg-dfa-routing-v1` STARK AIR (tests/src/dfa_circuit.rs), closing two faithfulness gaps Crypto.Dfa leaves: GAP-A δ is a deterministic table FUNCTION (step:State→Sym→State, not an arbitrary relation — "classified to S" is well-defined), GAP-B the Poseidon2 running-hash route-commitment chain Crypto.Dfa wrongly claims is absent ("no compress/hash anywhere"). air_final_state_is_classification (S = classify d input, no crypto) + route_commitment_binds_trace (CR-carried CollisionFree: same C ⇒ same entry-hash chain ⇒ same run) + triples_bind + dfaAir_verify_sound (extractable cascade). Reference = the exact 4-state router: accept→LOCAL, reject→REJECT≠LOCAL (reject_input_not_accepted), a genuine injective-Encodable CollisionFree FIRES the binding, a colliding compress FALSIFIES it. #assert_axioms-clean
import Dregg2.Crypto.Deriv.Determinize  -- DERIVATIVE-MATCHING faithfulness (docs/deos/DERIVATIVE-MATCHING-DESIGN.md): a Brzozowski/Antimirov symbolic-derivative matcher built over dregg's OWN Pred algebra (PredRE = ERE≤'s RE minus lookarounds, Pred leaf; ERE≤/finiteness-derivatives read as proof blueprints only). Stage 0 der/null/derives/Matches + Stage 1 correctness (derives ↔ Matches) + Stage 3 sim_sound (≅ language-sound) + Stage 4 derivativeDfa_correct/_matches (the derivative automaton AS the in-circuit DfaAccepts run; in-circuit Dfa.lean untouched). Transitively pulls in Dregg2.Crypto.Deriv.Finiteness.
import Dregg2.Crypto.Deriv.Finiteness   -- Stage 3 CAPSTONE der_finite: BROZOWSKI FINITENESS over PredRE — ∃ xs, ∀ n, steps r n ⊆[≅] xs (the whole symbolic-derivative state space fits up to similarity in the fixed finite ⊕(pieces r)). Ports the ITP'25 pieces/neSubsets/Permute(nodup)/step_to_pieces tower over dregg's Pred; depends only on {propext, Classical.choice, Quot.sound}.
import Dregg2.Crypto.Deriv.TableDfa      -- Stage 4 CLOSE (table-opaque): TableDfa models compiler.rs::Dfa::matches (total step-table fold). tableRun_dfaAccepts (a table run IS a Dfa.lean DfaAccepts run over the deterministic δ — closes DfaAcceptanceAir GAP-A, pure propext) + tableDfa_faithful (ANY table whose accepts agrees with `derives` everywhere decides EXACTLY Matches R — the compiled table's boolean meaning is a THEOREM, construction-agnostic) + derivativeMatcher_faithful/derivativeTable_finite (the faithful table EXISTS + is FINITE, chaining der_finite). The language-equivalence the design says SUFFICES for the table-opaque AIR.
import Dregg2.Crypto.Deriv.Powerset      -- Stage 4 NARROW-RESIDUAL CLOSE: the DEPLOYED determinized table specifically (not just "any agreeing table") agrees with `derives`. LangEquiv (R ≈ S := ∀ w, derives w R = derives w S; null/der congruences; ≅ ⊆ ≈ via sim_derives — so the smart-constructor canonicalization preserves it) + DerivDeterminizer (the correctness contract derivative.rs::Re::compile satisfies by construction: integer state IDs + decode-to-residual; start/step/accept up to LangEquiv) + determinizer_faithful (under the contract the emitted table decides EXACTLY Matches R — the deployed deny-filter table is a concrete tableDfa_faithful instance). Witnesses over residual-typed (derivAutomaton, incl. deny-filter neg) AND integer-ID (epsilonDet : State=Nat) state spaces. RESIDUAL named: the LEGACY complement-free pattern_to_nfa().determinize() Thompson-subset path instantiates the same contract only via the residual-language=Brzozowski-derivative bridge (Thompson + subset correctness, design §3.2 step 3); the deny-filter path closed here needs no such bridge.
import Dregg2.Crypto.Deriv.Thompson      -- Stage 4 LEGACY-PATH SUBSET-HALF CLOSE: the complement-free compiler.rs::pattern_to_nfa().determinize() path. The SUBSET/determinization factor (compiler.rs::Nfa::determinize = ε-closure powerset) is CLOSED end-to-end via mathlib's VERIFIED εNFA.toNFA_correct (ε-elim) + NFA.toDFA_correct (subset construction): ofDFA (mathlib DFA → our flat-table TableDfa, fold = DFA.evalFrom) + determinizedTable_accepts (the deployed determinized table recognizes EXACTLY the Thompson εNFA's language) + tableDfa_faithful' (keystone, generic over Set-σ states) + legacy_determinized_faithful (IF the Thompson εNFA recognizes R THEN the table is Matches-faithful). The LEFT factor — Thompson-construction correctness `accepts(thompson R)=Matches R` — is NAMED as the sole obligation ThompsonRecognizes (mathlib has NO regex→εNFA Thompson construction; design §3.2-step-3 months-scale automata theory), NOT sorry-ed; shown INHABITED by symENfa (the canonical 2-state single-symbol Thompson automaton) with symENfa_recognizes proven in full + determinized-table accept/reject witnesses. So the legacy residual is narrowed: subset half PROVEN (mathlib), Thompson half isolated.
import Dregg2.Proof.BFT                -- Byzantine/honesty model over World: bft_safety (conflicting quorums ⇒ ⊥ via honest-witness intersection, n−f quorum) + GST liveness; all model assumptions are structure fields, #assert_axioms-clean
import Dregg2.Proof.CrossCellLTS       -- cross-cell operational LTS: crossAbsStep_forward (bilateral jointApply square: conservation + authority + grounding) + crossAbsRun_forward; machine-checked that cross-cell does NOT reduce to single-cell (tensor-non-finality); N-ary forest OPEN
import Dregg2.Proof.WPCatalog          -- userspace-verification loop closed end-to-end: vcg_discharge (fail-loud) + eDSL multi-field ledgerSM → vcg → vcg_discharge → vcg_run_sound (conservation + monotonic seq)
-- ─── BFT liveness pacemaker, cross-cell forest, contention dichotomy, §8 discharge #6 ───
import Dregg2.Proof.BFTLiveness        -- closes the GST liveness OPEN: GSTRound obtained from DLS88-GST + ELRS-synchronizer + HotStuff-responsive-quorum Pacemaker; World.gst_liveness derived; randomized synchronizer construction OPEN
import Dregg2.Proof.Synchronizer       -- randomized leader-rotation synchronizer: expected_views_O1 + honest_hit_as (a.s. hit); reduces Pacemaker.synchronizes to the randomness+honest-fraction model; World.rand probability-measure bridge OPEN
import Dregg2.Proof.GST                 -- Track D-GST: a GSTModel BUILDS a BFTLiveness.Pacemaker (pacemaker_of_gstModel) → gst_liveness (∃ committed-by-quorum, the descent closing the consensus OPEN at the combinatorial level via liveness_of_pacemaker); honestLeader co-finality carried as an honest field; teeth = a bounded (r<5) leader admits no sync round past gst=10; World.rand measure bridge stays the SAME named OPEN
import Dregg2.Proof.CoinductiveAdversary -- unbounded-interleaving adversary: obsBisim_traj_of_bisim (confluence-up-to-bisimulation over νF) proved for the safe fragment; deriving it from the finite dichotomy needs up-to-context closure — OPEN
import Dregg2.Proof.BeaconSpace        -- probabilistic oracle layer: Measure over beacon streams + Bernoulli(h) independence; noHonestEverGe_measure_zero + honestLeader_index_exists discharge Synchronizer.hhit; Dirac h=1 boundary witness suffices (interior-h needs an unbuilt mathlib module)
-- ─── §8 kinds 7+8 (registry complete), catalog phase (ii), eDSL-B, dregg1 semantics mirrors ───
import Dregg2.Crypto.BlindedSet        -- §8 #7: blindedset_bridge (= merkle_bridge; a BlindedSet membership IS Merkle vs the issuer root) + HolderAnonymity carrier + dial acceptanceOnly (holder hidden)
import Dregg2.Crypto.Custom            -- §8 #8 (registry complete; extensibility OPEN): custom_bridge parametric over a CustomRegistration's own bridge field; any future kind registers (vk,circuit,relation,bridge) and inherits the cascade
import Dregg2.Crypto.GarbledJoint      -- §8 PRIVACY FLOOR (2PC): GarbledKernel (garble/eval over a joint predicate P a b) with garbled_correct (eval = P a b) + garbled_input_private (transcript = simulator(P a b); leaks only the outcome bit) modeling Yao garbled circuits (circuit/src/garbled.rs Poseidon2-garbled comparison); joint_turn_private_gate welds it to EntangledJoint's proven-atomic JOINT TURN — two cells transition together gated by a private predicate over BOTH their secret states, output-only disclosure at the acceptanceOnly dial floor; non-vacuity = geKernel (b≥a) admits one (a,b)/rejects another w/ privacy WITNESSED, + leakyEval ANTI breaks private_sim. The Lean SPEC the Rust 2PC must meet. #assert_axioms-clean
import Dregg2.CatalogInstances         -- dregg1's StateConstraint(27)/Authorization(9) catalogs as derived Guard smart-constructors + the LEAN-AUTHORED 53-kind Effect coloring (the Rust Effect::linearity twin was DELETED 2026-07-28); .burn RECOLORED Annihilative->Conservative and .mint ADDED, both as holder<->issuer-well moves; drift vs turn/src/action.rs pinned two-sidedly by tests/src/effect_catalog_lean_rust_pin.rs; 111 thms #assert_namespace_axioms-clean
import Dregg2.DSLChoreo                -- `dregg_choreo {…}` choreography eDSL → Coordination.GlobalType; auction inherits deadlock_freedom + privacy_by_projection; #check_projectable elaboration gate
import Dregg2.Exec.CapTP               -- CapTP transport: pipelining_preserves_seam + handoff_is_introduce/_non_amplifying/_forwarder_revocable (3-vat Granovetter handoff = Spec.Authority.Introduce crossing Φ); distributed-GC liveness OPEN
import Dregg2.Exec.CapTPConcrete       -- CONCRETE AuthRequired attenuation lattice the captp runtime enforces, pinned to Lean + tied to handoff_non_amplifying; decisionTable mirrored by captp/tests/handoff_lattice_differential.rs (drift-tooth across the FFI gap)
import Dregg2.Authority.Blocklace      -- byzantine-repelling DAG (Cordial-Miners C-spine): equivocation_detectable + honest_no_equivocation + cdt_is_blocklace bridge + attested finality; eventual-exclusion-under-partition OPEN
import Dregg2.Exec.DfaRouting          -- DFA message-routing automaton (mirrors dregg1): routed_message_followed_accepting_route (delivery soundness, fail-closed) + route_authorization (per-hop Guard) + unique_route + routing_projects_message_flow
import Dregg2.Crypto.UCBridge          -- UC cross-system bridge: FComDischarge bundles CryptHOL's Pedersen F_com guarantees as Prop carriers; binding_unlinkable_discharged_by_crypthol proved (#assert_axioms-clean). CAVEATED: trust widens to Isabelle/HOL + transport fidelity; Isabelle theory in uc-crypthol/ (local build blocked by afp-devel↔RC3 skew, see PHASE-UC-TRANSPORT.md)
-- ─── Wire emission, distributed GC, full op-set, cross-cell forest ───
import Dregg2.Exec.CircuitEmit         -- circuit extraction emitter: emit (ConstraintSystem→wire) + emit_faithful + emittedKernel_bridge (emitted kernelCircuit ↔ fullStepInv); fingerprint-bound to the real Rust AIR via dregg-lean-ffi
import Dregg2.Exec.CapTPGC             -- CapTP distributed GC by lease: captp_gc_by_lease + captp_no_premature_reclaim + captp_cycle_leak_is_the_price (the impossibility)
import Dregg2.Exec.CapTPGCConcrete     -- CapTP GC refcount-drop path: per-session buckets (F-12), mandatory session (F-11), byzantine_cannot_drop_victim_ref + f11/f12 closed-finding laws + swiss-table bridge
import Dregg2.CatalogEffects           -- all 53 dregg1 Effect variants colored onto Spec.Conservation LinearityClass + per-class obligations + Regime discriminator; bridgeFinalize is now the SOLE Annihilative kind; 70 thms #assert_namespace_axioms-clean
import Dregg2.Exec.TurnExecutor        -- turn-executor: Turn = call-forest of catalog-typed Actions run all-or-nothing; execTurn_attests = all 4 StepInv conjuncts over the whole multi-action turn + execTurn_conserves/_balance_domain/_unauthorized_fails
import Dregg2.Exec.TurnForest          -- nested call-forest executor: execForest_no_amplify (Granovetter via derive_no_amplify) + execForest_conserves (N-ary CG-5, Σ=0 whole tree) + execForest_attests (4 StepInv conjuncts); cross-cell forest OPEN
import Dregg2.Paco                      -- vendored+ported from hxrts/paco-lean (MIT): parametrized coinduction (paco/upaco/gpaco/gupaco + companion/up-to + the `pcofix` tactic), 23 modules; powers CoinductiveAdversary's up-to-context closure (obsBisim_of_uptoComm)
import Dregg2.Proof.ForestLTS          -- N-ary cross-cell forest LTS: forestAbsStep_forward (forestApply square, Finset.sum conservation, Σ=0 binding hypothesis-routed) + forestAbsRun_forward; bilateral = ι=Fin 2 slice
import Dregg2.Proof.ContendedCrossCell -- contention dichotomy (both poles): contended_commits_confluent (disjoint/I-confluent ⇒ schedule-agnostic) + coupled_no_schedule_agnostic_commit (coupled Σ=0 ⇒ ¬∃ schedule-agnostic commit — BEC/CAP impossibility); bridged to Confluence
import Dregg2.Proof.ContendedForest    -- ForestLTS §11 CLOSED: contended_forest_commutes (N-ary overlapping forests under an adversarial interleaver commute UNCONDITIONALLY — gate-free additive forest half: half_fires_frame + half_commute) + contended_forest_simulates (the abstract LTS survives the hostile schedule); boundary vs the availability-gated coupled impossibility (forest_unconditional_vs_gated_boundary)
import Dregg2.Crypto.Bridge            -- §8 discharge #6: bridge_bridge (Satisfies ↔ BridgeRelation, both directions; no seam) + bridge_verify_sound (derived) + dial at `selective`
import Dregg2.Crypto.Pedersen         -- §8 discharge #2: pedersen_conservation_bridge (Satisfies ↔ Conserves, both directions; commit_hom-grounded, range-gadget non-negativity, no seam) + pedersen_verify_sound (derived) + dial at `selective`
import Dregg2.Crypto.Deco              -- DECO/zkTLS money-in crown: DecoRelation + deco_bridge (Satisfies ↔ DecoRelation) + deco_verify_sound + deco_binds_payment + deco_authenticates_payment + the registry cascade; underpins the Stripe Verify cone
import Dregg2.Crypto.Cfg               -- §8 discharge #7 (NESTED structure the DFA can't): cfg_bridge ((∃ chain, CfgAccepts) ↔ input ∈ g.language) over mathlib's ContextFreeGrammar + cfg_verify_sound (derived) + Dyck non-vacuity
import Dregg2.Crypto.CfgCompact        -- the O(tokens) COMPACT certificate (leftmost rule sequence, pushdown Replay): compact_sound (accepted replay → language membership) + compact_to_chain (rebuilds the CfgAccepts object — the wire changes, the theorem doesn't)
import Dregg2.Crypto.ZkOracle       -- the zkOracle capstone: zkOracle_sound composes Deco (authentic zkTLS) + Cfg (well-formed JSON) + Deriv native-neg (input UNMATCHES the handlebars/injection template); runnable injection-catch demo
import Dregg2.Crypto.Handlebars     -- the REAL injection-freedom (superseding ZkOracle's delimiter-substring stand-in): a handlebars template T IS a context-free grammar handlebarsToGrammar T (fixed text→terminals, each {{hole}}→a nonterminal deriving safe no-`{{` data); injectionFree T output := output ∈ language. render_mem_language (safe render always lands in the language — GENERATION SOUNDNESS, proved) + injectionFree_of_verify (decided by the SAME cfg_verify_sound STARK as the well-formedness leg). Round-trip/uniqueness = NAMED RESIDUAL (§9), not sorry-ed. RustTwin #guards reproduce injection.rs's single-`{`-fine / `{{`-flagged split. #assert_axioms-clean
import Dregg2.Crypto.HandlebarsWitness -- PROOF-PRODUCING RENDER: renderRules materializes the leftmost-derivation witness the existence proof walks implicitly; renderRules_accepts : safe T d → ReplayAccepts (handlebarsToGrammar T) (renderRules T d) (render T d) — render EMITS a re-checkable generation certificate (CfgCompact.Replay); materialized_agrees ties it back to render_mem_language. Every render a receipt. #assert_axioms-clean.
import Dregg2.Crypto.HandlebarsCompose -- COMPOSITION (the fifth): a hole filled by ANOTHER template's proof-carrying render, the proofs NEST. handlebarsCompose T h T' = the combined grammar (CNT=NT⊕NT, hole h's recognizer REPLACED by a bridge safeD h→inner.start); derives_lift = the NT-injection Derives-transport (mathlib Derives.reverse shape); bridge_derives NESTS render_mem_language T' into the combined grammar; renderCompose_mem_language = nesting-soundness (minimal hyps: safe surviving holes + safe inner). Nested materialized Replay witness + composed-grammar unambiguity = named residuals (§9/§10), not sorry-ed. #assert_axioms-clean.
import Dregg2.Crypto.HandlebarsUniqueness -- THE INVERSE, restricted: render INJECTIVITY / unique data recovery for delimiter-guarded templates (delim_render_injective : delimTemplate + brace-free holes → render T d = render T d' → d=d' on every hole). The round-trip's load-bearing half — the S4-break/idempotent-closure direction — PROVEN where the structure permits (the reachable chip at the §9/§10 unambiguity wall). General CFG-unambiguity stays the NAMED wall (§6). #assert_axioms-clean.
import Dregg2.Crypto.HandlebarsFFI    -- WITNESS TOOLING (Lean authors, Rust/wasm CALL): @[export dregg_render_with_proof] renderWithProofWire (PROVER — serializes the PROVEN render+renderRules) + @[export dregg_replay_check] replayCheckWire (VERIFIER). The one new thing: replayCheckB (executable Bool decider) + replayCheckB_iff (decides Replay EXACTLY, full iff via a fuel bound). Closes the render.rs dual-authoring: Rust consumes the verified Lean by construction, not a differential-tested twin. The reusable checker for the composed-attestation surface. #assert_axioms-clean.
import Dregg2.Crypto.HandlebarsGuarded -- THE ATTESTABLE SCHEMA LANGUAGE: holes carry a PredRE GUARD (the verified Deriv matcher), not a hardcoded ban. guardedSafe := each hole's data satisfies ITS OWN guard via `derives`; guarded_render_mem_language = guard-parametric generation soundness. noDoubleBraceRE_iff proves NoDoubleBrace is ONE guard (neg(·{{·)) — the committed Handlebars is the instance where every guard = noDoubleBraceRE (guardedSafe_noDoubleBrace_iff). Alphabet = Value (rich, boolean-closed), so each hole is a REGULAR LEAF (the regex side of the composed-attestation substrate). Demo: a hole legitimately holds `{{` under a permissive guard — the brace-ban is now the author's choice. #assert_axioms-clean. Follow-ons (guarded witness/compose/uniqueness) named residuals.
import Dregg2.Crypto.DfaAsCert         -- THE LINCHPIN: regex/DFA and CFG on ONE certificate substrate. dfaAccepts_as_cert : Dfa.DfaAccepts <-> Hypergraph.Cert delta (+ the honest DFA state-projection wrapper Cert can't bake in); regular_and_cf_share_substrate : DFA and CFG are BOTH Hypergraph.bridge, differing only in R (delta vs Produces). Keystone of docs/DESIGN-composed-attestation-architecture.md — regex = CFG = hypergraph = rewrite, one object. #assert_axioms-clean, non-vacuous (a+b Reference DFA routed through Cert).
import Dregg2.Crypto.ReplayAsCert      -- THE CF-MACHINE RUNG (full-context find): replay_as_cert : CfgCompact.Replay <-> Hypergraph.Cert ReplayStep — the pushdown MACHINE on the substrate (regular=DfaAsCert, VPL=VpaAsCert, CF-grammar=cfg_parse_via_reduction, CF-machine=THIS). Generics Cert.map/Cert.foldSound (in Hypergraph) SUBSUME the design's "multi-month-risk" multi-row induction: mrun_imp_replay_via_fold proves the AbstractMachine.mrun_imp_replay statement as a Cert.foldSound instance (subsumption_stmt_eq machine-checks the type equality). #assert_axioms-clean. Layering CLEAN: the machine layer lives in Crypto.AbstractMachine (imported downward); no Crypto<-Circuit inversion.
import Dregg2.Crypto.AbstractMachine   -- the pushdown-run layer (Act/MRow/MStep/MRun/rulesOf + mrun_imp_replay) extracted VERBATIM from DyckStackRefine (imports only CfgCompact); DyckStackRefine re-exports the names byte-identically. Removes the old Crypto<-Circuit inversion.
import Dregg2.Crypto.VpaDecidable      -- the ONE VPL capability CFL provably can't have (decidable template equivalence). DELIVERED (not laundered em): a COMPUTABLE emptiness Decidable instance (decidableLangEmpty) via Alur-Madhusudan reachable-summary saturation (Finset (S×S), pigeonhole fixpoint) — with the dead-machine #guard canary; + prodVpa_lang (INTERSECTION) + equiv_iff_symmDiff_empty + decidableEquivOfComplements (equivalence decided GIVEN complement machines + their correctness — the exact ComplementClosure conclusion, never asserted). SOLE named residual: ComplementClosure itself (determinize-over-summary-pairs + accept-flip). #assert_axioms-clean.
import Dregg2.Crypto.CertFoldForward   -- PAY-FORWARD demo: a fresh depth-counter machine's whole-run soundness AND decode-faithfulness each land as a SINGLE Cert.foldSound term (~10 lines local per machine vs a ~37-line bespoke induction owed twice) — the substrate makes each future rung's multi-row assembly free. #assert_axioms-clean.
import Dregg2.Crypto.GuardedSpans      -- GRAMMAR-GENERALIZATION first slice (docs/DESIGN-guarded-grammar-circuit.md): mem_language_iff_spans — membership in a GUARDED template language ⇔ a per-segment span decomposition (literal pins + matcher-attested hole spans, SpanOk) — the composed circuit's obligation set proven SOUND AND COMPLETE for language membership, off the fixed Dyck grammar toward the templater's guarded grammars. Non-vacuous (Demo decomposes; tampered span rejected). #assert_axioms-clean.
import Dregg2.Crypto.Deriv.SatOracle   -- SYMBOLIC-VPA LIFT tier 0 (docs/DESIGN-symbolic-vpa-lift.md): the infinite Value alphabet is the SYMBOLIC (predicate-label) setting, NOT register — so finite-VPL decidability lifts via symbolic automata over the effective boolean algebra Pred, CONDITIONED on decidable PredSat (satisfiability), NOT forfeited (refutes both the "Value->Nat weld" claim AND the reframe's "forfeits" pessimism). PredSat φ := ∃ a:Value, leaf φ a = true, with WITNESS-backed Decidable instances for tt/ff/symEq/¬symEq + deployed_guard_minterms_decided (every guard the templater writes at HEAD). #assert_all_clean.
import Dregg2.Crypto.Deriv.SymbolicEmptiness -- SYMBOLIC-VPA LIFT tier 2: the first USER-VISIBLE symbolic decision — is a guarded REGULAR language EMPTY over the INFINITE Value alphabet. satStep (sat-filtered via the deployed minterm WITNESSES) + der_factors (for deployed R, der a R depends on a ONLY via leaf braceP a) => nonemptyWithin_iff_bounded: the BOUNDED decision is SOUND ⋀ COMPLETE (both directions, |w| ≤ n) with a genuine boundedNonemptyDecidable. DESIGN FINDING: the naive "filter 𝜕 leaves by PredSat" is UNSOUND (𝜕's node conditions are firstPred=.tt placeholders); the constructive minterm-witness form is the fix. UNSAT canary bites (contradictionRE decides false where a sat-FREE step wrongly reaches nullable). ⚠ NOT proven: the UNBOUNDED decision (false => no accepting word of ANY length) — needs a DECIDABLE ≅ / ACI normalizer (plain DecidableEq PredRE canNOT terminate: der a (star r) = cat (der a r) (star r) grows syntactically), a satStep↔der_finite-steps bridge, and pigeonhole excision. #assert_all_clean.
import Dregg2.Crypto.Deriv.SymbolicEmptinessUnbounded -- the PIGEONHOLE rung (c): derList_excise (a ≅-revisit in an accepting derivative path is EXCISABLE to a shorter accepting path, via sim_derList) + exists_sim_prefix_pair (Fintype pigeonhole over the finite ≅-classes from StepBridge.satStep_reachable_finite) => the n-FREE emptiness decision. ⚑ FINDING: decidable ≅ (ingredient a) is NOT required — ≅ occurs only INSIDE proofs (class representative, nullability transport), never in anything that computes, so predRENonemptyDecidable carries no DecidableRel Sim. (a) was never a prerequisite for (c).
import Dregg2.Crypto.Deriv.SymbolicDecision -- THE ASSEMBLED emptiness decision: predRE_emptiness_decidable : DeployedRE -> Decidable (∃ w, derives w R = true) — the n-free composition of the bounded decision + pigeonhole + StepBridge finiteness, as ONE instance on the fragment TYPE DeployedRE = {R // IsDeployed R}. #guard fires the WHOLE stack through `decide` (true on ε, false-pole on contradictionRE). NO fragment mismatch: bounded decision + pigeonhole BOTH need exactly IsDeployed. Emptiness over the INFINITE Value alphabet, decided.
import Dregg2.Crypto.Deriv.SymbolicEquivalence -- THE HEADLINE: predRE_equivalence_decidable : DeployedRE -> DeployedRE -> Decidable (∀ w, derives w R = derives w S) — DECIDABLE TEMPLATE EQUIVALENCE over the INFINITE Value alphabet (deployed fragment), the one capability CFL provably cannot have. Via symmetric-difference emptiness: langEq_iff_symDiff_empty ((∀w R=S) ↔ ¬∃w symDiff accepts) + symDiff_deployed (IsDeployed closed under alt/inter/neg) + predRE_emptiness_decidable. FULLY proven n-free (sound+complete, all words); the decision is total. ⚠ kernel-EVAL of the full decision is intractable (emptinessBound over two negs is astronomical) — PERFORMANCE not soundness, same frontier as emptiness, closed by ingredient (a) dedup. Poles proven directly (equiv_refl, eps_not_equiv_contradiction) + bounded #guards fire.
import Dregg2.Crypto.Deriv.SymbolicMinterms -- GENERALIZES the emptiness+equivalence decisions off the braceP delimiter to ARBITRARY templater guards: predRE_emptiness_decidable_general / predRE_equivalence_decidable_general : (R : SymbolicRE) -> Decidable, over ANY boolean combination of tt/ff/symEq(any field,sym)/digEq(any field,digest)/symMemberOf across INTERACTING fields, via PER-R MINTERM witnesses (der_factors_over: der a R depends on a only through leafSig L a; braceP is the L=[braceP] instance). Unsat minterms handled constructively (no SAT oracle). STRICTLY wider (roleP_not_leafDeployed). Boundary NAMED: atom(LIA)/digFieldEq/reactive/allOf-anyOf uncovered. 32 keystones #assert_all_clean.
import Dregg2.Crypto.Deriv.SymbolicFixpoint -- makes the emptiness decision RUNNABLE (proven -> computable): an adaptive ≅-fixpoint (simDecide-deduped worklist) decides in the FINITE ≅-class count, not the astronomical emptinessBound. #guards KERNEL-decide non-ε machines: roleContra (3^15+ bound-search) empty in 4 ≅-classes; a symEq cat/star guard non-empty; decide fires through predRE_emptiness_decidable_fix by rfl. CONSUMES AciComplete.simDecide (the orphaned ≅-decision — now load-bearing): seenSim gates worklist admission, sim_null/sim_der make the dedup answer-preserving. rigidRE_der = the RigidFull-reachability bridge (derivatives never escape). Total+correct at every fuel (Option worklist, proven bound-based fallback on exhaustion — no Classical.dec). 15 keystones #assert_all_clean.
import Dregg2.Crypto.Deriv.EquivalenceFixpoint -- the campaign WHOLE: predRE_equivalence_decidable_fix — RUNNABLE template equivalence over the infinite Value alphabet. Kernel-decides real pairs in ~1.4s: role=3 ≠ role=4 (disagree on a 1-frame word), role3⋓role3 ≡ role3 (a ∀-over-infinite-alphabet EQUIVALENCE computed by the fixpoint), rolePlus ≠ star(role3). Via emptyFix (the ≅-fixpoint) on symDiff + langEq_iff_symDiff_empty; symDiff of two RigidFull is RigidFull. predBEq now decides not/and/or (predBEq_sound re-proven), so compound-leaf regexes (contradictionRE's ¬braceP) are RigidFull. 5 keystones #assert_all_clean.
import Dregg2.Crypto.Deriv.SymbolicMintermsPlus -- closes the USEFUL residual predicate classes into the runnable fragment: symMemberOf (enum membership), allOf/anyOf (policy lists, via a language-preserving desugar), digFieldEq (owner-match, via a correlated two-frame MintermCover coverOfDigFieldEq — the plug-in seam made explicit, zero tower changes). predBEq_sound re-proven (mutual predBEqList). rigidRE_of_isSymbolic: every IsSymbolic root is RigidFull. Kernel #guards decide status∈{1,2,3}, ownerMatch, from≠to. Boundary named: digFieldEq mixed with per-field pins (EUF), fieldEqField/reactive, atom/LIA. 30 keystones #assert_all_clean.
import Dregg2.Crypto.Deriv.SymbolicDifference -- DBM MintermCover over (field×old/new) coordinates: negative-cycle feasibility iff shortest-path witness, exact reactive numeric atoms (monotonic/strict/delta/bounded/immutable/writeOnce), full-value fieldEqField + fieldLeField, and the existing generic emptiness/equivalence fixpoint assemblies. Singleton step rules exact; unlinked multi-symbol words overapproximate traces; general affine combinations remain out of scope. Bool-only guards + 15 clean keystones.
import Dregg2.Crypto.Deriv.SymbolicAffine -- the AFFINE/QF-LIA class. §A allowedTransitions: an EXACT, unbounded, TOTAL MintermCover for finite (old,new) pair sets (state-machine guards). §B affine (affineLe/affineEq, sumEquals=affineEq k=1): a BOUNDED-DOMAIN total cover — the leaf is the REAL atom CONJOINED with inRangeTwoSided domain gates on every mentioned field, enumerated over the box grid. ⚠ HONEST: a raw UNBOUNDED affine atom has NO finite total cover (∀-frames vs ∀-in-box gap, stated in-file); the general case needs the LIA a-priori box theorem (feasible ⟹ witness in a coefficient-computed box) — NAMED residual. Two-frame affine (affineDeltaLe/sumEqualsAcross/affineDeltaLeField) also residual. 26 keystones #assert_all_clean; ~3.5s/1.9GB.
import Dregg2.Crypto.Deriv.RealGuardAudit2 -- RE-AUDIT after closing 3 classes: of 9 originally-out-of-fragment REAL dregg guards, 6 genuinely reached (reactive WriteOnce, cross-field fieldLeField ×2, digFieldEq owner-match, 2 bounded-affine — 1 only at a trivially-true box), 2 residual (context-atom SenderIs, non-Pred RoleCapMap+templater-strings), 1 RETRACTED mis-model (BoundedBy is reactive anyOf[monotonic,strictMono], not fieldLeField — caught by adversarial verify). Nothing live found (all reachable guards satisfiable, no accidental equivalence). Cheap Bool #guards only.
import Dregg2.Crypto.Deriv.PredicateLibrary -- a USABLE policy-authoring surface on the decision: roleIs/statusIn/refIs/ownerMatch/noSelfTransfer/requireAll/requireAny combinators + WORKED policies (transfer auth, access control, enum gate), each wired to the decision — a builder can ask "is my guard contradictory?" (emptiness) and "did my edit change the policy?" (equivalence). 15 keystones #assert_all_clean.
import Dregg2.Crypto.Deriv.RealGuardAudit -- executable audit of actual deployed guard vocabulary through the symbolic derivative decision procedure; contradictions, widenings, and accidental equivalences fire as kernel #guards
import Dregg2.Crypto.Deriv.StepBridge  -- SYMBOLIC-VPA LIFT ingredient (b): the bridge from CONCRETE der-reachability into der_finite's SYMBOLIC steps-finiteness. der_mem_step : der a r ∈ step r — EXACT membership (not up-to-≅, not fragment-only); the suspect 𝜕 firstPred=.tt placeholder is a NON-obstruction because `leaves` collects BOTH Node subtrees, discarding the branch condition — the same over-approximation that makes `step` FATAL for emptiness is exactly RIGHT for finiteness. Hence satStep_subset_step + satStep_reachable_finite (in der_finite's exact ⊆[≅] shape): the satStep-walked state space is FINITE up to ≅. ⚠ For the pigeonhole lane: the witness list bounds the state COUNT but its LENGTH is not the ≅-CLASS count (may hold ≅-duplicates), so (a) decidable ≅ is a PREREQUISITE for (c) counting, not an independent axis. #assert_all_clean.
import Dregg2.Crypto.Deriv.AciNormal  -- ingredient (a) FIRST SLICE + the night's sharpest finding: `Sim` is a LEFT REGULAR BAND congruence, NOT ACI/commutative (its alt laws are x(yz)=(xy)z, xx=x, xyx=xy; the free LRB on 2 generators has ab≠ba, so `alt a b ≅ alt b a` is NOT derivable). So any SORTING normalizer has an unprovable obligation at its core — the design's months-scale estimate was priced against a canonical form that CANNOT EXIST. Order-preserving first-occurrence dedup instead: normalize + normalize_sim (Sim (normalize R) R) + normalize_derives, ~300 lines, 13 keystones. REVISED COST: pure-alt fragment completeness = DAYS not months; full PredRE = WEEKS, gated on adding starCong + right catCong to Sim (Sim is NOT a full congruence — no starCong, one-sided catCong — which caps any sound normalizer at the top-level spine). ⚠ The LRB non-commutativity is ARGUED (prose model), not yet a Lean theorem — that tooth is the falsifier for this whole design choice.
import Dregg2.Crypto.Deriv.AciComplete  -- the LRB claim, now a THEOREM against the ACTUAL Sim (was prose in AciNormal): sim_key (Sim R S -> key R = key S, one induction over all 10 generators; idem/dedup discharged by LRB list-laws mergeR_self/mergeR_dedup) yields BOTH (i) sim_normalize_eq (Sim R S -> normalize R = normalize S on the pure-alt rigid-leaf fragment Frag, so reEq(normalize R)(normalize S) DECIDES ≅ there) AND (ii) not_sim_alt_comm : ¬ Sim (.alt s7 s9)(.alt s9 s7) — the SORTED normal form is UNREACHABLE, not merely unproved. Fragment honest (excludes star/cat/inter/neg + non-decidable leaves) + inhabited (sim_dedup_witness). 26 keystones. Residual unchanged: full PredRE needs starCong+right-catCong on Sim.
import Dregg2.Circuit.Emit.DyckStackReplay -- the parse-as-derivation capstone (keeps DyckStackRefine + Replay CI-covered now that the inversion no longer pulls them in via ReplayAsCert): general parse_sat_imp_replay_emit_unconditional, keyed on the byte-pinned emitted descriptor.
import Dregg2.Circuit.Emit.AutomataflStepRefine -- STAGE 2 of automatafl-in-Lean: the SAT⇒SEM refinement of the byte-pinned automaton-step descriptor. Lands sub-lemma (1) IN FULL — the auto one-hot + dot-product pin force the decoded OLD board to hold the AUTO particle at the witnessed (AX,AY) (autoPin_of_sat / decoded_auto_holds_automaton, keyed on Satisfied2 automataflStepDesc) — plus coord-in-bounds and the cardinal-offset envelope (offset_of_sat). Non-vacuous (two-sided gate #guards), #assert_axioms-clean. Residual §7: raycast/evaluateAxis/chooseOffset/board-update soundness (the top-level composition is NOT yet stated as proven).
import Dregg2.Circuit.Emit.AutomataflResolveRefine -- LEG R STAGE 2: the SAT=>SEM refinement of the byte-pinned automatafl m=2 RESOLUTION descriptor (automataflResolveDesc, 306 cols / 371 constraints). PROVEN off the emitted gates: R1 the witnessed auto-read pins (ax,ay) to the AUTO cell (autoPinR_of_sat); R2 validate_move => the reference MoveValid + fp IS the real source cell (validMove_of_sat / sourceRead_of_sat); R3 the witnessed is_vertical bit = the real geometry (iv_of_sat: iv=1 <-> fx=tx) and occ is FORCED to 0 = the reference occluded at n=2 (occ_of_sat / occluded_false_n2); R4 the four eq_coords pattern bits are exact coordinate equality (eqCoords_of_sat) and fork/collide/survive are exactly their reference conditions (selection_of_sat). DEFECT FOUND: the descriptor emits NO board-cell alphabet range check (contrast AutomataflStepEmit.boardRangeConstraints), so anything reading the particle alphabet carries the named BoardAlphabet envelope (srcNonVac_of_sat). RESIDUAL: R5 (flow-through chain), R6 (write_mid rewrite) and the capstone are NOT yet proven.
import Dregg2.Circuit.Emit.AutomataflOcclusionBridge -- OCCLUSION BRIDGE DISCHARGED off Satisfied2. The AutomataflOcclusionGeneric capstones (occ_eq_occluded_vert/horiz) took NAMED column hypotheses (LineReads*, OsrcIsOtherSource*, the endpoint one-hot pins, osrc/line ranges); with the now n-parametric automataflResolveDesc every one becomes a THEOREM off the emitted gates: efrom_oneHot/eto_oneHot (the iv-gated selection IS a genuine one-hot of the along-axis endpoint), lineReadsVert/Horiz_of_sat (the iv-gated line scan reads the move's own line off the OLD board, vacuum iff felt 0 — via boardvalid_of_sat), osrcMeansVert/Horiz_of_sat (the og-gated osrc one-hot marks exactly the OTHER move's moving source on interior indices), lineRange_of_sat, occ_col_iff_msumVal (the occ bit IS [msum>=1] over segVal/msumVal). The capstone occ_iff_occluded_of_sat: the emitted cOcc = reference Automatafl.occluded, UNCONDITIONAL, at whatever NN the descriptor is instantiated (2). At NN=2 both sides are false, but through the GENERIC threshold/mask/one-hot machinery (occ_eq_occluded_*), not interior_nil_n2 vacuity. #assert_axioms-clean.
import Dregg2.Circuit.Emit.AutomataflCommit -- STEP 1 of the AUTOMATAFL-NGENERIC rebuild: the n-generic base-4 board-commitment family (ADDITIVE; does not touch bind_board_roots/node8Lookup). Emits packBoardConstraints n (ceil(n^2/15) degree-1 pack gates packed_j - sum 4^i*cell[15j+i] = 0) + commitBoardConstraints n (Option A: packed felts bound directly to PIs, no hash) + boardRangeCells n; automataflCommitDesc n bundles them (n=11: 130 cols, 139 constraints, 25 PIs). PROVEN pure-Lean n-generic: pack_injective (equal packed felt-tuples on the {0,1,2,3} alphabet => cell-wise code agreement, by base-4 positional decode with no modular collision since packed_j < 4^15 < p), its mod-p form pack_injective_modp, and packBoard_injective (the reference-Board seam corollary: equal packed felts => cellAt agrees at every in-bounds coord). This is the lemma that turns the whole-turn seam hypothesis into a discharged theorem under Option A. #assert_axioms-clean; non-vacuity #guards at n=2 (1 felt) and n=11 (9 felts). Residual = the descN refactor + wiring it into the two capstones.
import Dregg2.Circuit.Emit.AutomataflCommitRefine -- STEP 2 / THE SEAM-DISCHARGE BRIDGE off automataflCommitDesc n. Keyed on Satisfied2 in the AutomataflStepRefine extraction style: (1) pack_pi_of_sat — the PACKED TRANSPORT: a satisfying canonical witness has PI[16+j] = packBoard(boardDecode(cells)) j mod p, DERIVED from the emitted pack gate (packed_j - sum 4^i*cell = 0) + the commitBoardConstraints .piBinding + the boardRangeCells alphabet gates (the {0,1,2,3} decode is a THEOREM via cell_mem_of_sat, no-wrap via packCell_nonneg). (2) seam_of_equal_pis — THE SEAM-DISCHARGE LEMMA (UNCONDITIONAL, pure Lean, no crypto): two satisfying witnesses with EQUAL committed PIs have cell-wise-agreeing decoded boards, via pack_pi_of_sat both sides => pack_injective_modp. This turns the whole-turn board-agreement seam into a discharged consequence of the fold PI-equality once Leg R/A adopt this commitment (the coupled descN refactor; the live capstone is AutomataflTurnCapstone.turn_sat_imp_roundStep_pi over cMidV4). forge_rejected: a non-genuine committed PI is refused (the transport BITES). #assert_axioms-clean; two-sided #guard canary at n=2 (genuine gate=0, forge!=0, genuine felt=57).
import Dregg2.Circuit.Emit.AutomataflMarks -- MULTIROUND BRAID M0 / THE MARKS COMMITMENT (docs/reference/AUTOMATAFL-MULTIROUND-BRAID-DESIGN.md section 2.1, M0). LEAN-AUTHORED AIR: marks (the conflicted-coordinate set, illegal as src AND dst for ALL) is an n^2 {0,1} INDICATOR BOARD, so it packs through the SAME parametric base-4 family the board uses (marksPackConstraintsAt = packBoardConstraintsAt by rfl, marksCommitConstraintsAt = commitBoardConstraintsAt by rfl) into feltCount n felts (9 at n=11) -- a FREE REUSE of proven machinery, NOT new crypto. What is NEW is the TIGHTER alphabet: marksRangeCellsAt emits assert_member(cell,{0,1}), one factor narrower than boardRangeCells' {0,1,2,3}; mem2_of_gate derives it from the emitted gate. PROVEN: marksPack_pi_of_sat (the emitted PI window IS the pack of the decoded marks indicator, derived FROM AutomataflCommitRefine.pack_pi_of_mem through the decode bridge marksCode_eq_boardCode) with marks_forge_rejected as its contrapositive; marks_pack_injective (equal marks windows => cell-wise equal marks, via pack_injective_modp -- NO hash, NO chip-table soundness, so the RoundState seam stays hash-free); and the SPEC correspondence to AutomataflRules RoundState.marks : List Coord (marksBoardOf / marksCode_eq_one_iff_mem: membership IFF indicator=1, plus marks_window_eq_imp_mem_iff -- equal windows => the same marked SET on the board), with clashCoords_inBounds showing a round's fresh marks never fall off the n^2 grid. automataflMarksDesc n = the standalone descriptor (n=11: 130 cols, 25 PIs, 139 constraints, 0 hash sites). Non-vacuity: #guard canaries at n=3 and n=11 (empty / 1-coord / one-cell-differing marks sets all get DISTINCT packs, including the last-cell (10,10) flip the retired board_root8 was blind to), a SPEC-connected canary packing roundStep's ACTUAL .again marks on the rules file's collide/fork witnesses, and the tighter-alphabet canary (the marks gate REFUSES the code 2 the board gate ACCEPTS). #assert_axioms-clean. DESCRIPTOR-SIDE PREP: not wired into a Leg C descriptor (that is M1) and no golden on disk.
import Dregg2.Circuit.Emit.AutomataflLegCEmit -- ⚑ M1 OF THE MULTI-ROUND CONFLICT BRAID — THE LEG C DESCRIPTOR (docs/reference/AUTOMATAFL-MULTIROUND-BRAID-DESIGN.md section 1, M1). LEAN-AUTHORED AIR: AutomataflRules.roundStep's .again branch (the fork/collide RE-ENTRY round) as an emitted object, n-generic. Leg C = Leg R's FRONT HALF (onePin / autoRead / validateMove x2 / srcNonVac / patternBit / selection — spliced in VERBATIM at Leg R's own column indices, nothing re-authored) with the RESOLUTION TAIL REPLACED by a STATE-TRANSITION TAIL: csCell (the clash delta, = cFork*onehot(frm0) + cCollide*onehot(to0), per clashCoords_pair_iff), marksOut = marksIn OR cs (degree 2, per cell), legal[w] = NOT marksIn[frm] AND NOT marksIn[to] PINNED to 1 (= moveLegalB's marks half, the only place marksIn is consumed), inClash[w] = cs[frm] OR cs[to], and the seat-indexed lockedOut = (1-inClash)*move / waitingOut = inClash. cSurv is PINNED TO 0, so a CLEAN round is UNSATISFIABLE as a Leg C — design section 4.2's 'no spurious extra round' is a property of the emitted object, not a hypothesis, and M2's hclash comes free. M0's marksCommitFamilyAt is called TWICE (marksIn/marksOut windows) — the pack is NOT re-implemented; the board is FROZEN (mid gated cell-wise equal to old) and both board windows are published, so the RoundState window [boardPack || marksPack || lockedVec || waitingInd || auto] = 32 lanes is emitted at BOTH ends. n=11: 1208 cols / 78 PIs / 1425 constraints / 0 tables / 0 hash sites / 0 lookups (CHEAPER than Leg R's 1273/36/1618 — it never runs landMap/movers/landBad/occlusion). SCOPE, STATED: LEGC_SEATS = 2 (the design's 2-player collapse); lockedIn = [] and waitingIn = 1 are PINNED (true at P=2, FALSE at P>2), and lockedOut = [] is an EMITTED CONSEQUENCE (a pair clash conflicts BOTH seats) — the 'innocent seat stays locked' case needs a third move and is M9. Two-sided #guard canaries at n=5 on the rules file's own D4a COLLIDE and D4b FORK audit witnesses: every tail family vanishes on the honest row, and the load-bearing negatives BITE — a forged marksOut that DROPS the new mark (even with a consistently re-packed felt), one that INVENTS a mark, a forged cs at an unnamed coordinate, a forged lockedOut/waitingOut, a submission naming an ALREADY-MARKED square, and a CLEAN round (cSurv=1) refused by the clash pin. The decoded OUT window's marks/locked/waiting match roundStep's .again RoundState on both witnesses (the M2 statement, checked at a point). ALSO PROVEN HERE (the M2 handoff, #assert_axioms-clean): the MEMBERSHIP interface -- legCConstraints is a List.flatten of NAMED families, so mem_validateMove / mem_selection / mem_srcNonVac / mem_patternBit / mem_clashPin / mem_marksIn / mem_marksOut / mem_cs / mem_marksOr / mem_seat discharge in ONE List.mem_flatten step exactly the hypotheses every _of_mem extraction lemma (validMoveN_of_sat, marksAlpha_of_mem, marksPack_pi_of_mem, pack_pi_of_mem) takes. DESIGN-DOC CORRECTION found by building the object: section 1.4 states the refinement as roundStep ... = .again (roundStateDecodeOut ...), which is FALSE -- roundStep accumulates marks in INSERTION order and an n^2 indicator can only be read back ROW-MAJOR; a two-round #guard exhibits the divergence ([(2,0),(0,0)] vs [(0,0),(2,0)]). The provable statement is equality up to List.Perm on marks (RoundStateAgrees, defined here), which is all MoveLegal (not-in) and RoundWF (Nodup) ever read. NOT PROVEN HERE: legC_sat_imp_roundAgainN is M2; no witness generator and no EmitByName registration (M4).
import Dregg2.Circuit.Emit.AutomataflLegCGolden -- the BYTE-PINNED Leg C wire goldens: emitVmJson2 (automataflLegCDescN 11) (553 KiB, the deployed board) and (automataflLegCDescN 5) (153 KiB, the audit-witness board). Isolated in its own leaf for the same reason AutomataflNGenGolden is (large mechanical strings); imports read-only. Unlike Leg R, automataflLegCDescN computes its name from n, so these are the descriptors themselves rather than .name-patched wrappers.
import Dregg2.Circuit.Emit.AutomataflLegCRefine -- ⚑ M2 OF THE MULTI-ROUND CONFLICT BRAID — THE LEG C REFINEMENT SPINE (docs/reference/AUTOMATAFL-MULTIROUND-BRAID-DESIGN.md §1.4, M2). Pure refinement over the Lean-authored Leg C AIR (no AIR authored here; reads Satisfied2 (automataflLegCDescN n) and concludes about AutomataflRules.roundStep). §1 = THE SELECTION BLOCK RE-DERIVED DESCRIPTOR-GENERICALLY (M1's flagged first task: the resolve _of_sat extractors are keyed on automataflResolveDescN through resolveFactsN, which pulls carry/flow/occlusion facts Leg C does not emit, so surv_iff_clash_empty does NOT transfer for free): oneD/prodD/notBitD/condNonzeroD/ge0_5D/autoPinD/sourceReadD/validMoveD/eqCoordsD/srcNonVacD/selectionD/forkCollideBoolD — each the resolve body with rgateN/rgateHN swapped for AutomataflCoord.ngate/ngateH (descriptor-generic) plus explicit family-membership transport hypotheses; the evalH-rfl polynomial shapes are unchanged. §2 = INSTANTIATION at automataflLegCDescN n through AutomataflLegCEmit's membership interface + THE surv↔clash BRIDGE: legcMoveValid, legcSurvIff (the ResolveFactsN.survIff shape off Leg C's front half alone), legc_surv_iff_clash_empty (surv_iff_clash_empty_of_sat's twin), and legc_clash_of_sat — ⚑ hclash IS A CONSEQUENCE OF Satisfied2, NOT A HYPOTHESIS (clashPin gates cSurv=0 via mem_clashPin, the bridge turns that into clashCoords ≠ []). Non-vacuous at the deployed n=11 (legc_clash_of_sat11) and the minimal n=2 (legc_clash_of_sat2). 9 keystones #assert_axioms-clean {propext, Classical.choice, Quot.sound}. REMAINING for the full legC_sat_imp_roundAgainN (the tail correspondence): the csCell↔clashCoords indicator bridge (csCell_iff_mem_clash), the marks-indicator↔list membership bridge (from M0's marksCode_eq_one_iff_mem), and the marks Perm-of-Nodup assembly (marksOut = marksIn ∨ cs decodes to a permutation of (marks ++ cs).dedup) — plus the moveLegalB legality read (legal[w]=1 → frm/to ∉ marks). The bridge/hclash SPINE is complete and green.
import Dregg2.Circuit.Emit.AutomataflRevealEmit -- LEG S (commit->reveal), 11x11: the Lean-authored sealed-move reveal AIR. Two public openings + two published move commitments + the old-board nine-felt pack transported in one statement; contiguous commit-join copy at PI[39,41) shaped for the recursive app-root weld. Structural + mutation teeth; NOT registered (coupled Rust-caller action).
import Dregg2.Circuit.Emit.AutomataflRevealRefine -- LEG S SAT=>SEM: legS_sat_imp_semantics (Satisfied2 => two genuine openings commit_s = hash[11fy+fx,11ty+tx,seat,nonce] + coords in [0,11) + old-pack transport), under the standard ChipTableSound chip carrier + RevealCanon envelope (NO collision resistance smuggled). swapped_opening_extracts_collision = UNCONDITIONAL RED collision extractor; legS_swap_refused conditional on the explicitly-named Hash4NoCollision floor.
import Dregg2.Circuit.Emit.AutomataflRevealJoin -- LEG S JOIN + HONEST BOUNDARY: legS_commit_pair_bound_to_preceding composes CarriesCommitJoin (proven) with the deployed-shaped AppRootWeldsCommitPair + RevealPreservesCommitPair (LABELED boundary hypotheses a real caller must discharge). RED teeth pin the three open deployment blockers (63-bit BLAKE3 seal vs 1-felt Poseidon2 lane non-injective; octet too short for 9-felt pack; n=11 vs live 5x5). #assert_axioms-clean.
import Dregg2.Circuit.Emit.AutomataflOcclusionGeneric -- OCCLUSION AT ARBITRARY BOARD SIZE. AutomataflResolveRefine closes the occlusion leg at n=2 BY VACUITY (interior_nil_n2/occluded_false_n2: a 2-line has no strictly-interior cell, so segHead's index set is empty and the threshold cannot fire) — both of those lemmas are FALSE at n>=3. This file proves the replacement over n as a VARIABLE, off the emitter's own fold structure: segVal_eq (the emitted seg[k] head IS the strictly-between indicator of the two along-axis one-hots), msum_ge_one_iff (the msum>=1 threshold fires iff SOME strictly-interior index is non-vacuum and not a passable moving source — by term-wise non-negativity, the argument n=2 never had to make), mem_interior_vert/horiz + occluded_vert/horiz_iff (the reference side, replacing the interval_cases-over-2x2 proof), and occ_eq_occluded_vert/horiz (the bridge: emitted threshold = reference occluded, any n, any rook move). The descriptor-side hypotheses (LineReads*, OsrcIsOtherSource*, the one-hot pins) are the named seam: discharging them from Satisfied2 needs an n-parametric automataflResolveDesc, which does not exist yet. #assert_axioms-clean; non-vacuity witnessed at n=3 by decide.
import Dregg2.Circuit.Emit.AutomataflCoord -- THE n-GENERIC COORDINATE / ONE-HOT FOUNDATION for the automatafl capstones (the gate for n>2). Off the EMITTER's own fold structure (evalH bridge: (headToExpr h).eval a = clean semantic sum, general n): oneHotN_of_sat (a satisfied n-wide Builder::one_hot forces its selector VALUES into a genuine OneHotAt at the pinned index + index in [0,n); the read primitive the auto pin / source read / target read all use, generalising the 2-selector oneHot_of_sat), dot_oneHot / dot_oneHot2 (a one-hot / two one-hots collapse a gated dot-product / masked board double-sum to the selected cell), coordN_of_sat (decompose_coord_le forces the coordinate into [0,n) with NO wrap, under the labelled no-wrap window 2^(rbits+1) <= p), autoPinN_of_sat (DEMONSTRATION: the witnessed auto (AX,AY) is in [0,n) and old[AY*n+AX]=AUTO at ARBITRARY n, off Satisfied2 (automataflResolveDescN n)). NON-VACUOUS at n=3 (autoPin_n3_rejects_wrong: a wrong auto cell on a 3x3 board fails the pin). #assert_axioms-clean {propext, Classical.choice, Quot.sound}.
import Dregg2.Circuit.Emit.AutomataflOcclusionBridgeN -- THE n-GENERIC OCCLUSION DISCHARGE (886 lines, all off Satisfied2 automataflResolveDescN n, arbitrary n): the bridge hypotheses of occ_iff_occluded_of_sat discharged n-generic via the AutomataflCoord foundation — the novel List.range n fold-value lemmas (evalH_lineHead/segHead/msumHead), lineReadsVert/HorizN (the two places NN=2 dodged by vacuity), eqScalarN/ivN/efrom_eto_oneHotN/ogN/segN/occ_col_iff_msumValN. Non-vacuous at n=3 (routes into the n=3-validated AutomataflOcclusionGeneric core). Residual: ONE hypothesis (OsrcIsOtherSource) to fully assemble occ_iff_occluded_of_sat forall n.
import Dregg2.Circuit.Emit.AutomataflStepCapstone -- LEG A: the n-GENERIC ray fuel induction AND THE FULL FOUR-RAY WIRING at ARBITRARY n. SS1-SS2 (unchanged): raycastFuel_succ_vac / raycastFuel_scan (induction on the step count) / raycast_at_hit collapse the reference Board.raycast to the single read at the hit step K; boardDecodeN + raycast_of_hit = one landing lemma over all four cardinals at any size. NEW SS3-SS9 (the wiring that was the named residual): evalHStep_ibEqHead / evalHStep_rcReadHead turn the bif-guarded window filterMap and the bif-guarded NESTED gated-read fold into total List.range n sums; oneHot_window_sum = the window-predicate indicator; foldl_step_terms + headIsZero_false_of_mem discharge the beforeConstraints emitted-ness side condition at symbolic n (a decide at n=2); window_{xp,xn,yp,yn} compute each cardinal's prefix sum. rayN_of_sat = the DIRECTION-INDEPENDENT extraction forall n (hit one-hot => K in [1,n]; dist = K; every step strictly before K in-bounds+VACUUM from the occlusion gates; what = the K-th gated read; oob_hit/cond_nonzero split on ib_K). ibN_of_sat / rcN_of_sat = the two direction-carrying reads (in-bounds bit IS its window prefix sum; gated read IS the shifted cell, 0 off the board). CAPSTONE OF THIS FILE: raycast_{xp,xn,yp,yn}_of_satN -- ALL FOUR rays at ARBITRARY n, each witnessed (rDist,rWhat) = Board.raycast (boardDecodeN n e) auto dir. Instantiated at n=3 and the deployed n=11; two-sided window canary. #assert_axioms-clean. RESIDUAL (named, NOT asserted): decideAxis / chooseOffset / automatonOffset_of_sat / astep_sat_imp_automatonStep at n>2 -- blocked on an n-generic BACK-END membership layer (AutomataflStepRefine SS2.5 is front-end only; the Stage-1b back-end + commitment family are still decide-at-n=2) plus restating the ~2000-line forced_ge0/score/decision-decode chain over A_DECIDE_X_BASE n rather than the frozen literals 58/105/152/209.
import Dregg2.Circuit.Emit.AutomataflResolveCapstone -- LEG R at ARBITRARY n, ASSEMBLED (downstream of the occlusion bridge): oneHotHeadN_of_sat (the one-hot pinned to an interpolation HEAD), srcIndN/dstIndN (the indicator glue at any n), ResolveFactsN + resolveFactsN_of_sat (every derived per-row fact off Satisfied2 (automataflResolveDescN n) under an EXPLICIT BoardWindow, with the carry/ft occlusion conjuncts bridged to the reference Automatafl.occluded), writeCellN_of_sat (the per-cell rewrite gate at any n -- the emitted head is fixed-arity, so the {0,1}^2 four-cell enumeration is gone), and the OCCLUSION-AWARE caterpillar (nextOf_pairN states the m=2 move graph unconditionally with each edge gated by its own occluded; chainDest_aN/bN resolve the chain at ARBITRARY coordinates). THE CAPSTONE resolve_sat_imp_resolveMid IS NOT RESTATED AT n>2 -- and not for want of plumbing: WOUND, the descriptor and the reference DISAGREE at n>=3 when a non-vacuum OCCLUDED source STAYS (the reference filters pieceSrcs by VACUUM, not by occlusion) and the other, carrying piece lands on it: the reference keeps the stayer's particle (stayer_keeps_cell, journeys.find? scans in source order) while the emitted gate is forced to write the lander's (writeCell_forces_other). occludedStayer_witness_n3 exhibits the class at n=3 by decide (valid moves, A occluded, B not, B lands on A's source, conflictResolve keeps both, reference cell = attractor vs the gate-forced repulsor). #assert_axioms-clean.
import Dregg2.Circuit.Emit.AutomataflStepCoord -- THE STEP-SIDE n-GENERIC COORDINATE/ONE-HOT FOUNDATION (the Leg A crux). AutomataflCoord's evalH/headToExpr_eval are typed on AutomataflResolveEmit.Head, a DISTINCT type from AutomataflStepEmit.Head, so every STEP-side rw [headToExpr_eval] FAILED and the step chain was pinned at concrete n. This mirrors AutomataflCoord SS1-SS1.5 for the STEP Head: headToExpr_evalStep (the lowered step gate = clean semantic sum, ARBITRARY n) + the incremental evalHStep_addLin/addProd/addConst/scale/append/foldl laws; oneHotStepN_of_sat (forall n: a satisfied n-wide Builder::one_hot over the STEP descriptor forces its selector VALUES into a genuine OneHotAt and pins the index into [0,n)), absorbing the REAL structural difference from RESOLVE (the step index gate folds Sum j*sels[j]! over List.range sels.length via getElem!, where resolve folds over zipIdx); coordNStep_of_sat/coordStepN_of_sat (forall n, subsuming the concrete coordStep_n3/n11 pair) and autoPinStepN_of_sat (forall n: the witnessed auto (AX,AY) is in [0,n) and old[AY*n+AX]=AUTO off Satisfied2 (automataflStepDescN n)). The ray-family heads (hit one-hot, dist, what) now compute SYMBOLICALLY (evalHStep_rayHitSum/rayDist/rayWhat) — the concrete unblock for the ray-of-sat wiring lane. NON-VACUOUS at n=3 (wrong auto cell REFUSED) + instantiated at the deployed n=11. #assert_axioms-clean.
import Dregg2.Circuit.Emit.AutomataflStepBackend -- LEG A, THE BACK END AT ARBITRARY n (the last Leg A residual). AutomataflStepRefine SS2.5 made the FRONT-END membership n-generic and the rays closed forall n (raycast_*_of_satN), but ~2000 lines of the BACK-END refinement were written against the ABSOLUTE n=2 column literals 58/105/152/209 with `by decide` over the concrete 418-constraint list. This file lands the forall-n TWINS (nothing in StepRefine rewritten). (1) n-GENERIC BACK-END STRUCTURED MEMBERSHIP over the parametric bases: mem_be_decideX/decideY/choose/step off NGen.backEndConstraints, decideAxisConstraints internals IN FULL at an ARBITRARY base b (variant/pos/the two size-3 what-alphabet one-hots/the six forced_ge0 guard blocks/the min gate/the nine-case truth table via casesTable + mem_decideCases), plus chooseOffset col/col_rule/ox/oy/sgt/xmove/ymove and the per-cell board-UPDATE gate mem_step_update -- all climbed with explicit mem_append_left/right/mem_map/mem_range. (2) THE COMBINATOR CLOSED FORMS that unpinned it: varsVal is multiplicative over ++, so evalHStep_forcedGe0Term (2*ib*d + ib - d - 1 for ANY inner head) and evalHStep_caseGateHead (assert_case's gate = prod(gate)*(field - formula) for ANY gate/formula) replace the n=2-only `.eval e.loc = <explicit sum> from rfl` rewrites, which only ever closed because every column was a numeral -- ONE lemma now covers all 36 case gates. (3) THE decideAxis CHAIN forall n: ge0N_of_sat (the forced_ge0 no-wrap heart at board-scale magnitudes, on forcedGe0_coreW -- the n=2 forcedGe0_core hard-codes |D| <= 100), oneHot3N_of_sat, da_gpd/gnd/lt/gt/le/gm_sound + da_min_sound, the nine decodeN_* cases restated at 1 <= dist (the StepRefine decode_* family takes `pd = 1 or pd = 2`, an n=2 distance alphabet), the nine daCase_* off the gates, and decideAxis_soundN = decodeDecision = evaluateAxis at an ARBITRARY base. CAPSTONE: decideAxis_x_soundN / decideAxis_y_soundN forall n off Satisfied2 (automataflStepDescN n), consuming AutomataflStepCapstone.rayN_of_sat for the distances (NOT assumed) and the ray's own member gate for the what-alphabet; instantiated at n=3 and the deployed n=11. NAMED WIDTH CEILING (surfaced, not papered over): decide_axis's six comparison gadgets are SMALL_RBITS=5 bits, so a legitimate trace at n >= 34 has NO satisfying decomposition -- the emitted decide_axis block is UNSATISFIABLE there. That is a satisfiability ceiling, not a soundness hole; the SAT=>SEM direction here needs only the no-wrap window, carried as the EXPLICIT hypothesis (n : Int) <= 10^6. #assert_axioms-clean. RESIDUAL for the next lane: chooseOffset / automatonOffset / the astep capstone (the membership they need is landed here; the refinement is not).
import Dregg2.Circuit.Emit.AutomataflStepChoose -- LEG A, THE BACK HALF AT ARBITRARY n: chooseOffset + automatonOffset. (1) ge0RN_of_sat = the forced_ge0 range gadget at an ARBITRARY bit width (the committed ge0N_of_sat hard-codes the FIVE-bit unrolling, so the 20-bit sgt/slt score compare was unreachable from it) on forcedGe0_coreG (a 10^8 no-wrap window; forcedGe0_coreW's 10^6 cap is BELOW 2^20), with bitSum_bounds proven by induction on the width. (2) THE SCORE ORDER EMBEDDING AND ITS CEILING: decisionCmp_gt_iff_scoreB proves the felt score comparison IS the reference decision order under a distance envelope <= 99, and score_embedding_fails_at_150 / score_embedding_collides_at_100 EXHIBIT the failure past it -- SCORE_ATT = 100 is a RADIX, so at a ray distance >= 100 the emitted choose_offset block picks the WRONG axis. Carried as the explicit hypothesis (n : Int) <= 99, not papered over. (3) the oy equality is a FOUR-FOLD push_f with no name in the emitter -- oyHeadN/oxHeadN NAME it, locate it (mem_co_oxEq/oyEq/slt) and evaluate it symbolically; sgtN/xmoveN/ymoveN/colPinN/oxN/oyN/offsetN_of_sat = the whole choose_offset block off the gates at A_CHOOSE_BASE n. (4) daScoreN = the SCORE-FIELD determination at an ARBITRARY base over all nine evaluate_axis cases (the n=2 xScoreEval/yScoreEval twins, via daCaseField_of_sat). CAPSTONES: offset_matches_chooseOffsetN (the decoded (ox,oy) IS Automatafl.chooseOffset of the two decoded axis decisions, UNCONDITIONAL -- the score-field determination is discharged, not assumed) and automatonOffset_of_satN (rays . decideAxis . chooseOffset composed: the offset columns ARE automatonOffset (boardDecodeN n e)), both forall n <= 99, instantiated at n=3 and the deployed n=11. Plus the leg-(5) FOUNDATION: step-block segment membership (mem_st_txlo/txhi/tylo/tyhi/tib/offnz/targVac/moved), ge0RN_mod_of_sat (the mod-aware gadget the edge guards need, since ox = p-1 makes ax+ox a large field value), offCardN_of_sat and offnzN_of_sat. #assert_axioms-clean. RESIDUAL (named, NOT asserted): astep_sat_imp_automatonStepN -- blocked on the n-WIDE row x column gated target read and the n-wide board-update fold (the n=2 chain writes both out as explicit 2x2 selector sums); the membership and the offset semantics they stand on are landed here.
import Dregg2.Circuit.Emit.AutomataflStepStep -- LEG A, LEG (5) AT ARBITRARY n + THE STEP CAPSTONE. The n=2 chain (tread_of_sat/seltarg_of_sat/boardupd_of_sat/cell_of_data) wrote the `read_rowcol_gated` target read and the board-update fold as explicit 2x2 SELECTOR SUMS (every column a numeral, every head evaluated `from rfl`). This file lands them at symbolic n: (1) THE n-WIDE GATED ONE-HOT / ROW-COLUMN DOT-PRODUCT PRIMITIVE -- evalHStep_idxGatedHead / evalHStep_readRowcolHead (closed forms for the two heads that only ever computed at numerals) plus oneHotGatedStepN_of_sat (gate=0 forces every selector to 0; gate=1 forces a genuine OneHotAt at the MOD-p pinned index -- mod-aware because ox = p-1). (2) THE STEP GUARDS forall n: tibN_of_sat (four forced_ge0 edge gadgets on ax+ox / ay+oy via ge0RN_mod_of_sat, then tib = their product), treadN_of_sat (tib=0 => read 0; tib=1 => the read IS old[(ay+oy)*n + (ax+ox)], the two one-hots collapsing the total range n x range n double sum), tcellValidN/targVacN (targ_vac = 1 IFF the target is VACUUM, alphabet off boardRangeConstraints), movedN/movedPartsN, and movedIffGuardN_of_sat -- the move mask m = offnz*tib*targ_vac IS the reference automatonStep guard on the decoded board. (3) THE BOARD UPDATE: selTargN_of_sat (the m-gated sel_target one-hots), autoSelN_of_sat, boardupdN_of_sat (the per-cell equality at SYMBOLIC c, off mem_step_update). CAPSTONE astep_sat_imp_automatonStepN: on a satisfying canonical trace the decoded NEW board IS automatonStep (boardDecodeN n old), CELL-WISE over range n x range n, at ARBITRARY n, plus the automaton-location and size conjuncts. STATED CEILING: n <= 99 (the SCORE_ATT radix collision, defect #9, inherited through automatonOffset_of_satN) + the coordinate no-wrap window; instantiated at n=3 and the deployed n=11. #assert_axioms-clean.
import Dregg2.Circuit.Emit.AutomataflResolveMovesCapstone -- LEG R at n=11, the roundStep-faithful RESOLVE CAPSTONE: resolve_sat_imp_roundBoard11 (codeToParticle(cMidV4) IS the AutomataflRules.roundStep board this round -- old on a clash, the VALIDATED resolveMoves cell otherwise, UNCONDITIONAL) + roundStep_pair_outcomeBoard (the reference reduction: re-enter old on clash, resolve via automatonStepCfg cfg (resolveMoves ..) otherwise). Was an ORPHAN not reachable from this root; wired in here (transitively, via AutomataflTurnCapstone) so CI covers the roundStep capstone. #assert_axioms-clean.
import Dregg2.Circuit.Emit.AutomataflTurnCapstone -- THE WHOLE TURN as ONE theorem at the deployed n=11: turn_sat_imp_roundStep composes Leg R (resolve_sat_imp_roundBoard11: cMidV4 = the roundStep resolve board) and Leg A (astep_sat_imp_automatonStepCfgN: new board = automatonStepCfg .column of the step's old board) over the mid-board SEAM into codeToParticle(new) = outcomeBoard(roundStep .column g (openRound bR seats) [ma,mb]).cellAt, on a CLEAN round (clashCoords = []), via automatonStep_congr + roundStep_pair_outcomeBoard. The seam is carried as the fold-enforced board agreement (hseamCell cells + hseamAuto automaton); seamAuto_of_pi discharges the automaton half CRYPTO-FREE from the two EMITTED AUTO .piBindings + fold PI-equality; seamCell_of_cMidV4_pack discharges the cell half crypto-free via seam_of_pack_congr (pack_injective_modp, no modular collision) given Leg R's cMidV4 pack transport; turn_sat_imp_roundStep_pi threads all into one PI-carried statement. COMMITMENT NOW PACKS cMidV4: the resolve commitment (commitBoardsConstraints MID window) packs NGen.cMidV4 (the third-wound-fixed FINAL board the capstone characterizes as the roundStep resolve board), range-checked by assert_member(cMidV4,{0,1,2,3}); so resolve_midPack_pi_of_sat is EMITTED (PI[16+fc+j]=pack(cMidV4)) and turn_sat_imp_roundStep_pi DISCHARGES hRmidPack from it -- the whole turn takes ONLY the fold PI-equalities (no hash, no chip soundness, no un-emitted ingredient). The V2 resolve_step_sat_imp_applyTurn (applyTurn/resolveMid, NN=2) is RETIRED, superseded by this. Clash branch = re-entry with old board (no step); the completed-round theorem is the clean case. #assert_axioms-clean.
import Dregg2.Circuit.Emit.AutomataflResolveMarksCapstone -- ⚑ M6 OF THE MULTI-ROUND BRAID — LEG R FOR A CLEAN ROUND OF A MULTI-ROUND TURN (docs/reference/AUTOMATAFL-MULTIROUND-BRAID-DESIGN.md §4.3/§9.3/§10, the B3 crux). The clean round consumes an ACCUMULATED RoundState (marks != []); the moves must be MoveLegal against those accumulated marks. DECISION: RE-CHECK (not INHERIT) — round N's fresh moves passed NO Leg C gate (each clash round adjudicates its OWN round; N is not a clash), and the seam carries the marks VALUE not the fresh moves' legality against it, so Leg R itself must re-check `frm,to notin marksIn` — made a DESCRIPTOR THEOREM (resolveMarksMoveLegal), not assumed. KEY OBSERVATION exploited: resolveMoves is MARKS-INDEPENDENT, so the emitted cMidV4 board is unchanged and the board half REUSES resolve_sat_imp_roundBoardN verbatim via resolveMarks_restrict (Satisfied2 (marks desc) -> Satisfied2 (resolve desc); the appended marks tail carries NO memOp/mapOp/hash so every leg transfers). ADDITIVE: automataflResolveMarksDescN n = resolveConstraints n ++ marks tail (marksIn commitment marksCommitFamilyAt + dest one-hots + the frm,to-notin-marksIn legality gate) at fresh columns above R_WIDTH. LANDS: resolveMarks_marksIn_pi_of_sat (the base-4-injective marksIn WINDOW carry, closing the RoundState seam to Leg C's marksOut, hash-free); resolve_sat_imp_roundBoardMarksN (n=11 + n=2): cMidV4 IS roundStep's clean-round resolve board at accumulated marks; rmRoundStep_marks_outcomeBoard (reference reduction, hfresh DISCHARGED via resolveMarksMoveLegal + hseat0); turn_sat_imp_roundStepMarks_pi: turn_sat_imp_roundStep_pi generalized OFF marks=[] (no hfresh hypothesis; only who-routing hseat0 remains, the Leg-S residual per §9.2). P=2 scope (LEGC_SEATS=2: a pair clash re-enters both seats => clean round moves are both fresh, locked=[]). #assert_axioms-clean {propext, Classical.choice, Quot.sound}.
import Dregg2.Circuit.Emit.AutomataflResolveMarksGolden -- the BYTE-PINNED resolve-marks wire goldens: emitVmJson2 (automataflResolveMarksDescN 11) (1453w/45pi/1817c, the deployed clean-round leaf) and (automataflResolveMarksDescN 2) (460w/21pi/566c). Isolated in its own leaf for the same reason AutomataflLegCGolden/AutomataflNGenGolden are (large mechanical strings); imports read-only. automataflResolveMarksDescN computes its name from n, so these are the descriptors themselves (no .name-patched wrappers). Registered in EmitByName.lean + on disk under circuit/descriptors/by-name/ (M6->deploy landing, with the Rust witness generator resolve_marks_witness.rs).
import Dregg2.Circuit.Emit.AutomataflStepMarksCapstone -- ⚑ M6′ (the clean sub-chain UNIFORM-WIDTH landing): LEG A (the AUTOMATON STEP) carrying a FROZEN MARKS WINDOW. The clean round RM->A was mixed-width (RM.IN 20-lane [board||marks||auto], A.OUT 11-lane [board||auto]); automataflStepMarksDescN n = automataflStepDescN n VERBATIM + one marks window (marksCommitFamilyAt at fresh columns above A_WIDTH_N n / PIs above A_PI_COUNT_N n) makes A.OUT 20-lane too, so the nested sub-root [RM.IN(20)||A.OUT(20)] is symmetric. KEY OBSERVATION: automatonStepCfg is MARKS-INDEPENDENT, so the emitted `new` board is UNCHANGED and the step capstone REUSES verbatim on the RESTRICTED trace via stepMarks_restrict (Satisfied2 (marks desc) -> Satisfied2 (step desc); the appended marks window carries NO memOp/mapOp/hash/range so every leg transfers). LANDS: astep_marks_sat_imp_automatonStepCfgN (n=11 + n=2: the decoded `new` cell IS automatonStepCfg .column, off the marks descriptor); stepMarks_marksIn_pi_of_sat (the base-4-injective marksIn WINDOW carry, hash-free, closing the seam to RM's marksOut); stepMarks_marksOut_eq_marksIn (the FREEZE: the step publishes the window ONCE, so marksOut reads the same PIs as marksIn -> marksOut = marksIn by construction, exactly as M6's resolve-marks freezes marks). Simpler than resolve-marks: no legality re-check (that already happened at RM); the step only CARRIES marks. #assert_axioms-clean {propext, Classical.choice, Quot.sound}.
import Dregg2.Circuit.Emit.AutomataflStepMarksGolden -- the BYTE-PINNED step-marks wire goldens: emitVmJson2 (automataflStepMarksDescN 11) (810w/47pi/1213c, the deployed clean-round automaton-step leaf) and (automataflStepMarksDescN 2) (261w/23pi/415c). Isolated in its own leaf (large mechanical strings); imports read-only. automataflStepMarksDescN computes its name from n. Registered in EmitByName.lean + on disk under circuit/descriptors/by-name/ (M6′->deploy landing, with the Rust witness generator step_marks_witness.rs).
import Dregg2.Crypto.VpaAsCert         -- THE VPL RUNG: visibly-pushdown / nested-word acceptance as Hypergraph.Cert R_vpa (symbol-class-driven push/pop). three_rungs_share_substrate : regex(delta) ⊗ VPL(R_vpa) ⊗ CFG(Produces) all ONE Hypergraph.bridge. stack_height_input_determined : stack height is a function of the INPUT WORD ALONE (the root VPL property the DFA/CFG rungs LACK — source of boolean closure + determinizability + decidable equivalence). Honest (per DESIGN-visibly-pushdown-reframe): NAMES the uniqueness wall as the visibly-nested boundary, does NOT dissolve it; the one real win = decidable template equivalence (named, EXPTIME, not yet proven). #assert_axioms-clean.
import Dregg2.Crypto.HandlebarsGuardedWitness -- THE ATTESTED SCHEMA INSTANCE: render emits a per-hole guard-acceptance certificate (HoleEntry id guard data + the executable `derives` run). guardedVerify RE-CHECKS each hole by re-running the verified matcher (no trust); guardedVerify_iff decides guardedSafe exactly; guardedWitness_sound : an accepting re-check PROVES render ∈ language. This IS the cheap regular-leaf check of the composed-attestation architecture, per slot. Demo: the {{-bearing render certified, a guard-violating tamper REJECTED. #assert_axioms-clean.
import Dregg2.Crypto.HandlebarsGuardedBound -- CLOSES the guardedVerify scope hole: guardedVerifyBound T output witness consumes the TEMPLATE and the OUTPUT (rejects a witness whose hole-id list ≠ holeIds T.segments, checks data against T's OWN guards not the witness's declared ones, and requires render T (witnessAssign witness) = output), carrying the theorem the old one lacked — guardedVerifyBound_sound : accept ⇒ ∃ d, guardedSafe T d ∧ render T d = output, for an ARBITRARY prover-supplied witness. Both attacks the retraction described (a witness for a DIFFERENT template; a witness unrelated to any output) are exhibited accepted-by-old / rejected-by-new.
import Dregg2.Crypto.HandlebarsGuardDecision -- ⚑ THE TEMPLATER'S GUARDS ARE DECIDABLE, and the tower catches real refactor bugs. The deployed noDoubleBraceRE is IN the cheapest cover (IsSymbolic/RigidFull; the atom that blocks the interval/difference covers is a SYMBOL PIN, which the base minterm cover reaches exactly) and SATISFIABLE (11 ≅-classes); its contradictory variant decides EMPTY at all lengths and is INDEPENDENTLY hand-proven (machine and theorem agree). FIVE spellings decided: 3 EQUIVALENT (incl. a complement-free machine, a genuinely different automaton) and 2 REFACTOR BUGS caught NOT-equivalent, with separating words exhibited ([data,{,{] and [{,{,data] — rejected by the deployed guard, ADMITTED by the weakened ones). Cashed into templater Props: bad_edit_admits_injection (a concrete {{-bearing datum the refactored template calls SAFE), brickT_unrenderable/brickT_no_accepting_certificate. ⚑ CORRECTS RealGuardAudit2 row #9: templater guards are non-Pred only on the RUST byte/span side — the LEAN guarded templater's guards are PredRE over Value and in-fragment. 18 keystones #assert_all_clean. ⚑ AND THREE VERDICTS ARE NOW PROVEN Props, not #guards: pos_langEq (∀ w, derives w noDoubleBraceRE = derives w nodbPos — the complement-free spelling equivalent to the deployed brace-ban at EVERY word length over the infinite Value alphabet, the one verdict with no hand proof), noTail_not_langEq and exactly_not_langEq_fix (both refactor bugs), plus pos_edit_preserves cashed at the template level. The crossing was MEASURED, not feared: +0.7s CPU / +0.11GB for one reduction, +2.1–2.4s / +0.16–0.19GB for all three — the file's earlier '64GB/20min hazard' claim was ASSERTED-NOT-MEASURED and wrong by ~2.5 orders of magnitude, now retracted in-file with the real numbers and a discrimination canary (a deliberately false variant is REJECTED by rfl).
import Dregg2.Crypto.Deriv.RealGuardDecisionLift -- ⚑ 18 of 24 REAL dregg guard verdicts LIFTED from #guard (compiled Bool, proves no Prop) to kernel-reduced THEOREMS, and THE DUPLICATE-GATE QUESTION IS ANSWERED: dialogue.rs writes the LN_ASK_HISTORY availability rule TWICE — executor StateConstraints (:274-283) and renderer plain Rust (:438) — and they are PROVEN to accept exactly the same words at EVERY length over the infinite Value alphabet. A no-drift result on real shipped code, not a test. Costs measured (the retracted 64GB/20min claim falsified again at this scale).
import Dregg2.Crypto.HandlebarsGuardedCompose -- SCHEMA-IN-SCHEMA: composition = boolean guard refinement. templateRE (a guarded template's language AS a PredRE); guardedCompose replaces hole h's guard g_h with (g_h ⋒ templateRE T') — nesting = PredRE.inter. guardedCompose_guardedSafe's hole-h step IS derives_inter (outer guard AND inner render both fire); guardedCompose_render_mem_language = nesting-soundness. Demo pins that inter refines BOTH guards (rejects {{ breakout AND too-short). #assert_axioms-clean.
import Dregg2.Crypto.HandlebarsGuardedUniqueness -- THE INVERSE, guard-parametric: guarded_render_injective (unique data recovery) for a SEPARATED-guard class (Excludes g c := no g-word contains c; a hole's guard excludes its following delimiter). NoBrace is the coarsest instance (noBraceRE_excludes braceVal). abutting_ambiguous PROVES general-guard uniqueness FALSE (two star-any holes, distinct data, same output — the honest wall, demonstrated). #assert_axioms-clean.
import Dregg2.Crypto.HandlebarsGuardedParse -- THE VERIFIER-SIDE HALF (discharges the round_trip residual, guarded side): parse_exists_distinct_holes — output ∈ (guardedToGrammar T).language → ∃ d, guardedSafe T d ∧ render T d = output, for (holesOf T).Nodup. A party holding only (T, output) may conclude the output IS an expansion of T under its guards. Route: GuardedSpans.mem_language_iff_spans → the computable extractor `assign` reads d off the spans. Composed with guarded_render_injective this is the stack's first REAL ∃! (separated_parse_exists_unique, over the ordered hole-data vector — a raw ∃! d is false off holesOf T). Both side-conditions PROVEN load-bearing: nodup_is_load_bearing (a separated template naming one hole twice has language members NO render produces) beside the landed abutting_ambiguous. Demo: the output `data { { {` presented with no assignment in hand, and the recovered d COMPUTED (hole 0 ↦ [data], hole 1 ↦ [{,{]). #assert_axioms-clean.
import Dregg2.Crypto.DfaEncodingWeld    -- THE Value->Nat WELD (closes composed-architecture Gap B): weld_matches / air_finalState_decides_matches prove the CRYPTO-BOUND AIR-side TableDfa (the one route_commitment_binds_trace binds) word-decides EXACTLY Matches R, transported through the Value↪ℕ (+PredRE-state↪ℕ) encoding. The Value→ℕ half is CONCRETE (reuses Poseidon2Binding.encV, proven-injective). Transport is encoding-AGNOSTIC (consumes only injectivity). Residual: explicit injective state-numbering (existence via der_finite) + Gap A powerset-table-equality (off critical path). #assert_all_clean (8 keystones).
import Dregg2.Crypto.SlotConfinement    -- SLOT-CONFINEMENT (attested-field, derivative-matcher view): slot_confinement — a {{-free disclosed field (the zkOracle injection-free leg, InjectionFree/neg injectionTemplate over List Value) rendered into a template slot preserves the template literals' control-token structure VERBATIM (contributes ZERO {{); both-polarity non-vacuity; propext-only. The whole-output CFG-membership view is Dregg2.Crypto.Handlebars (the authoritative template type). NOT an LLM-prompt-injection defense (dregg's LLM path uses structured Messages).
import Dregg2.Crypto.DecoUnforgeable  -- RUNG 4: DECO authenticity PROVEN unforgeable (survey gap #1). F_attestation (ideal functionality, modelled on F_LC) + the game AttForgery/AttUnforgeable + the REDUCTION forgery_yields_break (a forged attestation ⟹ a concrete ed25519 SigForgery / HMAC MacForgery) ⟹ deco_attestation_unforgeable under STANDARD floors (ed25519 EUF-CMA + HMAC + Poseidon2 CR + STARK extractability = the deco_binds_payment trust base, NO dregg-specific assumption). deco_attestation_realizes = deco_authenticates_payment re-read as AttRealizes. Both-polarity non-vacuity: attestation_fires (a genuine reference attestation IS Authenticated + verifies) / Forge.attestation_bites (a forge kernel admits a concrete AttForgery whose reduction extracts a genuine SigForgery). Upgrades zkOracle's authentic leg from "∃ satisfying trace" to "F_attestation emitted this". #assert_axioms-clean.
import Dregg2.Crypto.DecoUC            -- RUNG 5 (the summit): DECO attestation UC-REALIZATION. The SIMULATOR (decoSimTranscript — a witness-free transcript that WORKS: satisfies the relation + the verifier accepts, decoSim_works) + the PERFECT/statistical ZK fragment (decoView_witness_free/decoView_indep — the disclosed view factors through the statement; decoLeaky_no_simulator the teeth) + the STATIC soundness half (deco_attestation_realizes, rung 4) assembled in DecoUCRealization; the computational ≈_c layer (STARK-ZK, handshake-sim, PPT, negligible-advantage, composition) CARRIED as named Prop carriers (never axiom), discharged cross-system (DecoUCComputationalDischarge). ⚑ The fully-computational apex is NAMED-not-faked: F_attestation is NOT in the CryptHOL F_com harness (needs a from-scratch spmf model of STARK-ZK + the DECO handshake) — the honest STOP finding in the header. UCRealizesFAtt FALSIFIABLE: ref_ucRealizes (holds) / forge_not_ucRealizes (a broken sim is not a realization). #assert_axioms-clean.
import Dregg2.Crypto.Hypergraph        -- arbitrary hypergraph reductions: bridge ((∃ chain, Cert R) ↔ ReflTransGen R, ANY reduction relation) + hypergraph_reduction_bridge (hyperedge replacement) + cfg_parse_via_reduction (CFG = the linear/string instance)
import Dregg2.Crypto.GraphRewrite      -- contextual hyperedge replacement: IsHom/Matches/Embeds, Rule + match-driven RewriteStep + graphRewrite_bridge; DPO freshness/dangling are explicit follow-ons
import Dregg2.Crypto.GraphRewriteHistory -- ROOT-LINKED SEMANTIC REWRITE HISTORIES: public (session,rulesRoot,index,oldRoot,newRoot) receipts + hidden sound RewriteStep openings; wrong session/rules/index/root refuse. ⚑ SPLIT (07-24): the bare `_or_collision` disjunctions are DELETED — pure core reduces_of_adjacent_agree/has_cert_of_adjacent_agree (no hash) + historySplice_binds_rom (the splice as a RomForgery over the sampled graph-commit oracle, mapOut extractor, keyedRom_hard floor) + const-answer defanged/excluded teeth; linked_reduces = strength-bridge ∘ pure-core. 19 clean.
import Dregg2.Crypto.PrivateGraphRewriteDescriptor -- LEAN-AUTHORED HIDING GRAPH REDUCTION: bounded injective match-driven replacement over private rules/graphs, faithful eight-lane old/new/ruleset roots, emitted IR2 soundness into the generic RewriteStep semantics, and collision-explicit binding. Rust only fills the witness for this descriptor.
import Dregg2.Crypto.HierarchicalGraphCommitment -- depth-two faithful graph root; local leaf/interface/redex openings; same-path ancestor updates; session/index occurrence wrapper; duplicate occurrence consumption refuses. ⚑ REBUILT (07-24): the `_or_collision` chain (digest/pair/genericRoot/semanticRoot) is DELETED — the exported binding is semanticRoot_binds_rom (the two-level tree at a role-keyed sampled oracle, 12-query bindComp tree-walk extractor, keyedRom_hard floor) + graph-forgery bridge + defanged/excluded teeth. 22 clean.
import Dregg2.Games.PrivateQuestGraphDescriptor -- GAME-SPECIFIC HIDING REDUCTION: specializes the reusable graph AIR by pinning all 64 private rule columns to the Warden quest's two authored reductions; proves specialization implies the generic relation, so a producer cannot substitute an easier hidden ruleset.
import Dregg2.Crypto.PrivateGraphRewriteCellDescriptor -- CUSTOM-CELL DOOR: prepends the mandatory old/new state-root prefix to the same graph relation and publishes its semantic output root for the fold's post-state field weld.
import Dregg2.Protocol.WorkflowGuard  -- Workflow's authz/order/attest gates as Spec.Guard instances; 3 refinement ↔s + whole-step Guard.all equivalence + exec⇒admits bridge + discriminating #eval
import Dregg2.DSL                      -- `dregg_program {…}` cell-program eDSL → RecordProgram; counter/escrow elaborate by rfl; #eval admit/reject
-- ─── Full op-set, eDSL-C, state migration, circuit gadgets to wire, Temporal/Stingray ───
import Dregg2.Exec.TurnExecutorFull    -- full op-set executor: FullAction = balance | delegate/revoke | mint/burn; execFull_attests = StepInv per kind + execFullTurn_ledger transaction-level conservation
import Dregg2.Exec.IssuerMove          -- W1 VALUE UNIFICATION (LANDED): mint/burn AS ISSUER-MOVES over the real kernel (DREGG3 §2.2). AssetId := CellId; the LIVE recKMintAsset/recKBurnAsset ARE issuerMoveK/issuerBurnK (rfl receipts recKMintAsset_is_issuerMove/recKBurnAsset_is_issuerBurn); authority over the ISSUER, negative-capable well, NO availability gate at the well; TEETH: the LEGACY supply-increment laws (recKMintAssetLegacy/recKBurnAssetLegacy) provably BREAK ExactConservation + genesis-order fail-close + issuerMint_pointwise_vs_credit (the real-step commuting relation)
import Dregg2.Exec.ReachableConservation -- W1 KEYSTONE: Σ_c bal c a = 0 as a REACHABILITY INVARIANT — reachable_total_zero: every state reachable from a value-empty genesis has EVERY asset's total identically ZERO (ledgerDeltaAsset ≡ 0: no non-conserving verb is left in the kernel); the issuer wells carry −supply, the books always close; #guard trajectory mint→transfer→burn with Σ=0 at every prefix + the issuer-authority/genesis-order refusal teeth
import Dregg2.Exec.CrossCellForest     -- cross-cell nested forest: crossForest_no_amplify + crossForest_conserves (N-ary cross-cell Σ=0) + crossForest_attests; bilateral = Fin-2 slice; overlapping-cells OPEN
import Dregg2.Exec.FullForest          -- FullActionA call-forest (wholesale-swap keystone): execFullForestA = recursive all-or-nothing tree over the full per-asset op-set, proven equal to execFullTurnA over the pre-order lowering; execFullForestA_conserves_per_asset (conservation vector, not scalar) + execFullForestA_no_amplify + execFullForestA_each_attests; cross-target routed to CrossCellForest
import Dregg2.Exec.Handler             -- ★ the EFFECT-HANDLER ALGEBRA scaffold (swap-grade executor foundation, DESIGN-EFFECT-HANDLER-ALGEBRA.md): EffectHandler bundles step/delta/auth/admission/trace + FORCED obligation proofs (auth_gated/admission_gated/conserves) so a handler can't register without its gates (R1-closure is a TYPING condition); Registry=List PackedHandler coproduct replaces the closed FullActionA; turn_conserves = ONE generic foldlM induction (the 56-arm ledger matrix collapses). 3-effect proof-of-approach slice; the 56-effect migration is the staged batches below
import Dregg2.Exec.Handlers.StateSupply -- migration batch 1: mint/burn handlers (the delta≠0 path — turn_conserves sums non-zero deltas) + createCell/factory/spawn (account-growth neutral) + state/write handlers; CLOSES R1/R6 (acceptsEffects Live-gate wrap the bare kernel ops lack)
import Dregg2.Exec.Handlers.Escrow      -- migration batch 1: createEscrow/release/refund/obligation/note handlers; CLOSES R2 (releaseSettleAuthB/refundSettleAuthB = authorizedB over recipient/creator — the bare releaseEscrowKAsset takes only id, no actor); double-spend gate; conserves via the proved escrow combined-conservation lemmas
import Dregg2.Exec.Handlers.Authority   -- migration batch 2: delegate/introduce/validateHandoff/attenuate/revoke/dropRef handlers; KEY FIX — delegation re-routed from rights-BLIND recKDelegate to ATTENUATED recKDelegateAtten (granted⊆held, non-amplifying); revoke/attenuate = total self-limiting (post caps ⊆ pre); delta=0
import Dregg2.Exec.Handlers.Bridge      -- migration batch 2: bridgeLock/finalize/cancel/pipelinedSend handlers; CLOSES R12 (bridgeFinalize.delta = -amount, the disclosed cross-chain OUTFLOW correctly accounted, NOT falsely Conservative) + creator-authority wrap (bridgeAuthOK; the bare op has no actor arg)
import Dregg2.Exec.Handlers.Exercise    -- the LAST handler: exerciseA recursive sub-effect forest; CLOSES R4 (facet-mask — inner effects run under the cap allowed_effects, not the inner-actor's full authority); fuel-bounded structural recursion (NO heartbeat pathology), conserves via subTurn_conserves (the turn_conserves twin); closedToSub drops the proved scaffold handlers into the inner forest. All 56 effects are now sound handlers
import Dregg2.Exec.Handlers.Lifecycle   -- migration batch 3: cellSeal/unseal/destroy + refreshDelegation + emitEvent handlers; REAL side-table semantics (`lifecycle`/`deathCert`/`delegations`, not field-write stubs); aligns handler algebra with execFullA + circuit specs
import Dregg2.Exec.HandlerExecutor      -- ★ THE CUTOVER KEYSTONE (additive; live-switch is mechanical): masterRegistry packs all 47 proved handler entries; toClosedEffect : FullActionA→ClosedEffect maps all 56 ctors (aliases collapse); execHandlerTurn = the registry executor over RecChainedState. execHandlerTurn_conserves = the DERIVED global per-asset conservation, LIFTING the generic turn_conserves (the 56-arm execFullA_ledger_per_asset matrix collapses into ONE lift) + gate companions (head_authorized/_admitted). handler_refines_execFullA_{transfer,release,stateWrite} = the SOUND-STRENGTHENING relation (handler-commits ⊆ execFullA-commits, kernels agree). TEETH: a transfer/state-write into a Sealed cell (R1/R6) + an escrow release by a stranger (R2) — execFullA=some (the live hole) vs execHandlerTurn=none (the algebra closes it)
import Dregg2.Exec.HandlerFloors        -- ★ P0 of the floor-obligation campaign: the UNIFORM Prop-valued FloorObligation surface — a named floor = a Prop the handler's step satisfies on every commit, bundled WITH its proof (ill-typed until discharged). ofBoolGate lifts the existing auth/admission/reserved Bool gates; the Prop shape ALSO takes the relational floors (nonce-monotone) the research flagged. PoC instances PROVEN: reservedFieldFloor (Bool-gate, via stateStepDev_notReserved) + nonceMonotoneFloor (RELATIONAL, via incrementNonceStep_advances) — settling the key risk: ONE surface, both floor kinds. *_discharges = the hnr/hmono side-hyps SUPPLIED from the commit (the silent-gate hole retired). #assert_axioms-clean
import Dregg2.Exec.FullForestAuthPortal -- real §8 AuthPortal: RealAuthPortal bundles ed25519/STARK/HMAC floors; portalVerifyReal routes each Authorization variant to its own oracle; realAuthPortal.soundness = ed25519.unforgeable ∧ STARK.extractable ∧ HMAC.unforgeable (not True, not one shared collisionHard); per-variant security theorems take the carrier as an explicit hypothesis; unchecked_arm_rejects + no_reachable_arm_is_trivially_true (anti-vacuity); #assert_axioms-clean
import Dregg2.Exec.FullForestAuth      -- executed credential+caveat auth gate on the call-forest: gated tree FullForestG with NodeAuth (10-variant Authorization + tiered caveats + macaroon chain); gateOK = credentialValid (§8 AuthPortal) ∧ capAuthorityG (granted≤held verified) ∧ caveatsDischarged; execFullForestG_conserves_per_asset/_no_amplify derived via eraseG; gatedNode_check_eq_use = no TOCTOU; non-vacuous (forged cred, false caveat, and per-asset launder all yield none)
import Dregg2.Exec.GatedForestCfg      -- first-class `GatedForestCarriers` bundle + `starbridgeCarriers` instance (replaces pinned Demo/Production namespace)
import Dregg2.Exec.CellExecutor        -- `CellExecutor.production` (gated starbridge) + internal `kernelForest` for erasure reuse
import Dregg2.Exec.SigningMessage      -- byte-exact signing-message preimages (guards the §8 AuthPortal's opaque `stmt` Digest), ported from dregg1 (authorize.rs:1713–1880): 6 preimage builders (sigMsgFull/Partial/Custom/Stealth/Bearer/HandoffCert) + domain-separator theorems (sigMsg*_hasPrefix, domainSep_injective — 15 pairs distinct) + binding teeth (tampered field ⇒ different preimage); #assert_axioms-clean, non-vacuous (#eval tamper ⇒ false)
import Dregg2.Exec.Admission           -- fail-closed admission prologue in front of the FullForestAuth kernel fold (admission ≠ kernel): `admissible` = 8 host-fed gates (expiry + agent-existence + nonce-replay + fee-coverage + write-set/agent freeze + receipt-chain self-binding + Stingray budget); `commitPrologue` (fee-debit + nonce-tick) is never rolled back — prologue_survives_failed_body proves fee−balance ∧ nonce+1 even on body failure (anti-DoS + replay-closed); pure_fold_loses_prologue proves the naive all-or-nothing bind discards the prologue; #assert_axioms-clean; #eval non-vacuity
import Dregg2.Exec.TurnAdmission        -- devnet-shaped entry: `runGatedForestTurn` = `Admission.runTurn` ∘ `FullForestAuth.execFullForestG` (prologue survives gated-forest body failure; inadmissible turns rejected with no edit); #assert_axioms-clean
import Dregg2.Exec.AdmissionWire       -- write-set extraction + `TurnHdr`/`AdmCtx` builders for the FFI admission seam
import Dregg2.Exec.HostCorrespondence  -- AssuranceCase boundary seam §2 DISCHARGED: the host-fed `ShadowHostCtx` admission inputs. `HostFacts` = the node's TRUE clock/freeze-set/stored-head/budget; `Reflects` = faithful per-field reflection (freeze only on the turn's READ cells = the marshalling fall-off); `admissible_sound_of_reflects` (CONDITIONAL SOUNDNESS: faithful ctx ⇒ gate verdict = verdict against true facts, gate adds no error) ⇒ §3 teeth transport to true facts (reflects_rejects_true_fork/over_budget/frozen); the producer OBLIGATION as TEETH (stored_head/budget/freeze/clock_obligation_teeth: each unsafe under-report ADMITS a true-reject); `marshalled_admission_sound` END-TO-END over the actual wire-built ctx, residual obligation = producer-coverage (every freeze-read cell gets a wire id); #assert_axioms-clean; both-polarity #guard
import Dregg2.Apps.OrbitalScreen       -- continuous-time-sound orbital screen: screen_clear_imp_continuous_clear (a `clear` verdict holds at every continuous time, not just samples; between-samples closest approach covered) + crossing-pair teeth; coarse Lipschitz fallback for speed-bounded trajectories; curvature term OPEN
import Dregg2.Apps.WhoYields           -- graph-symmetry who-yields bridge (computable WL color-refinement): rigid_of_discrete (asymmetric ⇒ forced role) + symmetric_needs_negotiation (tie ⇒ negotiation) + three_mutual_conflict_needs_three_roles + outOfFuel_breaks_symmetry
import Dregg2.Apps.EpistemicSheaf      -- constellation as a sheaf of verifiers: consensus_on_clearance (H⁰ = global section = honest distributed knowledge of a screened fact) + byzantine_section_does_not_glue (witnessed non-gluing obstruction); cohomology-as-object is POETRY (honest)
import Dregg2.Apps.SheafHyperedge      -- THE BRIDGE (DREGG4-HYPERSYSTEM §5.2 realized): the two dregg4 complexes welded into ONE theorem. Instantiates EpistemicSheaf.Frame AT a Hyperedge's incidence set (legs = agents, worlds = candidate global states ι→TurnId, ∼ᵢ sees only leg i's component, apex tid = the proposition apexKnown). agreement_is_distributed_knowledge (and legs_agree_is_distributed_knowledge, routed LITERALLY through Hyperedge.legs_agree) DERIVES DistKnows F.Honest (apexKnown H.tid) F.actual from H.agree — "Agreement = distributed knowledge" as code, not commentary. The fork direction: distributed_apex_forces_agreement + fork_has_no_distributed_apex connect a hyperedge NON-GLUING (legs with no shared apex) to the H¹ obstruction (a real Hyperedge can't fork — the apex forces legs_agree — so the fork is where the hyperedge is NOT; the missing H⁰ global section). Non-vacuous both polarities (#guard: agreeing legs ⇒ DistKnows via a real ringHyperedge; disagreeing forkObs 5≠99 ⇒ no apex, the epistemic shadow of opA/opB_byzantine); 7 keystones #assert_all_clean. Simplicial-OBJECT (face maps ∂ᵢ, non-Kan) named-next per §8.2
import Dregg2.Apps.ConservationBridge  -- conservation_is_flow_balance: JointCell Σδ=0 (committed avoidance maneuver) = conjunction graph's flow-balance across the symmetry boundary; multi-edge generalization OPEN
import Dregg2.Apps.RightOfWay          -- verified collision-avoidance referee: referee_sound (adversary-proof) + referee_sound_physics (committed ⇒ clear on the whole continuous step, via OrbitalScreen) + outOfFuel_cannot_burn + forced_trade_excludes_naive + collisionSafety_must_escalate
import Dregg2.Apps.AgentOrchestration  -- verified multi-agent orchestration ("the orchestration is a theorem"): least-privilege attenuated sub-agent delegation (worker_authority_subset_orchestrator, strict write-drop) + real per-asset transfers conserve (workForest_conserves) + out-of-scope worker fail-closed (badWorkerForest_fails_closed = none) + escrow combined-conservation (escrowLock_combined_conserves) + credential+caveat auth gate (committed ⇒ credential ∧ caveats; forged-cred & false-caveat denied) + whole forest certified (orchestration_conserves ∧ _no_amplify ∧ _each_attests); #assert_axioms-clean
import Dregg2.Apps.SwarmSignal         -- THE NOTIFY DEMO: verified async swarm-signal coordinator (NOTIFY-CASCADE.md §demo-app-design) — a coordinator NOTIFIES workers under ATTENUATED badge-masked notify caps (the async --wake the metatheory lacked; AgentOrchestration's async sibling). Runs on Step-1 NotifyAuthority ALONE (no core Auth.notify, no felt, no VK). 6 teeth both polarities, all reusing Step-1's signalGated/attenuateNotify: coordinator_wakes_worker_commits (OR's exactly the held badge) + worker_cannot_widen_mask (no-amplify, REFUSE) + worker_cannot_poke_peer (no cap targets the peer, structural) + worker_cannot_signal_back (no cap targets the coordinator + notify-cap NEVER confers Reply/Call) + out_of_mask_signal_refused (fail-closed) + attenuation_shrinks_admitted (the keystone signalAdmissible_attenuate_no_amplify); wake_is_balance_neutral (writes a badge accumulator, not the ledger). HONEST: the teeth demonstrate AUTHORITY containment, NOT information containment — the carried badge-OR covert-channel risk (a notify cap is never info-free) is flagged in the module doc, not laundered. #assert_all_clean, #guard non-vacuity both polarities
import Dregg2.Apps.Compositor          -- THE VERIFIED COMPOSITOR (DREGG-DESKTOP-OS §5/§7 R0): output-integrity = unfoolability on the scene — the compositor IS a dregg cell whose state is the scene graph; a present() is a caveat-gated turn whose admit-predicate folds T1 NON-OVERLAP (granted⊆held region-set, overpaint UNSAT) ∧ T2 LABEL-BINDING (label = fn of owner+sourceStateRoot, spoof UNSAT) ∧ T3 FOCUS-EXCLUSIVITY (at-most-one focus + input routes only to it, double-focus/misroute UNSAT). output_integrity_eq_unfoolability_on_scene (commit-iff-admit via VerificationToolkit) + 5 rejection teeth + conserves/no_amplify; #guard teeth BITE on a genuine two-surface scene (honest COMMITS, overpaint/label-spoof/input-steal/double-focus REJECT) both polarities; F1/F2/F3 graphics frontier honestly NOT claimed; #assert_axioms-clean
import Dregg2.Firmament.CapGradation    -- THE FIRMAMENT CAP-GRADATION, formal Lean semantics of sel4/dregg-firmament/ (DREGG-DESKTOP-OS §L2, FIRMAMENT.md §3): ONE Capability{target,rights} handle with Target = local{slot} | distributed{cell} | surface{cell}; attenuate gates on the REUSED authNarrowerOrEqual (granted⊆held from Exec.CapTPConcrete, = Rust is_attenuation) backing-AGNOSTICALLY. The 4 load-bearing theorems as l4v refinement obligations (Lean ⊑ Rust): attenuate_decision_backing_agnostic + attenuate_preserves_target (ATTENUATE-IS-BACKING-AGNOSTIC, refines Capability::attenuate); no_amplification ∀target + no_amplification_surface (NO-AMPLIFICATION incl. widening window-share REFUSED at the glass, refines attenuate_and_grant + DelegationDenied); distributed_collapses_at_one + bounds_relax_above_one + verbs_independent_of_n (n=1 COLLAPSE — bounds slide, verbs don't, refines Bounds::distributed; protocol precedent Distributed.Revocation.single_machine_collapse); surface_is_another_point_on_n (SURFACE-RIDES-THE-SAME-GATE — surface backing≡distributed ∧ same gate ∧ same bounds). Non-vacuity both polarities at each target (#guard: narrowing COMMITS, widening REJECTS); #assert_axioms-clean
import Dregg2.Firmament.SeL4Kernel       -- THE SEL4 CAP/IPC KERNEL, formal Lean semantics of sel4/dregg-firmament/src/emulated_kernel.rs (the n=1 EmulatedKernel, DREGG-DESKTOP-OS §3 semihosted-seL4 KEYSTONE). Models the EmulatedKernel's seL4 primitives as pure state transitions the Rust refines (l4v Lean⊑Rust): CNode slot-table + mint/revoke derivation tree (mintedFrom), synchronous Endpoint rendezvous (stageCall/recv/reply/takeReplyFor, generation-matched), Notification badge-OR (signal=lor, wait=read-and-clear), Untyped+Retype (exactly-permits-type ∧ within-budget). REUSES CapGradation.grantOk (= authNarrowerOrEqual = Rust is_attenuation) for the mint gate. 5 keystone theorems w/ Rust refinement correspondences: mint_child_attenuates_parent + mint_refuses_amplification + mint_records_parent (MINT-ATTENUATES-REFUSES-AMP, refines LocalBacking::mint); revoke_chain_synchronous_transitive + revoke_kills_all_doomed + revoke_child_spares_root (REVOKE-SYNC-TRANSITIVE, the n=1 immediate-dark subtree, refines LocalBacking::revoke); retype_mints_exactly_declared + retype_refuses_wrong_type + retype_refuses_over_budget (RETYPE-EXACTLY-DECLARED slot-caveat, refines EmulatedKernel::retype); recv_observes_call_message + rendezvous_round_trips + inline_call_agrees_with_serve (ENDPOINT rendezvous, refines recv/reply/call); signal_then_wait + wait_observes_badge_or + second_wait_is_zero (NOTIFICATION badge-OR, refines signal/wait). Non-vacuity both polarities (#guard: narrowing mint COMMITS / amplify REFUSES, root-revoke darkens subtree, declared retype mints / wrong-type+over-budget REFUSE, rendezvous round-trips, badge-OR accumulates+clears); #assert_all_clean
import Dregg2.Firmament.SeL4Abstract     -- THE TRANSCRIBED-SEL4 CAP-AUTHORITY GROUNDING (l4v pin e2f32e54). DISTINCT from the hand-rolled SeL4Kernel: a line-for-line TRANSCRIPTION of seL4's OWN abstract spec (each def headered to its `.thy` source+lines so a reviewer diffs it), grounding ONE LEG of dregg's cap discipline — derive-cap NON-AMPLIFICATION — in transcribed seL4 rather than a black box. PURE (non-monadic) fragment only: auth (12 ctors, Types.thy:51) + cap (11+ArchObjectCap-opaque, Structures_A.thy:102-117) + cap_rights_to_auth/reply_cap_rights_to_auth/cap_auth_conferred (Access.thy:107-131) + default_cap (Retype_A.thy:30-39) + derive_cap PURE-ISED (ensure_no_children's verdict threaded as Bool — VERIFIED the sole non-arch state-read, CSpace_A.thy:105-114/68-72). THE LEMMA seL4_derive_cap_non_amplifying (cap_auth_conferred c' ⊆ cap_auth_conferred c, axiom-clean, non-vacuous). THE PAYOFF dregg_executor_cap_authority_grounded_in_seL4: derives dregg's cap-non-amp leg (AssuranceCase running_entry_sound, AssuranceCase.lean:641) from the seL4 lemma + α-image-monotonicity, under the NAMED SeL4DeriveNonAmpBridge assumption (= SEL4-DERIVE-NONAMP-BRIDGE; embed/commutes/reflectAuth carried as hypotheses, NOT proved). REPORTED DIVERGENCE: α (seL4 auth → dregg Auth) is faithful+injective on the USED 7-ctor subset but NOT total — seL4's auth is richer (Notify/Write/Read/DeleteDerived/AAuth have no dregg image), so dregg authority is a 7-of-12 PROJECTION (alpha_total_iff_used). NOT "dregg composes with seL4" — one leg grounded, a named-assumption reduction. #assert_all_clean
import Dregg2.Firmament.NotifyAuthority   -- THE `notify` BRICK, STEP 1 (.docs-history-noclaude/NOTIFY-PRIMITIVE.md): async-signal AS a held capability — the transfer triangle that makes the cap-algebra-on-async-signal a THEOREM before the core Auth is touched (NO core-Auth ctor, NO felt/Fintype/FFI arm, NO VK bump — those are Step 2, held for ember). Names the brick-shaped hole the seL4→Lean transcription found (SeL4Abstract Notify↦none): five subsystems re-implement async-signal authority UNGATED; notify is the missing async dual of the synchronous endpoint write/call — the pure "may WAKE you" right. WELDS (reuses, does NOT reinvent): SeL4Kernel.Notification (the badge-OR object, already green) gated by a NotifyCap{target,rights,badgeMask} (the seL4 NotificationCap oref badge cap_rights shape) via signalGated; the badge-mask sub-lattice IS Exec.CapTPConcrete.facetAttenuation (the u32-mask bit-subset = Rust is_facet_attenuation, REUSED), completed to a partial order (maskNarrowerOrEqual refl/antisymm/trans, 0 the bottom); the rights ride CapGradation.grantOk (= the firmament mint's granted⊆held). PROVES: NON-AMPLIFICATION — attenuateNotify narrows mask+rights by those existing orders, a widening on either axis REFUSED, keystone signalAdmissible_attenuate_no_amplify (the attenuated cap admits a SUBSET of badges, via badgeWithinMask_mono). GATE TEETH both polarities — signalGated_commits_of_admissible (within-mask COMMITS, OR'ing exactly the badge) / signalGated_refuses_of_inadmissible (out-of-mask REFUSES, fail-closed). WELL-FORMEDNESS (seL4-faithful, over SeL4Abstract's Auth/Cap) — notificationCap_confers_at_most_notify_read ({Reset,Receive,Notify} only) + never_grant/call/reply (the SeL4Abstract:225 AllowGrant/AllowGrantReply strip as a dregg fact) + notify_distinct_from_sync (Notify ≠ SyncSend/Call/Reply) + notify_cap_confers_notify_not_syncsend (the "may poke not message" authority is real+inhabited, the new expressivity; same AllowWrite ⇒ Notify on a Notification, SyncSend on an Endpoint — the seL4 split). Non-vacuity both polarities (30+ #guard: within-mask COMMITS / out-of-mask REFUSES, attenuation narrows + admits strictly fewer / widening REFUSED, Notify conferred / SyncSend+Grant+Call+Reply not). #assert_all_clean (18 keystones).
import Dregg2.Firmament.NotifyOrgans      -- THE `notify` CASCADE / WELD across the organs (.docs-history-noclaude/NOTIFY-CASCADE.md §1e/§5, STEP-3 Lean): the ungated wake mechanisms ARE instances of the ONE notify authority. Census-driven (THE WELD METHOD): five subsystems re-implement async-signal UNGATED — pubsub publish (frozen publisher slot + ungated broadcast, PubsubFactory:157 / channels_service.rs:300), inbox deliver (sender-set MEMBERSHIP, InboxFactory:183), WorldEvent emit (AMBIENT, dynamics.rs:111), seL4 Notification, blocklace finality_notify. Each is modelled as signalGated under a NotifyCap (a publisher signalling subscribers = a notify cap; a mailbox deliver = a notify; an event emit = a notify) — so the ungated wake inherits the non-amplification. REUSES NotifyAuthority verbatim (signalGated / attenuateNotify / signalAdmissible_attenuate_no_amplify); models, does NOT edit, the existing organ Lean models (PubsubFactory/InboxFactory). PER ORGAN (Lean-modelled: pubsub + mailbox; Rust-only-later authority-shape: dynamics emit = W-organ-emit, blocklace = the canonical Notification): publish/deliver/emit re-expressed as signalGated — a publisher can't signal a topic-kind it doesn't hold (pubsubPublish_refuses_unheld_kind) + an attenuated subscriber receives a SUBSET (pubsubSubscriber_receives_subset); a sender can't deliver an unheld tag + attenuated sender delivers a SUBSET (inbox_*); an event producer can't emit an unheld subject-kind (eventEmit_*). THE UNIFICATION THEOREM organ_wakes_are_one_notify_authority: ANY OrganWake obeys the SAME three notify laws (commit-iff-admissible / fail-closed-refuse / attenuation-only-shrinks) — the ungated wakes modelled as signalGated ALL inherit the non-amplification, i.e. they are ONE authority; organ_wakes_are_definitionally_one (rfl: pubsub publish = inbox deliver = event emit = the ONE signalGated) is the structural heart; per-organ corollaries (pubsub/inbox/event_*_no_amplify) each ONE LINE off it. Non-vacuity both polarities per organ (#guard: held COMMITS / unheld REFUSES, attenuated ⇒ strict SUBSET, the three wakes agree identically). WIDE-SAFE (NO core Auth, NO circuit, NO VK). #assert_all_clean (14 keystones).

import Dregg2.Apps.NameserviceGated    -- D4: nameservice as a VERIFIED app on the ONE gated production entry (execFullForestG). 5 end-user theorems (forged-credential rejection ∀s; WriteOnce no-name-squat; Monotonic no-rent-shorten; WriteOnce permanent-revoke; per-asset conservation) + 22 #guard non-vacuity witnesses on a real registry-cell state; #assert_axioms-clean. First real starbridge-app on the credential-gated executor.
import Dregg2.Apps.IdentityGated       -- D4: identity app on the ONE gated entry — PERMANENT REVOCATION as the gate's revocation leg (id_revoked_rejected: a revoked credential's op = none at EVERY state, via gateOK_revoked_fails) + id_forged_rejected + id_op_conserves; #assert_axioms-clean + #guard witnesses (genuine commits / revoked ⇒ none / forged ⇒ none)
import Dregg2.Apps.AgentProvenanceGated -- usecase #3: PROOF-CARRYING AGENT PROVENANCE / verifiable memory on the ONE gated entry — capability-gated append-only tamper-evident scratchpad. prov_forged_credential_rejected (write needs the cap) + prov_entry_writeonce (APPEND-ONLY: committed entry never overwritten, WriteOnce) + prov_head_cannot_rewind (Monotonic cursor, no re-order) + prov_append_reads_back (faithful recording, via setField_fieldOf) + prov_append_audited (receipt-log audit row) + prov_chain_links/prov_chain_tamper_rejected (the provenance HASH CHAIN is verifiable: verifyChain accepts honest, rejects tamper/truncation) + prov_append_conserves; 18 #guards, 10 #assert_axioms-clean
import Dregg2.Apps.SubscriptionGated   -- D4: subscription app on the ONE gated entry — replay-safe sequencing as the MonotonicSequence slot caveat (sub_nonsequential_rejected) + sub_queue_wellformed_forever (Hatchery subWFContract on trajG) + sub_forged_rejected + sub_op_conserves; #assert_axioms-clean + #guard witnesses
import Dregg2.Apps.HostingLease        -- THE DEOS APP-HOSTING ECONOMY: paying coin to be hosted is a VERIFIED VALUE TURN, and a lapsed fee EVICTS. The hosting lease = a cap with a TIME (period) + BUDGET (balance) caveat over the durable persist-PD slot (.docs-history-noclaude/PG-DREGG-ON-SEL4-DEOS-SPINE.md). Charge gate = senderInField host (only the host collects) ∧ balanceGe leaseFee (the BUDGET caveat — an unfunded app's charge is UNSAT ⇒ the host EVICTS, fail-closed: lapsed_app_charge_rejected) ∧ balanceDeltaGe (−leaseFee) (the FEE FLOOR — no over-charge: over_charge_rejected) ∧ strictMono period (the TIME window — no double-bill: replayed_period_rejected) + impostor_charge_rejected + honest_charge_admits (the gate is non-vacuous — the paid period the Rust hosting.rs commits as a conserving Transfer). The Rust realization runs green over REAL redb durability (sel4/persist-hosttest/src/hosting.rs); this is the proof the lease cannot amplify + eviction is FORCED on budget lapse. #assert_all_clean + #guard both-polarity witnesses
import Dregg2.Exec.PrivacyTheorems     -- D7 privacy: nullifier NO-DOUBLE-SPEND fully proved at the ledger layer (chainNoteSpend_no_double_spend · nullifier_persists · seq_no_respend) + stealth-freshness⇒unlinkability structural (crypto as NAMED §8 portal hypotheses); 13 thms #assert_axioms-clean. Gate-wiring into the executor entry = separate task
import Dregg2.Exec.ShieldedValue        -- A4: noteCreate's commitment WELDED to a hidden value via the Pedersen/range-proof portal — noteCreateBound_binds (set entry IS commit(value,blinding)) + noteCreateBound_in_range (no negative/overflow ⇒ no hidden inflation at creation) + created_value_conservation (Σ commitments = commit(Σ value)(Σ blinding) via commit_hom ⇒ shielded value-conservation over executed state); the value-side dual of the nullifier discipline. Crypto residue = Pedersen binding carrier; #assert_axioms-clean
import Dregg2.Shielded.ClaimRefinement    -- THE shielded pool's EffectVM side-structure `_refines_` obligation (ABI §3.6/§4.1/§5): shielded_spend_claim_refines — a valid shielded-spend claim [nullifier, merkle_root, value_binding] (note is a MEMBER of the tree at merkleRoot ∧ nullifier FRESH) REFINES a sound VM step: (a) AUTHORIZED (spends a real committed member note, crediting exactly its value — the C6 value-theft tooth as the shielded authorization notion) + (b) NO-DOUBLE-SPEND (fresh → joins the set → never re-spendable, composed from the deployed note_spend_inserts/note_no_double_spend anti-replay). PROVED half = membership + nullifier; per-asset CONSERVATION over hidden notes stays ATTESTED (off-AIR Pedersen excess + Bulletproof range, named §ATTESTED — value-commitments-in-AIR = the remaining obligation). Non-vacuous BOTH polarities (demo_valid_spend_refines; forged-note + double-spend #guard-REFUSED). #assert_axioms-clean
import Dregg2.Shielded.WideNativePqCommitment -- canonical 4×16-bit u64 value/asset/randomness payload; field-faithful transcript with x/x+p refusal; 16-BabyBear-lane native-PQ output wall; binding conclusion remains explicitly conditional on the hash collision floor.
import Dregg2.Apps.GovernedNamespaceGated  -- D4: governed-namespace on the ONE gated entry — committee threshold-sig credential gate + 4 constitutional slot caveats (gn_committee_immutable · gn_threshold_immutable · gn_version_monotonic_seq · gn_dispute_window_cannot_shrink) + gn_forged/gn_revoked_member rejected + gn_commit_conserves
import Dregg2.Apps.PrivacyVotingGated      -- D4: private voting on the ONE gated entry — vote-as-nullifier NO-DOUBLE-VOTE (pv_no_double_vote, WriteOnce per-voter nullifier) + pv_forged/pv_revoked_voter rejected + pv_cast_conserves; the executed dual of PrivacyTheorems.kernel_no_double_spend
import Dregg2.Apps.PrivacyVoting         -- ungated cell-program: private ballot cast via WriteOnce per-voter nullifier slots on execFullForestA; no-double-vote + conservation; dual of PrivacyVotingGated
import Dregg2.Apps.EscrowFactory        -- W2 land-before-kill: the REAL escrow FACTORY (escrowFactoryEntry) — the verb-family replacement. mintEscrowCell via the live createCellFromFactoryChainA installs the state machine + deal-term immutables; depositEscrow = ordinary move IN; release/refund = the probe's escrowRelease/Refund on the factory-born cell. 4 keystones (conserve/no-double-resolve/condition/not-stranded) + the factory-shape D3 settle-into-live-target teeth, all re-proved on the minted cell. §DELETION enumerates the W2 escrow-verb burn-down (createEscrow/release/refund arms + escrows side-table + escrowHeldAsset + recTotalAsset's escrow term) + the re-point prerequisites. Land-before-kill: nothing deleted yet
import Dregg2.Apps.BountyBoardGated     -- D4 (VALUE-MOVING), W2 RE-POINTED onto the escrow FACTORY: bounty escrow is a factory-born cell holding its reward in `bal` (not the side-table). post = mintEscrowCell + depositEscrow; claim = release; cancel = refund. bb_forged/bb_revoked rejected (gate teeth preserved) + bb_post_mint/post_fund/claim/cancel_conserves + bb_no_double_resolve + bb_claim_requires_condition + bb_claim_requires_live_claimant / bb_cancel_requires_live_poster (factory-shape D3) + bb_open_claimable/cancellable (not-stranded)
import Dregg2.Apps.ComputeExchangeGated -- D4 (VALUE-MOVING), F1b RE-POINTED onto the escrow FACTORY: compute market on the gated entry — order = mintEscrowCell + depositEscrow; settle = release; refund = refund. cx_forged/cx_revoked rejected (gate teeth preserved) + cx_order_mint/fund/settle/refund_conserves + cx_no_double_resolve + cx_settle_requires_condition + cx_settle_requires_live_provider / cx_refund_requires_live_buyer (factory-shape D3) + cx_open_settleable/refundable (not-stranded) + the §8b trajG crowns
import Dregg2.Apps.GalleryGated         -- D4: gallery/artwork registry on the gated entry (8th/8 — suite complete) — mint/transfer/set-metadata; gallery_item_immutable (WriteOnce content-addressed id, no collision) + gallery_forged/gallery_revoked rejected + gallery_conserves; source-faithful to apps/gallery/src/artwork.rs; 17 #guards, 9 #assert_axioms
-- (F1b) Dregg2.Apps.CrossChainBridgeGated DELETED with the bridge-LFC verbs it demonstrated (bridgeLock/Finalize/Cancel over the kernel holding-store). Land-before-kill: the bridge lock/disclosed-outflow/cancel guarantees live in Apps/BridgeCell.lean (the bridge-cell contract); the gated-app re-point re-lands there.
-- (F1b) Dregg2.Apps.StakedSlaGated DELETED with the obligation verbs it demonstrated (createObligation/fulfill/slash over the kernel holding-store). Land-before-kill: the obligation lifecycle guarantees live in Apps/ObligationFactory.lean; the gated SLA app re-lands on factory-born bond cells.
import Dregg2.Apps.ShieldedPaymentGated -- D4 (PRIVATE PAYMENT, note effects): shielded payment on the gated entry via noteCreate/noteSpend — sp_forged/sp_revoked rejected + sp_no_double_spend (the GENUINE Zcash anti-replay on the real kernel nullifier SET via note_no_double_spend, not a slot-caveat dual) + sp_spend_requires_proof (§8 spending-proof tooth, captures apply.rs:929) + sp_spend_inserts_nullifier + sp_spend/deposit_conserves; 16 #guards, 13 #assert_axioms
import Dregg2.Apps.Gallery              -- ungated cell-program: artwork mint/transfer/set-metadata via WriteOnce item slot on execFullForestA; item-immutable + conservation; dual of GalleryGated
import Dregg2.Apps.NameService         -- nameservice cell-programs (register/transfer/revoke): nameservice_registration_forever carried via livingCellA_commitments_persist; nameCommit_inj + revoke tombstone; transfer preserves both old registration and new ownership binding forever
import Dregg2.Apps.Identity            -- identity cell-programs (present/verify/revoke): livingCellA_identity_revoked_forever = a revoked identity can never be re-validated, carried via livingCellA_carries on the grow-only `revoked` registry
import Dregg2.Apps.PreRotation         -- KERI pre-rotation for identity-as-council (the ORGANS identity rider): one `next_keys_digest` register + the rotate verb as a guarded write — rotate_exhibits_preimage (admitted ONLY exhibiting the committed next-set preimage) + rotate_current_keys_irrelevant (rfl: current keys powerless) + rotate_compromise_resistant (under named KeySetCR, any forged key set refused) + rotChain_pinned_by_commitments (the unforgeable forward chain) + the cooling composition (cooledSince × preimage STRICTLY dominates either: cooling_blocks_admitted_preimage / preimage_blocks_cooled_rotation) + the live-substrate carrier rotateWrite/rotateWriteCooled on stateStepGuarded (fresh-commit/conserves/caps-frame/audited/caveats-composed); #assert_axioms-clean, #guard-executed both polarities; KERI `rot` export = the named interop lane
import Dregg2.Apps.Subscription        -- subscription cell-programs: §A slot automaton safety (seq_tail ≤ seq_head, no-overflow) + subscription_consumer_safe_forever; §B subscription_wellformed_forever carried by the real execFullForestA living cell against any adversary
-- (F1b) Dregg2.Apps.AtomicSwap DELETED with the kernel escrow verbs it demonstrated (lock = createEscrowKAsset, settle = releaseEscrowKAsset). Land-before-kill: the escrow verb-family guarantees live in Apps/EscrowFactory.lean; the MULTI-PARTY atomic-swap composition re-lands on factory-born escrow cells in the F2 queue/factory migration.
-- (F1b) Dregg2.Apps.BountyBoard (ungated) DELETED with the kernel escrow verbs. Land-before-kill: the gated dual (Apps/BountyBoardGated.lean) is RE-POINTED onto the escrow factory and carries the full release-safety contract; Apps/BountyBoardForever.lean is the factory-born forever-crown twin.
import Dregg2.Apps.GovernedNamespace   -- ungated cell-program: governed route-table writes via setFieldA + constitutional slot caveats on execFullForestA; immutable committee/threshold + monotonic version/window + balance-neutral; dual of GovernedNamespaceGated
-- (F1b) Dregg2.Apps.ComputeExchange (ungated) DELETED with the kernel escrow verbs. Land-before-kill: the gated dual (Apps/ComputeExchangeGated.lean) is RE-POINTED onto the escrow factory and carries the full settle-safety contract.
import Dregg2.Authority.ClearanceGraph -- compartment clearance as explicit label-dominance graph (mayRead/needsAll); foundation for CompartmentWorkflowMandate
import Dregg2.Apps.CompartmentWorkflowMandate -- Phase A mandate crown: DAG workflow (review→redact→sign) + clearance + Stingray spend demo + cwm_step_legal_forever / cwm_pay_supply_forever on trajA
import Dregg2.Apps.CompartmentWorkflowMandateGated -- production mandate: cwm_safety_forever = step-legal ∩ pay-conserved ∩ revoked-dead ∩ compartment on trajG; MonotonicSequence DAG enforcement + Hatchery composeContracts
import Dregg2.Apps.CompartmentWorkflowMandate.CrossVatBridge -- Phase B: mandate commitment_anchor ↔ CrossVatCharter covenant; cwm_charter_refines_mandate_forest
import Dregg2.Apps.StorageGatewayMandate -- storage gateway mandate (ungated): op/prefix/clearance/volume rejection teeth + sgm_volume_legal_forever + sgm_pay_supply_forever on trajA; Stingray slice demo
import Dregg2.Apps.StorageGatewayMandateGated -- production storage gateway: sgm_safety_forever = step-legal ∩ pay-conserved ∩ revoked-dead ∩ bucket on trajG; execFullForestG setField/emit gated ops
import Dregg2.Apps.DelegAdmit -- THE RUNTIME-CALLABLE delegated tool/MCP-access ADMISSION DECISION, @[export dregg_deleg_admit]-ed so Rust CALLS it. Init-only (no Mathlib) so its leanc `.c` splices into libdregg_lean.a. Owns `Grant` + `delegAdmit` (SCOPE ∧ DEADLINE ∧ single-step ∧ sane-prior ∧ RATE); Dregg2.Apps.ToolAccessDelegation imports and re-exports them, so the headline commit-iff-admit theorem and the three rejection teeth are theorems about THE EXPORTED OBJECT. This deleted THREE hand-maintained Rust re-implementations (sdk/src/tool_gateway.rs, starbridge-apps/tool-access-delegation/src/lib.rs, dreggnet-offerings/src/session.rs). #guard teeth: the 3 legal advances admit, over-rate/out-of-scope/past-deadline/batch-jump/negative-prior refuse, and the wire fails CLOSED (empty answer = NO VERDICT) on short/long/non-numeric input.
import Dregg2.Apps.ToolAccessDelegation -- THE INTEGRATOR HEADLINE: delegated tool/MCP access as a verified mandate cell on the production caveat-gated executor (execFullA = stateStepGuarded). delegAdmit folds SCOPE (allowlisted toolId) ∧ DEADLINE ∧ single-step ∧ RATE (new ≤ rateLimit); tool_invocation_commit_iff_admit (commits IFF the delegated policy admits AND the worker held authority) + the §5 TEETH (tool_invocation_over_rate_rejected / _past_deadline_rejected / _out_of_scope_rejected — a worker can NEVER drive an invocation past the granted rate/deadline/scope) + §6 kernel keystones (conserves / no_amplify / authorized / counts_one). #assert_axioms-clean
import Dregg2.Apps.Trustline           -- ORGANS §1 bilateral line of credit (mutual credit): draw = attenuated-cap exercise debiting the Stingray counter (draw_slice_tracks_tryDebit = literally Slice.tryDebit) — within-line forever (SGM boundedBy pattern) + bilateral signed-well conservation (holderAcct+issuerWell=0 forever, issuer-move) + no-double-draw (digest nodup forever) + settlement restores the line (draw_repay_roundtrip; settleAll_clears conserves the hard pair) + the attenuation pun (holder_credit_le_line_forever, granted⊆held made quantitative) + the DEPLOYED node semantics (§9+: monotone settled register w/ settled-floor repay + solvency settled≤drawn≤ceiling=escrow + the Collateral axis — fullReserve=the Rust point, pureCredit=settlePay, settleC_conserves_hard parametric + derived-view faithfulness + the epoch-slice weld; draw_replay_refused_across_epochs = the TrustlineRegistry forever-carrier law); init/settle weld design = .docs-history-noclaude/TRUSTLINES.md
import Dregg2.Apps.ChannelGroup        -- ORGANS §4 channel-group cell program (the group-key epoch lift, blueprint twin of cell/src/blueprint.rs:838 / commit 72d43dc64): the epoch unification TRIPLE proved — membership_change_steps_epoch + epoch_step_rewrites_key + remove_without_rekey_unsat (THE keystone, both polarities) + epoch_never_rewinds (AdmittedChain forever) + admin_gates (consumes the proven actorBound_* polis binding) + remove_darkens_both (forward-key darkness ∧ R7 capability staleness in ONE epoch step, UNDER the NAMED interface premise DelegationEpochTie — the executor-side delegation_epoch tie the Rust builders carry fail-closed; closure lane = the program-readable executor atom, executor lane)
import Dregg2.Apps.VerificationToolkit -- REUSABLE app-verification toolkit: an author supplies an AppSpec (slot/cell/admit predicate/grid) and GETS generically — app_commit_iff_admit (executor caveat gate commits IFF admit, proven ONCE over any predicate), app_violation_rejected (anti-ghost tooth), app_commit_{conserves,no_amplify,authorized} (kernel keystones re-exported), AppDiffPinned (Rust-mirror drift corpus). DEMO: CompartmentWorkflowMandate (cursor) + StorageGatewayMandate op-leg (last_op) RE-DERIVED through it (cwm/sgm_*_via_toolkit). #assert_axioms-clean, non-vacuous (#guard teeth bite)
import Dregg2.Apps.CrossCellCovenantGated -- coordinated cross-cell covenant app: ccov_charter_commits + ccov_forged_cred_rejected + ccov_covenant_teeth + ccov_joint_conserves on the joint KernelState bilateral path
import Dregg2.Apps.MultisigVote        -- REAL-kernel multisig/DAO vote on RecordKernelState: enfranchisedB = registry-membership OR Cap.node vote-cap (a GENUINE gate, NOT the actor==src tautology); vote-as-nullifier no-double-vote (reuses the spend-nullifier anti-replay); SAFETY revote_tally_unchanged + AUTHORITY unenfranchised/outsider_vote_rejected (exercises the cap branch, capbearer_admitted) + CORRECTNESS passes_iff (threshold ≤ distinct-enfranchised tally, both directions) + LIVENESS votes_reach_quorum; decide-teeth (empty/sub-threshold does not pass, double-vote counted once)
import Dregg2.Apps.SealedBidAuction    -- Track-A Phase 4 (the proving ground): the gallery sealed-bid auction on the intent stack — (b) no_frontrunning (causal reveal-ordering = a lightcone fact; teeth: a fork-f1 reveal unfillable by concurrent-f2 fill) + (c) settle_conserves & settle_cannot_mint + one-shot settle_no_double + loser-refund ◇ (auction_loser_refunded via just_progress, auction_starvation_rejected). (a) userspace-escrow ≥ kernel = a CARRIED OPEN interface (UserspaceDominatesKernel), not stubbed — needs the userspace-escrow cell-program + a state clock (ember model call)
import Dregg2.Exec.CellRuntime         -- OS-level cell runtime theorems: checkpoint_restore_roundtrip + replay_deterministic_run + replayFrom_conserves + time_travel_fork; νF fork noted
import Dregg2.Exec.StateMigration      -- schema-upgrade state migration: migrate_conforms + migrate_conserves (balance survives) + migrate_anti_brick (gate-fail ⇒ signature fallback, never bricks)
import Dregg2.Exec.CircuitEmitGadgets  -- emits the remaining §8 gadgets to the wire: emittedTemporal/NonMembership/Pedersen/Dfa_bridge (each composes emit-faithfulness with the gadget's *_bridge)
import Dregg2.Circuit.Transfer         -- crown jewel: the circuit⟺protocol bridge for ONE REAL effect (Transfer) over the real recKExec executor — transfer_circuit_sound (circuit ⇒ executor admits + genuine debit/credit/conserve) + transfer_circuit_complete (every committed step satisfies) + transfer_bridge_iff (sat ⇔ admitGuard); NON-VACUOUS (rejects_nonconserving = Orchard-class value-forgery forbidden BY CONSTRUCTION · rejects_unauthorized · rejects_overdraft + decidable #guard forgery witnesses); emitTransferFaithful composes with emit. PLUS Stage-A: TransferSpec (independent declarative FULL-17-field spec) + recKExec_iff_spec (executor⟺spec, both directions, validates recKExec) + recTransfer_correct
import Dregg2.Circuit.Lookup            -- D5 IR extension (ember steer): the LOOKUP-argument IR — LookupConstraint (denotation = membership in a finite table; LogUp is the prover-side enforcement) + CircuitL (arithmetic gates PLUS lookups, ADDITIVE — the bare Constraint/31 specs untouched) + rangeCheck (range [0,2^k) check) with decidable #guards (in-range accepted; out-of-range/negative/boundary REJECTED). Closes the FIELD-soundness gap path: the Rust i64→BabyBear map needs range checks or field-wraparound forges value; this is the Lean IR for them. Poseidon2 gates next
import Dregg2.Circuit.Refinement        -- D5 l4v REFINEMENT framing: the circuit as the bottom of the refinement tower — circuit ⟺ spec ⟺ executor as explicit Refines/Equiv (circuit_refines_spec = soundness/⊑ · spec_refines_circuit = completeness · circuit_refines_exec = the algebraic statement refines the dynamic behaviour the kernel runs) + Refines.preserves (the l4v payoff) instantiated as circuit_conserves (CONSERVATION proven on the abstract spec DESCENDS for free to the arithmetic circuit). Realizable Poseidon-CR portals only
import Dregg2.Circuit.EffectCommit      -- D5 GENERIC framework: full-state circuit⟺spec proved ONCE — EffectSpec (per-effect data) + effect_circuit_full_sound/_complete + 4 anti-ghost teeth + emission, all #assert_axioms-clean (injectivity portals are hypotheses). touchedDigest = StateCommit.frameDigest over carrier T (one binding lemma, any |T|). The `ec_lookup` tactic (concrete wire indices, no omega-tarpit) is the reusable lookup recipe. Per-effect cost ~100 lines (fill EffectSpec + GuardDecodes/Encodes + apex↔bespoke bridge); the crown-jewel theorems come free
import Dregg2.Circuit.KeyedCommit       -- D5 v2 carrier (FUNCTION-field shape): keyedDigest + KeyedDigestBindsKeys — the key-polymorphic, state-free lift of StateCommit.frameDigest/FrameDigestBindsCells to ANY LinearOrder key, so `bal` (key CellId×AssetId) + `caps` (key Label) are bound by the SAME proof. #assert_axioms-clean
import Dregg2.Circuit.ListCommit        -- D5 v2 carrier (the breadth bottleneck-breaker): listDigest + ListDigestBindsList — the list-side-table commitment binding (compressN-inj + injective leaf ⇒ WHOLE post-list pinned, so a drop/reorder/wrong-cons is REJECTED). The ONE new crypto carrier v2 needs; unlocks the ~12 list-changing effects (notes/escrows/queues/seal/swiss/revoked). #assert_axioms-clean
import Dregg2.Circuit.Emit.CommitmentTreeAppendEmit -- the DEPLOYED note-tree root COMPUTED IN LEAN over the REAL KAT-locked Poseidon2 (Poseidon2BabyBearW16.perm): instantiates chunk-1 CommitmentTreeAccumulator.root at hash4to1Real (the real hash_4_to_1) → rootReal computes the deployed Poseidon2MerkleTree::root().0 byte-for-byte. #guard byte-pins the EXACT deployed roots (empty/sparse/half/full at depths 2-4 + every append prefix); commit/tests/poseidon2_tree_lean_differential.rs asserts the DEPLOYED Rust tree reproduces the SAME integers ⇒ byte-identical safety gate. rootReal_distinct_extracts_collision = chunk-1's HONEST binding at the real hash (a forged deployed root EXHIBITS a genuine hash_4_to_1 collision). NOT an EffectVmDescriptor2: an AIR is an acceptor, not a root evaluator (heap_root.rs::compute_heap_root is likewise hand-Rust; the descriptor only CONSTRAINS the write) — so no by-name JSON/EmitByName entry; the Rust root computation legitimately stays (held, not deleted). #assert_axioms ⊆ {propext, Classical.choice, Quot.sound}.
import Dregg2.Circuit.CommitmentTreeAccumulator -- THE LEAN AUTHOR of the DEPLOYED append-only 4-ary Poseidon2 note-commitment accumulator (`commit/src/poseidon2_tree.rs` is the Rust DEBT this replaces; law #1). Models the position-indexed, fan-out-4, EMPTY_LEAF(0x0DEAD1EF)-padded tree over the opaque node hash `H : List ℤ → ℤ` (the `hash_4_to_1` stand-in): leafAt/emptyHash/nodeAt/root/append EXACTLY as the Rust computes. PROVED: emptyHash_correct (the deployed empty-subtree SHORTCUT = the pure recursion — faithfulness to compute_node_at_level); root_binds_of_injective (root binds the padded leaf sequence off the compressNInjective carrier, the ListCommit-style reduction) AND root_distinct_extracts_collision (the HONEST binding: a distinguishing leaf under one root EXHIBITS a genuine H-collision — assumes NO floor, honest even where injectivity is false, the CommitTreeRegrounded posture); nodeAt_congr + append_offpath_unchanged (the INCREMENTAL-APPEND lemma — append recomputes only the single root-to-leaf path whose windows contain p=len); append_length (genuinely ADDITIVE, never idempotent — exactly-once is the finalization/nullifier layer's job, Exec.NullifierAccumulator). Golden roots #guard the fan-out-4 + position-index + padding SHAPE (mirrored by a Rust differential over the same toy). NOT RotatedKernelRefinementNotes.noteListRoot (that is a one-shot listDigest over List Nat — the KERNEL datum — a DIFFERENT object than this positional tree). #assert_axioms ⊆ {propext, Classical.choice, Quot.sound}. REMAINING to fully retire the Rust: emit this accumulator as a descriptor (Emit/*) + delete poseidon2_tree.rs append/root in favor of the call-in.
import Dregg2.Circuit.FieldLanes9 -- LEAN AUTHORITY for the INJECTIVE fields octet: nine lanes (the pinned kernel u64 pair, byte-identical, plus seven 28-bit digits of a 25-digit base-256 word carrying b[0..24] AND both mod-p quotients). fieldToLanes9_injective is an INJECTION with a total decoder and a machine-checked left inverse -- not a hash bound and not a birthday bound; nine_lanes_is_the_minimum pins P^8 < 2^256 <= P^9, the pigeonhole that made every 8-lane repair a cost move. Lean-computed lane vectors mirrored by circuit/tests/field_lanes9_injective.rs.
import Dregg2.Circuit.CommitmentTreeWide -- LEAN AUTHORITY for the faithful wide note tree: proves the exact 32-byte → sixteen-u16 carrier injective by an explicit decoder; defines the deployed rate-4 Poseidon2 hash_many_8 semantics over the KAT-locked real permutation, domain-separated wide leaf/empty/internal hashes, 4-ary root, and fail-closed membership replay. Lean-computed eight-lane leaf/node/root vectors are mirrored by the Rust differential; no one-felt intermediate or Rust-authored constraint authority.
import Dregg2.Circuit.CommitmentTreeWideHistory -- authenticated faithful-root HISTORY semantics: exact session/federation/epoch + predecessor/successor + consecutive height/note-count binding, parameterized hybrid auth, replay/same-height-fork rejection, and exact-head/count truncation refusal. Rust is a strict canonical interpreter; live attestation/spend cut remains explicit.
import Dregg2.Circuit.CommitmentTreeWideSpend -- authenticated historical (height, faithful-eight root) NoteSpend admission + batch freshness + atomic nullifier persistence/exact-root publication; refusal is state identity
import Dregg2.Circuit.EffectCommit2      -- D5 v2 GENERIC core (single non-cell component): effect2_circuit_full_sound/_complete + 3 anti-ghost teeth + emission, proved ONCE for an effect touching ONE non-cell component (a function-field via funcComponent/keyedComponent, or a list side-table via listComponent — off ListCommit/KeyedCommit). Generalizes v1's "touched=cell" to "touched=one ActiveComponent"; soundness gets the postClause from active.binds (no funext, no ghost). #assert_axioms-clean
import Dregg2.Circuit.EffectCommit2Dual  -- D5 v2-dual GENERIC core (TWO non-cell components): effect2dual_circuit_full_sound/_complete + 4 anti-ghost teeth + emission, proved ONCE for bal+escrows-style effects. Gate 1 keystone; unlocks escrow + bridge-outbound families. #assert_axioms-clean
import Dregg2.Circuit.AccountsCommit    -- D5 accounts-growth carrier: accountsComponent (sorted-Finset list digest) for createCellA/spawnA
import Dregg2.Circuit.BornEmptyCommit -- D5 born-empty side-table digest bundles for account-growth circuit wiring
import Dregg2.Circuit.EffectCommit3   -- D5 v2-triple GENERIC core (THREE non-cell components): queueEnqueue/Dequeue (queues+bal+escrows). #assert_axioms-clean
import Dregg2.Circuit.EffectCommit4    -- D5 v2-quad GENERIC core (FOUR non-cell components): createCellFromFactoryA. #assert_axioms-clean
import Dregg2.Circuit.EffectCommit5    -- D5 v2-quint GENERIC core (FIVE non-cell components): spawnA. #assert_axioms-clean
import Dregg2.Circuit.Inst.createCellA       -- D5 v2-triple: createCellA_full_sound ⇒ CreateCellSpec (accounts+bal+born-empty side)
import Dregg2.Circuit.Inst.createCellFromFactoryA -- D5 v2-quad: createCellFromFactoryA_full_sound ⇒ CreateFromFactorySpec
import Dregg2.Circuit.Inst.spawnA           -- D5 v2-quint: spawnA_full_sound ⇒ SpawnSpec
import Dregg2.Circuit.Inst.attenuateA        -- D5 v2 breadth: attenuateA_full_sound ⇒ AttenuateSpec (caps funcComponent)
import Dregg2.Circuit.Inst.balanceA         -- D5 v2 breadth: balanceA_full_sound ⇒ BalanceMovementSpec (bal funcComponent)
import Dregg2.Circuit.Inst.bridgeMintA     -- D5 v2 breadth: bridgeMintA_full_sound ⇒ InboundMintSpec (bal credit)
import Dregg2.Circuit.Inst.burnA           -- D5 v2 breadth: burnA_full_sound ⇒ BurnSpec (bal debit)
import Dregg2.Circuit.Inst.delegate        -- D5 v2 breadth: delegate_full_sound ⇒ DelegateSpec (caps)
import Dregg2.Circuit.Inst.delegateAttenA  -- D5 v2 breadth: delegateAttenA_full_sound ⇒ DelegateAttenSpec (caps)
import Dregg2.Circuit.Inst.introduceA      -- D5 v2 breadth: introduceA_full_sound ⇒ DelegateSpec (caps)
import Dregg2.Circuit.Inst.mintA           -- D5 v2 breadth: mintA_full_sound ⇒ MintASpec (bal credit)
import Dregg2.Circuit.EffectRefinement     -- D5 v2 diamond tower: generic effect2CircuitStep ⟺ apex + emitted ⟺ circuit; mint/burn instances (emitted ⟺ circuit ⟺ spec ⟺ execFullA); mint_supply_delta_descends (l4v payoff)
import Dregg2.Circuit.EffectEmittedRefinement -- Wave 2 batch emitted→spec: effect{1,2} emitted_refines_bespoke_spec + per-effect *_emitted_refines_spec diamonds
import Dregg2.Circuit.Spec.Turn            -- Wave 1 turn spec: turnSpec fold + execFullTurnA_iff_turnSpec + turnSpec_append (associativity)
import Dregg2.Circuit.ActionDispatch       -- Wave 1 apex dispatcher: fullActionStep (56/56 arms) + fullActionStep_exec_iff + actionTag
import Dregg2.Circuit.GadgetRefinement      -- Wave 1 gadget bridges: merkle/nonmembership/temporal emitted ⊑ portal; gadget_step auth witness StepRel
import Dregg2.Circuit.Poseidon2Emit         -- Wave 4: in-circuit Poseidon2 sponge compress via emitted `merkle_hash`; poseidon2_emitted_refines_merkle_portal + explicit HOLE sponges
import Dregg2.Circuit.DigestPortal         -- Wave 4: bridge `cellLeafInjective` → Poseidon2 emit. ⚑ BUNDLE CUTOVER 2026-07-25: `PortalBundle` (a structure whose FIRST FIELD was `cellLeafInjective CH`, a floor `StateCommitFloorRegrounded.cellLeafInjective_false_babyBear` REFUTES, hence UNINHABITABLE at deployment) is DELETED with both constructors + `ofPoseidon2CR`; zero references outside the file, and its one real fact survives as `emitted_poseidon2_registered`. `compressNInjective_from_poseidon2_emit` DELETED too — a floor-to-itself relabel (`compressNInjective_iff_poseidon2CR` is `Iff.rfl`); `Poseidon2Binding.compressNInjective_of_poseidon2CR` is the identical surviving statement. The two `*_from_poseidon2_*` realization discharges are KEPT and now LABELLED vacuous-at-deployment (their `LeafRealization`/`LogRealization` premises are uninhabitable, `StateCommitFloorRegrounded.{leaf,log}Realization_uninhabitable_babyBear`) — that cutover spans 8 and 5 files and is the NEXT bundle transaction, named not laundered.
import Dregg2.Circuit.BundleCutoverCheck   -- ⚙ TEETH of the 2026-07-25 BUNDLE cutover (the field-bundle class of the vacuity campaign): three structures carried a REFUTED FLOOR as a FIELD — `Shielded.RealCrypto.Poseidon2Tree.spongeCR : Poseidon2SpongeCR`, `Crypto.CommitmentBinding.Compress2.compress1CR : Compress1CR`, `Circuit.DigestPortal.PortalBundle.cellLeafInj : cellLeafInjective` — making each UNINHABITABLE at BabyBear and every theorem quantifying over it VACUOUS. All three fields are GONE (PortalBundle entirely). §1 pins the field's ABSENCE structurally (anonymous constructor from DATA alone — any resurrected Prop field, floor OR `noColl`, fails to elaborate); §2 pins each cured theorem's type by KERNEL DEFEQ (`example : ⟨new type⟩ := @original` — same conclusion, floor binder gone, per-instance side condition and nothing else); §3 RE-DERIVES every original statement from the deleted field's own type (machine-checked "no theorem weakened": old hypothesis ⟹ new one, so the ports are strictly stronger); §4 proves dropping the side condition is FALSE not merely unproved (`root_binds_unconditional_false`, `compressInjective_of_compress2_unconditional_false` at degenerate carriers); §5 THE ACCEPTANCE TEST — constructed DEPLOYED inhabitants (`deployedPoseidon2Tree`, `deployedCompress2`) whose own carriers REFUTE the deleted fields, so the very functions the teeth refute now INHABIT the structures; §6 `#assert_not_depends_on` clears the floor from the unconditional forms, with `#assert_depends_on` POSITIVE CONTROLS on the §3 re-derivations so the rejectors are not vacuous. Design decision for the whole bundle family (DELETE the field; never swap in a `noColl` field — a field has no argument position to be per-instance AT) is recorded at `Shielded.RealCrypto` §2.0. Also puts `Dregg2.Shielded.RealCrypto` in the root build for the first time. #assert_axioms-clean.
import Dregg2.Circuit.CapHashBundleCutoverCheck -- ⚙ TEETH of the 2026-07-25 CAP-HASH BUNDLE cutover (the 1-felt sibling the first bundle wave MISSED): `Circuit.DeployedCapTree.CapHashScheme` carried `chipCR : Compress1CR chipAbsorb` as a FIELD, so no `CapHashScheme` VALUE existed anywhere in the tree — `compress1CR_false_babyBear` refutes injectivity for every chip absorb landing in ONE bounded BabyBear felt, which `cap_root.rs::cap_chip_absorb` does — and every `∀ S : CapHashScheme State, …` theorem in §3–§6 of that module was VACUOUS. It was the LAST `Compress1CR` field in the tree (`Cap8Scheme.chip8CR` fell 07-20, `Compress2.compress1CR` earlier on 07-25), so `Compress1CR` is now a field of NOTHING. The field is DELETED; `capLeafDigest_injective` / `nodeOf_injective` / `recomposeUp_inj_of_path` keep their NAMES and CONCLUSIONS under a PER-INSTANCE `¬ Compress1Coll chipAbsorb (extractor args)` at the exact absorbed blocks the old proof fed the floor (implied by the deleted field via `compress1Coll_refutable_of_injective`, hence strictly stronger), the unconditional forms are `…_binds_or_collides`, and the spine extractor `recomposeUpFind` is a PURE RE-INSTANTIATION of the width-agnostic `CapMerkleGeneric.recomposeGFind` the 8-felt tree already rides. §5 THE ACCEPTANCE TEST: `deployedCapHashScheme` is a real constructed VALUE whose own chip REFUTES the deleted field (`deployedCapHashScheme_chip_not_Compress1CR`) — the very function the teeth refute now INHABITS the structure. §1 pins the field’s ABSENCE structurally (anonymous constructor from DATA alone — any resurrected Prop field, floor OR `noColl`, fails to elaborate); §2 pins each cured type by KERNEL DEFEQ; §3 carries the no-strength-lost relation via the ONE pre-existing grandfathered bridge `compress1Coll_refutable_of_injective` and DELIBERATELY mints no per-theorem `…_of_injective` restatements (each would be a NEW declaration on a refuted floor — the accrual the ratchet exists to stop; the honest cost is stated in-file); §4 proves dropping the side condition is FALSE not merely unproved (`capLeafDigest_unconditional_false` / `nodeOf_unconditional_false` at a degenerate chip, with the collision branch exhibited REACHABLE there); §6 `#assert_not_depends_on` clears the floor from the unconditional forms with `#assert_depends_on` POSITIVE CONTROLS on the §3 re-derivations so the rejectors are not vacuous. #assert_all_clean (10 keystones).
import Dregg2.Circuit.TurnWitness          -- Wave 1 turn witness: StepWitness/TurnWitness + turnWitnessSatisfies + turn_witness_refines_turnCircuit
import Dregg2.Circuit.EffectEmitRegistry   -- Wave 2 central AIR registry: 53 Inst/commit descriptors + actionAirName (56/56)
import Dregg2.Circuit.TurnEmit             -- Wave 2 turn emission: registry-backed StepEmittedSat/turnEmittedSat + step_emitted_refines_fullActionStep + turn_emitted_refines_turnSpec/exec
import Dregg2.Circuit.TurnCircuitCompose   -- Wave 5 whole-turn composition: turnCircuitOfEmitted fold + turn_emitted_refines_exec_direct (open: macaroon/root-glue/multi-step)
import Dregg2.Circuit.Poseidon2Fr          -- The Poseidon2-BN254⟨3⟩ hash gadget over the R1csFr foundation (chain/gnark/poseidon2_bn254.go in Lean): (n,t,d)=(256,3,5), R_F=8, R_P=56, HorizenLabs RC3 constants verbatim; `permute` = the semantic Fr-function (exact poseidon2Bn254Ref schedule) + `permCircuit` = the gadget as the gnark-style frontend op-DAG (every S-box product AND linear-layer output snapped to a fresh var — Wire trees stay O(1), the DAG is the variable graph; 240 muls = 80 S-boxes × 3, matching the Go's R1CS cost) + `compress` = the deployed 2-to-1 Merkle squeeze. #guard KAT BIT-EXACT vs the zkhash/Go gold vector for [0,1,2] (bn254KATOutHex verbatim) at the reference, frontend-circuit AND lowered-R1CS levels, reject polarities mirrored from the Go tests (tampered input diverges; tampered output lane refused at both levels), circuit≡permute differential on the Go test's 4 input triples; cross-language differential vs poseidon2Bn254Ref verified on all of them + compress(0,1) at authoring. Soundness/completeness faces inherited from R1csFr (permCircuit_sound via lower_sound; permCircuit_complete via gHolds). #assert_axioms-clean
import Dregg2.Circuit.ChallengerFr         -- The MULTIFIELD Fiat-Shamir challenger as a frontend op-DAG over Fr, BIT-EXACT to the deployed Go gadgets (chain/gnark/challenger_bn254.go + multifield_challenger.go + poseidon2_bn254.go): pure ref twins (perm/Chal/MRef) #guard-pinned to the zkhash Poseidon2Bn254 gold vector, the 4-challenge+16-bit-idx native duplex KAT, and the FORK-EXECUTED 6-challenge+20-bit-idx MultiField gold KAT (pack-radix-2^31/8-limb, split-base-p/7-limb, length-tagged absorb) — all polarities canaried; PLUS the op-DAG gadget layer (GChal/GMChal, one minted var per gnark DAG node, hint-shaped witness decompositions with booleanity+exact-recomposition asserts) whose built KAT circuits are #guard-SATISFIED under the canonical assignment and REJECT tampered absorb/challenge/index; one full permutation probe pushed through Circuit.lower to GENUINE R1CS (satisfied at the gold vector, refuted off it) + lower_sound instantiated at the built circuits (no adversarial aux filling fakes the transcript). Named enforcement seam: gnark's range-check families on witness decompositions (canonicity/38-bit remainder/lex bound/reducedness) are soundness-against-malicious-prover constraints, not the computed transcript the KAT pins
import Dregg2.Circuit.R1csFr               -- R1CS-over-Fr FOUNDATION for the Lean-authored gnark verifier: Fr = ZMod rBN254 (BN254 scalar field), gnark-frontend op-DAG (var/const/add/mul/select + assertIsEqual) lowered to GENUINE R1CS (LinComb triples ⟨A,z⟩·⟨B,z⟩=⟨C,z⟩, degree ≤ 2 BY TYPE — not the dead ℤ rail's unbounded Expr) with fresh-aux minting per nonlinear node; gHolds (frontend satisfied ↔ lowered R1CS satisfied by the canonical extension) + the STRONGER lower_sound (ANY satisfying R1CS witness agreeing on frontend vars forces acceptance — constraints FORCE the aux region, prover can't cheat it). #assert_axioms-clean; #guard teeth both polarities (a·b=c accepted at 15 / refuted at 14, frontend AND R1CS level; select mux both branches). Named seam (non-load-bearing): Field Fr upgrade awaits a 254-bit Pratt primality certificate — CommRing is all R1CS satisfaction consumes
import Dregg2.Circuit.BabyBearFr           -- The BabyBear-in-BN254 field gadget over the R1csFr foundation (chain/gnark/babybear.go + babybear_ext.go in Lean): p = 2^31−2^27+1 carried as ONE canonical Fr variable; ReduceBounded = hinted (q,r) Euclidean decomposition with r canonical + q range-pinned, branch-for-branch the Go (Add/Sub/Mul/Pow7 + the degree-4 extension X⁴=11 schoolbook ExtMul with one Reduce(·,68) per coefficient). Two synchronized layers: ℕ value mirror of babybear_ref.go/babybear_ext_ref.go + the StateM circuit builder emitting Wire asserts with honest-hint witness generation. #guard KAT BIT-EXACT vs deployed-Go-EXECUTED outputs (8 base pairs × add/sub/mul, 7 pow7, 5 extension vectors × mul/add/sub incl. the X³·X³=11·X² binomial pin), each checking circuit-satisfied + wire-eval-exact + canonical; one vector through the FULL R1csFr lowering (lower + extend satisfied); reject polarities mirrored from the Go tests (p / [p,2^31) gap / 2^31 / rBN254−5 ingestion refused; tampered mul + tampered ext coefficient refused). Named seam: rangecheck realized as bit decomposition (same contract v<2^bits; deployed gnark uses a lookup argument). #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.EmitFaithful -- gnark emit socket over R1csFr: GnarkCircuitData (name + public-input map + gadget records + Circuit) → Emitted wire mirror (EWire) with decode inverse + standalone satisfiedEmitted; decode_emit (round trip, serializer injective) + emit_faithful (gHolds d a ↔ satisfiedEmitted (emit d) a — the R1CS-level denotation ↔ the wire form, composing the foundation's PROVEN gHolds bridge with pointwise emitW_eval). The CircuitEmit discipline re-based to Fr. #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.CanonicityToy -- THE toy end-to-end ∀-refinement: ONE emitted AssertIsCanonical(babybear v) (two 31-bit range checks, gnark babybear.go:69 shape; var 0 = v, 62 bit-hint vars) PROVED for EVERY v : Fr — canonicity_refines : gHolds canonicityData (canonAsg v) ↔ v.val < 2013265921 (BabyBearP) + canonicity_refines_emitted at the wire form + canonicity_sound_of_boolean (ANY boolean hint fill forces canonicity — adversarial face; booleanity-from-b·b=b is the named Pratt/primality seam of R1csFr). Honest hint fill = the BabyBearFr solver posture, closed under a ∀ instead of #guard samples; #guard teeth both polarities retained. #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.EmitJson -- the canonical JSON byte grammar for the gnark emit socket: emitGnarkJson (node grammar — gates {op,args,out} flattened op-DAG + asserts {l,r} + public-input map + gadget-invocation records), rendered from the proof-covered Emitted form (byte renderer adds no constraint content; semantics carried by emit_faithful). canonicityToyJson #guard-pinned byte-for-byte (20034 bytes, 504 gates / 64 asserts) and committed verbatim at chain/gnark/emitted/canonicity_toy.json
import Dregg2.Circuit.Emit.GnarkVerifier.SegmentEmit -- the settlement SEGMENT BIND emitted + leaf-refined (settlement_circuit.go:241–264: the 25-lane public-statement binding, claim[k] = public[k] lane-for-lane in the pinned genesis8++final8++numTurns++chainDigest8 order): segment_refines (gHolds segmentData (segAsg stmt claim) ↔ claim = stmt, ∀ 25-lane vectors) + segment_refines_emitted (wire form) + segment_rejects_tamper (reject polarity) + segment_sound_any_r1cs_witness (purely-linear bind mints ZERO aux vars → ANY satisfying R1CS witness forces equality, no hint hypothesis) + segment_refines_segmentTooth (ties to verifyAlgo/verifyAlgoO tooth 3, FriVerifier.segmentTooth, verbatim). #guard KATs on the REAL apex-shrink fixture claim channel (apex_shrink_fri_real.json table_publics[claim_instance][0:25]) at all three levels, accept + per-lane tamper + lane-swap tamper. #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.QueryPowEmit -- the QueryPow GRINDING check emitted + leaf-refined (grinding.go CheckWitness / fri_verify_native.go CheckWitnessNative, p3 check_witness rev 82cfad7): emitQueryPow n = ToBinary(base,31) hint block + AssertIsEqual(bits[i],0) zero-pins, deployed n=16. queryPow_refines (gHolds (emitQueryPow n) (powAsg v) ↔ v.val < 2^31 ∧ v.val % 2^n = 0, ∀ v : Fr) + _emitted (wire form) + queryPow_refines_deriveQueryPow (AGAINST THE SPEC: ↔ FriVerifier.deriveQueryPow … = some 0 — the verifyAlgoO PoW transition / queryPowCheckUnified accept — ∀ challenger state × witness × carrier) + queryPow_refines_deployed (w16 Poseidon2BabyBear carrier) + queryPow_refines_native (ChallengerFr.MRef MultiField twin, HYPOTHESIS-FREE: observe→squeeze canonicality < p PROVEN) + queryPow_rejects_native (explicit reject polarity, fail-closed InvalidPowWitness) + queryPow_sound_of_boolean (adversarial face; Pratt seam named). #guard KATs CROSS-LANGUAGE: gold vectors computed by chain/gnark/grinding_ref.go's own grindRef/firstRejectingWitness oracles at the grinding_test.go prefix (bits 4/10/16: witness 42/152/46542 → challenge 160998336/1292138496/275578880; reject 0 → 830621580, low16=18316), replayed bit-exact through the Lean w16 challenger + circuit both polarities; plus a live brute-force grind at the MultiField twin. #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.BatchTableEmit -- the BATCH-TABLE check emitted + leaf-refined (stark_verify_native.go VerifyShrinkStarkAlgebra + stark_constraint_interp.go): the symbolic-DAG rail REUSED not re-authored — RawNode mirrors the Go SymNode taxonomy, resolveNode = the deployed leaf resolution incl. zero substitution, symVals/symFolded = the generic topological interpreter + alpha fold, instantiated at ExtV (KAT: gold folded outputs of the deployed evalSymbolicFoldedRef on the REAL emitted const-table DAG, accept + tamper) and at Fr (the emitted circuit). batchTable_refines (gHolds (batchTableData shapes) (encodeBatchTable insts) ↔ batchTablesCheckUnified frArith (openingsOf insts) = true — the check verifyAlgoUnified consumes: degree pin + vanishing recompute + genuine-inverse pin + quotient identity over rail-derived constraintEvals + global LogUp balance — ∀ well-formed instance lists) + _emitted (wire form) + batchTable_rejects_tampered_quotient / _rejects_unbalanced_bus (reject polarity as theorems). Every slot FORCED by its defining equation (no range-check hints); circuit emitted from VK-side SHAPE only, the deployed sym-file/witness split. #guard teeth both polarities through the FULL R1CS lowering. #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.MerkleEmit -- the MERKLE-BN254 path opening emitted + leaf-refined (merkle_bn254.go VerifyMerklePathBn254 + fri_verify_native.go friMerkleLeafHashNative — the ~80% open_input constraint mass): pathW = the deployed per-level walk (AssertIsBoolean + two Select muxes + Poseidon2Bn254Compress) built READ-ONLY on the committed Poseidon2Fr.compress/permuteW via a generic Emits builder-spec framework (compressW kept irreducible so defeq never drags in the 240-mul permutation). merkle_path_refines (gHolds (merklePathData |sibs|) (pathAsg leaf root sibs bits) ↔ refRoot leaf (sibs.zip bits) = root — the deployed merkleBn254RefRoot walk — ∀ leaf/root/sibs/length-matched bits, both polarities) + _emitted (wire form via emit_faithful) + merkle_path_sound (adversarial face: ANY satisfying witness has var 1 = the recomputed compression walk, no honest-fill hypothesis). #guard KATs CROSS-SIDE bit-exact: the Rust MMCS leaf digests katLeafAHex/katLeafBHex + real MerkleTreeMmcs::commit root katMmcsRootHex (fri_leaf_hash_kat_test.go) via leafHashRef/compress, and the depth-4 merkleBn254KATRootHex (merkle_bn254_test.go) via refRoot — accept + tampered-leaf/wrong-sibling/flipped-bit/corrupted-root reject canaries. #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.FriFoldEmit -- the FRI arity-2 FOLD-CONSISTENCY check emitted + leaf-refined (fri_query.go:136 friFoldRowArity2 + babybear_ext.go:96 ExtAssertIsEqual, consumed by the fri_verify_native.go:228 commit-phase fold loop): the one-round check folded = (e0+e1)/2 + β·(e0−e1)·inv(2s) over the degree-4 BabyBear extension, invS = Π Select(bit_j, ginv_{2+j}, 1) the bit-selected inverse-coset product. Circuit built node-for-node from the committed BabyBearFr gadgets (inputExt/gExtAdd/gExtSub/gExtMul/gExtMulBase/gMul/Wire.select) — field arithmetic REUSED not re-authored (CanonicityToy pattern scaled to a composed gadget program, one honest-run StepTo spec per gadget). friFold_leaf_refines (gHolds (friFoldData s0 s1 β claimed bits) (friFoldAsg …) ↔ foldCheckV … = true — claimed = foldCoreV, ∀ canonical siblings/challenge/claimed × ∀ parent-bit vectors) + _emitted (wire form via emit_faithful) + friFold_leaf_reject (reject polarity: claimed ≠ fold ⇒ ¬ gHolds). #guard KATs on gold fold vectors produced by running the deployed friFoldCoreRef/invSFromParentRef (generator table + Fermat inverses cross-checked vs Go), accept + tampered-coordinate reject both polarities. #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.InputOpenEmit -- TRUE AIR-IN-LEAN for the INPUT-OPEN MMCS opening (stark_open_input.go verifyOpenInputBatchNative + fri_verify_native.go multiField32HashNative/packShiftedBn254 — the last soundness seam of the native STARK verify): the input-MMCS LEAF HASH is the GENERAL multi-block MultiField32 padding-free sponge (multiFieldHashRef/multiFieldHashW: blocks of 16 limbs, 8 shifted BabyBear→BN254-packed limbs per rate slot via the reused packShiftedW, partial-block slot-retention overwrite mode, one Poseidon2 permutation per block, digest = state[0]) — distinct from MerkleEmit's single-slot commit-phase leaf. multiFieldHashW_emits: the sponge builder emits a define-chain whose forced denotation is multiFieldHashRef over the opened row values (BabyBear→BN254 packing AUTHORED IN LEAN, not supplied by the Go gadget). inputOpen_refines (gHolds (inputOpenData |rows| |sibs|) (ioAsg …) ↔ refRoot (multiFieldHashRef rows) (sibs.zip bits) = root — "hash(rows) laddered up the depth-d path == input root", ∀ rows/root/sibs/length-matched bits, both polarities) composes the leaf-hash chain and the pathW compression chain at the CIRCUIT (list-append) level via two single Emits runs (avoids the Bind.bind/StateM whnf blowup) + inputOpen_rejects/_accepts + _refines_emitted (wire form via emit_faithful) + inputOpen_sound (adversarial: ANY satisfying witness has var W = the leaf-hash-then-path recomputation, no honest-fill). #guard KATs CROSS-SIDE bit-exact vs the Rust MMCS gold digests (fri_leaf_hash_kat_test.go): kat4/kat16/kat20 exercise partial-slot / two-slot-one-block / two-block-slot-retention absorb paths, plus composed leaf-hash + 1-level path to the real MerkleTreeMmcs::commit root (katMmcsRootHex), accept + tampered-row reject. depth-18 template JSON pinned by length + FNV-1a, committed at chain/gnark/emitted/inputopen_template.json. #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.InputOpenBatchEmit -- TRUE AIR-IN-LEAN for the REAL multi-height MMCS BATCH tree opening (stark_open_input.go verifyOpenInputBatchNative + stark_open_input_ref.go openInputBatchRootRef — the deployed input-open shape InputOpenEmit's single-leaf template MISSED): a query opens matrices at several LDE heights (per-round log-heights e.g. {18,17,12,3}), sorted tallest-first into HEIGHT CLASSES; the leaf digest is the MultiField sponge (multiFieldHashRef, reused from InputOpenEmit) over the concatenation of ALL rows at the max height; the walk is one arity-2 Poseidon2Bn254Compress per level steered by the query bit; and — the structural piece — when the walk descends to a class-carrying height, that class's row hash is INJECTED via C(digest, hashGroup(class)) consuming NO path node and NO index bit (mmcs.rs:1146-1167). leafHash_refines: the per-WIDTH (width-parametric) MMCS leaf-hash ReplayTemplate (rows-in→leaf-out, NO select) is satisfied IFF the claimed leaf is multiFieldHashRef of the rows. inputOpenBatch_refines (gHolds (batchData (groupRows.map length) |sibs| injMask) (batchAsg …) ↔ batchRefRoot (multiFieldHashRef groupRows.head) (sibs.zip bits) injMask groupRows.tail = root — the injection-interleaved batch walk over the per-class leaf hashes reproduces the claimed input root, ∀ class rows/root/path/length-matched bits/injection schedule, both polarities) via emitsW_walkB (generic wire-accumulator foldlM emit) + the woven batchWalkStepW (compressW + optional multiFieldHashW+compressW) + foldl_weave (the crux: the woven builder walk denotes batchRefRoot) + inputOpenBatch_rejects/_accepts + _refines_emitted (wire form via emit_faithful) + inputOpenBatch_sound (adversarial: ANY satisfying witness has var R = the batch-walk recomputation, no honest-fill). #guard KAT: the batch walk reproduces the REAL apex_shrink fixture input root (query 0 input round 1, classes {18,17,12,3}, root 0x17cef0…, computed by openInputBatchRootRef / zz_kat_dump_test.go) BIT-EXACTLY + tamper-rejects. Templates committed at chain/gnark/emitted/leafhash_template.json + inputopen_batch_template.json (length + FNV-1a pinned). #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.Poseidon2Emit -- TRUE AIR-IN-LEAN for the compression primitive: the Poseidon2-BN254 permutation as a LEAN-EMITTED R1CS constraint TEMPLATE (poseidon2Template : GnarkCircuitData) — the constraints are AUTHORED IN LEAN (emitted from Poseidon2Fr.permuteW over R1csFr), not supplied by the hand-Go gadget; chain/gnark/poseidon2_bn254.go is the pinned REFERENCE, and the Go side REPLAYS the template (8436 invocations) rather than consuming a flattened blob. ONE permutation: 438 asserts = 435 emitted round-schedule defines (240 of them S-box multiplications) + 3 output pins; lowered = 678 R1CS rows (240 bilinear + 438 linear naming rows — the named cost-only seam). poseidon2Template_refines (gHolds poseidon2Template (permAsg x y) ↔ y = Poseidon2Fr.permute x, ∀ x y : St — the emitted CONSTRAINTS tied to the Lean permutation model) + _rejects/_accepts (both polarities) + _refines_emitted (wire form via the proven emit_faithful) + poseidon2Template_sound (adversarial face: ANY satisfying R1CS witness has (z 3, z 4, z 5) = permute (z 0, z 1, z 2), no honest-fill hypothesis). JSON pinned by length + FNV-1a digest, committed at chain/gnark/emitted/poseidon2_template.json. #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.ChallengerReplayEmit -- the FIAT-SHAMIR CHALLENGER DUPLEX emitted + refined (challenger_bn254.go native width-3 duplex sponge, RATE=2/CAPACITY=1, p3 DuplexChallenger rev 82cfad7): the transcript-replay bind — a fresh challenger absorbs a full rate [a0,a1] OVERWRITING lanes 0,1 of the zero state, applies Poseidon2Bn254 (reused Poseidon2Fr.permuteW via MerkleEmit's permuteW_emits — NOT re-authored), and squeezes rate-lane-1 then rate-lane-0 (Sample pops from END). emitChallengerReplay a0 a1 : GnarkCircuitData = the 435-assert permutation define-chain + 2 squeeze pins (absorbed rate vars 0,1; drawn challenges vars 2,3; capacity const 0). challengerReplay_refines (gHolds (emitChallengerReplay a0 a1) (replayAsg a0 a1 c0 c1) ↔ (c0,c1) = duplexDraw2 a0 a1 = ((permute (a0,a1,0)).2.1, (permute (a0,a1,0)).1) — the emitted R1CS accepts a claimed challenge pair IFF it is the deployed challenger's true FS squeeze, ∀ absorbed pair × ∀ claimed pair) + _rejects/_accepts (both polarities: FS binding) + _refines_emitted (wire form via emit_faithful) + challengerReplay_sound (adversarial: ANY satisfying R1CS witness has (a 2, a 3) = duplexDraw2 (a 0) (a 1), no honest-fill). KAT: the EMITTED duplex re-derives challenger_bn254.go's own pinned fixture (observe 11,22,33→s0,s1,s2→observe 44→s3→sampleBits 16 = [bnKS0..bnKS3]+bnKIdx from challenger_bn254_test.go) bit-exact at the wire form via the wrapped ChallengerFr.bnKATBuilt circuit + challengerFixture_no_aux_forgery (lower_sound cover). JSON pinned by length + FNV-1a, committed at chain/gnark/emitted/challenger_replay_template.json. NAMED RESIDUAL: multi-permutation full-fixture chain as ONE ∀-theorem (compose the atom via DefChain.append, mirror pathW_emits), query-index sampleBits EQUALITY, MultiField BabyBear↔BN254 pack/split + the deployed ζ/α/PermAlpha/PermBeta/betas/query shrink-STARK schedule order. #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.SelectorEmit -- the STARK Lagrange-SELECTOR derivation emitted + refined (stark_verify_native.go:379 computeStarkSelectorsNative + the ExtInv gadget :147; domain.rs:262-271 over the trace domain, shift ONE size 2^db): E = ζ^{2^db} by db squarings, Z_H = E−1, isTransition = ζ−g^{-1}, isFirstRow = Z_H·(ζ−1)^{-1}, isLastRow = Z_H·isTransition^{-1} — the exact leaf emitted_verifier_full.go:bindBlockZeta NAMES as the tractable Lean replacement for its hand-Go selector derivation. Circuit built from the committed BabyBearFr gadgets (inputExt/gExtSub/gExtMul) + the ζ-squaring chain gExtExpPow2 + gExtInv (the deployed hinted-inverse ExtInv: mint canonical inv, CONSTRAIN gExtMul(a,inv)==one). selectorTemplate_refines (gHolds (selectorsData db ζ c0 c1 c2) (selectorsAsg …) ↔ selectorCheckV ζ db c0 c1 c2 = true — the claimed triple (isFirstRow,isLastRow,isTransition) IS selectorsV ζ db = computeStarkSelectorsNative, ∀ canonical ζ/claimed under the two honest-inverse pins ζ≠1, ζ≠g^{-1}) + _refines_emitted (wire form via emit_faithful) + selectorTemplate_reject (claimed ≠ derivation ⇒ ¬ gHolds). #guard KAT: selectorsV bit-exact vs the deployed Go on the REAL fixture ζ for every db the shrink uses {0,9,14,15}, honest-inverse pins discharged by decide at the real ζ. Templates committed at chain/gnark/emitted/selectors_db{0,9,14,15}.json (length + FNV-1a pinned). Honest-fill posture (the FriFoldEmit ledger); frontend→R1CS leg inherits R1csFr.lower_sound. #assert_axioms-clean
import Dregg2.Circuit.Emit.GnarkVerifier.EmitVerifier -- THE KEYSTONE: the five committed leaf refinements (fold/merkle/batch/queryPow/segment) + canonicity composed into ONE emitted GnarkCircuitData (emitVerifier) walking verifyAlgoO's six-way AND via disjoint Nat.pair variable blocks (one generic remap_eval_mkM plumbing lemma, no per-leaf offset work). emitVerifier_refines (gHolds (emitVerifier I proof pub) (encodeWitness …) ↔ verifyAlgo … (mkVk I) (mkChecks I) … = true, mkVk/mkChecks = the emitted leaves' verdicts) + emitVerifier_refines_deployed (↔ the six DEPLOYED spec-sides, CITING each committed *_refines) + gnarkDenote/emitVerifier_is_GnarkRefines (IS FriVerifier.GnarkRefines) → emitVerifier_wrap_sound FIRES the already-proven wrap_sound: the abstract-Bool refinement obligation is now a STRUCTURAL theorem (soundness conditional on the named FriLowDegreeSound carrier, as wrap_sound is). #assert_axioms-clean
import Dregg2.Circuit.TurnAdmission        -- Wave 5 admission gate: turnProofRequired + hole_rust_proof_at_commit (explicit open hole)
import Dregg2.Circuit.TurnRefinement      -- Wave 1 turn refinement: turnCircuitStep ⊑ turnSpec ⊑ execFullTurnA; turn_conservation_descends; TurnStateChain scaffolding
import Dregg2.Circuit.TurnEffectRefinement -- Wave 1 turn effect diamond: fullActionCircuitStep dispatch + fullAction_turn_circuit_refines_spec/exec/conservation
import Dregg2.Circuit.CommitDifferential -- THE COMMITMENT DIFFERENTIAL: faithful Lean model of the deployed Rust `CellState::compute_commitment` (`hash_4_to_1` tree over `[balLo,balHi,nonce,fields[0..8],cap_root,record_digest]`) + the named field correspondence (record_digest at index 12 = the authority-residue limb = Lean RH/systemRootsDigest role) + full-limb binding off `compress4Injective` (effectVmCommit_binds_record_digest = audit-P0-2 anti-ghost tooth) + the residue-free ZERO no-op. Twin: `circuit/tests/effect_vm_commit_lean_differential.rs`
import Dregg2.Circuit.RotatedCommitDifferential -- THE ROTATED wire-commit COMMITMENT DIFFERENTIAL (the ACTUALLY-PUBLISHED commitment): faithful Lean model of the deployed Rust `compute_canonical_state_commitment_v9_felt` / `rotation_witness::wire_commit` (`wireCommitR` over the 31 rotated pre-iroot limbs `[cells_root,r0..r23,cap_root,nullifier_root,heap_root,lifecycle,epoch,committed_height]`) — the OLD/NEW_COMMIT the light client pins — with the authority residue NAMED at index 24 (= register r23 = `compute_authority_digest_felt`) + full-limb binding off the ONE `Poseidon2SpongeCR` floor via `wireCommitR_binds` (rotatedCommit_binds_authority_digest = audit-P0-2 anti-ghost tooth on the PUBLISHED commit) + the per-cell↔rotated shape correspondence. Twin: `circuit/tests/effect_vm_rotation_flip.rs`
import Dregg2.Circuit.WitnessExtractPerEffect -- per-effect ADVERSARIAL-witness extraction (v2 single-component): an ARBITRARY satisfying PI-bound trace FORCES the declarative spec (hostile-witness closure, no dead `hEnc`) + anti-ghost `*_extract_rejects_*` teeth, for 17 v2 effects (mint[ref], transfer, balanceA, burn, the cap-write authority family, noteCreate/Spend, bridgeMint, cellSeal/Unseal, refreshDelegation, receiptArchiveLifecycle). #assert_axioms-clean.
import Dregg2.Circuit.WitnessExtractV1PerEffect -- per-effect ADVERSARIAL-witness extraction (v1 single-component, `EffectCommit`): the generic `WitnessExtractV1.effect_extract` instantiated for setPermissions/setVK/setProgram (the cap-write family), incrementNonce, emitEvent, makeSovereign, refusal, receiptArchive, pipelinedSend (9 effects) + reject teeth. #assert_axioms-clean.
import Dregg2.Circuit.WitnessExtractDual -- per-effect ADVERSARIAL-witness extraction (DUAL-component, `EffectCommit2Dual`): the generic `effect2dual_extract` + cellDestroy (lifecycle+death-cert) and heapWrite (cell+heap) instances + component-1/component-2/log reject teeth. #assert_axioms-clean.
import Dregg2.Circuit.WitnessExtract3 -- per-effect ADVERSARIAL-witness extraction (TRIPLE-component, `EffectCommit3`): the generic `effect2triple_extract` (PIBindsDigestsTriple over the 3 components) + createCellA (accounts+bal+born-empty-side) instance + component-1/2/3/log reject teeth + mutation #guards. #assert_axioms-clean.
import Dregg2.Circuit.WitnessExtract5 -- per-effect ADVERSARIAL-witness extraction (QUINT-component, `EffectCommit5`): the generic `effect2quint_extract` (PIBindsDigestsQuint over the 5 components) + spawnA (accounts+create-leg+caps+delegate+delegations) and createCellFromFactoryA (accounts+bal+cell+slotCaveats+born-empty-authority) instances + component-1..5/log reject teeth + mutation #guards. #assert_axioms-clean.
import Dregg2.Circuit.WitnessExtractComposite -- COMPOSITE ADVERSARIAL-witness extraction (exerciseA = v1 hold-gate ∘ inner-turn fold): exerciseHold_extract/_authority (ARBITRARY PI-bound hold witness FORCES ExerciseHoldSpec/exerciseGuard via WitnessExtractV1.effect_extract) + exerciseA_extract (hold leg ∘ inner-turn bridge ⇒ ExerciseSpec) + log-forge reject tooth + the named inner-fold residual. #assert_axioms-clean.
import Dregg2.Circuit.CircuitOpenFronts -- explicit open-front registry (Waves 3–7); no lurking holes
import Dregg2.Exec.HandlerOpenFronts    -- explicit HANDLER-executor open-front registry (Wave 6/7); now in default build so CI catches the countOpenFronts drift + portal re-exports
import Dregg2.Circuit.CoordinatedTurnRefinement -- Wave 1 inter-vat: bilateral/coordinated turn spec + circuit scaffold + charter binding
import Dregg2.Circuit.CoordinatedTurnEmit      -- Wave 1 inter-vat emission: coordinatedTurnCircuit + emittedCoordinatedTurn + emit faithfulness + spec soundness
import Dregg2.Circuit.Spec.exercise        -- Wave 1 exercise composite: re-exports ActionDispatch + execFullA_exerciseA_iff_spec
import Dregg2.Circuit.Inst.exerciseA       -- D5 v1 composite: exerciseA_full_sound ⇒ ExerciseHoldSpec; hold circuit + inner turn ⇒ ExerciseSpec
import Dregg2.Circuit.Inst.noteCreateA     -- D5 v2 breadth: noteCreateA_full_sound ⇒ NoteCreateASpec (commitments list)
import Dregg2.Circuit.Inst.noteSpendA      -- D5 v2 breadth: noteSpendA_full_sound ⇒ NoteSpendSpec (nullifiers list)
-- (F2a) the queue family's per-effect circuit strata DELETED (Inst/Witness/Emit/Spec/Argus-weld ×
-- queueAllocate/Enqueue/Dequeue/Resize/AtomicTx/PipelineStep): VerbRegistry classifies all six
-- `.factory .queue`; the behavior lives in Apps/{QueueFactory,InboxFactory,PubsubFactory}. The
-- kernel verbs themselves die in F2b.
import Dregg2.Circuit.Inst.pipelinedSendA   -- D5 v1 breadth: pipelinedSendA_full_sound ⇒ PipelinedSendSpec (log-only)
import Dregg2.Circuit.Inst.revoke          -- D5 v2 breadth: revoke_full_sound ⇒ RevokeSpec (caps)
import Dregg2.Circuit.Inst.revokeDelegationA -- D5 v2 breadth: revokeDelegationA_full_sound ⇒ RevokeSpec (caps)
import Dregg2.Circuit.Inst.revokeDelegationFullA -- D5 v2-dual: revokeDelegationFull_full_sound ⇒ RevokeDelegationFullSpec (caps + forced epoch step)
import Dregg2.Circuit.Inst.transfer        -- D5 v2 breadth: transfer_full_sound ⇒ BalanceMovementSpec (bal funcComponent)
import Dregg2.Circuit.Inst.emitEventA      -- D5 v1 breadth: emitEventA_full_sound ⇒ EmitEventSpec (log-only, touched=∅)
import Dregg2.Circuit.Inst.incrementNonceA -- D5 v1 breadth: incrementNonceA_full_sound ⇒ IncrementNonceSpec (cell)
import Dregg2.Circuit.Inst.makeSovereignA -- D5 v1 breadth: makeSovereignA_full_sound ⇒ MakeSovereignSpec (cell rebind)
import Dregg2.Circuit.Inst.receiptArchiveA -- D5 v1 breadth: receiptArchiveA_full_sound ⇒ ReceiptArchiveSpec (cell audit)
import Dregg2.Circuit.Inst.refusalA        -- D5 v1 breadth: refusalA_full_sound ⇒ RefusalSpec (cell audit)
import Dregg2.Circuit.Inst.setPermissionsA -- D5 v1 breadth: setPermissionsA_full_sound ⇒ SetPermissionsSpec (cell)
import Dregg2.Circuit.Inst.setVKA          -- D5 v1 breadth: setVKA_full_sound ⇒ SetVKSpec (cell)
import Dregg2.Circuit.Inst.setProgramA    -- SetProgram apex weld: setProgramA_full_sound ⇒ SetProgramSpec (cell)
import Dregg2.Circuit.Inst.cellSealA       -- D5 v2 breadth: cellSealA_full_sound ⇒ CellSealSpec (lifecycle funcComponent)
import Dregg2.Circuit.Inst.cellUnsealA    -- D5 v2 breadth: cellUnsealA_full_sound ⇒ CellUnsealSpec (lifecycle funcComponent)
import Dregg2.Circuit.Inst.cellDestroyA    -- D5 v2-dual: cellDestroyA_full_sound ⇒ CellDestroySpec (lifecycle+deathCert)
import Dregg2.Circuit.Inst.refreshDelegationA -- D5 v2 breadth: refreshDelegationA_full_sound ⇒ RefreshDelegationSpec (delegations)
import Dregg2.Circuit.Inst.heapWriteA      -- THE ROTATION: heapWriteA_full_sound ⇒ HeapWriteSpec (cell heap_root register + heaps splice, v2-dual)
import Dregg2.Circuit.Emit.EffectVmEmitHeapRoot -- THE ROTATION: the in-row heap_root recompute descriptor gadget (address→leaf→advance sites, cap_root gate family reuse) + heapRoot_binds_write anti-ghost under Poseidon2SpongeCR
import Dregg2.Circuit.EffectInstances2   -- D5 v2 VALIDATION: mintE_full_sound ⇒ MintASpec (bal, funcComponent) + noteSpendE_full_sound ⇒ NoteSpendSpec (nullifiers list, listComponent + growing log), both re-derived THROUGH the v2 framework. Proves v2 works for BOTH non-cell shapes; the TEMPLATE for the ~25 non-cell effects
import Dregg2.Circuit.EffectInstances   -- D5 framework VALIDATION + templates: transferE/setFieldE re-derive Transfer's & setFieldA's circuit⟺spec THROUGH the generic EffectCommit framework — transferE_full_sound ⇒ TransferSpec, setFieldE_full_sound ⇒ SetFieldSpec (the bespoke specs, from realizable Poseidon-CR portals only). Proves the framework is instantiable + sound; the worked TEMPLATE the remaining ~29 effects copy (~100 lines each). #assert_axioms-clean
import Dregg2.Circuit.SetFieldCommit    -- D5 breadth (effect #2/31): FULL-STATE circuit⟺spec for setFieldA — setfield_circuit_full_sound (satisfiedSF ⇒ the full SetFieldSpec, frame reconstructed by funext) + completeness + 4 ANTI-GHOST teeth (rejects field-tamper / third-cell / wrong-target / log-forge) + concrete #guard. INJECTIVITY-only portals (compressNInjective/cellLeafInjective/RestHashIffFrame REUSED from StateCommit + logHashInjective NEW for the receipt-chain growth + AccountsWF). Confirms the generic framework shape: {restHash ⊕ frame-sponge ⊕ touched-leaf ⊕ log-hash}
import Dregg2.Circuit.StateCommit       -- D5-CORE KEYSTONE: FULL-STATE circuit⟺spec (kills the "two-balance projection" ghost). recStateCommit + injective-only portals (CombineInjective/FrameDigestBindsCells/MovedDigestBindsCells/RestHashIffFrame) + AccountsWF (PROVED preserved); transfer_circuit_full_sound (satisfiedS ⇒ FULL TransferSpec, frame RECONSTRUCTED by funext from reused frame-digest EQ gates + injectivity — NO frame/update-correctness portal) + _full_complete + ANTI-GHOST teeth (stateCircuit_rejects_third_cell / _field_tamper + concrete #guard: a forgery accepted by transferCircuit is REJECTED by stateCircuit). Frame PROVED not portaled. De-portaling (concrete Merkle/Poseidon over finite accounts) = tracked follow-up
import Dregg2.Circuit.TransferWitness   -- VERIFIABLE-EXECUTION BEACHHEAD: the WITNESS GENERATOR connecting the REAL executor (recKExec) to the full-state circuit. transferWitnessVec (execute → satisfying assignment, digest cols filled by the concrete commitment surface) + execute_produces_satisfying_witness (committed recKExec ⇒ encodeS satisfies stateCircuit, via _full_complete) + satisfying_witness_proves_full_state (sat ⇒ 18-field TransferSpec, via _full_sound). Concrete #guard: the EXECUTOR-DERIVED witness SATISFIES; the REAL forged post-state (mint bystander cell 2: 50→999) yields a witness the circuit REJECTS (frame-reuse gate 15≠16 = real UNSAT). honestWitnessJson/forgedWitnessJson = the exact executor-derived bytes the Rust lean_executor_derived_transfer prover proves+verifies / rejects. #assert_axioms-clean
import Dregg2.Circuit.TurnTransferWitness  -- WHOLE-TURN ZKP: compose per-effect proofs into ONE authenticated full-turn proof for a chained transfer FOREST. turnTransferWitnessVec runs the real chained executor (chainKernels over recKExec) + lays TWO per-step full-state witnesses + the turn-independent chain-digest columns into ONE width-44 vector. turnStateCircuit = two stateCircuit blocks (step1 offset 20) ++ rootBindGates (CLOSE the 11/12 caveat: vPreRoot/vPostRoot FORCED = cmbConcrete(compressConcrete frame moved) rest, an Expr over the digest children — tampered post-root ⇒ UNSAT) ++ chainGates (post-state of step i = pre-state of step i+1 via the turn-independent full-cell sponge allCellDig + rest digest). turn_witness_proves_both_steps (sat ⇒ BOTH steps' full TransferSpec, reusing transfer_circuit_full_sound twice) + turn_execute_produces_both_steps (chained recKExec ⇒ both step witnesses satisfy). Concrete #guard: executor-derived WHOLE-TURN witness SATISFIES turnStateCircuit (both blocks + root-bind + chain); the REAL forged FINAL post-state (mint bystander cell 0 in k₂) yields a witness the circuit REJECTS (step-1 frame-reuse gate 35≠36 = real UNSAT over the whole turn). turnHonestWitnessJson/turnForgedWitnessJson + turnStateDescriptorJson = the exact bytes the Rust lean_executor_derived_turn prover proves+verifies / rejects. #assert_axioms-clean.
import Dregg2.Circuit.Emit.EffectVmEmit  -- EFFECTVM-SHAPED EMISSION IR: VmGate/VmConstraint(gate/transition/boundary/piBinding)/VmHashSite(ordered Poseidon2 sites = the Rust hash_sites() contract)/EffectVmDescriptor over the REAL 186-col layout (sel/state/param/aux named offsets). satisfiedVm denotes the running EffectVmP3Air's row-quantified constraint set (when_first/when_last guarded). emitVmJson = the wire grammar. The IR the concrete per-effect circuits emit through.
import Dregg2.Circuit.Emit.EffectVmEmitTransfer  -- TRANSFER concrete circuit EMITTED through the EffectVM IR + FAITHFULNESS: transferVmDescriptor (debit/credit + nonce-increment + cap/field/reserved passthrough gates + GROUP-4 state-commit hash chain (4 ordered H4 sites) + 4 boundary pins + PI bindings) PROVED equivalent to the transfer intent (intentBalLo debit/credit, nonce+1, frame fixed) that CircuitSpecTriangle pins abstractly. transferVm_faithful (sat ⇔ intent) + anti-ghost (wrong balance/nonce/frame ⇒ UNSAT).
import Dregg2.Circuit.Emit.EffectVmEmitNoteSpendCompose  -- TURN-LEVEL COMPOSITION for the NOT-per-row noteSpend: per-row root-bound descriptor (binds the nullifier-set INSERT into state_commit, anti-ghost) ⊗ turn-level NON-MEMBERSHIP gadget (sorted-tree Merkle, dsl/revocation.rs, the `non-revocation` turn sub-proof — supplies FRESHNESS nf∉nullifiers) ⊗ turn-level §8 proof gadget ⟹ FULL NoteSpendSpec (all 17 fields). compose_perRow_and_turnGadget_suffices (sufficiency) + stale_nullifier_does_not_commit (turn gadget LOAD-BEARING, fail-closed) + compose_commits_iff_turn_gadgets_accept (IFF). #assert_axioms clean. The layering: every leg graduates at the per-row OR the turn/accumulator layer with a named gadget.
import Dregg2.Circuit.Witness.DelegateWitness  -- B2 v2 WITNESS BEACHHEAD (delegate): delegateWitnessVec runs recCDelegate + lays the v2 full-state witness (width 72), digest cols from a concrete computable surface (capsDigConcrete/rhConcrete/lhConcrete); execute_produces_satisfying_witness + satisfying_witness_proves_full_state reuse delegate_full_sound/effect2_circuit_full_complete (CR portals carried). Concrete #guard: executor-derived witness SATISFIES; the REAL forged post (recipient steals node 9) yields a witness the circuit REJECTS (component-bind gate 68≠69 = real UNSAT). The v2 witness TEMPLATE.
import Dregg2.Circuit.Witness.CellSealWitness  -- B2 v2 WITNESS (cellSealA/cellUnsealA lifecycle): seal/unsealWitnessVec run execFullA + lifeDigConcrete; executor-derived witness SATISFIES; forged 3rd-cell lifecycle flip REJECTED (component-bind 68≠69). Reuses cellSeal/UnsealA_full_sound.
import Dregg2.Circuit.Witness.CreateCellWitness  -- B2 v2-triple WITNESS (createCellA accounts+bal+born-empty): executor-derived witness SATISFIES; forged 3rd-cell bal mint REJECTED (comp2-bal gate 70≠71). Reuses createCellA_full_sound.
import Dregg2.Circuit.Witness.CreateCellFromFactoryWitness  -- B2 v2-quint WITNESS (createCellFromFactoryA accounts+bal+cell+slotCaveats+born-empty-authority): executor-derived witness (facS fixture) SATISFIES; forged bystander bal mint REJECTED (comp2-bal gate 70≠71). Reuses createCellFromFactoryA_full_sound.
import Dregg2.Circuit.Witness.RefusalWitness          -- B3 v1 WITNESS (refusalA audit-slot write): refusalWitnessVec runs execFullA + lays the v1 full-state witness (width 74) via Common.layoutE/SConc; executor-derived witness SATISFIES; forged 3rd-cell mint (50→999) REJECTED (frame-reuse gate 68≠69). Rust lean_executor_derived_refusal proves+verifies / rejects. Reuses refusalA_full_sound.
import Dregg2.Circuit.Witness.ReceiptArchiveWitness   -- B3 v1 WITNESS (receiptArchiveA lifecycle-slot write): same shape as refusalA; forged 3rd-cell mint REJECTED (frame-reuse 68≠69). Rust lean_executor_derived_receipt_archive. Reuses receiptArchiveA_full_sound.
import Dregg2.Circuit.Witness.RefreshDelegationWitness -- B3 v2 WITNESS (refreshDelegationA delegations funcComponent): refreshWitnessVec runs execFullA + delegationsDigConcrete; executor-derived witness SATISFIES; forged stolen delegation cap REJECTED (component-bind 68≠69). Rust lean_executor_derived_refresh_delegation. Reuses refreshDelegationA_full_sound.
import Dregg2.Circuit.Witness.MintWitness            -- B4 v2 WITNESS (mintA bal funcComponent): mintWitnessVec runs execFullA + balDigConcrete; executor-derived witness SATISFIES; forged 3rd-ledger mint (50→999) REJECTED (component-bind 68≠69). Rust b4_executor_derived_mint. Reuses mintA_full_sound.
import Dregg2.Circuit.Witness.NoteCreateWitness      -- B4 v2 WITNESS (noteCreateA commitments listComponent): forged 3rd-commitment tamper REJECTED (component-bind 68≠69). Rust b4_executor_derived_note_create. Reuses noteCreateA_full_sound.
import Dregg2.Circuit.Witness.NoteSpendWitness       -- B4 v2 WITNESS (noteSpendA nullifiers listComponent, anti-replay): forged nullifier-drop (double-spend laundering) REJECTED (component-bind 68≠69). Rust b4_executor_derived_note_spend. Reuses noteSpendA_full_sound.
import Dregg2.Circuit.Witness.PipelinedSendWitness   -- B4 v1 WITNESS (pipelinedSendA touched=∅ log-only): forged bystander-cell mint (frozen-frame) REJECTED (frame-reuse gate 68≠69). Rust b4_executor_derived_pipelined_send. Reuses pipelinedSendA_full_sound.
import Dregg2.Circuit.Witness.Common                  -- B3 SHARED v1 surface: concrete CommitSurface SConc (StateCommit toy primitives + lhConcrete log hash) + layoutE (encodeE tabulated, unconstrained roots 64/65 zeroed for i64-safety) + witnessJson. The v1 witness generators' common base.
import Dregg2.Circuit.Witness.EmitEventWitness        -- B3 v1 WITNESS (emitEventA log-only touched=∅): emitEventWitnessVec runs execFullA + layoutE/SConc; executor-derived witness SATISFIES; forged receipt-row (log gate 72≠73) + minted bystander cell (frame gate 68≠69) REJECTED. Rust lean_executor_derived_emit_event. Reuses emitEventA_full_sound.
import Dregg2.Circuit.Witness.IncrementNonceWitness   -- B3 v1 WITNESS (incrementNonceA touched={cell} nonce bump): forged bystander-cell mint (frame 68≠69) + tampered receipt (log 72≠73) REJECTED (wrong-nonce rides cellLeafInjective: toy leaf=balance-only). Rust lean_executor_derived_increment_nonce. Reuses incrementNonceA_full_sound.
import Dregg2.Circuit.Witness.MakeSovereignWitness    -- B3 v1 WITNESS (makeSovereignA commitment rebind touched={cell}): the rebind MOVES the balance so touched-bind gate (70/71) is meaningful — wrong-rebound (70≠71) + bystander mint (68≠69) REJECTED. Rust lean_executor_derived_make_sovereign. Reuses makeSovereignA_full_sound.
import Dregg2.Circuit.Witness.ExerciseWitness         -- B3 v1 WITNESS (exerciseA composite hold-gate, log-only outer layer): exerciseWitnessVec runs exerciseStepA (kernel frozen + auth receipt); forged auth receipt (log 72≠73) + bystander mint (frame 68≠69) REJECTED. Rust lean_executor_derived_exercise. Reuses exerciseA_full_sound.
import Dregg2.Circuit.Witness.DelegateAttenWitness    -- B3 v2 WITNESS (delegateAttenA gated attenuated caps grant): delegateAttenWitnessVec runs execFullA (recCDelegateAtten); forged stolen-cap REJECTED (component-bind 68≠69). Rust lean_executor_derived_delegate_atten. Reuses delegateAttenA_full_sound.
import Dregg2.Circuit.Witness.IntroduceWitness        -- B3 v2 WITNESS (introduceA authority-introduce caps grant): introduceWitnessVec runs execFullA (recCDelegate); forged stolen-cap REJECTED (component-bind 68≠69). Rust lean_executor_derived_introduce. Reuses introduceA_full_sound.
import Dregg2.Circuit.Witness.balanceAWitness        -- B1 v2 WITNESS (balanceA bal funcComponent): balanceWitnessVec runs recCexecAsset + balDigestC; executor-derived witness SATISFIES; forged bystander bal mint (50→999) REJECTED (component-bind 68≠69). Rust lean_executor_derived_balance_a. Reuses balanceA_full_sound.
import Dregg2.Circuit.Witness.burnAWitness           -- B1 v2 WITNESS (burnA bal funcComponent, debit): burnWitnessVec runs recCBurnAsset; forged bystander bal mint REJECTED (component-bind 68≠69). Rust lean_executor_derived_burn_a. Reuses burnA_full_sound.
import Dregg2.Circuit.Witness.bridgeMintAWitness     -- B1 v2 WITNESS (bridgeMintA bal funcComponent, credit): bridgeMintWitnessVec runs recCMintAsset; forged bystander bal mint REJECTED (component-bind 68≠69). Rust lean_executor_derived_bridge_mint_a. Reuses bridgeMintA_full_sound.
import Dregg2.Circuit.Witness.attenuateAWitness      -- B1 v2 WITNESS (attenuateA caps funcComponent, TOTAL): attenuateWitnessVec runs attenuateStepA; forged bystander stolen-cap (privilege escalation) REJECTED (component-bind 68≠69). Rust lean_executor_derived_attenuate_a. Reuses attenuateA_full_sound.
import Dregg2.Circuit.Witness.cellDestroyAWitness    -- B1 v2-dual WITNESS (cellDestroyA lifecycle+deathCert): cellDestroyWitnessVec runs cellDestroyChainA (width 74); forged bystander collateral-kill REJECTED (lifecycle comp-1 gate 68≠69). Rust lean_executor_derived_cell_destroy_a. Reuses cellDestroyA_full_sound.
-- B2/B1 v2 WITNESS (tail): the remaining executor-derived witness modules (CI-coverage; each forges a 3rd-party tamper that the component-bind gate rejects):
import Dregg2.Circuit.Witness.SetPermissionsWitness  -- setPermissionsA witness
import Dregg2.Circuit.Witness.SetVKWitness           -- setVKA witness
import Dregg2.Circuit.Witness.SpawnWitness           -- spawnA witness
import Dregg2.Circuit.Witness.RevokeDelegationWitness -- revokeDelegationA witness
-- D5 executor⟺spec wave (independent declarative full-state spec + executor⟺spec per effect family; each #assert_axioms-clean):
import Dregg2.Circuit.Spec.balancemovement       -- balanceA: recCexecAsset_iff_spec + debit/credit/other-untouched + rejects unauthorized/overdraft/self
import Dregg2.Circuit.Spec.supplycreation        -- mintA: per-asset supply mint spec (supply Δ = +amt at one asset, frame elsewhere)
import Dregg2.Circuit.Spec.supplydestruction     -- burnA: per-asset supply burn spec
import Dregg2.Circuit.Spec.SupplyAbstractBinding  -- binds MintASpec/BurnSpec to the abstract Metatheory.Dynamics laws (kernel ⟹ Spec ⟹ abstract Verb meta-law): mintVerb + mint_refines_abstract_verb (the mintAuthorizedB ⟹ AuthorizedProduction bridge + conservation Fpu), mutation tooth, non-vacuity
import Dregg2.Circuit.Spec.TransferAbstractBinding -- VALUE-MOVE exemplar #2: binds BalanceMovementSpec (transfer) to the abstract Verb meta-law — transferVerb + transfer_refines_abstract_verb (authorizedB ⟹ AuthorizedProduction bridge + conservation Fpu of the debit/credit move), mutation tooth, non-vacuity
import Dregg2.Circuit.Spec.AbstractVerbAdapter -- the SHARED adapter factoring the per-effect abstract-verb binding: heldOfGate/gate_covers_production (any boolean authority gate ⟹ AuthorizedProduction), stateWriteVerb (the delta=0 verb shape) + stateWriteVerb_fires/_footprint, gateProduction_not_fpu (the shared mutation tooth), idle_authorized_production (the no-authority leg)
import Dregg2.Circuit.Spec.StateWriteAbstractBinding -- STATE-WRITE family bound as governed Metatheory.Verb instances via the adapter: setField/setVK/setPermissions/setProgram/incrementNonce/makeSovereign/refusal/receiptArchive (authority via stateAuthB ⟹ AuthorizedProduction) + emitEvent (NO-authority impedance: idle authority leg, produced ∅). Shared mutation tooth + non-vacuity
import Dregg2.Circuit.Spec.CapFamilyAbstractBinding -- CAP family bound as governed Metatheory.Verb instances: delegate/delegateAtten (+ grantCap/introduce/attenuate shape; connectivity gate (caps del).any (confersEdgeTo t) ⟹ AuthorizedProduction, non-amplifying conferred ⊆ held) + refreshDelegation (stateAuthB gate); revoke/revokeDelegation = the IDLE-LEG impedance (removal produces nothing, produced ∅). Shared mutation tooth + non-vacuity
import Dregg2.Circuit.Spec.LifecycleAbstractBinding -- LIFECYCLE family (cellSeal/cellUnseal/cellDestroy) bound as governed Metatheory.Verb instances: delta=0 state-writes, authority via stateAuthB ⟹ AuthorizedProduction. Shared mutation tooth + non-vacuity
import Dregg2.Circuit.Spec.NoteGrowthAbstractBinding -- NOTE/GROWTH family bound as governed Metatheory.Verb instances across two honest shapes: EVIDENCE growth (noteSpend/noteCreate via auth_grow_fpu — one-shot linearity = monotone non-membership, evidence tooth) + CONSERVATION-NEUTRAL (createCell/spawn/factory/bridgeMint/pipelinedSend — value-conserving, idle authority/evidence). Teeth + non-vacuity
import Dregg2.Circuit.Spec.cellstatefield        -- setFieldA: field-write spec + full frame
import Dregg2.Circuit.Spec.cellstatemonotone     -- incrementNonceA: monotone nonce-bump spec
import Dregg2.Circuit.Spec.cellstatepermissions  -- setPermissionsA: permissions-write spec
import Dregg2.Circuit.Spec.cellstatevk           -- setVKA: verification-key-write spec
import Dregg2.Circuit.Spec.cellstateprogram      -- SetProgram: program-install (caveat-table) write spec — the ordered mid-session program-install effect (genesis-reframe escape hatch). Single PROTOCOL-managed record-slot write (programField) through the bare authority-gated stateStep, mirroring setVK; SetProgramSpec = the INDEPENDENT full-state spec (17 kernel fields + log enumerated, non-vacuous: rejects unauthorized/non-account/non-live). The program folds into compute_authority_digest_felt → r23, so its circuit witness is the record-pin setProgramV3.
import Dregg2.Circuit.Spec.cellstateaudit        -- receiptArchiveA: archive/checkpoint spec
import Dregg2.Circuit.Spec.sovereigncommitment   -- makeSovereignA: representation-flip spec (balance-neutral)
import Dregg2.Circuit.Spec.accountgrowth         -- createCellA: fresh-cell growth spec (born empty ⇒ conservation-neutral)
import Dregg2.Circuit.Spec.factorycreation       -- createCellFromFactoryA: factory-mint spec (caveats/programVk installed)
import Dregg2.Circuit.Spec.authorityunattenuated -- introduceA: Granovetter introduce spec (non-amplifying edge)
import Dregg2.Circuit.Spec.authorityattenuation  -- attenuateA/delegateAttenA: attenuated delegation spec (granted ⊆ held)
import Dregg2.Circuit.Spec.authorityrevocation   -- revoke/dropRefA/revokeDelegationA: edge-removal spec (authority shrinks)
import Dregg2.Circuit.Spec.notenullifier         -- noteSpendA: nullifier-set insert spec (anti-replay, balance-neutral)
import Dregg2.Circuit.Spec.notecommitment        -- noteCreateA: commitment-set grow-only spec
import Dregg2.Circuit.Spec.bridgeinboundmint     -- bridgeMintA: §8 portal inflow spec (disclosed +value at one asset)
import Dregg2.Circuit.Spec.queuepipelinedsend    -- pipelinedSendA: apply-time-neutral clock-row spec (log-only, full 17-field frame) — SURVIVOR (pipelinedSend ≠ the dissolved queuePipelineStep)
-- D5 executor⟺spec wave-2 (repaired harder families — root cause was a missing `open Dregg2.Exec.EffectsState (stateAuthB)` that cascaded into a leaked open-hole axiom; gate-verified green + #assert_axioms-clean):
import Dregg2.Circuit.Spec.cellstatelog          -- emitEventA: event/log-append spec + full frame (balance-neutral, no authority gate beyond cell-existence)
import Dregg2.Circuit.Spec.celllifecycle       -- cellSealA/cellUnsealA/cellDestroyA: Live↔Sealed↔Destroyed lifecycle spec
import Dregg2.Circuit.Spec.refreshdelegation     -- refreshDelegationA: parent c-list snapshot into delegations
import Dregg2.Circuit.Spec.heapwrite             -- THE ROTATION: heapWriteA full-state spec (heap_root register + heaps splice) + execFullA_heapWriteA_iff_spec triangle corner
import Dregg2.DSLEffect                -- `dregg_effect <name> : <Class>` effects eDSL → Spec.Conservation LinearityClass coloring + inherited obligation; #assert_namespace_axioms-clean
import Dregg2.Exec.Gas                  -- gas-metering layered beside execFullTurn: gasCost_pos (no free action) + gas_exhaustion_fails_closed (over-budget ⇒ none, no partial mutation) + gas_sufficient_runs (pure guard, identical state) + gas_conserves/_preserves_attests (removes no safety); Nat-resource orthogonal to ℤ-conservation
import Dregg2.Proof.BeaconSpaceInterior -- interior-h non-vacuity witness: Measure.infinitePi (Bernoulli 3/4)^ℕ at h=3/4, indep_block via infinitePi_pi; BeaconSpace is non-vacuously instantiable at a genuine interior honest-fraction
import Dregg2.Exec.ProofForest         -- proof-carrying forest composition: per-node StepProofValid × Linked ⇒ whole-run StepInv via execForest_attests; aggregation deferred
import Dregg2.Exec.TriDomain            -- tri-domain conservation (balance + authority + metadata): triConserved_of_execFull; the three domains that dregg1's atomic.rs enforces conserve independently
import Dregg2.Exec.AuthModes           -- 6 authorization modes (OneOf/Custom/CapTpDelivered/Bearer/Token/Unchecked) with witness dispatch + soundness; captp_granted_le_held proves non-amplification
import Dregg2.Exec.FacetAuthority      -- THE FAITHFUL deployed authority model (decision #2A): AuthRequired TIER (auth_tag, None=0…Custom=5; isSatisfiedBy/narrowerOrEqual, faithful to cell/src/permissions.rs) × EffectMask FACET (mask_lo/mask_hi; isEffectPermitted = bit&&&mask≠0 incl. some 0 = deny-all, isFacetAttenuation = bitwise subset, faithful to cell/src/facet.rs). FacetCap = CapabilityRef's authority core (target,tier,facet); authorizedFacetB = the two-axis gate (own src, OR hold a cap over src whose facet permits the turn's effect-kind AND whose tier is satisfied by the provided auth). Teeth (both polarities, #guard-executed): facet_attenuation_amplify_rejected (non-amp), wrong_facet_unauthorized + wrong_tier_unauthorized + impossibleTier_never_satisfied (end-to-end rejects), customTier_matches_vk_only. facet_refines_owner bridges to Kernel.authorizedB. ADDITIVE — toy authorizedB/List Auth untouched; §10 docstring = the file-by-file cutover plan (Kernel.exec if · BalanceMovementSpec.admitGuardA · DeployedCapTree.confersWriteLeaf→two-axis leaf · what List Auth retires). #assert_axioms-clean.
import Dregg2.Exec.EffectTransfer      -- Transfer effect vertical-slice reference template: conserves → authorized → metadata → forward-sim (AbsStep); the pattern remaining effects instantiate
import Dregg2.Spike.TransferAirSoundness -- real air.rs:473 Transfer constraint over BabyBear: field-constraint+range ⇒ ℤ balance update + transfer_underflow_attack (off-circuit wrap gap, now closed in-circuit by the RANGECHECK lane)
import Dregg2.Exec.EffectsPaired       -- Conservative/Paired effects (escrow/notes/obligations/queues/bridge-Σ=0) via the EffectTransfer template; generic pairedStep spine
import Dregg2.Exec.EffectsSupply       -- Generative/disclosed-supply effects (CreateCell/Factory/Spawn/BridgeMint/Lock/Cancel/Finalize); foreign-finality §8-portal
import Dregg2.Exec.EffectsAuthority    -- authority-edit effects (Introduce/Attenuate/Exercise/ValidateHandoff/Refresh/Drop/SetPermissions/RevokeDelegation), each with non-amplification
import Dregg2.Exec.EffectsState        -- Neutral/Monotonic/Terminal field+lifecycle effects (setField/seal/sovereign/emit/refusal/archive/destroy/refs…) via the generic field-write + non-interference
import Dregg2.Exec.UniversalBridge     -- THE EXECUTOR-STATE BRIDGE (the rotation's long pole): uproj = TOTAL projection of RecordKernelState+log onto the ONE Domain×UKey universal memory (all 17 fields + receipt log; registers EMPTY by design) + the 3-verb Blum trace emitters (pre-state-only, Disciplined) + THE AGREEMENT KEYSTONES gwrite/move/create_is_memory_program (post-projection = trace-fold over pre-projection, against stateStepGuarded/recCexec/createCellStep — the executor IS a memory program) + umem adapters (a) cap_leaf_value_codec (generic hash[addr,value] leaf binds the full cap tuple under the CR floor) and (b) index_boundary_mroot_derived/_from_memcheck (the MMR analogue of boundary_root_derived for the receipt-index domain)
import Dregg2.Exec.Continuation        -- THE MID-TURN YIELD SEAM: a yield_point BETWEEN two effects of one in-flight turn. midturn_split/yield_resume_sound — the journal-prefix snapshot then forward-the-rest equals running straight through (fold pre ops = fold (fold pre (take k)) (drop k), over the SAME MemoryChecking.step the executor bridge folds) + resumed_tail_disciplined (the remaining ops are a standalone self-Disciplined program — the passable continuation). State-fold only; the receipt/atomicity boundary stays whole-turn (named, not committed at the cut). #assert_all_clean
import Dregg2.Spec.ExecRefinementFull  -- general forward-sim refinement: exec_full_refines_spec (every execFull step is a permitted abstract step) + full operational square; whole-history closure OPEN
import Dregg2.Exec.ConditionalTurn     -- executable conditional/await turns: execConditionalTurn (topo-sort + EventualRef slots) + condTurn_conserves/_atomic/_dependency_sound/_forward_sim; EventualRef read ↔ Await.Op.await
import Dregg2.Exec.GuardedHole         -- the WEAK guarded hole first-class: GuardedHole (Pred guard on a late EventualRef slot) + fillGuarded (= predStateStepGuarded) + holeFill_binds_in_circuit (a fill binds BOTH its δ and its guard — the predicate cap-bridge / the trust-goal forcing principle) + holeFill_rejects_guard_violation; "determination eager, witness lazy"
import Dregg2.Exec.ConditionalTurnLift -- WIRE-3: lift the partial-turn (ConditionalBatch) into the apex FullActionA op-set. FullAction.toA embeds the 5-op partial-turn vocab into the verified apex vocab; execFullA_toA_eq_execFull_authority — the AUTHORITY fragment (delegate/revoke) lifts EXACTLY (rfl, same chained primitive); execFullTurnA_lift_authority — a whole authority-only ConditionalBatch node executes IDENTICALLY in the apex executor the light client verifies. Honest residual: the VALUE fragment (balance/mint/burn) embeds at a fixed asset modulo projAsset (scoped, not rfl) — see docs/WIRE3-CONDITIONALBATCH-LIFT-SCOPE.md
import Dregg2.Spike.EffectVmConstraints -- real EffectVmAir constraints in Lean: selector exactly-one, NoOp identity, transfer hi/dir, balance-lo range soundness, nonce tick; underflow_now_impossible proves the RANGECHECK closes the wrap gap
import Dregg2.Proof.CordialMiners       -- Cordial-Miners DAG-BFT model (dregg1's actual consensus): cordial_agreement (no two conflicting committed leaders) + cordial_agreement_from_lace (SuperRatification derived); liveness/GST/dissemination/Stingray OPENs
import Dregg2.Exec.ConsensusExec        -- FINALIZATION→EXECUTOR bridge: the tau-finalized block order (FinalizedOrder, anchors Committed) decodes (§8 Decoder seam) + folds recCexec over RecChainedState; finalized_run (finalized order ⇒ well-defined Run recChainedSystem) + finalized_conserves (value conserved across the whole finalized execution) + finalized_sound; SAFETY TOOTH no_conflicting_finalized_state[_from_lace] (rides cordial_no_conflicting_final_leaders: a wave anchors ≤1 leader, fault bound n>3f explicit) + execution-determinism finalized_execution_agreement; REJECTION TOOTH tampered_order_diverges (swapping a finalized turn diverges cell-1 balance — non-vacuous); #assert_axioms-clean
import Dregg2.Spike.EffectVmConstraints2 -- more EffectVmAir constraints: SetField per-field gating + balance_hi range-soundness + state_commitment binding-shape (hash as §8 portal); setfield_aux_honesty_gap (SetField intended-value rests on an off-circuit free aux column — a known gap, as a theorem)
import Dregg2.Distributed.Consensus      -- Titanium Phase 2.1 consensus long pole (grounded in Sridhar 2411.01689 + Wong "Beyond the Whitepaper"): RESILIENCE PAIR not scalar (ResiliencePair tS/tL SEPARATE, asymmetric tL<tS feature; dreggResilience concrete; safety_holds_below_tS rides cordial_no_conflicting_final_leaders_from_lace, liveness_needs_tL separate GSTRound; NEG tooth safety_can_break_above_tS) + equivocation_excluded (Wong 3.1 f+1: double-sign = excludable Equivocator, equivocator_repelled_from_approval; NEG honest_finalization_unforkable) + reconfiguration-safe finality (Wong 3.2 long-range: CheckpointChain strict_mono+authenticated, no_conflicting_finalized_state_reconfig survives validator-set change; NEG long_range_rewrite_rejected) + leaderless ⇒ view-sync class EMPTY (Wong 3.6: blocklace_is_leaderless via ratifyingVoters_perm_length, leaderless_progress from NAMED-OPEN PostGSTProgress, view_sync_class_empty); #assert_axioms-clean
import Dregg2.Distributed.StrandIntegrity   -- C1 FEED INTEGRITY (closes audit A1): the per-creator STRAND = SSB feed as the projection of the lace (Strand/strandTipSeq), the no-equivocation-at-a-slot write invariant StrandForkFree, KEYSTONE strand_single_tip (a fork-free strand has ONE feed head = tip_block_exists_unique) + le_strandTipSeq monotone-seq; the FIXED insert PRESERVES fork-freedom (insert_preserves_forkFree via the find_conflict guard StrandAccepts) whereas the OLD overwriting insert does NOT (NEG forked_strand_not_forkFree = insertOverwrite drops the single tip) + POS fixed_insert_keeps_single_tip; bridge forkFree_of_honestChain reuses Authority.Blocklace.HonestChain (no dup); receipt-chain connection stated read-only (no RecordKernel import); #assert_axioms-clean
import Dregg2.Distributed.StrandAdmission   -- F-4 CLOSURE (red-team Sybil admission): the HYBRID stake-OR-vouch admission gate IN FRONT of finalization (NOT a dup of MembershipSafety=constitution governance; this is WHICH keypairs are even eligible). admitted=isSeed∨vouchedToThreshold∨hasValidBond, vouch graph ROOTED (only seed/bonded vouchers count via isRoot ⇒ a ring of fresh Sybils cannot bootstrap). HEADLINE sybil_block_not_finalizable (every block anchoring the admission-gated finalized order admittedFinalLeaders is from an ADMITTED strand; finalLeaderAtAdmitted gates BlocklaceFinality.finalLeaderAt on admission ⇒ unadmitted_strand_no_final_leader: a Sybil anchors NOTHING) + STAKE path bonded_equivocator_slashable (Blocklace.Equivocation proof ⇒ slash burns ≥minBond, slash_covers_misbehavior burns the WHOLE bond + revokes admission) + VOUCH path vouch_admits_iff_threshold/below_threshold_not_admitted_vouch (≥N distinct rooted vouches admit, <N does not; vouchers_nodup+vouchers_are_rooted = no double-count, no unrooted Sybil vouch). NON-VACUITY #guard fedDemo (Sybil rejected / vouched+bonded admitted / ring-attack fails / equivocator slashed) + on BlocklaceFinality.trace3 (Sybil leader anchors NOTHING). Rust differential federation/src/admission.rs (12 tests: real Ed25519-signed vouches/bonds, forged-sig rejected, lean_feddemo_differential). #assert_axioms-clean
import Dregg2.Distributed.BlocklaceFinality -- Consensus pillar CONNECTION: a FAITHFUL, EXECUTABLE Lean model of the node's REAL finalization rule (blocklace/src/ordering.rs::tau — computeRounds/findAllFinalLeaders/tauOrder over Lace, NOT abstract BFT) + safety (finalLeaderAt_unique/finalLeaders_one_per_wave single-anchor; finalLeaderAt_needs_unique_candidate equivocation tooth; tauOrder_deterministic) + THE EXECUTOR WIRE (executeTau folds the COMPUTED tauOrder through verified ConsensusExec.executeFinalized/recCexec: tau_drives_verified_run = Run recChainedSystem; tau_execution_agreement = same lace ⇒ same executed state) + DIFFERENTIAL golden vector tauGolden (creator,seq order the Rust ordering::tau must reproduce; #guard non-vacuity on the 3-node trace + equivocator-excluded trace, matching Rust ordering.rs tests); #assert_axioms-clean
import Dregg2.Distributed.ConsensusTimeAck -- finalized execution reads authenticated causal time, not validator clocks; non-regression/+bound laws + typed durable Commit/Reject vs retry/fatal ACK prefix (never skips an operational hole); #assert_axioms-clean
import Dregg2.Distributed.FinalizationQuorum -- TIER-2: verified finalization-vote QUORUM DECISION (the collector's hand-Rust logic gets a proven counterpart). quorumRoot + quorumRoot_sound + quorum_no_conflict (THE SAFETY property: two conflicting roots cannot both reach quorum, 2·superMajority(n)>n). Over BlocklaceFinality.superMajority. To be @[export]-ed into the running collector. #assert_axioms-clean.
import Dregg2.Distributed.HybridFinalizationQuorum -- COMPOSES FinalizationQuorum (distinct-signer supermajority) with Crypto.HybridQuorum (per-cert classical∧pq unforgeability) so the RUNNING quorum decision is tied to HYBRID security. HybridVote(signer,root,sigC,sigP) + admittedTally FILTERS by hybridAdmit (mirrors VoteCollector.record's verify_hybrid gate) BEFORE quorumRoot. hybrid_quorum_needs_supermajority_hybrid_votes (≥superMajority n distinct signers each hybrid-verified) + HEADLINE hybrid_quorum_survives_classical_break (PQ-Unforgeable ⇒ even with classical=rubber-stamp/Shor, an accepted quorum ⇒ Signed root; composes quorumRoot_sound's ≥1 admitted vote through HybridQuorum.hybrid_survives_classical_break) + hybrid_no_forged_quorum_under_classical_break (contrapositive: ¬Signed ⇒ no quorum) + hybrid_quorum_no_conflict_under_break (transfers quorum_no_conflict; proof ignores Vc ⇒ safety survives the break). NON-VACUITY #guards: all-hybrid-valid tally reaches quorum; a forged-PQ member is EXCLUDED ⇒ no quorum. #assert_axioms-clean.
import Dregg2.Distributed.HistoryAggregation -- IVC FOLD MODEL under circuit/src/ivc_turn_chain.rs (the magnesium→gold meaning layer): a chain of ChainSteps = genuine recCexec transitions over RecChainedState whose roots are the §8 recStateCommit; the TurnChainBindingAir temporal tooth new_root[i]==old_root[i+1] (Continues/ChainBound); HEADLINE wellformed_attests_whole_history (every turn recCexec-correct + ChainBound ordered + the whole chain is Run recChainedSystem ⇒ wellformed_history_conserves no-mint/burn over ALL history); root_tooth_pins_state = the CR RECOVERY (recStateCommit_binds: matching seam roots ⇒ equal cellDigest+RH = the root IS the full-state commitment, so a light client seeing only roots learns state continuity); non-vacuity BOTH ways (honest_chain_wellformed positive over teethGenesis + tooth_rejects_broken_order = reordered chain is NOT ChainBound); #assert_axioms-clean
import Dregg2.Distributed.Economics      -- Titanium Phase 2.5 economics/DoS pillar (TITANIUM §2.5 + Wong 4.1 distributed-DoS): FeeSchedule(fee/proveCost/submitCost, proveCost+submitCost OPEN-named) + (1) spam_work_bounded_by_fees (fee≥marginalJunkCost ⇒ networkWork k ≤ feesCollected k ∀k, spam_no_unpaid_work; NEG spam_work_unbounded_when_fee_too_low) + (2) griefing_unprofitable (CONDITIONAL on exact fee≥victimLossPerTurn ⇒ victimLoss≤attackerCost; NEG griefing_profitable_when_fee_too_low) + (3) proposer_reward_covers_cost/proposer_self_sufficient (proposerShare=fee/2 REUSED from Exec.Admission; proveCost≤fee/2 ⇒ reward≥cost; NEG proposer_underwater_when_fee_too_low) + economics_pillar_coherent_witness (all three at one healthy schedule); #assert_axioms-clean
import Dregg2.Distributed.CellMigration  -- cross-federation atomic cell handoff (Hosted↔Sovereign): the two-step PREPARE→ACCEPT→COMMIT (faithful to cell/src/migration.rs + ledger.rs migrate_prepare/accept/commit; per-(fed,cell) Migrated tombstone) with NO-DOUBLE-EXISTENCE at n>1 (handoff_unique_home: live at d, NOT at s; handoff_singleton_home: liveHomes = {d}; accept_refuses_double = the DestinationOccupied gate) + AUTHORITY-CONSERVATION (handoff_full_auth ⇒ handoff_conserves_balance/caps: bal+caps carried byte-for-byte, the per-cell recTotalAsset image; aggBalance bridge) + anti-replay (migrated_cannot_reprepare) + Rust DIFFERENTIAL (#guard n=3 world reproduces prepare/accept/commit arc); commitment binding = named CR assumption (structural); #assert_axioms-clean
import Dregg2.Distributed.ThresholdDecrypt -- federation turn-privacy: t-of-n THRESHOLD DECRYPTION (faithful to federation/src/threshold_decrypt.rs) — Shamir-shared symmetric key over GF(256) AES-field (gf256Mul/gf256Inv byte-for-byte the Rust mod gf256, #guard golden vectors) + Lagrange reconstruction at x=0; HEADLINE shamir_any_t_reconstruct (ANY size-t quorum of distinct share-holders recovers the secret, via Mathlib Lagrange.eq_interpolate — no privileged subset) + secrecy floor shamir_below_t_undetermined (t-1 shares consistent with EVERY secret ⇒ <t leak nothing, needs 0∉eval-points = the 1..n discipline) + combine gate fail-closed (combine_rejects_below_threshold/dup_index/zero_index, combine_admits_iff exact) + share-MAC tamper detection (share_mac_detects_tamper RELATIVE to named Blake3Prf carrier); Rust differential federation/src/threshold_decrypt_diff.rs; #assert_axioms-clean
import Dregg2.Crypto.BlsThreshold       -- §8 discharge: the federation's WEIGHTED-THRESHOLD BLS quorum cert (BLS12-381 + KZG, faithful to federation/src/threshold.rs + the hints crate). The DUAL of ThresholdDecrypt's t-of-n decryption: a CONSTANT-size aggregate cert proving a weighted threshold signed. Models verify_aggregate's THREE gates (hints/src/lib.rs:208): (1) agg_weight≥threshold [pure Nat arithmetic, DISCHARGEABLE], (2) SNARK gate [SnarkPolyIOP, reduces to KzgBinding], (3) final BLS pairing [BlsAggUnforgeable]. HEADLINE accepting_cert_has_quorum (accept ⇒ ∃ sub-committee S⊆members, selectedWeight S≥threshold ∧ ≤totalWeight ∧ ∀i∈S signed) — the quorum-cert SOUNDNESS reduced to the 3 NAMED pairing-crypto primitives (KzgBinding/BlsAggUnforgeable/SnarkPolyIOP), each an IRREDUCIBLE curve/pairing assumption named (the PortalFloor discipline). ANTI-GHOST subquorum_cannot_accept (sub-threshold weight ⇒ ¬accept, given SNARK binding). SignedBy a GENUINE per-member Prop (NOT True): nonsigner_breaks_bls FALSE on a non-signer. Equal-weight #guard differential vs threshold.rs. #assert_axioms-clean
import Dregg2.Crypto.SpongeReduction     -- EXTEND #13: the Poseidon2 SPONGE CR (List ℤ→ℤ) REDUCED to ONE permutation call's compression CR — a REAL reduction (MD/sponge domain-extension), not a sponge-level assumption. Models hash_many (circuit/src/poseidon2.rs:369) line-for-line as a SpongeMachine (perm/init/absorb/squeeze/chunksOf); CompressionCR (one perm-call, primitive #4) + SqueezeBindsReachable (slot-0 truncation residual) + InitStepSeparated (structural length-prefix domain sep) ⇒ spongeCR_of_reduction (Poseidon2SpongeCR M.spongeOf). foldl_step_eq = the MD induction (nested reverseRecOn peels one CompressionCR per block; init-vs-step boundary closed by InitStepSeparated). realizedSpongeOfReduction packages it back as a Poseidon2RealizedSponge so the StateCommit tower's spongeCR is now a THEOREM. Non-vacuity: refMachine (injective perm, carriers HOLD) FIRES the whole reduction; badMachine (constant squeeze) FALSIFIES SqueezeBindsReachable (carrier ≠ True). #assert_axioms-clean
import Dregg2.Crypto.LightClientUC       -- UC light-client UNFOOLABILITY as a REAL reduction to the floor (the OTHER half of UCBridge's commitment-F_com obligation, the FRONTIER's named pale-ghost claim). §1 the ideal functionality F_LC (accept ⇔ executor Produced s) + the real client LCReal (accept ⇔ verify s π) + the environment Env. §2 the soundness GAME Foolable (∃Z: LCReal accepts ∧ ¬Produced) and its negation Unfoolable = REAL≈IDEAL (dummy-adversary form); unfoolable_iff_not_foolable. §3 THE REDUCTION unfoolable_of_floor: ExtractsTo (STARK/Fiat-Shamir extractability, VerifierKernel.extract shape) + SatBindsProduced (StateCommit injectivity = sponge-CR) ⇒ Unfoolable, i.e. light-client soundness ≤ FS-soundness + CR; contrapositive fooling_breaks_floor (a fooling attack CONSTRUCTS a break of extractability — the simulator's extraction run in reverse). §5 the SIMULATOR pinned to "run the extractor" (SimAccepts/real_ideal_observable_agree). §6 DynamicUCResidual names what a FULL Canetti dynamic-UC proof still needs (simulator_ppt/negligible_advantage/composes — the CryptHOL residue) WITH the static reduction DISCHARGED (static_sound := unfoolable_of_floor). Both-polarity non-vacuity: refVerify (sound floor) ⇒ Unfoolable; badVerify (accepts-everything) ⇒ Foolable ∧ ¬ExtractsTo. Does NOT assert UC. #assert_axioms-clean
import Dregg2.Crypto.CommitmentBinding    -- EXTEND #13: (§1) the Poseidon2 2-to-1 compress compressInjective (hash_2_to_1, the Merkle-node + recStateCommit_binds root portal) REDUCED to the SAME one-permutation-call CR (compressInjective_of_compress2 peels the packed-block collision then the injective 2-packing) — no new crypto. ⚑ BUNDLE CUTOVER 2026-07-25: the Compress1CR FIELD is DELETED (it made Compress2 UNINHABITABLE at BabyBear and every ∀ R : Compress2 h theorem VACUOUS); compressInjective_of_compress2 keeps its NAME and CONCLUSION under a PER-INSTANCE ¬ Compress1Coll at the exact packed blocks (implied by the deleted field via compress1Coll_refutable_of_injective, hence strictly stronger), the unconditional form is compressInjective_of_compress2_or_collides, and §1.1 constructs deployedCompress2 — a real deployed-shaped inhabitant whose own compression REFUTES the deleted field. (§2) the BLAKE3 cell-commitment v3/v4 (cell/src/commitment.rs::compute_canonical_state_commitment, a domain-separated blake3::Hasher) REDUCED to BLAKE3 CR (Blake3Kernel.collisionHard, IRREDUCIBLE PRIMITIVE #5) ∘ injective canonical serialization (STRUCTURAL prefix-free byte layout): blake3_commitment_binds (equal commitments ⇒ equal cells). Non-vacuity BOTH ways: ref instances FIRE; badCompress1 (constant) FALSIFIES Compress1CR. #assert_axioms-clean
import Dregg2.Distributed.EpochReconfig  -- federation validator-set RECONFIGURATION / epoch handoff (faithful to federation/src/epoch.rs; ORTHOGONAL to MembershipSafety=blocklace constitution & ThresholdDecrypt=decryption): quorumThreshold(n)=n−n/3 (= compute_bft_threshold, #guard golden + exhaustive diff) + applyDelta member-set transform (applyDelta_count = |old|−|removed|+|added|, applyDelta_nodup) + verifyTransition NO-SAFETY-GAP gate — HEADLINE epoch_handoff_no_gap (verified ⇒ epoch advances by exactly one + attested by quorumThreshold|old| DISTINCT old-epoch members + successor born at correct supermajority + well-formed delta); verified_needs_old_quorum (departing committee's own BFT quorum ratifies successor, no minority seizure) + applied_threshold_is_supermajority + quorum_gt_half/quorums_intersect (forked reconfig needs disjoint quorums = impossible). Ed25519 per-vote check = SigValid PORTAL (EUF-CMA). NON-VACUITY: 4 negative Demo witnesses (under-quorum/outsider-signer/wrong-threshold/non-sequential UNSAT). Rust differential federation/src/epoch_diff.rs (9 tests, incl. forged-Ed25519-sig rejected via real PublicKey::verify); #assert_axioms-clean
import Dregg2.Distributed.CheckpointPrune -- CHECKPOINT-PRUNE SAFETY (devnet --enable-pruning): a FAITHFUL, EXECUTABLE model of the node's storage prune (node/src/blocklace_sync.rs::maybe_produce_checkpoint + config.rs::RetentionPolicy::would_prune, attested by federation/src/checkpoint.rs). RetentionPolicy/wouldPrune byte-for-byte the Rust (Forever/RollingWindow/UntilArchive, #guard golden vs config.rs tests) + pruneLace/recoverFromCheckpoint over Lace. HEADLINE prune_preserves_finalized_prefix (pruning below an attested checkpoint + recovering finalizes the SAME tauGolden (creator,seq) sequence — drops NO finalized turn) + recovered_converges_to_unpruned (recovered node EXECUTES to the same RecChainedState as a never-pruned peer — rides CatchupConverges/LaceMerge.merge_convergence_to_state) + recover_keyset (snapshot+tail recover the FULL keyset, no id lost) + pruned_height_is_attested (config.rs:41 invariant: deletions sit below an attested checkpoint). BLS aggregate-QC attestation = NAMED CheckpointAttested portal (def over opaque blsVerifyCommittee = checkpoint.rs:144 verify_with_committee; the pairing check is IRREDUCIBLE, carried as hypothesis, never defined := True; connects to Crypto.BlsThreshold). NON-VACUITY: concrete trace3 prune/recover (#guard prune DELETES genesis round, recovery reconstructs full keyset, Forever prunes nothing). #assert_axioms-clean
import Dregg2.Distributed.BlsQuorumCert  -- the DISTRIBUTED-consensus meaning of a BLS quorum cert (federation/src/threshold.rs + hints BLS12-381/KZG): the layer ABOVE Crypto.BlsThreshold's single-cert reduction. Under the canonical BFT corruption bound f=⌊n/3⌋ (faultBudget = fault_tolerance, lib.rs:169; quorumThreshold = n−f REUSED from EpochReconfig, lib.rs:155 — #guard golden vectors), models a Corruption set B⊆members (|B|≤f) and the SNARK-extracted signer set (REUSES Crypto.BlsThreshold.SnarkContract/quorum_weight_suffices, NO duplication). HEADLINE quorum_has_honest_signer (an accepting equal-weight QC at quorum threshold has ≥1 HONEST signer ⇒ the corrupt set alone CANNOT forge a QC — the non-forgeability backbone at n>1) + two_quorums_share_honest_member (inclusion–exclusion 2q−n>f ⇒ two QCs over the same committee share an honest member) + no_equivocating_qcs (no honest member double-signs ⇒ ≤1 message gets a QC per slot — the per-slot single-decision theorem, FALSE without the corruption bound). EqualWeight specialization faithful to threshold.rs:228 (weight 1 ⇒ selectedWeight = card). The BLS pairing/SNARK is the NAMED irreducible primitive (Crypto.BlsThreshold carriers), consumed as SnarkContract; what is PROVED is pure finite combinatorics on top. NON-VACUITY: fed4 4-member + corruptOne (member 3 Byzantine) FIRES every theorem; corrupt_cannot_reach_quorum NEG corner. #assert_axioms-clean
import Dregg2.Distributed.ThresholdAdmission -- the HOST-POLICY threshold-signature ADMISSION decision (turn/src/executor/membership_verifier.rs::ThresholdSigVerifier::verify, the governed-namespace Authorization::Custom{vk_hash} gate). The layer ABOVE Crypto.BlsThreshold's single-cert soundness: the FIVE-conjunct verifier decision over a HOST-TRUSTED committee policy, of which TWO conjuncts have NO twin in BlsThreshold — (1) host-pinned committee (StaticThresholdSigPolicy commitment→committee, fail-closed if absent; :1266) and (2) the THRESHOLD-DOWNGRADE DEFENSE / host FLOOR (sig.threshold ≥ committee.threshold_k pinned from the HOST not the QC; :1289) — the security keystone the existing model is blind to (accepts reads the QC's OWN attacker-controlled threshold). admits = the if…return Err cascade conjoined. HEADLINE admits_genuine_quorum (ADMIT ⇒ a genuine weighted quorum of the HOST committee signed msg reaching the HOST FLOOR, not the aggregator's self-declared threshold — accepting_cert_has_quorum LIFTED through the floor, REUSED) + under_floor_refused (the downgrade-resistance keystone: threshold<floorK ⇒ REFUSED REGARDLESS of crypto — a valid 1-of-n QC still refused under a 3-of-n host policy, the malicious-aggregator forge closed) + unregistered_refused (fail-closed) + wrong_committee_refused + admits_iff (exact 3-conjunct extensional gate). Crypto content = Crypto.BlsThreshold NAMED carriers via CryptoAccepts (SnarkContract/BlsContract), never re-derived. NON-VACUITY: reference fed4 + 3-of-4 host floor — ADMIT fires on the genuine QC, REFUSE fires on the 2-threshold (under floor), unregistered commitment, and non-accepting (member-3 non-signer) aggregate; both-polarity #guards mirror the threshold_sig tests (valid_quorum_verifies/under_host_floor_rejected/unregistered_commitment_fails_closed) gate-for-gate. #assert_axioms-clean
import Dregg2.Distributed.QuorumThreshold -- the #170 quorum-unification Lean twin: supermajorityThreshold n = ⌊2n/3⌋+1 BYTE-FOR-BYTE blocklace/src/ordering.rs::supermajority_threshold (the ONE quorum formula; federation quorum_threshold DELEGATES to it) — n=0 FAIL-CLOSED pin (threshold 1, never the vacuous 0; #guard golden values from ordering.rs tests) + threshold_monotone + supermajority_formable/closed_form (= n−⌊(n−1)/3⌋ for n≥1) + HEADLINE supermajority_intersection (Finset form: ANY two threshold-sized quorums of an n-committee intersect in > ⌊n/3⌋ members, UNCONDITIONAL — no StrictBft 3∤n caveat) + two_quorums_share_honest (corrupt set ≤ ⌊n/3⌋ ⇒ two quorums share an HONEST member = the non-forgeability/non-equivocation backbone at every n incl. 3∣n) + bridging historical_le_supermajority/historical_relation (the EXACT +1-at-3∣n gap to EpochReconfig/BlsQuorumCert's historical n−⌊n/3⌋, pinning the safe-side migration). Both-polarity #guards (the n=3 StrictBft-hole witnessed NEGATIVE for the old formula, closed POSITIVE for this one; Demo 6-committee fires every lemma). Rust differential federation/src/bls_quorum_diff.rs (exhaustive 0..=512 sweep) + blocklace ordering.rs goldens. #assert_axioms-clean
import Dregg2.Distributed.DirectoryLaws  -- CLOSES the P3 directory GAP: the canonical named-capability directory (directory/src/directory.rs InMemoryDirectory) bind/resolve/unbind MONOTONE laws (the governed-namespace app + cli resolve names through it ⇒ load-bearing, NOT the ledger's "low-LB primitive" deflection). Faithful register/lookup/revoke models (every gate in source order) over a global monotone CAS version + keyed map. PROVES: register_version_monotone/revoke_version_monotone (the CAS counter only climbs), register_idempotent_noop (same (kind,handle) re-bind = no-op), register_conflict_rejected (different value to a live name REJECTED, no mutation — names don't silently re-bind), lookup_resolves_iff (resolve ⟺ present ∧ ¬revoked ∧ ¬expired), KEYSTONE revoke_is_final (once revoked the name NEVER resolves again at ANY height — the monotone unbind tombstone) + revoke_idempotent_noop + revoke_then_register_conflicts (tombstone is conflict-locked vs silent reclaim) + fresh_entry_version_le_dir. Rust differential directory/src/directory_diff.rs vs the real InMemoryDirectory. #assert_axioms-clean.
import Dregg2.Proof.Stingray            -- concurrent-spend budget slice: sliceCeiling = balance·(f+1)/(2f+1); stingray_no_concurrent_overspend + stingray_schedules_disagree (concrete two-schedule counterexample) + inbudget_both_commit_schedule_agnostic (in-budget = coordination-free); f=0 ⇒ ceiling=balance degenerate case
import Dregg2.Coord.CausalOrder          -- Layer-1 CAUSAL CHAINING (types/src/causal.rs CausalDag, the DAG net+coord share): faithful insertion-ordered Dag + insert (MissingDeps/Duplicate/self-cycle gates) + happenedBefore = transitive closure of the dep edges; PROVES the causal-ordering invariant is a STRICT PARTIAL ORDER (hb_irrefl from acyclicity, hb_trans, hb_asymm), insert_wf (the dep-presence gate preserves wellformedness across the whole history), hb_imp_index_lt (insertion order is a LINEAR EXTENSION ⇒ topological_order respects happened-before), fresh_is_maximal (frontier), concurrent_symm; GENUINELY uncovered by EntangledJoint (which models Layer-2/3, not Layer-1). Rust differential coord/src/coord_diff.rs vs the real dregg_types::CausalDag. #assert_axioms-clean
import Dregg2.Coord.TwoPhaseCommit       -- Layer-2 2PC COORDINATOR DECISION (coord/src/atomic.rs::evaluate_votes): the vote-counting Decision machine EntangledJoint's post-commit fold does NOT cover. evaluate = evaluate_votes byte-for-byte (Commit iff yes≥threshold, Abort iff no>n−threshold, else Pending); PROVES NO-CONFLICTING-DECISION (evaluate_not_commit_and_abort: Commit & Abort never both available — the 2PC agreement), commit_needs_threshold, abort_no_late_commit (abort is SOUND/irreversible: max future Yes < threshold, proving atomic.rs:773's comment), decision_total, unanimous_commit_iff_all_yes (threshold=n ⇒ the all-or-none gate of EntangledJoint.jointApplyAll). Rust differential runs the real atomic::Coordinator w/ Ed25519 votes. #assert_axioms-clean
import Dregg2.Coord.SharedBudgetDynamics -- Layer-3 SHARED-BUDGET DYNAMICS (coord/src/shared_budget.rs): closes the rebalance/resolution NAMED-OPEN that Proof.Stingray left (its §9) and that EntangledJoint's static snapshot does not reach. resolveOrdered = resolve_with_ordering (tau-ordered first-wins resolver) ⇒ TAU-RESOLUTION CONSERVATION (resolveOrdered_accepted_le_balance: Σ accepted ≤ true balance — why optimistic overspend is SAFE, the conservation across the coordination tree); rebalance_conserves (epoch-close exactly transfers reported spend out of the pool); the Stingray Byzantine ceiling = balance·(f+1)/(2f+1) with ceiling_le_balance + overspend_bounded_by_f_ceiling (f·ceiling worst-case undetected surplus). Rust differential vs the real SharedResourceBudget escalation round-trip. #assert_axioms-clean
import Dregg2.Coord.StingrayCertReconcile -- CROSS-EPOCH cert-reconciliation half of Stingray (coord/src/budget.rs::StingrayCounter::rebalance), closing Proof.Stingray §9(1)(2)(3) that SharedBudgetDynamics (the OTHER budget engine, shared_budget.rs) does not reach. Faithful rebalance_inner gate machine (incomplete-quorum → per-cert version/dup/ceiling/pubkey/sig gates → partial-mode missing-charge → balance clamp → version bump). PROVES §9(3) epoch monotonicity/no-replay (rebalance_version_strictly_increases + stale_cert_rejected: a cert for the wrong epoch is rejected ⇒ a spend certified in epoch v cannot be replayed into v+1), §9(2) quorum reconstruction + Byzantine bound (full_rebalance_total_is_cert_sum + byzantine_undetected_overspend_le_f_ceiling: ≤ f·ceiling undetected surplus), §9(1) accepted ⇒ silo's-own-cert under the named CertUnforgeable (Ed25519 EUF-CMA) portal, + rebalance_conserves_on_exact. Rust differential vs the real StingrayCounter::rebalance. #assert_axioms-clean (only CertUnforgeable named).
import Dregg2.Exec.DistributedExports    -- STRONG-FORM swap: the C-ABI @[export] boundary onto the VERIFIED CapTP+coord decision fns, so the RUNNING captp/coord runtime INVOKES the verified Lean (dreggrs Rust → differential sibling, not decider). dregg_captp_validate_handoff (=handoffNonAmplifyingC §6 Granovetter granted⊆held), dregg_captp_process_drop (=CapTPGCConcrete.processDrop F-11/F-12 session refcount), dregg_captp_pipeline_resolve (=Registry FIFO resolve/break drain), dregg_coord_2pc_decide (=TwoPhaseCommit.evaluate), dregg_coord_causal_order (=CausalOrder.happenedBefore via decidable hbBool), dregg_coord_shared_budget (=SharedBudgetDynamics.resolveOrdered tau-resolution). Each PROVED *_eq_* EQUAL to its verified spec (gating live logic on the export gates it on the verified fn by construction). Fail-closed wire codecs round-trip (#guard). #assert_axioms-clean
import Dregg2.Proof.Temporal            -- temporal logic over the living cell: □/◇/◯ on execFullForestA trajectories + proof algebra + always_of_reachable_invariant bridge; always_conserved + always_logMono + always_revoked_persists + always_conj_safety (□(conservation ∧ log-append-only ∧ revoked-persist))
import Dregg2.Proof.CTL                  -- Track B: branching-time CTL — EX/AX/EU/AU/EF/AF/EG/AG as named OrderHom.lfp/gfp over Set System.Config; fixpoint/monotone/duality laws; EF↔Reachable + AG↔Temporal.always cross-checks; SAFETY fragment + branching teeth (EX p ≠ AX p, EF p ≠ AF p on a Fin-3 system). AF/EG LIVENESS reading deferred to the justness gate (stutter self-loop ⇒ needs just-paths; see INTENT-REFS-fairness.md)
import Dregg2.Proof.MuCalculus           -- Track B: shallow modal-μ embedding over System; μ/ν as lfp/gfp of the interpretation; the CTL-encoding theorems (encode_EX/EF/AG/… each CTL op = its named μ-formula, proved) + a fixpoint-alternation example
import Dregg2.Proof.Fairness             -- Track D: van Glabbeek JUSTNESS over the reactive turn-executor — Just (Def-6 port) + EnabledAt (isSome-commit) + npcA/afcA concurrency relation + just_progress (the genuine ◇, justness ∧ progress-enabled ⟹ Eventually); commits_stable_off_npc stutter-half PROVED + universal half carried as JustLTSC.commits_stable field, de-vacuified by commits_stable_concrete; teeth badSched_not_just (unjust schedule starves an enabled cell) + refundDemo. cellNextA stutter self-loop is why fairness, not progress, is needed
import Dregg2.Proof.CTLLiveness          -- Track B liveness (the justness gate, now answered): AF_just/EG_just restrict CTL's deferred AF/EG path-quantifiers to van Glabbeek-just schedules; livingAF_just_progress derives the genuine ∀◇ from Fairness.just_progress, bridged back into CTL.EF via EF_iff_reachable; teeth af_just_separates_plain (a just schedule reaches Pgoal but NOT every schedule does — the stutter branch is excluded only by justness)
import Dregg2.Proof.Noninterference      -- Track C: confidentiality / seL4-Volpano-Smith — balance = HIGH (ember-locked, sealed-bid secrecy); lowEq over RecChainedState (HIGH = balance/amount/secret, LOW = metadata); unwinding K1 (LOW-write of LOW-derived preserves lowEq) + K2 (HIGH-write confined); teeth makeSovereign_leaks (writes LOW commitment = hash of whole HIGH pre-state ⇒ provable LEAK). Scope: transfers are declassifiers under HIGH, only the genuine slice proved
import Dregg2.InfoFlow.Confinement        -- Track C, CAPABILITY axis (the *-property pillar): attacker resource-view over Exec.Kernel; view_frame (off-view turn invisible) + cap tooth confined_cannot_debit_attacker (a cell with no node/endpoint-write cap reaching A cannot DRAIN A — rides exec_authorized) — but full noninterference PROVED FALSE (confined_can_credit_attacker: crediting dst needs no cap = *-property write-up leak) + refusal_leaks (commit-vs-refuse branches on the secret balance = termination covert channel). Honest verdict: confines authority+draining, NOT information
import Dregg2.Authority.CaveatChain      -- real macaroon HMAC append-only chain: verify_iff_wellTagged + append_narrows + integrity_tail_binds + forgery_requires_mac_query (forge ⇒ break-HMAC) + removal_breaks_tail; HMAC = §8 MacKernel portal
import Dregg2.Authority.MacaroonDischarge -- macaroon THIRD-PARTY DISCHARGE sub-tree + replay-binding tooth (faithful to macaroon/src/macaroon.rs bind_discharge/verify_discharge; the piece CaveatChain's abstract Discharges oracle leaves UNCOVERED). A discharge is its OWN HMAC chain seeded by an ephemeral discharge_key (foldBytes, NOT root key); bindTo appends bindingHash(parent_tail); verifyDischarge replays + requires the binding to NAME the expected root. PROVES: bound_discharge_verifies + unbound_discharge_rejected (fail-closed DischargeUnbound, even perfect-chain/zero-caveats) + binding_not_replayable_to_other_root (discharge bound to p REJECTED vs p'≠p = no cross-root replay onto a less-attenuated root) + binding_body_distinguishes_roots/rebinding_changes_replay (RELATIVE to named BindingHashCR = SHA-256 CR carrier + local hmacInj). REUSES CaveatChain.MacKernel HMAC portal. NON-VACUITY: BindingHashCR discharged on injective idBinding + REFUTED on collapsing constant. Rust differential macaroon/src/discharge_diff.rs (5 tests, real bind_discharge/Macaroon::verify flow); #assert_axioms-clean
import Dregg2.Authority.CaveatCapBridge   -- macaroon↔kernel-cap CONVERGENCE ARROW: caveatChainAuthority (the shared narrowing on ExecAuth) + caveatChainAuthority_le_held (REFINEMENT ⊆, the macaroon append_narrows rendered on the rights lattice) + delegationVerb_authority_eq (EQUALITY on the delegation verb when keep≤held) + chainGateG_implies_capAuthorityG (on a coherent node the macaroon chain gate ⇒ the kernel cap gate — the && of FullForestAuth's two legs is a PROVEN arrow on the overlap). REUSES CaveatChain + FullForestAuth + AuthModes + Caps; non-vacuity = equality-vs-strict-refine teeth (keepR EQUALITY, keepOver strict clip); #assert_axioms-clean
import Dregg2.Authority.TokenCapBridge    -- the AUTHORIZATION-MODEL CROWN: the agent-carried token (macaroon/biscuit caveat) ≡ the cell-program the circuit witnesses ≡ ONE granted⊆held predicate, proven from BOTH ends. tokenCaveat_authorizes_iff_cellProgram_admits (the offline biscuit-verify ↔ the in-circuit cell-program admit) + caveatSet_refines_capCrown (the carried caveat set REFINES the openable capability_root #103) + biscuitVerify_implies_capCrown_admits (a verifying biscuit ⇒ the cap-crown admits) + forged_token_cannot_forge_capCrown (non-forgeability rides the CaveatChain HMAC portal — a forged token cannot conjure cap-crown authority). The "two informal stories welded by &&" made one proven arrow; 13 theorems / 13 #assert_clean / 6 #guard
import Dregg2.Authority.ThirdPartyDischarge -- real third-party discharge protocol: accepts_iff (ticket/VID key-recovery ∧ bind-to-parent ∧ freshness ∧ predicate) + honest_discharge_accepted + rejection teeth; AEAD = §8 portal
import Dregg2.Authority.SelectiveDisclosure -- hidden-attribute predicate proofs (Gte/Lte/InRange) + selective reveal + anonymous unlinkable multi-show wired to credentials; ZK soundness = §8 portal
import Dregg2.Authority.DesignatedVerifier -- verifier-indexed DischargedFor + transferability dial (public = ∀V ⇒ non-repudiable; designated = V₀-only ⇒ deniable via the simulator property); DV-ZK = §8 portal
-- ── RELATIVISTIC TIME-TYPING (INTENT-AS-CO-RECEIPT.md §4): causal (lightcone, on the lace) vs frame (attested, with explicit δ) — the constructive/classical split applied to time. ──
import Dregg2.Time.Causal       -- causal_after(E) = E ≺ now over the lace partial order (Blocklace.precedes): MONOTONE (causalAfter_mono) + frame-INVARIANT (causalAfter_no_authority, no oracle) + NON-TOTAL (a Byzantine fork is concurrent, neither causally before — real teeth); the lightcone-fact deadline carrying NO §8 carrier
import Dregg2.Time.Frame        -- frame_within(F,T,δ) = a TimeAuthority's attested wall-clock frame, a Temporal WitnessedPredicate: frameWithin_sound (accepted attestation discharges via registry_sound) + frameWithin_false_without_authority (NOT true-for-free) + discharge_skew_is_explicit (the retyped MAX_DISCHARGE_AGE carries δ=300 first-class); FrameHonesty = the explicit §8 skew carrier (proved non-trivial, never True)
import Dregg2.Time.Deadline     -- the Deadline sum type FORCING the distinction (causalAfter_no_frame_dependency = Iff.rfl vs frameWithin_has_frame_dependency = flips across registries) + commit_wait_bridge (THE keystone: frame ∧ waited(2δ) ∧ honesty ⟹ causal — Spanner external consistency; "wait out the skew, frontrunning is causally excluded") + commit_wait_needs_full_2delta (the 2δ is load-bearing, a concrete counter-world)
-- Track A Phase 2 — intent-as-co-receipt (over the Phase-1 time foundation above):
import Dregg2.Intent.Resource    -- resource theory (Coecke–Fritz SMC) + convertibility preorder ⪰ = the match relation (Nonempty (a⟶c)); refl/trans/⊗-monotone; demo asset-bundle Discrete SMC with teeth (res_no_convert)
import Dregg2.Intent.Core        -- the four-faced Intent (offered/wanted boundary · predicate demand · escrow lockbox · Deadline validity); fulfill = the receipt⊣intent counit; fulfill_discharges keystone (outcome=wanted + predicate-satisfied + escrow consumed) + fulfill_conserves + no_double_fulfill; teeth on all four faces; causal-validity met for free
import Dregg2.Intent.Match       -- the solver as mathlib's coend ∫^B (A⟶B)×(B⟶C) (Types.coend over the offer profunctor): oneHop/twoHop non-vacuity + multi-hop typechecks + match_of_converts; the bilateral LensFill (get/put optic, the auction path) embeds as a one-hop route; co-Yoneda collapse PROVED — collapseIso : Match A C ≅ (A⟶C) (density via Quot.sound on the coend), so match_iff_converts
import Dregg2.Intent.Kernel      -- concrete KernelIntent over asset-bundle DreggResources: settle_discharges/settle_conserves/settle_no_double = the abstract keystones TRANSFERRED by instantiation; crossBid_needs_market (cross-asset bid unfillable w/o Phase-4 market offers); causal reveal-ordering = anti-frontrunning
import Dregg2.Intent.Centers     -- Phase-3 (escrow KEPT SEPARATE from one-shot Intent escrow): CommCentralMonoid := CommMon_(Center R); escrowMonad (–⊗M) + escrowMonad_isMonoidal (Thm 5.6, lax-monoidal for ANY braided monoid — NO mul_comm needed); escrowMonadHom = FLP Cor 3.11 monad morphism (–⊗𝟙_)⟹id (component=Prod.fst, not 𝟙) + escrowProjectionEquiv = Cor 3.20 rigidity equivalence; teeth = noncommuting_admits_no_halfBraiding (§4 centrality) + escrow_no_iso_without_rigidity (§7.3); mathlib Center/CommMon_/Rigid reuse
import Dregg2.Metatheory.Lawvere -- Track K: the Lawvere hyperdoctrine triple ∃_f⊣f*⊣∀_f — Part A posetal Set special case (Set.image_preimage/preimage_kernImage + Frobenius via image_inter_preimage + Beck-Chevalley; teeth: BC fails off-pullback, non-monotone reindex breaks Galois) + Part B the faithful relation-form ∀_R=Knows (□/◇ adjunction for ANY relation, contains Part A via the graph; teeth: S4 □□≠□ + ◇ fails binary meets on a Byzantine reflexive-non-transitive Fin 3 relation). K8 dual-H¹ PARKED
-- ── Metatheory/* tree (epistemic/categorical core): pulled into the default `lake build` root so CI covers it (previously only Categorical/Open.* were reachable via separate `lake env lean`). ──
import Metatheory.ConstructiveKnowledge   -- constructive (witness-carrying) knowledge modality
import Metatheory.EpistemicDial            -- the Disclosure×Transferability×Agreement epistemic dial
import Dregg2.AttestCube                    -- dregg4 ATTESTATION CUBE: assembles the 3 proved dials (EpistemicDial.Dial × DV.TransferDial × Finality.Tier) into ONE per-turn Turn.Attest point + the 2 coherence 2-cells (disclosure⟂transferability orthogonality · directed Agreement via Finality.no_downgrade) + the LOAD-BEARING impossibility corner deniable_bft_quorum_empty (deniable transferability ∧ BFT-quorum agreement is EMPTY under the quorum-verifiable bridge, via designated_excludes_public; INHABITED as a bare cube point ⇒ a semantic obstruction, not a type collapse). cubeIsThreeDimensional #guards non-vacuity (2 points differing on every axis); 10 keystones #assert_all_clean
import Metatheory.EpistemicConsensus       -- epistemic consensus / common-knowledge front
import Metatheory.Disputation              -- disputation / adjudication game
import Metatheory.OptimisticAdjudication   -- "fightable disproof" vs "checkable proof": the Refutable dual, the carried Actuated (liveness) hypothesis PROVED non-removable, and the DistKnows-vs-Knows challenger gap
import Metatheory.ResearchRegime         -- the RESEARCH regime: two auditors agreeing is the ballot regime, and the theorem already existed
import Metatheory.Categorical              -- categorical core (final-coalgebra base)
import Metatheory.Open.FinalCoalgebra      -- OPEN: final-coalgebra boundary
import Metatheory.Open.AuthorityClosure    -- OPEN: authority closure
import Metatheory.Open.ConservationMultiEdge -- OPEN: multi-edge conservation
import Metatheory.Open.CrossCellBisim      -- OPEN: cross-cell bisimulation
import Metatheory.Open.CurvatureScreen     -- OPEN: curvature screen
import Metatheory.Open.PerfectUC           -- OPEN: perfect UC-security front
import Metatheory.Open.PerfectZK           -- OPEN: perfect ZK front
import Dregg2.Metatheory.IndexedMonoidal -- the WELD (linear/dependent doctrine FLOOR): IndexedMonoidalCategory (base ⥤ Cat, monoidal fibres, lax reindexing) + Total = Grothendieck; concrete dreggIM (Ctx poset base + non-injective dropArt reindexing + Discrete Bundle ⊗-fibres) carries BOTH the ∃_f⊣f*⊣∀_f reindexing adjoints AND the fibrewise resource ⊗ on ONE total category; projection/Frobenius interaction (equality for the Set fibre, honest lax ⊆ for general relations + frobenius_eq_fails teeth); weldMorphism = one ∫F arrow touching both the predicate base and the resource fibre
import Dregg2.Intent.KernelBridge -- TOY-REMEDIATION: the DemoRes Intent settle REFINES the real RecordKernelState per-asset conservation — toBal (bundle→bal delta) + settleIntent_refines + bridge_accepts_iff_conserves; teeth crossBid_rejected_by_bridge / bridge_rejects_nonconserving (a non-conserving settle the thin shadow would pass is caught by the real ledger)
import Dregg2.Intent.Ring -- THE REAL RING TRADE (models intent/src/solver.rs + trustless.rs, not the toy coend): RingLeg = solver::Settlement, settleRing = ATOMIC fold of the legs through the VERIFIED recKExecAsset; settleRing_conserves (a settled ring conserves value PER ASSET — Σ legs = 0, proved by folding the kernel keystone) + settleRing_atomic/atomic_general (all-or-nothing rollback); RingBalanced models check_settlement_conservation (no-phantom/per-asset-balance/cycle-closure) w/ teeth freeMintRing_rejected + zeroLegRing_rejected + underfunded_leg_aborts
import Dregg2.Intent.RingFFI -- the FFI-EXPORT bridge: ONE settleRing leg IS the verified scalar export recKExec on the moved asset column (projAsset projection). recKExec_projAsset_commits_iff (export commits a leg iff recKExecAsset does) + recKExec_projAsset_column_agrees (export post-balance = verified a-column) ⇒ ffi_export_realises_settleRing_leg: the LIVE Rust path (intent/src/verified_settle.rs, routing each lowered leg through @[export] dregg_record_kernel_step) is a FAITHFUL per-leg settleRing executor, NOT a mirror
import Dregg2.Intent.Lifecycle -- the AI-to-AI CONTRACT lifecycle over the REAL bal-ledger: Contract (publisher/filler/escrow/asset/amount) with publish/fulfill/refund each a VERIFIED recKExecAsset turn. fulfill_is_settleRing_leg (fulfillment IS a one-leg settleRing, not a parallel path) + publish_locks_exactly (escrow holds exactly the funding) + fulfill_authorized/conserves/delivers + refund_restores + publish_then_refund_identity (unfulfilled intent refunds in full) + the ONE-SHOT teeth fulfilled_then_no_refund / refunded_then_no_fulfill (escrow funds EXACTLY ONE side — fulfilled XOR refunded, proved on the real ledger); demo lifecycle runs end-to-end w/ teeth
import Dregg2.Intent.SealedAuction -- USECASE APP #2 (sealed-intent multi-agent coordination): N agents COMMIT sealed bids (Blake3 hash binding bidder/value/nonce — the real CR carrier from Crypto/PortalFloor, NOT True), REVEAL after the commit phase seals, then the top bid SETTLES atomically as a balanced two-leg awardRing through the verified recKExecAsset. Keystones: seal_injective/reveal_binds_committed (NO late-switching — a commitment opens to EXACTLY its sealed bid, under CR) + reveal_requires_reveal_phase (no reveal before commit closes) + uncommitted_cannot_open/uncommitted_cannot_win (a non-committed party can't settle) + settle_atomic/settle_conserves (all-or-nothing, value-neutral; reuses the Ring keystones) + winner_was_committed (the award binds back to a real commitment). Demo: 3 agents over a real ledger w/ anti-ghost teeth (impostor + outsider + unfunded-winner rejected), all #guard + #assert_axioms clean
import Dregg2.Consistency               -- global soundness witness: dregg_consistent_nonempty = a single axiom-clean SystemModel instantiating all 11 Prop-carriers jointly; no carrier-pair derives False; not vacuous, not contradictory at the system level
import Dregg2.HandlerTransformer        -- higher-order handler-transformer: SafeStep preorder + instSafeStepFpu (camera Fpu = safe-composition instance) + safe_transformer_composes + conservation_is_safe_transformer + overshare_rejected (unsafe transformer is refused); unifies 'safe handler-transformer' with 'frame-preserving update' (Iris handler frame-rule); Fpu=gluing weld + comodel-morphism/sheaf-of-handlers tier are OPENs
import Dregg2.Confluence.DriftStable     -- drift-stable bridge: IConfluentUnder (conditional drift-stability = confluence in the sublattice E cuts out) + driftStable_composes (an I-confluent caveat survives forward drift, no re-check) + locked_driftStable (single-writer collapses merge ⇒ any φ drift-stable) + teeth (monotone composes free; bounded_caveat_needs_coordination) + TieredCaveat (carry the tier-proof, dispatch on DriftTier); #assert_axioms-clean, #eval non-vacuity
import Dregg2.Confluence.SemanticConvergence -- rhizomatic SPEC-9 metabolized: accountable, deterministic, negatable semantic convergence. grow-only mapping(name,slot,frag,by) + a one-hop `aliased` closure (slots=hubs, no transitivity). Monotone-mod-negation: aliased_mono + the no-negation fragment is Tier1Eligible (aliasedRaw_tier1 — the SAME gate as a G-Set) + the CALM classifier reused verbatim (classifyAliased/classify_sound, negation = the one non-monotone reason, both-polarity witnesses). Content-derived identity DISCHARGES the same-entity case (sameEntity_dedup_by_content — a G-Set union already converges same ids; aliasing does only the same-CONCEPT half) + convergence DECIDED never fuzzy. The one-better: a `trust`-gated mapping demands witnessed(vk) — a proof-carrying judge decidable over Pred (trustedReach_subset = accountable, never amplifies; still Tier1Eligible; forged EXCLUDED/witnessed INCLUDED). #assert_axioms-clean, #eval non-vacuity
import Dregg2.Exec.CoordinatedCaveat     -- Track F (completes it): promotes DriftStable's fail-closed DriftTier.coordinated (DriftWitness=PUnit) onto the proved CrossCaveat equalizer — coordinated_discharge_sound (CG-5 ∧ CG-2 ∧ φ-on-snapshot) + coordinated_no_toctou(_atomic) + coordinated_refines_failclosed + the bridge coordinated_tier_discharges_via_equalizer; teeth = overbroad_discharge_rejected. Single-machine equalizer (distributed variant OPEN)
import Dregg2.Exec.CoordinatedForestGate -- joint-layer bilateral equalizer: execBilateralCoordinated + execBilateralCredGated (coordinated covenant + per-leg credentials); intra-cell `.coordinated` still fail-closed on single-cell nodes
import Dregg2.Exec.CrossVatCharter       -- cross-vat charters as the common bilateral+credential pattern: charterDischarge + charterBilateral + per-leg biscuits; production cross-cell maneuver surface
import Dregg2.Exec.JointCharterBridge    -- charter↔bilateral bridge: charterDischarge refines execBilateralCoordinated; the common cross-vat wiring spine for Apps + regression
import Dregg2.Exec.CoordinatedForestGLift -- RecChainedState lift: execCoordinatedForestG / execCoordinatedForestCredG over BilateralForestPairG; charter_refines_coordinated_forest; honest positive path beside intra-cell fail-closed
-- ── THE HATCHERY (HATCHERY.md H1–H4): the verification toolkit — one-step proof ⇒ "forever, every adversary" free; the userspace smart-contract verification surface. ──
import Dregg2.Verify.Frames     -- Tier 2: the [Dregg2] aesop rule-set's forest-monotone frame family + cellNextA_carries_rel (the reusable one-step body subsuming every grow-only crown — log.length ≤ and registry ⊆ alike, via Trans)
import Dregg2.Verify.Tactics    -- Tier 1: carry_forever (livingCellA_carries plumbing) + exec_frame (executor split; UNIVERSAL stay-put arm; frame/grow commit arm; HONEST hand-back — never fakes a close, proven by logMono_handback_demo); gate reproduces the logMono + revoked-forever crowns via the tactics
import Dregg2.Verify.Contract   -- Tier 3: first-class CellContract (Inv + step_ob) → forever (livingCellA_carries) + always (the REAL LTL □ via Proof.Temporal.always_of_step_invariant); three concrete contracts discharged through the engine
import Dregg2.Verify.Catalog    -- Tier 4: declarative shape macros monotone_registry% / conservation% / confinement% / automaton_inv% → real CellContracts (confinement surfaces its control∈U hypothesis; automaton_inv is relational, obs a + obs b)
import Dregg2.Verify.StripeAttest      -- DECO/Stripe money-in Part A: StripeAttest attestation over DecoRelation (deco-authenticated payment surface)
import Dregg2.Verify.StripeReserve      -- DECO/Stripe money-in Part A: StripeReserve over Apps.Trustline (reserve-side accounting)
import Dregg2.Verify.StripeBridge       -- DECO/Stripe money-in Part A: StripeBridge (attest → escrow-factory bridge)
import Dregg2.Verify.StripeBridgeV2      -- DECO/Stripe money-in Part A: StripeBridgeV2 (attest → intent lifecycle bridge)
import Dregg2.Verify.StripeMoneyIn       -- DECO/Stripe money-in Part A: StripeMoneyIn (reserve + attest → money-in apex)
import Dregg2.Verify.StripeKernelRefine  -- DECO/Stripe money-in Part A: StripeKernelRefine (money-in → intent-lifecycle kernel refinement, the 6th Stripe apex)
import Dregg2.Verify.Regression -- H4 gate: six shipped crowns (identity_revoked_forever / no_double_spend / commitments_persist / …) reproduced via the catalog with both-directions defeq witnesses vs the hand proofs
import Dregg2.Verify.LivenessBridge   -- production liveness lift: `EventuallyG` + erasure bridge + `transferSchedG`
import Dregg2.Verify.LivenessContract  -- Hatchery liveness tier: `eventually%` + justness bridge
import Dregg2.Verify.ChoreographyBridge -- blue ↦ `CellContract.forever`, red ↦ `HyperAdmissible`
import Dregg2.Verify.JointRegression    -- cross-vat charter regression: joint_charter_commits + joint_forged_cred_rejected + joint_covenant_teeth + joint_cg5_conserved
import Dregg2.Verify.AppComposition     -- cross-app composed crown: agent_mandate_safety_forever (CWM ∩ SGM ∩ Identity on trajG)
import Dregg2.Verify.DemoRegressGated   -- H4 gate on production `trajG` (six crowns, catalog + tactics)
-- ── THE WEB-CITIZEN SURFACE: ProofWidgets driven by REAL Lean terms (no placeholder data — the prime directive). ──
import Dregg2.Widget.Basic             -- shared Html helpers + the trust-badge classifier: tier read off Lean.collectAxioms (KernelChecked / CarrierBounded / ExtraAxioms) — cannot be hand-faked; 2 by-design demo axioms exhibit the amber carrier-bounded tier
import Dregg2.Widget.ProofBadgeGallery -- tabulates the collectAxioms trust verdict over real theorems (green) vs a synthetic carrier-bounded demonstrator (amber)
import Dregg2.Widget.ConservationLedger -- per-asset bal deltas (Recharts) of the REAL committed transferCF-over-fma0 turn; conservation badged via recKExecAsset_conserves_per_asset
import Dregg2.Widget.CapabilityGraph   -- the Granovetter capability graph (InteractiveSvg) over a real Caps table; granted≤held non-amplification badged
import Dregg2.Widget.DreggForest       -- the call-forest (GraphDisplay) off a real FullForestA value; nodes = effects, edges = delegation
import Dregg2.Widget.ContractView      -- a verified guarantee rendered as a card: the invariant + the forever theorem's collectAxioms trust tier
import Dregg2.Widget.VerifiedTurn       -- the execute→prove→verify→anti-ghost pipeline off the REAL TransferWitness: 20-wire executor-derived assignment, stateCircuit verdict, forged-bystander rejection at the frame-reuse gate
import Dregg2.Spec.FunctionalRefinement -- corner (a): intent functional specs (mintSpec/escrowCreateSpec/...) + executor⟺spec triangles + anti-ghost
import Dregg2.Spec.CircuitSpecTriangle  -- CROWN JEWEL corner (b): circuit⟺intent-spec — a verifying ZK witness pins the EXACT intent ledger move (mint/transfer/escrow-create); anti-ghost = wrong-output UNSAT; amplification template pin_intent_of_bridge
import Dregg2.Circuit.RecursiveAggregation -- RECURSIVE-AGGREGATION SOUNDNESS (THE magnesium→gold bridge over circuit/src/ivc_turn_chain.rs prove_turn_chain_recursive→WholeChainProof): the SNARK recursion layer over HistoryAggregation. Opaque (Proof,verify) = plonky3 RecursionCompatibleProof/verify_recursive_batch_proof; EngineSound bundles the 3 NAMED, REALIZABLE soundness hypotheses you cannot prove in Lean — recursive_sound (root verifies ⇒ all child leaves verify; the ONE FRI obligation §H1), leaf_sound (POSITIONAL Forall₂ pairing leaf↔step ⇒ verifying leaf attests its recCexec step; the EffectVm circuit⟺executor soundness), binding_sound (TurnChainBindingAir leaf ⇒ ChainBound + genesis/final root pins). HEADLINE light_client_verifies_whole_history: checking ONLY verify agg.root (re-witnessing NOTHING) ⇒ AggregateAttests (every turn executed correctly + correctly ordered + final root = genuine fold) = proofs-as-additive-attestation, the verification IS the trust + attested_history_conserves inherited. NON-VACUITY both ways: real_engine_sound/light_client_fires_on_real_chain/real_chain_first_turn_executed (witnessed over the honest teethGenesis step) + ANTI-GHOST tampered_aggregate_cannot_bind (reordered chain ⇒ binding_sound forces ChainBound = FALSE ⇒ no verifying aggregate) + leaf_pairing_defeats_swap (positional pairing ⇒ leaf not re-pointable). #assert_axioms-clean; the named hyps are structure FIELDS not axioms
import Dregg2.Circuit.GeneralizedBilinearAccumulation -- DREGG-OWNED ALGEBRAIC ACCUMULATION FLOOR (inspired by ePrint 2026/538, not a protocol formalization): mixed-module generalized bilinear forms + finite weighted public checks; accumulation_preserves_check proves only component-valid ⇒ accumulated-valid. Explicit non-converse: two invalid integer checks cancel under nonzero coefficients. No transcript/commitment/random-challenge/PCD soundness claim; #assert_axioms-clean.
import Dregg2.Distributed.FeeHistory       -- the Argus FEE-WRAPPED history conserves MODULO THE BURN (the apex inventory's statement): FeeChainStep = one ACCEPTED runTurn (commits : runTurn ctx hdr (transferStmt turn) pre = .bodyCommitted post; fee cells wired/live/distinct as load-bearing fields) + runTurn_bodyCommitted_inv (the ACCEPTED-outcome inversion); per-step keystone feeStep_conserves_modulo_burn (recTotal moves by EXACTLY −feeBurned: prologue −fee ∘ exact transfer body ∘ epilogue +fee−burn, via the owned recTotal accounting recTotal_commitPrologue/recTotal_distributeFee over sum_indicator) → HEADLINE wellformed_history_conserves_modulo_burn (recTotal endpoint + totalBurn = recTotal genesis over ANY state-chained fee history: arbitrary length leaks exactly the named sink, additively; the bare strand's exact law = the totalBurn=0 face); feeStep_exposes_body_strand_step CONSUMES Aggregate §6 argus_full_turn_body_links (the fee chain sits ON the light-client body strand — not re-proved); W1 runTurnV note: at the VK-rotation swap this collapses to totalBurn=0 paid to the burn-pot; non-vacuity both polarities #guard-EXECUTED (real accepted transfer turn: 100→98; the =100 and =90 claims both FALSE); #assert_axioms-clean
import Dregg2.Distributed.FinalizedLightClient -- THE THREE-LEG FINALIZED light client (the (aggregate, finalizedRoot, finalityCert) surface the lightclient/ Rust crate realizes): adds the FINALITY-CERT (quorum/tau) leg RecursiveAggregation lacks — a bare valid aggregate proves a chain CORRECT but not FINALIZED (a fork attack). FinalityCert carries the certified lace + anchor + finalizedRoot; CertValid = finalLeaderAt = some anchor (the node's REAL BlocklaceFinality super-ratification leader-slot rule, NOT bare isSuperRatified — certValid_has_quorum proves valid ⇒ CertQuorum). HEADLINE light_client_accepts_finalized_history: verify agg.root (leg1+2 via RecursiveAggregation) ∧ Bound (root seam agg.finalRoot = cert.finalizedRoot = shown) ∧ CertValid (leg3 quorum) ⇒ FinalizedHistoryAttested (whole history correct+ordered+conserving AND endpoint root is QUORUM-finalized) — gap-free, re-witnessing nothing. NON-VACUITY: finalized_light_client_fires_for (Prop headline fires given a realized cert) + §5b #guard executable witnesses (finalLeaderAt trace3 = some creator-1 genesis, CertQuorum true). ANTI-GHOST: not_final_leader_invalidates (forged creator-2 anchor ⇒ ¬CertValid; #guard witnessed on trace3 — HONEST: super-ratification alone insufficient, leader-slot is the discriminator) + root_mismatch_unbinds/agg_root_mismatch_unbinds (seam tooth: cannot pair proof-of-A with cert-for-B). #assert_axioms-clean
import Dregg2.Distributed.FinalityCertWidth -- WOUND #1 (BFT finality cert felt-width): the felt-resolution binding of finality_signing_message the abstract-ℤ FinalizedLightClient model LAUNDERS. Models the wide finalized root (Fin 8 → Felt, = Rust [BabyBear; SEG_ANCHOR_WIDTH]) + the v1 (lane-0-only, HISTORICAL HOLE) vs v2 (all-8-lanes, HEAD) domain-separated signing message. FALSIFIER v1_forges/sigMsgV1_forkRoot: the v1 message is NOT injective in the wide root — forkRoot r (lane 0 fixed, lane 1 bumped, = the Rust forge tooth's w_fork) has a byte-identical v1 message, so a genuine committee signature over r authorizes a DISTINCT root (the ~2^31 finality-substitution forgery, PROVED over ALL inputs, not the #[ignore]-gated 2-witness Rust case-test). LAUNDER exposed: narrow_seam_underdetermines (lane-0 pin = what a v1 sig / the abstract Bound.cert_binds ℤ-seam constrains admits two distinct wide roots) vs wide_seam_determines (all-8 pins uniquely). FIX SOUND sigMsgV2_inj/v2_binds_unique: v2 message injective in (root, count) ⇒ no replay; v1_ne_v2 domain separation. Rust v2 widening ALREADY LANDED (beb2661fc1); this is the machine-checked proof the Kind-E fix is correct + the narrow binding was genuinely broken. #assert_axioms-clean
import Dregg2.Cell.InterfaceIdWidth        -- WOUND #12 (cell interface_id felt-width): the felt-resolution binding of compute_interface_id, which the abstract-model tree had NO Lean model for. Models the sorted per-method 8-felt leaf list (Digest8 := Fin 8 → Felt, = Rust MethodSig::leaf_digest8) + the deployed hash_many_8 fold (seed hash8[0x1FACE,len], step hash8(acc8‖leaf8), digest8_to_bytes32 tail). FALSIFIER narrow_conflates/narrow_colliding_interfaces_share_vk: the pre-widening ONE-felt (lane-0) leaf squeeze conflates two DISTINCT method sets, and since a factory VK is derive_service_factory_vk(interface_id,mode) (a function of the id), the two share a service-factory VK (the wound's harm), PROVED over all inputs. FIX SOUND wideId_injective/wide_separates: under the wide-hash CR floor (Function.Injective hash8, a NAMED hypothesis) the deployed 8-felt fold is injective in the leaf list — the lane-0 collision closes; via the generic foldl_step_inj engine (joint step-injectivity + seed∉range(step), from hash8 inj). ANTI-LAUNDER finalSqueezeOnly_still_conflates: widening ONLY the final squeeze over narrow leaves STILL conflates — the fold must be 8-felt END TO END. ARITY seed_arity_injective: the len-seed binds the leaf count. Rust wide fold ALREADY LANDED; wound-doc "NO wide twin" now STALE. #assert_axioms-clean
import Dregg2.Cell.CapUniquenessWidth      -- WOUND #3 (executor cap-uniqueness root gate felt-width): validate_capability_uniqueness leg (1) still compares the NARROW felt_to_bytes32(compute_canonical_capability_root_felt) = LANE-0 (~31-bit) projection, while the off-chain state commitment absorbs the WIDE digest8_to_bytes32(compute_canonical_capability_root_8). Models both at lane granularity (Lanes := Fin 8 → Felt). FALSIFIER narrow_capRoot_underdetermines: two DISTINCT 8-felt cap roots agreeing on lane 0 give the SAME narrow declared value — leg (1) cannot distinguish them (~2^15.5). FIX SOUND wide_capRoot_determines/wide_separates: wideEncode is injective, so the state-commitment's absorbed value binds the cap root uniquely at ~124 bits. REDUNDANT PROJECTION narrow_root_is_projection_of_wide/narrow_gate_redundant: the narrow declared value is a FUNCTION (lane 0) of the wide root the state commitment already binds — leg (1) is defense-in-depth (the wound's 07-19 downgrade, PROVED). COMPENSATING TOOTH accept_implies_unique: the security-relevant uniqueness is leg (2) (the dup-scan on the ACTUAL caps), independent of ANY root encoding. Widening leg (1)'s in-circuit gate itself = committed-state binding change (ember-gated deploy), out of scope. #assert_axioms-clean
import Dregg2.Grain.R3Verify            -- GRAIN R3 — the whole execution history is UNFOOLABLE, from a constant-size aggregate, NO host trust (closes grain-verify's WHOLE_HISTORY_GAP). Lean-first (the Fips204Verify pattern): r3VerifyCore is a plain executable def, @[export dregg_grain_r3_verify] r3VerifyFFI for the Rust grain-verify to call. **WOUND #22 CLOSED (felt-width + self-anchoring).** The PRE-FIX core was `aggregateVerified && aggregateHead == anchoredHead` over ONE felt, fed `proof.final_root[0].as_u32()` (~31 bits) while the Rust seam light-verified against `proof.root_vk_fingerprint()` — the fingerprint the ARTIFACT ITSELF supplied. BOTH conjuncts were empty and both are repaired: r3VerifyCore now decides over the FULL Digest8 (Fin 8 -> Felt = Rust [BabyBear; SEG_ANCHOR_WIDTH], ~124-bit) AND over presentedVk vs a CALLER-HELD expectedVk (Vk8 = RecursionVk's 32 bytes as 8 u32 lanes). FALSIFIERS narrowHead_conflates / narrow_seam_underdetermines: the lane-0 form ACCEPTS a genuinely-distinct wide anchor agreeing only on lane 0 (the ~2^31 offline grind), and a lane-0 pin does not determine which history was folded. FALSIFIER selfAnchoredVk_vacuous / selfAnchoredVk_accepts_foreign_root: the self-anchored shape is LITERALLY INDEPENDENT of which circuit the presented root belongs to, so it admits a foreign circuit's root — which is why the ~31-bit head equality was carrying the whole binding. ANTI-LAUNDER neither_half_alone_suffices: widening the head ALONE still admits a foreign circuit (headWidenedOnly_admits_foreign_circuit) and pinning the VK ALONE still admits the grind (vkPinnedOnly_still_conflates) — the InterfaceIdWidth.finalSqueezeOnly_still_conflates failure shape, proved for BOTH conjuncts. FIX SOUND r3_wide_head_mismatch_rejected / r3_vk_mismatch_rejected / wideHead_refuses_the_forgery / vkPin_refuses_the_foreign_root, plus r3_unverified_rejected (the fold-failure path Rust rests on). SOUNDNESS by COMPOSITION not re-derivation — r3_unfoolable reduces through r3_unfoolable_narrow (r3VerifyCore_implies_narrow: the widened accept set is a SUBSET) into RecursiveAggregation.light_client_verifies_whole_history; concludes R3Attested (integrity = every turn recCexec-correct + completeness = ChainBound no drop/insert/reorder + anchoring = the anchor is the genuine fold of THIS history). Boundary INHERITED not introduced: the verified-status Bool = verify agg.root (heavy Rust verify_whole_chain_proof_bytes, now run against the CALLER'S anchor per its own documented contract) whose soundness is the named realizable EngineSound hypothesis FIELD, carried through UNCHANGED — no new axiom, no def ...Hard, and the repair adds NO floor (exact 8-lane and 32-byte equality, no hash). RESIDUAL, named: Aggregate.finalRoot / foldedFinalRoot stay ℤ-valued, so r3_unfoolable takes an EXPLICIT denotation den : Digest8 -> Felt + its coherence hden — the modeling step the pre-fix seam performed silently and unfaithfully (as den := lane0) is now a visible hypothesis. NON-VACUITY both ways: r3_fires_on_real_chain / r3_real_chain_turn_executed (honest teethGenesis step) + r3_wrong_head_rejected (a LANE-1-ONLY wrong anchor rejects on the real instance) + r3_foreign_vk_rejected + wire #guards incl. the pre-repair 3-int wire failing CLOSED. #assert_axioms-clean
import Dregg2.Spec.WholeTurnTriangle    -- COMPOSITION: lifts per-EFFECT correctness to per-TURN. turnSpecG = gated FOLD of fullActionStep over the call-forest; execFullForestG_iff_turnSpecG (gated whole-turn FOREST triangle, full biconditional) + whole-turn anti-ghost; post-state IS the ungated intent fold (execFullForestG_post_is_intent_fold) ⇒ whole-turn conservation; whole_turn_circuit_pins_intent_fold (one authenticated §8 root over the composed post-state); cross-cell coordinated_turn_joint_triangle (joint conserves, no amplification) + N-ary Σ=0 NAMED, consensus-linearization carried

-- DREGG3 closeout wave (2026-06-09): the guard-algebra uplift + the W2 reductions + the polis/assurance surfaces.
import Dregg2.Exec.RelationalCaveat      -- §8 axis 1: the record-level relational guard (FieldLteOther) on the live surface — the queue/inbox/pubsub unlock
import Dregg2.Exec.RelationalCaveatLive  -- §8 axis 1 live-path: the Rust↔Lean correspondence (RelCaveat.eval ⟺ the executor's lhs ≤ rhs arm) + queue layout enforcement instantiation
import Dregg2.Exec.PredCaveatLive        -- D6 live-path: the reified CaveatPred AST PROMOTED onto the executor's field-write admission leg (predCaveatStateStepGuarded = stateStepGuarded ∘ caveatPredAdmit, a composed SUPERSET — nil_eq recovers the existing surface, violation_fails = no admission weakening), with the validAfter mutation-confirm rejects-before/admits-after through the live write. Expressiveness, NOT soundness (circuit binds the aggregate caveatBit). NON-VK
import Dregg2.Authority.RelationalClosure -- §8 axis 1 uplift: the RELATIONAL CLOSURE of the guard language — RelPred = the general affine half-space `Σ cᵢ·record[fᵢ] ≤ k` closed under .and/.or/.not (a genuine Boolean algebra on the evaluator), SUBSUMING FieldLteOther/FieldLteField/AffineLe/SumEquals as instances; the bounded-circuit argument (constraintBudget ≤ sizeBound) + the named escape-hatch boundary (unbounded ∃ / non-affine / causal → witnessed(vk)); non-vacuity discriminator
import Dregg2.Authority.CaveatConvergence  -- §3B CONVERGENCE: the parallel caveat surfaces are VIEWS of one core. coreOfCaveat : CaveatPred → Pred is a denotation-preserving embedding (caveatPred_validAfter_embeds = THE FIRST WELD: validAfter ≡ fieldGe on the shared (actor,old,new) transition; validUntil ≡ fieldLe; heightLt ≡ ¬fieldGe), proved structurally over the WHOLE connective layer (coreOfCaveat_view_embeds), so CaveatPred + PredCaveatLive + Pred collapse to ONE Pred core (caveatPredLive_is_pred_view: the two LIVE admission paths are one decision). The honest TOWER: floor_is_affine_halfspace lands the comparison inside RelPred's affine fragment, but affine_diagonal_not_caveat proves the diagonal Σcᵢ·rec[fᵢ]≤k reading TWO slots is STRICTLY more expressive — not all four are one algebra; they're one core with proven faithful views. Expressiveness/consolidation, NOT soundness. NON-VK
import Dregg2.Authority.PrivatePredicate  -- §8 privacy × programmability: PRIVATE PREDICATES — relations over COMMITTED (Pedersen) values proven WITHOUT revealing them. The keystone private_conservation_checks_homomorphically: conservation of hidden balances checks via commit_hom on the COMMITMENTS alone (zero disclosure, zero extra ZK); affine-≤/range routes through witnessed(vk) (§8 oracle interface, reveals only the truth bit); PrivPred = homomorphic-= ∧ witnessed-range ∧ cleartext RelPred, each carrying its Dial disclosure level (fully-committed PrivPred sits at the dial FLOOR — the proof reveals only that the relation holds). Non-vacuity: inflation caught privately + out-of-range rejected
import Dregg2.Authority.ArithmeticClosure -- §8 axis 1 DEEPER: the ARITHMETIC (polynomial) closure — ArithPred = a bounded-degree polynomial atom Σ coeffⱼ·∏ record[fᵢ] ≤ k (the circuit is natively QUADRATIC) closed under .and/.or/.not, SUBSUMING affine RelPred as its degree-1 slice; the non-affine gain = the x·y=k AMM invariant (mulInv_non_affine_witness); bounded-circuit (O(monomials×degree))
import Dregg2.Authority.QuantifiedPredicate -- §8 axis: the QUANTIFIED fragment — bounded ∃/∀ over a finite range (body : ι → RelPred) + the memberOf committed-set atom; forall_eq_andFold/exists_eq_orFold DE-QUANTIFY into the RelationalClosure (N-fold ∧/∨, O(N), no new primitive); membership routes to witnessed(vk) (senderAuthorized = merkleMembership)
import Dregg2.Authority.ConfluenceClassifier -- §8 dual: the I-confluence guard CLASSIFIER — guardKeepsConfluence (= IConfluent of the installed invariant) ↔ CoordinationFree; monotone-keeps / bounded-breaks / relational-decided-by-merge; every guard carries a proof of its coordination cost (runs free vs forces ordering)
import Dregg2.Authority.CausalGuard      -- §8 axis 2: installable causal guard atoms (causallyAfter / monotoneOverForks) over the lace happened-before order + the I-confluence boundary
import Dregg2.Authority.CausalGuardKinds  -- §8 axis 2 ergonomics: NAMED constructors for the two causal atoms (stable causalAfterVk/monotoneForkVk + install builders) at the custom-vk extension point WITHOUT editing the shared WitnessedKind enum; wrappers PROVED to denote the CausalGuard semantics
import Dregg2.Apps.CommitRevealApp        -- §8 axis 2 demo: a real front-running-resistant commit-reveal app whose reveal admits iff it causally-FOLLOWS its commit (causallyAfter(commit) installed by name); contract = honest-reveal-admitted + frontrun-rejected (∀ chain, unforgeable) + value-conserved; #guard non-vacuity on the demo lace
import Dregg2.Apps.QueueFactory          -- W2: bounded queue as a factory-born cell (FieldLteOther capacity/underflow + sender-auth + conservation) — the 6-verb QueueAllocate/Enqueue/Dequeue/Resize/AtomicTx/PipelineStep replacement
import Dregg2.Apps.QueueRoot             -- the queue message_root EVOLUTION modeled (closes QueueFactory's opaque-newRoot instance, the #173 dequeue-verifier scheme): pending-window root under named zero-free RootCR/LeafCR/LeafNonzero carriers (BLAKE3 floor); verify_dequeue_proof(_against) mirrored — dequeue_proof_pins (an admitted proof PINS entry/remaining/post-root or exhibits a collision) + honest completeness + stale_proof_refused (replay) + the zero-padding alias witness (full root injectivity is FALSE for the padded scheme; verifyDequeueStrict = the one-line hardening) + the WELD queueDequeueProven (tighten-only over queueDequeue; the admitted root pair IS the modeled root transition)
import Dregg2.Apps.CapSlotFactory        -- F3/W2: CAPS-IN-SLOTS + R7 retrieval-epoch freshness — the 7-verb CreateSealPair/Seal/Unseal/ExportSturdyRef/EnlivenRef/DropRef/ValidateHandoff replacement. storeCap (held-gated, epoch-stamped) + retrieveCap (refused iff stored_epoch < current_epoch(grantor), then the survivor grant). Keystones: stored_cap_only_fresh_if_epoch_unrevoked + no_forge_from_storage + revoke_stales_stored_cap (a stored/sealed cap can no longer outlive its grantor's revocation — the R7 security FIX, live in dregg1)
import Dregg2.Apps.InboxFactory          -- W2: value-less inbox = queue minus conservation (sequenced deliver/consume, owner-only consume) — strictly-easier sibling
import Dregg2.Apps.PubsubFactory         -- W2: pubsub = shared head_seq + per-subscriber cursor (reader_cursor ≤ head via per-reader FieldLteOther), publisher-authorized append, reader isolation
import Dregg2.Apps.ObligationFactory     -- W2: the obligation family as a factory-born cell-program (bond in own bal column, escrow-shaped, no new atom)
import Dregg2.Apps.BridgeCell            -- W2: the bridge family as a factory-born cell (lock / finalize-to-pot / cancel + safety keystones)
import Dregg2.Apps.Vault                 -- HOUSE WELD #1: the conditional-timelock VAULT as a factory-born cell — value LOCKED until a release rule (block-height TIMELOCK or hash-lock PREIMAGE proof), claimable EXACTLY ONCE by the beneficiary after the condition is met. A COMPOSITION (no new Effect): mint via createCellFromFactoryChainA installs the one-terminal claim machine + deal-term immutables; fundVault = ordinary move IN; claimTimelock/claimHashlock = the probe's gated claim on the factory-born cell. 4 keystones (conserve / one-shot no-double-claim / claim-only-on-condition [timelock no-early-release + hashlock no-forged-proof] / not-stranded) re-proved on the minted cell. Probe: Dregg2.Verify.VaultFactoryProbe (PASS). Supersedes the bespoke cell/src/vault.rs heap-cell library
import Dregg2.Apps.Allowance             -- HOUSE WELD #2: the rate-limited ALLOWANCE as a factory-born cell — a sub-capability that may spend up to a fixed per-epoch CEILING, the ceiling enforced so it can be neither EXCEEDED nor FORGED, refilling each epoch (pocket-money a sub-agent CANNOT overspend). A COMPOSITION (no new Effect): mint via createCellFromFactoryChainA installs the frozen ceiling/epoch immutables + monotone cursor + boundedBy [0,limit] per-epoch ceiling; fundAllowance = ordinary move IN; spend = the probe's allowanceSpend on the factory-born cell (the epoch is DERIVED from the block, so no early/stale refill). 4 keystones (conserve / ceiling-no-over-limit / no-forged-or-early-refill [stale+early] / within-budget-spendable) re-proved on the minted cell. Probe: Dregg2.Verify.AllowanceFactoryProbe (PASS). Supersedes the bespoke cell/src/allowance.rs heap-cell library
import Dregg2.Apps.Obligation            -- HOUSE WELD #3: the STANDING (recurring) OBLIGATION as a factory-born cell — a schedule-enforced recurring commitment: a cell OWES a FIXED amount to a beneficiary every PERIOD blocks (a subscription/salary/rent the protocol enforces on schedule). A COMPOSITION (no new Effect): mint via createCellFromFactoryChainA installs the frozen amount/period/start/beneficiary immutables + monotone nextDue cursor + monotone dischargedCount; fundObligation = ordinary move IN; discharge = the probe's obligationDischarge on the factory-born cell (the schedule is DERIVED from the block — atBlock ≥ nextDue — so no early discharge; the cursor is monotone, so no replay). 4 keystones (conserve / fixed-amount-no-over-or-under-pay / no-forged-or-early-discharge [early+replay] / due-period-dischargeable) re-proved on the minted cell. Probe: Dregg2.Verify.ObligationFactoryProbe (PASS). The recurring sibling of the one-shot bonded Apps.ObligationFactory; supersedes the bespoke cell/src/obligation_standing.rs heap-cell library
import Dregg2.Apps.BountyBoardForever    -- W2 residual: BountyBoard's lifetime conservation re-lifted onto the plain recTotalAsset measure
import Dregg2.Substrate.VerbRegistry      -- DREGG3 §2.3 made load-bearing: the reified kernel verb-set source of truth (8 survivor verbs + the 52-variant Effect cover + DOOMED→FACTORY map); total classification (exhaustive by compiler) + minimality of the 8 + non-vacuity. The anchor the executor dispatch / circuit descriptor table / deletion manifest reconcile against
import Dregg2.Verify.ReceiptContract     -- W4/assurance: the receipt (Q) contract face — app properties provable from the receipt chain alone (light-client-grade)
import Dregg2.AssuranceCase              -- W5: the five guarantees (Authority/Conservation/Integrity/Freshness/Unfoolability) organized BY GUARANTEE, superseding Claims' chronological journal
-- The apex-KEYSTONE integrity audit (`@[load_bearing_keystone satisfiable:=W teeth:=T]` + `#keystone_audit`):
-- each per-guarantee module TAGS its keystones with a non-vacuity (satisfiable) + a teeth (discriminating)
-- companion and `#keystone_audit`s them (THROWS on FAIL), so the corpus build is the per-push CI gate that
-- a keystone has not gone vacuous/toothless. Non-amp is the exemplar; the other four families follow.
import Dregg2.Verify.KeystoneAuditNonAmp        -- guarantee A (8): EffectsAuthority.*_non_amplifying
import Dregg2.Verify.KeystoneAuditAuthModes     -- guarantee A (5): AuthModes.{captp,bearer,token,custom}_sound
import Dregg2.Verify.KeystoneAuditIntegrity     -- guarantee C (8): Argus.Receipt + CommitmentCrossBind + UniversalBridge
import Dregg2.Verify.KeystoneAuditFreshness     -- guarantee D (10): noteSpend + NonMembership + CapSlotFactory
import Dregg2.Verify.KeystoneAuditUnfoolability -- guarantee E (12): RecursiveAggregation + HistoryAggregation + LightClientUC
-- AUDITABLE-not-yet · CHEAP waves (KEYSTONE-LEDGER Waves 1–3): extend the discipline with genuine witnesses.
import Dregg2.Verify.KeystoneAuditSupply        -- Wave 1 (5): mint/burn issuer-move (mintA_authorized + execMintA_iff_spec + recK{Mint,Burn}Asset_delta + requires_live_issuer)
import Dregg2.Verify.KeystoneAuditConservation  -- Wave 2 (15): Conserve.* + recTransferBal/recKExec/recTransfer + monoid layer + W1 executor closure
import Dregg2.Verify.KeystoneAuditTransport     -- Wave 3 (7): non-amp transport + writeCell0/stateCommit receipt-binding + moveAsset memory-program
import Dregg2.Verify.KeystoneAuditRunnable      -- Wave 4 HARD (12): the COMMITTED gated forest (10 execFullForestG_* + memory-program family, real NodeAuthS) + the stepped Argus strand (argus_strand_{conserves,light_client})
import Dregg2.Verify.KeystoneAuditSystemRoots   -- Wave 4 HARD (1): runnable_binds_same_system_roots — a concrete VmRowEnv satisfying wideHashSites over the realizable encodeSponge CR carrier
import Dregg2.Verify.KeystoneAuditArgusReceipt  -- Wave 4 HARD (3+1): argus_circuit_executor_receipts_agree + {argus,transfer}_published_index_pins_receipt + published_position_pins_value — concrete writeCell0 receipt + MMR opening over realized injective carriers
import Dregg2.Verify.KeystoneAuditTerminalAdapters -- the two UniversalBridge leaf/index adapters WELDED (cap_leaf_value_codec + index_boundary_mroot_derived over realizable encodeSponge / canonicity) — NONE left terminal

-- REFINEMENT wave R2 (2026-06-11): THE HEAP's Lean foundation (REFINEMENT-DESIGN Decision 1).
import Dregg2.Substrate.Heap              -- R2 keystone: the openable sorted-map semantics — the cap_root machinery generalized to a generic leaf (get/set/sorted-insert/non-membership via the REUSED sorted_gap_excludes; ext_get canonicity; root_deterministic + root_injective under the ONE named Poseidon2SpongeCR floor)
import Dregg2.Substrate.HeapKernel        -- R2 kernel face, SPLICED (phase 2): heapStepGuarded reads/writes the RecordKernelState.heaps field (a write-verb instance ON stateStepGuarded: atoms→authority→membership→lifecycle→caveats, fail-closed) + heap_contains/heap_get guard atoms (RelCaveat shape); PROVED balance-neutral (scalar + per-asset + ExactConservation), framed (cells/fields/keys), register-binds-heap (heap_root = Heap.root of the spliced leaf list); the wire/circuit binding rides THE ONE ROTATION
import Dregg2.Substrate.FpuProbe           -- §9 product apex: the three verbs (move/grant/write/spend) as Fpu frame-preserving updates in the 4-substance product camera (Sub4 = value × authority × evidence × state); each verb's moving leg by the §5–§8 corner theorems, untouched legs by Fpu.refl off the recCexecAsset_iff_spec frame clauses
import Dregg2.Substrate.VerbCompression   -- R2 investigation (the rotation question, ANSWERED): under the universal map the kernel is create · guarded-write · move (compressed_kernel_three) — grant/revoke/lifecycle/shieldUnshield PROVABLY dissolve into guarded writes (lifecycle/revoke in the landed local class; unshield needs the absence atom — freshness_not_positive separates it from the landed positive HeapAtom algebra; grant needs the order-relational guard — grant_guard_not_literal/not_local) while move SEPARATES (conservation is not a guard in ANY class: gwrite_conservation_trivializes + move_not_single_write) and create separates by birth-arity

-- EPOCH foundations (2026-06-11): boundary/interior proving — Blum memory-checking + descriptor IR v2 + the v2 re-anchor (.docs-history-noclaude/EPOCH-DESIGN.md).
import Dregg2.Crypto.MemoryChecking       -- Blum offline memory checking: read/write multiset balance (initSet+writeSet = readSet+finalSet) under per-op serial discipline ⇒ memory consistency (memcheck_sound); the semantic contract for IR v2's MemOp kind. Non-vacuity incl. the time-travel read (balances but inconsistent — rejected by Disciplined, the load-bearing hypothesis)
import Dregg2.Circuit.MapMerkleRoot        -- THE DEPLOYED depth-16 BINARY-MERKLE map root (heap_root.rs::CanonicalHeapTree): arity-2 mapNode (hash[l,r], the hash_fact(l,[r]) fold node) + perfectRoot (the d-level perfect-tree fold). ⚑ 07-28 THE WHOLE ARITY-2 ANTI-GHOST IS OFF THE REFUTED FLOOR: mapNode_injective/foldLevel_injective are DELETED (the level induction is §3b's foldLevel_binds_or_collides, which RETURNS the colliding pair); perfectRoot_injective, mapRoot_injective, opensToMerkle_functional/_some_excludes_none and writesToMerkle_functional keep their names and now take a NAMED, DECIDABLE per-instance residual (§4a mapRootFind/MapRootSpongeColl, §5a OpenColl/WriteColl at the heaps the openings supply) instead of Poseidon2SpongeCR, which poseidon2SpongeCR_false_babyBear PROVES FALSE at deployed parameters. mapRootFind/mapRoot_binds_or_collides MOVED here from MapPaddedDenotation §4c, and MapPaddedDenotation's duplicate 1-felt extractor tower (§1) is DELETED onto §3b's. Three poles each (dischargeable/refutable/…_refutes_poseidon2CR). ⚑ HONEST PRICE: this is a ONE-FELT commitment, so the residual is ~2^15.5 — a BREAK, not a security level; the ~2^123.5 object is §5b's Digest8 twin. DescriptorIR2.opensTo/writesTo now denote THIS (the last map-ops denotation gap closed). ⚑ §RomSuccessor (07-24): the EXPORTED §5b binding is heapTreeRoot_binds_rom — the depth-d tree equivocation as a RomForgery at the role-keyed sampled oracle, extractor = the PRICED tree walk heapFindComp (2^(d+2)−2 queries, additive; polyBounded_sq_add_const), floor = keyedRom_hard; the seven `_or_collides` forms are DEMOTED to exact-Prop skeletons (retained: Deos.DocSubstrateSound still consumes them — the next repoint site). #assert_axioms-clean.
import Dregg2.Circuit.DescriptorIR2        -- descriptor IR v2: the five EPOCH tables (main/poseidon2-chip/range/memory/map-ops) + Lookup/MemOp/MapOp constraint kinds; Satisfied2 multi-table satisfaction; opensTo/writesTo (the map-op leg) denote the DEPLOYED depth-16 BINARY-MERKLE root (MapMerkleRoot.mapRoot) — ⚑ 07-28 opensTo_functional/opensTo_some_excludes_none/writesTo_functional NO LONGER TAKE Poseidon2SpongeCR: the deployed map-op anti-ghost used to terminate, at its final step, in a theorem that is VACUOUS at deployed BabyBear, and now terminates in the named per-instance residual OpenColl/WriteColl; Lean emits tables+relations, Rust interprets
import Dregg2.Circuit.FullStateChip        -- additive wire-9 raw Poseidon2 transition: fixed `[16,state16,next16]` table + chip_lookup_sound_state16 forces ALL output/capacity lanes; the exact live hash_many_8 sponge can now chain arbitrary complete states without changing legacy chip/VK shapes
import Dregg2.Circuit.Emit.ExactFieldsRefusalEmit -- refusal's reserved audit-field write over the exact FLD2/FLN2 fields tree: full state16 leaf/node Poseidon2 transitions, before/after roots pinned to the record commitment, and all 32 audit bytes published as sixteen verifier-recomputed u16 limbs (no lossy scalar mapOp)
import Dregg2.Circuit.Emit.EffectVmEmitV2  -- the v2 RE-ANCHOR: graduateV1 (hash sites → chip Lookups, range teeth → range Lookups), graduateV1_sound/_complete/_faithful (the round trip — nothing gained/lost); 26-entry v2Registry incl. the newly-expressible Attenuate (cap-crown phase-B circuit leg: MapOp read/write + subset lookup ⇒ attenuateV2_non_amp)
import Dregg2.Circuit.ChipNarrowLookup     -- E7 same-rows bridge (byte-safe half of the by-name zoo re-emit): narrowTable = the 18-prefix of the WIDE chip table (the model of Rust narrow_hist — ONE physical chip serves both buses); narrowTable_sound DERIVES NarrowChip's ChipTableSoundNarrow from the deployed ChipTableSound (previously only ever a hypothesis) so chip_lookup_sound_narrow + the GraduateNarrow tower fire against the deployed serving; chip_lookup_narrow_sound_of_wide_table (the narrow lookup forces the IDENTICAL digest equation the 25-wide chip_lookup_sound forces — the 7 lanes never entered the legacy conclusion) + narrow_served_by_same_rows / narrow_lookup_holdsAt_of_wide (completeness: no honest witness lost). Descriptor bytes UNTOUCHED; the 24-member sweep regen is the staged Epoch-2-sequenced cutover (census: 387/1857 committed cols lanes-only or unreferenced)
import Dregg2.Circuit.Emit.EffectVmEmitCapReshape  -- CAP-RESHAPE CROWN (#103, the ARGUS linchpin): the OPENABLE capability_root — WELDS the proven openable sorted-Poseidon2 map (Substrate.Heap: get=membership-opening/root_injective=anti-ghost) with the REAL is_attenuation lattice (Exec.Caps: confRights/attenuate_confRights_le) into ONE openable cap-root + the production-authority leg that existed NOWHERE. (a) NON-AMP: nonAmplifying (confRights granted ≤ held over Finset Auth, the genuine ⊆); grant_attenuated_nonAmplifying admits every honest attenuation; overGrant_rejected/incomparableGrant_rejected = two-valued teeth (FULL partial order). (b) PRODUCTION-AUTHORITY (§3-correct, non-forgeability NOT affine descent): ProductionAuthorized = the held-set root OPENS to the issuer entry for the asset; production_of_held_issuer (EXHIBIT held issuer ⟹ authority) + unauthorizedMint_rejected/wrongAssetMint_rejected/noControlMint_rejected teeth. Anti-ghost: heldSet_determined/opensEntry_functional (equal roots BIND the held-set, a forged rights-inflated leaf excluded, under Poseidon2SpongeCR). THE CIRCUIT DESCRIPTOR (LAW#1): capReshapeVmDescriptor (186-col, held-leaf recompute site + 8-bit submask non-amp gates + production-authority issuer gates) → byte-pinned dregg-effectvm-capreshape-v1.json (circuit/src/cap_reshape_descriptor.rs parses+teeth-tests). ⚑ THE HELD BITS BIND THE HASHED FELT (fixed 2026-07-24): the two mask-recon gates read prmCol col.HELD_MASK = col 72 (the very felt siteHeldLeaf hashes) and prmCol col.GRANTED_MASK = col 74 — NOT the raw param INDICES 4/6, which are effect-SELECTOR columns this descriptor never constrains. Until then BOTH teeth were decoupled from the committed leaf: all-held-bits-set satisfied every submask gate vacuously AND the control-bit mint gate for free, off a read-only mask at col 72. Behavioural canary, both descriptor generations: cap_reshape_descriptor.rs::held_bits_bind_the_hashed_held_mask_felt (pre-fix bytes ACCEPT the forgery, fixed bytes REFUSE). RESIDUAL: col 72 is bound to the recomputed held LEAF, and that leaf is not opened against a published cap_root inside THIS json — the opening is the turn-level binding; capReshape_nonAmp_in_circuit/capReshape_production_in_circuit + capReshape_rejects_amplify/capReshape_rejects_no_control = the in-circuit teeth. Non-vacuity BOTH polarities #guard-EXECUTED. #assert_axioms-clean. SEAM: in-circuit checks the submask SHADOW (entryEncodes bridges to the Finset lattice); turn-level cap_root→cell-root binding = sdk full_turn_proof.rs (Phase-D)
import Dregg2.Circuit.CapRootBridge        -- THE cap_root ↔ kernel Caps AUTHORITY BRIDGE (in-circuit-authority foundation; VK-neutral): supplies the MISSING capRootOf-style commitment relation CapsEncodes (cap_root = Heap.root of a FeltHeap that FAITHFULLY realizes the kernel Caps — the link that existed NOWHERE) + capEdgeKey (= the circuit's hash[holder,target,rights,op] cap-edge leaf address) + the writeBit_decodes round-trip. THE BRIDGE capOpen_implies_authorizedB: given CapsEncodes + an in-circuit opensTo opening at the (actor⇒src) write-rights edge, the kernel's authorizedB caps ⟨actor,src,…⟩ = true (opensTo_functional under Poseidon2SpongeCR forces the witness to open THE committed cap-tree; faithfulness yields the real held cap). Non-vacuity: capsEncodes_inhabited + bridge_fires (END-TO-END on an injective sponge: a real opening ⇒ authorizedB=true) AND empty_caps_unauthorized (witness FALSE — the gate is real). #assert_axioms-clean (Poseidon2 CR only, via CapsEncodes)
import Dregg2.Circuit.DeployedCapTree     -- THE FAITHFUL model of the DEPLOYED 7-field cap-tree (cap_root.rs::CanonicalCapTree), now POSITION-FAITHFUL to circuit/src/poseidon2.rs: a CapHashScheme bundle carries the deployed permutation carriers — capLeafDigest = leafM.spongeOf ∘ leafFields (the RATE-4 hash_many, 2 permutes for 7 fields, length tag in capacity slot 4) and nodeOf = nodeCompress ∘ packNode where packNode l r = [l,r,0,0,0,0xFACF,1] (the SINGLE capacity-tagged hash_fact permute, 0xFACF/leaf-flag in capacity slots 5/6, NOT a rate prefix). Injectivity rides ONE named floor: leaf via SpongeReduction's hash_many MD reduction (spongeCR_of_reduction), node via CommitmentBinding.Compress1CR (the same single-permutation-call primitive #4 as hash_2_to_1). recomposeUp/MembersAt (depth-16 binary-Merkle opening) + deployedCapOpen_implies_authorizedB (kernel authorizedB) re-proved over the scheme. The HONEST replacement for the flat-sponge Substrate.Heap opensTo.
import Dregg2.Circuit.DeployedFieldsTree  -- THE FAITHFUL native-8-felt user-field-MAP tree (Phase H-FIELDS-8, the THIRD and LAST faithful root): Fields8Scheme mirrors Heap8Scheme/Cap8Scheme — fieldsLeafDigest8 (arity-3 IMT [addr,value,nextAddr], addr=field_key_hash key) + fieldsNodeOf8 (arity-16 pack8, the SAME node8 chip cap/heap ride). ⛑ THE chip8CR FIELD IS DELETED (07-20): it was Compress8CR chipAbsorb8, FALSE at deployed BabyBear params, so NO Fields8Scheme value could be built and every theorem over the type was VACUOUS. Fields8Scheme now carries ONLY the chip; §F.D constructs deployedFields8Scheme (a COMPUTING inhabitant, 4 #guards: 8 lanes, BabyBear bounds, value-write moves the digest, pointer-relink moves it, write moves the recomposed root) whose OWN chip the teeth refute (VacuitySweepTeeth.deployedFields8Scheme_chip_not_Compress8CR). Binding is EXTRACTED AS DATA: fieldsLeafDigest8/fieldsNodeOf8/recomposeUp8_binds_or_collides + the composite fieldsOpen8_binds_leaf_or_collides (the GENTIAN close, unconditional) — each concludes `binding ∨ Coll8 chipAbsorb8 (the pair a TOTAL extractor returned)`, never `∃ collision` (pigeonhole makes that free). Spine REUSED not re-authored: CapMerkleGeneric.recomposeGFind/_spec. §F.S proves the strength relation BOTH ways (every deleted theorem = the injective special case; the collision disjunct is REFUTABLE) and §F.R exhibits both branches reachable (refFieldsScheme8 / badFieldsScheme8). #assert_axioms-clean.
import Dregg2.Circuit.DeployedHeapTree    -- THE FAITHFUL native-8-felt HEAP tree (Phase H-HEAP-8, the SECOND faithful root): Heap8Scheme mirrors Cap8Scheme — heapLeafDigest8 (arity-3 IMT [addr,value,nextAddr]) + heapNodeOf8 (arity-16 pack8, the SAME node8 chip cap rides). ⛑ THE chip8CR FIELD IS DELETED (07-20): it was Compress8CR chipAbsorb8, FALSE at deployed BabyBear params, so NO Heap8Scheme value could be built and every theorem over the type — including the sorted-tree heap forcing the perimeter leans on — was VACUOUS. Heap8Scheme now carries ONLY the chip; §H.D constructs deployedHeap8Scheme (a COMPUTING inhabitant, 5 #guards: 8 lanes, BabyBear bounds, value mutation moves the digest, IMT pointer-relink moves it, mutation moves the recomposed root) whose OWN chip the teeth refute (VacuitySweepTeeth.deployedHeap8Scheme_chip_not_Compress8CR). Binding is EXTRACTED AS DATA: heapLeafDigest8/heapNodeOf8/recomposeUp8_binds_or_collides + the composite heapOpen8_binds_leaf_or_collides (the GENTIAN close, unconditional) — each concludes `binding ∨ Coll8 chipAbsorb8 (the pair a TOTAL extractor returned)`, never `∃ collision`. Spine REUSED not re-authored: CapMerkleGeneric.recomposeGFind/_spec. §H.S proves the strength relation BOTH ways (every deleted theorem = the injective special case; the collision disjunct is REFUTABLE) and §H.R exhibits both branches reachable (refHeapScheme8 / badHeapScheme8). #assert_axioms-clean.
import Dregg2.Circuit.DeployedCapOpen     -- THE IN-CIRCUIT cap-tree membership-OPEN (the authority leg's circuit foundation): a CapOpenConstraint whose denotation rides the Poseidon2 chip bus (DescriptorIR2.chip_lookup_sound) for the 7-field leaf absorb and each of the depth-16 node folds (mixed by the direction bit), CONSTRAINS the top == cap_root column, binds leaf.target == src + mask_lo == write mask. CHIP-RATE GAP NAMED: the rate-8 chip absorb is NOT the deployed rate-4 leaf / capacity-tagged node, so soundness threads the EXPLICIT named bridge SchemeRealizedByChip sponge S (carried as a hypothesis, NOT assumed) — with a witness-FALSE schemeRealizedByChip_node_unrealizable proving the deployed node single-permute does not satisfy it (rate-3 prefix [FACT_MARK,l,r] ≠ capacity-tagged packNode l r under nodeCompress injectivity). KEYSTONE capOpen_sound: Satisfied + SchemeRealizedByChip ⟹ MembersAt S cap_root leaf ∧ target=src ∧ confersWriteLeaf; capOpen_authorizes ⟹ kernel authorizedB. Discriminating teeth (writeMaskGate/targetBindGate witness-FALSE). #assert_axioms-clean (Compress1CR/CompressionCR floors via the chip lever).
import Dregg2.Crypto.UniversalMemory       -- UNIVERSAL MEMORY (.docs-history-noclaude/UNIVERSAL-MEMORY.md): ALL state (registers/heap/caps/nullifiers/index) = ONE Blum multiset over the domain-tagged address space; universal_memory_sound = the keystone (one balance ⇒ every per-domain projection consistent as a STANDALONE memory, via consistentFrom_filter + the tag-strip); memcheck_pins_final (the claimed final column = the genuine fold — the boundary view is forced); boundary_root_derived/_from_memcheck (the map roots are DERIVED views: sorted-sponge of the final cells = today's Heap.root by canonicity, NO crypto — a refactor not a semantic change); THE NULLIFIER WIN nullifier_fresh_sound (freshness = a read-none memory row: absent-from-init ∧ no intra-proof insert — NO Merkle path intra-proof) + nullifier_fresh_binds_root (cross-proof persistence = the boundary root, root_injective). Conservation stays OUT (rectangular ⇒ BiorthTensor's conservation_not_behaviour_rectangular — Σδ=0 needs the correlated pair, the in-row move constraint). Non-vacuity: honest 5-domain trace balances; cross-domain tuple-steal UNBALANCES; the flat untagged space ALIASES (tag load-bearing); intra-proof double-spend REFUSED
import Dregg2.Crypto.PerCellUmem          -- STAGE A of docs/deos/UMEM-PRIMITIVE.md (PER-CELL UMEM SOUNDNESS; Codex-flagged: verify the UKey::cell() filter preserves soundness): a per-cell umem = the global umem (Crypto/UniversalMemory) restricted to ONE cell's heap keys (turn/src/umem.rs:215 UKey::cell()). percell_consistentFrom_filter = THE FILTER PRESERVES SOUNDNESS (a direct consistentFrom_filter instance at the per-cell heap address class — dropped ops, other cells/domains, can't touch this cell); percell_consistent_strip = the filtered slice, tag-peeled, is a consistent STANDALONE memory (consistentFrom_strip after the cell filter — sound IN ISOLATION); percell_boundary_root_derived = the per-cell boundary root EQUALS the committed heap_root (cell/src/state.rs:210,409 compute_heap_root) by boundary_root_derived (canonicity, NO crypto); percell_umem_sound welds both. NO new soundness machinery — the tag-isolation keystone applied at the per-cell seam. Non-vacuity both polarities: cell-100 filter DROPS the other-cell + other-domain writes (teeth: cell-200 recovers a different slice), boundary derivation fires on a concrete one-cell heap. #assert_axioms-clean
import Dregg2.Crypto.UMemCodec            -- RANK 1 of the umem rotation-flip (UNIVERSAL-MAP-ROTATION.md §2.2 / UNIVERSAL-MEMORY.md §123-149): the umem ADDRESS/VALUE codec adapters — the REAL structured carriers replacing effect_vm_umem_real_turn.rs's per-proof dense relabeling. §1 ADDRESS codec uaddrEnc=hash[domainTag d, coll, key] + uaddrEnc_injective (CR + injective domain tag ⇒ the concrete hashed address realizes the abstract (Domain,coll,key) triple FAITHFULLY; the untagged Heap.addrOf collapses caps/nullifiers at the same (coll,key) — the tag is load-bearing). §2 CAP-LEAF value codec capLeafOf = edgeLeafOf hash holder target rights op (DEFINITIONALLY the deployed siteCapEdgeLeaf, capLeafOf_eq_deployed by rfl) — value codec splits WHERE(holder)/WHAT(target·rights·op) (capValEnc/capDecode round-trip, capLeaf_factors_codec); capLeaf_injective ⇒ capRoot_injective (the cap boundary root binds its cap cells, Heap.root_injective transferred to the live 4-felt leaf, NO new combinatorics). §3 MMR boundary-derivation analogue for the index domain: index_boundary_root_derived (refactor, NO crypto) + index_boundary_root_bound (⚑ STALE 2026-07-28, ed616d24c: `mroot_injective` is DELETED — it was FALSE→FALSE, concluding an injectivity of a `… → ℤ` map that the deployed sponge refutes by the same pigeonhole that refutes its own premise. `index_boundary_root_bound` now rides the per-instance residual `MRootColl`; tamper/REORDER/truncate are still refused, but at ≈2^15.5 queries, not unconditionally) + index_boundary_root_from_memcheck (rides memcheck_pins_final — derived index log = genuine pinned final column). Every binder rides the SAME named Poseidon2SpongeCR floor (no narrower bit-count), VK-risk-free (no wire/descriptor/VK change); non-vacuity both polarities on refSponge; #assert_axioms-clean (17 theorems pinned)
import Dregg2.Lightclient.HistoryIndex    -- the receipt index as an openable sorted map: lex (subject, height, seq) keys (per-subject ranges CONTIGUOUS), rangeOf exact range restriction + get⇔mem characterizations, iroot (sponge of hash[subject,height,seq,commitment] leaves) BINDS the whole index (iroot_injective) hence every range (iroot_binds_range) under the ONE Poseidon2SpongeCR floor; addReceipt = sorted insert (invariant preserved); non-vacuity incl. omission-moves-the-root
import Dregg2.Storage.DealCell           -- STORAGE-IN-LEAN PROTOCOL: the FAITHFUL executor cell-program — ALL LEGS refine DealLifecycle. A deal cell carries an explicit status slot (encoded DealState) + bond; each of the SIX guarded transitions (claim/activate/auditPass/auditFail/settle/slash) provably makes EXACTLY its DealLifecycle counterpart under absDeal. No coarsening, no collapsed audit states — the deployed cell-program IS the abstract protocol, all alignments. #assert_axioms-clean.
import Dregg2.Storage.MarketRefinement   -- STORAGE-IN-LEAN PROTOCOL: the executor-wired ProviderMarket cell-program REFINES the abstract DealLifecycle. abstractDeal maps a market cell to a Deal; claimDeal_refines_claim — when the executor admits a claim on an open deal, the abstract deal makes EXACTLY DealLifecycle.claim (open->claimed, bond locked). The deployed claim IS the proven protocol claim (via the cell field mechanics: pmWriteField_same/other). #assert_axioms-clean.
import Dregg2.Storage.ClientProtocol     -- STORAGE-IN-LEAN, THE END-TO-END PAYOFF: data_survives_and_cheaters_pay — one theorem composing verifiable_erasure_recovers + honest_provider_not_slashed + withholding_is_slashable: an erasure-coded blob RECOVERS as long as k providers pass their audit, honest providers CANNOT be slashed, withholding providers ARE slashed. The client-facing promise DERIVED, not asserted. #assert_axioms-clean.
import Dregg2.Storage.ProviderRegistry   -- STORAGE-IN-LEAN PROTOCOL: the PROVIDER entity stake lifecycle (Unregistered->Registered->Withdrawn). canServe cap-first gate; serves_only_if_registered, register_requires_unregistered (sole entry), slash_requires_registered (+reduces stake), withdraw_requires_registered, stake_nonincreasing_except_register (no conjuring stake to over-claim). #assert_axioms-clean.
import Dregg2.Storage.DealPayment        -- STORAGE-IN-LEAN PROTOCOL: the market CONSERVES VALUE (no minting/destruction). Ledger {bond, escrow, paidToProvider, refundedToClient, burned}; settle_conserves_value (provider paid escrow+bond) + slash_conserves_value (bond burned, escrow refunded) — total invariant; slash_burn_is_the_bond_reduction (the tooth is a real BURN, not a transfer to the house). #assert_axioms-clean.
import Dregg2.Storage.DealLifecycleTrace -- STORAGE-IN-LEAN PROTOCOL: the deal lifecycle is ACYCLIC + forward-only. stateRank monotone; step_advances_rank (every Step strictly increases the rank — a deal NEVER moves backward: no un-settling/un-slashing/re-opening); step_irreflexive (no self-loop); rank_le_max + terminal_has_max_rank. #assert_axioms-clean.
import Dregg2.Storage.MarketAudit        -- STORAGE-IN-LEAN PROTOCOL: the audit drives the lifecycle, honest=>safe / withholding=>slashed. runAudit maps the PoR verdict (Retrievability.passes) to DealLifecycle auditPass/Fail; honest_provider_not_slashed (a passing audit => auditedPass, from which slash is IMPOSSIBLE — composed with por_sound, an honest provider keeps its bond ALWAYS) + withholding_is_slashable (a failing audit => auditedFail, slash succeeds — the tooth bites real withholding, and por_refuses_substitution means it cannot fake a pass). #assert_axioms-clean.
import Dregg2.Storage.DealLifecycle     -- STORAGE-IN-LEAN, PROTOCOL LOGIC: the market deal as a PROVEN state machine (Open->Claimed->Active->Audited{Pass|Fail}->{Settled|Slashed}). Transitions are partial fns (illegal step = unrepresentable); proofs: terminal_is_final (a settled/slashed deal admits no step), settle_requires_passed_audit (can't pay without a passing PoR audit), slash_requires_failed_audit (can't slash without a failing one), bond_nonincreasing_after_claim (collateral never conjured once locked). The abstract protocol ProviderMarket.lean's cell-program refines. #assert_axioms-clean.
import Dregg2.Storage.Deployed          -- STORAGE-IN-LEAN, THE EXTRACTION (Lean side): the bucket content root over the DEPLOYED Poseidon2, called from Lean via @[extern "dregg_poseidon2_hash"] (the fast Rust primitive, same shape as @[extern "dregg_ed25519_verify"]). contentRootDeployed = contentRoot over the FFI hash. ⚑ CUTOVER 2026-07-25: contentRootDeployed_injective is DELETED — it was gated on Poseidon2SpongeCR poseidon2Hash, which DeployedFloorRefuted refutes by rfl, so it was VACUOUS at deployment. ⛑ ENCODER REPAIRED 2026-07-26: limbs now go through canonLimb ((x % babyBearP).toNat.toUInt64) instead of the CLAMPING x.toNat.toUInt64 that merged -1 with 0; clampLimb + clampLimb_merges_negative keep the defect nameable, canonLimb_separates_negative is the fix, and a fail_if_success tooth reds the build if rfl ever closes poseidon2Hash [-1] = poseidon2Hash [0] again. MIGRATION NIL: dregg_poseidon2_2to1 already reduces % BABYBEAR_P, so every root over limbs in [0, 2^64) is byte-identical across the change. Residual NAMED, not hidden: canonLimb is a reduction, so p-congruent integers still alias (canonLimb_aliases_mod_p) — a WIRE-contract question (contentRootFFI parses arbitrary String.toInt?), not an encoder one. Replaced by contentRootDeployed_binds_or_collides: UNCONDITIONAL (binds, or the extractor HANDS BACK the specific colliding pair), hence a statement about the deployed hash; contentRootDeployed_injective_of_binds_or_collides is the contradiction-style strength bridge recording that nothing was surrendered. The verified LOGIC is Lean (leanc-compiled), the hot PRIMITIVE is fast Rust. #assert_axioms-clean.
import Dregg2.Storage.DeployedFloorRegrounded -- ⚑ THE FLOOR CUTOVER: the storage binding moved off literal injectivity (Poseidon2SpongeCR, REFUTED at the deployed hash by rfl) onto the collision GAME the tree already had — FloorGames.HashCRHardQuant (HashSigMerkleRegrounded.spongeFamily D) Eff, REUSED verbatim, not built. §2 hashCRHardQuant_of_injective PROVES the old floor implies the new one at EVERY adversary class (the cutover is a WEAKENING, so every migrated theorem is strictly stronger); §3 the content-root break is a first-class Game, bucketBreakToFinder is the extractor as a MAP OF ADVERSARIES, bucket_wins_imp + bucket_adv_le give the reduction, contentRoot_binds_advantage_bound the repaired keystone (hEff undischarged and NAMED — no cost model, FloorGames §8), plus the canary (floor at ANOTHER finder does NOT discharge it) and the positive pole; §4 ⚑ THE FLOOR GOES RED AT DEPLOYMENT — deployedFloor_refuted_by_modp_alias: the constant finder ([0],[p]) has advantage 1, and deployed_ghost_object_exists exhibits a REAL ghost object under a genuine deployed content root. ⛑ THE WITNESS MOVED 2026-07-26 and the move IS the finding: it used to be the CLAMP pair ([-1],[0]) — an encoder BUG, now FIXED — and is now the mod-p ALIAS pair, which is correct field semantics. §4.1 isolates both (preFixLimb_collides vs babyBearLimb_separates vs babyBearLimb_aliases_mod_p, all by decide; babyBearLimb_is_the_deployed_limb pins that the fix is LANDED, not described), and prefix_clamp_ghost_is_closed proves the -1/0 ghost now costs a genuine Poseidon2 collision. The residual has a named recipe (range-check limbs at contentRootFFI) deliberately NOT taken here: it is a wire-contract change. #assert_axioms-clean.
import Dregg2.Crypto.HashSig
import Dregg2.Crypto.HashSigMerkle       -- PQ MANY-TIME hash signature (SLH-DSA/SPHINCS+ shape): a Merkle tree over N Lamport OTS keys -> a many-time PQ sig, reusing HashSig (the OTS) + Lightclient.MMR (mroot/Opens; ⚑ `mroot_injective` DELETED 2026-07-28, ed616d24c — false→false at the deployed sponge) now on the per-instance residuals `MRootColl`/`CommitColl`, all three poles proved, and OFF `Poseidon2SpongeCR` entirely. merkle_ots_correct (unconditional: honest sig verifies); merkle_ots_binds_index (a verifying sig's OTS pubkey IS the one committed at that index — via mroot_injective+CR+pkEncode_injective; a forgery is thus a Lamport forgery -> lamport_forgery_breaks_hash). Concrete non-vacuity: demo_honest_verifies / demo_wrong_key_rejected (+demo_keys_distinct) / demo_replay_rejected, all by decide. #assert_axioms-clean.
import Dregg2.Crypto.Frost
import Dregg2.Crypto.HermineThreshold
import Dregg2.Crypto.HermineExtractor
import Dregg2.Crypto.Lattice
import Dregg2.Crypto.HermineMSIS
import Dregg2.Crypto.HomomorphicDigest -- the SIS-based homomorphic MONOID digest (experiment): digest A encode S := A(∑ᵢ∈S encode i) over the real linear SIS map A. The one coordination gadget the census found ABSENT — a commutative/associative FOLD monoid (every fold in-tree is either the shape-sensitive Poseidon acc or order-dependent first-wins). digest_eq_sum/digest_union/digest_biUnion make the fold a Finset.sum: order- AND grouping-free — fold ANY partition into disjoint blocks and recombine (the distributed worker-pool's associativity, as a theorem; DISSOLVES the streaming fold's MMR shape-guard — folding is now literally addition). digest_collision_extracts_msis EXTRACTS a short nonzero kernel vector from a distinct-history collision (a Module-SIS solution, produced as DATA, HermineMSIS-style, no supplied adversary); digest_binds_under_msis + digest_binds_bounded reduce binding to MSISHard (the honest named lattice floor, one per bounded-capacity epoch). SumInjective carried + TEETH (digest_collides_of_not_sumInjective: drop it and a distinct-history collision reappears with a ZERO witness — prove-the-obligation-necessary). This is the PROOF-OF-CONCEPT the construction is sound; the parameterization (the SIS digest-width number), position-indexed sum-injectivity (make the obligation structural), and emit-to-AIR are the follow-up stones. #assert_axioms ⊆ {propext, Classical.choice, Quot.sound}.
import Dregg2.Crypto.HomomorphicDigestPositioned -- STONE #2: the position-indexed instantiation that makes SumInjective STRUCTURAL (discharges the last carried hypothesis). positionEncode val i := Finsupp.single i (val i) on ι →₀ R with val i ≠ 0. positionEncode_sum_support proves (∑ i∈S, encode i).support = S — the history is RECOVERED as the support of its sum — so positionIndexed_sumInjective DISCHARGES SumInjective from val≠0 (a genuine support argument, not a tautology). digest_binds_positionIndexed + _bounded then bind the digest with NO assumed sum-injectivity: only val≠0, shortness (nrm(val i)≤β₀), and the honest MSISHard floor remain. val_nonzero_load_bearing = teeth ({i} vs ∅ collide when val i = 0); demo_unitVals/demo_zeroVals exhibit both poles (predicate non-trivially true AND false). Full ShortNorm (ι →₀ R) coordinate-∞-norm instance connected; noncomputable (Finsupp.single, proof-layer). #assert_axioms ⊆ {propext, Classical.choice, Quot.sound}.
import Dregg2.Crypto.PqShareCommitment -- EXPERIMENTAL PQ SHARE-COMMITMENT RUNG: commit(m,r)=Aₘm+Aᵣr over an arbitrary linear SIS map. Proves exact opening iff, additive/rerandomization algebra, private opening-link ⇒ public commitment-link, and the adversarial converse as DATA: a false opening link beneath a valid public link extracts a short nonzero kernel vector of [Aₘ|Aᵣ]. Rust/Lean BabyBear KAT pins centered reduction + row order. Dimensions/hiding remain explicit assumptions; NO full-PQ claim. #assert_axioms-clean.
import Dregg2.Crypto.DealerlessQ0PartyLocal -- EXPERIMENTAL q0 PARTY-LOCAL ROUND: exact context/slot-key/ordered-roster inclusion, locally retained opening projection, shared-anchor split-view refusal under explicit binding, dealer/viewer-free private aggregation, and exact 4096-row radix reconstruction. The first-carry falsifier proves the radix codec is injective but NOT additive; current per-slot commitments must remain an ordered transcript list. NO SIS/hiding/authenticated-broadcast/live-dealer-removal claim. #assert_axioms-clean.
import Dregg2.Crypto.HermineSelfTargetMSIS  -- the SelfTargetMSIS discharge of u≠0 — the argument Dilithium/Raccoon ACTUALLY use, SUPERSEDING the ROLE of HermineDischarge's IsUnit(c−c') route (not satisfiable at deployed params: q^(1/256)≈1.06<2). The FS challenge is bound INTO the M-SIS relation; two forked forgeries share the commitment w but carry c≠c', so subtracting the verify equations gives [A|t]·(z−z', −(c−c'))=0, a NONZERO short MSIS solution on the augmented map — non-triviality FREE from c≠c' (the challenge sits in its own coordinate), NO invertibility, NO MLWE-lossiness for the u≠0 leg. selftarget_extract_nonzero (the headline: c≠c' ⇒ −(c−c')≠0 directly); forked_forgery_yields_msis_solution_selftarget + no_forgery_under_msis_selftarget (MSISHard ⟹ no forgery). Teeth: ZMod 5, c≠c' FIRES (nonzero (4,1)) vs c=c' collapses the challenge coordinate to 0. #assert_axioms-clean.
import Dregg2.Crypto.HermineHintMLWE
import Dregg2.Crypto.HermineTSUF        -- the FULL concurrent TS-UF-0 game (Bellare-Tessaro-Zhu threshold unforgeability), reduced to MSISHard ∨ MLWESearchHard ∨ HashCR — SUPERSEDES HermineHintMLWE.concurrent_unforgeable_reduces (which ASSUMED the two transcripts and modeled neither the signing oracle nor the t−1 corruption). Models keygen (Shamir-shared t=A·s) + static ≤thr−1 corruption + concurrent signing oracle + FRESH forgery (KeyGen/SigningSession/AdversaryView/Forger/Fresh; fresh_forgery_distinct_from_sessions). ORACLE SIMULATION grounded in MLWE: oracle_answer_secret_free (simulateCommit, no secret) + oracle_hiding_grounded_in_mlwe (=hint_mlwe_reduces_to_mlwe) + oracle_distinguished_breaks_mlwe (recovering the honest secret from the hints IS an MLWE break) + oracle_view_within_tv (Q·ε, reusing hint_mlwe_hybrid_leakage). CORRUPTION EMBEDDING grounded in ShamirPrivacy: corrupt_view_challenge_independent (the thr−1 shares consistent with EVERY secret → challenge-independent). FORKING — PRODUCED not assumed: Forger.rewind (explicit RO resample at the fork index) + Forger.fork_preserves_commitment (the shared commitment w is DERIVED from commitment_preChallenge, not a bare hypothesis) + fork_produces_msis (feeds the two runs to selftarget_extract_nonzero). HEADLINE concurrent_ts_uf_0_reduces: the three attack modes route to ¬HashCR (equivocation) / ¬MLWESearchHard (secret leaked) / an MSIS solution (fork). Single honest TODO: ForkingProbabilityBound (the ≈ε²/q_H rewinding PROBABILITY — a standard probability lemma, NOT a hardness carrier, assumed in NO proof). #guards: forking→MSIS pipeline fires (c≠c' load-bearing), all three headline doors fire, corruption/oracle-TV/secret-free/freshness. #assert_axioms-clean.
import Dregg2.Crypto.VRF -- abstract Micali-Rabin-Vadhan VRF security framework (provability, UNIQUENESS + the X-VRF/WOTS+ break as a load-bearing #guard, pseudorandomness) + the lattice-VRF uniqueness→MSIS reduction skeleton (reuses SelfTargetMSIS). For a PQ sortition VRF (XM-VRF/LB-VRF).  -- the REAL Raccoon CONCURRENT-security argument (masking + commit-reveal + SelfTargetMSIS, Hint-MLWE STRAIGHT-LINE — no forking, no ROS). SUPERSEDES the FROST-binding-factor/ROS framing (a group-setting mis-model). Pillar 1 (MASKING): HintMLWEHard — the many-session hint transcript is simulatable within statDist ε; hint_mlwe_of_smudge GROUNDS it in the single-session HermineHiding.signature_hides_secret/Smudging.smudge_bound (honest multi-session generalization, NOT a fresh axiom); hint_mlwe_hybrid_leakage (additive TV, Rényi tightens to √Q) + leakage_exceeds_budget_breaks_hint_mlwe. Pillar 2 (COMMIT-REVEAL): CommitReveal + HashCR; commitment_binding (rushing teeth — a signer can't open cm to two w) + equivocation_breaks_hashcr. Pillar 3: reuses no_forgery_under_msis_selftarget. HEADLINE concurrent_unforgeable_reduces: HashCR (binding forces ONE shared commitment w) + MSISHard (SelfTargetMSIS closes) ⟹ no concurrent forgery; concurrent_forgery_breaks_hashcr_or_msis is the rushing dichotomy (equivocate ∨ bound-forge→MSIS). #guards: binding opens/catches + colliding H equivocates; composition FIRES over ZMod 5 (two c≠c' forgeries under a binding CR → MSIS solution) while hints stay simulatable (≤1/10). #assert_axioms-clean.
import Dregg2.Crypto.SortitionGame
import Dregg2.Crypto.XmVrfRefinement
import Dregg2.Crypto.DoubleSidedO2H
import Dregg2.Crypto.LossyIdentification
import Dregg2.Circuit.AirSoundness
import Dregg2.Circuit.FriSoundness
import Dregg2.Circuit.ChainDigestRomBinding -- ⚑ THE §8 CHAIN-DIGEST D-DROP (07-24): Circuit.chain_digest_binds / chain_digest_binds_indicator / false_chain_not_bound / chain_digest_binds_chainOk and AirSoundness.committed_trace_pinned are DELETED (each carried HashCR — pure injectivity of the digest hash, PROVED FALSE for every compressing digest by hashCR_false_of_compressing); this leaf module is their keyed-ROM successor: chainDigestRomGame = the digest-OPENING game at the SAMPLED keyed oracle (published digest IN the win relation, romOpenGame over crRomFamily Unit Trace); chain_digest_binds_rom = every query-bounded trace-equivocator's advantage NEGLIGIBLE on the PROVED floor keyedRom_hard (birthday bound, NO refutable hypothesis); chainDigestRom_indicator_break transports a chainOk-equivocation (via Circuit.chainOk_ne_imp_trace_ne) into a win; teeth: the 0-query hardcoded opener is ADMITTED, wins at the constant oracle, and is DEFANGED (negligible) — the counterexample dies by keying; non-negligible forgers excluded; fail_if_success canary. Deterministic residue kept in Circuit.lean: distinct_traces_break_hashcr / digest_collision_is_hash_collision (¬HashCR-conclusion extractor witnesses only). A separate leaf because RomCarrierSites' closure imports Dregg2.Circuit (HashFloorHonesty → Poseidon2Binding → StateCommit → Transfer). The unbounded deployed List-Turn trace enters through the lossless BVec truncation (bvecOfList_inj). #assert_all_clean (5 keystones).
import Dregg2.Circuit.CommitDifferentialRom -- ⚑⚑ THE PER-CELL COMMITMENT BINDINGS AS REDUCTIONS ON THE PROVED KEYED-ROM FLOOR (07-24): the two CommitDifferential `_or_collides` headlines were bare disjunctions (a hash_4_to_1 collision EXISTS by pigeonhole at deployed BabyBear params, so each held through its right branch with NO binding). This module states the two LOCALIZED tamper forgeries over EffectVmRowCommitReduction's narrowRomFamily — recordDigestRomTamper (the audit-P0-2 authority-residue tamper, outer felt) and capRootRomTamper (block C slot 3 = deployed limb index 11) — and closes each INTO effectVmNarrowRomForgery via narrowRow_binds_rom (the inner/outer union bound on keyedRom_hard, the birthday bound, PROVED): effectVmCommit_binds_record_digest_rom / effectVmCommit_binds_cap_root_rom, NO refuted floor, NO bare disjunction. narrowValOfLimbs pins the 13-limb correspondence (record_digest = outer slot = index 12, cap_root = C[3] = index 11) with full losslessness (narrowValOfLimbs_inj). SAME COMMIT: the consumer-free effectVmCommit_binds_cap_root_or_collides is DELETED from CommitDifferential (its two-branch case split IS the union bound, now performed at the sampled oracle); the record_digest disjunction is RETAINED as the extractor witness RotatedCommitDifferential still rcases on, its re-point surfaced follow-up. Teeth: const answerer admitted+wins-at-const-oracle+DEFANGED; non-negligible tamperers EXCLUDED. #assert_axioms (8).
import Dregg2.Crypto.HashRandRefinement
import Dregg2.Crypto.ConsensusViewChange
import Dregg2.Crypto.BlocklaceSafety
import Dregg2.Crypto.LightClientSoundness
import Dregg2.Crypto.ThresholdSignerRefinement
import Dregg2.Crypto.WireAke
import Dregg2.Crypto.Fips203Kem
import Dregg2.Crypto.X25519HkdfExtract -- lifts the abstract VRF (Dregg2.Crypto.VRF) to the protocol-level LEADER-SORTITION guarantee: electing by `VRF(sk, epoch) < thr` is UNIQUE, FAIR, UNPREDICTABLE, each reducing to VRF.lean (NO new carrier). Model: elected := (eval sk epoch).1 < thr; ElectionProof (y,π) = verify ∧ y<thr. sortition_unique (← uniqueness_at_most_one): two verifying claims force the SAME output — no grind/double-claim; double_claim_refutes_uniqueness = the X-VRF break as a refutation. FAIRNESS: electionDensity thr = |{y<thr}|/|Output| (nonneg, ≤1); sortition_fair (← Pseudorandom): real election bit = uniform sample's below-thr bit ⟹ honest validator elected at density thr/|Output|. sortition_unpredictable (← Pseudorandom): any sk-free predictor's verdict on the real output = on a uniform surrogate — no edge over the base rate. #guards: electionDensity(1:Fin4)=1/4 & (3)=3/4 fire non-vacuously; toyVRF elects a UNIQUE winner (only 0 verifies); doubleClaimVRF accepts TWO distinct winning tickets 0,1 (<3) — the X-VRF double-claim tooth. #assert_axioms-clean.
import Dregg2.Crypto.HybridCombiner
import Dregg2.Crypto.ConcreteSecurity
import Dregg2.Crypto.ProbCrypto -- Track B of crypto-honesty: the PROBABILISTIC-CRYPTO substrate that makes the reductions genuine CONCRETE-SECURITY theorems, not Boolean implications. ADDITIVE — the Boolean floors (Lattice.MSISHard := ¬∃solver, SchnorrDLHard) are UNTOUCHED; this adds their quantitative siblings alongside. Builds on ConcreteSecurity's Negl algebra + HermineTSUF's section ProbForking (advantage/forkProb/forkProb_ge_advantage, the PROVED finite-counting-probability inequality forkProb ≥ ε·(ε−1/|C|)). §1 winProb — a finite game's real-valued advantage in [0,1] (winProb_top/bot teeth). §2 QBAdversary qH — the query-bounded-ROM adversary model (bounded oracle queries as Fin qH, no PPT Turing machine). §3 MSISHardQuantShape adv := ∀ s, Negl (adv s) — the QUANTITATIVE MSIS floor, ℝ-valued advantage that CAN be non-negligible (+ MLWE/DL/HashCR siblings; msisHardQuant_zero/broken teeth). §4 THE RE-THREADED FORKING→MSIS REDUCTION as a genuine advantage-inequality: negl_of_negl_sq (Negl ε² → Negl ε, a real squeeze) + negl_of_forking_bound turn forkProb_ge_advantage into forking_reduces_to_MSISHardQuant (MSISHardQuantShape ⟹ Negl (forger advantage)) and forking_reduces_against_floor (the derived solver IS one of the floor's solvers). NON-VACUITY: advantage_example_eq (the ZMod-5 forger's advantage = 2/5, a genuine real); const25_forger_breaks_floor (a 2/5 forger forces solver advantage ≥ 2/25, ¬Negl — the reduction BITES); zeroFamily_forger_negl (the reduction FIRES end-to-end under a growing challenge set). #assert_all_clean (19 keystones).
import Dregg2.Crypto.AdvCalculus -- the KEYSTONE of "hybrid, not PQ-only": the two combiner security theorems the campaign asserts but never proved. PART A (signatures): SigScheme/Forgery/EufCma abstract model (oracle = queried-message set Q, both hybrid halves see the SAME Q); hybridVerify = verify BOTH; hybrid_euf_cma_if_either (EufCma classical ∨ EufCma pq → EufCma hybrid) via the PROJECTION reductions hybrid_forger_projects_to_{classical,pq} (a hybrid forgery on fresh m* is a PAIR both valid on m* → project each coordinate → a component forgery). ANCHORING (no re-asserted carrier): classical_euf_cma_grounded_in_dl reduces the classical half to SchnorrCurveField.SchnorrDLHard via the Schnorr-EUF→DL forking reduction (a hypothesis, reusing HermineTSUF's PROVED forking); pq_euf_cma_grounded_in_msis reduces the pq half to Lattice.MSISHard THROUGH the proved HermineSelfTargetMSIS.no_forgery_under_msis_selftarget (forked ML-DSA forgery → two SelfTargetMSIS solutions, c≠c'). KEYSTONE hybrid_secure_if_either_floor: hybrid unforgeable if EITHER SchnorrDLHard OR MSISHard holds. PART B (X-Wing KEM): KEM model + hybridKEM (ss=KDF(ss_x‖ss_pq‖transcript)); DualPRF stated EXPLICITLY (unpredictability-preserving keyed on EITHER input, injective proxy à la RandomnessBeacon) and reduced to; hybrid_kem_secret_unpredictable_if_either + hybrid_kem_ind_cca_if_either (IND-CCA if EITHER X25519 or ML-KEM is, under the dual-PRF). Teeth: ONE secure component ⇒ hybrid EufCma (hybrid_secure_via_left/right) vs BOTH broken ⇒ forgeable (hybrid_broken_not_euf) — "either" load-bearing; goodKDF(k1−k2) dual-PRF propagates from either channel vs badKDF(k1) single-keyed FAILS to propagate a secure pq component (badKDF_pq_not_propagated) — dual-PRF load-bearing. #assert_axioms-clean (24 keystones).
import Dregg2.Crypto.MlKemIndCca -- DISCHARGES the ML-KEM component assumption of HybridCombiner.hybrid_kem_ind_cca_if_either (PART B, X-Wing): reduces ML-KEM (Kyber) IND-CCA to Lattice.MLWESearchHard + the explicitly-named QROM idealisation, so the hybrid KEM's PQ floor is now MLWE, not an assumed "ML-KEM is IND-CCA" carrier. PART 1 (IND-CPA lattice PKE = Kyber.CPAPKE): ciphertext_is_masked_mlwe (the ciphertext MINUS the message IS an IsMLWESample — the core lattice fact); key_is_mlwe_sample + pke_key_recovery_reduces_to_mlwe (key recovery → MLWESearchHard, search leg); ind_cpa_reduces_to_mlwe (an IND-CPA distinguisher IS a decisional-MLWE distinguisher on the masking sample, the faithful additive-shift reduction, no probability); message_hidden_by_wide_mask grounds the statistical hiding leg in HermineHiding.signature_hides_secret. PART 2 (FO transform): foEncaps/foDecaps (derandomise r=G(m), K=H(m), re-encryption check + implicit reject); fo_decaps_of_honest (honest ct decapsulates to H(m) via PKE correctness); decaps_oracle_simulable (the CCA decaps answer EQUALS a secret-free simulator foDecapsSim using only public pk,G,H,reject — the oracle leaks nothing beyond IND-CPA); fo_decaps_rejects (malformed ct → implicit reject). PART 3: QROMInjective H (the QROM random-oracle idealisation, stated EXPLICITLY — injective-hash proxy à la RandomnessBeacon, a standard cryptographic assumption NOT a lattice carrier); ml_kem_secret_unpredictable (K=H(m) injective in the coins = Unpredictable = KemIndCca); ml_kem_ind_cca_reduces_to_mlwe; hybrid_kem_ind_cca_grounded_in_mlwe feeds the pq disjunct into hybrid_kem_ind_cca_if_either. Honestly open: the FULL probabilistic QROM FO reduction (advantage/reprogramming bookkeeping) — captured structurally, not proved probabilistically; lattice core bottoms out at MLWESearchHard for real. Teeth over ZMod 5 / a concrete PKE (enc m=2m, dec c=c/2): the masked-MLWE identity + IND-CPA→decisional reduction fire; honest ct decaps=H(m) while a malformed odd ct hits implicit reject; the ML-KEM secret is injective. #assert_axioms-clean.
import Dregg2.Crypto.FoBookkeeping -- CLOSES the QUANTITATIVE half of the FO transform MlKemIndCca captured only STRUCTURALLY: the classical-ROM Fujisaki–Okamoto IND-CCA advantage bookkeeping, in the tree's finite-ℚ probability style (the SAME machinery as HermineTSUF.forking_probability_bound — a ℕ→ℚ weight world + Mathlib inequalities, NO measure theory). ENGINE: Adv win=|win−1/2|; abs_telescope (game-hopping |g 0−g m| ≤ ∑|g k−g(k+1)|, the ℚ analog of HermineHiding.statDist_telescope, induction from the triangle inequality) + abs_telescope_bound; union_bound (∑ᵢpᵢ ≤ |queries|•pmax, =Finset.sum_le_card_nsmul, the ℚ analog of HermineTSUF.oracle_view_within_tv's Q·ε). HEADLINE fo_ind_cca_bound: the four-game FO chain (G₀ real IND-CCA → G₁ decaps oracle→secret-free simulator → G₂ T-transform derandomisation idealised → G₃ IND-CPA / 1/2-guessing), telescoped, gives Adv^IND-CCA(A) ≤ cpaTerm + simFail + corrSpread; fo_ind_cca_bound_of_cpa gives the requested f(Adv^IND-CPA(B)) + (decaps-sim failure) + (correctness/γ-spreadness) shape. t_transform_hop_bound: corrSpread = q_G•(δ + 2^(−γ)) via the union bound; spreadPer γ=2^(−γ). GROUNDING (no named-carrier laundering — only floors already established): cpa_hop_grounded_in_mlwe re-exports MlKemIndCca.ind_cpa_reduces_to_mlwe (cpaTerm's structural content is decisional-MLWE, bottoming at Lattice.MLWESearchHard); SimDiverges + sim_no_divergence_on_honest (the G₀→G₁ bad event NEVER fires on an honest ciphertext, directly MlKemIndCca.decaps_oracle_simulable — simFail carried only by malformed/mis-extracted queries); δ/2^(−γ) are honestly-named PKE parameters, not carriers. Teeth: the telescope is TIGHT on a monotone chain; fo_ind_cca_bound is TIGHT (Adv=1/5=cpaTerm+simFail+corrSpread) so every term is load-bearing (corrSpread/cpaTerm drop breaks the bound); the union bound fires (4·(1/100+2^−10)); on MlKemIndCca.exPKE the honest query provably does NOT diverge (ex_sim_no_divergence_honest) while a mis-extracted candidate DOES. #assert_axioms-clean.
import Dregg2.Crypto.FoQrom -- WIRES the PROVED O2H lemma (OneWayToHiding.o2h_bound) into ML-KEM's FO transform, DISCHARGING the QROMInjective PROXY with the real quantum-adversary reprogramming bound. FO sets K=H(m*) with r=G(m*); an IND-CCA adversary distinguishing K from uniform = distinguishing H from H reprogrammed at m* (O2H set S={m*}), advantage ≤ 2·√(q·Pfind). pfind_le_query_guess: Pfind ≤ q·b with b = the message min-entropy / guessing bound Pguess(m*)=2^(−H∞(m*)) (Finset.sum_le_sum + sum_const, ℝ analog of FoBookkeeping.union_bound). reprog_term_bound chains o2h_bound → |amp_H−amp_{H'}| ≤ 2·√(q·(q·b)). HEADLINE ml_kem_ind_cca_qrom (+ _compose): the full QROM IND-CCA bound = the O2H reprogramming term + FoBookkeeping's three classical hops (simFail+corrSpread+cpaTerm), telescoped over the FO game chain — holds against the q-query QUANTUM adversary (RO = QuantumOracle unitary). foBookkeeping_bound_to_real casts FoBookkeeping's ℚ fo_ind_cca_bound verbatim. DISCHARGES QROMInjective: it is ABSENT from the hypotheses; reprog_advantage_zero_of_no_query exhibits the qualitative injective proxy as the b→0 PERFECT CORNER (zero query-mass on m* ⇒ zero K-distinguishing advantage, from O2H not injectivity). NO named-carrier laundering: b is the honestly-named message-guessing term, classical hops bottom out at MLWESearchHard, reprogramming term is o2h_bound PROVED from Mathlib primitives. Teeth on the O2H toy (Bool×ZMod 2, q=1, S={true}): pfind_le_query_guess fires (Pfind=1≤q·1, tight); the reprogramming term is exactly 2; the full QROM bound composes with FoBookkeeping.ex_fo_bound_fires; min-entropy LOAD-BEARING (b=1/4↦1 < b=1↦2 — low entropy degrades the bound, toy's b pinned at 1 by toy_proj_norm); the zero-query corner discharges the proxy. #assert_all_clean.
import Dregg2.Crypto.CapabilityChain -- the FIRST protocol-level game riding on the HybridCombiner keystone: biscuit/credential ATTENUATION SOUNDNESS. Model (REUSES HybridCombiner's SigScheme/Forgery/EufCma verbatim — no new carrier): a Credential = rootPk + root→leaf list of Block (authority, nextPk, sig); VerifyChain walks root-anchored, each block's sig verifying under the parent's nextPk (root under rootPk) over body(parent-marker, block) AND its authority ≤ parent's (attenuation). SOUNDNESS chain_unforgeable_under_eufcma: if every honest key is EufCma, any VerifyChain-accepting chain rooted at an honest key IS entirely honestly signed (ChainSigned) — the reduction (from_forgery/chain_forgery) walks to the FIRST block not honestly signed, whose parent key is honest (honestDelegation propagation) and whose sig on a never-queried body is a fresh Forgery refuting that key's EufCma. ATTENUATION-ONLY chain_only_attenuates (via verifyFrom_narrows): leaf.authority ≤ root.authority down a valid chain — offline attenuation only SHRINKS (mirrors Authority.Biscuit.biscuit_narrows). ANCHOR chain_unforgeable_under_hybrid_floor: the chain's sigs ARE hybrid, so each honest key's EufCma is DISCHARGED by HybridCombiner.hybrid_secure_if_either_floor from SchnorrDLHard ∨ MSISHard — chain soundness reduces to the DL/lattice floor via the combiner, no re-asserted carrier. Teeth (toy sig=pk+m, Finset ℕ authority): honest chain ACCEPTED + authority strictly shrinks; attacker-signed block REJECTED (forgedChain_rejected); AMPLIFYING block REJECTED despite a valid sig (ampChain_rejected — attenuation-only load-bearing); an accepting chain need NOT be ChainSigned absent unforgeability (eufcma_is_load_bearing). #assert_all_clean (12 keystones).
import Dregg2.Crypto.RevocationSoundness
import Dregg2.Crypto.ConsensusLiveness
import Dregg2.Crypto.TurnSoundness
import Dregg2.Crypto.DreggKemRefinement
import Dregg2.Crypto.Fips204Spec
import Dregg2.Crypto.AdaptiveTSUF
import Dregg2.Crypto.UcSignature -- the LAST protocol game, closing the crypto tree into ONE connected proof: REVOCATION SOUNDNESS (token/src/revocation.rs GAP #5 — you cannot pass a revoked token off as un-revoked). REUSES HybridCombiner's SigScheme/Forgery/EufCma AND HermineHintMLWE's HashCR carrier verbatim — no new carrier. Model: the revocation tree is a CommitReveal Unit (Finset Id) Digest whose H () s is the Merkle root of the revoked set s (HashCR = the root binds its member set, the abstraction of SortedRevocationTree::root's blake3 fold); the client accepts id as un-revoked iff an Absent witness opens the root to a set omitting id (the NonMembershipProof denotation); an AttestedRoot(root,epoch,sig) carries the authority's SigScheme signature over (root‖epoch), verifyAttested = SigScheme.verify under the ENROLLED key. TWO DOORS, both closed by a named carrier: (1) merkle_root_binds + revoked_cannot_prove_absence — under HashCR a valid absence for a revoked id against its own honestly-attested root IS a hash collision (you can't hide a revocation); (2) forged_attestation_is_a_signature_forgery — an accepted-but-unsigned root is a Forgery on the authority key, and wrong_root_for_epoch_is_forgery makes stale-root replay a forgery (epoch signed INSIDE the body). DICHOTOMY forged_non_revocation_breaks_cr_or_forges: a client accepting a revoked id ⇒ ¬HashCR ∨ Forgery (mirrors CapabilityChain.chain_forgery's case split; HonestAttestation = the honestDelegation analogue). HEADLINE revocation_sound_under_floor: with the per-key forking reductions, no revoked id is accepted provided HashCR ∧ (SchnorrDLHard ∨ MSISHard) — EufCma DISCHARGED by HybridCombiner.hybrid_secure_if_either_floor, so the last game bottoms out at the SAME DL/lattice floors as the whole tree. Teeth (a) binding: honest non-revoked id ACCEPTED, revoked id has NO absence (HashCR fires), colliding tree HIDES the revocation (HashCR load-bearing); (b) attestation (toy sig=pk+m): attacker-key REJECTED, accepted-but-unsigned root EXHIBITS the Forgery. #assert_all_clean (19 keystones).
import Dregg2.Crypto.UcSignatureQuant -- Track B step 2: CLOSING UcSignature's two named UC gaps G1/G2 on the ProbCrypto probabilistic substrate. §7.6's ComputationalUC carried G1 (reduction_bound advₑ≤εₛ) and G2 (forge_negl Negl εₛ) as ASSUMED fields; both are now PROVED. §1 winProb_prod_eq_advantage — the BRIDGE: winProb of a forking accept over World×Chal EQUALS HermineTSUF.advantage (so a distinguishing game's probability routes into the forking bound). §2 THE DISTINGUISHER AS A winProb GAME: distBool/forgeBool over a finite outcome space + per-outcome submission; winProb_dist_eq_forge proves winProb(distinguish)=winProb(forgery) — the winProb-level LIFT of distinguishes_iff_forgery (G1 as a genuine real EQUALITY, not an assumed inequality); some_distBool_gives_forgery reuses distinguishes_iff_forgery for the aggregate. §3 ucDistAdv_eq_forgerAdv — G1 CLOSED against the ForkingFamily: the distinguisher's advantage ensemble IS F.forgerAdv (via the bridge). §4 ucForger_negl_of_msis/_of_dl — G2 CLOSED: Negl F.forgerAdv from the QUANTITATIVE floor MSISHardQuantShape/DLHardQuantShape + Negl F.invChal, reusing ProbCrypto.forking_reduces_against_floor (NOT the tree's Boolean MSISHard). §5 computational_uc_realizes — THE PAYOFF: MSISHardQuantShape ⟹ Negl advₑ, via uc_advantage_transfer fed the PROVED G1+G2; ucRealizesWitness FILLS UcSignature.ComputationalUC's two former assumed fields with proof terms. §6 NON-VACUITY full spectrum: zeroFamily realizes (Negl ucDistAdv, FIRES); const25Family has ucDistAdv≡2/5 NOT negligible (BITES) + derived solver breaks the quant floor. So UcSignature's "TRUE-MODULO-(G1,G2)" becomes TRUE on the quantitative substrate; G1/G2 are no longer fields but theorems. #assert_all_clean (14 keystones).
import Dregg2.Crypto.HybridThresholdQuant -- Track B step 3: the HYBRID / THRESHOLD / UC-disjunctive reductions THREADED through the ProbCrypto quantitative substrate (genuine advantage-inequalities, NOT Boolean relabelled). §0 winProb_le_of_imp (winProb monotone in the win predicate — the probability-level lift of an event implication) + negl_of_le (nonneg dominated-by-negligible is negligible). §1 HYBRID COMBINER quantitative: HybridForkingFamily carries accC (DL leg) + accP (MSIS leg) over a SHARED outcome space; hybridForgerAdv = winProb of their CONJUNCTION; the combiner property (hybrid forge ⟹ BOTH-component forge, HybridCombiner.hybrid_forger_projects_to_{classical,pq}) becomes the pointwise accC∧accP⟹accC, lifting to hybridForgerAdv ≤ classicalForgerAdv AND ≤ pqForgerAdv; hybrid_forger_negl_under_floors: Negl hybridForgerAdv under DLHardQuantShape ∨ MSISHardQuantShape (case-split, each leg discharged by ucForger_negl_of_{dl,msis}, dominated). Teeth: ONE secure component ⇒ hybridForgerAdv≡0 (Negl, secureLeftHybrid); BOTH broken ⇒ ≡2/5 (NOT Negl, bothBrokenHybrid) — "either" load-bearing on REAL advantages. §2 THRESHOLD: the ForkingFamily forgerAdv IS the concurrent TS-UF-0 forger advantage (HermineTSUF §ProbForking); threshold_forger_negl_of_msis packages Negl thresholdForgerAdv under MSISHardQuantShape (reusing forking_reduces_against_floor); adaptive_threshold_negl_of_msis carries the adaptive/erasure statistical term as an ADDITIVE Negl summand (negl_add). Fires (zeroFamily base + 2⁻ⁿ erasure); const25 threshold breaks the floor. §3 UC DISJUNCTIVE BUNDLING (closes UcSignatureQuant's single-floor residual): uc_realizes_disjunctive carries TWO forking families Fc (DL) + Fp (MSIS) with advₑ ≤ each, concluding Negl advₑ under DLHardQuantShape ∨ MSISHardQuantShape. Fires (advₑ≡0); const25 advantage NOT negligible (disjunction load-bearing). #assert_all_clean (18 keystones).
import Dregg2.Crypto.ProtocolSoundnessQuant -- Track B step 4 (FINAL): the PROTOCOL soundness consumers migrated off the Boolean ¬∃solver floors onto the ProbCrypto quantitative substrate (ADDITIVE — the Boolean floors + Boolean soundness theorems are UNTOUCHED). ARCHITECTURE: the seven signature-grounded consumers (CapabilityChain/TurnSoundness/DowngradeResistance/LightClientSoundness/RevocationSoundness/ConsensusSafety/BlocklaceSafety) ALL discharge EufCma through the SAME anchor HybridCombiner.hybrid_secure_if_either_floor, so their quantitative siblings collapse to ONE keystone — the quant sibling of that anchor is HybridThresholdQuant.hybrid_forger_negl_under_floors. THREE floor-classes → three anchors. §1 HYBRID (DLHardQuantShape ∨ MSISHardQuantShape): hybridBreakAdv H bad = winProb of the JOINT event (hybrid forge accC∧accP) ∧ (consumer structural predicate bad); hybridBreakAdv_le_hybrid PROVES break ≤ H.hybridForgerAdv via winProb_le_of_imp (the consumer projection "a break IS a hybrid forgery" at the PROBABILITY level, not a carrier); hybrid_consumer_forge_negl: Negl (hybridBreakAdv H bad) under either floor (negl_of_le on hybrid_forger_negl_under_floors). Seven NAMED siblings (turn_authority_unforgeable_quant, chain_unforgeable_quant, downgrade_resistance_quant, lightclient_forge_negl_quant, settlement_finality_quant, no_forged_block_quant, revocation_forge_negl_quant) — genuine specializations, each vs its Boolean twin. §2 DL (DLHardQuantShape): dlBreakAdv F bad = winProb (F.acc ∧ bad) ≤ F.forgerAdv (via winProb_prod_eq_advantage); dl_consumer_forge_negl via ucForger_negl_of_dl. Siblings turnauth_authorization_quant + dualscheme_authorization_quant (pure Schnorr, no hybrid). §3 HashCR (HashCRHardQuantShape): hashcr_consumer_break_negl — break ≤ collision advantage, negligible by domination (collision resistance is not a forking argument); siblings revocation_nonrevocation_quant (Merkle non-membership binding) + lightclient_no_long_range_quant (frame collision). STRUCTURAL (honestly NO floor): chain_only_attenuates/verifyFrom_narrows (attenuation lattice), turn_sound/wrong_transition (CircuitSound boundary), ConsensusLiveness/ViewChange (progress given unforgeable votes), lightclient_agrees/no_fork (quorum intersection), DualScheme tag-committed/no-cross-scheme/modes-dont-cross (definitional). NON-VACUITY: hybridBreak_bothBroken_not_negl + dlBreak_const25_not_negl (both-broken ⇒ 2/5 advantage BREAKS the floor); hybridBreak_secureLeft_negl + dlBreak_zero_negl (secure ⇒ 0); hashcr_floor_load_bearing (const-1 collision solver refutes the floor). Every break advantage is a genuine winProb real that CAN be non-negligible; bounds PROVED not assumed. #assert_all_clean (23 keystones).
import Dregg2.Crypto.FloorBridge -- Seam 2 of crypto-honesty: WELD the QUANTITATIVE floors (ProbCrypto.MSISHardQuantShape/DLHardQuantShape/HashCRHardQuantShape := ∀s, Negl(adv s)) to the tree's BOOLEAN floors (Lattice.MSISHard := ¬∃solver, SchnorrCurveField.SchnorrDLHard := ¬DLSolver, HermineHintMLWE.HashCR := injectivity) so they are ONE foundation, not two. THE BRIDGE ARGUMENT: a Boolean solver is a DETERMINISTIC adversary that WINS ITS GAME WITH CERTAINTY — its win predicate is constantly true, so its winProb is 1 (boolWinAdv := winProb(fun _:Unit=>true), = 1 by ProbCrypto.winProb_top), which is NOT negligible (not_negl_boolWinAdv via not_negl_one). Embed the Boolean solvers as the CANONICAL solver family {z // IsMSISSolution …} with advantage fun _ => boolWinAdv; the quantitative floor forbids advantage 1, so no solver exists = the Boolean floor. THREE BRIDGES (Quant → Boolean, LOAD-BEARING): msisHard_of_msisHardQuant, schnorrDLHard_of_DLHardQuant, hashCR_of_HashCRHardQuant. REVERSE (Boolean → Quant canonical) is VACUOUS — msisHardQuant_of_msisHard holds ONLY because the canonical family is EMPTY when MSISHard; carries no rate content; for an arbitrary advantage family the reverse is FALSE (disclosed, not laundered). CONSUMER MIGRATION TEMPLATE: turnauth_forces_authorization_quant re-derives TurnAuthSignature.turnauth_forces_authorization (Boolean: SchnorrDLHard ⟹ verified ⟹ authorized) as a COROLLARY of the QUANTITATIVE DLHardQuantShape via the DL bridge — the Boolean consumer reused UNCHANGED, only its floor bridged; every _under_floor consumer migrates by this two-line plumb (consumer hext (bridge hquant) hver). NON-VACUITY: dlHardQuant_toy_refuted (on toyCurve DL is easy, Int.toNat is a canonical advantage-1 solver, so the quant DL floor is FALSE — mirrors SchnorrCurveField.toy_dl_not_hard) + msisHardQuant_refutable (a solution refutes the quant floor). No axiom, no def…Hard as hypothesis. #assert_all_clean (9 keystones).
import Dregg2.Crypto.ModelBridge -- Seam 3 (model↔reality): two honestly-named model-vs-reality gaps of the quantitative crypto campaign — one CLOSED, one reduced to a single NAMED measure step. §A HYBRID COMBINER, shared-challenge → INDEPENDENT challenge spaces (CLOSED): HybridThresholdQuant.HybridForkingFamily put accC/accP over a SHARED World×Chal (a common challenge, a modelling assumption). IndepHybridForkingFamily gives each leg its OWN challenge set (ChalC,ChalP) AND prefix world (WorldC,WorldP); the hybrid accepts iff both, over the PRODUCT (WorldC×ChalC)×(WorldP×ChalP). ENGINE winProb_prod_factor: the joint accept prob of a conjunction on DISJOINT coordinates FACTORISES, winProb(f p.1 && g p.2)=winProb f·winProb g (the independent-uniform/product-measure law). So hybridForgerAdv=classicalForgerAdv·pqForgerAdv (EQUALITY, hybridForgerAdv_eq_mul), dominating each marginal (hybridForgerAdv_le_classical/_le_pq, each ≤1); hybrid_forger_negl_under_floors_indep re-proves Negl hybridForgerAdv under DLHardQuantShape∨MSISHardQuantShape with the two challenges INDEPENDENT — the shared-challenge assumption removed. Teeth: one secure ⇒ 0 (secureLeftHybridI); BOTH broken (each 2/5) ⇒ 4/25=(2/5)·(2/5) (bothBrokenHybridI, the independent PRODUCT not 2/5) NOT Negl — "either" load-bearing AND factorisation exhibited numerically. §B FINITE SHADOW ↔ ABSTRACT FORGER (commitment leg CLOSED, one measure step NAMED): HermineTSUF.ProbForger is the fixed-fork-index finite shadow of the abstract Forger:(ℕ→Rq)→… (infinite RO), and the reductions IDENTIFY them. extend reconstructs a full RO from (prefix ω:Fin challengeIdx→Rq, challenge c, tail); commitment_extend_eq PROVES the abstract commitment factors through the prefix ω alone (indep of c and tail) via commitment_preChallenge — the coordinate-restriction of the commitment, UNCONDITIONAL (the two reruns share it). abstractShadow BUILDS a genuine ProbForger from an abstract Forger + frozen tail with acc_sound DISCHARGED (an accepting (ω,c) is a real IsSelfTargetMSISSolution on the shared prefix-commitment, challenge c). TailIndependent NAMES the exact residual: acceptance's independence from the RO answers ABOVE challengeIdx (the abstract response may read them) = marginalising the uniform tail; abstractShadow_{acc,advantage,forkProb}_tailIndep PROVE that GIVEN it the finite shadow is tail-canonical (faithful, no residual dependence on unmodelled coords). Teeth: exAbstractForger (reads only the challenge) IS TailIndependent; exTailForger (reads index 1>challengeIdx=0) is NOT (accepts under tail≡1, rejects under tail≡0) — load-bearing. EXACT REMAINING STEP: establishing TailIndependent in general needs an infinite-product RO measure this finite counting model lacks (ℕ→Rq is not a Fintype); named, not sorry-ed, not silently assumed. #assert_all_clean (19 keystones).
import Dregg2.Crypto.DowngradeResistance -- the POST-QUANTUM NEGOTIATION game riding the HybridCombiner keystone: DOWNGRADE RESISTANCE (an active MITM cannot force two honest parties onto a WEAKER classical-only cipher suite than both support — exactly the PQ threat model). REUSES HybridCombiner's SigScheme/Forgery/EufCma verbatim — no new carrier. Model: Suite carries a strength preorder (Classical < Hybrid); IsStrongestCommon a b s = s is in both supported sets and ≥ every common member (unique by antisymmetry, strongest_common_unique). The handshake is AUTHENTICATED: a SignedTranscript records both supported sets + the negotiated suite, each party signs the body (aSup‖bSup‖neg) and VERIFIES the peer's signature before accepting (AAccepts/BAccepts, each also pins its OWN supported set intact). HonestSigner (the HonestAttestation analogue) = an honest party signs ONLY a body recording its true set and the strongest-common negotiated suite. REDUCTION downgrade_forces_forgery: if A accepts neg STRICTLY WEAKER than the true strongest-common best, then had B signed it HonestSigner+uniqueness force neg=best (contra neg<best), so the accepted transcript B never signed IS a SigScheme.Forgery on B's key. HEADLINE downgrade_resistant: under B's EufCma no MITM makes BOTH accept a suite < best. ANCHOR downgrade_resistant_under_floor ("you can't strip the PQ"): with the per-key forking reductions, EufCma is DISCHARGED by HybridCombiner.hybrid_secure_if_either_floor from SchnorrDLHard ∨ MSISHard — so even a QUANTUM adversary that broke ed25519 (SchnorrDLHard falls) still faces MSISHard on the ML-DSA half keeping the transcript unforgeable; the downgrade fails unless BOTH floors fall. Teeth (Suite=Bool, classical=false<hybrid=true, toy sig=(pk,m)): honest handshake AGREES on the strongest common (hybrid) + A accepts; the authenticated downgrade to classical EXHIBITS the Forgery via the full downgrade_forces_forgery on concrete data (provable HonestSigner); and — LOAD-BEARING — an UNAUTHENTICATED negotiation (accept = "I support it", no sig) IS freely downgradable (both accept classical though strongest is hybrid), so the transcript authentication is exactly what buys downgrade resistance. #assert_all_clean (11 keystones).
import Dregg2.Crypto.ConsensusSafety -- the FIRST step UP from crypto primitives to protocol SOUNDNESS: CONSENSUS SAFETY as a crypto game — two conflicting blocks cannot both be finalized. The crypto sibling of Consensus.Safety (which carries the honesty law abstractly over a BFTModel): here the honesty law is DERIVED from (a) honest-supermajority quorum intersection + (b) the unforgeability of the HYBRID-SIGNED finalization votes. Model: a committee of n members (≤ f Byzantine, n > 3f), a FinalizationVote is a HybridCombiner.SigScheme signature by a member over voteMsg(height‖block); a block is Finalized at a height when a quorum of q = n − f distinct members hold valid votes. §1 two_quorums_share_honest — pure Finset counting (|A∩B| ≥ |A|+|B|−n via card_union_add_card_inter, then |(A∩B)\byz| ≥ n−3f ≥ 1): any two quorums share ≥1 HONEST member. §2 no_two_conflicting_finalized — two conflicting blocks both finalized ⇒ an honest member holds valid votes for BOTH; either it never cast one (a fresh valid sig on an un-queried body = a HybridCombiner.Forgery refuting its EufCma) or it double-voted (forbidden by the honest-voting rule, stated explicitly). §3 consensus_safe_under_floor — the votes ARE the ed25519∧ML-DSA hybrid, so each honest member's EufCma is DISCHARGED by HybridCombiner.hybrid_secure_if_either_floor: safety holds under (n>3f) ∧ (SchnorrDLHard ∨ MSISHard) — QUANTUM-SAFE FINALITY, the protocol-level payoff of the whole hybrid campaign; no named-carrier laundering (only the two floors + the threshold + the honest-voting rule are irreducible, the forking reductions are hypotheses). Teeth: n=4,f=1,q=3 two quorums share ≥2≥1 honest member; threshold_is_load_bearing (at n=3f the honest overlap is EMPTY — safety fails without n>3f); a double vote by a member that signed one block IS a Forgery; and without EufCma/the honest rule BOTH conflicting blocks finalize (tooth_eufcma_is_load_bearing). #assert_all_clean (8 keystones).
import Dregg2.Crypto.RandomnessBeacon -- abstract HashRand-style PQ randomness-beacon security (unbiasability + unpredictability → HashCR, reusing HermineHintMLWE commit-reveal). BLS-beacon replacement scaffolding.
import Dregg2.Crypto.IdentityCommitment -- the HYBRID IDENTITY-COMMITMENT BINDING: Id = H("dregg-hybrid-id-v1", ed25519_pk ‖ len(ml_dsa_pk) ‖ ml_dsa_pk) cryptographically BINDS both keys, so the id re-basing's verify_committed_ml_dsa gate is sound (types 7e3ea27ec; cell-crypto/captp/wire). commit cr frame ed ml = cr.H () (frame ed ml) over the injective length-framed preimage; verify_committed = (commit == id). FLOOR: id_commitment_binds (under HashCR, an id determines its (ed25519,ml_dsa) pair UNIQUELY) + attacker_key_not_committed (THE TEETH: honest ed + attacker's OWN ml_dsa is REJECTED unless a hash collision). REDUCTION: distinct_verifying_pairs_break_hashcr / commit_collision_is_hash_collision route a colliding enrollment to a raw H-collision — reuses HermineHintMLWE.HashCR verbatim, NO fresh carrier. #guards: teeth fire (honest passes, attacker ml + ed-swap rejected), HashCR-VIOLATING toy where two pairs share one id (binding's hypothesis load-bearing), and length-framing faithfulness (naive ++ collides, len-prefix injective). #assert_axioms-clean.
import Dregg2.Crypto.DreggPqRefinement
import Dregg2.Crypto.Fips204Verify -- the DOWN direction ("the code IS the proof", a beachhead): connects the DEPLOYED dregg-pq Rust API (dregg-pq/src/mldsa.rs + hybrid_kem.rs) to the abstract SigScheme/KEM models the security proofs are about, NAMING the code↔model gap precisely. DreggPqApi = the observable ML-DSA surface (keygen=from_ed25519_seed∘public_bytes deterministic-from-seed, sign(ctx,msg), verify=ml_dsa_verify fail-closed Bool); dreggPqSigScheme lifts it to a concrete HybridCombiner.SigScheme with the ctx riding INSIDE the message (Ctx×Msg) so domain separation is structural. THE HONEST BOUNDARY (named, not laundered): Fips204Correct/Fips203Correct — the fips204/ml-kem FIPS-204/203 primitive correctness (sign→verify / encaps→decaps round-trip) is a clearly-labeled axiom-free HYPOTHESIS the conclusion is CONDITIONED on (NOT a def…Hard carrier), exactly as the abstract games assume ML-DSA is a sig scheme / ML-KEM a KEM; a verified-fips204 effort would discharge it. dregg_pq_correct DERIVES Correct (round-trip) from Fips204Correct; badApi_not_correct proves it LOAD-BEARING (no floor ⇒ no round-trip). INHERITANCE: dregg_pq_is_eufcma_under_msis specializes HybridCombiner.pq_euf_cma_grounded_in_msis (the deployed ML-DSA inherits EufCma→MSIS); dregg_pq_hybrid_kem_ind_cca_if_either specializes hybrid_kem_ind_cca_if_either (the concat-KDF combiner = the modeled X-Wing DualPRF ⇒ IND-CCA-if-either). REFINEMENT: Refines api S (keygen/sign agree, Prop-verify ⟺ Bool-verify=true) + dregg_pq_refines_sigscheme (the model faithfully abstracts the contract, by construction); badModel_not_refines gives teeth (a ctx-blind model does NOT refine). #guards over a decidable ℕ toy: honest sig verifies, forgery+wrong-ctx rejected (non-vacuous SigScheme + ctx separation), the FIPS boundaries are falsifiable. CLOSES the dregg-pq GLUE (from-seed derivation, ctx separation, verify fail-closed, hybrid combiner) into the proved games; STILL TRUSTED = FIPS 204/203 primitive correctness. #assert_all_clean (15 keystones).
import Dregg2.Crypto.Fips204FullDim -- `Fips204Correct` AT FULL ML-DSA-65 DIMENSION — the deployed `dregg_pq_correct` is no longer fed the n=1 caricature. `DreggPqRefinement.dregg_pq_correct` takes `hfips : Fips204Correct api` as its trusted primitive floor; that floor WAS discharged only for `Fips204Verify.extractedApi` (`realParams`: n=1, A=LinearMap.id, constant challenge c=1, the single fixed secret (5,1,3) with fixed mask y=40 — deployed LITERALS but a SCALAR caricature of the ML-DSA-65 module). THIS FILE assembles the api at the REAL dimension and proves the floor there, UNIVERSALLY: `fullParams` instantiates the generic `Fips204Spec.fips204_correct` over `Fips204CorrectReal`'s real ring `R_q = ℤ_q[X]/(X²⁵⁶+1)` (q=8380417, root²⁵⁶=−1, power-basis dim EXACTLY 256) and modules `M = R_q^5`, `N = R_q^6` ((k,ℓ)=(6,5)), with the componentwise `realRoundingK` (round-to-nearest at γ₂=261888, α=523776, β=196, over all 6×256 coefficients, ℤ_q-wrap included) — and quantifies over EVERYTHING the caricature fixed: ARBITRARY R_q-linear A (the ExpandA matrix, not `id`), ARBITRARY Fiat–Shamir `hash`, ARBITRARY SampleInBall sampler `chal` (no c=1), ARBITRARY secret. `fullDimApi_fips204 : Fips204Correct (fullDimApi A hash chal)` is the ∀-theorem; `fullDimApi_correct` feeds it straight into `dregg_pq_correct`, so the deployed `Correct` conclusion now rides the real ML-DSA-65 module. KERNEL-CLEAN — no `native_decide`, no `Lean.ofReduceBool`/`trustCompiler` (the SYMBOLIC route was taken precisely so the full-dimension floor does not trade the toy residual for a compiler residual; `MlDsaVerifyReal.verify_accepts_real`'s native_decide KAT is NOT used). THE ONE NAMED RESIDUAL: the seed type is `HonestKey` — an accepted-iteration certificate bundling the four gates FIPS 204 Alg. 7's rejection loop evaluates (Power2Round consistency + ‖c·t₀‖≤γ₂ + ‖c·s₂‖≤β + LowBits(A·y) gapped + ‖y+c·s₁‖<γ₁−β), i.e. exactly the Dilithium REJECTION-TERMINATION fact `Fips204Spec` already names (probabilistic, ≈4.25 iterations; NOT a hardness carrier). `fips204Correct_of_fullDim` exports the bridge for any byte-level api (the remaining obligation is the Poly↔R_q codec bridge, not the ML-DSA algebra). NON-VACUITY both ways: `honestKey` = a NON-DEGENERATE inhabitant (s₁,s₂,t₀ ≠ 0 — `s1Rt_ne_zero`/`t0Rt_ne_zero` — gate values at the DEPLOYED magnitudes ‖c·s₂‖∞=η=4≤β, ‖c·t₀‖∞=2^{d−1}=4096≤γ₂, gapped commitment) whose challenge is c = root, a genuine NON-SCALAR ring element (`rt_not_scalar`: root ≠ a·1 for every a ∈ ℤ_q) and a UNIT (`rt_mul_rtInv`, from root²⁵⁶=−1) — the witness does NOT smuggle back the c=1 caricature; `fullDim_honest_verifies` (the honest full-dim signature VERIFIES) + `fullDim_rejects_out_of_norm` (for EVERY A/hash/sampler/key/message, an out-of-range z is REJECTED — the verify gate is real at full dimension) + `lowGap_zero_false` (the rejection gate is a real filter, so the certificate carries information). NEXT LANE (named): the negacyclic SHIFT lemma (repr(root·x) = the ±-shifted coordinates) unlocks the τ=49 SampleInBall norm bound ‖c·s‖∞ ≤ τ‖s‖∞, which turns THREE of the four gates into theorems and collapses the residual to `commit_gap` alone.
import Dregg2.Crypto.WgpuBfvNttSpec -- portable split-u32 BFV arithmetic contract: exact low-64 serialization/multiply, q<2^62 no-overflow invariant, per-row RNS negacyclic schoolbook reference, host NTT shape/root preflight, and the all-row/all-coefficient `NttRefines` boundary; proves CPU-reference algebra, not hardware execution.
import Dregg2.Crypto.WgpuTorusNegacyclicMacSpec -- portable TFHE arithmetic rung: exact flattened batched coefficient equation in (Z/2^64Z)[X]/(X^N+1), append/chunk law, exact output shape, and backend-metadata irrelevance. Explicitly not gadget decomposition/external product/CMUX/PBS.
import Dregg2.Circuit.TfhePbsRefinement -- Lean authority for the exact PBS-shaped boundary after blind rotation: degree-zero GLWE sample extraction pins the +constant/−reversed-tail signs; standard subtractive LWE key switching fixes `(k·N)×levels×(out+1)` KSK geometry; the composed WGPU theorem requires an explicit external implementation-equality premise and does NOT claim the WGSL is verified.
import Dregg2.Crypto.HermineDischarge
import Dregg2.Crypto.HermineConcrete
import Dregg2.Crypto.HermineLossiness
import Dregg2.Crypto.HermineInvertibility
import Dregg2.Crypto.InvertibilityCRT
import Dregg2.Crypto.InvertibilityNorm    -- CLOSES the n≥256 invertibility number-theory (the norm-bound the CRT skeleton reduced to): factor_nonzero_of_norm_lt + isUnit_of_norm_lt_ideal_min (framework, general R); the n=4 case DONE (Quart q = ℤ_q[X]/(X⁴+1), crtEquiv : Quart ≃+* Quad×Quad constructed algebraically for any q,r with r²=-1); and the number-theory HEART discharged at q=5,d=2: minNorm_factor (every nonzero multiple of X²∓2 in R₅ has ‖·‖∞≥2, decided over all 625 elts) -> low_norm_isUnit (‖f‖∞<2 ⟹ unit) + challenge_diff_isUnit_of_low_norm₅. norm_bound_sharp: 2+X² has norm exactly 2 and is NOT a unit (the bound is sharp, teeth). Residual (arbitrary-q general L-S) noted in-code. #assert_axioms-clean.     -- GENERAL-n challenge-diff invertibility, the CRT skeleton (n=2 field trick does NOT scale — X^n+1 SPLITS for real n). product_field_isUnit_iff (unit in a product of fields ⟺ nonzero in each component); isUnit_of_crt_nonzero (via the CRT iso φ: R ≃+* ∏Kᵢ — R_q's splitting into residue fields); challenge_diff_isUnit_general (invertibility ⟸ c-c' nonzero in each CRT factor). Reduces the n≥256 Lyubashevsky-Seiler lemma to its number-theoretic heart (low-∞-norm ⟹ nonzero mod each degree-d factor), scoped honestly as the interface hypothesis. crt_example fires + has teeth. #assert_axioms-clean.
import Dregg2.Crypto.InvertibilityNormGen  -- strengthens (1): n=2 invertibility for ALL q≡3 mod 4 (challenge_diff_isUnit_inert) + a q=13-splits contrast with teeth. #assert_axioms-clean.
import Dregg2.Crypto.InvertibilityD2  -- CONCRETE d=2 case (past d=1): n=4, q=13≡5 mod 8, X⁴+1=(X²−5)(X²+5) two irreducible quadratics. minNorm_quad_factor: both quadratic ideals have min centered ∞-norm EXACTLY 3 (minNorm_exactly_three; via kernel-elts-are-multiples + decide over the 169-elt ideal). low_norm_isUnit_d2_q13 / challenge_diff_isUnit_d2_q13 (‖·‖<3 ⇒ unit); δ=1−X² inverse exhibited; sharpD2_not_unit (3+2X²=2(X²−5), norm exactly 3, not a unit — threshold exact). General parametric d≥2 still needs the LS complex-resultant bound (no Mathlib infra). #assert_axioms-clean.
import Dregg2.Crypto.InvertibilityHadamard  -- the GENERAL-n invertibility lemma (any n=2^k, any prime q): norm_lt_isUnit — v≠0 with ‖v‖₂ⁿ<q is a UNIT in ℤ_q[X]/(Xⁿ+1), via q∤N(v)⟺unit + a self-proved HADAMARD det bound (Mathlib lacks it: det²=det(MᴴM)=∏λ≤(tr/n)ⁿ by Hermitian-eigenvalues+AM-GM) + N(v)≠0 from Xⁿ+1 irreducibility. challenge_diff_isUnit_general_n + lossiness_discharges_nonzero_general_n weld it into HermineDischarge at general n. Fires at n=8/q=17 (7e9-elt ring, beyond decide) + sharp8_not_unit teeth. Threshold q^(1/n) (weaker than tight LS q^(d/n)); GENERAL in n,q. #assert_axioms-clean.
import Dregg2.Crypto.InvertibilityTight  -- the TIGHT Lyubashevsky-Seiler threshold q^(d/n) (upgrades the Hadamard q^(1/n)): norm_lt_isUnit_tight — v≠0, every prime above q has residue degree ≥ d, ‖v‖₂ⁿ<q^d ⟹ unit. Multiplicity q_pow_residueDeg_dvd_det: q^{f(P)}∣N(v), proved WITHOUT Ideal.absNorm's Dedekind requirement — routed via Smith normal form (natAbs_det_basis_change) + index-divisibility + card_eq_pow_finrank. Residue-degree dischargers (no-root ⟹ d≥2; Bézout/Frobenius ⟹ general d). challenge_diff_isUnit_tight + lossiness_discharges_nonzero_tight weld it in. demo2 (n=2/q=3 inert) beats Hadamard. Item (1) CLOSED. #assert_axioms-clean.
import Dregg2.Crypto.HybridQuorum
import Dregg2.Crypto.HermineHybrid        -- HERMINE as the hybrid COMPACT PQ half (item 5): instantiates the scheme-generic HybridQuorum at Hermine's verifier. hermine_hybrid_survives_classical_break (ed25519 fully broken by Shor -> still unforgeable while the Hermine PQ half holds, which IS HermineMSIS.no_forgery_under_msis -> quantum-safety reduces to MSIS); hermine_hybrid_unforgeable_of_either. The Hermine PQ half is ONE cert (w,z), committee-INDEPENDENT (vs the ML-DSA hybrid's t×3.3KB concatenation) — the compact upgrade. Security shape proved; live-consensus swap awaits crypto-hermine deployment-grade maturity. #assert_axioms-clean.
import Dregg2.Crypto.Smudging
import Dregg2.Crypto.HermineHiding
import Dregg2.Crypto.RenyiHiding
import Dregg2.Crypto.GaussianRenyi         -- CLOSES the discrete-Gaussian Rényi bound (instantiates RenyiHiding to real Gaussian noise): gaussian_ratio_exponent (the pointwise exponent identity, Real.exp algebra); gaussian_renyiDiv2_eq — the divergence is EXACTLY exp(δ²/σ²) (not just ≤), with the FULL infinite-sum theta normalization (both D_σ and its δ-shift proven genuine distributions, tsum=1); gaussian_probability_preservation (P(E)² ≤ exp(δ²/σ²)·Q(E) — the transfer for the REAL noise) + forgery-transfer; gaussian_renyi2_pair (two coords = exp(2δ²/σ²), grounding σ≳√n·δ). Concrete σ=2,δ=1 -> exp(1/4). Residual (non-integer shift, general order-a) noted in-code. #assert_axioms-clean.          -- the RÉNYI-DIVERGENCE key-hiding bound (the tighter variant of Smudging's uniform+TV; what Raccoon actually uses). Order-2 collision divergence over ℚ: renyiDiv2; renyiDiv2_self (=1 for a distribution); one_le_renyiDiv2 (R₂≥1, Cauchy-Schwarz); renyiDiv2_mul (MULTIPLICATIVITY over independent coordinates — the structural lemma that makes an n-coord signature's divergence a product); renyi_probability_preservation (P(E)² ≤ R₂·Q(E) — THE theorem: hardness under the SIMULATOR Q transfers to the REAL scheme P) + renyi_forgery_transfer. Concrete R₂=5/4>1, product 25/16. General-a + discrete-Gaussian = the further tightening. #assert_axioms-clean.        -- KEY-HIDING: signing does NOT leak the secret — the last leg to a USABLE PQ signature. A Hermine sig z=y+c·s (mask y~unif of a wide S) has distribution unif(S.image(·+c·s)); a simulator with NO secret outputs unif S, and by the smudging lemma the real signature is within statistical distance B/M of it. signature_hides_secret (the sig is B/M-SIMULATABLE without the secret = HVZK/key-hiding); statDist_triangle (statDist is a metric); concrete_signature_hides (M=10 -> ≤1/10, decide); key_hiding_two_secrets (two secrets' sigs within 2B/M = un-linkability). Negligible once M≫B. #assert_axioms-clean.            -- NOISE-FLOODING (smudging), the load-bearing core of Hermine KEY-HIDING: wide noise hides a bounded shift. statDist (total-variation distance over exact ℚ) + unif; abs_diff_unif; statDist_unif_eq (SD(Uniform S, Uniform T) = |S\T|/M for equal-size sets); smudge_bound (SD ≤ B/M when the shift moves ≤ B of the M-wide support — the flooding bound); statDist_unif_image (the translation form y↦y+Δ); smudge_example (concrete: shift-by-1 over width-10 → SD = 1/10). Uniform+TV variant (Raccoon uses Gaussian+Rényi for tighter params, noted). #assert_axioms-clean.         -- HYBRID quorum-cert security (classical FROST + PQ ML-DSA): the disjunction that makes real quantum-safety deployable NOW with MATURE crypto. hybridVerify Vc Vp := Vc ∧ Vp (both halves); hybrid_forgery_breaks_both (a forgery breaks BOTH); hybrid_unforgeable_of_either (unforgeable if EITHER half is — no need to trust both); hybrid_survives_classical_break (THE headline: even with the classical verifier rubber-stamping everything = ed25519 fully broken by Shor, the hybrid still cannot be forged while the PQ half holds). Non-vacuity is load-bearing: both_broken_is_forgeable shows deleting the PQ hypothesis makes the forgery verify. The deployable counterpart to the Hermine compact-PQ future. #assert_axioms-clean.   -- CHALLENGE-DIFFERENCE INVERTIBILITY, PROVEN (not flagged), n=2: builds R_q=ℤ_q[X]/(X²+1) as GaussMod q (pairs over ZMod q, Gaussian mul), full CommRing proven. For prime q≡3 mod 4, -1 is a non-square (ZMod.exists_sq_eq_neg_one_iff) so the norm a²+b² is anisotropic -> the exhibited inverse ginv verifies (mul_ginv_cancel) -> Field (GaussMod q) -> challenge_diff_isUnit: c≠c' → IsUnit(c-c') for ALL distinct pairs. Concrete decided witness q=7, δ=(1,1), δ⁻¹=(4,3). Feeds lossiness_discharges_nonzero (hinv from c≠c' alone). n≥256 Lyubashevsky-Seiler = the larger effort. #assert_axioms-clean.      -- MLWE LOSSINESS, PROVEN (not flagged): lossiness_of_card_lt — a compressing A (a Finset S of β-short vectors with Fintype.card N < S.card) has two DISTINCT short preimages of the same value, via the pigeonhole (Finset.exists_ne_map_eq_of_card_lt_of_maps_to). Concrete instance Aq:(Fin 2→ℤ)→ZMod 3 with the card inequality PROVED (concrete_card_lt: 3<5, decide) — so it FIRES: explicit_collision ((1,0)≠(0,-1), both short, Aq equal). concrete_end_to_end: proven-lossiness → nonzero → IsMSISSolution, with NO assumed lossiness anywhere. #assert_axioms-clean.      -- the abstract PQ reduction made CONCRETE: a real ShortNorm instance over integer vectors (Fin d → ℤ, L1 norm, triangle proven) + a decide-checked WORKED EXAMPLE where the ACTUAL abstract reduction fires — A=[1 2], key s=(1,0), lossy twin s'=(3,-1) (both map to t=(1)), forged z=(3,-1)/z'=(0,0) -> the reduction produces u=(2,-1): concrete_msis_witness : IsMSISSolution Amat 9 uvec (nonzero, ‖u‖₁=3, A·u=0). concrete_discharged_reduction runs it through the DISCHARGED theorem (no u≠0 hyp). Non-vacuity on real numbers. #assert_axioms-clean.     -- DISCHARGES the u≠0 leg of the MSIS reduction: replaces the bare hypothesis with a derivation from two more-fundamental honest parameter facts — MLWE LOSSINESS (t=A·s has two distinct short preimages s≠s') + CHALLENGE-DIFFERENCE INVERTIBILITY (c-c' a unit in R_q). lossiness_discharges_nonzero: at least one candidate (z-z')-(c-c')·s{,'} is nonzero (else (c-c')·(s-s')=0, cancel the unit -> s=s'). forked_forgery_yields_msis_solution_discharged: a forgery yields an MSIS solution with NO u≠0 hypothesis. Only MLWE/MSIS HARDNESS (the Lattice floor) remains irreducible. #assert_axioms-clean.          -- THE PQ REDUCTION, complete to the line: a forked Hermine FORGERY yields an MSIS SOLUTION. forked_forgery_yields_msis_solution — u = (z-z') - (c-c')·s is a genuine Module-SIS solution for A: SHORT (the norm-bound leg via Lattice.nrm_sub_le — no longer a black box), in the KERNEL (unconditional: extractor A(z-z')=(c-c')·t + t=A·s), NONZERO (the explicit MLWE-hides-the-secret hypothesis, named not papered). no_forgery_under_msis — MSISHard ⟹ no forgery. So Hermine unforgeability rests on MLWE (u≠0) + MSIS (hardness) and NOTHING else. #assert_axioms-clean.             -- THE IRREDUCIBLE LINE for post-quantum security: MLWE + MSIS modeled as the ASSUMPTIONS they are (predicates: no adversary finds a short kernel vector / recovers the short secret), NEVER proved — the shared floor of ALL PQ lattice crypto (FIPS ML-DSA included). ShortNorm (integer coeff ∞-seminorm) + nrm_sub_le/IsShort.sub (the norm-bound leg that makes an extracted z-z' short — previously a black box); IsMSISSolution/MSISHard; IsMLWESample/MLWESearchHard (the key-hiding assumption). zero_not_msis_solution (non-degeneracy). Everything ABOVE this line reduces to it (HermineMSIS). #assert_axioms-clean.     -- PQ FORKING EXTRACTOR (Hermine, the lattice analog of SchnorrExtractor): two forked Raccoon transcripts sharing w with c≠c' yield the lattice relation. hermine_special_soundness_extracts_relation (A(z-z') = (c-c')·t, over CommRing, no invertibility); hermine_special_soundness_extracts_preimage/_key (over Field: t = (c-c')⁻¹·A(z-z') = A(extractPreimage) — the explicit MSIS/MLWE preimage of t). Non-vacuity: extractor_recovers_secret (extractPreimage on honest responses = the true s) + extractor_is_correct. So a forked Hermine forger extracts a preimage of t — the lattice break. Carriers (out of scope): forking probability + the SHORTNESS/norm of z-z'. #assert_axioms-clean.
import Dregg2.Crypto.ShamirPrivacy
import Dregg2.Crypto.HermineDkg          -- JOINT-FELDMAN DKG (Pedersen, NO trusted dealer) — closes the gap where crypto-hermine/src/dkg.rs ran ahead of the metatheory. Each member i shares its own sᵢ via a degree-(t−1) poly fᵢ over R_q^ℓ, broadcasts Feldman commitments Cᵢ,ₖ=A·aᵢ,ₖ; final share xⱼ=Σᵢ fᵢ(j), group key t=A·s=Σᵢ Cᵢ,₀ (s=Σᵢ sᵢ never materialized). dkg_group_key_eq (broadcasts assemble to A·s by map_sum); dkg_shares_reconstruct (a t-subset Lagrange-reconstructs s: reconstruction of the summed shares = summed reconstructions, via evalPoly_sum linearity + HermineThreshold's per-poly Lagrange-at-zero as hypothesis); dkg_share_verify_sound/off_poly (a share passing Feldman A·share=Σₖ jᵏ·Cᵢ,ₖ IS the committed evaluation mod ker A — a cheating dealer's off-poly share is CAUGHT, matching Rust verify_dkg_share). SECRECY = dkg_secrecy_reduces: the COMPOSITION (two distinct group secrets reproduce the whole t-minority view) over the TRUE FLOOR, NO named carriers (de-laundered 07-09) — dkg_collision_of_lossiness (A·s admits a distinct alt secret, from the PROVED pigeonhole HermineLossiness.lossiness_of_card_lt) + dkg_shamir_leg (from the PROVED ShamirPrivacy.shamir_t_privacy); the COMPUTATIONAL hiding of the SHORT secret is the honest MLWE floor, named not re-asserted (MLWESearchHard is FALSE at an honest key so it can't be consumed for hiding). secrecy_nonvacuous discharges BOTH on a compressing ℚ²→ℚ instance. #guards: group key assembles + t=2 shares reconstruct s=10 (ZMod 11) + Feldman pass/fail. #assert_axioms-clean.
import Dregg2.Crypto.ThresholdReduction
import Dregg2.Crypto.SchnorrExtractor
import Dregg2.Crypto.SchnorrEufCma        -- CLOSES the classical half of HybridCombiner.hybrid_secure_if_either_floor to SchnorrDLHard FOR REAL: proves the content classical_euf_cma_grounded_in_dl currently TAKES as a bare forking hypothesis. Models the Schnorr sig over the group (schnorrScheme: sign x m=(k·g, k+H(R,m)·x), verify s·g=R+c·pk, reusing Frost.SchnorrVerifies) with schnorr_correct; the SIGNING ORACLE is HVZK/secret-free (schnorr_sim_verifies: programmed challenge c, nonce R:=s·g−c·pk verifies with NO secret — the classical mirror of HermineTSUF.oracle_answer_secret_free). CRUX schnorr_forking_extracts_dl (= SchnorrExtractor.schnorr_special_soundness_extracts_dl, the EXACT classical mirror of HermineSelfTargetMSIS.selftarget_extract_nonzero): two accepting transcripts on a common R with c≠c' give pk=((s−s')/(c−c'))·g, the DL. FORKING PRODUCES the pair (mirror of HermineTSUF.fork_produces_msis): SchnorrForger + rewind + fork_preserves_commitment (shared R DERIVED not assumed) + schnorr_fork_produces_dl; schnorr_fork_probability_bound REUSES HermineTSUF.forking_probability_bound for the rewind-acceptance event. REDUCTION schnorr_euf_cma_reduces_to_dl: SchnorrDLHardF g (the field-scalar DL floor, honest char-generic form of SchnorrCurveField.SchnorrDLHard) ⟹ no Schnorr forger; schnorr_euf_cma_grounded_in_combiner_dl specializes the combiner's anchor to schnorrScheme, hybrid_classical_leg_grounded_in_dl is the discharged leg. Teeth over ℚ (g=1, DL easy): extractor recovers x=3 vs c=c' recovers nothing (c≠c' load-bearing), forking→DL pipeline fires, a forger family EXISTS (ex_dl_not_hard). #assert_all_clean (16 keystones).
import Dregg2.Crypto.ForkingDischarge          -- RETIRES the deterministic forking-extractor hypothesis (the #1 reducible in the crypto pathway). The keystone HybridCombiner.hybrid_secure_if_either_floor + pq_euf_cma_grounded_in_msis + DreggPqRefinement.dregg_pq_is_eufcma_under_msis (the DEPLOYED refinement) + TurnAuthSignature.ForkingExtractor all TOOK `fork : Forgery S pk Q -> DLSolver / two SelfTargetMSIS solutions` as an UN-DISCHARGED hypothesis, cited to "the PROVED forking machinery of HermineTSUF" but never wired. THE OBSTRUCTION, PROVED (no_forked_pair_of_hits_le_one / forkProb_eq_zero_of_hits_le_one): a bare exists-witness Forgery is a SINGLE transcript (hits <= 1), so forkProb = 0 and NO two-transcript event exists at ANY floor -- the deterministic shape is un-DISCHARGEABLE, not merely un-discharged. THE BRIDGE: ProbGameForger S pk Q A t beta Omega -- a probabilistic EUF-CMA adversary in HermineTSUF's finite forking model, carrying BOTH acc_wins (accepting = a genuine HybridCombiner.Forgery, the GAME leg) and acc_sound (accepting = an IsSelfTargetMSISSolution on the prefix-fixed commitment, the FORKING leg); toProbForger IS the map into HermineTSUF.ProbForger. THE DISCHARGE: forkPair_of_advantage PROVES the msisFork hypothesis' exact conclusion from advantage > 1/|Rq| (via forkProb_ge_advantage -> forkProb_pos_of_advantage -> exists_forked_pair_of_forkProb_pos + acc_sound); pq_advantage_bounded_under_msis is the keystone in the honest advantage-bounded form (under MSISHard every game forger's advantage <= 1/|Rq|, the challenge-guessing floor). THE ONE RESIDUAL: ForgeryRealizable (a MODELLING bridge -- the game's bare exists-forgery is produced by an actual adversary with a prefix world and a rewindable challenge; assumes NOTHING cryptographic), from which fork_of_realizable DERIVES the retired hypothesis. CLASSICAL LEG: ProbSchnorrForger DERIVES SchnorrEufCma.SchnorrForgeryFamily's ASSUMED accepts_rewind field from the advantage (prob_schnorr_yields_dlog), so no_prob_schnorr_family_under_dl rests on SchnorrDLHardF alone. KEYSTONES RESTATED: hybrid_secure_if_either_floor_discharged (NO dlFork, NO msisFork) + dregg_pq_is_eufcma_under_msis_discharged (the DEPLOYED dregg-pq ML-DSA). QUANT: GameForkingFamily welds the game predicate to ProbCrypto.ForkingFamily; game_forger_negl_under_msis_quant gives Negl(forger advantage) under MSISHardQuantShape. TEETH: the obstruction fires on a single-transcript adversary; the derived fork FIRES on a ZMod-5 adversary of advantage 2/5 > 1/5; keystone_msis_floor_load_bearing proves the discharged keystone NON-VACUOUS and its floor LOAD-BEARING (brokenToy is forgeable + realizable, so the keystone FORCES not-MSISHard on the toy -- and the toy is indeed easy). #assert_all_clean (27 keystones).
import Dregg2.Crypto.ForkingDischargeConsumers -- THREADS the forking discharge through the protocol layer: TWELVE consumers restated with BOTH forking hypotheses RETIRED. Every protocol game is (inner fork-FREE theorem taking heuf : EufCma) + (outer _under_floor wrapper that produces it from hybrid_secure_if_either_floor and therefore drags dlFork/msisFork through), so the fork enters at exactly ONE point per consumer; swapping that call for ForkingDischarge.hybrid_secure_if_either_floor_discharged retires it everywhere. Discharged: chain_unforgeable_under_hybrid_floor (CapabilityChain), turn_sound_under_floor (TurnSoundness), downgrade_resistant_under_floor (DowngradeResistance), revocation_sound_under_floor (RevocationSoundness), consensus_safe_under_floor (ConsensusSafety -- QUANTUM-SAFE FINALITY), no_forged_block_under_floor (BlocklaceSafety), ake_authentication_grounded (WireAke), hybrid_sig_uc_realizes + hybrid_multi_session_uc_realizes (UcSignature), accepting_forged_history_breaks_floor + lightclient_no_fork_under_floor (LightClientSoundness). PLUS the PQ-ONLY keystone hybrid_secure_under_msis_alone: hybrid_secure_if_either_floor demanded dlFork even on the MSISHard branch where it is NEVER used, so the deployed post-quantum statement now stands with ZERO classical hypotheses (no curve, no discrete log, no classical fork); hybrid_secure_under_dl_alone is the mirror. What each wrapper still assumes: the two named FLOORS (SchnorrDLHardF or MSISHard), the two REALIZABILITY modelling bridges, and the game's own protocol hypotheses -- the forking extraction appears in NO hypothesis. #assert_all_clean (13 keystones).
import Dregg2.Crypto.CryptoFloorTeeth       -- FALSIFIABILITY TEETH for the crypto hardness floors + the PROPER (adversary-indexed) floor. The Boolean floors are EXISTENCE-REFUTATIONS (Lattice.MSISHard := ¬∃solution, MLWESearchHard := ¬∃(s,e), SchnorrDLHard := ¬DLSolver), degenerate at real params — so every theorem conditioned on them is discharged by a vacuous hypothesis, invisible to #assert_axioms. §1 not_msisHard_of_short_ball: MSISHard is FALSE at a compressing A — the β₀-short ball outnumbers the codomain, two short vectors collide (pigeonhole), their difference is a short nonzero kernel vector (nrm_sub_le) = a genuine IsMSISSolution; fires on [id|1] over ZMod 5 (25>5), witness (4,1) exhibited via selftarget_extract_nonzero. §2 not_mlweSearchHard_of_sample: MLWE is FALSE for any genuine public key (the short (s,e) IS the secret key, exists by construction). §3 schnorrDLHard_of_smul_collision: SchnorrDLHard is TRIVIALLY TRUE at FINITE params (DLSolver demands solve(sk·G)=sk for ALL sk:ℕ; on a finite group sk↦sk·G is non-injective so DLSolver is UNSATISFIABLE) — opposite vacuity flavor, satisfied by anything finite; the infinite toy (SchnorrCurveField.toy_dl_not_hard) is the refutable pole. §4 msisHardQuant_solverAdv_iff_msisHard: FloorBridge.msisSolverAdv indexes the quant floor by SOLUTIONS, so MSISHardQuantShape(msisSolverAdv A β) ↔ MSISHard A β — the SAME bug in probabilistic costume, false at compressing (msisHardQuant_solverAdv_augmented_id_false). §5 THE PROPER FLOOR: keep the MSISHardQuantShape SHAPE (∀s, Negl(adv s)) but re-instantiate adv over a GENUINE resource-bounded adversary family with a λ-DECAYING advantage ENSEMBLE (a single-instance ∃-adversary collapses to ∃-solution via hardcoding, so it must be λ-indexed) — guessAdv=1/2^λ SATISFIES (negl_two_pow), constant-1 REFUTES (not_negl_one): non-vacuous AND non-trivial. §6 dregg_pq_game_forger_negl_under_comp_floor: the DEPLOYED dreggPqSigScheme keystone re-grounded via ForkingDischarge.game_forger_negl_under_msis_quant on the adversary-indexed floor (NOT the vacuous msisSolverAdv); deployed_boolean_floor_refuted shows the OLD Boolean floor of pq_advantage_bounded_under_msis is FALSE at compressing; FIRES (zeroFamily) + floor LOAD-BEARING (const25). #assert_all_clean (16 keystones), no sorry.
import Dregg2.Crypto.FloorGames -- ⚑ THE *HardQuant REPAIR (07-16): the five content-free floors RESTATED as the standard cryptographic games, with the problem IN the statement. §1 ONE SCHEMA, MANY PROBLEMS: a Game carries a λ-indexed finite Inst space, an Ans space and a WIN RELATION; Adversary G := run : ∀ l, Inst l → Ans l; gameAdv = ProbCrypto.winProb of the win event; Hard G Eff := ∀ A, Eff A → Negl (gameAdv G A). The five floors are this schema at FIVE DIFFERENT games — MSISHardQuant F Eff (wins := Lattice.IsMSISSolution, msisGame_wins_iff by Iff.rfl), MLWEHardQuant (wins := recovering the actual secret; mlweFamily_isSample PROVES every instance is a Lattice.IsMLWESample), DLHardQuant (wins := x·G = the sampled challenge point), HashCRHardQuant (wins := a genuine collision, reusing HashFloorHonesty.KeyedHashFamily/CollisionFinder), DecisionMLWEHardQuant (the LWE-vs-uniform gap, real world PINNED to A·s+e, ONE distinguisher applied to both worlds). So the sweep's defect is gone at the TYPE level: a HashCRHardQuant proof is Hard at a different Game and no longer elaborates in an MSISHardQuant slot (HardQuantVacuity §6 pins it with fail_if_success + the positive pole). §2 THE COLLAPSE — hard_top_iff_solvableFrac_negl: Hard G ⊤ ↔ Negl (solvableFrac G). At the UNRESTRICTED adversary class a game floor IS the probabilistic EXISTENCE floor — the Classical.choice adversary (choiceAdv, a perfectly good Adversary: nothing in that type bounds resources) wins on exactly the solvable instances. So the sweep's Horn A is NOT an artifact of msisSolverAdv or of the old shape: it is a theorem about EVERY unrestricted-adversary formulation, this one included (msisHardQuant_top_false_of_compressing, mlweHardQuant_top_false, dlHardQuant_top_false — the last two unconditionally). ⚑ AND IT REFUTES THE REPAIR THE SWEEP ITSELF PRESCRIBED: HashFloorHonesty.CollisionResistant F WAS HashCRHardQuant F ⊤ under a name that hid the ⊤ (proved by collisionResistant_iff_hashCRHardQuant_top; ⚑ 07-28 BOTH are DELETED — the def, and the iff whose only job was to see through the name — and every carrier is restated at (fun _ => True) written out), so hashCRHardQuant_top_false_of_compressing proves HashFloorHonesty's 'proper keyed computational floor' — named in VACUITY-SWEEP.md as 'the correct pattern already in the tree' — is itself FALSE at deployed parameters, by the same pigeonhole, one Classical.choice later (mod2Family_not_hashCRHardQuant_top fires it on the tree's own compressing example). Copying that pattern would have been the FOURTH costume. §8 THE ESCAPE, AND THE RESIDUAL NAMED: since §2 is an ↔, no restatement of the win relation escapes — only Eff does, and the tree HAS NO COST MODEL. Eff is carried as a PARAMETER with both poles PROVED (⊤ ⇒ false at deployed params; ⊥ ⇒ vacuous, hard_bot_vacuous); solvableIsAFiniteSearch shows Eff := Computable cannot help (every answer space is a Fintype, so brute force is total computable — what disqualifies it is COST, which Mathlib cannot state over an arbitrary carrier). What the repair buys, exactly: problem in the statement, five distinct floors, consumers must EXHIBIT a reduction, and the dilemma localized to ONE named object. #assert_all_clean (18 keystones).
import Dregg2.Crypto.HardQuantVacuity -- ⚠ VACUITY SWEEP (07-16): the *HardQuant floor FAMILY carries NO problem content. §1 the_five_floors_are_one_prop: MSISHardQuantShape/MLWEHardQuantShape/DLHardQuantShape/HashCRHardQuantShape/DecisionMLWEHardQuantShape are ONE Prop (∀ s, Negl (adv s)) — five names, Iff.rfl four ways; nothing in any of them mentions a lattice, a curve, a hash or IsMSISSolution, and DecisionMLWEHardQuant's own doc says the 'INTENDED' adv is a DecisionFamily.adv (intended, never enforced — the CoCurvilinearity docstring-constraint defect one level up). §2 the consumers are HYPOTHESIS APPLICATION: every re-grounded keystone has shape (adv)(s)(hfloor : <X>HardQuant adv) : Negl (adv s), which is `hfloor s`, a P → P instantiation — the_vrf_keystone_accepts_the_hash_floor CALLS the real VrfRegrounded.lattice_vrf_uniqueness_advantage_bound passing a HashCRHardQuantShape proof into the slot its signature declares MSISHardQuantShape, and it TYPECHECKS. §3 THE DILEMMA (both horns from the tree's own lemmas): horn A — tie adv to MSIS via the only MSIS-indexed adv in the tree (FloorBridge.msisSolverAdv) and msisHardQuant_solverAdv_iff_msisHard collapses the floor to the Boolean MSISHard, FALSE at compressing params, so every consumer is VACUOUS; horn B — leave adv untied (guessAdv = 1/2^λ, the tree's OWN non-vacuity witness, mentions no A/β/IsMSISSolution) and MSISHardQuantShape holds SIMULTANEOUSLY with ¬MSISHard (augmented id 1) 0 — the floor is satisfied in a world where its named problem is refuted. No third instantiation exists. §4 the METHOD tooth: SheepCountingHardQuant (name chosen to mean nothing) passes CryptoFloorTeeth.proper_floor_is_genuine's 'satisfiable AND refutable' test verbatim AND is definitionally MSISHardQuantShape (Iff.rfl) — so that test measures the SHAPE of a predicate over an arbitrary adv, never whether a floor is ABOUT its named problem. Affected consumers (statement-identical): VrfRegrounded.lattice_vrf_uniqueness_advantage_bound/_with_guessing_bound, ThreadAdvantageBound.forger_advantage_bound_under_msis/_with_challenge_bound/decision_distinguisher_advantage_bound/lossy_id_advantage_bound. NOT claimed: no downstream theorem is FALSE — they are all true, for reasons unrelated to their names. Repair (named, not applied): index adv by a resource-bounded adversary against the actual problem RELATION, so the floor is neither the Boolean floor (horn A) nor problem-free (horn B). #assert_axioms-clean, sorry-free.
import Dregg2.Crypto.RomOracle -- ⚑ THE COST-MODEL ESCAPE, part 1/3 (07-16): the SYNTAX of a random-oracle adversary — one that can only learn the oracle BY QUERYING IT. OracleComp D R A is a decision tree (pure a | query d k) that branches ONLY on answers it asked for; QueryBounded Q M is an INDUCTIVE, SYNTACTIC bound on the calls along every path (it quantifies over all answers, so it depends on no oracle). eval/queried run and instrument it. ⚑ THE DETERMINATION THEOREM eval_congr_of_agree_on_queried: two oracles agreeing on M.queried H are INDISTINGUISHABLE to M — same path, same output. That converts the syntactic bound into a constraint on BEHAVIOUR, and it is exactly what FloorGames.choiceAdv (which reads the whole instance) does not obey. Teeth: ofList exhibits real members (query a list, return a function of the answers, ofList_eval); queryBounded_zero_pure inhabits the class; queryBounded_zero_const proves the budget is a GENUINE restriction (a 0-query computation's output is constant in the oracle). #assert_all_clean (13 keystones).
import Dregg2.Crypto.RomCounting -- ⚑ THE COST-MODEL ESCAPE, part 2/3 (07-16): LAZY SAMPLING AS PURE FINITE COUNTING — no measure theory. A uniform random oracle IS a uniform element of the finite type D → R, so conditioning is a Finset: cyl S σ = the oracles agreeing with the partial assignment σ on S, condProb C win = the fraction of C that wins (cyl_empty/condProb_cyl_empty tie it back to ProbCrypto.winProb, so this composes with the existing probability core). The workhorses: cyl_filter_eq (slicing a cylinder at a FRESH d ∉ S gives the cylinder over insert d S), cyl_insert_card_eq (the |R| slices are EQUINUMEROUS, by the bijection H ↦ Function.update H d r'), cyl_card_insert_mul, condProb_split (⚑ THE LAW OF TOTAL PROBABILITY over a fresh coordinate), and condProb_fresh_eq: ⚑ A FRESH POINT HITS A FIXED TARGET WITH PROBABILITY EXACTLY 1/|R| — what the adversary has not queried, it does not know. That single lemma is the whole information-theoretic content of the ROM bound in part 3. No [Nonempty R] needed anywhere: every fresh-point lemma takes σ : D → R and a point of D, so σ d already inhabits R. #assert_all_clean (16 keystones).
import Dregg2.Crypto.RomQueryFloor -- ⚑⚑⚑ THE COST-MODEL ESCAPE, part 3/3 — A FLOOR THAT IS PROVED, NOT ASSUMED (07-16). FloorGames §8 named the residual: Eff has no content because the tree has no cost model, and hard_top_iff_solvableFrac_negl proves NOTHING BUT Eff can escape. ⚑ THE ESCAPE IS NOT A COMPLEXITY CLASS — IT IS THE RANDOM ORACLE. romCollisionGame F: the instance IS a uniformly sampled oracle H : D l → R l, the win relation IS a genuine collision. RomEff F Q (the Eff): the adversaries that FACTOR THROUGH a QueryBounded (Q l) tree — a predicate on the SAME Adversary type Hard quantifies over, so this is the tree's existing floor schema at a class with real content. §3 ⚑ birthday_cond/birthday_bound PROVE, by induction on the query tree over RomCounting's counting lemmas, the classic UNCONDITIONAL bound: a Q-query adversary finds a collision with probability ≤ (Q²+1)/|R|. Nothing is assumed under it — no MSIS, no MLWE, no 'hash is collision resistant'; the invariant is that what the adversary has LEARNED stays collision-free, and it pays 1 whenever that breaks. §6 romCollision_hard: at |R l| = 2^l and a polynomially-bounded budget the floor is TRUE (Negl from negl_two_pow — the digest space outruns the budget). §7 ⚑ BOTH ESCAPES PROVED, not asserted: NOT ⊤ — choiceAdv_not_romEff proves Classical.choice's adversary is EXCLUDED (it would have to know a collision it never asked for), romEff_not_iff_solvableFrac_negl proves the COLLAPSE ↔ FAILS for RomEff, romEff_ne_top; NOT ⊥ — twoPointAdv_in_romEff exhibits a member that genuinely uses its queries and twoPointAdv_gameAdv_eq computes its advantage EXACTLY 1/|R l| > 0. §8 binaryRomFamily (λ-bit digest, 2× domain): binaryRom_top_false (the UNRESTRICTED floor at the SAME game is FALSE, by the SAME pigeonhole that refutes HashFloorHonesty.CollisionResistant) vs binaryRom_hard_linear_budget (the query-bounded floor is TRUE) — same game, same win relation, two Eff's, opposite verdicts, which is what it means for Eff to do real work. ⚠ SCOPE, NOT HIDDEN: the budget is a QUERY count, not a running time — a Q-query adversary may do unbounded work BETWEEN queries (that is SOUND for hash-based bounds, and is why they need no computational assumption), but it restricts a lattice adversary NOT AT ALL, since MSIS/MLWE adversaries make no oracle calls. The general PPT Eff remains open and remains a parameter. Contrast ConcreteSecurity.StepBound.PPT, already in-tree: PolyBoundedNat on a bare steps : ℕ → ℕ attached to NO adversary, constraining NO behaviour — a name for a number sequence, unusable as an Eff, and used as one nowhere. #assert_all_clean (25 keystones).
import Dregg2.Crypto.RomQueryLog -- ⚑ THE QUERY-LOG SUBSTRATE (07-17, idea A2) — the one interface a STRAIGHT-LINE EXTRACTOR needs on top of RomOracle, imitating VCVio's QueryLog/withLogging in SHAPE only (mathlib-only, no VCVio dep). OracleComp.log M H = the list of (d, H d) pairs M produces under H — every queried point PAIRED with its answer, in order; OracleComp.evalLog M H = the one-pass logging run returning (result, log). Agreement with the un-instrumented substrate: evalLog_fst_eq_eval (the log run does not change the output), evalLog_snd_eq_log, bundled as evalLog_eq : M.evalLog H = (M.eval H, M.log H). ⚑ THE EXTRACTION SUBSTRATE, three facts a query-log extractor consumes: evalLog_snd_map_fst_eq_queried — the log's DOMAIN PROJECTION IS queried (the log records the very points eval_congr_of_agree_on_queried quantifies over, so log_length_eq_queried_length + QueryBounded.log_length_le make a budget on the read set and a budget on queried THE SAME bound); evalLog_fst_eq_eval; and mem_evalLog_answer — the READ-BACK, (d, r) ∈ (M.evalLog H).2 → H d = r, so the extractor recovers H's answer at any logged point from the log alone (the log records H on the queried set and records nothing false). Tooth: ofList_log computes the canonical member's log EXACTLY as ds.map (fun d => (d, H d)) — the trace is not vacuously empty. ADDITIVE: touches NO existing soundness statement; B1 (re-basing FriLdtExtractV3 over this model) is the consumer, not this file. #assert_all_clean (14 keystones).
import Dregg2.Crypto.KeyedRomFloor -- ⚑⚑⚑ THE KEYED, QUERY-COUNTED COLLISION FLOOR, and the DEATH of the counterexample (07-23). Exec.SystemRootsBindingReduction.sysRoots_floor_polyTime_false_babyBear PROVES the deployed sponge floor is FALSE at Eff := CostAdversary.IsPolyTime — the ArkLib KZG disease: IsPolyTime prices ANSWER SIZE (isPolyTime_of_polySize_answers), so a Classical.choice .pure-leaf answering a short collision is in the class and wins with prob 1. This module gives the FIXED floor as the keyed presentation of RomQueryFloor: keyedRomGame F = romCollisionGame over the tag-separated domain Key × Input (domain separation), KeyedRomEff F Q = RomEff there. keyedRom_hard: the floor is PROVED (birthday bound). ⚑ THE COUNTEREXAMPLE DIES TWO WAYS. (a) keyedChoiceAdv_excluded — the full-oracle Classical.choice answerer (the shortCollAdv analogue) is OUTSIDE KeyedRomEff (choiceAdv_not_romEff): it would have to know a collision it never queried. (b) fixedPairAdv_negl — the SHARPER mirror: the EXACT shape that refutes the IsPolyTime floor, a .pure leaf answering a FIXED short collision, IS admitted by KeyedRomEff (a 0-query tree) but over the SAMPLED oracle a fixed pair collides with prob only 1/|R| (fixedPairAdv_gameAdv_eq), NEGLIGIBLE — so the adversary that wins prob 1 against the FIXED sponge wins only negligibly against the SAMPLED oracle; sampling the key after the adversary is what collapses the advantage. Both poles at the fixed model: keyedRom_top_false (¬⊤, compressing) + twoPointKeyed_in_keyedRomEff/_gameAdv_pos (¬⊥, positive advantage). deployedKeyed_verdict: all four facts at one concrete keyed family + budget. #assert_all_clean (14 keystones).
import Dregg2.Crypto.RomBindingReduction -- ⚑⚑ THE DEPLOYED COMMITMENT BINDING, RE-GROUNDED on the keyed query-counted floor (07-23). carrier_binds_from_polyTime / stateCommit_binds_from_polyTime discharge efficiency at Eff := IsPolyTime, whose floor is FALSE; and that floor CANNOT be repaired over the SAME game — in hashGame (spongeFamily D) the hash is a FIXED KNOWN function, only the tag is sampled, and against a fixed public function even a QUERY-BOUNDED adversary brute-forces a collision, so no query-counted Eff makes it true. The floor MUST move to a SAMPLED oracle (the ROM of the sponge), where keyedRom_hard is a THEOREM. This module is the re-grounded shape: the forger is an ORACLE PROGRAM (RomCarrierComp, Q-query-bounded = RomCarrierEff, learning the sponge only by querying it); OracleComp.mapOut post-maps the tree so carrierFinder_in_romEff PRESERVES the query bound (the honest successor to the poly_time answer-size discharge); romCarrier_binds closes the binding from the PROVED floor with no false hypothesis; romCarrier_choiceForger_excluded is the counterexample dying at the binding layer (a non-negligible forger is outside the class). idRomCarrier witnesses the interface inhabited. ⚑ WHAT A1 STILL NEEDS: re-pointing each deployed _binds_from_polyTime is mechanical — restate its raw-function forger (Adversary (carrierBreakGame …) / (stateCommitBreakGame …)) as a RomCarrierComp oracle program and compose the extractor with mapOut; the reduction bodies transfer unchanged, and no new CR proof is created (the birthday floor supplies it). The chained STATE_COMMIT (wireCommitR8) additionally needs wireCommitR8 modeled as an oracle computation. #assert_all_clean (9 keystones).
import Dregg2.Crypto.RomChainedReduction -- ⚑⚑ THE CHAINED COMMITMENT AS AN ORACLE COMPUTATION (07-23) — the one genuinely non-mechanical repoint piece: wireCommitR8 is a CHAINED absorption, so its forger equivocates a CHAIN of queries and the collision sits at an INTERIOR step that depends on the oracle's answers — mapOut cannot extract it. §0 OracleComp.bind (budgets ADD: bind_queryBounded). §1 chainComp — one query node per absorption, eval = the fixed-function fold chainEval, budget = block count; Market.WideCommitBoundary.wireCommitComp instantiates it and proves eval permW = wireCommitR8 ON THE DEPLOYED OBJECT (62 queries at the 184-limb payload, proved). §2 RomChainedCarrier (encHd/encStep injectivity = LAYOUT facts) + romChainedGame. §3 the extractor is an ORACLE PROGRAM that re-walks both chains PAYING 2·len+2 queries (chainCollComp + breakFinderComp, correctness breakFinderComp_eval_collides); chainedFinder_in_romEff prices the composed finder at Q+2·len+2. §4 romChained_binds — NO floor hypothesis, rests on keyedRom_hard (birthday, PROVED); romChained_choiceForger_excluded kills the Classical.choice answerer at the chained layer. §5 class inhabited (fixed 0-query member, DEFANGED not excluded) + game winnable at the constant oracle. Deployed landing: Market.WideCommitBoundary §P (stateCommit_floor_polyTime_false_babyBear REFUTES the old IsPolyTime floor at wideFamily; stateCommit_binds_from_polyTime DELETED as vacuous) + §Q (the oracle model of the deployed chain) + §S (stateCommitRom_binds/_choiceForger_excluded/_verdict at the deployed 4-head/61×3-limb schedule — the λ-growing digest is a LABELLED modelling step, hw is unsatisfiable at the fixed 31-bit BabyBear felt). #assert_all_clean (21 keystones).
import Dregg2.Crypto.RomCarrierSites -- ⚑⚑ THE PER-SITE INSTANTIATION KIT for the keyed-ROM binding (07-23): what every re-pointed _binds_from_polyTime site consumes. flatFamily puts any finite deployed message shape under the ideal λ-bit digest so the floor's hw : card (R l) = 2^l obligation is discharged ONCE (flatFamily_card_R) — the biggest per-site trap (card BabyBear = 2^l holds at NO l) is closed at the constructor; taggedCarrier builds the RomCarrier from a site's structural embedding with the domain-separation tag in the CONTEXT (per-site content = the embedding + payload-injectivity, the same two facts ofFlatEncoding took); OracleComp.bindComp is sequential composition of oracle programs with ADDITIVE query accounting (bindComp_queryBounded), what a chained extractor needs to RE-QUERY inner digests; chained_rom_binds is the union bound over two carriers of ONE family — the ROM successor of chained_binding_advantage_bound with the floor PROVED, not parametric. Non-vacuity generic: constTriple (the 0-query shortCollAdv-shape answerer) is IN the class (constTriple_in_eff), wins with POSITIVE probability (constTriple_gameAdv_pos via winProb_pos_of_witness), and is DEFANGED not excluded (constTriple_binds — negligible against the sampled oracle where it won with probability 1 against the fixed sponge). ⚑ The ROM idealisation stays a labelled MODELLING step (module header) — a theorem about the model, never about the fixed deployed sponge. #assert_all_clean (11 keystones).
import Dregg2.Crypto.RomMerkleOpening -- ⚑⚑ THE MERKLE-PATH OPENING AS A KEYED-ROM OBJECT (07-24) — the missing shape between RomCarrier (one query) and RomChainedCarrier (payload-owned blocks): the path recompute is a CHAIN whose block schedule (index bits + siblings) is SHARED CONTEXT and whose starting accumulator IS the payload (the opened leaf). §0 nodeMsg + LEFT-injectivity (a layout fact — the accumulator occupies a fixed coordinate, the transpose of nodeAt_ne). §1 merkleRomFamily = flatFamily over ordered node pairs Fin(2^l) × Fin(2^l), keyed by the site's own tag space. §2 merkleOpenForgery/merkleOpenRomGame: two leaves, ONE shared (bit,sibling) path of pinned depth d, one recomputed chainEval root, at the SAMPLED oracle — FloorRegroundedConsumers.merkleOpenBreakGame's win relation transposed to the oracle model. §3 walkComp: the extractor RE-WALKS both chains PAYING 2 queries per level (walkComp_queryBounded), correctness UNCONDITIONAL (walkComp_eval_collides — two distinct accumulators whose folds agree merge at a genuine collision). §4 merkleOpenRom_binds — NO floor hypothesis, budget Q+2·d PAID in the floor's own currency, closed from keyedRom_hard (the birthday bound, PROVED); what every IsPolyTime-discharged merkleOpen/oodCommitmentOpening bound becomes. §5 the counterexample dies: non-negligible forger excluded; constOpenAdv (the 0-query fixed-answer refuter shape) ADMITTED, wins at the constant oracle (positive advantage at depth ≥ 1), DEFANGED (negligible); canary: the floor at ANOTHER finder does not close the bound. ⚡ The ROM idealisation stays a labelled MODELLING step (header): ideal Fin(2^l) digest, deployed felt ~31-bit — the felt-width wound said out loud. #assert_all_clean (16 keystones).
import Dregg2.Crypto.RomHashedLeafOpening -- ⚑⚑ THE HASHED-LEAF MERKLE OPENING AS A KEYED-ROM OBJECT (07-24) — the shape the three deployed native-8-felt trees (DeployedCapTree/DeployedHeapTree/DeployedFieldsTree) need and neither RomCarrier nor RomMerkleOpening covers: the opened LEAF is PRE-HASHED (H (t, inl block)) before the shared-path fold, so the equivocation's collision sits EITHER at the leaf absorb or at an interior node, decided by the sampled oracle. ONE family per tree (hlFam: truncated leaf blocks ⊕ ordered ideal-digest node pairs, domain-separated by constructor = the deployed chip's arity separation), so the leaf carrier (hlLeaf_binds_rom), the node carrier (hlNode_binds_rom), the pure-path walk (hlPathForgery/hlPathRom_binds — the recomposeUp8 shape, walkComp re-descent 2·d PAID) and the hashed-leaf opening (hlOpenForgery/hlOpenRom_binds — the capOpen8/GENTIAN shape: extractor queries BOTH leaf points, equal answers ARE the leaf-block collision, distinct answers seed the walk; budget Q+2·d+2 PAID) all play over ONE sampled oracle and close from keyedRom_hard (birthday bound, PROVED) — no bare disjunction, no refuted floor, no cost model. hlOpenRootForgery/hlOpenRootRom_binds = the published-root (MembersAt8-functional) form via a root-dropping mapOut. Teeth: const answerer ADMITTED+wins at the constant oracle+DEFANGED; non-negligible forger EXCLUDED; floor-at-another-finder canary. ⚡ The ROM idealisation is the labelled modelling step (header): a whole 8-felt child digest is ONE ideal Fin(2^l) value — the felt-width wound said out loud. #assert_all_clean (23 keystones).
import Dregg2.Crypto.KeyedRomHidingFloor -- ⚑⚑ THE KEYED-ROM HIDING FLOOR, PROVED (07-23) — hiding's twin of RomQueryFloor, by the same lazy-sampling counting. §1 condProb_pin_untouched (⚑ THE REPROGRAMMING LEMMA: pinning the oracle at an UNQUERIED point moves nothing — the identical-until-bad hop the collision bound never needed) + condProb_pin_diff_le. §2-§4 the two-world hiding game (HidingDistinguisher fixes msg0/msg1 up front; challenger samples H AND the blinding r AFTER; challenge = H(m_b, r)) with hidingAdv a genuine distAdv-shaped DIFFERENCE, and hidingAdv_le PROVED information-theoretic: every Q-query keyed adversary's advantage ≤ 2Q/|B|. Poles: topAdv_hidingAdv (unrestricted full-tape adversary wins with EXACTLY 1−1/|R| — the ⊤ floor is FALSE), oneQueryAdv_hidingAdv (class inhabited, wins EXACTLY (1−1/|R|)/|B| > 0), zeroQuery_hidingAdv_eq_zero (the hardcoder is ADMITTED and DEFANGED — fixedPairAdv_negl's story on the hiding side). §9 spanHiding_regrounded: at the deployed-SHAPE family (blindedSpanRomFamily, λ-bit blinding felt) the keyed floor HOLDS while Lane B's WidePreimageFloor(IsPolyTime) — the floor the old conditional hiding rested on — is REFUTED (re-exported). ⚑ Residual as shipped: the family MIRRORS the deployed shape in prose only — the weld to the real BlindedSpanCommitment is the NEXT import. #assert_all_clean (31 keystones).
import Dregg2.Crypto.Poseidon2RomHidingInstantiation -- ⚑⚑ THE DEPLOYED BlindedSpanCommitment WELDED to the keyed-ROM hiding floor through ONE NAMED, PER-ADVERSARY ROM-instantiation assumption (07-23) — the honest ceiling of the hiding floor, addressed. The old gap was TWO gaps in one prose sentence: (1) the asymptotic-shape gap (blindedSpanRomFamily's Fin (2^l) blinding equals the deployed felt count at NO l — mirrorFamily_card_never_deployed pins babyBearP odd) — CLOSED OUTRIGHT: hidingAdv_le is applied AT the exact deployed cardinalities (blinding literally Fin 2013265921, 8-felt spans/digests); (2) the ROM-instantiation gap proper — NAMED: RomFaithfulHiding H₀ A (fixedHidingAdv H₀ A ≤ hidingAdv A, "the fixed hash serves THIS adversary no better than a sampled oracle") and Poseidon2RandomOracleInstantiation D A at the deployed object, a def/hypothesis reduced-to, NEVER an axiom. deployedSpongeOracle_spec pins the weld rfl-tight (the game's oracle IS wideHash(tagCode ‖ span8 ‖ C_T ‖ BLINDING ‖ 0) decoded; encode/decode round-trip + injectivity proved). ⚑⚑ deployed_blindedSpan_hiding_of_romInstantiation: under the named assumption, every Q-query adversary's two-world advantage vs the DEPLOYED commitment ≤ 2Q/2013265921 (≤ 2Q/2^30) — CONCRETE, and it says the felt-width wound out loud: the one-felt blinding is a ~31-bit hiding budget. ⚑ BOTH POLES OF THE ASSUMPTION: satisfiable (constAdv), and the ∀-adversary form REFUTED IN-FILE (poseidon2RomInstantiation_not_universal — a leak-sponge commitment + a 0-QUERY table-baking adversary: fixed advantage 1, ROM advantage 0; a fixed public hash is available at adversary-design time — the finitary CGH shadow), so the assumption is per-adversary BY THEOREM, not by taste. ⚑ AUDIT ANSWER: collision-resistance names NO such assumption in Lean — its tower carries the same residual as RomCarrierSites' PROSE caveat only; RomFaithfulHiding is the residual's FIRST named form, generic so the collision face can adopt the discipline (it cannot literally reuse the predicate: at a fixed sponge the collision game has no residual randomness at all). #assert_all_clean (20 keystones).
import Dregg2.Crypto.Poseidon2RomInstantiation -- ⚑⚑⚑ THE ONE CENTRAL Poseidon2-is-a-keyed-RO assumption (07-23) — the ROM tower's scatter (hiding named in Lean, collision the SAME idealization as PROSE at asymptotic Fin(2^l) width) centralized into ONE named per-experiment predicate at the EXACT deployed object+cardinalities. §1 KeyedRomFaithful H₀ w (generic: fixed-oracle two-world advantage ≤ sampled-oracle advantage, for any observable w : Bool → oracle → aux → Bool — hiding = aux-is-the-blinding instance, collision = one-world/Unit-aux instance; the shared OBSERVABLE shape is what unifies the two faces the hiding lane said could not literally share a predicate). §2 Poseidon2IsKeyedRandomOracle D w := KeyedRomFaithful (deployedSpongeOracle D) w — blinding literally Fin 2013265921, digests 2013265921^8 (card_Digest), no asymptotic mirror. §3 HIDING FROM IT: the bridge is DEFINITIONAL (fixedWorldAdv/romWorldAdv at hidingObservable ARE fixedHidingAdv/hidingAdv, rfl) so the central assumption is EQUIVALENT to Poseidon2RandomOracleInstantiation (poseidon2KeyedRO_iff_hidingInstantiation — re-expression, ONE floor not two) and deployed hiding ≤ 2Q/2013265921 follows. §4 BINDING FROM IT, through the UNTOUCHED birthday_bound: fixed-oracle advantage = the deployed collision INDICATOR, ROM advantage = winProb(collWin M), so a Q-query forger's indicator ≤ (Q²+1)/2013265921^8 — and being 0-or-1, under the birthday bar the forger exhibits NO collision (= no equivocation of the deployed blinded-span commitment) at all. §5 BOTH POLES ON BOTH FACES: satisfiable (constAdv hiding + diagonal forger collision), and the ∀-form REFUTED on the hiding face (transported table-baker) AND the collision face (0-query fixed pair: fixed indicator 1 vs sampled probability exactly 1/2013265921^8). HONEST: NAMES+CENTRALIZES the idealization, does NOT discharge it — Poseidon2-is-a-keyed-RO stays the one named floor; the asymptotic keyed-ROM tower (RomCarrierSites sites the FRI closure consumes) is UNCHANGED beside it, its per-site prose caveats' re-pointing at KeyedRomFaithful = surfaced follow-up. #assert_all_clean (20 keystones).
import Dregg2.Crypto.AdversaryUnification -- ⚑⚑⚑ THE A1 UNIFICATION (07-23): ONE adversary notion, ONE cost semantics — the QUERY BUDGET — with the two classical worlds PROVED one world. THE THEOREM: romEff_exhaustive_eq_top / adversary_romEff_exhaustive — EVERY raw FloorGames.Adversary of an oracle game (Classical.choice included) factors through an exhaustive-budget query tree (tabulateComp = ofList + RomQueryDial.reconstruct, generic in the answer type), so RomEff F (fun l => |D l|) = ⊤ as a predicate EQUALITY and the scale is TOTAL (adversary_romEff_some_budget). Where RomQueryDial embedded ONE adversary (bruteComp, the paying twin) at the unkeyed game, this embeds ALL, and extends to the KEYED game (adversary_keyedRomEff_exhaustive, keyedRom_exhaustive_not_hard reused from the dial) and the CARRIER/binding games (carrierAdversary_romCarrierEff_exhaustive). The counterexample is PRICED, not dodged: choiceAdv_separates — choiceAdv itself IS an exhaustive-budget member and is NOT a poly-budget member (keyedChoiceAdv_excluded verbatim); keyedRomEff_ne_exhaustive — the hierarchy is strict; deployedUnification_verdict — totality + poly-budget floor TRUE + exhaustive floor FALSE + exact placement, at one concrete family. QUANTUM (World 3), the sound share: bornProb_diff_le = PROBABILITY-level O2H (|Pr−Pr'| ≤ 4√(q·Pfind), from o2h_bound via state_norm/amp_le_one), QRomAdversaryFamily = λ-indexed quantum adversaries on the SAME Q : ℕ → ℕ budget shape with advantage as an Ensemble and Negl transport (adv_negl); toyQFamily inhabits it. ⚑ NOT claimed, precisely: no bridge to winProb over a SAMPLED oracle — OracleDiffData fixes ONE oracle pair; the missing object is an average-over-H Born semantics (and a reversible OracleComp→Unitary compilation), so keyedRom_hard remains CLASSICAL-ROM and every post-quantum-sounding consumer carries an unstated classical-adversary hypothesis. The FIXED-function world (hashGame) is deliberately NOT unified — it is the refuted IsPolyTime disease, not a pole. #assert_all_clean (28 keystones).
import Dregg2.Circuit.Emit.EffectVmKeyedStateCommit -- ⚑⚑ THE PER-EFFECT STATE-COMMIT BINDING, CLOSED ON THE KEYED FLOOR (07-23) — the fraud-undo. A 07-22 swarm DELETED the 29 per-effect commitment keystones (…_commit_binds_block/state_or_collides across 16 emission files) and pointed their docstrings at Circuit.Emit.EffectVmCommitReduction — 128KB, imported by NOTHING, NO olean, NEVER ELABORATED, and discharging efficiency at Eff := IsPolyTime, the floor sysRoots_floor_polyTime_false_babyBear REFUTES. That module is DELETED; every deleted keystone is RESTORED in its emission file (the extraction-as-data disjunctions — TRUE at deployed parameters, naming the concrete colliding pair); and THIS module closes their collision branch on the PROVED keyed floor: the GROUP-4 surface idealized as a sampled oracle (vmSpongeFamily — ⚑ a LABELLED MODELLING step: |R l| = 2^l is NOT the fixed 31-bit BabyBear digest), the forger an ORACLE PROGRAM returning TWO ROWS (BreakComp/NarrowBreakEff), the extractor a PURE chain peel over the INTER digest COLUMNS (stateColl — a mapOut post-map, so the query budget is PRESERVED), and narrowStateCommit_binds concluding Negl from keyedRom_hard (the birthday bound) with NO false hypothesis. Per-effect keystones (…Vm_commit_binds_keyed) live in the 8 narrow emission files at their OWN hash-site lists. Both poles: breakWin_satisfiable (a genuine equivocating row pair — the event is non-empty), probeComp_in_eff (class inhabited, 1-query), breakWitness_extracts (the peel FIRES on the witness), vmSponge_choiceAdv_excluded + narrowStateCommit_forger_excluded (the Classical.choice/short-collision escapes die). Honest scope: the mod-p boundaryLastPins step stays deployed-side (a [0,p) hypothesis on a 2^l-wide digest cell would be vacuity smuggling); the WIDE full-state surface (WideColl/RootsColl pairs) is restored but NOT yet keyed-floor-closed. #assert_all_clean (15 keystones).
import Dregg2.Crypto.RomQueryDial -- ⚑⚑ THE BUDGET IS LOAD-BEARING (07-16) — the tooth that proves RomQueryFloor's Eff is a genuine DIAL and not a costume. A skeptic asks: is the `hQ : PolyBounded` hypothesis of romCollision_hard decoration — does the floor hold for EVERY Q? NO, and here is the refutation. bruteComp builds the exhaustive adversary as an OracleComp: OracleComp.ofList over Finset.univ.toList reads EVERY point of the domain, reconstruct_map proves reading a complete list recovers the oracle exactly (H = fun x => rs.getD (ds.idxOf x) d0 — Nodup NOT needed, idxOf returns the first index), and then it Classical.choice's a collision out of the reconstructed H. ⚑ IT IS noncomputable AND USES Classical.choice — THAT IS THE POINT: a query-bounded adversary may be as non-constructive as it likes BETWEEN queries; the model bounds CALLS, not thought. bruteComp is choiceAdv in every respect except that it PAYS one query per domain point. bruteAdv_gameAdv computes its advantage as the constant 1 (pigeonhole: a collision exists at every oracle), so romCollision_exhaustive_budget_false refutes the floor at budget Q l = card (D l) via not_negl_one. ⚑⚑ binaryRom_budget_separates — AT THE SAME GAME: a LINEAR budget gives a TRUE floor, an EXHAUSTIVE budget gives a FALSE one. binaryRom_romEff_budget_ne separates the CLASSES (not merely which Prop is provable), and bruteAdv_not_romEff_linear is the mirror of RomQueryFloor.choiceAdv_not_romEff — the brute-forcer IS in the exhaustive class and IS NOT in the linear one, so the exclusion is about QUERIES and nothing else. Together the two files bracket the dial: at poly Q the class is a strict, provably-hard subclass; at Q = |D| it recovers Classical.choice and the collapse. #assert_all_clean (13 keystones).
import Dregg2.Crypto.ThresholdForking     -- CAPSTONE: a forked threshold forger HANDS YOU the group secret. Composes frost_cert_verifies_under_group_key (a threshold cert = a Schnorr sig under the group key) x2 with schnorr_special_soundness_extracts_dl (two forked transcripts -> the DL witness): forked_threshold_cert_extracts_group_secret — running the threshold adversary twice on the SAME nonces with e≠e' yields x·g = extractWitness(...)·g, so threshold-EUF-CMA reduces to the group DL all the way to the extractor (only the ROM forking PROBABILITY left as carrier). #assert_axioms-clean.     -- FORKING-LEMMA EXTRACTOR (the algebraic heart discharging the DL carrier structure): schnorr_special_soundness_extracts_dl — two ACCEPTING Schnorr transcripts sharing R with e≠e' determine the DL witness pk = ((z-z')/(e-e'))·g (exactly what a forking rewind produces, so a forger yields the secret). extractor_is_correct (non-vacuity: recovers the TRUE x from honest responses) + extractor_composes_on_honest_transcripts. The forking PROBABILITY (rewinding) is the ROM statistical carrier, out of scope; this is the clean algebraic content. #assert_axioms-clean.   -- THRESHOLD-EUF-CMA REDUCES TO SINGLE-SIGNER-EUF-CMA (the computational reduction shape). threshold_unforgeable_of_single_signer: since a threshold cert verifies under the group key via the EXACT single-signer verifier (frost_cert_verifies), single-signer Unforgeable ⟹ no threshold forgery — threshold adds NO new hardness over the single-signer carrier. corrupt_minority_is_simulatable: t-privacy ⟹ the t-1 corrupt shares give no advantage. Together with ShamirPrivacy = the full reduction, bottoming out at the named DL/forking (FROST) / MLWE/MSIS (Hermine) carrier — NOT discharged. #assert_axioms-clean.       -- THRESHOLD SECURITY FOUNDATION (shared by Frost + HermineThreshold): Shamir t-PRIVACY. The info-theoretic half of threshold security, UNCONDITIONAL (unlike the ROM forking-lemma EUF-CMA half we keep as a named carrier). shamir_t_privacy: for ANY t-1 observed shares and ANY candidate secret, a degree-<t sharing polynomial is consistent with both -> a corrupt MINORITY reveals nothing about the group key; t is the strict threshold. Via Mathlib Lagrange (t-1 shares + (0,secret) = t nodes -> unique degree-<t interpolant). #assert_axioms-clean.    -- POST-QUANTUM threshold QUORUM CERT (Hermine, ePrint 2026/419) — likely the FIRST Lean formalization of a PQ-threshold sig. The lattice analog of Frost: Raccoon-based (Fiat-Shamir WITHOUT aborts = NO rejection sampling), so signing is a linear map z = y + c·s verified by A·z = w + c·t; the threshold uses an everywhere-short LINEAR (Vandermonde) reconstruction s = Σ lam_i·s_i, so partials combine EXACTLY like FROST but over a module. raccoon_sig_verifies + hermine_cert_verifies_under_group_key (correctness, unconditional module algebra) + hermine_share_is_valid_under_key_share (no-zero-shares -> identifiable abort). Carrier = MLWE/MSIS, named at the unforgeability boundary (follow-on), NOT axiomatized. #assert_axioms-clean.               -- QUORUM CERT via THRESHOLD SCHNORR (FROST): a t-of-n cert IS a plain Schnorr signature verified by the SINGLE-SIGNER verifier — constant-size (one (R,z) regardless of committee size), on the DL carrier we already have (no pairing, unlike BlsThreshold). frost_cert_verifies_under_group_key: given Lagrange reconstruction x = Σ lam_i·shares_i, the combined partial responses = the group-key Schnorr response, so the cert verifies under pk=x·g via SchnorrVerifies. #assert_axioms-clean. (The high-assurance QC path; migrate off BLS once matured — ember.)             -- POST-QUANTUM SIG ANCHOR: a hash-based one-time signature (Lamport = the SLH-DSA/SPHINCS+ atom). Adds NO new hardness carrier — security reduces to hash preimage/collision resistance (the Poseidon2SpongeCR class dregg already assumes), not DL/pairing/lattice. lamport_correct (unconditional); lamport_forgery_breaks_hash (any verifying forgery on a differing message yields the unrevealed preimage OR a collision — the exact break, carrier not axiomatized); sign_reveals_only_signed_bit (one-time discipline). Next: a Merkle tree over N OTS keys (reuse Lightclient.MMR) -> many-time PQ sig. #assert_axioms-clean.
import Dregg2.Storage.BucketCommitment    -- STORAGE-IN-LEAN (rebuild the Rust storage::bucket_commitment as the Lean source of truth): the bucket object-store content commitment as an MMR.mroot fold over per-object wide leaves objectLeaf = H(key,contentType,bodyDigest) (arity-3 preimage separates object-leaf from Merkle nodes [arity 2] and MMR log leaves [arity 1]). ⚑ CUTOVER 2026-07-25: objectLeaf_injective / contentRoot_injective / read_sound are DELETED — each was gated on the REFUTED Poseidon2SpongeCR and therefore vacuous at the deployed hash. Replaced by the EXTRACTION-AS-DATA forms objectLeaf_binds_or_collides / contentRoot_binds_or_collides / read_sound_or_collides, all UNCONDITIONAL (bind, or the total extractors objectLeafFind / firstObjFind / contentRootFind HAND BACK the specific sponge collision), so they hold OF the deployed Poseidon2; the three *_of_binds_or_collides bridges recover the deleted conclusions at the (refuted) injectivity, which is the machine-checked proof that nothing was surrendered. NO floor on the live path. #assert_axioms-clean.
import Dregg2.Storage.ProviderMarket      -- STORAGE-IN-LEAN: the decentralized storage-provider MARKET as a Lean cell-program (the economic layer). A deal cell: a client blob (content-root committed) stored by a bonded provider at a price; a failed PoR (Retrievability) lets an auditor SLASH the bond. In the SlotCaveat + guarded-transition vocabulary (like QueueFactory): providerMarketFactory_conforms (well-formed genesis) + unauthorized_claim_rejected (cap-first: only a BONDED provider claims) + open_deal_only (no double-sell) + slash_decreases_collateral (the economic teeth: a slash strictly burns the bond). Decidable-eval, REAL, #assert_axioms-clean.
import Dregg2.Storage.Availability        -- STORAGE-IN-LEAN CAPSTONE: end-to-end verifiable erasure-coded storage. verifiable_erasure_recovers — a light client holding ONLY the content root recovers the TRUE blob from any k audited-genuine shards, no provider trust: composes Retrievability.por_sound / BucketCommitment.read_sound (a served shard that opens IS the genuine committed shard) with Erasure.rs_decode_correct (any k genuine shards at distinct code points determine the unique degree-<k message). #assert_axioms-clean; only Poseidon2SpongeCR (commitment) + the standard axioms (algebra).
import Dregg2.Storage.Fountain            -- STORAGE-IN-LEAN: fountain / rateless (LT) coding decode-uniqueness (the RATELESS complement to RS's fixed rate). A droplet is the field-sum (XOR) of a random message-symbol subset — a linear functional; Decodable D = the droplets' incidence SPANS the message space (the exact peeling/BP rank condition). fountain_decode_unique (over a decodable set, two messages with the same droplet values are EQUAL — unique recovery, no trick) + no_wrong_recovery (the negative pole: distinct messages can't share droplet values on a decodable set). REAL linear algebra (Finset.sum linearity), #assert_axioms-clean; the soliton->decodable-w.h.p. rateless guarantee is the documented probabilistic boundary, NOT assumed into the theorem.
import Dregg2.Storage.Erasure             -- STORAGE-IN-LEAN: Reed-Solomon erasure decode-correctness (rebuild storage::erasure's k-of-n reconstruction in Lean). RS = polynomial evaluation: a message is a degree-<k polynomial, shards are its evals at n distinct points. message_unique_of_k_shards (two degree-<k messages agreeing on >=k distinct points are EQUAL) + rs_decode_correct (a decoder producing any degree-<k candidate matching k received shards recovers the UNIQUE original — true k-of-n) + no_wrong_reconstruction (the negative pole: distinct messages can't share k shards — no substitution). REAL field algebra via Polynomial.eq_of_natDegree_lt_card_of_eval_eq, NO crypto carrier; #assert_axioms-clean (the 3 standard axioms only). The concrete GF(2^8) Vandermonde impl binds to this spec via a diff-test (a Vandermonde generator IS evaluation at points).
import Dregg2.Storage.Retrievability      -- STORAGE-IN-LEAN: proof-of-retrievability (the provider-side dual of the client sampling/retrieval). An auditor challenges positions; the provider answers with served objects; each is Merkle-checked against the committed content root. por_sound (a passing provider holds the GENUINE committed object at every challenged position — reduces to BucketCommitment.read_sound) + por_refuses_substitution (the negative pole: a substituted object CANNOT pass — different leaf, refused by CR) + por_holds_committed (the passing set is genuine end-to-end — the load-bearing input to sampling-extractability). Only Poseidon2SpongeCR assumed, #assert_axioms-clean; the sampling→reconstruction step composes with Erasure.lean (forthcoming), documented as the honest boundary, NOT assumed away.
import Dregg2.Lightclient.MMR             -- EPOCH lane: the receipt index as an APPEND-ONLY range structure (Merkle Mountain Range): peaks = perfect Poseidon2 trees, push = the carry (amortized O(1) append; peaksOf_append: incremental ≡ recomputed by foldl), forestLeaves_peaksOf recovery keystone (the forest IS the log — prefix-stable by construction), peaksOf_mountains (binary-decomposition shape), ⚑ `mroot_injective` DELETED 2026-07-28 (ed616d24c) — it claimed the root BINDS the whole log unconditionally, which is pigeonhole-false at a one-felt digest; the honest form is `mroot_binds_or_collides` on `MRootColl`, and the bar is ≈2^15.5 queries because `mroot : … → ℤ` returns ONE BabyBear felt at every log length (the MMR widens the PREIMAGE, never the digest). Formerly: tamper/truncate/extend/REORDER all move it) under the ONE Poseidon2SpongeCR floor; positional + range openings with append_preserves_opens/append_preserves_range VERBATIM; POSITIONAL COMPLETENESS range_complete (non-omission with NO gap openings — positions are dense; the count + dense slots are the whole argument) + rverifies_iff_exact + server_cannot_omit_position; CHAIN face: CommitBindsMMR = the CommitBindsIndex limb obligation VERBATIM (iroot := mroot) and light_client_position_non_omission restates the per-block face MINUS two obligations (hsorted gone — append-only is canonically ordered; gap machinery gone). Non-vacuity TRUE (exact answer verifies; carry shape 3→[0,1], 4→[2]) and FALSE (skipped/substituted/reordered answers REJECTED; root moves on every tamper class)
import Dregg2.Circuit.RotationLayout      -- THE ROTATED COMMITMENT LAYOUT (the one VK epoch's state shape, STAGED — no wire flip; .docs-history-noclaude/UNIVERSAL-MAP-ROTATION.md §2.1/§2.4/§2.6, cutover = .docs-history-noclaude/ROTATION-CUTOVER.md): RotatedLimbs = cells root · 16 NAMED registers (8→16, the widening) · map roots ADJACENT-UNIFORM (cap/nullifier/heap — the heap_root limb) · lifecycle/epoch/committedHeight · receipt-index MMR root literally LAST, so rotatedCommit_binds_mmr discharges CommitBindsMMR (= CommitBindsIndex, iroot:=mroot — THE rotation obligation) by `rfl`; rotatedCommit_binds = the anti-ghost keystone (equal commits ⇒ equal limbs ∧ equal logs under the ONE CR floor; limb-tamper + log truncate/extend/REORDER all refused, #guard-witnessed both polarities); resolve = the FactoryDescriptor.fields first-match name→register resolution (resolve_total on ≤16 declarations / resolve_inj with NO nodup hypothesis) welded to the commitment via rotatedCommit_binds_named_field; PiV3 = the staged v3 PI tail (COMMITTED_HEIGHT=201 + RATE_BOUND_TAG + CHALLENGE_WINDOW_TAG appended after the frozen v2 prefix, Rust drift-guard pi_v3_offsets_match_lean) with committed_height_not_prover_chosen closing the temporal gate's prover-chosen-height note at the flag-day
import Dregg2.Circuit.Emit.EffectVmEmitRotation -- the ROTATED STATE BLOCK, EMITTED (staged behind IR-v2; nothing rides the live wire): the 25-slot block (23 RotatedLimbs limbs absorption-ordered at 0..22 + IROOT 23 + STATE_COMMIT 24; absorbCols = List.range 24, readLimbs_spec welds the column reader to RotatedLimbs.toList ++ [iroot] BY rfl); wireCommit = the 4-ary CHAINED chip realization of rotatedCommit (8 sites, 24 limbs, iroot LAST) with the anti-ghost keystone RE-PROVED for the chained shape (wireCommit_binds + heap_root/reg/named-field/log teeth, #guard both polarities); rotationProbeVmDescriptor2 = the graduated IR-v2 probe (8 chip lookups + PI pins: published commit + committedHeight) with rotationProbeV2_pins_commit (every row's STATE_COMMIT = the genuine chained absorption) and rotationProbe_commit_binds_published (equal published commits ⇒ equal WHOLE rotated block + iroot + height — committed_height_not_prover_chosen in wire form); rotationLayoutManifest byte-pinned, Rust twin rotation_layout_matches_lean; driver EmitRotationV3.lean
import Dregg2.Circuit.Emit.EffectVmEmitUMemCohort -- THE UMEM-FORM COHORT, EMITTED (staged beside the deployed per-map v1; no VK bump, umem_witness_enabled untouched): the rotation-flip deployed-plumbing piece #1 — each cohort effect's state TOUCH emitted as a UMemOp read/write against the universal Domain×κ boundary table (the faithful Lean twin of the Rust build_umem_form), REPLACING the per-map MapOp Merkle reconciliation. umemCohortDesc = the width-7 shape (base 0..5 + guard col 6) one umemOp per touched domain; the cohort = setFieldUMem/setHeapUMem/transferBalanceUMem (heap 1) · grantUMem/attenuateUMem (caps 2) · nullifierFreshUMem (nullifiers 3, the absent none-read freshness leg) — domains matching turn/src/umem.rs. rotV3-style SURVIVAL: umemCohort_pins_final (a Satisfied2U witness's claimed final image is FORCED to the genuine fold — the post-state is not prover-chosen) + umemCohort_post_root/_pre_root (the touched map domain's committed boundary root = the derived root at BOTH endpoints, satisfied2U_boundary_root/_init_root — the umem reconciliation moves exactly what the per-map MapOp moves), grounded NON-VACUOUSLY by the worked setFieldUMem_satisfied on which setFieldUMem_survives_concrete fires (forced fold carries some 42 at (heap,7)). UMemCodec INJECTIVITY: umemCohort_addr_faithful (= uaddrEnc_injective — the (domain,key) address realizes the abstract triple, planes don't alias) + umemCohort_cap_root_binds (= capRoot_injective — the caps boundary root binds its grant/attenuate cap edges). Every cohort descriptor's emitVmJson2 BYTE-PINNED (#guard, the committed-descriptor discipline); umemCohortRegistry the staged set; driver EmitUMemCohort.lean → circuit/descriptors/umem-cohort-v1-staged-registry.tsv (NEW staged set, NOT replacing v1). #assert_axioms-clean.
import Dregg2.Circuit.Emit.EffectVmEmitUMemCohortMulti -- THE MULTI-DOMAIN UMEM-FORM COHORT, EMITTED (staged beside the single-domain umem cohort + the deployed per-map v1; no VK bump, umem_witness_enabled untouched): completes the umem cohort to the FULL effect set by covering the effects whose state touch spans MORE THAN ONE domain in one effect — the NOTE/BRIDGE economic verbs (noteSpendUMem · bridgeMintUMem) that combine a nullifiers-domain freshness insert with a heap-domain balance credit, on which the single-domain cohort FAILS CLOSED (c67796d8). umemCohortDesc2 = the width-8 two-domain shape (shared base cols 0..5 + guard col 6 for d₀, guard col 7 for d₁, ONE umemOp per domain) = the producer's sorted-domain form (turn/src/umem.rs, heap 1 col 6 · nullifiers 3 col 7, both write) with the domain set BAKED IN, so it is a FIXED descriptor backing one committed VK (the variable producer form cannot). The production prover ALREADY proves this exact multi-domain shape (circuit/tests/effect_vm_umem_real_turn.rs). SURVIVAL PER DOMAIN: noteSpend_pins_final (every declared address across BOTH domains forced to the genuine fold) + noteSpend_post_root/_pre_root PARAMETRIC over the touched domain dm (fire SEPARATELY at heap and nullifiers — each domain's committed boundary root = its derived root), grounded NON-VACUOUSLY by the TWO-row worked noteSpendUMem_satisfied (a heap balance-credit row + a nullifiers freshness-insert row; umemNullifierInsertOnly satisfied) on which noteSpendUMem_survives_concrete fires at BOTH planes. HONEST SCOPE: the cohort leg reconciles each domain INDEPENDENTLY; the CROSS-domain economic invariant (credit==value) is NOT a memory-reconciliation property — it rides the effect's rotated AIR gates (the weld 926124e6 preserves them), exactly as for the single-domain cohort. INJECTIVITY: noteSpend_addr_faithful (= uaddrEnc_injective — the heap balance cell and nullifier freshness cell never alias). emitVmJson2 BYTE-PINNED; umemCohortMultiRegistry the staged set; driver EmitUMemCohortMulti.lean → circuit/descriptors/umem-cohort-multidomain-v1-staged-registry.tsv. #assert_axioms-clean.
import Dregg2.Circuit.Emit.EffectVmEmitUMemWeldWide -- THE LEAN-EMITTED WIDE+UMEM WELDED REGISTRY, EMITTED (staged beside the bare wide registry; no VK bump, umem_witness_enabled untouched): the WIDE+umem weld's MISSING VERIFIER LEG, grounded. The producer leg (prove_wide_umem_welded_staged) + the IVC leg existed, but a welded proof verified under NO deployed descriptor — only the descriptor the producer just built / the leg's carried copy. weldUMemIntoWide = the Lean twin of the Rust additive weld_umem_into_descriptor_with_suffix (cohort:=false): append the single-domain cohort umemOp over 7 fresh columns [base..base+7) PAST the wide carriers + the umemory (id 6, arity 8) / umem_boundary (id 7, arity 7 GENERAL) tables, bump the MAIN arity to base+7, leave EVERY existing constraint + piCount UNCHANGED — so the 16 wide-commit PiBindings (the 8-felt ~124-bit anchors) ride through at the same offsets, NO narrowing. weldedWideRegistry = v3RegistryCapOpenWide welded member-for-member with each member's touched domain (wideKeyUMemDomain: cap verbs AND setPerms/setVK → caps 2, since SetPermissions/SetVerificationKey move UKey::Permissions/VerificationKey which are caps-plane; else heap 1), name-stable keys (the by-name executor verifier resolves the welded member as <live key>). #guard pins: 45-member cover · name-stable · exactly one welded umemOp · the NO-NARROWING invariant (traceWidth = host+7 ∧ piCount unchanged) · the two umem tables declared. Driver EmitWideUMemWeldRegistryProbe.lean → circuit/descriptors/rotation-wide-umem-welded-registry-staged.tsv (NEW staged set, byte-pinned by WIDE_UMEM_WELD_REGISTRY_FP + the Rust per-member weld-parity tooth). The Rust verify paths (verify_effect_vm_rotated_with_cutover, the IVC admit_welded_leg) iterate it as a NEW accepted form.
import Dregg2.Circuit.Emit.EffectVmEmitRotationR -- the rotated state block PARAMETRIC in the register count R (the 16-vs-24-vs-32 measurement probes, `.docs-history-noclaude/ROTATION-CUTOVER.md` pre-gates): layout columns / arity-{2,4} chunking (chunk31 — never an arity-3 site) / wireCommitR (the chained commitment over an ARBITRARY pre-iroot limb list) as functions of R; wireCommitR_eq_pinned (R=16 IS the pinned wireCommit, by rfl) + the #guard BYTE IDENTITY of the graduated R=16 wire JSON (the pins do not move); THE PARAMETRIC ANTI-GHOST KEYSTONE wireCommitR_binds (equal-length limb lists + equal chained commits ⇒ equal lists ∧ equal iroots under the ONE CR floor — no per-R axiom) + wireCommitR_binds_log; rotationProbeVmDescriptorR2 24/32 = the measured probes (Rust harness: descriptor_ir2.rs proof-size/timing + tamper teeth) with per-R welds rotationProbeR{24,32}_pins_commit / _commit_binds_published
import Dregg2.Circuit.Emit.EffectVmEmitRotationV3 -- THE FULL-COHORT REGEN at the rotated block (R=24 CONFIRMED, staged — ROTATION-CUTOVER.md §5 item 1): rotateV3 = ONE parametric transformation appending the rotated BEFORE/AFTER blocks (43 cols each, the R=24 geometry verbatim) + the widened-caveat region (39 cols) past ANY cohort descriptor (width +125, 4 appended PI pins: rotated OLD/NEW commit · height · caveat commit), with COL-CHAINED sites (position-independent; #guard byte-identity with the digest-chained R=24 probe) and the welds r0↔BALANCE_LO · r1↔NONCE (the CONFIRMED balance/nonce→r0/r1 assignment) · r2↔BALANCE_HI (staged home) · r3..r10↔fields · CAP_ROOT↔CAP_ROOT; keystones ONCE, parametric in d: go_colOnly_mem (the col-only walk lemma), rotV3SitesAt_pin/caveatV3SitesAt_pin (base-parametric chain pins), rotateV3_satisfiedVm_v1 (the FULL v1 denotation survives — every per-effect theorem composes unchanged), graduable_rotateV3 (side conditions lift), rotV3_pins/rotV3_publishes, and THE END-TO-END rotV3_binds_published (same published rotated commits ⇒ equal WHOLE before+after blocks + iroots + height + caveat manifest, under the ONE CR floor — one theorem, 26 descriptors); v3Registry = all 26 v2Registry members rotated (attenuate keeps its phase-B extras: attenuateV3_non_amp; setFieldDyn keeps its mem ops: setFieldDynV3_readback_genuine via memLog rfl-transport); STAGED beside v1/v2 (no VK bump; Rust twin circuit/descriptors/rotation-v3-staged-registry.tsv)
import Dregg2.Circuit.Emit.EffectVmEmitCellSealFrozenPubkey -- E10 PROOF-HALF (byte-safe, deploy-gated): cellSealFrozenPubkey = the deployed cellSealV3 WITH the gPubkeyFreeze owner weld (AFTER B_PUBKEY_OCTET+i == BEFORE, i<8 — the frozenAuthorityColEqs colEq shape), a NEW hardened variant (E8 pattern; deployed cellSeal descriptor BYTE-UNTOUCHED, no VK regen). THE WOUND (falsifier circuit/tests/zzz_e10_freeze_owner_falsifier.rs): cellSeal is a lifecycle mover (not v3OfFrozen), so the owner octet 105..112 is in NO freeze/weld set — the AFTER owner is a free felt and the deployed AIR ACCEPTS a seal that silently rewrites the operated cell's owner. THE TOOTH cellSealFrozenPubkey_rejects_owner_forge: the forge (AFTER owner ≠ BEFORE owner on a transition row, both canonical) is UNSAT under the frozen variant. COMPLETENESS cellSealFrozenPubkey_complete_honest: an honest owner-preserving seal (Cell::seal never touches public_key — cell/src/cell.rs:797) still SATISFIES it, so the freeze is no DoS. PEEL satisfied2_of_withCellSealPubkeyFreeze (only rejects, never newly-accepts). All #assert_axioms-clean {propext,Quot.sound}. DEPLOY (weld into live descriptor + VK regen) HELD for a clean-tree cutover — NOT done here.
import Dregg2.Circuit.Emit.EffectVmEmitRotationWide -- THE WIDE EMISSION LANE (Phase B-ROTATION, faithful 8-felt commitment chain emitted as wide chip lookups), STAGED additive beside the live 1-felt path: siteLookupN/siteLookupsN_sound (the wide analog of siteLookup/siteLookups_sound — binds ALL 8 output columns via chip_lookup_sound_N, NOT just out0); rotV3WideSitesAt = the 13 chained absorptions emitted as wide lookups (head 4 / eleven body groups carrier‖3 = arity 11 / iroot final carrier‖1), each carrier 8 COLUMNS; rotV3WidePin = the WIDE pin (state-commit carrier = wireCommitR8 of the row's own 37 limbs + iroot, the chainFrom8 fold via chip_lookup_sound_N); rotateV3Wide/v3OfWide = the parallel emission (the live registry KEEPS v3Of — live wire untouched); the re-proved wide keystone tower rotV3Wide_pins/_publishes/_binds_published (same published 8-felt commit columns ⇒ equal WHOLE limb list + iroot, the GENUINE ~124-bit binding via wireCommitR8_binds, under the named floors Poseidon2WideCR + Poseidon2Width8) + the anti-laundering #guard (the wide binding distinguishes states differing ONLY beyond lane0). NO descriptor-JSON/FP/VK/geometry change; the live flip repoints the registry next.
import Dregg2.Circuit.Emit.EffectVmRotationWideCommitIff -- THE `air_accepts ⟺ spec` AT EIGHT FELTS, over the object 977e73b19 chains as the consensus anchor (wireCommitR8 / wire_commit_8_chip — the CHIP chain, arity-tagged, NOT the divergent plain wire_commit_8): supplies the missing COMPLETENESS (`←`) leg the wide lane never had. rotV3Wide_commit_iff8 = the sixty-two wide lookups hold ⟺ (WideChain8 the carriers ARE the genuine chained absorption ∧ carrier-61 = wireCommitR8 of the row's own 184 rotated limbs + iroot); `→` is the DEPLOYED pin rotV3WidePin (unchanged), `←` is the prover-BUILT chip table wideChipTableOf (sound + contains this block's genuine rows — no new floor posited). CHIP-vs-plain confirmed STRUCTURALLY: chipLookupTupleN's head is the arity tag ins.length, so ChipTableSoundN forces permW = the arity-tagged chip_absorb_all_lanes, which single_perm_compress cannot realize. Binding carried as the HONEST DISJUNCTION rotV3Wide_commit8_binds_or_collides (wireCommitR8_binds_or_collides — the extraction-as-data keystone that replaced the deleted-FALSE Poseidon2WideCR), restored instance-locally via ¬WireColl (NOT the universal floor smuggled back). Canaries at 8 felts both polarities + a 184-limb #guard the honest fill LANDS on the commit + a proved non-vacuity inhabitant. The seventeen 1-felt *_commit_iff STAY VALID (different object/column set — NOT subsumed). #assert_axioms-clean.
import Dregg2.Circuit.Emit.EffectVmEffectsHashPin -- THE effects_hash PIN, DESIGNED (Lean-authored AIR, law #1): PI[16..20) — the four-felt effects_hash — carries ZERO piBinding in any deployed member, compute_effects_hash_4 is called only by the PROVER's trace generator (trace.rs:1289), and its witness cols 94/95 are deleted in all 57 wide members by an E1 kill-set whose own criterion is `referenced by NO surviving constraint` — so the deletion IS the proof nothing read them. Full-node checking is PI-substitution only (proof_verify.rs:711/1745 regenerates the whole vector, so the proof adds nothing the host did not compute); ledgerless (full_turn_proof.rs:4460) it is FREE. withEffectsHashPin appends a 41-col fold block (tag/ext/accIn/2 carriers/accOut) + 24 constraints: 3 wide-chip lookups (chip_lookup_sound_N forces ALL 8 output lanes), a linear tag gate off the one-hot selector row (the effects_hash_inputs domain tag IS the columns::sel index), 8 windowGate continuity, 8 boundary-first IV pins, and 4 piBinding .last — the column each PI slot names AND what forces it, against a tree carrying 173 PI slots pinned to free columns. piCount UNCHANGED (16..20 already ride the V1_PI_COUNT=42 window). SOUNDNESS THEOREMS AND THE RUST TWIN ARE NOT BUILT: this file is the design + shape #guards; the acceptance test (effects_hash_pi_pin_inverts.rs) and the inversion of vk_epoch_misc_light_client_binding.rs's three forged-hash-ACCEPTED assertions are the gate.
import Dregg2.Circuit.Emit.CapOpenEmit -- THE LIVE cap-membership OPEN, emitted: DeployedCapOpen's PROVEN leafLookup + 16 nodeLookups + 4 gates laid down over a concrete CapOpenCols appendix (58 cols past the rotated R=24 width) into the LIVE transferCapOpenEffV3/attenuateCapOpenEffV3 (genuine SUBMASK facet + DECODED tier); the bridge effCapOpenV3_satisfiedEff REBUILDS DeployedCapOpen.SatisfiedEff from a live Satisfied2 witness ⟹ effCapOpenV3_authorizes (MembersAt cap_root leaf ∧ target=src ∧ submask permits the effect) ⟹ transferCapOpenEffV3_authorizes (kernel authorizedFacetB) — the LIVE descriptor the deployed prover routes AND the apex authority leg refines (the Signature-pinned capOpenAttenuateV3/transferCapOpenV3 DELETED, Stage D) — the &[] placeholder discharged in-circuit. LAW#1: zero new constraint semantics (every constraint is a DeployedCapOpen Lookup/gate the Rust interpreter already realizes); writeMaskGate emitted FAITHFULLY (mask_lo==3, the abstract Auth rights mask — NOT faked to the deployed EffectMask convention, the NAMED HORIZONLOG fork). #assert_axioms-clean.
import Dregg2.Circuit.Emit.HeapOpenEmit -- THE LIVE heap-membership OPEN (the SECOND faithful 8-felt Merkle root's after-spine): the exact twin of CapOpenEmit §11/§12 for heap_root. HeapMembershipCore (arity-2 (addr,value) leaf absorb + the SHARED node8 nodeLookup spine, re-instantiated at Heap8Scheme) + heapLeafDigest_sound8/heapNode_sound8/heapRecompose_reaches_cur8 → heapOpen_recompose8 (the leaf's native-8-felt digest recomposes the committed heap-root GROUP) → heapOpen_writesTo8: TWO HeapMembershipCore witnesses sharing a path FORCE EffectVmEmitRotationV3.heapWritesTo8 over the FULL ~124-bit root (NEVER the lane-0 squeeze the heap GENTIAN tooth refutes). Trace-forced from Satisfied2. #assert_axioms-clean.
import Dregg2.Circuit.Emit.FieldsOpenEmit -- THE LIVE fields-membership OPEN (the THIRD and LAST faithful 8-felt Merkle root's after-spine): the exact twin of HeapOpenEmit §11/§12 for fields_root. FieldsMembershipCore (arity-2 (addr,value) leaf absorb + the SHARED node8 nodeLookup spine, re-instantiated at Fields8Scheme) + fieldsLeafDigest_sound8/fieldsNode_sound8/fieldsRecompose_reaches_cur8 → fieldsOpen_recompose8 → fieldsOpen_writesTo8: TWO FieldsMembershipCore witnesses sharing a path FORCE EffectVmEmitRotationV3.fieldsWritesTo8 over the FULL ~124-bit root (NEVER the lane-0 squeeze the fields GENTIAN tooth refutes). The CONSTANT-key bind (constEqGate at refusalAuditKeyFelt — the reserved audit slot, not a runtime address) is the sole divergence from heap. effFieldsWriteV3 (read appendix + after-spine) + effFieldsWriteV3_forces_write8 + effFieldsWriteV3_satisfied2_strips_to_base (the apex bridge). Trace-forced from Satisfied2. #assert_axioms-clean.
import Dregg2.Circuit.Emit.CapOpenTurnPins -- THE TURN-IDENTITY PI WELD (in-circuit realization of the owner-authority/turn-identity smuggle close). effCapOpenV3TB base name n = effCapOpenV3 + two NEW turn-identity columns (capOpenActorCol/capOpenDstCol past the cap-open appendix, every capOpenCols proof untouched) + three turnIdentityPins (.piBinding .last welding capOpenCols.src, actor col, dst col to NEW PI slots piCount+0/1/2 — the first past rotateV3's 4 commit pins). effCapOpenV3TB_to_base: the appended PI pins only ADD constraints (and declare no mem/map ops, effCapOpenV3TB_memLog/mapLog), so every cap-open keystone (effCapOpenV3_satisfiedEff/effCapOpenV3_authorizes) lifts verbatim. effCapOpenV3TB_publishes: a Satisfied2 witness's LAST row FORCES src/actor/dst columns = PI[piCount+0/1/2]. TurnIdentityAnchored = the NAMED verifier PI override (it recomputes the turn-identity PIs from the trusted turn before verify_vm_descriptor2, exactly as the record-pin family anchors dpis[38] from the trusted post-cell — REALIZABLE, deployment analog of rotateV3WithRecordPin's anchor). effCapOpenV3TB_hsrc: pin + anchor ⟹ capOpenCols.src = turn.src — the hsrc obligation of effCapOpenV3_authorizes DISCHARGED from a real PI binding, no longer carried (targetBindGate already pins leaf.target=src, so the OPENED LEAF's target welds to the published source). effCapOpenV3TB_authorizes: the fan-out authority leg with hsrc REPLACED by the turn-identity anchor (cap-open src welded to the PUBLISHED source, not a free column). NEGATIVE TOOTH effCapOpenV3TB_rejects_mismatched_src: a last row whose cap-open src ≠ PI[piCount] is UNSAT (the equality gate bites) — with the anchor, a forged src ≠ turn.src is rejected. RESIDUAL (named, reported): the ACTOR↔leaf-position binding (so an owner-claim actor must be the real owner of src) needs the Merkle path to encode the actor — the heavier fan-out; this module publishes the actor column+PI (light client SEES it) but does not yet weld it into the cap-tree root. #assert_axioms-clean.
import Dregg2.Circuit.Emit.EffectVmEmitRotationCaveat -- the WIDENED CAVEAT OPERAND, staged (the heap-caveat wire-shape pre-gate, ROTATION-CUTOVER.md §3): operand = (domain, key) on the umem domainCode discipline (registers 0 · heap 1; key widens u8 → felt) with THE NO-ALIASING KEYSTONE caveat_operand_no_aliasing (a slot operand and a heap operand can NEVER collide — domain separation as a theorem) + caveatOperand_wire_injective + fail-closed decode (operand?_unknown_refuses); RotCaveatEntry = 7 felts [type_tag, domain_tag, key, p0..p3], manifest = 29 felts, caveatCommit = the chunk31 arity-{2,4} chained chip commitment with caveatCommit_binds (equal commits ⇒ equal manifests: forged domain tag / tampered heap key REFUSED, #guard both polarities); rotationCaveatProbeVmDescriptor2 = the staged R=24 probe (rotated block + caveat manifest + caveat chain, 21 chip sites, 3 PI pins) with the end-to-end keystone rotationCaveatProbe_binds_published; the semantics bridge: tagHeapAtom decodes SLOT_CAVEAT_TAG_* into the landed HeapAtom vocabulary (heapAdmits_strictMono_iff PROVEN), the executor leg = the NAMED PREMISE HeapCaveatRuntimeDischarge (HORIZONLOG follow-up); byte-pinned rotationCaveatLayoutManifest, Rust twin rotation_caveat_layout_matches_lean
import Dregg2.Circuit.Emit.EffectVmEmitBilateralAgg -- THE BILATERAL AGGREGATION AIR, emitted from Lean (law #1; the cutover-deletion of the hand-authored Rust `BilateralAggregationAir`): the cross-federation conservation outer AIR (N witnessed-receipts of one turn → ONE outer STARK binding shared turn-id CG-2 + schedule replay CG-3 + the unique-agent accounting CG-4). DECOUPLED from the v1 `effect_vm::pi` (the ~49-felt bilateral-SCHEDULE contract `Sched.*` is its OWN region, fed independently of the rotated effect-vm 38-PI). Introduces the NEW two-row IR-v2 primitive `WindowConstraint`/`windowGate` (the cumulative-sum `next[cum]=local[cum]+next[is_agent]` the single-row `EmittedExpr` gate cannot express); the aggregation descriptor `bilateralAggDescriptor` (width 87, 23 fixed outer PI, 70 constraints incl. 2 window gates, byte-pinned `#guard`s) + the SOUNDNESS TEETH `agg_rejects_turn_mismatch` (a row disagreeing on turn-id is UNSAT) + `agg_rejects_bad_agent_count` (the last-row cum≠1 boundary — two agent rows REFUSED), axiom-clean. The Rust interpreter (decode windowGate + the WR schedule-block restructure + the prover rewire) LANDED (wall A, b0baf026c).
import Dregg2.Circuit.Emit.BilateralAggregationCompact -- E8 STAGED width-87→52 compaction of the bilateral aggregation AIR, byte-safe soundness core (S2/E1 pattern: proof now, regen later). The v2 descriptor spends 35/38 gates on the prover-filled sched==expected self-check (nothing pins the expected block externally) while middle rows' turn-identity columns are in-AIR UNCONSTRAINED. `bilateralAggDescriptorV3` (width 52, 48 constraints, same 23 outer PI, FULL wire-string #guard-pinned pre-emission) deletes the expected block and adds 13 identity-carry windowGates; PROVEN: `expand_satisfies` (v3 accepts ⊆ v2 — expansion concludes FULL Satisfied2 of the deployed v2), `contract_preserves` (every identity-constant honest witness survives; the deployed trace builder is identity-constant by construction), STRICT via `gapTrace` (v2 ACCEPTS a middle-row forged turn identity, v3 REFUSES it, no v3 preimage exists) + `compact_identity_every_row` (v3 pins EVERY row to the published identity) + the Rung-1/Rung-2 crowns carried through the bridge (`compact_refines`/`compact_unique_agent`). Rooting this module also brings the previously ORPHANED BilateralAggregationEmit (full v2 byte-pin) / BilateralAggregationRefine (Rung-1 bridge) / BilateralAggregationRung2 (no-double-spend discharge + the p−1 phantom-agent forgery) into the CI-built closure. Cutover recipe staged in HORIZONLOG.md (E8).
import Dregg2.Circuit.Emit.EffectVmEmitCrossSide -- THE CG-5 CROSS-SIDE-EXISTENCE AIR, emitted from Lean (law #1; retires the hand-authored `CrossSideExistenceAir`): each agent's witnessed-receipt has a matching cross-side entry (the balance prefix-sum closes to 0 ⇒ no missing peer) bound to a rolling edge-sequence commitment; width 10, PI 2, 11 constraints (2 window gates + 2 Poseidon2 chip lookups — the fingerprint is now a REAL in-circuit lookup the hand-AIR never constrained). The empty-PI hand-AIR bound its proof to the shipped trace OUT of band; the v2 descriptor pins a verifier-derived rolling commitment IN-circuit (a strengthening, not a port). Teeth cse_rejects_unbalanced/cse_rejects_wrong_commit/cse_fingerprint_is_hashed, axiom-clean.
import Dregg2.Circuit.Emit.EffectVmEmitBundleFold -- THE PROOF-OF-PROOFS FOLD, emitted from Lean (law #1; retires `BundleTreeFoldAir`): folds child-bundle DIGESTS (Poseidon2(child.outer_pi) — NOT inner STARKs, which verify classically in verify_aggregated_tree, so NO genuine recursion / no named seam) into the root accumulator; width 3, PI 2, 4 constraints (1 window gate + 1 arity-2 compress chip lookup — the compress is now an IN-circuit constraint, was a verifier-side recompute, retiring the hand-AIR's named residual). Teeth fold_rejects_tampered_final/fold_compress_is_hashed, axiom-clean.
import Dregg2.Circuit.Emit.EffectVmEmitTurnChainBinding -- THE DEPLOYED WHOLE-HISTORY TURN-CHAIN AIR, emitted from Lean (law #1): exact 14-site semantic twin of TurnChainBindingAir — root/acc/index continuity, endpoint+digest+turn-count PI pins, genuine turn-chain Poseidon2 preimage [acc_in,old_root,new_root,idx], and real-prefix padding count. Rung 1 sound + Rung 2 constraint-set equivalence; four-row real→padding witness; continuity/idx/real-count forgeries refuted; axiom-clean.
-- (NonRevocationAdjacencyEmit + NonRevocationRefineBridge RETIRED 2026-07-20 — felt-width #11
-- fold-in: the 1-felt sorted-tree non-revocation rail is deleted; spend freshness is the limb-26
-- `nullifierFreshOp` `.absent` grow-gate and delegation-ancestor revocation is the limb-37
-- `spendAncestorFreshOp` `.absent` open, both in EffectVmEmitRotationV3.noteSpendV3. The
-- air_accepts⟺spec schema the RefineBridge pioneered lives on in its replicas
-- (GarbledEvalRefineBridge, EffectVmEmitTransferComplete, EffectVmFullStateRunnableComplete below).
import Dregg2.Circuit.Emit.GarbledEvalRefineBridge -- ASSURANCE-PERIMETER (garbled-eval): the air_accepts⟺spec SCHEMA replicated to garbled-circuit evaluation. garbled_accepts_iff (accept-set = canonical instances) + garbled_bridge (∀-soundness ∧ ∃-completeness), all trust folded into the ONE named `GarbledCarriers` bundle (GarbledTraceCanon 30-bit range envelope + GarblingHashBound Poseidon2 garbling-hash carrier). Soundness folds RUNG1 satisfied2_implies_garbledEvalRun + RUNG2 garbled_rung2_no_output_forgery into GarbledEvalRunH (Yao decryption under the REAL hash); the NEW content is the PARAMETRIC completeness — honestG_satisfied2 generalizes the single-point garbled_honest_satisfied2 (t₀) to ∀ canonical (pub,o), constructing the trace AND both carriers. Canaries load-bearing both directions, #assert_axioms-clean. Wired here so CI compiles the garbled completeness leg (RUNG1/RUNG2/this were previously lake-env-only).
import Dregg2.Circuit.Emit.EffectVmEmitTransferComplete -- STATE-COMMITMENT PROVEN-COMPLETE, transfer family (the flagship): transferDescriptor_commit_iff — the both-windows biconditional air_accepts ⟺ (CellTransferSpec ∧ NEW_COMMIT = the genuine wire absorption cellWireCommit). The ← completeness construction welded to the → soundness (full_sound); the commit is the DEPLOYED commitOf (GROUP-4 H4-of-H4), not a mirror; canary-confirmed two-valued. Turns audit #1/#2 (the chained/signed state commitment) from trusted-Rust into a proven AIR value. Wired here for CI coverage.
import Dregg2.Circuit.Emit.EffectVmFullStateRunnableComplete -- THE GENERIC FULL-STATE COMPLETENESS ENGINE (the cycle-3 unlock): lifts the transfer completeness flagship to the generic RunnableFullStateSpec — runnable_full_complete (SEM⟹SAT, both windows) + runnable_full_commit_iff (the literal air_accepts ⟺ (fullClause ∧ NEW_COMMIT = wireCommitOfRow)). The reusable, discharged-ONCE crypto is factored out: wide_sites_of_carrier CONSTRUCTS the Poseidon GROUP-4 carrier (the de-transfer'd sem_sites) + runnable_forces_genuine_commit forces the commit (= wide_commit_eq). RunnableFullStateCompleteSpec bundles the THIN per-tag obligations (only build_active/build_last — the effect's own gates, the converse of decodeFull); transferRunnableCompleteSpec is the validated reference, demo discharged for any hash. So the ~15 kernel-only effect tags get ⟺ as thin instantiations, no new crypto. Named carrier = Poseidon2SpongeCR; canaries both directions (tamper→commit MOVES + clause fails; bogus commit UNSAT), #assert_axioms-clean.
import Dregg2.Circuit.Emit.EffectVmFullStateTagsA -- CYCLE-3 FAN A: 8 KERNEL/LIFECYCLE TAGS get air_accepts ⟺ spec as thin instantiations of the engine — IncrementNonce, SetVK, SetPermissions, CellSeal, CellUnseal, CellDestroy, MakeSovereign, Noop. Each carries its literal *_commit_iff over the DEPLOYED *Wide descriptor (decodeFull reuses the real *Gates_give_cellSpec, NOT a True stub — the → genuinely bites) plus a per-tag canary (tampered after-state → fullClause fails). The genuine per-tag work is build_active (the effect's own freeze+tick / zero-block gates on the constructed witness); the transition/boundary/selector window structure is proved ONCE (kernelWindowsSel/NoSel) and the crypto is the engine's ONE named Poseidon2SpongeCR. #assert_axioms-clean.
import Dregg2.Circuit.Emit.EffectVmFullStateTagsB -- CYCLE-3 FAN B: 9 KERNEL-EMIT TAGS get air_accepts ⟺ spec — CreateCell, CreateCellFromFactory, EmitEvent, Exercise, ReceiptArchive, Burn, PipelinedSend, SetField (classified KERNEL-FIELD-ONLY: gFieldWrite writes fields[slot] with NO sorted-tree gate, so instantiated here, not deferred), BridgeMint. Each with its *_commit_iff + concrete demo + canary over the deployed descriptor; a per-module shared witness builder (mkRow/WReads) discharges the transition/boundary/selector/carrier/range facts once, so each tag supplies only its rowGates-from-clause via the tag's *_faithful.mpr. SetField/BridgeMint/PipelinedSend carry the deployed range envelope as an HONEST named precondition (relocated isRow→decodeAfter — a faithful re-partition of the precondition, the same envelope the deployed *_runnable_full_sound already assume). 37 #assert_axioms pass.
import Dregg2.Circuit.RotatedKernelRefinementNotesFreshBridge -- NOTE-SPEND FRESHNESS PROVEN-COMPLETE (⟺ replication, audit #6): noteSpendFresh_accepts_iff — NoteFreshAccepts ↔ nf ∉ nulls, a genuine full biconditional (soundness `freshness_forced` ∧ ∃-completeness `gapOpen_complete`, the forward gap-construction from a fresh key). Trust folded into the named carrier bundle; `NullifierTreeEncodes` kept as the HONEST Rust-accumulator-boundary differential residual (named, not laundered). Canary both directions, #assert_axioms-clean. Wired for CI coverage.
import Dregg2.Circuit.CrossCellConservation -- THE TURN-WIDE CROSS-CELL VALUE-CONSERVATION AIR (Σδ=0), emitted from Lean (law #1; closes foolable gap #6). The deployed rotated per-cell proof forces per-cell balance arithmetic + the per-cell signed NET_DELTA PI (pi::{NET_DELTA_MAG,NET_DELTA_SIGN}) but NOT the turn-wide cross-cell pairing: a single-cell proof can't conclude no value was MINTED across the turn (the debit↔credit cancellation was reconstructed OFF-AIR). This aggregation AIR realizes the abstract `Spec.Conservation.conservedInDomain (deltas.sum=0)` as a CONCRETE prefix-sum-to-zero over the per-cell signed NET_DELTA PIs: one row per contributing delta (asset · mag · sign · present · balance), boundary `balance[last]=0` = per-asset Σδ=0, mint/burn enter as explicit declared ±supply rows. Width 5, PI 1 (asset), 8 constraints (1 window gate, NO chip — the signed delta IS the contribution) + 1 magnitude range rib. Teeth ccc_rejects_unbalanced (non-conserving UNSAT) / ccc_forged_mint_unsat (the A−10,B+999 forgery balances to 989≠0 ⇒ UNSAT, honest A−10,B+10 ⇒ 0) / ccc_rejects_wrong_asset (per-asset partition) / ccc_mag_is_ranged, axiom-clean. ADDITIVE — not wired into the live proof_verify.rs (the main loop owns the verifier handoff).
import Dregg2.Circuit.CrossCellConserveDecision -- THE RUNTIME-CALLABLE cross-cell conservation DECISION (Σδ=0 per asset), @[export dregg_cross_cell_conserves]-ed (House Law #1): the deployed executor CALLS this instead of the hand-written Rust BlockConservation twin. Init-only (no Mathlib) so its leanc `.c` splices into libdregg_lean.a. Groups (asset,δ) rows + declared mint/burn supply, ADMITS iff every asset's signed sum is 0, reports the first imbalanced asset in ascending key order (matching the Rust BTreeMap so the routed PerAssetConservationViolation is byte-identical). #guard teeth: honest transfer conserves, forged +999 refuses (first-imbalanced 989), cross-asset borrowing refuses (lower key first), declared supply restores, the p-sum forgery (1006632961+1006632960=p) REFUSED over ℤ, wire round-trip.
import Dregg2.Circuit.CrossCellConserveRefine -- THE REFINEMENT of the runtime conservation decision against the COMMITTED Σδ=0 AIR (House Law #1). Proves the @[export]-ed decision is the AIR's faithful runtime image, not a re-authored peer: balanceList_deltasOfTrace (the decision's sumInts over a trace's realized ccv−dcv deltas EQUALS the committed creditSum−debitSum), decision_conserves_iff_air_boundary (THE REFINEMENT IFF: the decision ADMITS iff the committed creditSum=debitSum boundary balances), satisfied_imp_decision_conserves (instantiates the committed ccc_conserves: every trace the AIR ACCEPTS the decision ADMITS), conservesRows_iff (multi-asset = per-asset boundary conjunction). psum_forgery_refused non-vacuity. #assert_axioms-clean, NO sorry.
import Dregg2.Circuit.CircuitSoundness
import Dregg2.Circuit.RestFrameCardinalityFloor -- ⛑⛑ THE FLOOR BOTH INSTRUMENTS WERE BLIND TO, and it is STRICTLY WORSE than the gated ones: `StateCommit.RestHashIffFrame RH` (the rest-hash is injective on the non-`cell` kernel components) is FALSE for EVERY `RH : RecordKernelState -> Z` at EVERY width, BY CARDINALITY. `restHashIffFrame_false_by_cardinality` proves it by Cantor: two of the components it must separate are FUNCTION SPACES (`bal : CellId -> AssetId -> Z`, `Caps = Label -> List Cap`, all three indices `Nat`), so the `Set N`-indexed ledger family composed with `RH` and `Encodable.encode` would be an injection `Set N -> N`. Every gated floor in the campaign (Poseidon2SpongeCR, compressNInjective, cellLeafInjective, logHashInjective, ...) is false only AT DEPLOYED BABYBEAR WIDTH and has an honest repair -- a per-instance non-collision at the pair the proof actually uses; this one has NO instance to be per-instance at, and widening the digest does nothing (`restHashIffFrame_false_at_babyBear` discharges the range bound UNUSED to say so). CONSEQUENCE: `CircuitSoundness.CommitSurface` carries it as its FIFTH field beside the four gated CR carriers, so the surface has NO INHABITANT AT ALL and the ~101 modules quantified over a `CommitSurface` are vacuous today -- draining the four gated fields alone would NOT re-inhabit it, which is the `shed one floor of a multi-floor declaration and the metric moves by zero` failure one level deeper. WHY BOTH INSTRUMENTS MISS IT: the Python ruler's tier A is binder-position over a hand-checked floor list and documents structure FIELDS as a known false negative; `#floor_ratchet` DERIVES its floor set but requires injectivity SHAPE (`FloorCensus.injShape`/`injShapeAnd`), and this body is a forall-IFF over a 17-fold conjunction, so landing the refutation does NOT promote it and the tree stays green -- checked against both shape tests BEFORE writing, not after. 234 binder positions in 85 modules are newly known-vacuous. REPAIR (named, not done): digest the FINITE support actually touched -- the `accounts : Finset CellId` rows -- never the whole function, which is what `Verify/InjSpelledFloors` already prescribes for this class. #assert_axioms-clean.
import Dregg2.Circuit.TurnDecodeChainLogBundleCutoverCheck -- ⚙ TEETH of the 2026-07-25 TURN-LOG BUNDLE cutover (the intra-turn receipt-log seam): `Circuit.CircuitSoundness.TurnDecodeChainLog` carried `hLog : logHashInjective LH` as a FIELD, so no value of it existed at ANY deployed accumulator — `StateCommitFloorRegrounded.logHashInjective_false_babyBear` refutes injectivity for every receipt-chain hash landing in ONE bounded BabyBear felt (the chain grows without bound, the digest is one field element), and a field has no argument position to be per-instance AT. So `turnDecodeChainLog_seam_log_derived` / `_seam_full_derived` / `_rejects_forged_log` — the theorems that say the published turn-chain BINDS the intermediate receipt log, i.e. the dregg through-line "a turn leaves a VERIFIABLE receipt" at the composition seam — were VACUOUS at deployed parameters. THE FIELD IS DELETED (never swapped for a `noColl` field: `Shielded.RealCrypto` §2.0), and the three consumers now take `CircuitSoundness.NoLogSeamColl LH c.steps` — `¬ LogColl` at EXACTLY the chain adjacencies the old proof fed the floor at, and no others. RE-INHABITED, and TOTALLY: the honest prover's chained-root builds the bundle for EVERY `hash`/`S`/`LH`/`c` with no emptiness hypothesis and no injectivity (publish the true log digest at each boundary, whereupon `logDecode` is `rfl` and the published log seam is the chain's own threaded `seam` pushed through `LH`), and it is instantiated at a BabyBear-reduced accumulator whose `logHashInjective` this module REFUTES (`deployedTurnLogHash_not_logHashInjective`). ⚑ THE INHABITANTS ARE `example`s, DELIBERATELY, and that is the honest cost: any NAMED declaration of that type is a `bundle-user` of `S : CommitSurface` and `#floor_ratchet` gates it — MEASURED, it did, on the first attempt at this cutover, and it was RIGHT to, because such a value is still vacuous through the OTHER bundle; grandfathering it would have been a baseline RAISE taken to record a win. TEETH: three kernel-defeq CUTOVER TYPE PINS (a weakening, a dropped hypothesis or a floor resurrection goes RED); the side condition is LOAD-BEARING (`seamStep_unconditional_false` — the per-adjacency step with `hno` deleted is REFUTED at a constant accumulator, and `logColl_reachable` shows the equivocation branch is live there); five `#assert_not_depends_on` putting `logHashInjective` out of proof-closure reach of the bundle and all three cured theorems (2743/3734/2745 constants scanned), with an `#assert_depends_on` POSITIVE CONTROL on the bridge so the rejectors are not blind. NO STRENGTH LOST, at ZERO new carriers: the deleted field's own type gives the side condition by `noLogSeamColl_of_forall (fun _ _ => LogCommitRegrounded.noLogColl_of_inj hLog)` — the lift is FLOOR-FREE and the bridge is already grandfathered, so the recovery mints nothing. NAMED RESIDUAL at the current resolution (module §7): `S` stays universally quantified because `CircuitSoundness.CommitSurface` still carries FOUR refuted floors as fields — this is a deployed inhabitant in the LOG dimension, which is the dimension this cutover owns. ⚑ AND A FINDING FOR WHOEVER TAKES `CommitSurface`: draining its four GATED floors would NOT re-inhabit it, because its fifth field `restFrame : RestHashIffFrame RH` is invisible to BOTH instruments and is unsatisfiable BY CARDINALITY — it demands `RH : RecordKernelState -> Z` be injective on the non-`cell` components, two of which are FUNCTION SPACES (`bal : CellId -> AssetId -> Z`, `Caps = Label -> List Cap`, all three indices `Nat`), so the domain has size >= 2^aleph0 while `Z` is countable. False for EVERY `RH` at ANY width, not merely at BabyBear; the binder-keyed ruler cannot see a structure field and the gate never promotes `RestHashIffFrame` to a floor (its body is an forall-iff, not injectivity-shaped, and nothing in the tree refutes it). Shed it in the SAME pass or the structure stays uninhabitable at a subtler width. #assert_axioms-clean.
import Dregg2.Circuit.ProofByteDecoder -- REAL byte->field-view decoder for the deployed public-input wire (turn/src/binding_proof.rs hash_into layout + BabyBear::new reduction, circuit/src/field.rs): parses a length-prefixed u32-LE field stream into WrapPublics.segment + BatchProofData.exposedSegment (tooth-3 identity), with parse∘encode=id roundtrip theorems (decodeView_encode, decodeFieldStreamExact_encode) and a #guard golden vector pinned to the deployed vec![0xDEADBEEF,0xCAFEF00D] PI test. DISCHARGES CircuitSoundness.cfgView opacity for the (proof/PI bytes -> WrapPublics.segment/exposedSegment) leg; RESIDUAL (named): the trace/quotient/FRI/table BatchProofData fields live in the postcard-serialized inner p3 BatchStarkProof blob, NOT parsed here. #assert_axioms-clean.
import Dregg2.Circuit.Freshness
import Dregg2.Circuit.FinKernelState
import Dregg2.Circuit.FinKernelStep
import Dregg2.Circuit.FinInterp
import Dregg2.Circuit.FinAllocCell
import Dregg2.Circuit.FinProgramSquares
import Dregg2.Circuit.FinCreateCellSquares
import Dregg2.Circuit.FinGrantSpawnSquares
import Dregg2.Circuit.FinReactSquare -- HISTORICAL pre-64477 shared-note-nullifier model ONLY; not deployed refinement (kept imported so the historical artifact remains build-checked)
import Dregg2.Exec.ReactiveRegistry -- CURRENT: dedicated ReactiveNullifierSet; React leaves faithful note/FNSP nullifiers unchanged
import Dregg2.Circuit.BabyBearFriField
import Dregg2.Circuit.BabyBearFriSetup
import Dregg2.Circuit.BabyBearFriDeployed
import Dregg2.Circuit.FriFoldArity
import Dregg2.Circuit.FriArityTransfer   -- THE ARITY-8 PROXIMITY-GAP COUNT: the standing ~112.6-bit per-fold posture (FriCorrelatedAgreementSharp §8, wrap_perFold_soundness_capacity) is proved over a 2-to-1 fold; the DEPLOYED config folds at ARITY 8 (IR2_FRI_MAX_LOG_ARITY = 3). good_card_le_of_phase_injective generalizes §8's pair count to arity m by a PAIR double-count: for y ≠ z, H y − H z is a nonzero degree-≤(m−1) polynomial and every good β folding both to the same constant is a ROOT, so each pair lies in ≤ m−1 agreement sets ⟹ |Good|·C(s,2) ≤ (m−1)·C(|κ|,2). Recovers §8's 2016 EXACTLY at m=2 (arity2_recovers_capacity_count). ⚑ THE HONEST FINDING: at the deployed m=8 the count is 7·2016 = 14112, giving ~109.84 proven bits (arity8_perFold_soundness, < 2⁻¹⁰⁹) — log₂7 ≈ 2.807 bits BELOW the ~112.6, which does NOT hold at the deployed arity (arity8_error_not_lt_2e112, a both-truth tooth). Complements FriFoldArity (the unique-decoding n²·d reconstruction, whose |Good| < n count needs the degenerate d = 0 radius). ⛑ THE hΦ OBLIGATION IS NOW DISCHARGED (FriArityFiberDischarge): the Prop this file named as the open residual (Arity8FiberBound, now Arity8FiberBoundNaive) was FALSE — it quantified over EVERY phase map with no farness link, so the constant map refutes it (arity8FiberBoundNaive_false); nothing consumed it. Correctly stated over the real |L|=512 dim-8 setup it is a THEOREM. good_card_le_of_phase_injective still CARRIES hΦ because it is arity-generic and mentions no setup; #assert_axioms is blind to hypotheses.
import Dregg2.Circuit.FriArityFiberDischarge   -- THE M=1 FIBER BOUND DISCHARGED AT THE DEPLOYED ARITY 8: FriArityTransfer.good_card_le_of_phase_injective (which every FriLedger perFoldBits reads) carries the M=1 fiber bound as the HYPOTHESIS hΦ, discharged only at arity 2/logBlowup 6 (FriCorrelatedAgreementSharp §8's far_fiber_card + wrap_fiber_le_one) and OPEN at the deployed arity 8 and every logBlowup=3 config. TWO FINDINGS. (1) The Prop that NAMED the arity-8 obligation was FALSE, not open: it quantified over EVERY Φ with no link to a far word, so Φ = 0 refutes it (FriArityTransfer.arity8FiberBoundNaive_false); it had no consumers, so nothing was contaminated — but it named no obligation. (2) With the farness link restored the bound DISCHARGES. far_fiber_card_arity generalizes far_fiber_card to arity n (a coefficient vector IS a codeword via unfold_closed at the constants; each fibre contributes all n of its domain points to that codeword's agreement by self_decomp + reps_injective; farness caps it): n·|Φ⁻¹(a)| + dOut < |L|. friSetupK builds the arity-2^k rate-2^(−b) RS setup PARAMETRICALLY (|L| = 2^(k+b), |κ| = 2^b, dim 2^k) — the setup FriArityTransfer said the tree does not build, of which the deployed |L|=512/|κ|=64/dim-8 rate-1/64 is one instance — and phase_injective_of_far PROVES hΦ from farness whenever dOut ≥ |L| − 2n. ALL SIX shipped configs are covered by four (k,b) instances: ir2LeafWrap (arity 8, lb 6, dOut≥496, arity8_phase_injective), ir2LeafWrapRotated (2, 6, ≥124), ethWrapOuter/recursion (2, 3, ≥12), prodV1/zk (8, 3, ≥48). NON-VACUOUS: fPowK (the degree-2^k monomial, agreement ≤ 2^k by a monic root count) is (2^(k+b)−2^k−1)-far, and phase_injective_fires proves the window is non-empty at EVERY (k,b) — at the deployed config fPow8Wrap is 503-far ≥ 496. ANTI-MIRROR: at n=2 far_fiber_card_arity IS far_fiber_card's 2·|Y| + dOut < |L| (arity2_recovers_far_fiber_card), and the counts 2016/14112 are unchanged. PAYOFF: arity8_good_card_le_unconditional / arity8_perFold_soundness_unconditional — the deployed ~109.84 bits with NO hΦ. Corrects FriArityTransfer's window upper end 504→503 (farN is strict). This removes a HYPOTHESIS; it does NOT move the number (still ~109.84, not ~112.6), and perFoldBits remains ONE FACTOR of the soundness product — the query ledger is the other, never multiplied into a headline.
import Dregg2.Circuit.FriJohnsonRadiusGap   -- ⚑ THE M=1 DISCHARGE DOES NOT REACH THE RADIUS FRI OPERATES AT: FriArityFiberDischarge.arity8_phase_injective discharges hPhi only for dOut >= 496 of 512 (96.9% farness); FRI's proven soundness argument runs at the JOHNSON radius delta_J = 1 - sqrt(rho) = 7/8, i.e. dOut = 448. The band [448, 496) is the whole cheating-prover regime and the discharge does not cover it. THE VERDICT IS NOT 'OPEN' — IT IS FALSE THERE: fPow2N (the 2n-th power monomial, x |-> (omega^x)^16 at the deployed config) is constant 1 on BOTH fibre 0 and fibre 2^(b-1), so its phase map is NOT injective (fPow2N_phase_not_injective), and it is 495-far — exactly ONE below the 496 threshold (deployed_discharge_threshold_tight), hence also 448-far (deployed_M1_false_at_johnson). So the 496 hypothesis is the exact TRUTH BOUNDARY, not proof slack, and no argument can establish M=1 at Johnson. The gap is STRUCTURAL, not numeric: the threshold 1 - 2^(1-b) exceeds Johnson 1 - 2^(-b/2) exactly when b > 2 (two_n_over_domain_sq_lt_rate, proved by SQUARES over any ordered field so no irrational sqrt is needed), and EVERY shipped config has logBlowup in {3,6} — raising blowup WIDENS the gap. WHAT IS TRUE AT JOHNSON: far_fiber_card_arity degrades rather than vanishing — M <= 7 (johnson_fiber_le_seven), the same M=7 the arity-2 file already reports at ITS Johnson radius (FriProximityGapWitness.wrap_fiber_le_seven at dOut=112 of 128). good_card_le_of_phase_injective cannot consume M=7 (H y - H z is ZERO for same-phase pairs, so the root count says nothing); good_card_le_of_phase_fiber_le is the honest replacement, an ORDERED-pair double count over pairs of DISTINCT phase giving |Good|*(s^2 - M*s) <= (m-1)*|kappa|*(|kappa|-1). ANTI-MIRROR: it recovers 14112 EXACTLY at M=1,s=2 (fiber_le_recovers_arity8_count) — same model, greater generality. At Johnson the honest instantiation is M=7, s=8 (s = |kappa| - dIn = 8 = sqrt(rho)*|kappa| is the folded code's OWN Johnson agreement floor, the FriCorrelatedAgreementSharp §5 wall; s > M by EXACTLY ONE) giving |Good| <= 3528 (arity8_johnson_good_card_le) => ~111 bits. ⚑ THE 111 IS NOT AN UPGRADE OF THE 109: it is a WEAKER claim (bounds only challenges folding to within dIn=56, where 109 bounds the far larger dIn=62 family) at the RIGHT radius. Different objects; neither dominates; the ledger now carries both, separately. ⚑ THE METHOD LESSON: arity8_phase_injective is kernel-clean, sorry-free, non-vacuous and TRUE, and it still did not cover the regime the ledger quoted it for — #assert_axioms is blind to HYPOTHESES, and only reading the STATEMENT's reach finds that.
import Dregg2.Circuit.FriLedgerSound   -- THE PARAMETRIC, EXPORTED FRI SOUNDNESS LEDGER: `Dregg2.Circuit.FriLedger.friLedger` is a COMPUTABLE FriParams -> Ledger (arity / foldedDomain / goodCount / perFoldBits / johnsonBits / capacityBits), `@[export dregg_fri_ledger]`-ed so Rust CALLS the model instead of re-typing its formulas (the hand-written twin `circuit/tests/fri_params_soundness_budget.rs` used to be is DELETED; `circuit-prove/tests/fri_params_soundness_budget.rs` now reports these numbers). This file JUSTIFIES the exported numbers: ledger_perFold_soundness is ONE parametric theorem (instantiating FriArityTransfer.good_card_le_of_phase_injective at each config's arity m = 2^maxLogArity and folded domain |k| = 2^logBlowup) that every shipped config is an INSTANCE of - no headline number reused across configs. PINS the thin core defs to the modeled ones (ledgerP_eq_babyBearP, chooseTwo_eq_choose_two, log2_eq_log_two) so the export is not a second model. FINDINGS: the deployed IR-v2 wrap reads 109 (not 112); the gnark ETH-wrap's outer shrink is arity-2 at logBlowup=3, so it reads 118 and the standing "~112.6 applies at the ETH wrap" was quoting the arity-2 number from the logBlowup=6 config (ethWrap_is_not_the_112_config); the rotated ir2_leaf_wrap_config (arity 2 at logBlowup 6) is the ONE shipped config ~112.6 describes. NAMED OBLIGATION: every per-fold number carries the M=1 fiber bound as the per-config HYPOTHESIS hPhi; #assert_axioms is blind to hypotheses. ⚑ eps_C MECHANIZED + THE RADIUS NAMED (2026-07-15). TWO repairs. (1) `johnsonBits` = numQueries*logBlowup/2 + powBits is the m->infinity IDEALISATION of BCIKS20 Thm 8.3 and DROPS the commit-phase error eps_C. `FriLedger.friCommitLedger` now computes it as a real PARAMETRIC column (`commitBits`), transcribed verbatim from BCIKS20 Lemma 8.2/Thm 8.3 (eprint 2020/654, printed pp.40-41) and PROVED to bound it: `epsC` states the paper's expression as a real number and `ledger_epsC_soundness` proves epsC < 2^(-commitBits); `rate_rpow_three_halves` pins that our rho*sqrt(rho) IS the paper's rho^{3/2}. Every rounding rounds eps_C UP (bits DOWN), and the numerator carries no Nat division (which truncates DOWN and would inflate the bits). ⚑ eps_C ∝ |D0|^2/|F| so the proven posture is NOT TRACE-INVARIANT and NOT an FRI-knob function: commitBits reads 71 at the measured cost FIXTURE (|D0| = 2^12) and 61 at (recursionConfig, 2^19) (ledger_commitBits_at_measured_heights). ⚠ CORRECTED 2026-07-20 (FriDeployedHeightPairing): (recursionConfig, 2^19) IS NOT THE DEPLOYED PAIR — the accumulator's running fold binds ir2_leaf_wrap_config() (logBlowup 6) together with WRAP_LOG_CEIL's 2^16 floor in ONE prove call, so the deployed domain is 2^(16+6) = 2^22, and create_recursion_config (lb 3) is not on that path; DEPLOYED_WORST_LOG_D0 = WRAP_LOG_CEIL + RECURSION_FRI_LOG_BLOWUP = 19 adds one path's trace floor to the OTHER path's blowup. The deployed reading is 51 (deployed_wrap_commitBits), NOT 61 and NOT the 57 of PROVEN-120-CONFIG.md §3 (which derives 2^22 in its own sentence and then reports the 2^19 number — deployed_wrap_is_not_61 / deployed_wrap_is_not_the_proven120_number / the_proven120_correction_is_half_applied). The fixture flattered us by 20 bits, not ~10, and neither perFoldBits nor johnsonBits can see the trace at all. 73 was never a proven FRI bound. ⚑ THE CEILING, MECHANIZED: eps_C's formula contains NO numQueries and NO powBits, so query_and_pow_cannot_pass_epsC shows q=200/pow=27 balloons the query columns (Johnson 73->627, capacity 130->1227) and moves commitBits not one bit — which is what WITHDRAWS 'proven 128 at ext-degree 4'; only extDeg moves it, at log2 p ~ 30.91 bits/degree since eps_C ∝ 1/p^extDeg. three_columns_three_dependencies vindicates the two-column law QUANTITATIVELY: the trace moves commitBits and nothing else, the queries move johnsonBits and not commitBits — no product could track both. Wire is now NINE in (+logD0 +bciksM +commitPow, deliberately NOT FriParams fields) / TEN out (+commitBits +commitPowBranch +johnsonBitsAtM +compositeBits -- so the eq.(20) COMPOSITE is readable by the caller who sets the knobs, not only provable in Lean), fail-closed on the old 8-field and 6-field wires, on m<3 (BCIKS20's own hypothesis), and on commitPow>30 (where plonky3's grind asserts out, so the number would describe an object no prover can produce). No native_decide: commitBits_eq_of_bracket keeps ofReduceBool out of a soundness column's trust base. (2) perFoldBits' RADIUS is now named in its field doc: its hPhi is discharged only at 96.9% farness while FRI runs at Johnson 87.5%, where M=1 is FALSE — see FriJohnsonRadiusGap. ⚠ NAMED, NOT SWEPT: BCHKS25 (ECCC TR25-169) improves the Johnson gap O(n^2)->O(n), attacking exactly the |D0|^2 that binds, but does NOT restate a full FRI soundness theorem — composing it into BCIKS20's round structure is an unpublished ASSEMBLY and is refused here; and plonky3's commit_proof_of_work_bits (fri/src/config.rs:18) is in NO column and not in FriParams, so the ceiling theorem is scoped exactly to numQueries/powBits and says nothing about it. ⚑ THAT GAP IS NOW CLOSED — see `Dregg2.Circuit.FriCommitPow` on the next line, and note it OVERTURNS two claims above. (a) commit_proof_of_work_bits IS implemented on the path dregg runs (prover grinds per fold round after the round commitment is observed and before beta is drawn, fri/src/prover.rs:224; verifier checks per round with a witness-count pin, verifier.rs:222) and all ten dregg sites set it to 0. It is worth +commit_pow bits on the COMMIT branch — a CORRECTION, not an inflation, because johnsonBits already added its own grinding bits, so the two branches of the eq.(20) min were being compared in DIFFERENT CONVENTIONS. (b) ⚑ It is HARD-CAPPED AT 30 BITS: `grind` asserts (1 << bits) < F::ORDER_U64 over a single base-field witness, so BabyBear caps BOTH pow knobs — a fact NO prior doc records, and one that caught a real fake-green (a cpow=34 config that read 100 and no prover can produce). Every config theorem now carries <= maxGrindBits. (c) ⚑ THE 50 WAS A UNITS BUG: the min composed the commit branch at m=7 against the Johnson branch at m=INFINITY, while BCIKS20 Thm 8.3 has ONE proximity parameter in BOTH terms — so min{51,73}-1 = 50 is not eq.(20) at all. Read at a single m the deployed posture is 57. That corrects a number; it does not rescue a posture. (d) ⚑ SO "only extDeg moves it" IS WITHDRAWN: at the deployed geometry commit-PoW buys +10 and then SATURATES (57->67) because the Johnson branch takes over the min — proved at 16, 32 and 64 bits of grinding. >=100 was believed reachable with NO ember-gated step (extDeg 4, lb 2, q 110, arity 8, commit_pow 28, |D0|=2^17) -- ⚑ WITHDRAWN 2026-07-28 WHEN THE FLIP WAS ATTEMPTED: lb 2 is BELOW the deployed AIRs' quotient-domain floor (IR-v2's frozen degree budget is 8, the Poseidon2 chip 7, the recursion AIR 7 => log_blowup >= 3), and the failure is not a panic but an INVALID PROOF (p3-fri re-interpolates and the verifier then rejects at the degree-7 chip -- measured, circuit-prove/tests/fri_hundred_bit_cutover.rs). The wrap trace was also MEASURED off a real proof at 2^16 NATURAL (degree_bits [10,9,16,14,16]), not the 2^15-padded-to-2^16 the doc assumed, so |D0| is 2^19 at the lowest LEGAL blowup and never the 2^17 the row needed. What survives is ext4_cannot_reach_100_at_any_runnable_blowup_and_the_measured_wrap_height: at extDeg 4, lb 3, |D0| = 2^19, the composite is <= 97 FOR EVERY q AND pow even at the 30-bit grind cap, because eps_C contains neither; ext4_reaches_100_only_if_the_wrap_trace_shrinks_to_2e14 shows 100 IS reached at a 2^14 wrap trace, so the ceiling is a fact about the HEIGHT (a recursion-circuit change) and not an impossibility. ⚑ The generalisable lesson: maxGrindBits was a runnability guard that WAS checked, and the file then read as though runnability had been handled -- a checked guard does not imply the unchecked ones. >=128 is reachable at extDeg 8 where the PROVER GETS CHEAPER (0.62x, the domain shrink outrunning the degree cost) but the flag day includes an ember-gated fresh Groth16 setup. The narrow claim that SURVIVES is exactly: extDeg 4 cannot reach 128 at any RUNNABLE setting. ⚠ gnark caveat: at the ETH wrap this is NOT a knob turn — the in-circuit verifier passes a hardcoded witness of 0 (fri_verify.go:117) and the fixture test fails closed on CommitPowBits != 0.
import Dregg2.Circuit.FriCommitPow   -- ⚑ THE UNPRICED KNOB, PRICED. Closes the "says nothing about it" gap the line above names: commit_proof_of_work_bits enters the COMMIT branch (not the query branch), is capped at 30 bits by `grind`'s single-base-field-witness assertion, saturates at +10 at the deployed geometry once Johnson takes over the min, and — with the m-mismatch corrected — puts >=100 within reach of pure knob turns and >=128 within reach of the extDeg-8 flag day at 0.62x prover cost. Measured grind cost (circuit/tests/commit_pow_cost_measure.rs): 0.85M-7.9M whole-machine trials/s, so cpow 16 ~ 1s, 24 ~ 5min, 28 ~ 26min, 30 ~ 5.6h; verifier-free, +4 bytes per fold round. ⚠ ONE UNRECONCILED NUMBER, FLAGGED NOT SWEPT: FRI-BOTH-WIN-LEVERS.md §4.4 prices commit_pow 0->16 at +1.45 bits where this module proves +10; the saturation MECHANISM agrees, the POINT does not, and the module argues 68 is forced by the tree's own exported johnsonBits=73 at m->infinity plus a finite-m penalty bounded by ~4.2 bits, with PROVEN-120-CONFIG.md independently corroborating (2.961 vs 2.97 bits/query). Noted in both docs rather than resolved by fiat.
import Dregg2.Circuit.FriDeployedHeightPairing   -- WHICH (config, |D0|) PAIR IS DEPLOYED — and the refutation of the two numbers in circulation. friCommitLedger is parametric in the config AND the trace-derived logD0 precisely so that pairing a config with ANOTHER config's height cannot happen; it happened anyway. THE DEPLOYED PAIR, read off the Rust: Accumulator::accumulate binds config = ir2_leaf_wrap_config() (ivc_turn_chain.rs:1028 -> create_recursion_config_for_inner_fri(IR2_INNER_LOG_BLOWUP = 6), whose log_blowup reaches MyPcs::new — the PCS that MINTS the proof) and fold_params = wrap_params() (accumulator.rs:242, min_trace_height = 1 << WRAP_LOG_CEIL = 2^16) in ONE prove call, so |D0| = 2^16 * 2^6 = 2^22 at the arity-2 lb-6 knob set (ir2LeafWrapRotatedConfig; arity 2 because INNER_FRI_MAX_LOG_ARITY = 1). create_recursion_config (lb 3) is NOT on that path. TWO ERRORS THAT AGREED WITH EACH OTHER (which is why the pair looked coherent): FriLedgerSound.ledger_commitBits_at_measured_heights row 4 uses recursionConfig, and fri_trace_height_measure.rs:133 computes DEPLOYED_WORST_LOG_D0 = WRAP_LOG_CEIL + RECURSION_FRI_LOG_BLOWUP = 19 — one path's trace floor plus the other path's blowup (and it is a sum of two compile-time constants, NOT the measurement its prose calls it). deployed_wrap_commitBits: the corrected reading is 51. REFUTATIONS: deployed_wrap_is_not_61 (the tree's standing figure; the_61_is_the_recursion_config_reading shows 61 is TRUE of recursionConfig@2^19 — a mis-pairing, not bad arithmetic) and deployed_wrap_is_not_the_proven120_number (PROVEN-120-CONFIG.md §3 derives 2^16*2^6 = 2^22 in its own sentence then reports the 2^19 number; the_proven120_correction_is_half_applied exhibits BOTH readings of the SAME config so only logD0 moves). the_fixture_gap_is_twenty_bits: COMMIT_FLOOR_BITS = 71 flatters by 20 bits, not the ~10 its docstring claims. E2 DE-RISKED: arity_flip_does_not_move_the_commit_column — arity enters eps_C only via sumL in the dominated second term, so the 51 survives the pending INNER_FRI_MAX_LOG_ARITY 1->3 flip, while the_flip_moves_perFold_only shows perFoldBits moves 112->109 — the two-column law again, at the deployed pairing. the_commit_column_binds_at_the_deployed_pairing: min{51, johnson 73} - 1 = 50, and the Johnson column is 22 bits of unspendable slack (query_and_pow_cannot_pass_epsC). ⚑ SCOPE, STATED IN THE FILE: every theorem here is ARITHMETIC about friCommitLedger's own formula. NONE is quantified over an adversary — no prover strategy, no interaction, no ROM query bound, no efficient/Q-bounded epsilon anywhere; commitBits is floor(-log2) of a transcribed upper bound on BCIKS20's eps_C from a paper whose body this tree could never fetch, and FriLdtExtractV3 (the guarantee the apex consumes) remains ASSUMED. Kernel-clean here means the arithmetic is right, never that a security claim is discharged. Mutation-canaried (one digit reds four theorems); two-sided brackets, no native_decide.
import Dregg2.Circuit.FriWeightingTransfer   -- THE WEIGHTING TRANSFER IS [Sta25]-FREE — the honest path past the 61-bit commit-phase ε_C, MAPPED NOT EARNED (07-16: the named residual was VACUOUS; fixed + Lemma 7.6 mechanized; bits NOT banked). The deployed commit posture (~61-64) binds because BCIKS20's ε_C carries a term ∝ |D⁰|²/|F| (the O(n²) exceptional-set bound); BCSS25 (ECCC TR25-169 = eprint 2025/2055) improves that to O(n) up to Johnson, which would make ε_C LINEAR in |D⁰|. FriLedger refused to bank it because the WEIGHTED correlated-agreement theorem FRI's round analysis consumes (BCSS25 Cor 4.4) is derived from Thm 4.3, which BCSS25 obtains "by plugging improved bounds into [Sta25, Thm 22]" — a PERSONAL COMMUNICATION. THIS FILE PROVES THE [Sta25] DEPENDENCY IS ELIMINABLE FOR FRI. The route (verified vs primary sources): BCIKS20 §7.1 derives its WEIGHTED curve theorem from the UNWEIGHTED one plus ONE structural fact — co-curvilinearity on a large S′ (its Prop 5.5); the transfer itself is BCIKS20 Lemmas 7.5/7.6, elementary double-counting (§7.2, public). BCSS25 §3.2 Step 4 STATES that co-curvilinear S′ with the IMPROVED O(1)-in-n-at-fixed-rate Z-degree interpolant (making S′ larger) — but does NOT prove it: §3.2 is explicitly "a brief summary of the steps from [BCI+20, Section 5]", and Step 4's conclusion is "the major part of the proof, comprising sections 5.2.5 – 5.2.7 as well as Appendix A of [BCI+20]" — a Hensel lift over an ALGEBRAIC FUNCTION FIELD, which BCSS25 calls "quite technical". BCI+20 is public, so the [Sta25]-free headline SURVIVES; the obligation is just far larger than "15pp of bookkeeping". So the weighted bound FRI needs is reachable by feeding BCSS25's improved S′ into BCIKS20's public weighting transfer — the [Sta25]-backed Thm 4.3 (adversarial agreement sets) is STRONGER than FRI needs. MECHANIZED HERE: BCIKS20 Lemma 7.5 in full — curveAgree_forces_pointwise (the polynomial-roots core: curves agreeing at a point for > l challenges force pointwise equality, since ∑ zʲ(uⱼ−vⱼ) is degree-≤l with >l roots) + weighting_transfer_double_count (the double count α·|S′| ≤ W(D′)·|S′| + (W_tot−W(D′))·l) + weighting_transfer_bound (µ(D′) ≥ α − l/(|S′|−l)). weighted_agreement_of_coCurvilinear composes it from CoCurvilinearity. ⚑ 07-16 FIX: CoCurvilinearity WAS VACUOUS — it quantified ∃v over ARBITRARY words with the codeword constraint only in a docstring aside, so v:=u proved it outright (coCurvilinearity_unconstrained_is_vacuous) and "discharging" it would have earned ZERO bits. It now carries ∀j≤l, IsRSCodeword pt k (v j), and non-vacuity is PROVED not asserted (coCurvilinearity_has_a_falsifier exhibits a u where it is FALSE). NEW: BCIKS20 Lemma 7.6 MECHANIZED (OnGrid/grid_round/weighting_transfer_rounded) — the grid rounding that removes 7.5's loss, previously only prose; given CoCurvilinearity, FRI's grid weights now yield µ(D′) ≥ α EXACTLY. SCOPE, AUDITED: the public route closes for the DENOMINATOR-BOUNDED weights FRI uses (µ⁽ⁱ⁾ = subtree-acceptance, denominator |D⁰|/|D⁽ⁱ⁺¹⁾| — on the grid, so Lemma 7.6's rounding fires) — NOT for full Cor 4.4's arbitrary real weights (no grid ⟹ Thm 4.3 genuinely needed). THE BITS, HONESTLY: recomputed with the linear ε_C, composed via ethSTARK eq.(20), the deployed wrap moves ~64→~71 (+7), leaf ~70→~72 (+1.7), outer ~66→~71 (+5.5) — NOT the +17…+31 the naive log₂|D⁰| suggested, because once ε_C clears the query column the min in eq.(20) is set by ζ−s·log₂α≈72, and the arity-8 degree-7 CURVE needs multiplicity 2h where the binary line needs h. RESIDUAL (public, no personal comm, but LARGE): mechanize BCI+20 §5.2.5–5.2.7 + Appendix A (Hensel lift over the algebraic function field K[X,Y]/H(X,Y), K=F_q(Z)) PLUS BCSS25 §3.2's improved bookkeeping so Step-4's S′ clears |S′| ≥ (|D⁰|+1)·l. MATHLIB GAP SCOPED: no coding theory AT ALL (no RS/linear codes/distance/Johnson/list-decoding); weightedTotalDegree has a def and 4 lemmas (no _mul_le — the GS degree bound IS that); NO bivariate multiplicity (no multivariate Hasse derivative); NO bivariate root extraction. GS layer alone ~3000–6000 lines, Hensel lift on top. MULTI-MONTH, NOT A WEEK — the +7 is NOT banked and FriLedger's column is UNTOUCHED — the constant reconstruction (scratchpad/BCSS25-COMMIT-DERIVATION.md, checked vs Lemma 3.1/Thm 4.2/§7.2) reproduces Thm 4.2 exactly and pins the linear bound |S| > d·(2(m+½)⁵/(3ρ^{3/2})·n + (m+½)/√ρ·(Wn+1)). #assert_axioms-clean; CoCurvilinearity carried as a HYPOTHESIS (the residual), which the axiom check does not see.
import Dregg2.Circuit.FriProximityBridge
import Dregg2.Circuit.FriBridgeDeployedArity
import Dregg2.Circuit.FriQuerySoundness
import Dregg2.Circuit.AlgoStarkSoundInstance
import Dregg2.Circuit.OodQuotientConsistency
-- StarkSound endgame (2026-07-13): K′ bridge, extraction decoder (link D), proximity (link A),
-- verifyBatch reduction (E), and the carrier-free StarkSound capstones. All lake-verified,
-- #assert_axioms-clean; wired into the root so the whole tree (not just per-file) build guards them.
import Dregg2.Circuit.RlcSoundnessBound
import Dregg2.Circuit.KprimeCompose
import Dregg2.Circuit.LowDegreeUniqueness
import Dregg2.Circuit.RsUniqueDecoding
import Dregg2.Circuit.DeployedUdrRegime
import Dregg2.Circuit.LagrangeExistence
import Dregg2.Circuit.DeployedProximitySoundness
import Dregg2.Circuit.NearDecodesWitness
import Dregg2.Circuit.DecodeAgreesOpenings
import Dregg2.Circuit.StarkSoundDischarge
import Dregg2.Circuit.StarkSoundAssembled
import Dregg2.Circuit.StarkSoundFriLdt
import Dregg2.Circuit.StarkSoundFriLdtCorrected -- ⚑ THE NON-VACUOUS REPLACEMENT APEX. `StarkSoundFriLdt.starkSound_of_friLdtExtract_transferV3` is conditioned on `FriLdtExtractV3` at the deployed args, which `FriLdtExtractDeployed.friLdtExtractV3_makes_verifyBatch_reject_everything` PROVES forces `verifyBatch` to reject EVERY input — so it quantifies over an empty accepting set and has nowhere to go. Sharpest form of that collapse, proved here: `landedPremise_gives_starkSound_for_ANY_hash_and_registry` (the landed premise alone, with NO Poseidon2 CR and NO descriptor, yields `StarkSound hash' R` for EVERY hash and EVERY registry). REPLACEMENTS over the corrected bundles: `starkSound_of_friLdtExtractCons_transferV3` (from `FriLdtExtractV3Cons`), `starkSound_of_friLdtExtractFaithful_deployed` (from `FriLdtExtractV3Faithful`, the bundle indexed by the predicate `verifyBatch` actually evaluates), `starkSound_of_noOodShape_transferV3` (from a premise mentioning NO OOD shape at all). RELATION: ⚑ STALE AS OF 2026-07-27 (6b5bf4433) — `landedPremise_imp_correctedPremise` and `starkSound_of_friLdtExtract_transferV3_via_corrected` are DELETED, along with `merkleRecomputeZ_binds`, `commitmentOpening_binds_of_poseidon2CR`, `friLdtExtractV3_imp_cons` and two sibling receipts. They transported OUT of `FriLdtExtractV3` — a premise this very module PROVES makes `verifyBatch` reject every input — so nothing ever transported through them; the converse never existed (`bundles_are_not_interchangeable`). ⚑ AND THE APEX IS NOW FLOOR-FREE: `algoStarkSound_transferV3_cons`, `starkSound_of_friLdtExtractFaithful_transferV3` and `StarkSoundFriLdt.starkSound_of_friLdtExtract_transferV3` take NO floor hypothesis at all. They rest on the per-instance residual `OodCommitmentBinding.OpeningColl` (both poles proved — an opening that does not equivocate discharges it for EVERY sponge/index/path with no hypothesis, so the honest path pays nothing; and it is refutable, so its negation is not free), priced by the floor-free `merkleOpening_binds_rom` on `KeyedRomFloor.keyedRom_hard` — rung 3, PROVED, no assumption. Nothing adopted rung 1. INHABITABILITY (apex-level, at the DEPLOYED opaque args, with NO hypothesis): `verifyBatch_accept_gives_cons_shape` — every `verifyBatch`-accepting triple ALREADY satisfies the three OOD conjuncts the replacement adds — and `verifyBatch_accept_refutes_singleton` — the landed conjunct is FALSE on those same runs; plus `correctedPremise_iff_noOodShape` (the replacement premise ≡ itself with those conjuncts deleted: zero added strength). NOT CLAIMED: satisfiability of the corrected bundle at deployed args — `cfg*` are `opaque` and the residual conjuncts ARE the undischarged FRI-LDT/Merkle/FS floor, unchanged in content by this repair. sorry-free, #assert_axioms-clean (11 keystones).
import Dregg2.Circuit.FriBatchedOracle -- ⚑ L5 SHARED RUNG — the deployed BATCHED MULTI-COLUMN FRI commitment as ONE reusable oracle shape. MatrixOracle ι c F = the committed LDE matrix (rows = evaluation domain, columns = the c committed trace/poly columns), matching p3_commit::BatchOpening.opened_values: Vec<Vec<T>> (rev 82cfad7, commit/src/mmcs.rs:163-169) opened PER ROW: one verify_batch per commitment opens ALL columns at the sampled row (fri/src/verifier.rs:590-597; in-tree LayerOpening.leaf IS that row, FriVerifier.lean:277). API: row (the deployed opening unit) / col (the per-column word the proximity math consumes) / ofWord/toWord (single-column IS the c=1 special case, round-trip PROVED — the nothing-regresses bridge) / RowAccepts + rowAccepts_iff_col_accepts (one Merkle path opens all columns ⟺ every column spot-checked at the SAME positions) / ColsClose (EVERY column d-close — the multi-column positive-radius decode input) + colsClose_ofWord_iff/colsClose_one_iff/colsClose_of_forall_mem. Deliberately NOT here: the α-RLC input reduction (fri/src/verifier.rs:620-640) — that is the FRI FOLD's input-reduction seam (BCIKS correlated agreement), SEPARATE from the commitment; consumers must name it, never smuggle it into the commitment type. TEETH: cols_genuinely_distinct (the matrix carries strictly more than any single word), rowAccepts_bites (a row sample cannot hide one bad column behind good ones). DeployedTraceExtract §3′ (DeployedFriSampledEmbedding, PositiveRadiusTraceDecode) is retyped onto this shape; R4a/R4b consume it. sorry-free, no axiom, #assert_all_clean (11 keystones).
import Dregg2.Circuit.FriColumnDecode
import Dregg2.Circuit.FriColumnLogExtract -- ⚑ THE STRAIGHT-LINE QUERY-LOG EXTRACTOR (commitment → total column), sub-seam (a) of FriColumnDecode.ColumnDecodeBridge — AND its REFUTATION. Builds the BCS16-style straight-line extractor treeExtract on the RomQueryLog substrate: a PURE FUNCTION of the adversary's own query log (treeExtractComp_queryBounded: QueryBounded 0, never touches the oracle), reconstructing the depth-d Merkle column top-down from (preimage,answer) pairs. PROVED: soundness (treeExtract_log_isTree — whatever it returns is a genuine H-tree of the root, faithfulness discharged free by RomQueryLog.mem_log_answer, NO floating hypothesis), binding (isTree_unique_or_collision — two columns under one root ⟹ H-collision), and THE (a)-PAYLOAD isTree_opening_agrees/log_column_total_and_binds: every opening the DEPLOYED verifier accepts (merkleRecO H i v sibs = root, reusing FriVerifierMerkle's pnode/merkleRecO — NOT forked) reads back the extracted column entry at i%2^d, or exhibits a collision; the getD-with-default reading is EXACTLY FriColumnDecode.decodeColumn's shape (modulo the felt↔leaf-digest layer, NAMED a1). THE ε over a Q-BOUNDED adversary: freshHit_prob_le — conditioned on the whole of what a Q-query adversary learned (cylinder over its own queriedFinset, card ≤ Q), a published node with a NEVER-QUERIED preimage verifies with prob EXACTLY 1/|α| (freshHit_prob_eq_of_fresh, via RomCounting.condProb_fresh_eq — NOT slack); deployed_freshHit_prob_le at |α|=|F|=2013265921 gives (2^d−1)/|F| ≈ 2^-9.9 at logN=21 (union bound condProb_anyFresh_le over one shared oracle, NO independence). ⚑⚑ THE FINDING (partial_commitment_verifies_but_no_total_column): Merkle binding does NOT yield a committed TOTAL column. A QueryBounded 2 adversary hashes the right half but INVENTS the left interior node w un-queried; for EVERY oracle in general position its opening at leaf-index 2 VERIFIES (merkleRecO accepts) yet its COMPLETE log admits NO depth-2 total column (treeExtract = none) — pays no 1/|α|, exhibits no collision, DETERMINISTIC. Positional binding at OPENED positions is all Merkle gives, and positional binding is not a function. So sub-seam (a) does not PRECEDE (b) — it REDUCES to it: the un-hashed remainder is the spot-check gap, (1−μ)^k = FriQuerySamplingBias.epsQueryBias's exponent, NOT a Merkle-binding argument. no_deterministic_extraction (weaker, forgery mode) + partial_commitment (stronger, omission mode) = refutations of increasing force. REMAINS NAMED: (a1) felt↔leaf-digest, (a2) the α-pin (sponge_pair_oracle_bridge, permanent ROM carrier), (a3) root↔verifyAlgo plumbing, (a4)=the finding, (b) proximity untouched. ADDITIVE: FriVerifierMerkle/FriVerifierFS/RomOracle/RomQueryLog/RomCounting imported read-only + untouched; conceptual decodeColumn tie documented, NOT import-load-bearing (stays off the churning StarkSoundReduction chain). sorry-free, no axiom/native_decide, #assert_all_clean (30 keystones).
import Dregg2.Circuit.FriColumnLogExtractHonest -- ⚑ THE HONEST EXTRACTION STATEMENT (wave-2 of the query-log extractor): what treeExtract ACTUALLY yields over a Q-BOUNDED adversary run, adversary-quantified with an explicit ε. The adversary M : OracleComp (α×α) α (List ((α×α)×α)) PUBLISHES a list of claimed interior nodes; anyFreshHit M fires iff some published node verifies (H p.1 = p.2) with a NEVER-QUERIED preimage — the node at which the straight-line extractor has nothing to read. PROVED: (1) anyFreshHit_prob_le — conditioned on the whole of what a Q-query adversary learned (its own queriedFinset), the forgery probability is ≤ (#published nodes)/|α|, quantified over ADVERSARIES not words (route: eval_const_on_own_cylinder makes M.eval/queriedFinset CONSTANT on the own-cylinder, reducing to wave-1's fixed-list condProb_anyFresh_le at ps:=M.eval σ — the adversary-derived list union bound wave-1 named but never stated). (2) no_anyFreshHit_accepted_logged / no_anyFreshHit_find_succeeds — on the COMPLEMENT (prob ≥ 1−ε) every accepted published node has its preimage LOGGED ((p.1,p.2) ∈ M.log H), so treeExtract's per-node find? SUCCEEDS; NON-vacuous — wave-1's partial_commitment witness is exactly an accepted opening whose tree omits a node, the anyFreshHit event this rules out. (3) honest_extraction bundles (1)∧(2); honest_extraction_binds reuses wave-1 log_column_total_and_binds so a COMPLETE published tree yields the total, unique-up-to-collision, opening-binding column FriColumnDecode.decodeColumn consumes (getD…0 shape, modulo a1). (4) deployed_anyFreshHit_prob_le at |α|=2013265921 gives (2^d−1)/2013265921 ≈ 2^-9.9 at logN=21. TEETH: pubGuesser_prob_eq — the guesser's forgery prob is EXACTLY 1/|Bool|=1/2, the length-1 bound realised, so the ε is TIGHT and no degenerate instance is vacuous. NAMED, not papered: the ε prices ONLY the forgery mode; the UN-published remainder (partial_commitment's DETERMINISTIC omission, no 1/|α|, no collision) is sub-seam (b), cost (1−μ)^k — NOT this ε. ADDITIVE: FriColumnLogExtract (wave-1)/FriVerifierMerkle/RomOracle/RomQueryLog/RomCounting imported read-only + untouched; extractor + condProb_anyFresh_le + eval_const_on_own_cylinder + log_column_total_and_binds REUSED not forked. sorry-free, no axiom/native_decide, #assert_all_clean (12 keystones).
import Dregg2.Circuit.StarkSoundColumnCfg
import Dregg2.Circuit.LightClientDeployedGeneral
import Dregg2.Circuit.MerkleAcceptForces
import Dregg2.Circuit.FriTranscriptBind
import Dregg2.Circuit.FriChallengerUnified
import Dregg2.Circuit.ExtFieldChallenge   -- ExtensionFoldWidthResidual CLOSED at the apex: p3's `(f(x)+f(-x))/2 + β·(f(x)-f(-x))/(2x)` runs over all four BabyBear[X]/(X^4−11) lanes, with transcript β, parity, domain inverse, Merkle row, and final coefficient bound by `foldConsistentExt`. `verifyBatch` consumes `verifyAlgoUnifiedFaithfulExt`; `deployedRefines_cfg` keeps its statement through the proved strengthening. Scalar fold = extDeg-one restriction; honest/full acceptance and higher-lane-tamper rejection are executable guards. #assert_axioms-clean.
import Dregg2.Circuit.BatchTablesSingleAir
import Dregg2.Circuit.LightClientFusion
import Dregg2.Circuit.S5Closure
import Dregg2.Circuit.OodRomBound
import Dregg2.Circuit.RlcRomBound -- ⚑ THE SECOND FIAT–SHAMIR CONJUNCT (Λ, the constraint-RLC challenge), adversary-quantified — the ζ half's missing twin. `FriLdtExtractV3` carries TWO FS non-exceptionality clauses; ζ was discharged as a bounded-advantage event by `OodRomBound.ood_nonexceptionality_is_bounded`, while Λ had only a CARDINALITY fact (`rlc_lambda_is_bounded_fs_form`: the bad set is small) with no acceptance event, no prover, no probability. PROVED here on the same recipe (forcing + uniform transport + degree cap): `rlc_lambda_nonexceptionality_is_bounded` — for ANY adaptive strategy `proofOf : Ω → BatchProofData F`, under the named `RomUniformAlpha` (the ROM idealization of the α squeeze, the exact analogue of `RomUniformDerive`), `winProb (verifyAlgoUnifiedFaithful ACCEPTS ∧ the proof's declared RLC challenge ∈ exceptionalSet R) ≤ natDegree R / |F|`; `…_ext` lands the same bound on `verifyAlgoUnifiedFaithfulExt`, the predicate `verifyBatch` actually evaluates; `hLam_clause_bounded_babybear` instantiates at the deployed object `batchResidual (Rfam transferV3 t ζ qp)` — the `FriLdtExtractV3` Λ-clause itself — at `(#arithList transferV3 − 1)/2013265921`. The forcing is `FriChallengerUnified.verifyAlgoUnifiedFaithful_forces_challenges`, and it is LOAD-BEARING, not decorative: `rlc_lambda_bound_FALSE_at_bare_verifyAlgo` exhibits ONE prover strategy over the REAL `deriveTranscript` (ZMod 7, identity permutation) for which `RomUniformAlpha` HOLDS, bare `verifyAlgo` accepts EVERY ω (it never reads `singleAirOpenings`) with the constant exceptional challenge `alpha := 0` — probability 1, exceeding the claimed 1/7 — while the SAME strategy against the faithful verifier is admitted only at ω = 0, probability exactly 1/7, the bound ATTAINED (`rlc_lambda_bound_fires_and_is_tight`). One family, two verifiers, 1 vs 1/7 — and `fixture_arithmetic_teeth_pass` pins that the α binding is the ONLY tooth that can differ (the degree/vanishing/inverse/quotient teeth hold for every ω ≠ 1 whatever challenge is declared), while the HONEST strategy (declaring the transcript's own challenge) is accepted on six of seven runs with the joint event still exactly 1/7 — so §3's bound is ATTAINED at an instance satisfying §3's hypotheses. SCOPE, named not papered: the event is about the prover's DECLARED α (as the ζ bound is about the declared `oodPoint.headI`); equating the extraction's existentially-bound Λ to the declared field is the same unmodeled-plumbing wire the column-layout clause names, and is NOT closed here. sorry-free, #assert_axioms-clean (20 keystones).
import Dregg2.Circuit.FriLdtExtractDeployed -- ⚑ THE SINGLETON-OOD VACUITY AT THE STARK APEX, PROVED AND REPAIRED. `AlgoStarkSoundTransferV3.FriLdtExtractV3` (`:143`) concludes `oodPoint = [ood]` — ONE base felt — on every accepting run. The deployed verifier forces four lanes: `verifyBatch` runs `ExtFieldChallenge.verifyAlgoUnifiedFaithfulExt`, whose conjuncts include `decide (params.extDeg = 4)` and (via `unifiedTranscriptChecks`) `decide (proof.oodPoint = d.ζ)`, with `d.ζ` a `sampleExt … params.extDeg` squeeze of provably that length (`Challenger.sampleN_length`). PROVED here, not asserted: `faithfulExt_forces_oodPoint_length` (accept ⇒ `oodPoint.length = 4`), `singletonBundle_makes_deployed_verifier_accept_nothing`, and the apex consequence `friLdtExtractV3_makes_verifyBatch_reject_everything` — under the LANDED bundle at the deployed cfg args, `verifyBatch` rejects EVERY input, so `StarkSoundFriLdt.starkSound_of_friLdtExtract_transferV3` quantifies over an empty accepting set. REPAIR (landed defs untouched; the wound is bundle-side — `batchTablesCheck` already matches `| ood :: _`): `FriLdtExtractV3Cons` (bare, cons-shaped, implied by the landed bundle) and `FriLdtExtractV3Faithful` (deployed-verifier antecedent; OOD point = the transcript's `d.ζ`, `params.extDeg` lanes, `ood` its head), the OOD reduction chain re-proved at the cons shape (`…_cons`), and `starkSound_of_friLdtExtractFaithful_transferV3` re-derived from the corrected bundle. NON-VACUITY of the repair: `faithfulExt_accept_gives_cons_shape` (acceptance SUPPLIES the corrected conjuncts) + `friLdtExtractV3Faithful_iff_noOodShape` (the corrected bundle ≡ the bundle with those conjuncts deleted — zero added strength) + `deployed_predicate_accepts_pole`/`corrected_ood_shape_inhabited` (a concrete run the apex-facing predicate ACCEPTS, by `decide`, on which the corrected shape holds and the singleton is refuted). Same wound still open at `AlgoStarkSoundGeneral.lean:147`, `FriVerifierFS.lean:112`, `FriDecodedTraceWitness.lean:464`, `OodExtChallengeLayout.lean:617`, `OodColumnLayout.lean:229`, `DeployedRefinesProof.lean:103`, `FriVerifierBridge.lean:185`. sorry-free, #assert_axioms-clean (22 keystones).
import Dregg2.Circuit.ApexOodLaneRepair -- ⚑ THE SINGLETON-OOD WOUND AT THE APEX/SOUNDNESS CHAIN — three more sites, PROVED and REPAIRED (the `FriLdtExtractDeployed` pattern, applied to the general-descriptor apex). SITE 1 `AlgoStarkSoundGeneral.lean:147`: the ∀-d bundle `FriLdtExtract` concludes `oodPoint = [ood]` on every accepting run; it is the FRI floor input of `algoStarkSound_of_memoryLegs`/`_of_memoryFree` and hence of `AlgoStarkSoundKernel`, `AlgoStarkSoundKernelAvail`, `AlgoStarkSoundFanout{MemFree,Memory}`, `KernelConfigSoundness{,Avail}`, `FriDecodedTraceWitness`. PROVED here, not asserted: `friLdtExtract_makes_deployed_verifier_accept_nothing` (assuming the landed bundle, `verifyAlgoUnifiedFaithfulExt` — the predicate `verifyBatch` evaluates — returns `false` on EVERY input) and `friLdtExtract_makes_verifyBatch_reject_everything` (at the deployed `cfg*` args, `verifyBatch vkey pi π = Verdict.reject` for every triple and EVERY descriptor `d`/extracted trace `tr`, so every apex conditioned on it quantifies over an empty accepting set). SHARPER, and independent of the extension verifier: `landed_bundle_conjunct_refuted_on_an_accepting_run` exhibits a run the bundle's OWN antecedent (the bare `verifyAlgo` at `fullChecks`) genuinely ACCEPTS on which `oodPoint = [ood]` is false for every `ood`. REPAIR (landed defs untouched — seven modules consume `FriLdtExtract`): `FriLdtExtractCons` (`oodPoint = ood :: oodRest`, the shape `batchTablesCheck` matches at `FriVerifier.lean:805`), implied by the landed bundle (`friLdtExtract_imp_cons`), plus the general assembler re-derived at that shape — `hood_of_oodColumnLayout_cons`, `algoStarkSound_of_memoryLegs_cons`, `algoStarkSound_of_memoryFree_cons`, `algoStarkSound_transferV3_ofBusModels_cons` — with `BusModelFamily`/`MemoryLegs`/`MemMapFree`/the graduated-shape legs reused unchanged. NON-VACUITY OF THE REPAIR: `acceptsFull_forces_oodPoint_ne_nil` + `acceptsFull_gives_cons_shape` (the bundle's own antecedent SUPPLIES the corrected shape — `batchTablesCheck` returns `false` on an empty `oodPoint`), `friLdtExtractCons_iff_noOodShape` (the corrected bundle ≡ the bundle with the OOD conjunct DELETED — exactly zero added strength), and `acceptsFull_cons_shape_inhabited` (a concrete `decide`-backed accepting run realizing the corrected shape and refuting the singleton). SITES 2/3 `DeployedRefinesProof.lean:103` and `FriVerifierBridge.lean:185`: there the singleton is a HYPOTHESIS of the two tampered-quotient rejection teeth, so the wound is claim-hollowing rather than apex-emptying — `singleton_oodPoint_forces_deployed_reject` / `singleton_tooth_only_bites_already_rejected` prove their hypothesis class lies entirely inside the already-rejected set, i.e. neither landed tooth constrains ANY deployed-accepting batch. Repaired at any nonempty OOD point by `model_rejects_tampered_quotient_cons` and `deployed_rejects_tampered_quotient_cons`, with the landed statements re-derived as the `oodRest = []` instances; for rejection teeth the non-vacuity obligation is hypothesis-inhabitation, discharged by `cons_tooth_hypothesis_inhabited_at_deployed_shape` (an accepting run at the deployed four-lane shape). NOT claimed: satisfiability of the corrected bundle at the deployed `cfg*` args (its remaining conjuncts ARE the FRI-LDT floor and `cfgPerm`/`cfgView`/… are `opaque`). sorry-free, 19 `#assert_axioms` keystones. ⚑ CUTOVER LANDED 2026-07-25 — the parenthetical "seven modules consume `FriLdtExtract`" above is now HISTORICAL: **zero** modules consume it. The three superseded assemblers `AlgoStarkSoundGeneral.algoStarkSound_of_memoryLegs` / `_of_memoryFree` / `_transferV3_ofBusModels` are DELETED (they took the empty bundle in their FRI slot), as is `FriDecodedTraceWitness.decodedLdtLink_of_friLdtExtract` (the only entry into the DEEP-ALI residual, replaced by `FriFsDecodedOodRepair.decodedLdtLinkCons_of_friLdtExtractCons`). `AlgoStarkSoundGeneral.FriLdtExtract` the DEFINITION is retained deliberately as the SUBJECT of the vacuity theorems; its only remaining occurrences in code position are those instruments (`PremiseInhabitability`, `PremiseInhabitabilitySweep`, `ApexOodLaneRepair`, and the two `deleted_*_premise_was_empty` receipts). Consumers now ride `FriLdtExtractCons` with per-site zero-added-strength receipts (`fanout_correctedPremise_adds_no_strength`, `kernelPremise_adds_no_strength`, `kernelAvailPremise_adds_no_strength`, `kernelConfigPremise_adds_no_strength`, `kernelConfigAvailPremise_adds_no_strength`, `memFreeFanout_premise_adds_no_strength`). ⚠ STILL VACUOUS, NOT migrated: the `FriLdtExtractV3` lane — `StarkSoundFriLdt.starkSound_of_friLdtExtract_transferV3`, `AlgoStarkSoundTransferV3.algoStarkSound_transferV3`, `AlgoStarkSoundGeneral.algoStarkSound_transferV3_subsumed`, `FriVerifierFS.friLdtExtractV3_imp_sansFS` and four `StarkSoundReduce` theorems, whose corrected chain `StarkSoundFriLdtCorrected` exists but has no callers. ⛑ **PER-RUN OPENING RESIDUAL LANDED 2026-07-30 — the `∀ d` lane is OFF `Poseidon2SpongeCR`.** `FriLdtExtractCons` now carries `¬ OpeningColl sponge idx topen.constraintEval vCommitted siblings` as a conjunct, at the witnesses its own existential produces — the same conjunct `FriLdtExtractDeployed.FriLdtExtractV3Cons` has carried since 2026-07-28, on the bundle that was left behind (its own source said `NOT PORTED IN THIS PASS ... still buys it with the REFUTED global floor`). Consequences: `algoStarkSound_of_memoryLegs_cons`/`_of_memoryFree_cons`/`_transferV3_ofBusModels_cons` no longer bind `Poseidon2SpongeCR sponge`, and neither does ANY of `AlgoStarkSoundFanoutMemFree` (27 per-effect apexes + assembler), `AlgoStarkSoundFanoutSetField`, or the sponge half of `AlgoStarkSoundFanoutMemory`/`AlgoStarkSoundKernel{,Avail}`/`KernelConfigSoundness{,Avail}`. ⚠ **`friLdtExtract_imp_cons` IS DELETED** — the landed bundle never carried the residual, and restoring the implication would mean assuming non-collision at every opening reaching a common root, i.e. the global Merkle-binding floor in disguise; it transported out of a premise this very module proves empties `verifyBatch`, so nothing reachable was lost. New tooth: `consResidual_iff_opening_functional` (at two values recomputing to the same root, `¬ OpeningColl` is EQUIVALENT to their equality — the conjunct asks only that the run did not equivocate at its own opening; dischargeable by `openingColl_self_false` with no hypothesis on the sponge, refutable by `openingColl_of_constant_sponge`). ⚑ STILL CARRIERS, deliberately: everything binding the OTHER instance `Poseidon2SpongeCR hash`, which is APPLIED by the map-op arm (`mapOpsArm_of_modeler`) and is an unported ENDPOINT — the 8 map-shape fan-out apexes, `algoStarkSound_kernel{,Avail}`, `kernelConfigSound{,Avail}`. If those clear, the ruler moved rather than the tree.
import Dregg2.Circuit.FriFsDecodedOodRepair -- ⚑ THE SINGLETON-OOD WOUND AT THE FS BUNDLE AND AT THE DEEP-ALI RESIDUAL — two more sites, PROVED and REPAIRED (the `FriLdtExtractDeployed` pattern). SITE 1 `FriVerifierFS.lean:112`: `ExtractBundleSansFS` (the ten-conjunct bundle Stage 2's FS ε-theorem is about) concludes `oodPoint = [ood]`; `sansFS_makes_deployed_verifier_accept_nothing` + `extractBundleSansFS_makes_verifyBatch_reject_everything` prove that at the deployed `cfg*` args the landed bundle forces `verifyBatch vkey pi π = Verdict.reject` on EVERY triple. SITE 2 `FriDecodedTraceWitness.lean:464` — ⚑ THE MAJOR ONE: `DecodedLdtLink` is the NAMED DEEP-ALI residual the whole L5·R4b decode assembly reduces to, and it carries the same singleton. `decodedLdtLink_makes_deployed_verifier_reject_close_runs` + `decodedLdtLink_makes_verifyBatch_reject_every_close_run` prove that at the deployed args the landed residual forces `verifyBatch` to REJECT every `ColsClose`-close run — which is EXACTLY the set `DeployedTraceExtract.PositiveRadiusTraceDecode` quantifies over, so `positiveRadiusTraceDecode_decoded`/`_transferV3` deliver `TraceWitnessed` only on runs the deployed verifier rejects: the residual is INERT as landed, not merely unfaithful. HONEST SCOPE (§1): against their OWN antecedent (the bare `verifyAlgo` at `fullChecks`) the singleton is NOT contradictory — bare acceptance forces only `oodPoint ≠ []` (`batchTablesCheck` has `| [] => false`); the emptiness is at the verifier `verifyBatch` runs. REPAIR (landed defs untouched): `ExtractBundleSansFSCons` and `DecodedLdtLinkCons` at `oodPoint = ood :: oodRest`, each implied by its landed form (`extractBundleSansFS_imp_cons`, `decodedLdtLink_imp_cons`); `friLdtExtractV3Cons_imp_sansFSCons` re-runs `friLdtExtractV3_imp_sansFS`'s token-for-token audit gate from `FriLdtExtractDeployed.FriLdtExtractV3Cons`; and the ENTIRE L5·R4b assembly is re-derived from the corrected residual — `positiveRadiusTraceDecode_decoded_cons` / `positiveRadiusTraceDecode_transferV3_cons`, same conclusion at the same `friSetupDeployed`/UD-radius-7340032 instance, via `ApexOodLaneRepair.hood_of_oodColumnLayout_cons`: the singleton was load-bearing NOWHERE. NON-VACUITY OF THE REPAIR: `extractBundleSansFSCons_iff_noOodShape` and `decodedLdtLinkCons_iff_noOodShape` — each corrected bundle is EQUIVALENT to itself with the OOD conjunct DELETED (acceptance supplies it), so the repair adds exactly zero strength and cannot empty a premise; plus `cons_ood_shape_inhabited_at_bare_verifier`, a concrete `decide`-backed accepting run realizing the corrected shape at the DEPLOYED FOUR-LANE WIDTH (`(ood :: oodRest).length = params.extDeg`) and refuting the singleton. Reuses (does not duplicate) `ApexOodLaneRepair`'s `acceptsFull_forces_oodPoint_ne_nil`/`acceptsFull_gives_cons_shape`/`hood_of_oodColumnLayout_cons`. NOT claimed: satisfiability of either corrected bundle at the deployed `cfg*` args (`opaque`; the residual conjuncts ARE the FRI-LDT/DEEP-ALI floor). sorry-free, 13 `#assert_axioms` keystones. ⛑ **PER-RUN OPENING RESIDUAL LANDED 2026-07-30.** `DecodedLdtLinkCons` (and `OodSingletonRepair.DecodedLdtLinkExtCons`, and both `…NoOodShape` twins) now carry `¬ OpeningColl sponge idx topen.constraintEval vCommitted siblings`, threaded in from `ApexOodLaneRepair.FriLdtExtractCons` by `decodedLdtLinkCons_of_friLdtExtractCons`. The whole L5·R4b assembly is therefore OFF `Poseidon2SpongeCR`: `positiveRadiusTraceDecode_{decoded,transferV3}_{cons,extCons,extChallenges}` and `mainAirAcceptF_of_decodedLdtLinkExtCons` no longer bind it, and the two `NOT PORTED IN THIS PASS` comments that named the gap are gone with it. ⚠ **DELETED: `decodedLdtLink_imp_cons`, `decodedLdtLinkExt_imp_cons`, `decodedLdtLinkExtCons_of_decodedLdtLink`** — all three transported a LANDED link (which does not carry the residual) into a corrected one; each transported out of a premise proved to make `verifyBatch` reject every close run at deployed arguments. The corrected extension link is still reached from the corrected BASE link by `decodedLdtLinkExtCons_of_decodedLdtLinkCons`. ⚠ RESIDUAL, named: `OodExtChallengeLayout.lean:707` still discharges the same residual from `Poseidon2SpongeCR` inline, on the LANDED `DecodedLdtLinkExt` — the object the vacuity theorems are ABOUT, deliberately untouched.
import Dregg2.Circuit.OpeningResidualCutoverCheck -- ⚙ BOTH-WAYS TOOTH of the 2026-07-30 OPENING-RESIDUAL cutover, read off the ELABORATED environment on every build. §1 pins 42 declarations that MUST NOT bind `Poseidon2SpongeCR` any more (the fan-out apexes, assemblers and L5·R4b assemblies that shed it when `FriLdtExtractCons`/`DecodedLdtLinkCons`/`DecodedLdtLinkExtCons` took the per-run `¬ OpeningColl` conjunct); §2 pins 16 that MUST STILL bind it — the ones carrying the OTHER instance `Poseidon2SpongeCR hash`, which the map-op arm APPLIES (`mapOpsArm_of_modeler`) and which is an unported ENDPOINT needing a hand extractor. ⚑ The negative half is the point: a port measured only by what CLEARS cannot tell an improved tree from a moved ruler, so if the 16 ever clear without the extractor landing, THIS goes red and names them. Fail-closed both ways — a subject that no longer resolves is an error, not a pass.
import Dregg2.Circuit.FriExtractNonCircular
import Dregg2.Circuit.CommitmentReduction
import Dregg2.Circuit.CollisionReduce
import Dregg2.Circuit.StateCommitReduce
import Dregg2.Circuit.CommitFaithfulRegrounded -- G3 CLOSED at the LIVE surface: the legacy 13-limb/lane-0 tree remains only as a differential diagnostic. RotatedCell/rotatedLimb model the deployed 184-limb layout (authority [24,12..18], full cap+heap groups, lifecycle/epoch/height 29/30/31) and wireCommitR8 publishes all 8 lanes. CH_faithful8 losslessly serializes that tuple only for the scalar recStateCommit API. Kernel, nonce, no-replay, and transfer keystones now reduce to a genuine full AuthorityDigest8Collision, WireCommit8Collision, or outer tree collision; lane-0-only collisions are absent from the live break surface. #assert_axioms-clean
import Dregg2.Circuit.Blake3FloorReduce
import Dregg2.Circuit.DescriptorRefinesReduce
import Dregg2.Circuit.SpongeCollisionShirk -- ⚑⚑ VACUITY CAMPAIGN, B-CLASS REFUTED (07-25): the "reduction-form twin" shape — replace a refuted floor ANTECEDENT with an `OrBreak (SpongeCollision hash) _` CONCLUSION — is NOT a re-grounding. `SpongeCollision` is a GLOBAL existential and the same pigeonhole that refutes `Poseidon2SpongeCR` at BabyBear ESTABLISHES it (`spongeCollision_of_fieldBounded`), so at every deployed-shaped sponge the break branch is unconditionally available: `orBreak_spongeCollision_trivial` proves the dichotomy holds for EVERY good branch `P`, `orBreak_spongeCollision_False` at `P := False`, and `bareDisjunction_is_not_a_regrounding` states it as `↔ True`. Quantified over `P`, so it refutes the WHOLE B-class (103 sites at this commit by `scripts/binding_surface_complete.py`) with no per-site inspection. THE REPLACEMENT: `SpongeColl hash xs ys := xs ≠ ys ∧ hash xs = hash ys` — the collision AT THE PAIR IN PLAY — with the bridge `noSpongeColl_of_spongeCR` (old carrier ⟹ new condition, so every port is strictly STRONGER and consumers keep their statements), the S3 form `spongeBinds_of_noSpongeColl`, and `noSpongeColl_self` as the `ConePort` `AppRule` target. This is the `LogCommitRegrounded.noLogColl_of_inj` pattern at the sponge level. TEETH: REFUTABLE (`spongeColl_refutable` fires at a constant hash), SATISFIABLE (`noSpongeColl_satisfiable`), LOAD-BEARING (`spongeBinds_unconditional_false` — drop `hno` and S3 is FALSE), and the CROWN `perInstance_strictly_sharper_than_bareDisjunction`: at ONE AND THE SAME field-bounded sponge the bare disjunction holds with good branch `False` while `¬ SpongeColl` HOLDS at a pair — so the per-instance form carries content the disjunctive form provably cannot. #assert_all_clean (14 keystones).
import Dregg2.Circuit.DescriptorRefinesShirkRefuted -- ⚑⚑ VACUITY CAMPAIGN, THE `descriptorRefines` KEYSTONE MEASURED (07-25): `CircuitSoundness.descriptorRefines` hides `Poseidon2SpongeCR hash` in its DEF BODY, so theorems that merely MENTION it carry the refuted floor with NO binder anywhere in their type — invisible to every binder-keyed ruler, which is why it was the highest-leverage port on the board. THE PORT TARGET THE TREE OFFERED IS DEAD: `descriptorRefines_vacuous_babyBear` (the original holds for EVERY `kstep`, by its refuted antecedent) and `descriptorRefinesR_vacuous_babyBear` (the `DescriptorRefinesReduce` twin holds for EVERY `kstep` TOO, by its break branch) — and `both_hold_at_False_kstep` proves BOTH at `kstep := fun _ _ => False`, a step relation nothing satisfies. A refinement obligation discharged at an unsatisfiable conclusion constrains the circuit in NO way. `descriptorRefines_family_vacuous_babyBear` extends it to the registry-wide family the apex `lightclient_unfoolable` consumes. ⚑ THE TWIN'S OWN MODULE DOCUMENTED THE BREAK BRANCH AS "load-bearing, not decorative" AND CARRIED THE DISPROOF: `descriptorRefinesR_iff_refines` is an `↔` with the vacuous original, and an equivalence cannot remove content one side lacks — read as a compatibility result for a whole campaign window. `equivalent_candidate_is_refused` turns that into the STANDING TEST: any candidate re-grounding provably equivalent to the original inherits its vacuity and is refused BEFORE porting work is scheduled. #assert_all_clean (5 keystones).
import Dregg2.Circuit.StarkSoundReduce
import Dregg2.Circuit.ChainStepNonTautology
import Dregg2.Circuit.ExecutorApplyDifferential  -- P5b: recKExec ⟺ deployed TurnExecutor::apply_effect (selector coverage + Transfer semantic pin)
import Dregg2.Circuit.ClosureReadoutsRealizable
import Dregg2.Circuit.ClosedLogExtractPortCheck -- ⚑⚑ VACUITY CAMPAIGN, TWO OF THE THREE PROP-DEF-BODY KEYSTONES PORTED (07-25): `ClosureAll.ClosedLogExtract` hid `Poseidon2SpongeCR hash` in its DEF BODY — no floor binder anywhere in its type, so every theorem that merely MENTIONED it was a silent carrier and every binder-keyed ruler read it as clean. `#floor_census` Pass 1b (RUN FOR THE FIRST TIME here) is what made the class visible. THE ANTECEDENT WAS DEAD: all 35 rungs in the tree that discharge a `ClosedLogExtract` slot bound it `_hCR` and never used it (the binding they consume is `logHashInjective`, carried inside `StateDecodeLog`), so the port is a DELETION, not a re-grounding — strictly STRONGER, no bridge lemma, no successor theorem beside a surviving vacuous original. PAYOFF beyond the def: `closedLogExtract_emptyTag_false` and `closureReadouts_uninstantiable` shed the `Poseidon2SpongeCR` hypothesis they carried ONLY to feed the deleted antecedent — a refutation resting on a hypothesis FALSE at deployed BabyBear proved nothing, and now both refute with no sponge-CR assumed; `lightclient_unfoolable_live`/`_closed_final_live` shed it too. TEETH, all inside lake's import graph (no `IO.FS`, so no cached-olean theatre): `closedLogExtract_shape` pins the floor-free telescope by KERNEL DEFEQ (re-adding the antecedent is a type error — DEMONSTRATED biting), `#assert_not_depends_on` puts the refuted floor out of proof-closure reach for the def and both refutations, and both no-strength-lost directions are re-derived. The census-level half is `FloorCensus.portedPropBody`, an INVERSE sentinel that fails the build if the floor ever returns to the body. MEASURED, honestly: `closureReadouts_uninstantiable_concrete` still reaches `Poseidon2SpongeCR` — correctly, via `refLogRealization.spongeCR`, which PROVES it at the reference injective sponge; a proof-closure rejector cannot tell "assumes" from "proves where it is true", so that dependency is left NAMED rather than asserted away. MEASURED, `#floor_census` Pass 1b RUN FOR THE FIRST TIME (it needs `import Dregg2` and the root was red): 1861 carriers, of which **346 are INVISIBLE to v1** — 11 prop-body defs (not 3; the census found 8 more, incl. a second family on `HashFloorHonesty.CollisionResistant`), 70 propdef-users (not the ~250 a textual count suggested), 265 embedded. SECOND KEYSTONE, same finding: `CircuitCompleteness.descriptorComplete` — every rung is built by `descriptorComplete_of_satFloor`, which binds the antecedent `_hCR` and never uses it, because completeness CONSTRUCTS its published commitment from the kernel it already has; only SOUNDNESS reads a kernel back out of a commitment, and only that direction can be fooled by a collision. Porting it let `lightclient_complete` — THE COMPLETENESS APEX — shed the hash floor entirely (its one use of `hCR` was feeding that antecedent), so completeness now rests on `StarkComplete` + the per-effect satisfiability rung and NOTHING else. ⚑ THE THIRD, `CircuitSoundness.descriptorRefines`, IS NOT PORTED and its twin `descriptorRefinesR` is NOT a target (`DescriptorRefinesShirkRefuted` proves both vacuous); its rungs discharge by TERM APPLICATION, not `intro`, so "is the antecedent dead?" needs `Tools/ConePort`'s elaborated-term analysis, not a grep — that is the next lane's measurement, stated so it is inherited rather than guessed.
import Dregg2.Circuit.TraceColumnInterp
import Dregg2.Circuit.FieldIntegerLift
import Dregg2.Circuit.FriExtractReal
import Dregg2.Circuit.DeployedRefinesProof
import Dregg2.Circuit.ShieldedTransferStark
import Dregg2.Circuit.ShieldedMerkleRootPin -- WOUND #15 (shielded pool THEFT): the membership merkle_root is PROVER-SUPPLIED (apply.rs:1738 from_serialized_parts(payload.merkle_root,…)), checked against ITSELF, never pinned to the committed commitments_root (state_commit.rs:117) — the gap ShieldedTransferStark.StarkResidual LAUNDERS (member commitment pi.merkleRoot, root unpinned; absent from its §5 floor list). FALSIFIER root_substitution_forges / deployed_admits_but_pin_rejects: the deployed accept (membership vs suppliedRoot, no committed-root conjunct — accepts_independent_of_committedRoot) admits an attacker-tree forgery (a ¬IsCommitted leaf, suppliedRoot ≠ committedRoot) = spend a note never created (theft/inflation, "larger than any birthday collision", DECISION-shielded-redesign-2026-07-20 #15). LAUNDER exposed mutation_breaks_membership vs mutation_test_is_not_the_pin: the Rust forged_membership_root_rejects test (apply.rs:5174, root+=1 w/o reproof) rejects mutation, NOT an attacker proving vs a chosen root — not the pin. FIX SOUND pinned_membership_against_committed (pure: pin ⟹ leaf ∈ committed tree) + pinned_accept_is_committed (under AccumulatorSound hyp, à la SigValid ⟹ genuinely committed) + pin_rejects_root_substitution / pin_closes_the_falsifier. Hash-agnostic (holds for real Poseidon2), non-vacuous #guards. Byte-safe: the Rust pin-check + in-AIR fold (Option-A ring-clearing apex) are the gated deploy, NOT this file. FIRST brick of spend_circuit→Lean; #16 value-link / #17 PQ-commitment / full AIR port = residual. #assert_axioms-clean.
import Dregg2.Circuit.ShieldedValueLinkPin -- WOUND #16 (shielded pool INFLATION): the spent-leaf VALUE is never tied to the amount conservation clears. Deployed apply_shielded_transfer runs verify_stark_with_wide_bindings (apply.rs:1753 — ties value_binding+wide carrier to the leaf value) THEN verify_full_conservation_bytes (apply.rs:1764, value_commitment.rs:490 — Σ C_in=Σ C_out over the PROVER-SUPPLIED Pedersen legs, transfer.rs:247), and NOTHING ties the leg value to the leaf: transfer.rs:196 "This transcript binding does not prove leaf↔leg value equality"; verify_value_link (value_commitment.rs:1655, the leg↔leaf tie) is called ONLY from tests (shielded_transfer_m2a.rs:475). FALSIFIER genuine_note_mints_anything / genuine_note_inflates: a note the attacker GENUINELY owns (Member v) mints ANY amount (a v-note mints v+1) — no forged leaf (that is #15), the theft is purely the missing link (DECISION-shielded-redesign-2026-07-20 #16 "mint by decoupling them"). LAUNDER exposed verify_value_link_forces_link (the check IS the link, under injective Bind) vs link_test_is_not_the_gate: the value_binding IS honestly bound to the leaf and the wide carrier is mandatory + full-u64, and the Rust leaf_leg_value_link test rejects the mismatch — yet conservation sums the SEPARATE free Pedersen leg, and apply never calls verify_value_link. FIX SOUND linked_conservation_over_real_value (link ⟹ minted = real spent) + linked_no_inflation + link_closes_the_falsifier + link_rejects_leg_leaf_mismatch. Hash-agnostic (Bind/Wide abstract deterministic; holds for real Poseidon2), non-vacuous #guards (a real 1-note mints 1_000_000 with conservation passing). Byte-safe: the in-AIR fix is authored in Emit.ShieldedValueLinkDescriptor (Option-A lane 2, NOT yet routed into apply) — routing the deployed transfer through it is the gated deploy, NOT this file. #17 Ristretto/PQ cut + full spend_circuit AIR port = residual. #assert_axioms-clean.
import Dregg2.Circuit.Satisfied2FaithfulDeployed
import Dregg2.Circuit.AirChecksSatisfied
import Dregg2.Circuit.AirLegsDischarged
import Dregg2.Circuit.LogUpSoundness
import Dregg2.Circuit.LogUpMultiset    -- the higher-order-pole MULTISET SZ extension: busBalance_forces_membership_multiset forces support containment with NO Nodup hypothesis (memory logs repeat addresses) — busNum factors as (X+Cc)^(k−1)·G with residue k·∏(−c+aᵢ)·∏(−c+bⱼ) ≠ 0 (rootMultiplicity EXACTLY k−1), ε ≤ (|A|+|B|)/|F| unchanged; subsumes the Nodup theorem (multiplicity-1 specialization) and §5's forged tooth (k=1); no-wrap side condition discharged for any trace shorter than char F (BabyBear instantiation). DISCHARGES the LogUpSoundness §8 repeated-value residual (:473-477). Teeth: forged doubled lookup UNSAT + exact pole order + repeated honest value forced into the table.
-- RETIRED FROM THE APEX: `FinBindsKernel.CH_fin` is a whole-Value toy sponge and
-- `FinInjectivityCollapse` assumes the false bounded-hash injectivity floor.  The historical
-- modules remain available for archaeology, but `Dregg2` no longer imports them as keystones;
-- `CommitFaithfulRegrounded` above is the binding state-commitment consumer.
import Dregg2.Circuit.FinFrameHash     -- THE APEX BACKBONE: lightclient_unfoolable (verifyBatch accept ⟹ ∃ genuine kernel transition) over the v3-registry circuit. StateDecode = the FAITHFUL published-commitment→kernel decode (faithfulness PROVED via recStateCommit_binds_kernel — NO admissibility/frame assumed; cross-step frame DERIVED by stateDecodeChain_frame_continuous). descriptorRefines = the per-effect rung. CARRIED obligations (named, not laundered): StarkSound (audited p3 batch-STARK verify⟹∃Satisfied2 witness — the FRI extraction; minimal honest interface DEFINED here: verifyBatch/vkOfRegistry/VerifyKey), Poseidon2SpongeCR + the CommitSurface CR set, and the per-effect descriptorRefines family. #assert_axioms-clean on every theorem.
import Dregg2.Circuit.Poseidon2BabyBearW16 -- THE REAL HASH, BIT-EXACT: the deployed BabyBear Poseidon2-w16 permutation (default_babybear_poseidon2_16, the ONE permutation the recursion uses for ALL MMCS/FRI/sponge hashing) implemented in Lean over canonical ℕ-mod-p — real round constants (RC_16_EXTERNAL_INITIAL/FINAL + INTERNAL), the MDSMat4 external layer + the (1+Diag(V)) internal shift-diagonal, the x^7 sbox — and VALIDATED bit-exact against KATs emitted from the deployed Rust permute (permute [0..15] + the all-zero state) + the TruncatedPermutation compress. Closes the FriVerifier §6 "fidelity against the REAL hash" gap (the reference-vector #guards there check the Challenger LOGIC over a reverse stand-in; this checks the actual Poseidon2). The merkleRecompute_binds calibration now has a concrete validated referent. Import-free.
import Dregg2.Circuit.FriVerifierBridge   -- THE VERIFIER-OUT-OF-TCB BRIDGE: makes CircuitSoundness.StarkSound a THEOREM (starkSound_of_verifyAlgo) over the SPECIFIED FriVerifier.verifyAlgo instead of the opaque verifyBatch. AlgoStarkSound = the FRI/STARK extraction floor re-stated over verifyAlgo (the IRREDUCIBLE FRI-LDT + Poseidon2-CR floor, a NAMED Prop class — same floor as the apex, now sitting ON TOP of the proven algorithm not swallowing it). DeployedRefines = the deployed Rust verify_batch computes the SAME accept Boolean as verifyAlgo (the analogue of GnarkRefines for the Rust verifier — the SOLE remaining code-trust). starkSound_of_verifyAlgo: AlgoStarkSound + DeployedRefines ⟹ StarkSound (the opaque whole-verifier carrier GONE). lightclient_unfoolable_via_algo rests the apex on the bridge (verifier algorithm OUT of the TCB; only the math floor + the Rust=spec refinement trusted). deployed_rejects_tampered_quotient = the algorithm's PROVEN tooth biting the deployed verifier (a tampered-quotient batch CANNOT be accepted — ruled out by verifyAlgo_full_rejects_tampered_quotient + the refinement, no appeal to the crypto floor). #assert_axioms-clean (carriers/hyps as typeclass/Prop, no new axiom).
import Dregg2.Circuit.FriVerifier         -- THE WRAP-VERIFIER SPEC: lifts the deployed batch-STARK FRI verifier from an OPAQUE verdict (CircuitSoundness.verifyBatch) to a SPECIFIED algorithm, so the gnark/BN254 ETH-wrap circuit (chain/gnark/fri_verifier.go) becomes a LEAN-DERIVED circuit proven to REFINE it (docs/deos/FRI-VERIFIER-PROOF-ENGINEERING.md). KEYSTONE = Challenger, a pure functional model of DuplexChallenger<Poseidon2-w16,WIDTH16,RATE8> (observe/duplexing/sampleBase/sampleBits byte-faithful to p3-challenger; overwrite-first-len + pop-from-end) — the bit-exact Fiat-Shamir transcript fidelity that was the wraps load-bearing unknown. observeList_append = the absorb compositionality law (proven). verifyAlgo = the specified verifier: deriveFri commit-phase betas + deriveQueryIndices (CONCRETE Fiat-Shamir core) AND the three teeth; the per-query FRI-fold/Merkle/quotient/logup checks carried as EXPLICIT FriChecks record fields (specified week-by-week, NOT sorry). CARRIER = FriLowDegreeSound (FRI low-degree soundness, a NAMED Prop class like StarkSound — NOT re-derived). PAYOFF wrap_sound: GnarkRefines (gnark = verifyAlgo) + the FRI carrier ⟹ gnark accept ⟹ ∃ genuine transition — the gnark circuit INHERITS the specs soundness once it refines it; the silent-soundness-break becomes the refinement equality. wrap_sound axiom-free; rests ONLY on the SAME FRI+Poseidon2 floor as the apex, no new crypto assumption. ir2LeafWrapConfig = the deployed knobs (blowup 6/19 queries/16 PoW). MULTI-WEEK: this is milestone 1 (transcript keystone + refinement skeleton); §5 roadmap = fixtures, fold/Merkle/batch checks specified, verifyAlgo→StarkSound bridge, gnark discharge.
import Dregg2.Circuit.FriVerifierO -- THE FAITHFULNESS BRIDGE (FRI-soundness re-basing, Stage 1 — docs/reference/FRI-EXTRACTION-FLOOR-DESIGN.md §5). The deployed verifyAlgo is Bool-valued over a SUPPLIED proof at a FIXED perm; §4.2's fortunate fact is that verifyAlgo is PARAMETRIC in perm (every perm use flows through deriveTranscript), so the random-oracle re-basing is a RE-INTERPRETATION not a rewrite. verifyAlgoO = the oracle image over RomOracle.OracleComp (List F) (List F) (domain=range=the sponge state), each `perm x` lifted to `OracleComp.query x`; the checks : FriChecks bundle (per-query FRI/Merkle recomputes over FriCore.compress, a SEPARATE parameter, not perm) threads UNCHANGED — the mirror had nowhere to differ. ⚑ verifyAlgoO_run_eq = THE FAITHFULNESS THEOREM: (verifyAlgoO …).eval perm = verifyAlgo perm … — running the oracle version against the deterministic perm-oracle recovers the deployed Bool, so the re-basing is CONSERVATIVE (verifyAlgo untouched + recoverable). permCallCount = an explicit arithmetic UPPER bound on the transcript's perm calls read off the proof shape (observe-list lengths + extDeg per squeeze + commit-phase fold + 2 for query-PoW + numQueries); verifyAlgoO_queryBounded : QueryBounded (verifyAlgoO …) (permCallCount …). Substrate glue (additive; RomOracle untouched): obind + obind_eval + queryBounded_obind. PURELY STRUCTURAL (no probability, no crypto — Stages 2–4). ADDITIVE: modifies NO deployed spec/proof (verifyAlgo/deriveTranscript/fullChecks/FriLdtExtractV3 untouched). Stage 2 (the FS-terms ε-bound) consumes verifyAlgoO + the query budget + the faithfulness lemma from here. #assert_all_clean (19 keystones).
import Dregg2.Circuit.FriVerifierFS -- STAGE 2: THE FS TERMS BECOME THEOREMS (FRI-soundness re-basing — docs/reference/FRI-EXTRACTION-FLOOR-DESIGN.md §5 Stage 2). Turns the TWO Fiat–Shamir non-exceptionality conjuncts of FriLdtExtractV3 (Λ ∉ exceptionalSet …, and per-arith-constraint ζ ∉ exceptionalSet …) from ASSUMED into PROVEN-except-with-ε over the RomCounting counting model. (1) ExtractBundleSansFS = FriLdtExtractV3's existential body with EXACTLY the two FS conjuncts removed (the ten kept conjuncts token-for-token); friLdtExtractV3_imp_sansFS is the adversarial-audit gate (the containment holds ONLY because the ten match verbatim — a paraphrase would not destructure/reassemble). (2) ⚑ fs_epsilon_bound = the FS ε-THEOREM: for a Q-attempt oracle adversary over the product oracle H : D → F × Fin(2^pow), Pr[ some derived challenge exceptional ∨ some PoW freebie ] ≤ (Q+1)·deg/|F| + Q/2^pow — a REAL probability bound, NOT a restated ≤1. CRUX = RomCounting.condProb_fresh_eq (a fresh point hits a fixed target with prob EXACTLY 1/|R|); the workhorse condProb_fresh_mem_le unions it over the exceptional set (fresh challenge exceptional ≤ |E|/|R|, the ROM form of OodSoundnessGame.oodNonExc_winProb_le but over a CONDITIONED coordinate), condProb_fresh_family_le unions over the Q+1 transcript attempts (the (Q+1) factor) and Q grinding attempts (Q/2^pow — the grinding accounting as a THEOREM for the first time). fs_epsilon_bound_babybear grounds it at the deployed field via exceptionalSet_card_le (|F|=2013265921). Union-bound layer added on RomCounting: condProb_or_le (Boolean subadditivity) + condProb_exists_le (finite ∃-union). ⚠ HONEST NAMED CARRIER (FRI-EXTRACTION-FLOOR-DESIGN.md §4.5): the freshness hypotheses (fsPt i ∉ S, powPt j ∉ S — the squeeze points are outside the conditioned set) are supplied, NOT yet derived from verifyAlgoO — RomOracle lacks the QueryLog erasure interface that would tie D/R to perm : List F → List F + count attempts to permCallCount. Stage 2's falsifier (§6): if the deployed FS transcript order let the adversary see a challenge before committing to what it must be non-exceptional for, freshness fails — a real transcript bug, a WIN. Two of FriLdtExtractV3's twelve conjuncts move from assumed toward proven; the exceptional-set arithmetic + grinding term are PROVEN, the residual is the freshness bridge (named). Additive: verifyAlgo/FriLdtExtractV3/deriveTranscript/RomCounting/OodSoundnessGame untouched. #assert_all_clean (10 keystones).
import Dregg2.Circuit.FriVerifierMerkle -- STAGE 3: MERKLE BINDING AS EXTRACTION-AS-DATA + BIRTHDAY, and the DISCHARGE of Stage 2's freshness carrier (FRI-soundness re-basing — docs/reference/FRI-EXTRACTION-FLOOR-DESIGN.md §5 Stage 3). (1) ⚑ findCollisionZ = EXTRACTION-AS-DATA over the REAL OodCommitmentBinding.merkleRecomputeZ (node hash sponge [·,·]): a COMPUTABLE … → Option (List ℤ × List ℤ) that walks the recompute of two leaves and returns a genuine sponge COLLISION as data — VCVio's findCollision shape ported to the deployed Merkle recompute. findCollisionZ_sound (some (x,y) ⟹ x≠y ∧ sponge x = sponge y) + findCollisionZ_complete (two DISTINCT leaves at ONE root ⟹ some) — NO adversary, NO probability, NOTHING to falsify. findCollisionZ_none_binds = MERKLE BINDING with Poseidon2SpongeCR DERIVED (none + equal roots ⟹ leaves EQUAL, from path-collision-freeness ALONE, no CR hypothesis); equivocation_extracts_collisionZ turns OodCommitmentBinding's break⟹¬CR into break⟹HERE-IS-THE-COLLISION. (2) ⚑ THE MERKLE ε via birthday_cond: over the finite two-arg node oracle H : α×α → α (the §4.2/§4.5 width-pinned sponge, sponge_pair_oracle_bridge records the deployed sponge [a,b]=H(a,b) embedding), collFinder is a QueryBounded (2·|path|) OracleComp that READS the recompute path (a straight-line query-log extractor); collFinder_equivocation_collWin wins collWin on ANY equivocation, so birthday_cond bounds Pr[query log contains a collision] ≤ (2L·|S|+(2L)²+1)/|α| (merkle_path_collision_prob_le, merkle_equivocation_prob_le). The two Merkle-recompute conjuncts hold except-with-ε in the ROM; the CR class stays only for the concrete Poseidon2 instantiation. (3) ⚑⚑ DISCHARGE Stage 2's freshness carrier — the QueryLog freshness interface (§4.5): queriedFinset M H = the adversary's ACTUAL query set (read off OracleComp.evalLog/queried, queriedFinset_eq_log_image), queriedFinset_card_le : QueryBounded Q M → card ≤ Q (verifier_queriedFinset_card_le_permCallCount ties the count to permCallCount). fs_epsilon_bound_of_log RE-BASES Stage 2's fs_epsilon_bound with S := queriedFinset A H, σ := H: the SUPPLIED fsPt i ∉ S becomes fsPt i ∉ queriedFinset A H — "the challenge squeeze point was NOT queried by the adversary", a CONCRETE fact about the real log with |S| ≤ permCallCount. ⚑ RESIDUAL, NAMED (not faked): the transcript-ordering non-membership fsPt i ∉ queriedFinset A H for the SPECIFIC derived challenges (the squeeze is post-commitment sponge state the adversary cannot have queried before committing — Stage 2's §6 falsifier) + the finite-oracle instantiation (perm : List F → List F vs α×α → α, pinning α to BabyBear = the permanent industry-standard ROM carrier). The interface reduces the carrier to EXACTLY that non-membership, no further. ADDITIVE: merkleRecomputeZ/Poseidon2SpongeCR/verifyAlgo/verifyAlgoO/FriVerifierFS/FriLdtExtractV3/RomOracle/RomQueryFloor/RomQueryLog all untouched. #assert_all_clean (21 keystones).
import Dregg2.Circuit.FriVerifierQuery -- STAGE 4: THE QUERY-PHASE COMPOSITION — the hard, honest stage (FRI-soundness re-basing — docs/reference/FRI-EXTRACTION-FLOOR-DESIGN.md §5 Stage 4). PROVES εQuery: for a fold layer whose committed word is 4d-FAR from the domain code, the fraction of (fold-challenge α, k-query sample Q) ∈ F × (Fin k → κ) that ACCEPTS the folded oracle is ≤ L/|F| + (1−δ)^k (epsilon_query_layer_carried) — the two ledger columns finally COMPOSED into one probability over a shared sample space (soundness_error_compose union bound). ⚑ TWO addends, both REAL: (fold-density) L/|F| = the correlated-agreement / proximity-gap carrier FriProximityGapChallenges S (4d) d L (a 4d-far word has ≤ L folding challenges that fold it back close); (query) (1−δ)^k = FriQuerySoundness.accept_prob_le (a δ-far oracle survives k uniform spot-checks w.p. ≤ (1−δ)^k). ⚑ THE johnsonBits EXPONENT k IS NOW A THEOREM about the (α,Q) randomness, not by norm_num bookkeeping. RADIUS-HONEST (project-fri-soundness-reality): epsilon_query_layer is the FULLY-PROVEN L=1 UNIQUE-DECODING instance (NO carrier hypothesis — proximityGap_uniqueDecoding/good_alpha_subsingleton discharge the fold term to 1/|F|, a theorem); epsilon_query_deployed instantiates δ=7/16, k=38 → ≤ 1/|F| + (9/16)^38 < 1/|F| + 2^-31 (the ~31.5-bit unique-decoding column — the honest PROVEN query leg, deployed_far_survival_lt; NOT the 2^-57 Johnson idealisation). ⚑⚑ DESIGN FORK SURFACED, NOT PICKED: (i) unique-decoding L=1 — fold term 1/|F| PROVEN, query (9/16)^38 PROVEN, NOTHING carried at code level (only the permanent ROM instantiation); (ii) Johnson L>1 δ_J=1−√ρ — fold term L/|F| under the NAMED carrier, query (1−δ_J)^k smaller, the radius the ~112.6 number + the ledger's sharper perFoldBits live at and where M=1 is PROVABLY FALSE (FriJohnsonRadiusGap). The parametric _carried theorem makes (ii) a ONE-hypothesis discharge; this file REFUSES to read the 112-bit number out of the L=1 pipeline (design §6 falsifier (ii)). Composition (b) qidx↔transcript binding: verifyAlgoO_rejects_wrong_query_count/verifyAlgoO_rejects_tampered_quotient LIFT the deployed teeth (:719/:900) through Stage 1's verifyAlgoO_run_eq to the oracle verifier — "all numQueries checks pass" IS the uniform-sample Accepts event (adversary opens EXACTLY the transcript's positions). LEDGER BRIDGE NAMED (ledger_density_is_the_carried_fold_term): ledger_perFold_soundness's 1/2^perFoldBits density composes ONLY through the Johnson carrier; L=1 uses 1/|F| instead, stated not faked. Stage-3 residuals NAMED, not discharged (transcript-ordering freshness needs an ordered-log model RomOracle lacks; finite-oracle instantiation = permanent ROM carrier). ADDITIVE: no deployed spec / earlier stage / FriLdtExtractV3 touched. #assert_axioms-clean (9 keystones).
import Dregg2.Circuit.FriVerifierCompose -- STAGE 5: THE ASSEMBLY, and the honest bottom line (FRI-soundness re-basing — docs/reference/FRI-EXTRACTION-FLOOR-DESIGN.md §5 Stage 5). ⚑⚑ THE STAGE-5 FINDING — Stages 2–3's freshness carrier was the WRONG PREDICATE and is REFUTABLE: challenge_computing_adversary_is_not_log_fresh proves that deriving a Fiat–Shamir challenge IS querying the permutation at the post-commitment sponge state, so EVERY adversary that computes its own transcript — the honest prover included, hence every accepting-proof-finder — has fsPt ∈ queriedFinset A H. fs_epsilon_bound_of_log (Stage 3) is a TRUE theorem whose hypothesis EXCLUDES every adversary the floor is about; log_freshness_premise_false records that it could never have been discharged. NOT a transcript bug (the deployed FS order is fine) — the ε-statement's SHAPE was wrong. ⚑⚑ THE SEAM, CLOSED: the correct ROM argument (BCS16 / lazy-sampling) never claims the point was unqueried — it claims the answer is FRESH AT THE MOMENT OF ITS OWN QUERY (uniform given the strictly-earlier prefix), paying deg/|F| per query. hit_cond : QueryBounded Q M → (∀ d, (E d).card ≤ b) → (∀ d ∈ S, σ d ∉ E d) → condProb (cyl S σ) (hitWin E M) ≤ Q·b/|R|, where hitWin E M H = 'SOME query along M's run receives an answer in that point's target set' — NO freshness hypothesis, NO excluded adversary, true of the honest prover. ⚑ AND IT NEEDS NO NEW ORDERED-LOG DATA: OracleComp's query TREE ALREADY IS the ordered model (in `query d k` the continuation k r IS the suffix; RomCounting.condProb_split conditions on the prefix). The design's asked-for queriedBefore/prefix predicate is unnecessary; the missing piece was the per-query HIT bound, proven by the same induction pattern as birthday_cond. Strictly strengthens Stage 2 (its (Q+1)·deg/|F| arithmetic survives; its hypothesis does not). §2 COMPOSITION: εFri = εFS + εGrind + εMerkle + εQuery = Q·deg/|F| + Q/2^pow + (Q²+1)/|α| + (1/|F| + (1−δ)^k); epsFri_compose proves the disjunction of the four bad events ≤ εFri over ONE SHARED oracle for ONE adversary (condProb_or3_le/or4_le) — a REAL union bound, NO independence assumed (all events of one run against one H, which is exactly the design §6 Stage-5 falsifier's requirement: a coupled adversary cannot violate it). ⚑ epsFri_closed_legs DISCHARGES THREE OF THE FOUR LEGS over that shared oracle with NO supplied ε: hit_cond gives FS + grind, birthday_cond gives Merkle. RADIUS-HONEST: εQuery is instantiated ONLY at Stage 4's PROVEN L=1 unique-decoding line; the Johnson carrier is NOT assumed and the ~112.6-bit number is NOT read out of this pipeline. §3 ⚑ friLdtExtractV3_rom is NOT PROVEN — friLdtExtractV3_rom_of_legs states the design §4.3 target as an implication FROM the four leg bounds, and NAMES the two blockers under hQuery rather than papering them. (a) THE WORD↔PROOF BRIDGE: εQuery bounds Pr[FAR word passes] over ABSTRACT (f, f') in a FriSetup; NOTHING in the tree connects a BatchProofData's committed columns to such an f, nor derives 'bundle fails ⟹ committed word is far'. That bridge IS DeployedTraceExtract.DeployedFriEmbedding (accept_folds, decode_trace) — an explicit HYPOTHESIS STRUCTURE already in-tree, not a theorem, and not lemma-sized: it is the FRI-proximity-to-VmTrace decode. WordProofBridge names the obligation; bundleFail_imp_far_of_bridge is a deliberate ONE-LINER exhibiting that ALL content sits on the hypothesis side. (b) ⚑ A SAMPLING DEFECT DISCOVERED IN DEPLOYED CODE: εQuery's sample space is UNIFORM (α,Q) ∈ F × (Fin k → κ), but the deployed qidx are Challenger.sampleBits = toNat(squeeze) % 2^logN — a modular reduction. sampleBits_modular_bias_real PROVES the reduction is non-uniform when 2^bits ∤ range (counts unequal), and babybear_order_not_divisible_by_two proves |F| = 2013265921 is ODD, so 2^logN ∤ |F| at EVERY shipped logN: the deployed indices are PROVABLY NOT uniform. Bias small (≈2^logN/|F| relative) but NONZERO and NO in-tree theorem accounts for it. Composing εQuery over the oracle needs a uniformity-defect term that does not exist. A DISCOVERY, not a modelling nicety. §4 APEX RE-READ: nodes_union_bound (per-node failure over a SHARED oracle union-bounds to #nodes·ε, no independence) + apex_probabilistic_nodeCarrier (on oracles where NO node fails the DETERMINISTIC NodeCarrier holds, so recursive_sound_from_nodes applies verbatim; that set has measure ≥ 1 − #nodes·ε). So GroundedApex re-reads as '…except with probability ≤ (#nodes)·εFri(Q, params) for any Q-query adversary' — CONDITIONAL on the per-node εFri. THE TREE SIDE IS DONE; the gap is ENTIRELY per-node (§3). §5 ⚑⚑ THE HONEST BOTTOM LINE — CAN WE SAY 'the bits = the Q at which εFri reaches ½'? NO, NOT YET, and it is stated in Lean not prose. epsClosedLegs = εFS+εGrind+εMerkle is proven against ALL Q-query adversaries with NO excluded class (§1's closure earned that) and is a real, non-vacuous, Q-GROWING function (epsClosedLegs_lt_half_example, epsClosedLegs_strictMono_example — the budget dial is real). But epsFri_eq_closed_plus_query + epsFri_exceeds_closed_legs record that εFri = epsClosedLegs + εQuery STRICTLY exceeds the closed legs (εQuery ≥ 1/|F| > 0 always), so any budget read off the closed legs alone OMITS the FRI content — calling it 'the bits' would be the exact laundering project-fri-soundness-reality names. The design §7 sentence requires (a) + (b). PERMANENT HONEST CARRIERS, NAMED: the ROM α-pin / Poseidon2-as-random-function (§4.2 — every condProb/cyl statement lives under it) and, NOT assumed here, the Johnson L>1 correlated-agreement carrier. ADDITIVE: FriLdtExtractV3, verifyAlgo, verifyAlgoO, RomOracle, RomCounting, RomQueryLog, FriVerifierFS/Merkle/Query, RecursiveSoundFromNodes all imported read-only and untouched. #assert_all_clean (22 keystones).
import Dregg2.Circuit.FriQuerySamplingBias -- STAGE 4/5 BLOCKER (b) QUANTIFIED: the uniformity-defect term for the deployed sampleBits query indices (FRI-EXTRACTION-FLOOR-DESIGN.md §5). FriVerifierCompose §3(b) PROVED the deployed qidx = toNat(squeeze) % 2^logN are QUALITATIVELY non-uniform (babybear_sampleBits_not_balanced: 2^logN ∤ |F|=2013265921 since |F| ODD ⟹ unequal buckets) but left the defect UNQUANTIFIED — "NO in-tree theorem accounts for it; composing εQuery over the oracle needs a uniformity-defect term that does not exist." This file supplies exactly that term. ⚑ residue_reduction_prob_le: for n uniform over range N and ANY residue event E (|E|≤m), Pr[n%m ∈ E] ≤ |E|/m + m/N — the first addend is the uniform value εQuery (epsilon_query_layer_carried) already uses, the SECOND m/N = 2^logN/|F| IS the missing defect (babybear_query_bias_le grounds it at the deployed field). Taking E the per-index MISS set upgrades the deployed per-index survival (1−δ)→(1−δ)+2^logN/|F|, so a bias-aware εQuery composes as L/|F|+((1−δ)+2^logN/|F|)^k. Core counting: residueClassCard_le (a residue class of range N has ≤ ⌊N/m⌋+1 elements, by the injection n↦n/m) + residueSetCard_le (summed over E). NON-VACUITY: residue_bias_defect_load_bearing exhibits N=3,m=2,E={0} where Pr=2/3 STRICTLY EXCEEDS the naive uniform 1/2 — the +m/N addend is what a non-dividing m∤N (the deployed regime) FORCES; the bound without it is FALSE. ADDITIVE: FriVerifierCompose/Query, sampleBits, |F| all imported read-only + untouched. #assert_all_clean (7 keystones).
import Dregg2.Circuit.FriQueryBiasSharp -- ⚑ RED-TEAM of the deployed query sampler at DEPLOYED params (counterproof hunt): the sampling-bias defect is δ/|F|, NOT 2^logN/|F|, and the OPTIMAL adversary attains it EXACTLY. ATTACKS FriQuerySamplingBias (reuses it, forks nothing). §1 THE COLLAPSE: |F|−1 = 2013265920 = 2^27·15, so 2^b ∣ |F|−1 for every b ≤ 27 ⟹ babybear_mod_two_pow_eq_one: |F| % 2^b = 1 at EVERY realizable logN — exactly ONE residue class (j=0) is heavy, so an event collects ONE extra preimage in total, not one PER RESIDUE. residueSetCard_le_sharp: |E|·⌊N/m⌋+1 (vs the carried |E|·(⌊N/m⌋+1)); residue_survival_le_sharp: the honest per-index survival is (1−δ)+δ/N. The b ≤ 27 side condition is FORCED not chosen — babybear_no_2adic_subgroup_above_27 (¬ 2^28 ∣ |F|−1, whence BabyBear TWO_ADICITY=27 and no 2^28 domain exists); babybear_mod_two_pow_28 shows the hypothesis FAILS catastrophically one bit past it (|F| % 2^28 = 134217729 ≈ 2^27, HALF the residues heavy), so the sharpening genuinely CONSUMES two-adicity. ⚑ FINDING — the carried term is SOUND but LOOSE by 2^logN, and NOT cosmetically: 2^logN/|F| GROWS with trace height (logN = log_global_max_height, fri/src/verifier.rs:268, extra_query_index_bits()=0), so wired as-is at lb=6,q=19 it would read a query column of 56.886 at logN=20 and 45.283 at logN=27 — an 11.7-BIT COLLAPSE THAT DOES NOT HAPPEN. The sharp term is logN-UNIFORM (deployed_sharp_bound_is_logN_uniform) and costs 9.53e−8 bits. ⚑ THE BOUND IS THE ADVERSARY'S EXACT OPTIMUM, not an estimate: sharp_bound_attained_exactly exhibits N=17,m=16 with the optimal strategy (put the word's AGREEMENT set on the heavy residue 0) succeeding with probability 2/17 = EXACTLY (1−δ)+δ/N at 1−δ=1/16 — EQUALITY, so δ/|F| cannot be improved; adversary_beats_uniform (2/17 > 1/16, the defect is REAL) and codex_bound_not_attained (the carried base overshoots that optimum by >8×). ⚑ VERDICT AT DEPLOYED PARAMS — BOUND HOLDS: deployed_ir2_query_leg_le quantifies over EVERY adversary word (∀ agreement-residue set E with density ≤ 1/8) at the deployed IR-v2 wrap (lb=6 ⟹ Johnson 1−δ=√ρ=1/8, q=19, logN=12) and bounds the all-19-miss probability by 2^(−57)·(1+10^(−7)) against a uniform 2^(−57) — the Johnson QUERY column reads 72.99999990 vs 73, a 9.53e−8-bit cost, twelve orders of magnitude below the ε_C term that BINDS at 71 (composite ~70). TEETH: sharp_term_exceeds_uniform proves the survival STRICTLY exceeds 2^(−57) — the bias is priced, not proved away; deployed_codex_pow_ge proves the carried term overstates the deployed loss by >3000× (4.46e−4 vs 9.53e−8 bits). epsQuery_le_sharp_le_epsQueryBias sandwiches the new epsQueryBiasSharp strictly between the (unsound-over-the-deployed-sampler) uniform epsQuery and the carried epsQueryBias — a TIGHTENING, contradicting nothing. SCOPE (honest): QUERY leg only, under the ROM idealisation that Challenger.sample() is uniform over F (sponge assumption, NOT discharged); says nothing about ε_C, the per-fold column, or blocker (a) WordProofBridge/DeployedFriEmbedding — which remains THE open one. Arity-8 posture unchanged (the fold challenge α has no query sampler in it): 109.84 at 96.9% farness stands. ADDITIVE: FriQuerySamplingBias imported read-only + untouched. #assert_all_clean (19 keystones).
import Dregg2.Circuit.FriCarrierVacuity -- ⚑⚑ THE FRI CARRIER (FriVerifier.FriLowDegreeSound, :995) IS VACUOUS — PROVEN ↔ True. friLowDegreeSound_content_iff_true: its extract field concludes ∃ w : GenuineWitness F, w.exists_ ∧ proof.exposedSegment = pub.segment from verifyAlgo=true, and BOTH conjuncts are free — GenuineWitness (:986) has ONE unconstrained Prop field (⟨True⟩ + trivial discharges it; genuineWitness_exists_trivially) and proof.exposedSegment = pub.segment IS segmentTooth (:700), the SIXTH && of verifyAlgo (:724), so the consequent is a SYNTACTIC CONJUNCT of the antecedent (verifyAlgo_imp_segment). friLowDegreeSoundTrivial CONSTRUCTS the class at ARBITRARY params with no hypotheses ⟹ wrap_sound (:1037) + EmitVerifier.emitVerifier_wrap_sound (:342) conclude ONLY proof.exposedSegment = pub.segment (wrap_sound_conclusion_iff_segment; wrap_sound_needs_no_carrier proves the payoff with the [carrier] binder DELETED). ⚑ THE VACUITY CANARY (reusable): Falsifier acc C = an ACCEPTED instance where the consequent FAILS; falsifier_refutes_implied + implied_has_no_falsifier make 'ship a Falsifier term beside every carrier' a compile-time gate — friLowDegreeSound_has_no_falsifier proves this carrier admits NONE at any params (the machine-checked 'not refutable in principle'). §3 verifyAlgo AT THE EMITTED CHECKS IS BLIND TO THE PROOF: mkChecks/mkVk (EmitVerifier :249) are all CONSTANT, so verifyAlgo_of_constant_checks factors it as const && segmentTooth and verifyAlgo_blind_to_proof_of_constant_checks proves an honest proof and a garbage twin (same segment, ZERO FRI data) get the SAME verdict (garbage_twin_accepted, #guard). §4 THE NAIVE REPAIR IS FALSE BY COUNTING: spotCheck_accepts_non_codeword exhibits a Falsifier for accept⟹codeword; uniform_all_agree_card/far_word_has_accepting_sample: a δ-far word's accepting-sample set is (agree/m)^k > 0. §5 DEPLOYED ARITHMETIC (dregg_outer_config.rs lb=3⟹ρ=1/8, q=38): capacity δ=1−ρ ⟹ (1/8)^38=2^−114 (the config's claimed 3·38+16=130); Johnson δ=1−√ρ ⟹ 2^−57; UNIQUE DECODING δ=(1−ρ)/2=7/16 (the ONLY radius the tree proves, FriVerifierCompose §2) ⟹ (9/16)^38 ∈ (2^−32,2^−31) = ~31.5 BITS. capacity_claim_understates_by_2pow82 + johnson_understates_by_2pow25 price the gap. §6 attempt_union_le: the Q-attempt Bernoulli union bound (PROVEN, Fintype.card_piFinset_const) — the replacement carrier's budget half. ADDITIVE: FriVerifier imported read-only + untouched. #assert_all_clean (21 keystones).
import Dregg2.Circuit.FriCarrierEpsilon -- ⚑ THE REPLACEMENT for FriLowDegreeSound: an ε-BOUNDED, Q-ATTEMPT-QUANTIFIED forgery bound over the DEPLOYED sampleBits sampler, PROVEN not carried. FriQueryForgeryBound N m k Q Emiss ε = 'over Q Fiat–Shamir attempts each drawing k indices as v%m (v uniform over N=|F|), the fraction of runs where SOME attempt's k spot-checks all miss a δ-far word is ≤ ε' — a statement about the MEASURE of acceptance (not implied by a single accepting instance, unlike the old carrier). friQueryForgeryBound_proven derives it from codex's FriQuerySamplingBias.biased_query_survival_pow_le (the deployed non-uniform sampler, defect term m/N included — REUSED, not forked) ∘ FriCarrierVacuity.attempt_union_le: forgery ≤ Q·((1−δ)+m/N)^k. ⚑ REFUTABILITY (the sufficient test the old carrier FAILED): friQueryForgeryBound_false_without_defect knocks the shape over at N=3,m=2,k=1,Q=1,δ=1/2 (actual 2/3 ⊄ un-defected 1/2 — codex's biased_survival_defect_load_bearing witness in the carrier's own shape); friQueryForgeryBound_holds_at_witness shows the defect addend restores truth. ⚑ DEPLOYED: deployed_forgery_bound at |F|=2013265921, k=38, δ=7/16 reads Q·((9/16)+2^logN/|F|)^38; deployed_bound_useless_at_2pow32 + deployed_bound_exceeds_half_at_2pow31 prove it exceeds 1 (resp. 1/2) by Q≈2^31 attempts ⟹ THE HONEST CEILING OF THE DEPLOYED FRI QUERY LEG IS ~31 BITS, not 130, not 57. The 16 query-PoW bits are NOT in the object wrap_sound quantifies over (FriChecks.queryPow = queryPowWitnessShape, a wire-shape check, :822). ADDITIVE: FriVerifier/FriQuerySamplingBias/FriCarrierVacuity read-only + untouched. #assert_all_clean (8 keystones).
import Dregg2.Circuit.DeployedProximitySoundnessSampler -- ⚑ FRI LADDER RUNG L1a — link A re-proven over the ACTUAL (non-uniform) deployed query sampler. DeployedProximitySoundness idealised the 38 queries as uniform over Fin 16; the deployed sample_bits(4) squeezes BabyBear and reduces n % 16, and |F| % 16 = 1 makes residue 0 heavy. far_word_rarely_accepted_deployed_sampler: a 7/16-far word passes the deployed 38-query check on ≤ ((1−δ)+δ/|F|)^38 of the SQUEEZE space Fin 38 → Fin |F| (composes FriQuerySoundness's concrete agreement predicate with FriQueryBiasSharp.biased_query_survival_sharp — no new socket). deployed_sampler_soundness_lt: still < 2⁻³¹ (bias inflation ~1.5e−8 sits inside the 2⁻³¹·⁵ → 2⁻³¹ margin). deployed_sampler_fires: FIRES on the concrete fSq far word, every hypothesis discharged. Scope inherited verbatim from FriQueryBiasSharp (ROM squeeze uniform over F); δ = 7/16 = unique-decoding radius. #assert_all_clean (3 keystones).
import Dregg2.Circuit.FriVerifierComposeDefected -- ⚑⚑ FRI LADDER RUNG L1 LANDED — epsFri_compose restated with the DEFECTED query leg, in the SAME condProb model, deployed leg DISCHARGED. epsQueryDefected = 1/|F| + ((1−δ)+m/|F|)^k replaces the uniform (1−δ)^k that babybear_sampleBits_not_balanced proves the deployed sampler does not have; epsFri_compose_defected = the union bound with the honest leg (epsQuery_le_epsQueryDefected: sound correction, dominates uniform — claims nothing more). THE UNIFICATION: deployed_query_leg_condProb re-reads far_word_rarely_accepted_deployed_sampler as a condProb bound over the squeeze oracle, so epsFri_compose_deployed_defected lands with the query leg a THEOREM at deployed params (|F|=2013265921, m=16, k=38, δ=7/16) — not a hypothesis (contrast friLdtExtractV3_rom_of_legs's hQuery that "does not come home"). NON-VACUITY through the committed falsifier: undefected_query_leg_refuted knocks over the un-defected (1−δ)^k leg at the N=3,m=2,k=1,δ=1/2 witness BY ROUTING THROUGH FriCarrierEpsilon.friQueryForgeryBound_false_without_defect (undefected_leg_transfers), while defected_query_leg_holds_at_witness shows the defected leg survives — the m/N addend is load-bearing, not cosmetic. Blocker (a) WordProofBridge/DeployedFriEmbedding UNTOUCHED (leg discharged for the abstract far word, not yet a BatchProofData extraction bundle); blocker (b) sampling defect = RETIRED. #assert_all_clean (8 keystones).
import Dregg2.Circuit.FriWrapHonest -- ⚑ THE HONEST DEPLOYED WRAP + the vacuity CANARY welded to it (WAVE 2, retiring the vacuous carrier in the shipped wrap). Makes EmitVerifier.emitVerifier_wrap_sound (:342 — the object the gnark verifier INHERITS) honest: emitVerifier_wrap_sound_honest concludes ONLY proof.exposedSegment = pub.segment from a gnarkDenote-accepted proof, the [FriLowDegreeSound] instance binder + the GenuineWitness ∃-hole STRIPPED (emitVerifier_wrap_needs_no_carrier reproduces the OLD conclusion with the carrier binder DELETED; honest_wrap_loses_nothing: the GenuineWitness ∃ ↔ the bare segment eq, so nothing real is lost). ⚑ OPTION (b) — a real per-proof witness tied to the committed word — IS UNREACHABLE AT THE DEPLOYED OBJECT, not merely unproven: gnarkDenote_blind (the emitted FRI leaves bCanon..bQPow are CONSTANTS of I, not the proof, EmitVerifier:251) ⟹ emitVerifier_wrap_cannot_certify_fri_content: whenever the circuit accepts ANY proof it ALSO accepts its garbageTwin (same segment, EVERY FRI field emptied), so even the weakest content predicate 'friCommitments ≠ []' has a Falsifier against gnarkDenote I — no wrap over the emitted object can soundly conclude the accepted proof carries so much as one FRI commitment (independently corroborated: FriColumnLogExtract finds Merkle binding yields NO total committed column). ⚑ THE HONEST FRI FLOOR IS THE Q-BOUNDED ε BOUND, NOT A PER-PROOF EXTRACTION: deployed_wrap_honest_floor re-states FriCarrierEpsilon.deployed_forgery_bound (Q·((9/16)+2^logN/|F|)^38 at the δ=7/16 unique-decoding radius); deployed_wrap_ceiling = ~31 bits (bound exceeds 1 already at Q=2^32, vs the config's claimed 130); deployed_wrap_floor_refutable = the falsifier the old carrier could not have (a wrong ε makes FriQueryForgeryBound FALSE). ⚑ THE CANARY (permanent + reusable): NonVacuousCarrier acc C is inhabited IFF C ships a Falsifier — old_carrier_fails_canary (FriLowDegreeSound admits NONE at any params) vs honest_deployed_carrier_passes_canary; wrap_floor_trivial_instance_dichotomy = the discharge-with-trivial-instance detector (the OLD carrier is Nonempty at ARBITRARY params with no hypotheses via friLowDegreeSoundTrivial; the honest floor is NOT universally true, false at a concrete witness) — if a future edit collapses the honest floor into a trivially-dischargeable one, that conjunct becomes unprovable and the file stops compiling. VERDICT: the per-proof deployed wrap is an HONEST PARTIAL (segment eq, vacuous carrier removed); the adversary-quantified, non-trivializable, refutable object is the SEPARATE FRI floor (~31 bits), which the emitted object provably cannot deliver per accepted proof. ADDITIVE: FriVerifier/FriCarrierVacuity/FriCarrierEpsilon/EmitVerifier imported read-only + untouched. sorry-free, #assert_all_clean (14 keystones).
import Dregg2.Circuit.WordProofBridgeDeployed -- STAGE 5 BLOCKER (a) WORD↔PROOF BRIDGE, WRITTEN: connects FriVerifierCompose's abstract WordProofBridge predicate to the in-tree DeployedTraceExtract.DeployedFriEmbedding carrier, over the REAL deployed word encoding (decodeColumn of the committed Int FRI column). FriVerifierCompose §3(a) named the obligation ("supplying WordProofBridge is supplying DeployedFriEmbedding") in PROSE only — the two subtrees never touched. wordProofBridge_of_embedding PROVES the implication DeployedFriEmbedding → WordProofBridge (committed:=oracle, accepts:=verifyAlgo, bundleFails:=¬TraceWitnessed, isFar:=(·∉friSetupK8.C)) — the bridge IS decode_trace contrapositively: on accept, a codeword-committed run yields TraceWitnessed, so bundle-failure forces the committed word far. wordProofBridge_of_traceDecode lifts it through the shrunk DeployedTraceDecode; wordProofBridge_decodedColumn states it over decodeColumn(column pi π) — the mod-p reduction of the actual committed Int column (hcol:=rfl for FriColumnDecode.deployedTraceDecode_of_columnDecode B hood). bridge_feeds_compose plugs into FriVerifierCompose.bundleFail_imp_far_of_bridge to yield the accepts∧far containment the compose step's wQuery leg consumes. TEETH: isFar_f0 (f0 far) + not_isFar_fHon8 (fHon8 not far) — isFar is genuinely two-valued, the conclusion is a real constraint not a tautology; bridge_violation_refutes_decode_trace states EXACTLY what makes the bridge false (an accepting codeword-committed run whose bundle fails) and shows decode_trace forbids it. SCOPE (honest): DOES connect the predicate to the named carrier; does NOT discharge DeployedFriEmbedding/ColumnDecodeBridge (still the assumed FRI residual with its 4 verifier-side sub-seams), and isFar here is 0-farness (∉C), NOT the δ-far quantitative radius epsQuery uses — reconciling 0-farness with epsQuery's radius, and blocker (b) sampleBits bias, remain OPEN. ADDITIVE: FriVerifierCompose, FriColumnDecode, DeployedTraceExtract, FriFoldArity all imported read-only + untouched. #assert_axioms-clean (7 keystones).
import Dregg2.Circuit.FriFarnessReconcile -- STAGE 5 BLOCKER (a) LAST RESIDUAL — RECONCILING the two FRI-farness radii. WordProofBridgeDeployed's bridge produces isFar := (·∉friSetupK8.C) = 0-farness ("not a codeword"), but FriVerifierQuery.epsilon_query_layer quantifies farN S.C (4*d) — the QUANTITATIVE unique-decoding radius. These differ, and the difference is REAL: zeroFar_not_imp_udrFar exhibits a code+word (⊥ over Fin 2→ZMod 5, ![1,0]) that is 0-far (∉⊥) yet (4*1)-CLOSE — so the bridge's 0-far is STRICTLY WEAKER than epsQuery's (4*d)-far; the pointwise implication runs ONLY strong⇒weak (farN_imp_not_mem: (4*d)-far ⟹ ∉C, via closeN_mono/farN_antitone radius monotonicity). HOW THEY RECONCILE (the deterministic collapse): under DeployedFriEmbedding, accept_folds + the PROVED arity-8 keystone friProximityK8_discharge0 force oracle∈friSetupK8.C on EVERY accepting run (embedding_oracle_mem), a codeword is d-close at every radius ⟹ ¬farN … d oracle for ALL d (embedding_not_far), so BOTH the bridge's 0-far event AND epsQuery's (4*d)-far event are deterministically EMPTY hence COINCIDE — bridge_isFar_iff_epsQuery_far: oracle∉C ↔ farN … (4*d) oracle, both false. That is the reconciliation the compose step consumes: on the deployed embedding the word the bridge calls "far" and the word epsQuery calls "δ-far" are the SAME (empty) event, so epsQuery bounds the bridge event with Pr=0. TEETH: embedding_far_oracle_refutes_proximity (an accepting far-oracle run refutes friProximityK8_discharge0 — the reconciliation's truth IS the proved proximity), farN_zero_f0/not_farN_zero_fHon8 (the predicate is two-valued). RESIDUAL (named, honest): composes by DETERMINISTIC COLLAPSE (both events empty under accept→∈C), NOT by paying epsQuery's (1−δ)^k at a POSITIVE radius — that needs a QUANTITATIVE decode_trace firing at d>0 (friProximityK8_discharge's 64·d form) rather than only the d=0 friProximityK8_discharge0; plus the deployed-proofspace↔oracle-conditioned-probability coupling of friLdtExtractV3_rom_of_legs (blocker b's bias term closed by FriQuerySamplingBias). ADDITIVE: WordProofBridgeDeployed/FriFoldArity/FriBridgeDeployedArity/DeployedTraceExtract/FriSoundness imported read-only + untouched. #assert_axioms-clean (13 keystones).
import Dregg2.Circuit.FriPositiveRadiusPayment -- STAGE 5 BLOCKER (a) FOLLOW-UP to FriFarnessReconcile — the POSITIVE-RADIUS quantitative decode + the (1−δ)^k PAYMENT, wired from the PROVED arity-n keystone; plus the PRECISE reason the deployed friSetupK8 cannot exhibit it non-vacuously. STEP-1 GROUND TRUTH: friProximityK8_discharge at d>0 is PROVED, not a residual — it IS fold_close_of_arity_challenges (a full size-n Vandermonde/BBHR18 reconstruction, d universally quantified, #assert_axioms-clean), NOT the deep FRI/ethSTARK list-decoding conjecture (that conjectural probabilistic content lives in εQuery's L>1 Johnson carrier + FriVerifierCompose's two named blockers). So this is the "IF PROVED — wire it" case. WIRING (generic over any FriSetupK F ι κ n, so mechanical once a realistic-domain instance lands): far_oracle_has_far_fold — the CONTRAPOSITIVE of the keystone: farN S.C (n²·d) f + injective α ⟹ ∃ i, farN S.C' d (Fold (α i) f) ("bundle fails at positive radius ⟹ some fold is d-far"), generalizing the committed d=0 no_injective_good; far_fold_query_payment — accept_prob_le_of_farN pays (1−δ)^k on the caught d-far fold against any claimed f'∈S.C'; far_oracle_query_payment — the two composed = the εQuery addend PAID at a genuine positive radius, replacing FriFarnessReconcile's deterministic Pr=0 collapse. ⚑ THE PRECISE RESIDUAL (proved, not hand-waved): the deployed friSetupK8 : FriSetupK BabyBear (Fin 16) (Fin 2) 8 is a MODEL at coset size |L|=16 / folded |κ|=2; farness caps at the domain size (|disagree|≤|ι|), so the reconstruction radius n²·d=64·d>16 makes farN friSetupK8.C (64·d) UNINHABITED (friSetupK8_no_positive_far_oracle) and every folded word is 1-close to a constant so farN friSetupK8.C' d is UNINHABITED for d≥1 (friSetupK8_no_positive_far_fold) — hence positive_radius_payment_vacuous_at_friSetupK8: the payment specialized to friSetupK8 is VACUOUS at d≥1. The residual is therefore a MODEL-RESOLUTION gap (the size-16 coset needs sharpening toward |L|=2^24), NOT the proximity math (proved) and NOT a conjecture. TEETH: f0_no_injective_good_via_positive_radius (d=0 RE-DERIVES the committed f0_no_injective_good through the generic lemma — strict generalization, non-vacuous far side f0); payment_lt_one_deployed ((9/16)^38<1, a real sub-1 probability). ADDITIVE: FriQuerySoundness/FriFoldArity/FriSoundness imported read-only + untouched. #assert_axioms-clean (8 keystones).
import Dregg2.Circuit.FriQueryAdversary -- STAGE 5 QUANTIFIER CLOSURE + ⚑ THE ARITHMETIC REFUTATION: εQuery ATTACHED TO A Q-BOUNDED ADVERSARY, and what it is then WORTH at deployed params. FriVerifierQuery.epsilon_query_layer/_deployed are quantified over WORDS, not adversaries — the far word is FIXED IN ADVANCE and (α,Q) is the VERIFIER's own uniform sampling over F × (Fin k → κ); no strategy, no adaptivity, no budget. THE CONVERSION: the uniform εQuery IS a DENSITY BOUND ON AN EXCEPTIONAL ANSWER SET, which is exactly what FriVerifierCompose.hit_cond consumes. badAnswers chal Bad d = {r | chal d r ∈ Bad d} (the oracle answers whose DERIVED challenge is exceptional for the word committed at d); badAnswers_card_le/_density_le push the challenge-space density through the derivation map's fibres; hit_cond_density restates hit_cond in real-valued density form. ⚑ epsQuery_adversary: for ANY Q-query OracleComp adversary — no freshness premise, no excluded class, honest prover included — Pr[the run EVER lands an exceptional challenge] ≤ Q·εΩ. epsQueryBias_adversary gives codex's FriQuerySamplingBias.epsQueryBias its FIRST CONSUMER (the deployed biased sampler); epsQueryAdv_le_epsQueryBiasAdv shows the sampleBits defect moves the bound in the SOUND direction and is nowhere near what caps the leg. epsFri_four_legs composes ALL FOUR legs over ONE oracle for ONE adversary — epsFri_closed_legs (:392) discharged three and left εQuery off the disjunction entirely. ⚑⚑ THE REFUTATION (arithmetic over the shipped constants, norm_num-decided, exact rationals): εQuery(|F|=2013265921, k=38, δ=7/16) = 1/|F| + (9/16)^38 ≈ 2^-30.19, so Q·εQuery ≥ 1 — a probability bound of ≥1, i.e. NOTHING — already at Q = 2^31 (epsQueryAdv_deployed_vacuous_at_2_31), lifted by monotonicity to 2^112 (epsQueryAdv_deployed_vacuous_at_2_112) and bracketed from below by epsQueryAdv_deployed_lt_half_at_2_28 (< 1/2 at 2^28). THE QUERY LEG CAPS AT ≈29 BITS. The ~112.6-bit and ~73-bit query columns are not merely unproven through this pipeline — they are UNREACHABLE through it, BY COUNTING: once an adversary gets Q retries no union bound can beat -log2 εQuery ≈ 30. ⚑ AND MORE FRI QUERIES CANNOT HELP: deployed_query_term_below_fold_term proves (9/16)^38 < 1/|F| — at k=38 the query term has ALREADY sunk below the fold term, so the binding constraint is the SIZE OF THE FIELD THE FOLD CHALLENGE IS DRAWN FROM, not k (the same ~31-bit ceiling as reference-umem-boundary-31bit / the felt-width campaign). ⚑ DEPLOYED-CODE MISMATCH FOUND (conservative direction, NOT load-bearing): FriVerifierQuery:304 reads the fold term as 1/|F| ≈ 2^-31 'at BabyBear' = the BASE field, but the deployed folding challenge lives in the degree-4 EXTENSION (plonky3_prover.rs PROD_EXT_DEGREE=4, EF=BinomialExtensionField<BabyBear,4>, |EF|≈2^123.6, whose own comment calls it 'the denominator of every per-fold proximity-gap bound'). The modelled fold term is ~2^92 too PESSIMISTIC; correcting it buys exactly ONE bit — epsQueryAdv_ext_not_vacuous_at_2_31 (still informative at 2^31) + epsQueryAdv_ext_still_vacuous_at_2_32 (vacuous at 2^32), moving the cap 2^29.19 → 2^30.54. ⚑ CLOSES A NAMED GAP: miss_density_of_far DERIVES codex's hmiss ((E.card)/m ≤ 1−δ) from δ-farness by counting — agreement and disagreement partition the index set — so the bias-aware bound loses an assumption rather than gaining one. NAMED, NOT DISCHARGED: TranscriptWordCommitment — hit_cond needs the commitment indexed by the ORACLE QUERY POINT (the exceptional set must be fixed BEFORE the answer at d is drawn), whereas WordProofBridgeDeployed's bridge is PER-PROOF; converting proof-indexed to point-indexed is FS transcript-binding and no in-tree theorem does it (narrower than blocker (a), not implied by it). SampleBitsGrounding — still no lemma links the abstract n % m to the deployed Challenger.sampleBits. FibreBalanced is an IDEALISATION that babybear_sampleBits_not_balanced PROVES false at the deployed sampler, which is why the deployed route goes through epsQueryBias. ADDITIVE: FriQuerySamplingBias/FriVerifierCompose/FriVerifierQuery imported read-only + untouched. #assert_all_clean (17 keystones).
import Dregg2.Circuit.FriAdversaryObject -- ⚑ D1: THE FRI PROVER STRATEGY AS A TYPED OBJECT (07-23, docs/DESIGN-fri-adversary-object.md) — the FRI-soundness program's step (1): something for an adversary to BE. Strategy R C := List R → C (next-round commitment as a function of the FS challenge prefix; the honest prover inhabits it via honestStrategy = repeated fold; NOTHING ties a cheating strategy's rounds together — that tie is the verifier's job and the ladder's price). THE FS TRANSFORM AS AN OracleComp: fsRun enc S n cs queries the ROM at the encoded post-commitment transcript prefix each round and takes the answer as the challenge — fsRun_queryBounded (FS challenges ARE ROM queries, one per round: the budget = the round count, so hit_cond/birthday_cond/RomQueryLog apply to EVERY strategy by construction), fsRun_eval (FAITHFULNESS: eval against a fixed oracle recovers the deterministic chain fsChain — the strategy-level verifyAlgoO_run_eq; the re-basing is conservative), fsRun_queried (the QUERY LOG IS THE TRANSCRIPT: queried points = the encoded prefixes in round order — what a straight-line extractor reads). §3 THE FOLD CHAIN: ChainStep far goodSet fold = the TYPED per-round distance-preservation obligation (deliberately a def — its UD-radius instance is ladder stage L2 engineering on FriFoldArity where goodSet cardinality IS the ledger's goodCount; at Johnson M=1 is REFUTED, FriJohnsonRadiusGap); honest_chain_far PROVES the easy half — far survives the WHOLE fold chain when every round's challenge avoids its point's bad set (AvoidsBad, the fsRun-specialized complement of hitWin; its failure is priced ≤ n·b/|R| by hit_cond via fsRun_queryBounded — corollary DEFERRED because FriVerifierCompose's closure is RED at HEAD: SpongeForgeReduction.lean:324,329 references effFloor_top_false_babyBear/effFloor_bot_vacuous without importing DomainSeparatedCREffRegrounded, the unpropagated regrounding rename; this module imports ONLY the green RomOracle substrate). Teeth: chainStep_is_load_bearing (drop ChainStep ⟹ conclusion FALSE at a witness) + honest_chain_far_fires (positive pole). ADDITIVE; no deployed statement touched. #assert_all_clean (9 keystones).
import Dregg2.Circuit.FriChainStepIdx -- ⚑ LADDER RUNG L2 (07-23): the INDEXED per-layer fold chain + the winProb corollary FriAdversaryObject deferred, both PROVEN. ChainStepIdx far good fold = the per-LAYER distance-preservation obligation (word TYPE W i shrinks per round at deployed arity 8; far i = the per-layer RADIUS SCHEDULE); chainStepIdx_iff_sigma reduces it to the landed fixed-C ChainStep on the layer-tagged carrier (a theorem, not a re-proof), honest_sig_layer/terminal_layer pin the honest evolution to layer=round. chain_far_survival = the DEFERRED corollary landed: any event implying the terminal word of the n-round honest-fold FS chain from a far w0 is not far has winProb <= n*b/|R| (fsRun_queryBounded + hit_cond + honest_chain_far glued by hitWin_fsRun_true_iff = the hit event on the FS tree IS the failure of AvoidsBad); the cover is deliberately FAR-CONDITIONAL because the landed unconditional shape is UNINSTANTIABLE at the deployed RS shape (unconditional_cover_trivial_at_deployed: a MEMBER word's good set is ALL of F via fold_complete, forcing b >= |BabyBear| and a bound >= n). goodδ_card_lt = the NEW distance-graded cap over FriFoldArity: not-(n^2*d)-close ==> fewer than n challenges fold d-close (good_challenge_card_lt is its d=0 case) — the per-layer b of the UD radius ladder. FIRES at friSetupK8 from the proven-far f0: Pr[the FS challenge folds f0 INTO the folded code] <= 7/|BabyBear| (k8_chain_far_survival, genuinely < 1: k8_bound_lt_one). FALSIFIERS (the acceptance bar): fixed_radius_collapse_refuted (schedule collapsed to one radius = FALSE where ChainStepIdx holds — the schedule is load-bearing); goodSet_by_definition_is_free + goodSet_by_definition_no_content (THE VACUITY TRAP priced: a goodSet defined to make ChainStep true holds for EVERY fold and forces b >= |R|, bound >= 1 — content = an externally-PROVEN cap, never the definition). ⚠ DUPLICATION DEBT flagged in-module: hitWin/hit_cond/hit_bound re-derived from green RomCounting because FriVerifierCompose's closure is red at HEAD (SpongeForgeReduction sorryAx via AggAirSound); one copy must die when it heals. REPORTED not faked: no welded FriSetupK TOWER in-tree ((S i).C' = (S (i+1)).C), and no vocabulary weld from goodδ to FriLedgerSound's goodCount (Φ/H-polynomial vs FriSetupK/closeN) — the multi-layer deployed instance waits on those. ADDITIVE. #assert_all_clean (21 keystones).
import Dregg2.Circuit.FriGoodCountWeld -- ⚑ THE VOCABULARY WELD (07-24): goodδ/closeN (FriSetupK distance vocabulary, FriChainStepIdx.goodδ_card_lt) ⇔ goodCount (the Φ/H-polynomial vocabulary FriLedgerSound.ledger_perFold_soundness prices) — the gap FriChainStepIdx:72-76 named. H_phasesOf_eval = THE IDENTITY: the ledger phase polynomial at the setup's own decomposition components evaluates to EXACTLY Fold S.geom — the two files were counting the same folds. goodδ_eq_ledger_goodSet identifies the SETS at the terminal constants code (C' = constants = the deployed logFinalPolyLen = 0 shape; SCOPE stated plainly: every weld theorem carries the constants-code hypothesis — non-constant intermediate C' is NOT welded here). goodδ_card_le_ledger_goodCount = THE WELD: under the ledger's own hΦ (M=1 fiber bound, still a per-config HYPOTHESIS), |goodδ S d f| ≤ (friLedger cfg).goodCount at matching shape — L2's per-layer b is now read off the EXPORTED ledger; goodδ_ledger_perFold_error composes it into ledger_perFold_error's 2^(−perFoldBits) density. NON-VACUITY on an INHABITED set: fW = (−1)^x(1 − ω₁₆^x) is far (fW_not_mem, DFT functional) with goodδ = EXACTLY {1} (goodδ_fW_eq; f0's good set is empty so it could not witness the cap constraining anything), phase-injectivity DISCHARGED (fW_phase_injective), weld_fires_k8: 1 ∈ goodδ ∧ |goodδ| ≤ goodCount(cfgK8) = 7 = n−1 — the two caps AGREE on ONE Finset (both_caps_k8), priced by the exported exponent (weld_priced_k8). CANARY: weld_canary_mismatched_count — an arity-mismatched config's goodCount = 0 REFUTES the weld conclusion on fW: the count is load-bearing, not slack. ADDITIVE. #assert_all_clean (18 keystones).
import Dregg2.Circuit.FriSetupTower -- ⚑ LADDER RUNG L5 (07-24): THE WELDED MULTI-LAYER FriSetupK TOWER — the family FriChainStepIdx:63-71 named as missing, LANDED at the deployed arity-8 production schedule AT ITS OWN HEIGHT PAIRING (PROD_FRI_MAX_LOG_ARITY=3 ⟹ |D0| = 2^16·2^3 = 2^19, per fri_trace_height_measure DEPLOYED_WORST_LOG_D0=19 — NOT the wrap path's arity-2/lb-6/2^22 pairing FriDeployedHeightPairing refutes mixing with). towerS = FIVE welded arity-8 layers 2^19→2^16→2^13→2^10→2^7→2^4 (degree 2^16→2, rate 2^-3 every layer, depth 5 = where the schedule exhausts the degree; a TOTAL ℕ-tower at arity 8 is proven-impossible in the header — |ι| ≥ 8|κ| forces finite depth, the Fin-L shape L2 specified). tower_link: (S i).C' = (S (i+1)).C at every weld (one root chain wchain, wchain(i+1)^8 = wchain(i+2) DEFINITIONALLY; wchain 6 = omega16 — the chain lands on the tree's deployed root, tower_bottom_is_omega16_code). The layer constructor towerLayer is ARITY-GENERIC (the arity-2 wrap tower is the same constructor at n=2, reachable not landed): geometry from a root of exact ORDER (no kernel enumeration — fires at the real 2^19), both FriSetupK closure obligations PROVED from ONE layer identity (reassemble_tower) + decomposition uniqueness through the fiber Vandermonde (Cj_reassemble); w22 order-2^22 root machine-checked by a 21-step squaring chain (no giant pow). chainStepIdx_tower: ChainStepIdx GENUINELY INSTANTIATES at 5 real layers (the link load-bearing at each step) — tower_chain_far_survival runs chain_far_survival_idx end-to-end from the PROVEN-far degree-2^16 monomial (DFT-functional farness at 2^19, monoW_not_mem — symbolic, no decide over the domain): Pr[farness dies across the five welded folds] ≤ 5·7/|BabyBear| < 2^-25 (tower_bound_lt_one), vs L2 §6's ONE-layer 7/|B|. CANARIES (the link is CONTENT, both polarities): canary_halved_degree_link (the link EQUATION refutes a halved-degree layer), canary_halved_degree_unbuildable (too-small C' fails foldC_mem — cannot even construct the mis-welded layer), canary_inflated_degree_unbuildable (too-big C' fails unfold_closed — its reassembled 'codeword' is LITERALLY the far word); wrong domain SIZE is type-rejected by κ_i = ι_{i+1} in towerS's signature. SCOPE honest: d=0 radius schedule (far = ∉code) like L2 §6; the 2^19 domain UNLOCKS the positive-radius schedule FriPositiveRadiusPayment proved uninhabited at |ι|=16 — that is the next rung, not claimed. ROM/FS statement about honest-fold word evolution; NOT prover soundness; FRI floor untouched. ADDITIVE. #assert_all_clean (22 keystones).
import Dregg2.Circuit.FriQueryAdversaryLaunch -- ⚑ THE NEW (q, pow) THAT BUYS REAL PROVEN QUERY-LEG BITS — the NON-vacuous converse of FriQueryAdversary §5, over the SAME machinery (epsQueryAdv/epsQueryAdv_mono/epsQuery_nonneg reused, not re-derived). §5 proved the DEPLOYED leg VACUOUS: Q·(1/|F|+(9/16)^38) ≥ 1 at Q=2^31 (~31 bits at δ=7/16). This file computes the honest (q=150, pow=16) that makes it non-vacuous and states exactly what it buys + where it STILL caps. THE LEVER is q, isolated as arithmetic: q_lever_old38_vacuous_at_2_40 vs q_lever_new150_tiny_at_2_40 — SAME ext field (cardF=2013265921^4, the correct degree-4 extension the fold challenge α actually lives in, per §5's own mismatch finding), SAME budget 2^40, ONLY q changes: 38 is VACUOUS (≥1), 150 is ≤2^-80 (a ≥80-bit swing from q alone; true values ~2^8.5 and ~2^-84). ⚑⚑ THE DELIVERABLE (adversary-quantified, explicit ε, exact wave-1 shape): launch_100bits_vs_2_22_adv — ∀ Q≤2^22, epsQueryAdv Q (2013265921^4) 150 (7/16) ≤ 2^-100 (100 bits vs a 2^22 adversary, via epsQueryAdv_mono + the norm_num value launch_at_2_22); launch_60bits_vs_2_62_adv — ∀ Q≤2^62, ≤ 2^-60 (60 bits vs a 2^62 adversary, the strong-adversary reading). ⚑ THE HONEST CEILING q CANNOT PASS: epsQueryAdv_ge_fold proves epsQueryAdv ≥ Q/cardF UNCONDITIONALLY (the query term Q·(1−δ)^k is nonneg), so field_ceiling_vacuous_at_2_124 is VACUOUS at Q=2^124 at ANY q — the fold ceiling is the SIZE OF THE degree-4 EXTENSION (~123.63 bits), moved only by extDeg. Consequence: proven query-leg λ against a Q≤2^Qmax adversary requires λ+Qmax ≤ ~122 — there is NO honest 100 bits vs a 2^128 adversary at BabyBear deg-4 (100+128=228). The two launch readings (100+22, 60+62) are two points on that one line. AND the q-invariant SYSTEM ceiling sits below anyway: the commit-phase ε_C (FriLedger.commitBits ~61 bits at the deployed height) carries no q/pow, so q=150 is chosen to lift THIS leg to that ε_C ceiling self-consistently (launch_at_2_62 = 2^-61 vs 2^62), NOT to claim a 100-bit system. ⚑ THE GRINDING KNOB pow, as arithmetic: grind_pow16_vacuous_at_2_16 (epsGrind (2^16) 16 = 1 — grinding carries no union-bound security above 2^16) + grind_pow16_vacuous_at_2_22 + grind_needs_pow122_for_2_22_100bit (matching the query leg's (22,100) point would need pow≥122, and 2^122 honest-prover hashes is infeasible) — so pow stays 16 and the proven bits rest ENTIRELY on q. ⚑ DEPLOY NOTE: the config change (q 38/19 → 150, both plonky3_prover.rs + descriptor_ir2.rs) is NOT applied in-tree — it REDS the pinned fri_params_soundness_budget gate (modeled num_queries) and invalidates the apex_shrink fixture / VK / gnark verifier / AIR-in-Lean cycles all generated at q=38/19. The theorem + exact diff ship; the regen is NAMED follow-up. ADDITIVE: FriQueryAdversary/FriVerifierCompose imported read-only + untouched. #assert_all_clean (11 keystones).
import Dregg2.Circuit.RotatedKernelRefinementFacet -- THE AUTHORITY LEG of the soundness apex, closed FAITHFULLY for transfer (the template the ~29 other effects follow). Additive over the toy BalanceMovementSpec/authorizedB (~150 modules untouched). (1) BalanceMovementSpecFacetK + execFaithful_iff_specFacet — the FAITHFUL executor (gates on the deployed two-axis authorizedFacetB) ⟺ the FAITHFUL full-state spec, both directions (the §10(A) exec_authorized as a ⟺). (2) BalanceMovementSpecFacet (over RecChainedState, parameterized by the deployed fcaps/provided): identical to BalanceMovementSpec but the admitGuardA authority conjunct is authorizedFacetB fcaps .signature tr — VALUE leg (debit/credit/avail/frame/log) REUSED verbatim from transfer_descriptorRefines; AUTHORITY leg FORCED by the in-circuit cap-open (transferCapOpenEffV3_authorizes ⟹ authorizedFacetB, over the LIVE membership descriptor), NOT carried as guardAuth. The StateDecode↔cap-open fcaps wiring is the NAMED bridge TransferAuthoritySource (the cap-tree residual the LEDGER commitment cannot certify; the authority CONCLUSION is derived from it via authoritySource_authorizes, not a field). transfer_descriptorRefines_facet = the template keystone. (3) dispatchArmFacet + lightclient_transfer_faithful (the parametric single-step apex at the FAITHFUL arm — concludes a faithful transfer transition; authority = the deployed gate, discharged in-circuit) + dispatchArmFacet_to_dispatchArm (the toy-executor lowering, the named §10(A) residual). Teeth: specFacet_rejects_unauthorized / transfer_descriptorRefines_facet_rejects_unauthorized (the faithful authority leg BITES). #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementFacetTurnBound -- THE TURN-IDENTITY binding — closes the owner-authority / turn-identity SMUGGLE. The prior faithful apex (lightclient_transfer_faithful / …forest_facet) concludes ∃ tr a, BalanceMovementSpecFacet … pre tr a post with the kernel turn tr EXISTENTIALLY quantified; authorizedFacetB fcaps provided tr reads tr.actor/tr.src, NEITHER of which the light client's published commitment binds (recStateCommit uses t.src/t.dst only as the cell-digest PARTITION index and never absorbs t.actor; rotPins publishes only 4 PI pins = OLD/NEW commit·height·caveat, no turn-identity; capOpenCols.src/capRoot are FREE appendix columns — the CapOpenEmit §1 weld is documented but NOT in capOpenConstraintsEff). So a prover could pick tr.actor:=tr.src (owner disjunct, no cap) for ANY src it moves and the apex still holds — owner authority OFF-CIRCUIT. THE FIX: bind the kernel step's turn to the PUBLISHED pc.turn. (1) TurnIdentityBound pc tr := tr = pc.turn — the NAMED in-circuit obligation a turn-identity PI gate discharges (publish the witness turn fields to the PI turn slots), carried EXPLICITLY (like StarkSound/WitnessDecodes), no longer a hidden existential. (2) dispatchArmFacetTB fcaps provided pubTurn — the TURN-BOUND arm (turn = pubTurn, only the asset existential); authorizedFacetB reads the COMMITTED published turn. (3) ownerGateForced/dispatchArmFacetTB_owner — the OWNER disjunct over pc.turn (decide(pc.turn.actor=pc.turn.src)): a decision on PUBLISHED identity the light client CAN check. (4) transfer_descriptorRefines_facetTB — the turn-bound refinement (VALUE leg from transfer_descriptorRefines, witness turn rewritten to pc.turn via TurnIdentityBound, AUTHORITY over pc.turn). (5) descriptorRefinesTB + lightclient_transfer_faithful_turnbound — the apex whose conclusion's turn IS pi.turn: the authority a light client gets is over the turn it PUBLISHED. Teeth: dispatchArmFacetTB_rejects_unauthorized (a rejected published turn has no faithful arm) + dispatchArmFacetTB_owner_fires (owner arm non-vacuous). descriptorRefinesTB_to_descriptorRefines: the turn-bound rung is STRICTLY STRONGER than the old existential rung (it pins what the old left free). RESIDUAL: the deployed realization of TurnIdentityBound = add 4 turn-field PI pins + the equality gate to rotPins (VK-affecting, named). #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementAttenuate -- THE capability/attenuation VALUE leg, closed HONESTLY for attenuate against the GENUINE non-amp descriptor (attenuateVmDescriptorGenuineNonAmp, EffectVmEmitAttenuateA, width 186 — recompute + submask). CLASS = VALUE_PARTIAL. THE LOAD-BEARING FINDING: the genuine recompute does NOT pin the exact attenuateSlotF Caps-function update — it forces a SINGLE-EDGE PREPEND-ACCUMULATOR ADVANCE over a cap_root FELT (post.capRoot = hash[hash[holder,target,rights,op], pre.capRoot], capAdvanceOf/edgeLeafOf), anti-ghosted (tamper any edge field ⇒ root moves ⇒ UNSAT, attenuateGenuine_binds_edge), with the bound rights felt gated granted⊑held bitwise (attenuateGenuineNonAmp_in_circuit). There is NO theorem (structurally cannot be from THIS descriptor) relating that felt accumulator to a sorted-Merkle commitment of the Caps function. So the circuit FORCES the NON-AMPLIFICATION AXIS (the security crux) + the genuine recompute-bound per-cell post (CapCellSpecGenuine, frame frozen), and the EXACT Caps move (attenuateSlotF) + the receipt-log + the 16-field frame are carried as NAMED decode residuals in attenuateEncodes (the cap-tree Caps↔felt residual the per-edge recompute cannot certify). attenuate_descriptorRefines: attenuateEncodes ⟹ AttenuateSpec pre actor idx keep post (= the .attenuateA arm of fullActionStep). attenuate_nonAmp_forced: the in-circuit granted⊑held per bit, FORCED. Tooth: attenuate_descriptorRefines_rejects_amplify (an over-grant row — granted bit set, held bit clear — is UNSAT, reusing attenuateGenuineNonAmp_rejects_amplify). attenuate_spec_non_amplifying: confRights(attenuate keep c)≤confRights c off the forced spec. NAMED RESIDUALS: (a) the Caps↔felt-accumulator lift (capsMove); (b) the registry cutover — the genuine descriptor is v1-level (teeth at per-row constraint satisfaction), so swapping the live v3Registry entry attenuateVmDescriptor2R24 (=attenuateV3) requires FIRST a rotateV3/v3OfWith lift of the genuine-non-amp descriptor + a Satisfied2⟹per-row bridge (its own strand) + the Rust JSON re-emit/V3_STAGED_REGISTRY_FP re-pin — NOT done; the registry was NOT swapped. #assert_axioms-clean on every theorem.
import Dregg2.Circuit.RotatedKernelRefinementMintBurn -- THE burn/mint/bridgeMint VALUE-leg circuit→kernel refinements (VALUE_FORCED), on the LIVE rotated burnV3 (burnVmDescriptor2R24) / mintV3 (mintVmDescriptor2R24 = v3Of mintVmDescriptor) descriptors — the transfer template cloned to the two supply effects. burn_descriptorRefines: a Satisfied2 hash burnV3 witness + rotatedEncodesBurn (designated HOLDER debit row + named guard/frame/log/well-credit ledger-frame residual) ⟹ Spec.SupplyDestruction.BurnSpec pre actor cell a amt post. The holder bal debit MOVEMENT (burn_debit_forced: gBalLoDebit pins bal_lo'≡bal_lo−param1 [ZMOD p], mod-p after the DEBT-A migration) is CIRCUIT-FORCED; the well credit (a,a)+amt + the cross-cell frame ride the named recTransferBal leg. ⚠⚠ DEPLOYED FORGERY: burn availability (amt≤bal cell a) is NOT circuit-forced under mod-p — BURN_AMOUNT_LO is un-range-checked and p<2^31, so an over-burn wraps into [0,2^30) AND the well-credit frame inflates well supply = mint-from-nothing (§3 witness pre.bal=0,amt=10^9). Relocated to the NAMED rotatedEncodesBurn.guardAvail residual pending the EMBER-GATED denotation fix (range-check BURN_AMOUNT_LO<p−2^30 / borrow bit / p≥2^{2·bits}). mint_descriptorRefines: a Satisfied2 hash mintV3 witness + rotatedEncodesMint (designated RECIPIENT credit row) ⟹ Spec.SupplyCreation.MintASpec (the recipient credit mint_credit_forced: gBalLoCredit pins bal_lo'≡bal_lo+param1 [ZMOD p]; mint has NO availability gate — the well is negative-capable, no mint-side forgery). bridgeMint_descriptorRefines = mint_descriptorRefines re-exported (bridgeMintA dispatches to the SAME recCMintAsset / mintV3; execBridgeMintA_iff_spec). Teeth: burn_descriptorRefines_rejects_wrong_debit / mint_descriptorRefines_rejects_wrong_credit (a forged moved-limb post is UNSAT — the gate pins the limb). #assert_axioms-clean on every theorem.
import Dregg2.Circuit.RotatedKernelRefinementSetField -- THE setField VALUE-leg circuit→kernel refinement (VALUE_FORCED), on the LIVE rotated setFieldV3 slot descriptor (the transfer template applied to a field-write effect, per field slot). The selector-gated write gate gFieldWriteP1 slot PINS fields[slot]_after = param1 onto an IN-COMMITMENT state-block column (the same injective Poseidon2 binding transfer's bal_lo rides), so the written content is genuinely circuit-FORCED. rotated_row_cellSpec: a Satisfied2 hash (setFieldV3 slot) witness ⟹ the per-cell CellSetFieldSpec (slot written to param1, nonce ticked, frame frozen) via rotV3_sound_v1 + setFieldVm_faithful. rotatedEncodesSF = the active-row⟷kernel decode (carries the setFieldCellMap whole-map move, the SetFieldGuard, the 16-field frame, the receipt log as the NAMED off-row residual — exactly transfer's hledgerFrame/guard/fr*/logAdv shape). setField_value_forced: the written slot value = param1 = v is forced by the circuit (not the decode). setField_descriptorRefines ⟹ SetFieldSpec pre actor cell (slotName slot) v post (= the .setFieldA arm of fullActionStep). Teeth: descriptorRefines_rejects_wrong_value (a forged written value is UNSAT) + descriptorRefines_rejects_moved_bystander (a mutated non-slot field is UNSAT, frozen by gFieldPass). #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementIncNonce -- THE incrementNonce VALUE-leg circuit→kernel refinement (VALUE_FORCED), on the LIVE rotated incNonceV3 descriptor (incrementNonceVmDescriptor2R24 = v3Of incrementNonceVmDescriptor — no tick-face; the source already carries the reconciled gNonce TICK gate). The committed nonce column IS part of state_commit (hash(bal_lo, bal_hi, NONCE, fields[0..8], cap_root)); the reconciled gNonce gate PINS nonce_after = nonce_before + 1 on the active non-NoOp row, riding the same injective Poseidon2 binding transfer's bal_lo uses, so the nonce TICK is genuinely circuit-FORCED. rotated_row_cellSpec: a Satisfied2 hash incNonceV3 witness ⟹ the per-cell CellIncNonceSpec (nonce +1, economic block frozen) via rotV3_sound_v1 + incNonceVm_faithful (IsIncNonceRow gives s_noop=0). rotatedEncodesIncNonce = the active-row⟷kernel decode (carries the incNonceCellMap whole-map bump, the incNonceGuard, the 16-field frame, the receipt log, AND hnVal : n = cellPre.nonce + 1 — the kernel write value tied to the forced tick, exactly setField's param1=v boundary). incNonce_nonce_forced: cellPost.nonce = cellPre.nonce + 1 is forced by the circuit. incrementNonce_descriptorRefines ⟹ IncrementNonceSpec pre actor cell n post (= the .incrementNonceA arm of execFullA); incrementNonce_descriptorRefines_incremented states it at the forced n = cellPre.nonce + 1 explicitly (the increment SEMANTICS — the leaf spec's n instantiated at old+1). Teeth: descriptorRefines_rejects_frozen_nonce (a stale/wrong-delta nonce is UNSAT, the gNonce gate bites) + descriptorRefines_rejects_moved_balance (a nonce bump that moves value is UNSAT, frozen by gBalLoFreeze). #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementCellSeal -- THE PRINCIPLED-FIX VALUE-leg circuit→kernel refinement for cellSeal (the FIRST of the ~17 VALUE_MISSING effects — those whose write lands in a kernel SIDE-TABLE not a committed column). The deployed circuit's per-cell commitment hash(bal_lo,bal_hi,nonce,fields[0..7],cap_root) (cell_state.rs::compute_commitment) binds NO lifecycle column, and the live cellSeal row freezes the economic block + ticks nonce while the lifecycle Live→Sealed flip is OFF-ROW (EffectVmEmitCellSeal.cellSeal_offrow_unenforced) — so a cellSeal_descriptorRefines against the DEPLOYED descriptor is genuinely FALSE (a prover can publish a commitment to an un-sealed post). The Lean apex recStateCommit=cmb(cellDigest,RH) already ASSUMES RH binds the full kernel incl. lifecycle (StateCommit.RestHashIffFrame); this module builds the FIX that REALIZES it. BINDING MECHANISM (additive — NOT a 9th N_SYSTEM_ROOTS index, which would mutate ~45 modules + the Rust [FieldElement;8]): a dedicated committed lifecycleRoot limb modelled the SAME way the system_roots limbs are (a ListCommit.listDigest over the cell's lifecycle entry, binding via the realizable compressNInjective Poseidon-CR carrier + lifecycleLeaf_injective). gLifecycleSeal = the FIX gate that FORCES the post lifecycle-root column to the SEALED digest (lifecycleRoot of lifecycle[cell↦Sealed]), exactly as setField's gFieldWrite forces fields[slot]_after=param1 into a committed column. lifecycleSealForced: the gate ⟹ post.lifecycle cell = lcSealed (via lifecycleRoot_binds); lifecycleMapForced lifts it to the whole map = sealLifecycleMap (the cell entry FORCED, the off-cell entries the named frame residual). cellSealGenuineEncodes = the active-row⟷kernel decode (carries the FIX gate as the WITNESS leg + the off-map frame + the CellSealGuard + the 16-field frame + the receipt log as NAMED residuals). cellSeal_descriptorRefines: cellSealGenuineEncodes ⟹ CellSealSpec pre actor cell post (= the .cellSealA arm of execFullA, also stated as execFullA pre (.cellSealA actor cell)=some post). TEETH: cellSeal_descriptorRefines_rejects_unsealed (a post whose cell lifecycle ≠ lcSealed is UNSAT — the lifecycle-root gate bites, EXACTLY what the deployed circuit cannot reject) + cellSeal_descriptorRefines_rejects_wrong_map (any whole-map tamper is UNSAT). This is the fix PATTERN the other ~16 VALUE_MISSING effects follow. RUST SCOPE (to make it load-bearing, not scaffolding): cell_state.rs::compute_commitment must absorb a lifecycle_root limb; the cellSeal trace-fill (trace.rs) must emit the sealed-lifecycle root; VK-epoch rotation required (commitment shape changes). #assert_axioms-clean on every theorem (only carried crypto = compressNInjective + lifecycleLeaf_injective, the realizable Poseidon CR SystemRoots/ListCommit already use).
import Dregg2.Circuit.RotatedKernelRefinementLifecycle -- THE PRINCIPLED-FIX VALUE-leg circuit→kernel refinements for FOUR more VALUE_MISSING effects, fanning out the cellSeal template: cellUnseal (lifecycle[cell]:=Live — REUSES lifecycleRoot, inverse of seal), cellDestroy (lifecycle[cell]:=Destroyed via lifecycleRoot REUSED + deathCert[cell]:=certHash via a NEW deathCertRoot limb — BOTH forced), refusal (cell.cell[cell]."refusal":=1 audit RECORD slot via a NEW auditSlotRoot limb), receiptArchive (cell.cell[cell]."lifecycle":=1 audit RECORD slot — REUSES auditSlotRoot over lifecycleField; NOT the lifecycle side-table, frLifecycle confirms it frozen). Each FIX adds a dedicated committed root (ListCommit.listDigest + compressNInjective + an injective leaf — lifecycleLeaf REUSED for lifecycle/deathCert, auditLeaf=id:Int→felt for the audit slots) + a gate forcing the post root to the written value. cellUnseal_descriptorRefines/cellDestroy_descriptorRefines/refusal_descriptorRefines/receiptArchive_descriptorRefines ⟹ the respective leaf Spec (= the matching execFullA arm; also stated as execFullA …=some post). Forced rungs: lifecycleSetForced (generic target), deathCertSetForced, auditSlotForced. NAMED decode residuals: cellUnseal/cellDestroy carry the off-cell side-table freeze(s)+guard+log+frame; refusal/receiptArchive carry the WHOLE cell-map move (cellMapMove)+guard+log+16-field frame (the per-slot root cannot witness the off-slot/off-cell record content, exactly setField's setFieldCellMap). TEETH: cellUnseal_rejects_unrevived/_wrong_map, cellDestroy_rejects_undestroyed/_wrong_cert (the death-cert-root gate bites — the bind the deployed circuit cannot enforce), audit_rejects_unwritten (slot≠1 ⟹ UNSAT, covers both audit variants). ALL FOUR + cellSeal share ONE Rust realization: cell_state.rs::compute_commitment absorbs lifecycle_root + death_cert_root + audit_slot_root limbs, the trace-fill emits them, ONE VK-epoch rotation. ADDITIVE — recStateCommit/cellCommit/SystemRoots/existing descriptors untouched. #assert_axioms-clean on every theorem (only carried crypto = compressNInjective + the injective leaves, the realizable Poseidon-CR SystemRoots/ListCommit already use).
import Dregg2.Circuit.RotatedKernelRefinementProgram -- SetProgram CLASS-A circuit→kernel refinement (the ordered mid-session program-install / genesis-reframe escape hatch). The DEPLOYED setProgramV3 = graduateV1 (rotateV3WithRecordPin B_RECORD_DIGEST setVKVmDescriptor) is the refusal RECORD-PIN family: SetProgram writes the cell program slot (apply_set_program: c.program=program), which folds into compute_authority_digest_felt → r23 (B_RECORD_DIGEST), so a genuine install MOVES the AFTER record-digest limb (distinct authority surface from VK — the caveat table). setProgram_descriptorRefines_sat consumes Satisfied2 setProgramV3 + SetProgramTraceReadout and forces SetProgramSpec; setProgram_forced pins programField:=prog via the record pin (rotateV3WithRecordPin_pins + ListDigestBindsList); TOOTH setProgram_sat_rejects_unwritten (a frozen-program forgery is UNSAT). Mutation-confirmed: editing setProgramV3 reds the rung. SetProgram has its OWN effect-tag EFFECT_SET_PROGRAM (1<<24) in the Rust executor (action.rs effect_kind_mask), no longer reusing setVK. ⚑ RESIDUAL CLOSED AND THE LINE WAS STALE (struck 2026-07-28): `setProgramA` HAS its constructor, its `execFullA` arm, `actionTag 13`, a `SetProgramSpec` arm, an `actionAirName`, a DEPLOYED position (`actionTagToPos 13 = 51`, `Rfix 13 = setProgramV3`) and its apex closure `closedLogExtract_setProgram_closed` — the full 34 arms across 18 files. This line survived its own completion and was then read forward: a lane priced dregg-in-dregg's blocker as "two effects queued behind one constructor" on the strength of it, and I relayed that. One effect was queued; the other had landed. A residual that outlives its closure does not merely go stale, it gets BUILT ON.
import Dregg2.Circuit.RotatedKernelRefinementAvail -- transfer guardAvail DISCHARGED on the hardened graduable-wide path (the DEBT-A availability forgery, closed in-proof end-to-end). transferV3Avail = v3OfFrozenWide transferVmDescriptorAvail (the §11.7 borrow-weld descriptor; its 15-bit teeth lower per-width via the NEW graduateV1Wide/graduableWide/rangeTidW multi-width graduation in EffectVmEmitV2 §10 + the rotV3FrozenWide_sound_v1 rotation lift — the single-width graduable/rangeRows-30 lowering would have bounded a 15-bit limb only < 2^30, defeating the borrow proof). rotatedEncodesAvail = the hardened decode WITHOUT guardAvail (+ the width-only hdiCanon canonicality envelope, the deployed canonical-element invariant — NOT availability laundered in). availability_and_exact_move_forced = THE DISCHARGE: rotated accept → per-row hardened satisfiedVm → (transferAvail_derives_availability_row, at the row's own flags) amt ≤ pre.bal src a FORCED **and** the EXACT ℤ debit post.bal = pre.bal − amt (strictly stronger than the bare mod-p debit_forced). rotatedEncodesAvail.toEncodes recovers the bare decode with guardAvail PROVEN (every bare-path consumer applies); transfer_descriptorRefinesAvail = the full BalanceMovementSpec refinement with availability from the WITNESS. TEETH: descriptorRefinesAvail_rejects_overdebit + _audit_forgery_unsat (pre.bal=0, amt=10⁹ — forgery 1 of docs/FINDING-modp-wrap-forgery-audit.md — UNSAT on the hardened path). REMAINING (EMBER-GATED, not done here): the v3RegistryBare transfer-entry flip to transferV3Avail + the Rust assembly realizing the 15-bit range table + VK regen — until then production availability stays the named rotatedEncodes.guardAvail residual. #assert_axioms-clean on every theorem.
import Dregg2.Circuit.RotatedKernelRefinementMintBurnAvail -- burn guardAvail DISCHARGED on the hardened graduable-wide path (the DEBT-A well-supply-inflation forgery, closed in-proof end-to-end; the transfer RotatedKernelRefinementAvail template mirrored to burn). burnV3Avail = v3OfFrozenWide burnVmDescriptorAvail (the §8¾ borrow-weld burn descriptor of EffectVmEmitBurn: 15-bit limb decomposition of before/after/BURN_AMOUNT_LO + borrow chain + no-final-borrow gate, teeth lowered per-width via graduableWide/graduateV1Wide + rotV3FrozenWide_sound_v1). rotatedEncodesBurnAvail = the hardened decode WITHOUT guardAvail (+ the width-only hdiCanon canonicality envelope — NOT availability laundered in). burn_availability_and_exact_move_forced = THE DISCHARGE: rotated accept → per-row hardened satisfiedVm → (burnAvail_derives_availability_row, at the row's own flags; burn is debit-only, no direction premise) amt ≤ pre.bal cell a FORCED **and** the EXACT ℤ debit post.bal = pre.bal − amt (strictly stronger than the bare mod-p burn_debit_forced). rotatedEncodesBurnAvail.toEncodes recovers the bare rotatedEncodesBurn with guardAvail PROVEN (every bare-path consumer applies); burn_descriptorRefinesAvail = the full Spec.SupplyDestruction.BurnSpec refinement with availability from the WITNESS. TEETH: burn_descriptorRefinesAvail_rejects_overburn + _audit_forgery_unsat (pre.bal=1, amt=1006632961 — forgery 2 of docs/FINDING-modp-wrap-forgery-audit.md, the well-supply-inflation mint-from-nothing — UNSAT on the hardened path). REMAINING (EMBER-GATED, not done here): the registry burn-entry flip to burnV3Avail + the Rust assembly realizing the 15-bit range table + VK regen — until then production burn availability stays the named rotatedEncodesBurn.guardAvail residual. #assert_axioms-clean on every theorem.
import Dregg2.Circuit.Emit.AvailWireMembers -- the GAP #4 AVAILABILITY FLIP wire members: transferV3AvailWire/burnV3AvailWire = the hardened `…-v1-avail` faces (v3OfFrozenWide transferVmDescriptorAvail / burnVmDescriptorAvail) wrapped through PARAMETRIC twins of the deployed wrappers (gentianDeployedBareRefuseAt at the avail-SHIFTED caveat base + withDfaRcPinsAt at the shifted rc carrier — the fixed-geometry wrappers would read the WRONG columns on the widened face). Peels compose (satisfied2_of_availWireOf) down to the exact objects the RotatedKernelRefinement{Avail,MintBurnAvail} discharges consume; the three capacity-floor refuse dodges re-closed on the hardened members via the column-parametric declared_tag_unsat_at (declared_{escrow,discharge,vault}_unsat_availWire). EmitRotationV3.lean overrides the two cohort keys with these members — the emission retarget of the VK flip; the regen (ember-gated) mints them into rotation-v3-staged-registry.tsv. #assert_axioms-clean.
import Dregg2.Circuit.Emit.AvailWideMembers -- the WIDE-transfer availability members (the wide-transfer wrap-forgery closure's emission half): transferV3MembershipAvailWide (the transferVmDescriptor2R24 wide crown host — the membership-teeth transfer REBUILT over v3OfFrozenWide transferVmDescriptorAvail; teeth PIs 50..51 UNCHANGED, rc pins at the avail-shifted carrier via withDfaRcPinsAt, teeth cols 2617..2618, width 2619) + transferCapOpenTBAvailWide (the transferCapOpenTBVmDescriptor2R24 live-only host = wideAppend of the ALREADY-FLIPPED narrow transferCapOpenEffV3TBAvail at the AVAIL face base 198; width 2948) + transferAvailWideRefused (the crown row under the capacity-floor refuse at the AVAIL caveat base cavBaseOf AVAIL_WIDTH = 676 — the fixed-base gentianWideBareRefuse would decode the WRONG columns). KEYSTONE wideEmbedded_sound_v1: the membership-parametric fusion of rotV3FrozenWide_sound_v1 — wideAppend RETIRES the two legacy 1-felt commit pins, so Satisfied2(wide member) cannot recover Satisfied2(v3OfFrozenWide face); instead, from ANY descriptor whose constraints EMBED the legacy-pin-FILTERED v3OfFrozenWide constraints (+ the decidable hclean: no face constraint is pin-shaped), the FULL per-row v1 denotation of the PRE-ROTATION face returns (base gates via embed, the chained site walk via siteLookups_sound + go_append_left prefix, the 15-bit teeth via lookup_replaces_rangeW) — exactly what transferAvail_derives_availability_row consumes. membershipAvailWide_row_v1 / tbAvailWide_row_v1 instantiate it; transferV3MembershipAvailWide_publishes_teeth carries the fold-arm claim-pin exposure through the retarget at the SAME PI slots; declared_{escrow,discharge,vault}_unsat_availWideRefused re-close the three flag-day capacity dodges on the refused avail member. #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementAvailWide -- transfer availability DISCHARGED on the WIDE + WELDED registry members (the wide-transfer availability wrap-forgery, closed in-proof — the wide member of the GAP #4 full closure; bare transfer+burn aa282f8c0, cap-open RotatedKernelRefinementCapOpenAvail, THIS = the wide leg). Chain: welded accept → satisfied2_of_weldUMemIntoWide (THE WELD PEEL, EffectVmEmitUMemWeldWide: the umem weld appends ONE row-locally-True umemOp + two tables, NO gate/lookup/range/site/mem-op/map-op — so the §11.7 borrow chain survives the weld untouched) → satisfied2_of_gentianDeployedBareRefuseAt (refuse off) → AvailWideMembers.membershipAvailWide_row_v1 (the wide collapse) → transferAvail_derives_availability_row → the kernel statement over the UNCHANGED descriptor-independent rotatedEncodesAvail decode: availability_and_exact_move_forced_welded{Wide,TB} + _refusedWide force amt ≤ pre.bal src a AND the EXACT ℤ debit on the exact post-retarget wire objects (weldedTransferAvailWide / weldedTransferCapOpenTBAvailWide / transferAvailWideRefused — emitted by EmitWideRegistryProbe + EmitWideUMemWeldRegistryProbe under the LIVE keys). transfer_descriptorRefinesAvail_weldedWide = the full BalanceMovementSpec refinement on the welded wide path, guardAvail from the WITNESS. TEETH: weldedWide_rejects_overdebit + weldedWide_audit_forgery_unsat (pre.bal=0, amt=10⁹ — forgery 1) + weldedTB/refusedWide twins. REMAINING (EMBER-GATED): the wide/welded TSV+FP regen mints the retargeted bytes (regen checklist: the Rust membership fold tooth's teeth COLUMNS move 2607→2617; PIs unchanged). #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementMintBurnAvailWide -- burn availability DISCHARGED on the WIDE + WELDED registry members (the wide-burn availability wrap-forgery, closed in-proof — the LAST member of the wrap-class full-closure: bare transfer+burn aa282f8c0, cap-open, fee 3c79e6798, wide transfer 98c148699, THIS = the wide burn). Chain: welded accept → satisfied2_of_weldUMemIntoWide (THE WELD PEEL) → satisfied2_of_gentianDeployedBareRefuseAt (refuse off at the burn-avail caveat base cavBaseOf 196 = 674) → AvailWideMembers.burnAvailWide_row_v1 (the wide collapse via wideEmbedded_sound_v1 — burn carries NO membership teeth, so the crown retarget is rc + wideAppend only, 66 PIs UNCHANGED, width 2607→2615) → burnAvail_derives_availability_row (burn is debit-only: NO direction premise) → the kernel statement over the UNCHANGED descriptor-independent rotatedEncodesBurnAvail decode: wideBurn_availability_and_exact_move_forced (+_refused) force amt ≤ pre.bal cell a AND the EXACT ℤ debit on the exact post-retarget wire objects (weldedBurnAvailWide / burnAvailWideRefused — emitted by EmitWideRegistryProbe + EmitWideUMemWeldRegistryProbe under the LIVE burnVmDescriptor2R24 key). burn_descriptorRefinesAvail_weldedWide = the full BurnSpec refinement on the welded wide path, guardAvail from the WITNESS. TEETH: weldedBurnWide_rejects_overburn + weldedBurnWide_audit_forgery_unsat (pre.bal=1, amt=1006632961 — forgery 2, the WELL-SUPPLY-INFLATION witness) + refused twins; the three capacity-floor dodges re-closed on the refused member (declared_*_unsat_burnAvailWideRefused). REMAINING (EMBER-GATED): the wide/welded TSV+FP regen mints the retargeted bytes. #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementCapOpenAvail -- the CAP-OPEN transfer availability weld (the GAP #4 wrap class closed on the cap-authorized route): transferCapOpenEffV3Avail / transferCapOpenEffV3TBAvail = the LIVE cap-open appendix + #225 turn-identity weld RE-HOSTED on transferV3Avail (the borrow-weld rotated base) — availability circuit-forced through the selector/TB/appendix peels (capOpenAvail_availability_and_exact_move_forced consumes RotatedKernelRefinementAvail.availability_and_exact_move_forced verbatim; rejects_overdebit teeth), authority keystones re-established by instantiating the base-parametric effCapOpenV3_authorizes/effCapOpenV3TB_authorizes at the hardened base (facet gates + non-amp untouched). EmitRotationV3 overrides transferCapOpenEffVmDescriptor2R24 + the TB key to these members (ACK-gated regen mints the bytes; committed registry/VK stay bare until the ember-gated regen). #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementCapOpenAvailWide -- the WIDE cap-open-EFF transfer availability discharge, AUTHORITY INTACT (the wide-cap-open-EFF availability wrap-forgery twin, closed). The transferCapOpenEffVmDescriptor2R24 crown wide host (position 42) retargeted to wideAppend transferCapOpenEffV3Avail at the AVAIL face base (AvailWideMembers.transferCapOpenEffAvailWide, width 2946 / 62 PIs) + its umem-welded twin; wideCapOpenEff_availability_and_exact_move_forced(_welded) forces amt <= pre.bal src a AND the exact Z debit through effAvailWide_row_v1 (wideEmbedded_sound_v1) over the UNCHANGED rotatedEncodesAvail decode; TEETH: overdebit + the audit pre.bal=0/amt=10^9 witness UNSAT. AUTHORITY INTACT: the cap-open appendix rides the wide member verbatim (capOpenAppendix_mem_effAvailWide) and the membership-parametric bridge (capOpenMem_satisfiedEff / capOpenMem_gate_forces) re-establishes wideCapOpenEffAvail_authorizes + rejects_wrong_facet on the wide AND welded members. #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementFeeAvail -- the FEE'D-transfer availability wrap-forgery closed (the fee member of the GAP #4 full-closure). The deployed transferFeeVmDescriptor2R24 debits amount AND fee from BALANCE_LO with no availability check — the wrap is open through EITHER debit leg (fee-leg witness: before=1, amount=0, fee=1006632961 wraps and passes the bare 30-bit ranges). ⚠ the §11.7 chain can't be reused blind (forcing before = after + amount over ℤ against the fee'd gate would force fee = 0 — a liveness break); the §11.8 weld (EffectVmEmitTransfer.transferFeeVmDescriptorAvail) chains through a MID witness: dir-gated borrow chain before−amount=mid + (1−dir)-gated carry chain before+amount=mid + an UNGATED fee borrow chain mid−fee=after (valid in both directions), all 15-bit limbs. transferFeeAvail_derives_availability_row FORCES amount ≤ before ∧ amount+fee ≤ before ∧ after = before−amount−fee on the debit row (credit twin transferFeeAvail_credit_exact); NO fee guardAvail residual existed anywhere in the tower — the closure IS the hardened descriptor + the emission retarget. Rotation lift = rotV3FrozenFeeWide_sound_v1 (v3OfFrozenFeeWide = freeze → fee pin → WIDE graduation, transferFeeV3's composition); wire member AvailWireMembers.transferFeeV3AvailWire (rc pins only — the deployed fee member's wrapper shape, NO refuse); EmitRotationV3 overrides transferFeeVmDescriptor2R24. TEETH: feeWire_fee_forgery_unsat + feeWire_amount_forgery_unsat (both wrap witnesses UNSAT on the hardened wire); Lean-side fee-leg tooth transferFeeAvail_fee_forgery_unsat. Rust twin transfer_fee_avail_weld.rs (16 witness cols, pad 16). VK regen ember-gated. #assert_axioms-clean.
import Dregg2.Circuit.Emit.AvailWideFeeMember -- the WIDE-FEE availability member (the wide-fee wrap-forgery closure's emission half): transferFeeAvailWide (the transferFeeVmDescriptor2R24 wide crown host, tail position 44 = the LIVE SOVEREIGN transfer's effect-vm leg — the narrow hardened wire member transferFeeV3AvailWire = withDfaRcPinsAt FEE_AVAIL_WIDTH (v3OfFrozenFeeWide transferFeeVmDescriptorAvail) wide-appended at the FEE avail face base FEE_AVAIL_BB = 204; the 67-PI layout UNCHANGED — 46 base + fee pin 46 + rc 47..50 + 16 wide anchors; width 2607 → 2623, +16 fee avail pad; NO refuse and NO membership teeth, the deployed fee member's wrapper shape mirrored wrapper-for-wrapper). KEYSTONE wideEmbeddedFee_sound_v1: the FEE-PINNED twin of wideEmbedded_sound_v1 — the fee face composes v3OfFrozenFeeWide = graduateV1Wide ∘ rotateV3WithFeePin ∘ rotateV3FrozenAuthority (one appended .piBinding between freeze and graduation, which the v3OfFrozenWide keystone cannot see); from ANY descriptor whose constraints EMBED the legacy-pin-FILTERED v3OfFrozenFeeWide constraints (+ decidable hclean), the FULL per-row v1 denotation of the PRE-ROTATION fee face returns — exactly what transferFeeAvail_derives_availability_row consumes. feeAvailWide_row_v1 instantiates it. #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementFeeAvailWide -- the FEE'D-transfer availability DISCHARGED on the WIDE + WELDED registry members (the wide-fee availability wrap-forgery, closed in-proof — the fee member of the WIDE wrap-class closure beside the wide transfer 98c148699 / wide burn / wide cap-open-EFF twins). Chain: welded accept → satisfied2_of_weldUMemIntoWide (THE WELD PEEL — the §11.8 MID-linked borrow/carry chains survive the umem weld untouched; NO refuse on the fee key) → AvailWideFeeMember.feeAvailWide_row_v1 (the FEE-PINNED wide collapse wideEmbeddedFee_sound_v1) → transferFeeAvail_derives_availability_row: wideFee_availability_and_exact_move_forced (+_welded) force amount ≤ before ∧ amount + fee ≤ before (availability INCLUDING the fee — BOTH debit legs) ∧ after = before − amount − fee over ℤ on the exact post-retarget wire objects (transferFeeAvailWide / weldedTransferFeeAvailWide — emitted by EmitWideRegistryProbe + EmitWideUMemWeldRegistryProbe under the LIVE transferFeeVmDescriptor2R24 key); credit twin wideFee_credit_exact_forced_welded. TEETH: wideFee_fee_forgery_unsat (before=1, amount=0, fee=1006632961 — the fee-leg wrap witness the BARE wide fee member ADMITS) + wideFee_amount_forgery_unsat (before=1, amount=1006632961) + weldedFee twins. Rust: generate_rotated_transfer_shape_with_fee_wide_avail (pad-16 wide fee producer; the SDK wide fee provers key the pad by avail_pad_for_descriptor_name — dormant at pad 0 until the ember-gated TSV regen). REMAINING (EMBER-GATED): the wide/welded TSV+FP regen mints the retargeted bytes. #assert_axioms-clean.
import Dregg2.Circuit.Emit.RotWideCompactS2 -- THE S2 DELETION (Epoch 1) + THE MASTER BRIDGE (the flag-day's proof half): compactS2 M bb laneBase = the wide member with the S2 dead stratum — the two rotated 1-felt Merkle–Damgård chains (124 lookup sites, 992 columns) — DELETED by index-map compaction (isDeadCol/dropIdx/cutBelow O(1) arithmetic forms, so the 57-member emit-time gate runs in interpreter time); compactS2_expand = the proven expansion: a Satisfied2 witness of the COMPACT member re-derives Satisfied2 of the ORIGINAL (expandTrace recomputes the chain columns via the walk plan, the chip table extends GENUINELY — ChipTableSoundN preserved via expandTrace_chipSoundN — every surviving column untouched), so every wide keystone transports onto the deployed compact bytes; compactOk = the whole decidable side-condition bundle (exact dead-pair lookup shape + no surviving constraint/hash-site/range touches a dead column + planOk walk well-formedness + band disjointness bb+494 ≤ laneBase) — the emit gate AND the bridge hypothesis, ONE predicate; compactS2Checked_ok: a checked compaction only returns under a TRUE compactOk (fail-closed). Consumed by WideCompactTable (the emit table) + RotatedKernelRefinementAvailWideCompact (the crown corollary). #assert_axioms-clean.
import Dregg2.Circuit.Emit.WideCompactTable -- the per-member S2-compaction GEOMETRY TABLE for the wide emit drivers: bbFor key = the block base per wide registry key (positional v3RegistryWideBB/v3RegistryCapOpenWideBB zips + the avail-retargeted/live-only specials that shadow them — transfer/burn/cap-open-EFF/fee/TB at their AVAIL face widths, heapWrite/supplyMint at their splice faces); compactForEmit = compact a member at its tabulated bb with the lane base READ OFF the member's own first S2 lookup (s2LaneBaseOf — never tabulated), FAILING CLOSED unless the whole compactOk bundle holds (a wrong bb cannot mis-compact — it refuses the emit; the values are CHECKED, not trusted). Consumed by EmitWideRegistryProbe / EmitWideUMemWeldRegistryProbe / EmitWideTransferProbe — the drivers that minted the deployed Epoch-1 bytes (all 57×2 members passed the gate; the dead-stratum verdict survived its live falsifier).
import Dregg2.Circuit.Emit.RotWideCompactE1 -- THE E1 DELETION (Epoch 1, SECOND flag-day: docs/EFFICIENCY-BACKLOG-circuit-minimality.md item E1) + ITS MASTER BRIDGE. compactE1 M ks = the wide member with the DEAD v1-face bands DELETED (per-member kill-set ks: every column at index ≥ AUX_BASE=90 referenced by ZERO surviving constraint/hash-site/range — the retired aux band 90..187 incl the 60-col balance bit-decomp superseded by the 15-bit-limb avail weld, + the gentian refuse tail). deadColsE1 M floor = the DERIVED kill-set (a filtered List.range — no hand-listing; the same refs-completeness the gate checks). dropIdxG ks = the general index map (a survivor falls by the killed count below it; the IDENTITY on the offset-encoded .transition face columns, since floor 90 > every saCol ≤ 89 — the S2-proven pattern). Unlike S2 the E1 kill-set is PURE-DEAD (referenced by nothing surviving), so compactE1_expand — a Satisfied2 witness of the COMPACT member re-derives Satisfied2 of the ORIGINAL — needs NO permutation walk and NO chip-table extension (the auxiliary tables are UNCHANGED; killed columns alias the compact row and constrain nothing). compactE1Ok = the decidable emit gate AND bridge hypothesis (value-preservation: no surviving constraint's meaning changes; transitions below the kill-set). compactE1Ok_deadColsE1 / compactE1Ok_of_ceiling = the GENERIC proof the derived kill-set ALWAYS passes the gate (killability IS unreferencedness by construction — no per-constraint kernel cross-product; the only member-specific fact is transitionCeilingOk, a cheap transition-only scan). compactE1Checked = fail-closed. #assert_axioms-clean.
import Dregg2.Circuit.Emit.LastRowFrameHardening -- THE LAST-ROW ANCHOR FORGE CLOSURE (flag day: every deployed wide member's constraint list changes). hardenLastRow re-lowers every `.base (.gate b)` as the WHOLE-domain `.windowGate (ofLocal b) (onTransition := false)`, so the row-local frame/economic algebra survives on the LAST row — the row every member pins its published 8-felt AFTER anchor on, and the row on which `holdsVm .gate` was definitionally True. MEASURED first (circuit/tests/last_row_anchor_forge.rs: an honest wide transfer, mutated on row n-1 only and re-hashed honestly, PROVES and VERIFIES with a forged endpoint; circuit/tests/last_row_gate_domain_probe.rs: the honest trace violates ZERO gate bodies on all 64 rows, and the forge's gate residue on row n-1 is the SAME {0,46,47} as on the transition-covered row n-2 — so the vacuity is the `is_transition()` MULTIPLIER, not missing algebra). ofLocal_eval = the lift is value-preserving (WindowExpr is a strict generalization of EmittedExpr); satisfied2_of_hardened = THE REFINEMENT (hardened witnesses satisfy the original, so every existing keystone re-connects; the converse is FALSE and is not claimed); hardened_gate_binds_on_last_row = THE CONTENT (under Satisfied2 of the hardened descriptor every gate body vanishes on the LAST row — refuted for the unhardened descriptor by holdsVm_gate_true, stated beside it). `.transition` is deliberately LEFT on the transition domain (its body reads `nxt`, which wraps to row 0 on the last row). Geometry/PI layout/tables/hash sites/ranges/constraint order all definitionally preserved, so widths and the E1 kill-sets do NOT move. Kernel-clean: propext + Quot.sound only.
import Dregg2.Circuit.RotatedKernelRefinementAvailWideCompact -- THE CROWN COROLLARY of the S2 flag-day (the HORIZONLOG 2026-07-18 per-member-corollary follow-up, transfer member LANDED): the transfer availability tooth RESTATED over the DEPLOYED Epoch-1 compact bytes. transferWideDeployedC := compactS2 transferAvailWideRefused 198 747 — byte source: EmitWideRegistryProbe emits emitVmJson2 of EXACTLY this value under the transferVmDescriptor2R24 key (the s2compact companion line pins the geometry pair). transferWide_compactOk + transferWide_planOk = the per-member side conditions KERNEL-decided (`decide +kernel` — the emit driver's interpreter gate now a kernel fact; NOT native_decide, whose Lean.ofReduceBool would fail the #assert_axioms pin; ~2 min kernel compute). availability_and_exact_move_forced_deployedCompact: a Satisfied2 witness of the DEPLOYED compact row EXPANDS (compactS2_expand) to the pre-compact refuse-carrying crown member and the landed RotatedKernelRefinementAvailWide keystone chain fires — tr.amt ≤ pre.bal src a AND the EXACT ℤ debit, on the object the light client actually resolves, not a retired sibling; deployedCompact_rejects_overdebit = the audit wrap-forgery class UNSAT on the deployed bytes; RotTableSideW_expand transports the wide table side onto the expansion (range pins ride untouched via expandTrace_tf_other). NAMED RESIDUAL: the decode (rotatedEncodesAvail) is stated over the EXPANDED trace — expandTrace_shape pins the expansion row-for-row on every surviving column (every column the decode reads survives); the field-wise transport of the decode structure itself + the sibling crown members (burn/fee/cap-open-EFF/TB + welded twins) remain the standing HORIZONLOG follow-up. #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementAvailWideCompactE1 -- THE CROWN COROLLARY of the E1 flag-day, STACKED on the S2 crown (transfer member LANDED): the transfer availability tooth RESTATED over the DEPLOYED E1-compact bytes. transferWideDeployedE1 := compactE1 transferWideDeployedC (deadColsE1 transferWideDeployedC 90) — the S2-compact transfer row with its 103 dead v1-face columns (aux 90..187 + gentian tail) deleted, 1704→1601; byte source: EmitWideRegistryProbe emits emitVmJson2 of EXACTLY this value under the transferVmDescriptor2R24 key. transferWide_ceilingOk (`decide +kernel` — NOT native_decide; the cheap transition-only scan, ~seconds) → transferWide_compactE1Ok via compactE1Ok_of_ceiling. availability_and_exact_move_forced_deployedE1: a Satisfied2 witness of the DEPLOYED E1 row EXPANDS (compactE1_expand) to the S2-compact crown member and the landed S2 crown fires — tr.amt ≤ pre.bal src a AND the EXACT ℤ debit, on the deployed E1 bytes; deployedE1_rejects_overdebit = the wrap-forgery class UNSAT; RotTableSideW_expandG transports the wide table side (expandTraceG leaves every auxiliary table untouched). Same NAMED RESIDUAL as the S2 crown (rotatedEncodesAvail decode transport + sibling crown members). #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementLifecycleDisc -- WAVE 1 of the record-digest split-reshape: the lifecycle-DISCRIMINANT sub-limb + per-effect disc-transition gates that make a LEDGERLESS LIGHT CLIENT unfoolable on the lifecycle movers (cellSeal/cellUnseal/cellDestroy/receiptArchive). THE LIVE FORGERY closed: the deployed `rotateV3WithRecordPin B_LIFECYCLE` welds the OPAQUE lifecycle_felt=hash(disc‖payload) (limb 29) to FREE PI[38], anchored from the TRUSTED post-cell (proof_verify.rs:253-303, lifecycle_felt_cell(post)) — a FULL-NODE check. For a ledgerless client PI[38] is free, so a prover can publish a frozen seal, a Destroyed→Live resurrection, or Archived-claimed-as-Sealed and verifyBatch accepts. lifecycle_felt cannot gate the disc (it's a hash output), so this COMMITS the disc (u8 0..4) as its OWN sub-limb (discRoot = listDigest lifecycleLeaf — REUSES the lifecycleRoot Poseidon-CR carrier, NO fresh axiom) and GATES the per-effect disc TRANSITION in-circuit (gDiscTransition pins BOTH before+after via discRoot_binds): cellSeal Live(0)→Sealed(1), cellUnseal Sealed(1)→Live(0), cellDestroy *→Destroyed(3), receiptArchive *→Archived(4) [lcArchived:=4 added — the deployed Archived disc]. discBeforeForced/discAfterForced FORCE both endpoints with NO trusted post-cell. TEETH (the light-client bites, all UNSAT ledgerless): cellSeal_disc_rejects_frozen (after-disc stays Live), cellSeal_disc_rejects_wrong_after, cellUnseal_disc_rejects_unrevived, cellDestroy_disc_rejects_resurrection (Destroyed published as Live), cellDestroy_disc_before_forced (the no-resurrection-input before-pin), receiptArchive_disc_rejects_wrong_after. RESIDUAL NAMED: the PAYLOAD felt (reason_hash/deathCert/sealed_at) STAYS the prover's (effect data the light client reads from the published effect — the disc is the safety property, not the payload); the disc-sub-limb COMMITMENT realization (a 33rd pre_limb OR a B_LIFECYCLE re-fold to H(disc_felt,payload_felt)) is a VK-epoch flag-day = EMBER-GATED (this module proves gate+teeth against the FIX descriptor, the same beachhead shape as RotatedKernelRefinementCellSeal); the kernel-spec/deployed receiptArchive layer divergence (record-slot vs side-table disc) is WAVE-3. #assert_axioms-clean (only carried crypto = compressNInjective + lifecycleLeaf_injective, REUSED).
import Dregg2.Circuit.RotatedKernelRefinementPermsVK -- THE PRINCIPLED-FIX VALUE-leg circuit→kernel refinements for THREE more VALUE_MISSING effects, fanning the cellSeal/audit template. setPermissions (cell.cell[cell]."permissions":=p) + setVK (cell.cell[cell]."verification_key":=vk): RECORD-slot writes to a PARAMETER value — clone the auditSlotRoot record-slot flavor (REUSED auditSlotRoot/auditLeaf/auditLeaf_injective from the Lifecycle file) but the FIX gate gSlotSet forces the committed slot-root to the digest of the parameter (p/vk), not the one-shot 1. slotSetForced: the gate ⟹ fieldOf f (post.cell cell) = target (via auditSlotRoot_binds). setPermissions_descriptorRefines/setVK_descriptorRefines ⟹ SetPermissionsSpec/SetVKSpec (= the .setPermissionsA/.setVKA arm of execFullA; also stated as execFullA …=some post). NAMED decode residual: the WHOLE cell-map move (cellMapMove = setPermsCellMap/setVKCellMap — the off-slot fields of cell + off-cell records the per-slot root cannot certify, exactly setField's setFieldCellMap) + guard + log + 16-field frame. TEETH: setPermissions/setVK_rejects_wrong_value (slot≠param ⟹ UNSAT — the slot-root gate bites; for setVK this is the upgrade-safety tooth) + _rejects_wrong_map. emitEvent (log:=emitReceipt(actor,cell)::log, whole kernel frozen) is DIFFERENT — a genuine VALUE_FORCED against the LIVE descriptor, NO new root: the deployed sel::EMIT_EVENT constraints (circuit/src/effect_vm/air.rs:840-905) ALREADY (i) freeze the entire per-cell state row (bal_lo/bal_hi/cap_root/fields[0..8] passthrough) and (ii) bind (topic_hash,payload_hash) to PI (per-row PI-equality + effects_hash chain pinned to PI); the receipt row {actor,src:=cell,dst:=cell,amt:=0} is INDEPENDENT of the payload, so EmitEventSpec's receipt-advance + frozen-kernel are all FORCED by the live circuit. emitEvent_descriptorRefines ⟹ EmitEventSpec via the LIVE descriptor (emitEventEncodes introduces NO gate, carries the receipt advance + the live-passthrough kernel frame). TEETH: emitEvent_rejects_wrong_receipt + _rejects_mutated_kernel (an emit that silently moves value is UNSAT — the live passthrough freezes the row). setPermissions/setVK share the Lifecycle file's Rust realization (compute_commitment absorbs a slot-root limb, trace-fill emits it, VK-epoch rotation); emitEvent needs NO Rust change. ADDITIVE — recStateCommit/cellCommit/SystemRoots/existing descriptors/the three leaf specs untouched. #assert_axioms-clean on every theorem (only carried crypto = compressNInjective + auditLeaf_injective for perms/vk; emitEvent carries NO crypto carrier at all — pure live-descriptor).
import Dregg2.Circuit.RotatedKernelRefinementMisc -- THE VALUE-leg circuit→kernel refinements for THREE more effects, classified HONESTLY. makeSovereign (cell.cell[cell] REBOUND to the commitment-only record [(commitmentField, .dig (stateCommitment (cell cell)))]) = PROVEN-FIX: the published commitment digest stateCommitment(pre.cell cell) is NOT in the deployed per-cell preimage hash(bal,nonce,fields,cap_root) (the rebind drops the WHOLE record behind a SEPARATE digest no column pins), so a NEW committed sovereignCommitRoot limb (listDigest auditLeaf over [stateCommitment(pre.cell cell)]) is FORCED to the genuine pre-value commitment (makeSovereign_commit_forced via fixRootBinds); the whole-cell-map rebind (sovereignRebind) + MakeSovereignGuard + log + 16-field frame = NAMED residual. makeSovereign_descriptorRefines ⟹ MakeSovereignSpec (= the .makeSovereignA arm; also execFullA …=some post). setFieldDyn (cell.f:=v at a DYNAMIC param-indexed slot) = PROVEN-FIX: the live setFieldDynVmDescriptor2 binds the value via an UNCOMMITTED memory READBACK at the dynamic address param[SLOT] (fieldReadbackOp/setFieldDyn_readback_genuine = Blum), NOT the per-slot committed write COLUMN setFieldV3 forces — the dynamic address is not a committed column, so a NEW committed dynFieldSlotRoot over the written slot f is FORCED to v (setFieldDyn_slot_forced); kernel leaf = the EXISTING SetFieldSpec actor cell f v (setFieldDyn is the dynamic-slot circuit shape of the same setFieldA effect); whole-map move (setFieldCellMap) + SetFieldGuard + log + frame = NAMED. setFieldDyn_descriptorRefines ⟹ SetFieldSpec. pipelinedSend (log:=pipelinedSendReceipt actor::log, whole kernel LITERALLY frozen) = PROVEN-LIVE, NO new root: the actual Lean PipelinedSendSpec is TOTAL (no guard) and its FRAME is all 17 kernel fields literally unchanged — there is NO nonce field that ticks against this spec (the brief's literal-freeze-vs-nonce-tick mismatch does NOT arise); structurally emitEvent (receipt-advance + whole-kernel freeze), cloned from PermsVK's emitEvent arm. pipelinedSend_descriptorRefines ⟹ PipelinedSendSpec via the LIVE descriptor (pipelinedSendEncodes introduces NO gate). TEETH (both polarity): makeSovereign_rejects_wrong_commitment/_wrong_map, setFieldDyn_rejects_wrong_value/_wrong_map, pipelinedSend_rejects_wrong_receipt/_mutated_kernel. RUST REALIZATION: makeSovereign + setFieldDyn share the slot-root realization shape (compute_commitment absorbs a sovereign_commit_root / dyn_field_slot_root limb, trace-fill emits it, VK-epoch rotation); pipelinedSend needs NO Rust change. ADDITIVE — recStateCommit/cellCommit/existing descriptors/the three leaf specs untouched. #assert_axioms-clean on every theorem (carried crypto = compressNInjective + auditLeaf_injective for the two fix effects; pipelinedSend carries NO crypto carrier — pure live-descriptor).
import Dregg2.Circuit.RotatedKernelRefinementBirth -- THE PRINCIPLED-FIX VALUE-leg circuit→kernel refinements for the CELL-BIRTH family (createCell / createCellFromFactory / spawn), fanning the cellSeal committed-root template to account GROWTH. The load-bearing move of every birth is accounts := insert newCell accounts (the new cell born EMPTY) — a Finset the deployed per-cell commitment hash(bal_lo,bal_hi,nonce,fields[0..7],cap_root) (PER existing cell) binds NO column for (a prover could silently drop/reorder the account index). BINDING MECHANISM (additive — NOT N_SYSTEM_ROOTS+1): a dedicated committed accountsRoot limb = listDigest accountsLeaf compressN (accountsSorted k) over the canonical sorted account index, REUSING the existing AccountsCommit carrier (accounts_eq_of_sorted_eq: equal sorted lists ⇒ equal Finsets, a drop/reorder REJECTED) + compressNInjective + accountsLeaf_injective. gAccountsGrow = the FIX gate forcing the post accounts-root to the digest of insert newCell pre.accounts; accountsGrowForced: the gate ⟹ post.accounts = insert newCell pre.accounts (via accountsRoot_binds), exactly lifecycleSealForced for the account set. createCell_descriptorRefines: createCellGenuineEncodes ⟹ CreateCellSpec (accounts FORCED; the bornEmptyAt per-cell records + guard + receipt + global frame the NAMED residual). createCellFromFactory_descriptorRefines: ⟹ CreateFromFactorySpec (accounts FORCED; the factory VK/fields/caveats install (factoryPostCell/factoryPostCaveats) + born-empty + receipt + frame NAMED). spawn = VALUE_PARTIAL, HONEST: the accounts insert + born-empty growth is FORCED via accountsRoot, but the parent→child CAPABILITY HANDOFF (spawnCapsMap/spawnDelegateMap/spawnDelegationsMap — writes into the per-cell cap side-tables) is NOT forced: the live descriptor pins cap_root FROZEN (gCapPass), and forcing the cap-tree UPDATE needs the OPENABLE sorted cap-tree (cap-reshape PHASE-D), NOT yet available — so the handoff is carried as the NAMED capHandoff/delegateHandoff/delegationsHandoff PHASE-D residual, NOT claimed bound. spawn_descriptorRefines ⟹ SpawnSpec; spawn_accounts_forced is the genuinely-forced leg. Each *_descriptorRefines_execFullA states it against the executor arm directly (execCreateCellA_iff_spec / createCellFromFactoryChainA_iff_spec / spawnChainA_iff_spec). TEETH (both polarity): *_rejects_wrong_accounts (a drop/reorder/missing-insert post is UNSAT — the accounts-root gate bites, EXACTLY what the deployed per-cell circuit cannot reject). SHARED Rust realization: cell_state.rs::compute_commitment absorbs an accounts_root limb, the birth trace-fills emit the grown-set root, ONE VK-epoch rotation across all three. ADDITIVE — recStateCommit/cellCommit/AccountsCommit/existing descriptors/the three leaf specs untouched. #assert_axioms-clean on every theorem (carried crypto = compressNInjective + accountsLeaf_injective, the realizable Poseidon-CR AccountsCommit/ListCommit already use; spawn's cap handoff carries NO extra carrier — it is a NAMED residual, not a forced root).
import Dregg2.Circuit.RotatedKernelRefinementNotes -- THE PRINCIPLED-FIX VALUE-leg circuit→kernel refinements for the NOTE family (noteSpend / noteCreate), fanning the Birth committed-list-root template to the SHIELDED-SET growth effects. The load-bearing move is the GROWTH of a tracked shielded List Nat — nullifiers := nf :: nullifiers (spend) / commitments := cm :: commitments (create) — with bal FROZEN (note effects are balance-NEUTRAL; a prior audit found the live note descriptor forces a transparent bal CREDIT that DIVERGES from the frozen-bal spec, so this is a genuine FIX not a live-realize). The deployed per-cell commitment hash(bal_lo,bal_hi,nonce,fields[0..7],cap_root) binds NO column for the nullifiers/commitments SET. BINDING MECHANISM (additive — NOT N_SYSTEM_ROOTS+1): a dedicated committed noteListRoot limb = listDigest noteLeaf compressN xs over the List Nat DIRECTLY (no Finset/sort — simpler than Birth), REUSING ListCommit.listDigest + ListDigestBindsList + compressNInjective + noteLeaf_injective (Nat↪ℤ). gNoteGrow = the FIX gate forcing the post note-root to the digest of x :: preList; noteGrowForced: the gate ⟹ postList = x :: preList (via noteListRoot_binds), exactly accountsGrowForced for a shielded list. noteCreate = PROVEN-FIX: NoteCreateASpec has NO guard (append-only), so commitmentsRoot FORCES the WHOLE load-bearing content post.commitments = cm :: pre.commitments; receipt + (balance-frozen) frame = the NAMED residual. noteSpend = VALUE_PARTIAL, HONEST: the SET-INSERT post.nullifiers = nf :: pre.nullifiers is FORCED via nullifiersRoot, but the DOUBLE-SPEND FRESHNESS nf ∉ pre.nullifiers is NOT forced — the committed root binds the LIST VALUE not its non-membership, and forcing it in-circuit needs the sorted NON-MEMBERSHIP OPEN (cap-reshape PHASE-D), NOT yet available — so freshness AND the spendProof=true §8 gate are carried as the NAMED freshness/proof decode residuals, NOT laundered as bound. Each *_descriptorRefines_execFullA states it against the executor arm directly (execFullA_noteSpend_iff_spec / execNoteCreateA_iff_spec). TEETH (both polarity): *_rejects_wrong_nullifiers / *_rejects_wrong_commitments (a drop/reorder/wrong-insert post is UNSAT — the note-root gate bites, EXACTLY what the deployed per-cell circuit cannot reject); noteSpend_descriptorRefines_rejects_double (a nf ∈ pre.nullifiers double-spend is contradictory — the freshness residual BITES, the security crux; HONEST: bites on the CARRIED freshness, the in-circuit non-membership open is PHASE-D). SHARED Rust realization: cell_state.rs::compute_commitment absorbs a nullifiers_root limb AND a commitments_root limb, the note trace-fills emit the grown-list root, ONE VK-epoch rotation. ADDITIVE — the two leaf specs (notenullifier/notecommitment) + ListCommit untouched. #assert_axioms-clean (carried crypto = compressNInjective + noteLeaf_injective, the realizable Poseidon-CR ListCommit already uses; noteSpend's freshness carries NO extra carrier — it is a NAMED residual, not a forced open).
import Dregg2.Circuit.RotatedKernelRefinementExercise -- THE VALUE-leg circuit→kernel refinements (or HONEST classification) for the THREE awkward effects left open. exercise = PARTIAL-named-residual: ExerciseSpec = innerFacetsAdmittedA=true ∧ exerciseGuard (hold-gate cap MEMBERSHIP, discharged in-circuit by the deployed cap-open — the Facet TransferAuthoritySource template) ∧ turnSpec(exerciseHoldState pre actor) inner post (the inner FOLD, each inner step its OWN per-row descriptor — admissibility deferred there). exerciseEncodes carries the three legs NAMED (facetMask/holdGate/innerFold); exercise_descriptorRefines ⟹ ExerciseSpec pre actor target inner post (= the .exerciseA arm; also execFullA …=some post via execFullA_exerciseA_iff_spec). Teeth: rejects_unheld (hold-gate bites), rejects_facet_violation (R4 mask bites), rejects_wrong_inner_post (the inner fold pins the post). custom = OUT-OF-SCOPE — no kernel arm: FullActionA has NO customA constructor and fullActionStep has NO CustomSpec; customVmDescriptor2R24 is the RECURSIVE-PROOF-BINDING circuit (a nested verifier-proof digest bound to PI) + .custom is an AUTHORITY MODE (AuthModes), both ORTHOGONAL to the RecChainedState step the rung quantifies over — there is no effect to refine to (no_customA_arm records it). heapWrite = ⚠⚠ THIS ANNOTATION WAS TWICE-STALE AND IS CORRECTED HERE (2026-07-25). (1) It said "OUT-OF-LIVE-APEX (ABSENT from v3Registry — has an EmittedDescriptor but no rotated entry)" and "RUST SCOPE to make heapWrite LIVE … NOT done": heapWriteVmDescriptor2R24 IS in BOTH committed registries (narrow circuit/descriptors/rotation-v3-staged-registry.tsv line 47 as dregg-effectvm-heapWrite-splice-v1-rot24-v3-staged, WIDE rotation-wide-registry-staged.tsv as dregg-effectvm-heapWrite-v1-rot24-v3-write-heapopen), the apex ranges over it (CircuitSoundnessAssembled Rfix 56 = heapWriteV3 at v3RegistryHeap tail position 45), and the DEPLOYED LIGHT CLIENT RESOLVES THE WIDE MEMBER (turn/src/executor/proof_verify.rs, LIVE_ONLY_BARE_KEYS — it is admitted in bare-wide form because its multi-domain before→after projection refuses the single-domain umem weld). The narrow member is the stranded one; the wide member is live. (2) The PREPEND-ACCUMULATOR advance it describes was REPLACED by the genuine sorted-Merkle SPLICE (a .write MapOp on the 8-felt heap-root groups; heapRootHolds / heapAdvanceOf / heapRootAdvance_forced are the retired Phase-D story) — and the leaf shape it names is ALSO retired: arity-2 leafOf(addr, value) stopped being the deployed heap leaf on 2026-07-12 (919b2b0b8d), which made it arity-3 hash[addr, value, next_addr] (heap_root.rs::HeapLeaf::preimage, HEAP_LEAF_ARITY = 3, HEAP_SENTINEL_LEAVES = 1). ★ WHAT THE EMIT SIDE CARRIES, measured 2026-07-25 in EffectVmEmitHeapRoot §8: the load-bearing binding is siteHeapAddr (the splice MapOp's KEY), and siteHeapLeaf WAS a VESTIGE — an arity-2 hash[addr,value] chip lookup EMITTED into all three deployed registries (narrow out0 103, wide + umem-welded out0 91) that is no longer any leaf of any deployed tree and that NO constraint in the descriptor read. It was a DEAD PIN, not a wrong binding: it relaxed nothing, it cost a Poseidon2 request per row, and it made a false claim. The leaf that DOES authenticate against the committed root is the arity-3 IMT leaf at native 8-felt width in the heap-open READ appendix + after-spine (HeapOpenEmit.heapLeafInputs, #guard-pinned at length 3, heapLeafDigest_sound8), which WAS moved with the tree and is correct. ⚑ THE FLAG-DAY WAS TAKEN 2026-07-26 (ember-authorized VK epoch): heapSpliceSites = [siteHeapAddr], the arity-2 lookup is GONE from all three registries, and the structural tooth is FALSIFIABLE (heapSpliceSites_have_no_HEAP_LEAF_site with readding_siteHeapLeaf_breaks_the_tooth as the machine-checked "re-add the site → RED", plus the deployed-byte half in circuit/tests/heap_write_deployed_root_forced.rs). Re-POINTING the site at arity 3 was refused as a felt-width regression (a second 1-felt commitment of a fact already forced at 8 felts). heapSplice_leaf_forced is DELETED — the name invited "the heap leaf is forced" and it only ever forced a retired digest. The VK for heapWriteVmDescriptor2R24 moved with the bytes; priced as a RE-GENESIS not a migration (498d27a2b8 / f97c561c8b), and for this member there is no live selector at all, so no committed turn of it exists. heapWriteEncodes carries the recompute-honest row pinning newRoot to the recompute (newRootIsAfter) + the register write (setFieldCellMap heap_root) + the heap splice (heapWriteHeapsMap — Heap.set, sorted-tree recompute is the PHASE-E residual) + SetFieldGuard + log + 14-field frame. heapWrite_newRoot_forced exhibits the pinned newRoot; heapWrite_descriptorRefines ⟹ HeapWriteSpec (= the .heapWriteA arm; also execFullA …=some post). Teeth: rejects_wrong_value (two rows pinning the SAME newRoot wrote the SAME value — the recompute anti-ghost bites; needs Poseidon2SpongeCR) + rejects_wrong_splice. ⛑ THAT ⚠ IS DISCHARGED (2026-07-30). It said BOTH teeth and heapWrite_realizes_heapSet / _sat_rejects_forged_root / _sat_rejects_wrong_splice_root were stated over mapRoot (Heap.set …) = the RETIRED arity-2 commitment, i.e. were theorems about an object the prover never folds. The DENOTATION MOVED: DescriptorIR2.writesTo now denotes padImtRoot MAP_SENTINEL (arity-3 IMT leaves, deployed relink, sparse zero padding), so the six theorems phrased in writesTo were rebound onto the deployed root for free, and the three phrased in mapRoot were restated onto it. heapWrite_realizes_heapSet's occupancy conjunct weakened from = 2^d to ≤ 2^d — NOT a narrowing to keep a green: the deployed tree is sparse, so the = form was a stronger claim about a heap the prover never commits. The two anti-ghost teeth also dropped their Poseidon2SpongeCR binder for the per-instance residual (¬ WriteColl), pinned by #assert_not_depends_on. ⚠ hCR does NOT by itself imply the new residual: WriteColl's PadGhost3 disjunct — a live arity-3 leaf digest equal to the literal BabyBear::ZERO padding — has no arity-2 analogue and is a FIXED-TARGET PREIMAGE, which collision-resistance does not exclude. Closing it is a heap_root.rs change. The deployed-BYTE splice tooth remains circuit/tests/heap_write_deployed_root_forced.rs. #assert_axioms-clean (only carried crypto = Poseidon2SpongeCR for the heapWrite anti-ghost; exercise/custom carry NO crypto).
import Dregg2.Circuit.RotatedKernelForestFacet -- THE FAITHFUL WHOLE-TURN (forest) apex: lifts the single-step faithful transfer apex to a LIST of effects landing on a FAITHFUL kernel TURN relation, WITHOUT the toy-authority side-condition the single-step lowering (dispatchArmFacet_to_dispatchArm) carries. (1) fullActionStepFacet fcaps provided — the per-action step identical to ActionDispatch.fullActionStep EXCEPT the .balanceA arm is the FAITHFUL BalanceMovementSpecFacet (authority via the deployed two-axis authorizedFacetB); every other arm verbatim (additive). fullActionStepFacet_balanceA pins the swapped arm. (2) dispatchArmFacet_to_fullActionStepFacet — the KEY win: dispatchArmFacet … 0 pre post ⟹ ∃ fa, actionTag fa = 0 ∧ fullActionStepFacet … pre fa post, NO toy side-condition (witness .balanceA tr a lands the faithful arm directly). (3) turnSpecFacet := Spec.Turn.turnSpec (fullActionStepFacet …) — the faithful turn relation (the faithful executor IS this spec; execFaithful_iff_specFacet is the single-step executor corner). (4) lightclient_turn_unfoolable_forest_facet — the FAITHFUL whole-turn apex: a verified TurnDecodeChain (transfer steps) + TurnEndpoints + floors + hrefines AT dispatchArmFacet ⟹ a genuine declarative faithful turn whose endpoints commit to the published (pre,post); fold via the NEW generic CircuitSoundness.turnDecodeChain_refines_turnSpec_gen (added: the list-induction abstracted over an arm + step relation). Tooth: fullActionStepFacet_forest_rejects_unauthorized (an authorizedFacetB-REJECTED transfer cannot be the faithful .balanceA step at any position). #assert_axioms-clean.
import Dregg2.Circuit.SortedTreeNonMembership -- THE PHASE-D KEYSTONE: the in-circuit sorted-Merkle NON-MEMBERSHIP open (the complement of DeployedCapOpen's MembershipAt open). A key k is ABSENT from the committed sorted tree iff a COVERING GAP opens — two ADJACENT present neighbor leaves lo<k<hi (GapOpen.inner, the REUSED Crypto.NonMembership.sorted_gap_excludes bracketing), or below-min/above-max/empty. KEYSTONE nonMembership_sound: a GapOpen valid against the committed spine ⟹ k ∉ keysOf S root (the analog of attenuate's non-amplification). The "present neighbor" is the DEPLOYED MembersAt opening, so the in-circuit row form NonMemberRowInner rides TWO DeployedCapOpen.Satisfied sub-rows (chip soundness PROVEN, no new chip seam) — nonMemberRowInner_sound: an inner non-membership row ⟹ kCol ∉ keysOf S cap_root. UPDATE keystone update_sound: old root binds spine + k fresh + new root binds sortedInsert k spine ⟹ keysOf newRoot = insert k (keysOf oldRoot) (the gadget the capability family / spawn / noteSpend-insert consumes next); sortedInsert_sorted/mem_sortedInsert proved unconditionally. ONE crypto residue beyond the deployed CapHashScheme: SpineCommits (the sorted-key-spine↔root binding, the SAME residue NonMembership.NonMember/AttestedQuery carry; realizable via the deployed binary-Merkle fold of the sorted padded leaf list), a HYPOTHESIS never an axiom. #assert_axioms-clean. Non-vacuity TRUE (25 bracketed-excluded from [10,20,30]) + ANTI-GHOST (a present key cannot be excluded; sortedInsert grows the set by exactly the fresh key, no-op on a present key).
import Dregg2.Circuit.SortedTreeNonMembershipHeap8 -- THE Heap8Scheme sorted-Merkle NON-MEMBERSHIP twin of SortedTreeNonMembership, over the node8 lane the three DEDICATED accumulator roots (nullifier/commitments/cells) ride (a 2-felt (addr,value) leaf keyed by addr, NOT the cap slot_hash). GapOpen8/SpineCommits8/keysOf8/nonMembership_sound8/update_sound8: the non-membership half of the INSERT-shaped accumulator keystone. The COMBINATORIAL heart is REUSED (Crypto.NonMembership.sorted_gap_excludes + SortedTreeNonMembership.sortedInsert lemmas, both scheme-independent over List ℤ); the SOLE new material is the wrapping over Heap8Scheme.MembersAt8. SpineCommits8 = the realizable sorted-key-spine↔root binding (the deployed compute_canonical_heap_root_8 fold), a HYPOTHESIS never an axiom. #assert_axioms-clean; non-vacuity TRUE (25 bracketed-excluded) + ANTI-GHOST (present key not excludable; insert grows by exactly the fresh key).
import Dregg2.Circuit.IndexedMerkleTree -- THE INDEXED-MERKLE-TREE (IMT) CLOSURE OF GAP #5: heap-sortedness made an IN-CIRCUIT INDUCTIVE invariant so CanonicalHeapExtract (the ∃ h, SortedKeys h ∧ mapRoot h = pre-root premise the 7 mapOp effects carry inside MapOpsColumnLayout.ReconcileGatesAt / MapReconcileFamily, ASSUMED per CANONICAL-HEAP-TREE-INVESTIGATION.md) is DERIVED. The leaf becomes the linked node ImtLeaf = hash[addr,value,nextAddr] (imtLeafHash, CR-injective imtLeafHash_injective) embedding a sorted chain: ABSENCE of k = ONE low-leaf POINTER bracket low.addr<k<low.nextAddr (ImtAbsent — the MapAbsent pathPos-hi=pathPos-lo+1 physical-adjacency gate RETIRES); INSERT = update-low-nextAddr→k + append (k,value,low_oldNext), TWO O(depth) path updates (imtLowUpdate_binds, reusing pathRecompute_binds_updates). ImtSorted = the well-linked strictly-increasing chain (per-leaf pointer property, NOT a 2^16 scan). THE IN-CIRCUIT INDUCTION: genesis_sorted ([MIN→MAX] sentinel chain, a pinnable constant) + imtInsert_preserves (the LOCAL pointer-bracket check preserves ImtSorted, sorted(pre)⟹sorted(post)) + the CHAIN reachable_sorted (Reachable ⟹ ImtSorted). imtSorted_sortedKeys: ImtSorted c ⟹ Heap.SortedKeys (imtToHeap c) — canonicalHeapExtract_of_imt DERIVES the load-bearing sortedness conjunct into {Poseidon2SpongeCR, FRI-LDT}, no separate heap-sortedness assumption. ★ SOUNDNESS imt_double_spend_unsat: under ImtSorted a PRESENT key cannot be pointer-bracket absent (imtAbsent_excludes via imtSorted_dichotomy — the bracket would point THROUGH k) — the §3 out-of-order-25 double-spend is UNSAT. LIVENESS + both-polarity teeth on short literal ℤ chains (no BabyBear field decide, no 2^16 object). #assert_axioms-clean, sorry/carrier-free. Module doc carries the PRECISE deployed-Rust change spec (heap_root.rs leaf format + nextAddr; descriptor_ir2.rs MapOps insert :2817 → update+append + MapAbsent :2880 → pointer bracket; trace_rotated.rs :1415/1516 fill) for the follow-up Rust lane. KEY-GENERIC: ImtLeaf K V with [LinearOrder K], deployed shape = the DEFAULT instantiation (bare ImtLeaf IS ImtLeaf ℤ ℤ); the ordering core (imtSorted_addrs_sorted/imtSorted_dichotomy/imtAbsent_excludes/imtInsert_preserves/mem_imtAddrs_imtInsert/imt_double_spend_unsat) uses only <, ≤, .trans, lt_irrefl — no ℤ arithmetic.
import Dregg2.Crypto.Digest8KeySpike -- THE #4/#10 NOTE-NULLIFIER KEY RECLASSIFICATION: Digest8Key := Lex (Fin 8 → ℤ) (the bare pointwise product order on Fin 8 → ℤ is NOT linear — product_order_incomparable witnesses it; Lex repairs it via Mathlib Pi.Lex.linearOrder). sorted_gap_excludes AND the deployed key-generic IMT keystones (imtAbsent_excludes, imtInsert_preserves) instantiate at the 8-felt lex key DIRECTLY (imtAbsent_excludes_digest8/imtInsert_preserves_digest8 — same object the felt chain deploys, no twin). Teeth run a REAL multi-felt bracket: the lo/e compare decided at felt 7, e/hi at felt 0. Remaining #4/#10 work correctly scoped = the 8-limb lex-compare AIR gadget + leaf widening (1→8 felts), NOT new bracketing math. #assert_axioms-clean.
import Dregg2.Circuit.SortedTreeNonMembershipWide8 -- THE LEAF-SCHEMA WIDENING (felt-width site #10): the sorted-tree NON-MEMBERSHIP wrapper re-keyed from the 1-felt (~31-bit, birthday-collidable) addr felt to the FULL 8-felt Digest8Key. A [LinearOrder K]-GENERIC key-widened wrapper (keyOfW/SpineCommitsW/keysOfW/GapOpenW/GapOpenW.excludesSpine/nonMembership_soundW) reusing Crypto.NonMembership.sorted_gap_excludes for the combinatorics (NOT re-proved — the deployed narrow keysOf8/nonMembership_sound8 are the definitional K:=ℤ shadow), INSTANTIATED at K:=Digest8Key: nonMembership_sound_digest8 authorizes non-membership on ALL 8 felts. NON-VACUITY at the wrapper level (demo_excludes: a real 8-felt gap excludes keyE from a committed [keyLo,keyHi], the keyLo<keyE bracket decided at felt 7). ★ SECURITY DELTA machine-checked (security_delta/lowfelt_would_collide): keyE and the PRESENT neighbor keyLo COLLIDE on the low felt (proj0 keyE = proj0 keyLo) yet are DISTINCT 8-felt keys — the 1-felt projection sees keyE's key as present and cannot authorize its absence, while the full 8-felt key EXCLUDES it; the ~31-bit waist is closed at the proof level. BRIDGE nonMember_digest8: the widened exclusion lands as the generic Crypto.NonMembership.NonMember at Digest:=Digest8Key (a deploy is a re-instantiation, not a re-proof). #assert_axioms-clean (kernel triple). Residuals NAMED (not closed): value-widening (hash_many_8/digest8_to_bytes32), the widened chip ROW (Opens left abstract — its realizing chip is the leaf-widening Rust re-emit), the update_sound8 analogue (needs generic sortedInsert lemmas), and the EMBER-GATED frozen kernel flip (NullifierAccumulator.lean:12-23). Byte-safe additive — DEPLOYED sorted-tree descriptor bytes untouched.
import Dregg2.Circuit.SortedTreeInsertWide8 -- THE INSERT-side leaf-schema widening (felt-width site #10): the sorted-tree FRESH-KEY INSERT (update_sound8 analogue) re-keyed from the 1-felt (~31-bit) key to the FULL 8-felt Digest8Key — the insert-side twin of SortedTreeNonMembershipWide8, closing the named update_sound8-analogue residual. Authors the [LinearOrder K]-GENERIC insert core ONCE (sortedInsertW/mem_sortedInsertW/sortedInsert_sortedW — the mechanical ℤ→[LinearOrder K] widening of SortedTreeNonMembership.sortedInsert; the deployed ℤ lemmas are the definitional K:=ℤ shadow) + the wrapper update_soundW/update_preserves_sortedW over SortedTreeNonMembershipWide8's SpineCommitsW/keysOfW, then INSTANTIATES at K:=Digest8Key: update_sound_digest8 (a fresh 8-felt insert grows the committed set by EXACTLY the key, all 8 felts) + update_preserves_sorted_digest8. CAPSTONE insert_then_no_nonmembership(_digest8) = the insert/exclude DUALITY: after inserting k, ANY non-membership GapOpenW for k against the new root is CONTRADICTORY (present via update_soundW, absent via nonMembership_soundW) — an inserted key admits no subsequent non-membership witness. NON-VACUITY wrapper-level: demo_insert_grows (real 8-felt insert of keyE into [keyLo,keyHi]) + demo_keyE_present_after + demo_insert_strict_growth (keyE present-after/absent-before, bracket at felt 7) + demo_after_sorted + demo_insert_eq (grown spine = EXACTLY [keyLo,keyE,keyHi]) + demo_no_rewitness (the concrete duality tooth). #assert_axioms-clean (kernel triple). Residuals NAMED (NON-Lean/gated): value-widening (hash_many_8/digest8_to_bytes32), the widened chip ROW re-emit (Rust), the EMBER-GATED frozen kernel flip (NullifierAccumulator.lean:12-23). Byte-safe additive — DEPLOYED sorted-tree descriptor bytes untouched.
import Dregg2.Circuit.MapOpWideKey -- THE KIND-D ROOT CAUSE, ADDRESSED: DescriptorIR2.MapOp types root/newRoot as 8-felt groups but key as ONE felt (DescriptorIR2.lean:301-313) — the single line that makes every kind-D felt-width site (WOUND #5/#9/#11/#20) un-widenable PRODUCER-SIDE. Authors MapOpW (key : Fin 8 -> EmittedExpr) BESIDE the deployed node, with narrow_key_is_lane0 (rfl) proving the DEPLOYED MapOp is EXACTLY the lane-0 projection — a conservative extension, not a parallel universe; wire cost a theorem (map-log row 19 -> 26 felts, IMT leaf arity 3 -> 17). ★ THE WELD nothing had: absentBracket_of_lexBlocks composes the EMITTED lex-8 compare (LexCompare8Emit.lexLt8_refines) with the widened pointer-bracket keystone (Digest8KeySpike.imtAbsent_excludes_digest8) — two satisfied lexLt8 blocks + a committed low leaf FORCE absence at the FULL 8-felt key; its anti-DoS twin absentBracket_realizable shows an honest bracket ADMITS both blocks. WIDE LEAF imtLeafHash8 (arity 17 = addr8||value||next8) + imtLeafHash8_binds_or_collides, FLOOR-FREE up to a named collision at the two ABSORBED 17-felt preimages, with imtLeafHash8_injective / wideLeaf_separates CUT OVER (2026-07-26) off Poseidon2SpongeCR onto that per-instance side condition and DE-LISTED from FloorRatchetBaseline, with the anti-launder pair narrowLeaf_conflates (pre-folding addr to lane 0 then hashing STILL conflates keyE/keyLo, for EVERY hash, no CR hypothesis) / wideLeaf_separates. Per-kind denotation HoldsKindW + absentW_sound/insertW_sound/aafiInsertW_sound = nonMembership_soundW/update_soundW INSTANTIATED (zero re-proved tree combinatorics), and insertW_then_absentW_unsat is WHY the insert and open sides must move in ONE epoch. ★ WOUND #20 PRICED IN BOTH DIRECTIONS AS THEOREMS: narrow_only_over_revokes (a projection is a function ⇒ the same-fold-keyed .absent op can only OVER-revoke ⇒ soundness NONE) + narrowAbsent_unprovable (the SAME lane-0 collision refutes EVERY narrow gap witness for an honest fresh key, for every spine and gap shape ⇒ availability HIGH, permanent) + wideAbsent_provable, packaged as dosDelta_site20. NON-VACUITY: demoAncestorOpW reads a real 8-felt key off columns 0..7 (keyAt = keyE, distinguishing limb felt 7) while its lane-0 projection reads the COLLIDING felt. BYTE-SAFE: NEW object only — no descriptor registered, no emit/JSON face, nothing deployed touched (the widened chip row + Rust decoder are the named VK-epoch residual). #assert_axioms-clean (22), no sorry/admit/native_decide.
import Dregg2.Circuit.WholeImageFoldRealization -- ⚑ THE CHIP CLAIMED TO REALIZE AN `hpin` IT PROVABLY DOES NOT. `circuit/src/whole_image_fold.rs`'s banner said the deployed map root is `CanonicalHeapTree::root`, "modelled byte-identically by `MapMerkleRoot.mapRoot`" — and `mapRoot`'s own doc-comment RETRACTS exactly that (since `919b2b0b8d`, the tree became an arity-3 IMT). THREE independent mismatches, not one: arity (2 vs 3), **WIDTH** (`ℤ` vs eight felts — a TYPE WALL, so there was no `hpin` of the theorems' shape at the chip's public input AT ALL), and occupancy (dense `2^d` vs a MIN sentinel over a zero-padded sparse prefix). Blast radius measured and small: zero production callers (all three verifiers classed THEATRE in `baseline/production-callers.tsv`), in no registry/manifest/VK, and the chip is itself ledgered hand-written Rust AIR — so the cost was a ratchet-counted SPEC-APEX claim, and nothing re-emits. ⚑ REPAIRED BY MOVING THE THEOREMS, NOT THE WORDS: this module authors `padImtRoot8`/`wholeBoundaryFold8` — the object the chip actually folds — with a floor-free binding, the whole no-extra-cells cone re-proved over it, and `padImtRoot8_ne_arity2Root8` as the separation AT THE CHIP'S OWN WIDTH. Editing the banner to agree with whatever was there would have made the claim true by weakening it, which is the laundering this repo forbids. Tooth: `circuit/tests/whole_image_fold_lean_correspondent.rs` performs six drifts (leaf arity, preimage order, sentinel, padding constant, node packing, relink) and shows the root moves on each, so the gate demonstrably goes red. ⚠ NAMED AND LEFT OPEN: the FLAT twin `*_bound_mem` is the same class — documented as realizing `DescriptorIR2.satisfied2_init_whole_image` over the flat sponge while reusing `build_whole_image_fold`, so it pins a MERKLE root, a third object again. Its cross-table teeth are real; what is unproved is that they discharge that Lean family.
import Dregg2.Circuit.MapOpWideDigest8 -- ⚑ THE WIDTH THAT ACTUALLY BUYS BITS — the DIGEST, not the preimage. The kind-D epoch widened the ABSORBED PREIMAGE (map leaf 2→9, IMT leaf 3→17) and left the digest at ONE BabyBear felt, so on `RomQueryFloor.birthday_bound` every wide-key endpoint still read ≈2^15.5 — a BREAK, not a bound, and the number is invariant in key width because injectivity names no width. This module keeps the image in `Digest8` all the way to the root (`leafOfW8` over the byte-identical preimage `leafPreW` → `chipAbsorb8`, folded by `MapMerkleRoot` §5b's arity-16 `node8`/`perfectRoot8`). **A some/none equivocation behind one committed root costs ≈2^15.45 queries at `mapRootW` and ≈2^123.63 at `mapRootW8`** — and both are now THEOREMS rather than docstrings, with the break points exact: the one-felt bar dies at Q = 44870, the eight-felt bar at exactly `babyBearP^4`. Every bar theorem is stated over an ARBITRARY finite domain, which is the kind-D error written in its own general form. HONEST COST, in Lean not prose: node absorb arity 2→16 and committed digest felts `2^(d+1)−1` → `8·(2^(d+1)−1)` = 917,497 extra felts per depth-16 tree, against a bar improving by ≈2^216. Three poles, the third load-bearing: `mapRootCollW8_fires_on_constant_chip` proves SATISFIABILITY, without which the endpoint could have been secretly unconditional — and the floor preflight CAUGHT the first draft smuggling `Compress8CR` as a hypothesis, which was then ported rather than baselined (net new baseline entries: zero). ⚑⚑ AND THE SEAM RUNS THE OTHER WAY FROM WHAT WE ASSUMED: `DescriptorIR2.MapOp` carries `root : Fin 8 → EmittedExpr` and `circuit/src/descriptor_ir2.rs` HARD-ERRORS unless `root.len() = CHIP_OUT_LANES`, while `MapOp.holdsAt:577` reads `(m.root 0)` in all five kinds — **the SHIPPING CIRCUIT commits ~2^247 of digest and the LEAN SEMANTICS OF THAT CIRCUIT describes ~2^30.9.** So the 2^15.5 is a property of our MODEL, and the deployed object is very likely stronger; the wound is that our proofs describe the weaker thing (the MapOp denotation/arity disease on a new axis). The named rescue `HeapOpenEmit.heapOpen_writesTo8` is a REAL keystone but lands in the rotated cone and NO declaration relates it to `MapOp.holdsAt` — open and unstated, neither a hole nor closed. Ordered path in `docs/DIGEST-WIDENING-PLAN.md`: ~90 one-felt endpoints, four structural blockers (incl. the INVERSE wound — three sites that take a `Digest8` and squeeze it BACK, where widening is the wrong repair), and the structural reading that FOUR WIDE TOWERS ARE ALREADY BUILT and axiom-clean with the narrow twin still load-bearing in every case — so the remainder is re-pointing and DELETING, not construction.
import Dregg2.Circuit.MapOpWideKeyGate -- THE WIDENED MAP-OP DENOTATION + GATE MODEL at the 8-felt key (kind-D epoch, chunk 2, byte-safe): what sits between MapOpWideKey's widened IR node and a deployable widened AIR. S4 — the key ABSORPTION SCHEDULE is factored out as LaneEnc K (how many felts of the key a leaf digest eats), and leafOf/mapRoot/opensToMerkle/writesToMerkle are re-stated over it; the DEPLOYED objects come back as the narrowEnc instance DEFINITIONALLY (leafOfW_narrow / mapRootW_narrow / opensToMerkleW_narrow / writesToMerkleW_narrow, all rfl), and MapOp.holdsAt / ReconcileGatesAt are recovered by Iff.rfl (narrow_holdsAt_is_instance / narrow_gates_is_instance) — the kernel, not the eye, certifies that the generalization changed no deployed meaning. ★ L3 CLOSURE (the epoch's first NON-ADDITIVE change): LaneEnc also carries the ADMISSIBLE committed-heap shape as a FIELD (HeapOk, with heapOk_sorted / heapOk_set), and opensToMerkleW / writesToMerkleW / ReconcileGatesAtW quantify over it instead of bare Heap.SortedKeys — narrowEnc.HeapOk IS Heap.SortedKeys (so ALL SIX conservativity rfls above survived untouched), wideEnc.HeapOk h is Heap.SortedKeys h ∧ CanonHeapW h, so the committed spine is CANONICALLY KEYED by definition and MapOpWideKeyRowBoundary's alias instance is no longer an admissible commitment. ⚠ CanonHeapW did not become gate-FORCED (it cannot — gate_teeth_cannot_force_canonicity); it became DEFINITIONAL, sitting exactly where Heap.SortedKeys already sat, inside the knowledge-extraction premise. wideEnc and the three symbolic roots downstream are noncomputable (HeapOk names the classical Digest8Key order); the two arity #guards became rfl THEOREMS, i.e. kernel-checked instead of evaluator-checked. S8 — the THREE PER-KIND OPENERS at [LinearOrder K] (opensToMerkleW_of_path / opensToMerkleW_none_of_bracket / writesToMerkleW_of_path) plus the widened MapOps law reconcileGatesW_force_openingW, with the deployed narrow openers RE-DERIVED as instances (deployed_{read,bracket,write}_opener_is_instance). pathRecompute_binds_updates / perfectRoot_injective / every Heap lemma / sorted_gap_excludes are COMPOSED, never re-derived — the key width never enters the digest-vector fold. S7 — the ARITY-17 leaf schema at ImtLeaf Digest8Key ℤ (addr8 || value || next8, imtLeafInput8_length = 17 by rfl) with imtLeafHash8Of_binds_or_collides FLOOR-FREE and imtLeafHash8Of_injective / wideLeaf_slot_binds / wideLeaf_kills_the_pointer_forgery CUT OVER (2026-07-26) onto the per-instance non-collision at the two NAMED 17-felt preimages and DE-LISTED from FloorRatchetBaseline. ★ THE DESIGN'S BLOCKER #2 IS NOW A THEOREM: halfWideLeaf_forges_absence_of_present — widening the ADDRESS while the POINTER stays projected to lane 0 gives the honest low leaf <keyLo,1,keyE> and the fabricated <keyLo,1,ptrHi> the SAME digest, for EVERY hash with NO CR hypothesis, and the fabricated pointer bracket (keyLo, ptrHi) CONTAINS keyE, an address the honest chain PRESENTS: a non-membership FORGERY, not a hardening. wideLeaf_kills_the_pointer_forgery / wideLeaf_slot_binds are the arity-17 repair. S5/S6 — AafiGatesAtW (arity-17 leaf, 8-felt lex pointer bracket) + aafiInsertW_forces_imtInsertW (the two-path append law, key width substituted; pathRecompute_binds_updates used TWICE verbatim) + aafiGatesW_force_imtAbsentW, and aafiBracketW_of_lexBlocks welds the EMITTED lexLt8 blocks to BOTH gate (b) and the chain-level exclusion through MapOpWideKey.absentBracket_of_lexBlocks. ★ BLOCKER #1 (insert and open MOVE TOGETHER) in BOTH directions: insertW_absentW_jointly_unsat (same width, both legs — composed from HoldsKindW, zero combinatorics restated); narrowInsert_wideOpen_double_spend (narrow insert + wide open = the key is CONSUMED and its absence still PROVABLE); proj0_bracket_sound_but_incomplete (a lane-0 bracket implies the lex bracket, so a narrow gate never forges — it BRICKS: keyLo/keyE share lane 0 and admit no narrow bracket at all). NON-VACUITY: a REAL accepting widened .absent gate at depth 1 over an ARBITRARY hash (demoAbsentGateW_accepts, every path obligation rfl) forcing a genuine wide non-membership of keyE (bracket decided at felt 7 — the limb the deployed projection discards), with teeth demoAbsentGateW_rejects_present (a forged .absent on a PRESENT key is UNSAT) and demoRows_indistinguishable_narrowly (the deployed one-felt key column carries the SAME value on both rows). Install surface #guarded: bracket compare 2x26 -> 2x40 cols (+28), IMT leaf 3 -> 17, map-tree leaf 2 -> 9. BYTE-SAFE: NEW objects only — no descriptor registered, no emit/JSON face, no deployed byte touched; the widened chip ROW and the Rust decode are the named VK-epoch residual (W3/W4). Floors: Poseidon2SpongeCR ONLY (no new floor). #assert_axioms-clean (35), no sorry/admit/native_decide.
import Dregg2.Circuit.MapOpWideKeyWeld -- ★ THE WELD THE GATE LANE NAMED AND DID NOT CLAIM, plus the widened ImtSorted-preservation it unblocks (kind-D epoch, chunk 3, byte-safe). MapOpWideKeyGate closed with TWO named open items; both are closed here. ① THE WELD: opensAtW hash E dep r e := opensToMerkleW hash E dep r e.1 (some e.2) is the CONCRETE opening predicate the abstract Wide8 Opens was always a stand-in for, and spineCommitsW_of_heap DISCHARGES SortedTreeNonMembershipWide8's SpineCommitsW at it from a committed sorted 2^dep-leaf heap — the wrapper's crypto residue, described there as "a HYPOTHESIS, never an axiom", is now a THEOREM at the concrete Merkle instantiation under the SAME named Poseidon2SpongeCR floor (forward = the mapRootW_injective anti-ghost, backward = Heap.get_eq_none_iff). Everything the abstract layer says about keysOfW then transports BOTH ways (notMem_keysOfW_of_opensNone / mem_keysOfW_of_opensSome), so ★ absentGatesW_force_keysOfW_absence: an ACCEPTING widened .absent gate FORCES k ∉ keysOfW — the predicate HoldsKindW/dosDelta_site20/the whole Wide8 wrapper speak in, now gate-forced rather than assumed. The abstract keystone FIRES at the concrete predicate (concrete_nonMembership_soundW) and its conclusion transports BACK to the deployed opening (concrete_gap_forces_opensNone). ★ BLOCKER #1 RE-DERIVED SO IT BITES ON THE EMITTED GATE: gates_insertW_absentW_jointly_unsat (+ the .aafiInsert twin) — two accepting widened rows, an insert of the 8-felt k moving r→r' and an .absent of the SAME k against r', are jointly UNSAT under CR; gates_jointly_unsat_via_abstract routes the SAME refusal through MapOpWideKeyGate's abstract insertW_absentW_jointly_unsat at Opens := opensAtW, showing the abstract theorem is not a parallel statement but THIS one at an instantiation now inhabited by emitted rows. ★ FINDING (writesToMerkleW_forces_present): at the MAP-TREE layer a write PINS its key already committed — (Heap.set h k v).length = 2^dep = h.length is satisfiable only when k ∈ Heap.keys h (length_set_fresh grows by one otherwise) — so map-tree .insert/.write/.aafiInsert denote an in-place UPDATE and genuine fresh-key GROWTH lives at the AAFI/IMT layer, which is exactly item ②. ② ImtSorted-PRESERVATION AT THE WIDENED KEY THROUGH THE GATE: the gate lane's "blocked below IMT in the module DAG" is an ARTIFACT — the NARROW twin had to sit inside IndexedMerkleTree because its input (MapOpsColumnLayout.aafiInsert_forces_imtInsert) is BELOW imtInsert, but this file's closure is MapOpWideKeyGate → MapOpWideKey → SortedTreeInsertWide8 → Digest8KeySpike → IndexedMerkleTree, so imtInsert_preserves_digest8 and aafiGatesW_force_imtAbsentW are in scope TOGETHER. aafiGatesW_force_sortedChainW (the wide twin of aafiGates_force_sortedKeys: ImtSorted post-chain + its Heap.SortedKeys projection via the key-generic imtToHeapW, ImtAbsent premise GATE-FORCED) + aafiGatesW_force_spine_growthW + ★ aafiGatesW_no_rewitnessW (the chain-level double-spend refusal the preservation UNLOCKS — imtAbsent_excludes_digest8 consumes ImtSorted of the POST-chain) + aafiGatesW_post_chain_commitsW (§1∘§3: the sortedness ② forces IS the hypothesis ① consumes, so the insert side and the map-opening side are ONE object at the widened key). NON-VACUITY reuses the gate lane's arbitrary-hash depth-1 witnesses: demo_concrete_excludes and demoAbsentGateW_forces_keysOfW_absence derive the SAME absence of keyE by two INDEPENDENT routes (abstract gap keystone; emitted path gates); demoInsertGateW_accepts is a real ACCEPTING widened write row and demoInsert_then_absent_unsat REFUSES every .absent gate for its key at the post-root (the concrete double-spend witness) while demoAbsentGateW2_accepts shows the SAME post-root still accepts absence for the genuinely-fresh keyE (the refusal is TARGETED, not blanket); demoAafiGateW_accepts is a real accepting widened AAFI row (arity-17 leaf, 8-felt bracket, every path obligation rfl) whose post-chain is ImtSorted and admits no re-witness. LABOUR, honestly: the bridge itself IS one lemma, but making the joint-unsat bite needed 9 supporting lemmas (two transports, the Heap.set growth/length bookkeeping, the per-kind HoldsKindW derivations) — no new mathematics. BYTE-SAFE: NEW object only — no descriptor registered, no emit/JSON face, no deployed byte touched; the widened chip ROW and Rust decode remain the named VK-epoch residual (W3/W4). Floors: Poseidon2SpongeCR ONLY (no new floor). #assert_axioms-clean (43), no sorry/admit/native_decide.
import Dregg2.Circuit.MapOpWideKeyCanonDischarge -- ★ THE `KeyCanon` DISCHARGE (kind-D epoch, chunk 4, byte-safe): the wide-MapOp-key lane carried `KeyCanon k := ∀ i : Fin 8, 0 ≤ k i ∧ k i < p` as a BARE hypothesis at four weld sites (absentBracket_of_lexBlocks / absentBracket_realizable / MapOpWideKeyGate.absentGateW_of_lexBlocks / aafiBracketW_of_lexBlocks) because lexLt8_refines consumes it, and NOTHING forced it. ① MEASUREMENT (lex_lookup_tuples, a rfl on the deployed constraint list): the emitted lex-8 block's ONLY ℤ-exact teeth are three 27-bit lookups on the WITNESS columns 30/37/39 — not one of the 16 KEY cells is range-checked. ② ★ REFUTATION, not a repair-by-more-teeth: gate_teeth_cannot_force_canonicity — for ANY constraint list whose lookups avoid column j (i.e. any enlargement whatever of the gate set), satisfaction is INVARIANT under adding p to column j, so a raw ℤ cell's canonicity is unforceable by gates (a gate is a mod-p fact) and no power-of-two range table has content [0,p) (2^27 < p < 2^31); lexBlock_invariant_under_p_shift instantiates it at the emitted block. The design's W3 note "the emit must range-check sixteen cells per compare" is therefore WRONG about the fix: sixteen more teeth discharge nothing. ③ ★ THE DISCHARGE: lexLt8_refines_canon is lexLt8_refines with BOTH canonicity hypotheses GONE — (∃ env, key cells = a,b ∧ lexBlockHolds) ↔ toLex (canonKey a) < toLex (canonKey b), canonKey k i := k i % p, whose KeyCanon is a THEOREM (canonKey_isCanon). Proved by TRANSPORT, not re-derivation: EmittedExpr is var/const/add/mul so gate satisfaction is congruence-blind (eval_congr), and block_congr_general moves a satisfying row across any cell-wise congruence that FIXES the looked-up columns (structural pin lex_shape). The original lexLt8_refines returns as a corollary (lexLt8_refines_of_canon). ④ ALL FOUR WELD SITES re-derived hypothesis-free at the canonical reading (absentBracket_of_lexBlocks_canon / absentBracket_realizable_canon / absentGateW_of_lexBlocks_canon / aafiBracketW_of_lexBlocks_canon), each with its hypothesis-carrying original recovered from it — nothing downstream loses a theorem. ⑤ ★★ LOAD-BEARING CANARY (the ShieldedValueRangeDischarge shape): forgeKk reads p+5 in lane 0 (residue 5) and forgeChain = [0 → 6, 6 → p+5, p+5 → p+6] is ImtSorted and PRESENTS p+5; forge_blocks_hold proves the emitted gadget ACCEPTS both bracket rows, forge_raw_bracket_false proves the high bracket is a LIE raw (p+5 > 6), and rawBracketClaim_false proves the KeyCanon-FREE RAW weld is FALSE — it "proves" a PRESENT key absent (a non-membership forgery) — while canon_weld_survives proves the DISCHARGED weld TRUE on the SAME trace. ⑥ W3's real obligation is WIRING, not teeth: keyAtCanon/keyAtCanon_of_block state exactly what the emit must feed (the same columns into the compare block and into the map-op key group / arity-17 leaf). Floors: NONE added — no crypto at all in this file, the range-table content is STRUCTURAL. BYTE-SAFE: NEW file, no descriptor/emit/golden/JSON byte touched. #assert_axioms-clean (27), no sorry/admit/native_decide.
import Dregg2.Circuit.MapOpWideKeyRowBoundary -- ★ THE ROW BOUNDARY of the wide-MapOp-key epoch, CLOSED — and the next boundary down, NAMED and CANARIED (kind-D epoch, chunk 5, byte-safe). The canonicity lane discharged KeyCanon but left one seam: MapOpWideKeyWeld's gate-forced results (absentGatesW_force_keysOfW_absence, gates_insertW_absentW_jointly_unsat + the .aafiInsert twin, the per-kind gates_force_holdsKindW_*) speak in an ABSTRACT k : K — keyAt/keyAtCanon appear ZERO times in that file — while the place a trace row's key is actually READ (MapOpWideKeyGate.gatesAtW / holdsAtW / MapOpW.HoldsW) reads it RAW via MapOpW.keyAt, re-importing at the row exactly the hypothesis the discharge deleted. ① RowKeyWiredW is the W3 wiring obligation as ONE proposition (the row's 8 key columns are the HIGH block's a-side cells and the LOW block's b-side cells) and RowKeyWiredW.wireLo DERIVES that both brackets are about one key rather than two; RowKeyWiredW.keyAtCanon_eq (keyAtCanon_of_block composed) gives keyAtCanon m a = toLex (canonKey kk) with NO hypothesis on the row's cells, and keyAtCanon_row_isCanon makes KeyCanon at the row boundary a THEOREM for every row and every assignment. ② ★ THE FULL COMPOSITION absentRow_of_lexBlocks_forces_absence: two satisfied EMITTED lexLt8 blocks + the row wiring + a committed heap + two adjacent Merkle paths ⇒ the ROW's key, as the compare block reads it, is absent from the gate's own committed key set — the .absent gate is BUILT (absentGateW_of_lexBlocks_canon), not assumed; plus the IMT-chain half, the four gates_force_holdsKindW_*_row legs, the row denotation holdsAtWCanon forced by gatesAtWCanon (mapOpW_gatesCanon_force_holdsCanon), the two-ROW double-spend refusals (gates_{insertW,aafiInsertW}_absentW_jointly_unsat_row + both via_abstract routings) and aafiGatesW_no_rewitnessW_row. The abstract versions are untouched; these are the primary ones for W3. ③ AGREE/DIFFER, both machine-checked: readings_agree + gatesAtWCanon_eq_gatesAtW (on canonical cells the canonical row gate IS gatesAtW — an honest deployed row loses nothing), against ⚑⚑ rowRawBracketClaim_false — RowRawBracketClaim is the row-level bracket weld VERBATIM with the conclusion read at MapOpW.keyAt, and it is FALSE on the discharge lane's own accepting trace lifted to a COLUMN-SHARING row (rowOfVars, key columns = trace columns 0..7): it proves the key p+5 ABSENT from a chain that PRESENTS it, while varRow_composition_fires proves the keyAtCanon statement TRUE on the SAME trace. So the routing is load-bearing, not stylistic. ④ ⚑ L3, THE THIRD AND DEEPEST LEVEL, NOW CLOSED BY DEFINITION: composing at the row had surfaced the NEXT unforced hypothesis ONE LEVEL DOWN, at the EXTRACTION PREMISE rather than the trace row — ReconcileGatesAtW opened with ∃ h and nothing made that heap's KEYS canonical, exhibited by an accepting widened .absent gate over [0 ↦ 1, (p+5) ↦ 1]. The cure was a DEFINITION change and it is now made in MapOpWideKeyGate (LaneEnc.HeapOk): row_absence_is_residue_absence_of_canonHeap LOSES its CanonHeapW hypothesis, absentGate_forces_residue_absence is the hypothesis-free form W3 consumes, and ⚑ aliasRoot_admits_no_gate proves the alias root now admits NO widened gate at all (any key/value/post-root/kind) because the only 2-leaf heap behind it is inadmissible — the old counterexample is UNCONSTRUCTIBLE, kept as that ¬-form with its evidence (aliasHeap_not_canon / aliasHeap_not_heapOk) intact; canonAliasRoot_refuses_absence_of_five is the discrimination side (the CANONICAL twin heap is admissible and refuses for the honest reason). ⑤ ⚑⚑⚑ QUESTION ZERO — THE DEPLOYED NARROW 1-FELT MAP-OP HAS THE SAME ALIAS HOLE, and it is EASIER there (no keyAtCanon needed): narrowAliasGate_accepts builds a REAL accepting MapOpsColumnLayout.ReconcileGatesAt row (depth 1, arbitrary hash, every path obligation rfl) over the DEPLOYED ℤ-keyed heap [0 ↦ 1, (p+5) ↦ 1] whose key column carries 5, narrowAliasGate_forces_absence turns it into opensToMerkle … 5 none through the deployed MapOps law, and deployed_narrow_heapOk_is_only_sortedness (a rfl) shows the deployed extraction premise says NOTHING about canonical keys. CLASSIFICATION: (ii) a MODEL-LEVEL ARTIFACT, NOT a live deployed wound and NOT closed elsewhere in Lean — the deployed blockers are (1) type-level (a committed heap key IS a BabyBear element; p+5 is not an inhabitant) and, decisively, (2) residueBlind_collapses_alias: for a residue-blind leaf absorb — what a sponge over field elements IS — the alias root EQUALS the canonical root, against which absence of 5 is refused for the honest reason (narrowCanonRoot_refuses_absence_of_five), and residueBlind_refutes_spongeCR proves no residue-blind hash satisfies Poseidon2SpongeCR, so the construction is powered by exactly the part of the floor Poseidon2Binding already records as FALSE at deployed parameters. Separately (a real finding, not a wound): the deployed Rust .absent row DOES pin its three compared felts to unique canonical lifts (descriptor_ir2.rs eval_canon_decomp: nibble lookup on hi4, 27-bit lo27 decomposition, uniqueness tooth is15·lo27 = 0) and eval_lex_lt compares THOSE lifts — a constraint ReconcileGatesAt does not model at all, so the deployed narrow AIR is STRICTLY STRONGER than its Lean model on this axis. NON-VACUITY: every row-level statement fires on the epoch's existing accepting rows (demoAbsentGateW_accepts, demoInsertGateW_accepts, demoAafiGateW_accepts, demoAbsentGateW2_accepts, demo_concrete_excludes) through rowOfKey/rowOfVars. Floors: Poseidon2SpongeCR ONLY — NO new floor (ResidueBlindLeaf is a theorem HYPOTHESIS, never a carrier: no keystone depends on it). BYTE-SAFE: no descriptor/emit/golden/JSON byte touched. #assert_axioms-clean (57), no sorry/admit/native_decide.
import Dregg2.Circuit.MapAbsentImtGate -- ⚑⚑ MODEL-vs-DEPLOYED SHAPE MISMATCH on the `.absent` ARM, FOUND, MEASURED, AND RE-DERIVED ON THE SHAPE THAT SHIPS. MapOpsColumnLayout.ReconcileGatesAt's `.absent` arm models the deployed Ir2Air::MapAbsent table as TWO ADJACENT-POSITION paths bracketing the key; the DEPLOYED table checks ONE low-leaf opening whose ARITY-3 digest hash[lo_addr,lo_value,low_next] BINDS the pointer, plus lo_addr < key < low_next on canonical lifts (eval_canon_decomp 13 cols x3 pinning unique lifts + eval_lex_lt 13 cols x2) — NO second leaf, NO pathPos adjacency. Two different constraint systems over two different committed objects. ARM-SPECIFIC: AafiGatesAt / AafiGatesAtW ALREADY carry the pointer-bracket shape; `.absent` is the arm never repointed when heap_root.rs moved to the IMT and DROPPED the MAX sentinel LEAF ("it survives only as the terminal next_addr pointer"). ① AbsentImtGatesAt models the deployed arm 1:1 (committed digest vector NAMED exactly as ReconcileGatesAt's ∃ h, one path at imtLeafHash, the pointer bracket, root preservation) and ★ absentImtGates_force_absence RE-DERIVES the `.absent` soundness on it: an accepting deployed row FORCES key ∉ imtAddrs c and Heap.get (imtToHeap c) key = none, with the low leaf's chain MEMBERSHIP DERIVED (imtLeafHash_injective + perfectRoot_injective), not assumed. ② ★ THE DIRECTION IS ONE-SIDED AND IT IS THE BAD ONE: adjacentBracket_forces_committed_pair shows the model's arm can only fire when TWO PHYSICALLY ADJACENT committed entries bracket the key, hence adjacentBracket_unsat_above_max — ABOVE THE LARGEST COMMITTED KEY THE MODEL HAS NO GATE DATA, for any heap behind the root and any pair of paths — while the deployed last leaf's terminal SENTINEL_MAX pointer brackets exactly those keys. ★★ absent_model_is_incomplete_at_the_top_gap exhibits it on the CR-PROVED refSponge at ONE logical map (topChain projects to MapOpsColumnLayout.toyHeap by rfl): the deployed gate ACCEPTS key 50, ¬AdjacentBracketGates, ★ ¬ReconcileGatesAt (topGap_reconcileGates_unsat — the epoch's `.absent` hypothesis is FALSE on an honest deployed row, so every theorem over it is VACUOUS exactly where the prover operates), and the key IS genuinely absent both faces. This is the ORDINARY path: any fresh nullifier/cell-id above the current maximum lands there. ③ WHERE THEY DO AGREE, AS A THEOREM: imtSorted_next_eq_succ_addr (a leaf's pointer IS its physical successor's addr on an ImtSorted chain) ⟹ ★ interior_pointerBracket_iff_adjacent — at every INTERIOR low leaf the two brackets are the SAME predicate, so most of the mismatch is a NAMED DEPENDENCY on ImtSorted and the residue is exactly ONE SLOT (which is why it went unnoticed). ④ imtLeafHash_ne_heapLeafOf: under CR an arity-3 IMT leaf digest is NEVER an arity-2 heap leaf digest (equal digests ⟹ equal preimage lists ⟹ 3 = 2), so NEITHER constraint system refines the other and ③ is an equivalence of BRACKET PREDICATES on a shared chain, never a translation of gate data — which is why (ii) prove-them-equivalent was IMPOSSIBLE and (i) model-the-deployed-shape was the only honest route. ⑤ ImtSorted STATUS, checked in the code not the comments: PER-TURN it IS forced — write/read hold the pointer fixed BY COLUMN SHARING (both leaf absorbs read MAP_NEXT), the AAFI flip HAS LANDED on every set an `.absent` op brackets (nullifierInsertOp/revokedInsertOp/cellsInsertOp are all .aafiInsert, forced by aafiInsert_forces_imtInsert), and the two surviving op=3 sorted-splice `.insert`s (insertWriteOp/insertWriteOpRot, which do NOT relink in-circuit) write ONLY cap_root, which no `.absent` op brackets. What is NOT forced is the INDUCTION: no per-turn AIR pins the genesis root or composes turns into Reachable, so ImtSorted at an arbitrary pre-root rests on reachable_sorted over an HONEST stream plus the still-open digest-vector→chain-symbol bridge (MapOpsColumnLayout §8's named REMAINING WIRE). Carried as an EXPLICIT hypothesis, in the slot ReconcileGatesAt's ∃ h, SortedKeys h already occupied — same premise, stated at the pointer-chain resolution the deployed gate actually needs. TEETH both polarities on refSponge: the deployed law FIRES on the top-gap row; ★ present_key_has_no_deployed_gate refuses a PRESENT key for ANY claimed low leaf and ANY claimed post-root (anti-ghost). Floors: Poseidon2SpongeCR ONLY — no new floor. BYTE-SAFE: NEW file, adjacency model UNTOUCHED (it is the right target for the widened gate), no descriptor/emit/JSON/Rust byte touched. #assert_axioms-clean (7), no sorry/admit/native_decide.
import Dregg2.Circuit.MapAbsentImtGateWide -- ⚑⚑ THE WIDE/EPOCH `.absent` FAMILY, REPOINTED ONTO THE SHAPE THE PROVER RUNS. MapAbsentImtGate proved the Lean `.absent` model and the deployed Ir2Air::MapAbsent are INCOMPARABLE (arity-3 pointer-in-digest + ONE path vs TWO arity-2 adjacent paths; imtLeafHash_ne_heapLeafOf rules equivalence OUT). This file does the WIDE half. ① AbsentImtGatesAtW = the deployed `.absent` gate at the widened key: ONE path at the ARITY-17 leaf imtLeafHash8Of ⟨addr8,value,next8⟩, the pointer bracket at the FULL 8-felt lex order, root preservation. The pointer rides at 8 felts ALONGSIDE the key because halfWideLeaf_forges_absence_of_present proves — for EVERY hash, NO CR hypothesis — that widening the key alone is a non-membership FORGERY; there is no width knob, the schema is 17 felts or it is broken. ★ absentImtGatesW_force_absence: under the ONE named Poseidon2SpongeCR floor plus the committed ImtSorted chain, an accepting deployed wide row FORCES key ∉ imtAddrs c and Heap.get (imtToHeapW c) key = none, low-leaf chain MEMBERSHIP DERIVED (imtLeafHash8Of_injective + perfectRoot_injective), not assumed. ② imtLeafHash8Of_ne_leafOfW: arity 17 ≠ arity 9 under CR — the IMT pre-root the row opens and the mapRootW the keysOfW wrapper layer speaks about are DIFFERENT commitments over the same logical map, so every repointed keysOfW result names BOTH and bridges through imtToHeapW (the exact bridge aafiGatesW_post_chain_commitsW already uses on the AAFI side). ③ AdjacentBracketGatesW isolates the wide model's `.absent` body (adjacentBracketGatesW_is_model_body is Iff.rfl, so it is the real object) and ★ adjacentBracketW_unsat_above_max ports the incompleteness to the 8-felt key: ABOVE the largest committed address the model has NO gate data at any heap and any pair of paths — widening the key did NOT fix it, only changing the SHAPE does. interiorW_pointerBracket_iff_adjacent localizes the residue to exactly ONE SLOT (the top gap) at the wide width too. ④ THE REPOINTED FAMILY, composed out of ① rather than re-derived, and PRIMARY for W3: absentImtGatesW_force_keysOfW_absence, imtGates_force_holdsKindW_absent, absentImtGatesW_force_row_absence, absentImtGateW_of_lexBlocks(_canon) = THE EMISSION SURFACE (two satisfied lexLt8 blocks + ONE arity-17 path; the stepsHi path, the hadj position-adjacency obligation and the arity-9 leaf are all GONE — that is the whole W3 delta), absentImtRow_of_lexBlocks_forces_absence, absentImtGate_forces_residue_absence, and aafiInsertW_then_absentImtW_unsat (the `.absent` leg of the joint-unsat family, now at ONE commitment because both legs open arity-17 leaves). The adjacency-shaped originals are RETAINED UNTOUCHED — they are the right target for a future model in which the AIR does open two adjacent leaves — but they are no longer the statements to quote about the deployed `.absent` arm. ⑤ NON-VACUITY IN THE TOP GAP, the slot the adjacency model cannot reach, on MapOpWideKeyGate's own forgeChain (terminal pointer ptrHi is a SENTINEL_MAX FACE, not a stored leaf) with the top-gap key ptrMid: topGapW_deployed_accepts (arbitrary hash, every obligation rfl) vs topGapW_model_rejects vs ★ topGapW_reconcileGatesW_unsat (the epoch's ReconcileGatesAtW `.absent` hypothesis is FALSE on an honest deployed row, so every theorem over it is VACUOUS exactly where the prover operates), and the repointed laws FIRE there: topGapW_deployed_law_fires, topGapW_keysOfW_absence_fires, topGapW_row_absence_fires, topGapW_residue_absence_fires, topGapW_holdsKindW_fires, plus the anti-ghost present_keyW_has_no_deployed_gate. APEX NOT TOUCHED: AlgoStarkSoundFanoutMemory carries MapReconcileModelOkOfAccepts into Satisfied2 at the NARROW model, so on a turn containing a top-gap `.absent` row its hypothesis is FALSE (MapAbsentImtGate.topGap_reconcileGates_unsat) and its conclusion is vacuous there; repointing it needs MapReconcileModelOk's per-row body to dispatch `.absent` to AbsentImtGatesAt plus the chain-symbol premise (MapOpsColumnLayout §8's named REMAINING WIRE) — a scoped follow-up, NOT done. RESIDUALS NAMED, not claimed: (1) NO PADDING — every law is stated at c.length = 2^dep (MapAbsentImtGate's shape verbatim); the deployment pads, for which imtLayout/ImtVecCorr/PreRootModelsChain are the existing bridge and there is no wide twin; (2) aafiInsertW_then_absentImtW_unsat has no single-trace witness (both legs separately inhabited; one shared chain needs a 2^d-1 pre-chain). ImtSorted stays a HYPOTHESIS in the slot ReconcileGatesAtW's ∃ h already occupies — NOT promoted to a floor. Floors: Poseidon2SpongeCR ONLY — no new floor. BYTE-SAFE: NEW file, adjacency model untouched, no descriptor/emit/JSON/Rust byte touched. #assert_axioms-clean (28), no sorry/admit/native_decide.
import Dregg2.Exec.NullifierAccumulator -- THE nullifier/revocation accumulator GATE (executor-side weld): state carries only a Digest8 root (NfAccState.nullifierRoot/.revokedRoot — EXACTLY the additive pair the VK epoch lands in RecordKernelState), the transaction supplies a client-side NfAccWitness (non-membership GapOpen8 + fresh-key insert, verified against the root — the set NEVER crosses the wire). The three double-spend obligations re-derived over the root from the proven Heap8 lemmas: present_no_witness (no-double-spend/non-forgeability ← nonMembership_sound8 contrapositive — a present nullifier admits NO witness), spend_inserts_root (← update_sound8 y=nf disjunct — the spend advances the root so nf is now present), spend_then_no_rewitness (composed anti-replay). Revocation dual: revoked_gate_fails (a revoked credNul admits no non-membership witness ⇒ gate fails). Non-vacuity TWO-VALUED: witness_inhabited_of_bindings (TRUE pole — a fresh key HAS a witness once the compute_canonical_heap_root_8 bindings realize) + present_no_witness (FALSE pole) + decidable spine demos (25 admissible, 20 present-refused, insert grows by exactly the key). SpineCommits8 = the sole named Poseidon2/Compress8CR floor, a HYPOTHESIS carried by the witness never an axiom. #assert_axioms-clean. Kept over NfAccState (not RecordKernelState) deliberately: wiring the roots into RecordKernelState forces RH to absorb them + the whole full-state frame apex (TransferSpec/RestHashIffFrame/RotatedKernelRefinement* fr-structures/every effect ↔-spec) = the ember-gated VK epoch (design §6), NOT a free additive step.
import Dregg2.Exec.NullifierAccumulatorKernelBridge -- THE ↔-KERNEL WELD (VK epoch landed): RecordKernelState now CARRIES the two Digest8 accumulator roots (apex-absorbed by RestHashIffFrame, so the published state commitment binds them each turn). noteSpendNullifierAcc advances the committed nullifierRoot via the proven spendNullifierRoot (migration List kept in sync — no note_no_double_spend regression); the double-spend/anti-replay/revocation TEETH re-derive in kernel terms through a toNfAccState projection: noteSpendNullifierAcc_present (← spend_inserts_root), noteSpendNullifierAcc_no_rewitness + noteSpendNullifierAcc_no_double_spend (← present_no_witness — a committed nf admits NO spend witness, fail-closed by witness scarcity not a List scan), kernel_revoked_gate_fails (← revoked_gate_fails). No new crypto — SpineCommits8/Compress8CR stays the sole named floor, a HYPOTHESIS in the witness. #assert_axioms-clean. Frozen until the wire codec (stage E) feeds the client witness into the executor spend path.
import Dregg2.Circuit.Emit.AccumulatorInsertEmit -- THE INSERT-shaped accumulator keystone, the CORRECT-shaped genuine close for the fourth/fifth/sixth faithful root (the three accumulators are sorted-tree FRESH-KEY INSERTS, NOT the update-at-key AccumulatorOpenEmit shape — no shared before/after path). accumInserts8 = the faithful insert relation (fresh in BEFORE, spliced (key,value) present in AFTER, set-grows-by-exactly-key); accumInsert_writesTo8 = THE KEYSTONE forcing accumInserts8 over the FULL committed 8-felt BEFORE/AFTER root groups from (a) nonMembership_sound8 (the pred/succ GapOpen8 bracket), (b) MembersAt8 afterRoot (key,value) (the insert_witness membership in the REBUILT tree), (c) update_sound8 (keysOf8 afterRoot = insert key (keysOf8 beforeRoot)). Parametric over the 3 families. #assert_axioms-clean; the Poseidon-CR floor enters only through the Heap8Scheme node8 carrier, SpineCommits8 a HYPOTHESIS.
import Dregg2.Circuit.Emit.ShieldedNoteAppendDescriptor -- ⚑ L2 of the shielded-apex redesign: THE IN-AIR APPEND GROW-GATE for the SHIELDED-note accumulator (cell/src/shielded_note_set.rs, L0), so ShieldedNoteSet::root8() is trustworthy as the piCOMMITTED source that closes seam #15. GROUND-TRUTH FINDING: the grow-gate ALREADY GENERALIZES — AccumulatorInsertEmit.effAccumInsertV3 is parametric over (groupCol,keyCol,valueCol,sel) with effAccumInsertV3_forces_write8 : Satisfied2 ⟹ accumInserts8 over the FULL 8-felt BEFORE/AFTER root groups (the 3 cleartext families instantiate it), and MapOpsColumnLayout.aafiInsert_forces_imtInsert is the DEPTH- and VALUE-generic aafi32 two-path append law. The shielded set is the FOURTH family, so L2 = INSTANTIATION + the one thing the cleartext families lack: the HIDING-LEAF ZERO PIN. AUTHORED: effShieldedNoteAppendV3 = effAccumInsertV3 + hidingZeroConstraints (the leaf VALUE column pinned to 0 — the DESIGN §3 R1 hiding leaf HeapLeaf::entry(fold(cm),0); the cleartext families all key split_u64(value).0, which is why R2 was rejected). SOUNDNESS shieldedNoteAppend_forces_grow8: Satisfied2 ⟹ shieldedAppends8 (= accumInserts8 at value 0) over the full 8-felt groups — AFTER key set = BEFORE + EXACTLY the appended commitment (growth/preserves_prior/no_extra/fresh/leaf_is_hiding), with the 0 TRACE-FORCED by shieldedAppend_forces_hiding_leaf, not assumed. TEETH (5, each non-vacuous): a cleartext value in the shielded leaf is UNSAT; a duplicate append is UNSAT; a dropped prior leaf is UNSAT; an after-root omitting the append is UNSAT; ★ frozen_after_root_unsat (after = before) is UNSAT UNCONDITIONALLY (no hypothesis at all). Wide8 COMPOSITION (§4): sortedInsert_eq_sortedInsertW + spineCommits8_toW + keysOf8_eq_keysOfW make the growth literally SortedTreeInsertWide8.update_soundW instantiated (no re-proved tree combinatorics), and update_preserves_sortedW gives sortedness-preservation. PHYSICAL FACE (§5): ShieldedAafiGatesAt = AafiGatesAt at dep := HEAP_TREE_DEPTH and the hiding value 0 — shieldedAafi_forces_two_point_append + ★ shieldedAafi_prior_slots_untouched (every committed slot other than the two the append touches is BYTE-IDENTICAL: no prior leaf rewritten, none dropped, no shift) + fresh-commitment/sorted-chain/chain-growth instantiations. COMPLETENESS (§6): an honest append of a fresh hiding commitment FIRES (strict growth, prior notes survive, grown spine exactly [cmLo,cmNew,cmHi], appended leaf opens as (cm,0), a value-carrying leaf NEVER opens) — plus two-sided #guard canaries on the hiding pin, so the gate is neither vacuous nor a DoS. FLOORS NAMED: Poseidon2SpongeCR (the §5 AAFI law), SpineCommits8/GapOpen8 + PreRootModelsChain as HYPOTHESES never axioms. RESIDUALS NAMED: L1 (root8 as a rotation base limb = a VK epoch; this module is parametric over groupCol so it does NOT choose one), L4 (route apply_shielded_transfer + source piCOMMITTED), the lossy ~31-bit fold_bytes32_to_bb sort key (as for all 4 siblings), and §6's witness living at the generic SpineCommitsW wrapper (a concrete SpineCommits8 at a real Poseidon2 scheme is a CR-strength claim, not a Lean term). BYTE-SAFE: NEW object only — shieldedAppend_is_additive/_width are rfl, effAccumInsertV3 and every deployed descriptor untouched, no emit/registry wiring. #assert_axioms-clean (39), no sorry/admit/native_decide.
import Dregg2.Circuit.Emit.AccumulatorNonRevocationComplete -- ⚑ felt-width repair: the COMPLETENESS leg (SEM ⟹ SAT) of the alpha-batch NON-REVOCATION accumulator, restored onto the deployed 8-felt object (transitively wires AccumulatorNonRevocationRefine/Emit). NonMemberWitness (the certificate WITH its field-inverse witness, what the `check`=v·vinv=1 gate genuinely needs) → certSat CONSTRUCTS a satisfying trace PARAMETRICALLY (every C1..C4/sum/check/pin/constancy gate proven; empty mem/map legs built, not assumed). accepts_iff = the honest accept-set ⟺ (Accepts ↔ NonMemberWitness): ⟸ completeness builds the trace, ⟹ sat_implies_witness_public reads the FULL witness back (inverse from V_INV, no abstract field-invertibility). accumNonRev_roundtrip composes with Refine's soundness → literal NonMemberCertified. SWEEP FINDING: this AIR uses NO Poseidon2 chip / range lookup, so soundness AND completeness rest on NO refuted hash floor (no Poseidon2SpongeCR/compressNInjective); the content is the polynomial-division certificate + the unit `check` gate. Canary certTamper_check_fails (check=0 ⟹ REJECT). #assert_axioms-clean.
import Dregg2.Circuit.CapTreeUpdate -- THE THREE sorted-tree UPDATE operations the capability family needs, over the deployed cap-tree, CONSUMING the PHASE-D non-membership gadget (SortedTreeNonMembership). Generic, LAW#1-clean (no Rust constraints): capInsert_sound (delegate/introduce/grantCap — keysOf newRoot = insert k (keysOf oldRoot), a thin specialization of the gadget's update_sound); capUpdateAt_sound (attenuate/delegateAtten/refresh — membership-open an EXISTING key + rebind the root at the SAME spine ⟹ keysOf UNCHANGED, the in-place leaf-value narrow's sorted-tree shadow) + capUpdateAt_present; capRemove_sound (revoke/revokeDelegation/revokeCapability — rebind at sortedRemove k spine ⟹ keysOf newRoot = keysOf oldRoot \ {k}) + capRemove_drops_key + capRemove_preserves_sorted. Combinatorial cores proved UNCONDITIONALLY: sortedRemove + mem_sortedRemove (removes exactly the key) + sortedRemove_sorted (preserves sortedness), mirroring the gadget's sortedInsert lemmas. The KEY-SET layer; the Caps-FUNCTION lift is the named faithful-encoding residual the per-effect consumers carry (NOT faked here). SpineCommits = the only crypto residue (a HYPOTHESIS). #assert_axioms-clean. Non-vacuity: insert grows / remove shrinks / round-trips on the key spine.
import Dregg2.Circuit.Emit.CapInsertEmit -- THE INSERT-shaped CAP-tree keystone (the assurance core for the cap-key-SET-GROWING cap-writes delegate/introduce/delegateAtten — writesTo8 is UPDATE-at-key only, which covers attenuate; these SPLICE a fresh cap key so there is NO shared before/after path). The arity-7 cap twin of AccumulatorInsertEmit: capInserts8 = the faithful cap insert relation (fresh keyOf leaf in BEFORE, spliced 7-field leaf present in AFTER, set-grows-by-exactly-key); capInsert_writesTo8 = THE KEYSTONE forcing capInserts8 over the FULL committed 8-felt BEFORE/AFTER cap-root groups from (a) nonMembership_sound (the pred/succ GapOpen bracket over Cap8Scheme.MembersAt8), (b) MembersAt8 afterRoot leaf (the insert_witness recompose in the REBUILT cap-tree), (c) update_sound. effCapInsertV3 = the DEPLOYED insert descriptor (reused effCapOpenV3 membership read welded to the committed AFTER cap-root group); effCapInsertV3_forces_write8 TRACE-FORCES the after-membership (b), (a)+(c) ride the realizable GapOpen/SpineCommits carriers. #assert_axioms-clean; Poseidon-CR enters only through the Cap8Scheme node8 carrier, SpineCommits a HYPOTHESIS.
import Dregg2.Circuit.Emit.CapRemoveEmit -- THE REMOVE-shaped CAP-tree keystone (the assurance core for the cap-key-SET-SHRINKING cap-write revokeDelegation — the inverse of CapInsertEmit; also NO shared before/after path). capRemoves8 = the faithful cap remove relation (7-field leaf present in BEFORE, keyOf leaf gone in AFTER, set-shrinks-by-exactly-key); capRemove_writesTo8 = THE KEYSTONE forcing capRemoves8 over the FULL committed 8-felt BEFORE/AFTER cap-root groups from (a) MembersAt8 beforeRoot leaf (the remove_witness recompose reaching BEFORE — the revocation READ), (b) nonMembership_sound (the pred/succ GapOpen bracket over the REMOVED spine ⟹ the key is gone in AFTER), (c) capRemove_sound (over CapTreeUpdate.sortedRemove). effCapRemoveV3 = the DEPLOYED remove descriptor (reused effCapOpenV3 membership read welded to the committed BEFORE cap-root group); effCapRemoveV3_forces_write8 TRACE-FORCES the before-membership (a), (b)+(c) ride the realizable GapOpen/SpineCommits carriers. #assert_axioms-clean; Poseidon-CR enters only through the Cap8Scheme node8 carrier, SpineCommits a HYPOTHESIS.
import Dregg2.Circuit.RefusalAuthorityForced -- REFUSAL's authority change FORCED IN-CIRCUIT via the OPENABLE-fields_root insertion (the #103 ledgerless-authority close, build-half): refusal writes an audit record into the protocol-reserved fields_root key, and the deployed fields_root is a sponge over the WHOLE map (so a ledgerless client can't recompute the post-root; today the verifier anchors it OFF-CIRCUIT from the trusted post-cell via Anchor::RecordDigest). Modeling fields_root as the SAME sorted-Poseidon2 binary Merkle commitment cap/heap roots use (crate::openable_fields_root, Rust-side), the post-root is the single-leaf INSERTION of the public audit at the audit key, FORCED in-circuit. Over the proven gadget interface (SpineCommits/MembersAt, no new chip seam, CONSUMING CapTreeUpdate): refusal_authority_forced_in_circuit (keysOf postRoot = insert auditKey (keysOf preRoot) — the audit lands, derived from preRoot+auditKey, no trusted post-cell) + refusal_audit_present_after. THE TOOTH forged_refusal_post_root_absurd: a forged refusal claiming the audit key is ABSENT from the post-root yet carrying the insertion-gate binding is CONTRADICTORY — UNSAT via the proof ALONE, no executor, no trusted post-cell. Plus refusal_overwrite_preserves_keys (re-fire = capUpdateAt). SpineCommits = the only crypto residue (a HYPOTHESIS). #assert_axioms-clean. Non-vacuity: the audit key MOVES into the committed set (25↪[10,20,25,30]). The deployed-sponge→openable representation change + the EffectVmEmitRotationV3 refusal re-point are the named live-wire.
import Dregg2.Circuit.RotatedKernelRefinementCapFamily -- THE CAPABILITY-FAMILY refinements, FORCING the exact sorted-tree cap-table move via CapTreeUpdate (the #103 cap-reshape payoff). Per effect a DATA-bearing <Effect>CapsTreeEncodes decode bundles (1) the sorted-tree update DATA (SpineCommits old/new + present/fresh) from which the gadget FORCES the exact key-set move, and (2) the kernel-side Caps-FUNCTION residual (the faithful cap-tree↔kernel-caps ENCODING the commitment cannot certify — a HYPOTHESIS, exactly as attenuateEncodes.capsMove / NullifierTreeEncodes). INSERT (delegate/introduce/grantCap): delegate_forces_insert (keysOf grows by exactly the fresh edge key) + delegate_descriptorRefines ⟹ DelegateSpec + delegate_execFullA + delegate_rejects_ungrounded (the Granovetter gate bites). UPGRADE — attenuate goes PAST the felt-accumulator VALUE_PARTIAL: attenuate_forces_keyset_preserved (capUpdateAt_sound — the key set is PRESERVED, the in-place slot narrow's sorted-tree shadow, FORCED against the real deployed binary-Merkle commitment) + attenuate_descriptorRefines_exact ⟹ AttenuateSpec (SET-EXACT, not just non-amp) + attenuate_execFullA. delegateAtten (insert of an attenuated cap): delegateAtten_forces_insert + delegateAtten_descriptorRefines ⟹ DelegateAttenSpec + delegateAtten_non_amplifying (granted⊑held off the forced spec). refreshDelegation (update-at-key over the DELEGATIONS tree): refreshDelegation_forces_keyset_preserved + refreshDelegation_descriptorRefines ⟹ RefreshDelegationSpec. REMOVE (revoke/revokeDelegation/revokeCapability): revoke_forces_remove (keysOf loses exactly remKey + remKey absent) + revoke_descriptorRefines ⟹ RevokeSpec + revoke_execFullA + revokeDelegation_execFullA + revoke_drops_edge (holder confers no t-edge after). The exact Caps-function move is FORCED per effect (recDelegateCaps/attenuateSlotF/grant-attenuated/refreshDelegationsMap/removeEdgeCaps), with the SET move now forced by the gadget — the Caps↔tree encoding is the named carrier. #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementNotesFresh -- THE PHASE-D PAYOFF for noteSpend: the DOUBLE-SPEND FRESHNESS nf ∉ pre.nullifiers, FORCED in-circuit by the SortedTreeNonMembership open — replacing the CARRIED freshness field of RotatedKernelRefinementNotes.noteSpendGenuineEncodes. NullifierTreeEncodes ties the deployed sorted-Merkle nullifier tree's committed key set to the kernel nullifier keys (nfKey = noteLeaf = Nat↪ℤ); absent_key_absent_nullifier lifts tree-absence to nullifier-absence; freshness_forced: a GapOpen for nfKey nf valid against the committed nullifier spine ⟹ nf ∉ pre.nullifiers (DERIVED, not assumed). noteSpendFreshEncodes = noteSpendGenuineEncodes minus the freshness field plus the non-membership open ingredients (faithful tree + spine binding + gap open); noteSpendFresh_to_base rebuilds the base decode with the FORCED freshness in the slot, so noteSpendFresh_descriptorRefines ⟹ NoteSpendSpec pre nf actor spendProof post with freshness FORCED (the VALUE_PARTIAL residual the base file NAMED is now CLOSED — VALUE-COMPLETE up to the orthogonal §8 spendProof gate); also execFullA …=some post. THE SECURITY CRUX (both-polarity tooth): noteSpendFresh_rejects_double — a nf ∈ pre.nullifiers double-spend makes the non-membership open UNSAT (bites on the FORCED open, NOT the carried field — unlike the base rejects_double); noteSpendFresh_gapOpen_unsat_on_double states it at the gadget boundary (a faithful tree containing nf admits no valid non-membership open of nfKey nf). The double-spend hole closes IN-CIRCUIT. ADDITIVE — the base note rung untouched. #assert_axioms-clean (carriers = CapHashScheme.chipCR via the nullifier tree + compressNInjective/noteLeaf_injective; NullifierTreeEncodes/SpineCommits are HYPOTHESES).
import Dregg2.Circuit.RotatedKernelRefinementSpawnHandoff -- THE PHASE-D PAYOFF for spawn: the parent→child CAPABILITY HANDOFF, FORCED in-circuit via the deployed sorted cap-tree INSERT (CapTreeUpdate.capInsert_sound) — replacing the FROZEN cap_root (gCapPass) of the live spawn descriptor that RotatedKernelRefinementBirth carried as the NAMED capHandoff/delegateHandoff/delegationsHandoff residual. spawnHandoffEncodes bundles the EXISTING accounts-growth decode (spawnGenuineEncodes — accounts insert FORCED) + the sorted-tree INSERT data for the CHILD cap_root (SpineCommits old binds spine, childKey FRESH, new binds sortedInsert childKey spine). spawn_handoff_forces_insert: capInsert_sound FORCES keysOf newRoot = insert childKey (keysOf oldRoot) against the REAL deployed binary-Merkle commitment (the SET move, NOT frozen, NOT a felt accumulator) — the cap-family delegate_descriptorRefines template. spawn_handoff_key_present: the conferred cap key IS committed after the handoff. spawn_descriptorRefines_handoff ⟹ SpawnSpec (accounts FORCED via spawn_descriptorRefines + the cap-tree insert FORCED at the SET level; the spawnCapsMap/spawnDelegateMap/spawnDelegationsMap Caps-FUNCTION moves ride birth's named faithful-encoding residual, NOW backed by the forced insert — the SOLE remaining carrier, exactly the cap-family class) + _execFullA. NEGATIVE TEST (the upgrade's point): spawn_handoff_rejects_frozen_root — a spawn that FREEZES the child cap_root (newRoot = oldRoot, no handoff) while claiming the handoff is UNSAT (the fresh childKey would be BOTH absent (hfresh) AND present (forced insert) — contradiction), EXACTLY what the live gCapPass freeze could NOT catch. spawn_handoff_rejects_wrong_accounts (the accounts tooth inherited). NON-VACUITY: spawn_handoff_nonvacuous — the forced insert STRICTLY grows the child's committed cap key set (childKey ∈ new ∧ ∉ old). #assert_axioms-clean (carriers = CapHashScheme.chipCR/SpineCommits via CapTreeUpdate + compressNInjective via Birth, all HYPOTHESES).
import Dregg2.Circuit.Emit.EffectVmCapFamilyComplete -- THE CAP-FAMILY sorted-tree WRITE-gate COMPLETENESS (`←`) + the per-tag `air_accepts ⟺ spec` at 8-felt cap-tree write resolution, the complement of RotatedKernelRefinementCapFamily's soundness (`→`). Reuses the LANDED DeployedCapTree.Cap8Scheme machinery (capOpen8_binds_leaf_or_collides — the arity-16 chip binding, EXTRACTED AS DATA since 07-20; the chip8CR FIELD is DELETED and Cap8Scheme is INHABITED). GENERIC cores: capWrite8_afterRoot_iff (the WRITE-GATE BICONDITIONAL — along the committed membership path the published after cap-root opens to nl' IFF nl'=the genuine written leaf; both directions, NON-VACUOUS — a forged after-leaf yields a different root) + capWrite8_realizes (REALIZABILITY `←`: the honest narrowed leaf INHABITS the deployed writesTo8). UPDATE-AT-KEY tags (attenuate #1, refresh #5): the full new writesTo8 anti-forge `⟺` (attenuate/refresh_afterRoot_iff_spec) + forge-rejection canary + the SPEC-WELDED realization companion (X_spec_and_write_realized: the kernel decode's Spec via the LANDED X_descriptorRefines + writesTo8 realized, over the SAME committed key/roots). INSERT tags (delegate #2, delegateAtten #3, introduce #6): capInsert8_realizes + capInsert_keyset_iff (LANDED capInsert_sound both-directions) + <X>_spec_and_insert_realized. REMOVE tags (revoke/revokeDelegation #4): capRemove8_realizes + capRemove_keyset_iff + <X>_spec_and_remove_realized. NAMED carrier bundle (honest): NO chip CR is carried any more — the write-gate `⟺` legs are unconditional `binds_or_collides` disjunctions naming the extracted colliding pair (CapOpenColl), with the biconditional restored under the instance-local `¬ CapOpenColl` side condition the deployed chip SATISFIES; plus the sorted-tree decode (before/after leaves + membership path — the honest prover's cap-open readout). SCOPED residuals (stated in header, NOT laundered): this is the WRITE-GATE (writesTo8/capInserts8/capRemoves8) `⟺`, NOT a single Satisfied2(effCapOpenWriteV3)⟺full-effect-spec (the SAT-reconstruction `←` is the StarkComplete dual carried as a trace floor, as CircuitCompletenessAuthorityConstruct's CapOpenTraceFloor carries the authority open — NOT reconstructed here, NOT near-instantiation); the after-leaf's non-key/non-rights fields + the Caps↔cap-tree encoding remain the named residuals the cap family already carries. #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelRefinementExerciseAuth -- THE exercise HOLD-GATE, FORCED in-circuit via the deployed depth-16 cap-open membership (CapOpenEmit.effCapOpenV3_satisfiedEff over the LIVE attenuateCapOpenEffV3) — discharging the bare-Prop holdGate field RotatedKernelRefinementExercise.exerciseEncodes carried. The hold-gate IS a cap MEMBERSHIP ((caps actor).any (confersEdgeTo target)); ExerciseHoldSource (DATA-bearing, the Facet TransferAuthoritySource template for exercise) bundles the cap-open Satisfied2 + chip-soundness + row index + htarget (the opened leaf's target-column IS the exercise target) + ExerciseHoldFaithful (the toy↔deployed cap-tree encoding — the SAME residual class DeployedFaithful occupies for transfer, a HYPOTHESIS). exercise_holdGate_forced: the live cap-open membership FORCES the deployed membership conferring a target edge, and hfaith lifts it to the toy exerciseGuard — the hold-gate MEMBERSHIP is now circuit-FORCED, only the toy↔deployed encoding is the named carrier. exerciseEncodesAuth repoints the decode's hold-gate onto the source; exerciseEncodesAuth_to_exerciseEncodes lowers it to the base decode with the hold-gate FORCED; exercise_descriptorRefines_auth ⟹ ExerciseSpec + _execFullA. INNER-FOLD connected to the WHOLE-TURN closed forest: exercise_innerFold_rides_forest — the inner fold turnSpec (exerciseHoldState pre actor) inner post IS Spec.Turn.turnSpec fullActionStep (via turnSpec_eq_spec), EXACTLY what CircuitSoundness.turnDecodeChain_refines_turnSpec produces, so the inner-fold rides the SAME closed apex RECURSIVELY; exercise_innerFold_recursion_bound exhibits the structurally-decreasing measure (inner.length < (a::inner).length — each cons peels one action, terminating at the empty chain). NEGATIVE TEST: exercise_holdSource_rejects_unheld — an actor lacking a target-conferring cap (¬ exerciseGuard) admits NO ExerciseHoldSource (the in-circuit open would FORCE the hold-gate, contradiction) + exercise_descriptorRefines_auth_rejects_unheld. #assert_axioms-clean (carriers = the cap-hash ChipTableSound/Compress1CR via CapOpenEmit + the named ExerciseHoldFaithful encoding, HYPOTHESES).
import Dregg2.Lightclient.AttestedQuery   -- THE NON-OMISSION THEOREM: a query answer = membership openings + gap openings (Gap.inner = the REUSED sorted_gap_excludes bracketing + below/above/empty boundary forms); answer_complete: a VERIFYING answer contains EVERY in-range key in the index (omission impossible — an omitted key would sit in a valid gap, absurd) + answer_sound (no forgery) + verifies_iff_exact (verifying ≡ THE unique exact answer) + exact_answer_verifies (honest prover total; gapsOf bracket-EXISTENCE proved); ROOT face server_cannot_omit; CHAIN face: CommitBindsIndex NAMES the rotation obligation (recStateCommit must absorb iroot as a sponge limb, the heap_root discipline) and light_client_query_non_omission composes with RecursiveAggregation: ONE verify(agg.root) ⇒ non-omission over the WHOLE history. Non-vacuity TRUE (complete answer verifies) and FALSE (dropped receipt REJECTED / forged covering gap INVALID / forged extra + tampered value REJECTED)
import Dregg2.Lightclient.NonOmissionAttack -- REFUTE-AND-REPAIR the non-omission guarantee: the kernel-digest LIMB alone (recStateCommit k t = cmb(cellDigest)(RH k); RestHashIffFrame lists 16 NON-CELL kernel fields with NO receipt log) is BLIND to the receipt log (recStateCommit_admits_receipt_omission: that limb is rfl-identical when a receipt is dropped while mroot differs) ⇒ a kernel-ONLY chained commit would leave the hweld premise of light_client_{query,position}_non_omission VACUOUS-as-deployed. REPAIR: the deployed Rust chains the ROTATED wide commit (trace_rotated.rs B_IROOT=37; proof_verify.rs:349 iroot absorbed into the v9 commit), whose RotationLayout.rotatedCommit_binds_mmr discharges CommitBindsMMR by rfl — so light_client_position_non_omission_grounded (MMR) + light_client_query_non_omission_grounded (sorted, via rotatedCommitIdx_binds_index) rest on the PROVEN binding with NO hweld; non-vacuity FIRES on honest + REJECTS the omission. WHOLE-HISTORY CLOSED: the aggregation model now folds the rotated commit (ChainStep.newRoot = HistoryAggregation.chainedCommit = hash_many[d,iroot]), so newRoot_binds_log binds the receipt log at EVERY step (root_tooth_pins_log) and light_client_whole_history_non_omission derives LogChained over the whole history with NO hweld — the residual that named the kernel commit is GONE

-- CONSENSUS-FLEX lane (2026-06-11): the consensus-on-demand feasibility core (.docs-history-noclaude/CONSENSUS-FLEX.md).
import Dregg2.Consensus.OnDemandFeasibility -- T1 PROVED: pairwise-commuting turn lists are linearization-agnostic (tau ≡ any causal-ack order on the commuting fragment, rides Perm.foldl_eq') + join-shaped (CRDT/monotone) steps always commute (the POS pole) + revoke×exercise non-commuting NEG witness (the conflict relation is real) + FrameCommutes interface (T2, the wave obligation: footprint-disjoint ⇒ commute for recKExecAsset) with the assembled frame_fastpath_sound corollary; T3/T6 named (T5 RESOLVED → Consensus.TauPrefixMonotone)
import Dregg2.Consensus.SuperRatifyBridge -- ⚑ DOES THE SAFETY THEOREM COVER THE FINALIZER THE NODE ACTUALLY RUNS? ANSWER: YES, CONDITIONALLY — and one condition is BYZANTINE-CONTROLLED. `isSuperRatified` and `Committed` are INCOMPARABLE across six axes (outer quorum, inner threshold, leader-in-past, equivocation scope, unique-leader, id→block), so the bridge runs through `finalLeaderAt` — where `leader_blocks.len() == 1` lives and what `findAllFinalLeaders`/`tauOrder` actually call. `committed_of_finalLeaderAt` → `finalizedBy_deployedHistory` → `deployed_nodes_no_conflicting_history`: two nodes each running the DEPLOYED rule never disagree, general in n, f, B and participants. It also DISCHARGES the apex's `FinalizedBy` premise, which had NO SUPPLIER before. Two named+modelled hypotheses: `ThresholdAgrees` is BENIGN (equal on 3f+1 ≤ n ≤ 3f+3, band proved tight — at n=7,f=1 the node finalizes on 5 where `Committed` demands 6); `LeaderNotGlobalEquivocator` is NOT — `Model.forked` is a lace where the node's observers ratify at full supermajority AND the node finalizes while `Committed` is FALSE for every round map. ⚑ That is a COVERAGE hole, not an attack: the deployed observer-LOCAL equivocation guard is the MORE faithful one, so the ALGEBRA is over-strong and the THEOREM is what is weak. Repair named `OPEN-CM-LOCAL-EQUIVOCATION`, not done — so if a leader's author forked outside every observer's causal past, the node finalizes and this theorem is SILENT. ⚑ AND IT FIXED A MODEL BUG THAT MADE THE BRIDGE UNPROVABLE AT EVERY REAL PARAMETER: `CordialState.approves` rendered `ordering.rs::approves` with STRICT `≺` where the code uses `causal_past_INCLUSIVE`, dropping exactly one voter (`l.creator` via `l` itself) — and since `superMajority n = n − f` at every canonical parameter, nothing anyone runs could have been bridged. Now inclusive; `Committed` is EASIER to satisfy, so the apex is STRONGER. New crossing `causalPastIncl_sound`/`past_block` takes `ordering.rs`'s BFS over `BlockId`s to the inductive `precedes` over `Block`s — nothing in the tree had crossed that boundary, and `Lace.Canonical` is what does it (a sixth consumer). ⚠ MEASURED ALONGSIDE: NO Lean theorem in this tree can EVALUATE the deployed finalizer on a concrete lace — `roundOf → computeRounds → Array.qsort`, whose worker is well-founded and private to `Init`, so the kernel cannot reduce it and `unseal` cannot reach it. Every `#guard` about `tauOrder`/`isSuperRatified` runs in the COMPILER, not the kernel; the models are built round-free for exactly that reason. ⚠ Landed under the DECLARED escape `DREGG_ALLOW_NEW_FLOOR_CARRIER=1` (the capstone carries `CrossNodeCanonical`, whose apex is itself un-baselined) — a knowing, recorded use, not a silent one.
import Dregg2.Consensus.Safety -- CHAIN-LEVEL CONSENSUS SAFETY apex (the sibling of lightclient_unfoolable, lifted from the TURN to the CHAIN): no two honest nodes finalize CONFLICTING histories. Every prior consensus-safety theorem fixes ONE lace/state (bft_safety, cordial_agreement, finalLeaders_one_per_wave); this lifts to TWO nodes' DIFFERENT laces. §1 quorum_pair_agreement = bft_safety generalized from one vote pool to two independent pools (the honest witness in the UNION-quorum intersection collapses the ids); §2 cross_node_leader_agreement (two CordialStates, one wave, one leader) + via_bft (honesty discharged by the BFT model); §3 cross_node_agreement_from_lace (quorum READ OFF each node's lace via SuperRatification.ofLace, not assumed); §4 ⚑ REWRITTEN 2026-07-28 (4c65977c4). The APEX title has MOVED to `no_conflicting_finalized_history_of_committed`, because the old `no_conflicting_finalized_history` took `S₁ S₂` and `h₁ h₂` and NEVER RELATED THEM — read literally it was a statement about two arbitrary lists while this very comment called them "two honest nodes and their finalized histories". The new apex ties each history to its node via `FinalizedBy`, and the per-pair id-determinism collapses to ONE lace-level `CrossNodeCanonical` with each anchor's lace-membership DERIVED (`committed_mem_lace`), not assumed. ⚑ AND THE CARRIER IS NOW INHABITED: `CrossNodeWitness` had ZERO constructions tree-wide, so this apex proved nothing; `Model.crossWitness` is a real one with every field derived — two nodes with genuinely DIFFERENT laces (gap two-sided, by `decide`), each `Committed` off its own blocks, and a BFT field whose adversary is REAL (participant 2 is Byzantine AND proved an actual ratifier on both laces, so the f=1 budget is SPENT — unlike the old `unionModel`, whose adversary was `fun _ => False`). ⚑ THE OLD NON-VACUITY CLAIM WAS THE LAUNDERING: "§1 kernel fires + Consistent two-sided" demonstrates a DIFFERENT thing firing and never inhabits this apex's hypothesis; the real tooth is `Model.crossNodeCanonical_is_the_gap` — two nodes, laces EACH canonical, EACH genuinely committing a wave-0 leader, SHARING an inhabited BFT model (so what fails is NOT honest-majority), whose anchors differ by ONE BIT (`signed`) at the same content-address: `Consistent` FAILS and the light client is on two chains. HONEST SCOPE, now in the file header: safety is on the BFT algebra, NOT the deployed `BlocklaceFinality.isSuperRatified` (OPEN-CM-SUPERRATIFY-BRIDGE, unchanged); the union BFT model is never discharged from the runtime; `CrossNodeCanonical` is backed by NOTHING in-tree (it is collision resistance at the consensus boundary — naming it buys census visibility and decidability, and that is all); and the model is n=4, f=1, one wave, six blocks — satisfiability and refutability, not scale. #assert_axioms-clean (19 here, 15 in CordialMiners)
import Dregg2.Consensus.TauPrefixMonotone -- T5 RESOLVED with a SOUNDNESS FINDING: tauOrder finalized-prefix monotonicity is REFUTED unconditionally (honest-laggard counterexample lagBase→lagGrown: a lagging validator's late wave-end ratifier grows an ALREADY-FINAL wave's coverage and xsorts MID-PREFIX; insert-valid, equivocation-free — the node's executed_up_to index slicing at blocklace_sync.rs::poll_finalized_blocks would re-execute block 32 and PERMANENTLY skip block 41) + PROVED conditional: tau_finalized_prefix_monotone under FinalizedRegionStable (leaders_extend + fold_agrees = the finalized region undisturbed; executable mirror stableCheck = the check the node is MISSING; pointwise sufficient condition fold_agrees_of_pointwise; node-shaped corollary tau_executed_prefix_fixed = the executed region is bit-identical under take) + structural foldl_tauStep_fst_extend (tau's loop only APPENDS — reorder risk lives entirely in the leader list + segments); POS tooth trace3→trace6 (full second wave: stableCheck holds, 9→18 prefix) + NEG teeth isolate the failing field (leaders_extend holds, fold_agrees fails); #assert_axioms-clean

-- TEMPORAL-MODAL lane (2026-06-11): the guard algebra's temporal-modal layer (REFINEMENT epoch).
import Dregg2.Authority.TemporalAlgebra   -- the temporal guard-atom family (afterHeight/beforeHeight/withinWindow/cooledSince/rateBound/challengeWindow) in the RelCaveat/HeapAtom shape, installed as a PRECONDITION beside the per-slot caveat gate (temporalStateStepGuarded; nil = the existing write, keystones lift verbatim); the algebra PROVED (window = after∧before, cooling refuses-inside/admits-after, upward/downward closure) + THE CTL BRIDGE: each pure-height atom's admission set IS a satisfaction set of the PROVEN Proof/CTL fixpoint calculus over heightClock (afterHeight_iff_AG "once vested, vested forever" / afterHeight_eventually_EF / beforeHeight_expiry_permanent_AG / cooledSince_iff_AG), welded to the real executor by committed_write_advances_clock; dregg1's TemporalGate + the polis cooling gate + the LIVE macaroon window chain are INSTANCES (temporalGate_eq_withinWindow / polisCooling_is_cooledSince / windowed_chain_is_withinWindow — zero behavior change); vesting + auction #guard-witnessed both ways
import Dregg2.Authority.TemporalAlgebra2  -- the TEMPORAL BOOST: (1) UNTIL/SINCE — the real U/S pair as register-flip atoms (untilEvent/sinceEvent, exact complements) with the fixpoint faces INHERITED (until_holds_EU_flip via the proven EU_unfold; until_mu_formula via encode_EU; sinceEvent_iff_AG permanence; since_flip_in_past = the past-witness S; flip_not_inevitable = the honest AF negative) + the executor weld (close_write_flips_gates / committed_flip_write_steps_flagClock) + the eventStateStepGuarded install; (2) THE DEADLINE UNIFICATION: frame ≅ height (frame_deadline_embeds_afterHeight under the §8 honesty carrier + afterHeight_is_chain_frame_deadline, an iff) while causal ⊋ height with the excess characterized = INCOMPARABILITY (causal_deadline_implies_height_gate sound projection; height_cannot_recover_causal on the demo fork — the lightcone part); (3) SCHEDULING: vesting_admits_iff_prefix (k-tranche = exactly the cumulative-unlock prefix), auction_phases_exclusive + settle_sees_valid_reveals (bid/reveal/settle with the SealedAuction preimage gate composed in, no late switching), rateBound_meet/nested_rate_gate_refines/withinWindow_split (rate composition over nested windows); (4) THE JUSTNESS WELD (Track-D): cooledSince_eventually_admits_of_just — a persistently-enabled cooling gate eventually admits under just scheduling, INHABITED on the real executor (coolDemo ⇒ unconditional cooling_liveness_demo) and SEPARATED (cooling_starves_without_justness on the stutter schedule)

-- EPISTEMIC lane (2026-06-11): the modality tower for the guard algebra over the constructive witness semantics.
import Dregg2.Authority.Epistemic         -- K/E_G/D_G/C_G + non-transferable-K over witness pockets; mechanism welds: council actor-bound slots = E_G (approval_witness_pins_sender), ThresholdDecrypt Shamir = D_G with the D-without-K separation, FinalizedLightClient CertValid = the C_G constructor (one shared transferable witness; no conflicting common per wave; n=1 collapse honest); the Lawvere triple ∃_a ⊣ q_a* ⊣ ∀_a PROVED over the holding fibration with Knows = the ∃-composite, faithful ∀_a=Knows iff functional pockets (mixed-pocket obstruction witnessed); guard-atom DESIGNS knownBy/distributedAmong/commonAt/privateTo + the commonAt partition-safe bridge-settlement killer theorem

-- CALCULUS lane (2026-06-11): naming WHAT KIND of runtime dregg is — a small formal calculus whose laws are POINTERS to landed theorems (docs/DREGG-CALCULUS.md).
import Dregg2.Calculus.DreggCalculus       -- dregg = a CAPABILITY CALCULUS with ATTESTABLE REDUCTION and COORDINATION-TYPED GUARD MODALITIES. Thin presentation over existing types: syntax = cells (≈processes) · capabilities (≈names) · the three compressed verb shapes create/gwrite/move (CTerm, verbs_are_three = compressed_kernel_three); reduction Reduces IS stateStepGuarded (reduces_iff_step) and is ATTESTED — every step emits one receipt row (reduces_is_attested via state_obsadvance) and is fail-closed (reduces_fail_closed); correspondences: attenuation≈scope-restriction-w/-non-amplification PROVED (attenuation_is_scope_restriction via introduce_non_amplifying + amplifying_grant_rejected), exercise≈communication / factory≈replication / program≈input-guard as typechecking defs; THE NOVEL PART = coordination-typed modalities: modality_price = guardKeepsConfluence, modality_price_is_tier (the type IS the consensus cost), modality_price_monotone (grow-only ⇒ coordination-free) / modality_price_bounded (ceiling ⇒ forces ordering, w/ constructive clashing witness); headline dregg_calculus assembles all three over a concrete reduction. #assert_axioms-clean
import Dregg2.Calculus.BiorthTensor        -- TRANSCENDENTAL-SYNTAX bridge S3+S4 VERDICTS (the load-bearing test): T1 LINEARITY = conditional YES with the boundary exact — conservation (Σδ=0 over the PAIRED move writes) IS a behaviour once the deployed pair observable (valTotal's Σδ comparator) is fielded as a test (conservation_is_behaviour; = the closure of the matched-delta tensors, Cons = (⋃ₖ Δₖ ⊙ Δ₋ₖ)^⊥⊥, conservation_eq_closure_of_matched_tensors) and is NOT a behaviour under ANY rectangular/per-component family over ANY test type (conservation_not_behaviour_rectangular via the one-line generic mix law rect_mix_in_biorth; live instance = paired SlotCaveats) — LINEARITY RECOVERED FROM ORTHOGONALITY IFF the tests may CORRELATE the pair (linearity_recovered_from_orthogonality); tensor = biorth ∘ pre-tensor over the COMPOSITE universe (stella §69.35-36; the same-universe union-of-refutations form is the additive & — orth_union_is_with); non-vacuity both polarities (conservation_proper / pair_closure_grows re-pairing growth). T2 GRADING = PARTIAL + the conjectured CALM/directed form REFUTED: the tier IS pairwise-join closure of the admission set (free_iff_joinClosed, definitional), EMERGES soundly through ^⊥ (join-stable tests inherited — behaviour_grade_sound; markGuard_grade_emerges re-derives the monotone pole) but is INCOMPLETE (grading_incomplete), and directed-lub-closure is VACUOUS on the deployed lattice (budget_directed_lub_attained ⇒ directedLubClosed_vacuous ⇒ directed_conjecture_refuted: budgetGuard is directed-closed yet ForcesOrdering — coordination prices DIVERGENT incomparable replicas, not growing views). #assert_axioms-clean
import Dregg2.Calculus.Biorthogonality     -- TRANSCENDENTAL-SYNTAX bridge S1+S2 (docs/TRANSCENDENTAL-SYNTAX-BRIDGE.md): dregg guard classes ARE biorthogonally-closed behaviours. Generic orthogonality core (orthSet/coOrthSet/biorth, the Galois duality + triple law, orthogonals-are-behaviours); S1 = the relation t ⊥ r over the literal/order atom family (senderIs/senderInField/balanceGe/Le/fieldEquals/Ge/Le/fieldLeField) via the DEPLOYED evalConstraintCtx (admits_is_program_admitsCtx welds to RecordProgram.admitsCtx; refusal_pins_counterwitness = the fail-closed dual); S2 = guard_class_is_biorthogonally_closed (Adm(g) = Adm(g)^⊥⊥ for EVERY finite conjunction), non-vacuous BOTH polarities (demo_behaviour_proper / singleton_not_behaviour via old-blindness — the closure strictly grows); THE EXECUTOR WELD: caveatsAdmit IS orthogonality membership (Iff.rfl) + every committed Reduces LANDS in a behaviour (reduces_lands_in_behaviour) + the SlotCaveat family FACTORS actor×transition (eval_mix ⇒ pair_not_behaviour); S3 begun: additive & (behaviour meet) + attenuation = antitone refutation growth (affine face); linear/⊗ (conservation over composite turns) = the named open continuation. #assert_axioms-clean
import Dregg2.Calculus.BiorthRelational    -- the RELATIONAL fronts of the transcendental-syntax bridge: TEMPORAL — point guards are behaviours with an EXACT characterization (height_behaviour_iff_convex: behaviours = convex sets = until/since windows; behaviour_is_window_guard COMPLETENESS; installed gates temporalAtomsAdmit/eventAtomsAdmit ARE orthogonality membership, Iff.rfl) while the MODAL until/since content = the write-once step discipline is NOT a behaviour under any per-time-point family (writeOnce_not_behaviour_rectangular) and IS one with the step observable fielded (writeOnce_is_behaviour; executor weld committed_close_lands_in_behaviour; until refutations = since witnesses); EPISTEMIC — K/E/C are behaviours over the challenge orthogonality (Knows IS {phi}-orthogonal membership; E = the additive & of K-classes; CommonAt = K of the LEDGER, the floor law invisible to testing), IGNORANCE is NOT a behaviour (challenge behaviours are evidence-monotone), and the sharp one: threshold-D over share pairs is the conservation parallel EXACTLY (threshold_iff_sum: pooled knowledge = an affine correlation; NOT rectangular-closed, IS pooled-closed, = the closure of matched-RANDOMNESS tensors — distributed_knowledge_recovered_from_orthogonality); THE SYNTHESIS correlation_classifies_the_family: one two-sided pattern across time/resource/members — biorthogonality is load-bearing exactly where correlation matters, and the correlation TYPE classifies the guard family

-- VERIFIED-DEOS lane (2026-06-14): "a verified desktop OS" made literal — the deos visual/interactive primitives reduce to kernel theorems (docs/deos/DEOS.md §"the verified-deos program"). The Rust realization shipped (starbridge-web-surface rehydration + affordance steel); this is the PROOF it cannot amplify / the liveness-type IS the confined fragment. NONE is new mathematics — the firmament's attenuation/gateOK/receipt-chain proofs restated for surfaces/membranes/rehydration/affordances.
import Dregg2.Deos                          -- THE FOUR TARGETS: (1) Surface-as-capability — a Target::Surface(cell) IS a kernel Cap.endpoint; a window confers no authority beyond its rights, a view/notify-only surface confers NO Granovetter edge (viewSurface_confers_no_edge = the notifyCap_confers_no_edge shape), projection cannot amplify (surface_attenuate_no_amplify = Exec.attenuate_subset). (2) Membrane non-amplification — reshare A→B→C ⟹ C⊆B⊆A (reshare_chain_attenuates = per-hop attenuate_subset ∘ List.Subset.trans; generalized to n-hop chains reshareN_attenuates; a widening darkened reshare_refuses_amplification). The Rust Membrane is the realization. (3) THE CROWN — Rehydration confinement = the liveness-type: ReplayedDeterministic IS EXACTLY the confined fragment (replayedDeterministic_iff_confined: non-Live ⇒ classify=ReplayedDeterministic ↔ every interaction a witnessed attested turn; the doc's "derived" row as an ↔; dual reconstructedApproximate_iff_unconfined partitions non-live contexts), invalid attestation is NOT a witness (anti-ghost invalidAttestation_not_replayed), replay payoff rides Exec.Receipts.chain_tamper_evident under the §8 digest oracle as NAMED hyps. (4) Affordance soundness — an agent fires ONLY what its caps authorize (fire_authorized_iff: the is_attenuation gate required⊆held, both polarities), the post-state surface binds the attested root (firedSurface_binds_attested_root = the receipt's newCommit), unauthorized fire binds no root (anti-ghost), progressive enhancement → progressive ATTENUATION (projectFor_monotone). Legs 1/2/4 + the leg-3 classifier CROWN fully discharged (no oracle); leg-3 replay payoff carries the digest collision-resistance as named §8 hyps (never an open hole or axiom). All #assert_all_clean.

-- PRIVACY/OFFLINE-CELL lane (2026-06-13): witnessless participation in a multi-cell turn (docs/PRIVATE-OFFLINE-CELLS.md).
import Dregg2.Distributed.PrivateLeg       -- THE WITNESSLESS PARTICIPANT: a cell maintained OFFLINE contributes a ZK-proof-only leg to the N-cell atomic turn (extends EntangledJoint's public-leg fold with a private-leg role). PrivLeg = the public face (asset + commitPre/commitPost + jid; hidden state is NOT a field); PrivLegHolds = the ∃-over-hidden-states relation the §8 STARK proof certifies (a real recKExecAsset step existed offline whose commitments match) discharged via PortalFloor.VerifierKernel.verify_sound (the EXTRACTABILITY carrier, never a Lean law); MixedJoint/MixedAdmissible = public legs on the shared machine ⊗ private proof-only legs all binding ONE CG-2 jid; KEYSTONE joint_turn_sound_with_private_legs (public conservation + no-cap-amplification from the machine fold ⊕ every private leg certifiably ran a real CONSERVING offline step with its witness HIDDEN under the ∃ ⊕ one-jid CG-2) + mixed_turn_composite_conserves; teeth BOTH polarities (privLeg_real_verifies honest accept + privLeg_forged_rejected/exists_forged_leg: a conjure-from-nothing leg is FALSE under an injective commitment); private_reveals_strictly_less = the disclosure-dial position (acceptanceOnly floor). Named crypto floor = the per-leg ZK proof's STARK extractability. #assert_axioms-clean

-- CIRCUIT-SOUNDNESS CAPSTONE (2026-06-17): the COMPOSITION CAPSTONE — the apex's opaque per-effect `∀` made an EXPLICIT, ENUMERATED, NAMED family.
import Dregg2.Circuit.CrossTurnFreshness   -- THE CROSS-TURN NO-REPLAY THEOREM (the freshness scope the single-transition apex does NOT cover): lightclient_unfoolable proves only that a verifying batch decodes to a GENUINE kernel step at the given pi.turn — it says nothing about FRESHNESS/replay/ordering. The cross-turn defense rests on the DEPLOYED commitment-chain CAS (proof_verify.rs: live stored commitment == proof pre-anchor) + cell-nonce monotonicity (cell_state.rs Monotonic; the nonce is bound into recStateCommit via the agent cell's leaf). This module models + PROVES the core: commit_inj_nonce (the commitment is injective in the agent nonce — from CommitSurface.commit_binds, the CR set; a commitment CANNOT hide a stale nonce) → TurnChain (a chain with strictly-monotone agent nonce, grounded by prologue_strictly_increases_nonce = Admission.commitPrologue_nonce) → commit_no_repeat (the live-commitment sequence NEVER repeats) → no_replay (a fixed pre-anchor opens the CAS gate at most ONCE) + replay_rejected_after_apply (every later turn rejects the same proof — live commitment ≠ pre once advanced). NON-VACUOUS (witnessChain inhabits TurnChain, nonce=i). The named RESIDUAL (R1/R2, neither an axiom nor a hole): wiring the full Admission.runTurn-driven accepted sequence into a TurnChain (mechanical composition over recKExec_preserves_AccountsWF + admissible_links_to_head) + "net agent nonce strictly increases across prologue+body". #assert_axioms-clean.
import Dregg2.Circuit.CircuitSoundnessAssembled -- assembles CircuitSoundness.lightclient_unfoolable's carried hrefines into an enumerated per-effect family. (1) Rfix : Registry = the live v3Registry as a total actionTag-indexed lookup (the descriptor each per-effect rung discharges; fix + phase-D cap descriptors are the corresponding v3Registry entries). (2) kstepAll := dispatchArm (the generic dispatcher arm; e=0 transfer arm faithful-upgradable via dispatchArmFacet_to_dispatchArm). (3) EffectDecodeBridge S hash R e := descriptorRefines S hash (R e) (kstepAll e) — the per-effect rung in apex shape; the HONEST residual = the StateDecode ⟹ <effect>Encode decode-extraction the LEDGER-root commitment cannot certify (every rung's logical core <effect>Encode ⟹ <effect>Spec ⟹ dispatchArm IS landed in RotatedKernelRefinement*; ALL 36 effects need the bridge — none discharges from StateDecode alone). (4) hrefinesAll = assemble the named per-effect bridge family into the apex's ∀. (5) lightclient_unfoolable_assembled + lightclient_turn_unfoolable_forest_assembled = the apex/whole-turn capstone at Rfix/kstepAll, carried floors EXPLICIT: StarkSound, Poseidon2SpongeCR, CommitSurface CR, WitnessDecodes, the ENUMERATED EffectDecodeBridge family. The apex's per-effect gap is now a NAMED enumerated residual set, not an opaque carried ∀. #assert_axioms-clean.
import Dregg2.Circuit.CustomApex -- THE STAGED-AIR MODEL for Effect::Custom proof-binding (the refutation's object; the vacuous companion apex is RETIRED). Holds: VmConstraint2.holdsAtStaged (the staged proofBind row gate True→ProofBind.boundAt E env, the in-AIR recursion check beside the UNCHANGED deployed vacuous holdsAt), Satisfied2Staged (Satisfied2 over the staged gate), satisfied2Staged_toCustom (a staged witness IS a Satisfied2Custom, via holdsAtStaged_imp_holdsAt). This is the staged AIR that CustomCarrierAttack runs its forged trace against (deployed True-gate admits what staged rejects). SUPERSEDED/DELETED: the StarkSoundCustom carrier + lightclient_unfoolable_custom / lightclient_unfoolable_custom_binds / lightclient_custom_v3_binds + descriptorRefinesCustom — CustomCarrierAttack proved that path vacuous-as-deployed (no uniform Satisfied2⇒Satisfied2Staged bridge), and the main apex carves Effect::Custom OUT (no_customA_arm) so nothing consumed them. The REAL deployed-grounded guarantee lives in CustomBindingFromFold (custom_binding_from_fold / custom_companion_grounded, on the FRI+Poseidon2-CR+connect floor). #assert_axioms-clean.
import Dregg2.Circuit.BindingAirSound -- discharges EngineSound.binding_sound: a satisfying TurnChainBindingAir trace FORCES ChainBound + genesis/final root pins, resting only on Poseidon2SpongeCR. The ordering carrier moves assumption→theorem (the light-client's chain ordering is PROVEN, not assumed). #assert_axioms-clean, anti-ghost both ways.
import Dregg2.Circuit.AggAirSound -- reduces recursive_sound's per-node LAYER-1 carrier (SegSound) to {FriExtract floor + Poseidon2SpongeCR} ⊕ the PROVEN segment-combine gates (continuity + count additivity + ordered Poseidon2 digest). The recursion AIR's semantic soundness: the combine math is no longer assumed. segsound_node_discharged + combine_digest_binds (CR tooth). #assert_axioms-clean.
import Dregg2.Circuit.WitnessRealizing -- realizes WitnessDecodes (witnessDecodes_of_genuine_roots + lightclient_unfoolable_witness_realized = the apex re-proven with the carrier REMOVED, discharged from genuine recStateCommit-bound kernels) AND derives leaf_sound from descriptorRefines (engineSound_of_refinements: EngineSound with leaf_sound DERIVED). #assert_axioms-clean.
import Dregg2.Circuit.DescriptorRefinesComplete -- the censused front door to ∀ e, descriptorRefines (Rfix e) (kstepAll e): VERIFIED whole at HEAD — all 31 deployed effect tags route to their own proven <e>_closedLog rung, NO dregg-specific arm unproven, no catch-all laundering. Reduces to Poseidon2SpongeCR + deployed-chip faithfulness + the named ClosureReadouts extraction carriers. kstepAll_discriminates + kstepAll_not_total (non-vacuity). #assert_axioms-clean.
import Dregg2.Circuit.NormalizeToShapeSound -- the Pickles step∘wrap soundness twin: normalizing a recursion proof to the CANONICAL fixed manifest preserves its published commitment AND committed-execution semantics (Poseidon2SpongeCR only; the forward leg needs no crypto). canon_fixed_at_depth_one = the DEPTH-1 fixed VK proven STRUCTURALLY (vs the measured depth-4) + canon_perpetually_constant. The Lean obligation the fork normalize primitive carries. #assert_axioms-clean.
import Dregg2.Circuit.StateTransitionAirSound -- denotational twin of the live ivc.rs StateTransitionAir (the attenuation/delegation fold): a satisfying trace FORCES the genuine 8-felt wide-hash fold + hash-chain continuity + step-increment (state_transition_forces_fold), the depth tooth (stepCount_is_length), and the CR tooth (state_digest_binds_chain). Closes a status-C "no Lean model" gap. #assert_axioms-clean.
import Dregg2.Circuit.AggregationAirSound -- denotational twin of plonky3_recursion.rs AggregationAir. Honest two-layer model: agg_bare_endpoints = what eval ACTUALLY enforces (continuity-only scaffold, NOT history-binding); agg_chain_is_genuine_hash_fold = the hashed-layer keystone (the documented-but-absent hash gate). The deployed history-binding aggregation rides the fork (ivc_turn_chain/joint_turn_recursive, soundness in AggAirSound). #assert_axioms-clean.
import Dregg2.Circuit.RecursiveSoundFromNodes -- CLOSES the last carried apex hypothesis: recursive_sound_from_nodes proves the WHOLE-TREE fold (per-node carrier → root verifies ⟹ ALL agg.leafProofs verify) by induction over the proof-carrying PTree, resting on ONLY the per-node FriExtract floor (Poseidon2-CR not needed for proof-propagation). engineSound_recursive_derived builds an EngineSound with recursive_sound DERIVED (the recursion analog of WitnessRealizing.engineSound_of_refinements). The "extracted child == the agg.leafProofs element" reading is encoded in the PTree/NodeCarrier (function-form per-node floor), NOT axiomatized. #assert_axioms-clean. RETIRES GroundedApex's carried hrec: the apex swaps hrec for (PTree + NodeCarrier + wrapping inputs), so the deployed assurance case's last FRI-composition hypothesis collapses to per-node in-circuit verifier soundness.
import Dregg2.Circuit.GroundedApex -- THE GROUNDED APEX (additive payoff, non-breaking): light_client_verifies_whole_history_grounded + lightclient_unfoolable_grounded(_live) conclude the SAME AggregateAttests/unfoolability facts as the assumed-carrier apex, but with binding_sound + leaf_sound DERIVED (BindingAirSound + WitnessRealizing.engineSound_of_refinements), WitnessDecodes REPLACED by genuine recStateCommit-bound kernels, and the per-effect family DISCHARGED by DescriptorRefinesComplete.descriptorRefines_complete (the _live variant) — so the grounded apex rests on NO assumed EngineSound legs, only {Poseidon2SpongeCR, StarkSound, CommitSurface CR, the named ClosureReadouts limb-decode carriers, mkLog} + honest-prover data. hrec NOW DERIVED (the last carried FRI-composition hypothesis is RETIRED): engineSound_grounded_v2 + light_client_verifies_whole_history_grounded_v2 DROP hrec and instead thread (PTree + NodeCarrier + wrapping inputs), building recursive_sound via RecursiveSoundFromNodes.recursive_sound_from_nodes (the whole-tree fold) — so ALL THREE EngineSound legs are derived and the residual collapses to the PER-NODE FriExtract floor (NodeCarrier) + Poseidon2-CR + CommitSurface-CR + the named limb-decode carriers + honest-prover data; no whole-tree FRI-composition hypothesis remains. The original engineSound_grounded (carrying hrec) is KEPT for back-compat (additive, non-breaking). Fires on real honest chains both ways (grounded_light_client_fires / grounded_light_client_fires_v2 / lightclient_unfoolable_grounded_fires; the _v2 firing uses the concrete honest PTree + per-node carrier, so the whole-tree fold is load-bearing). #assert_axioms-clean. Migration: deployed_system_secure swaps its EngineSound hyp for the realizer inputs (carrier or per-node tree).
import Dregg2.AssuranceCaseGrounded -- THE GROUNDED CAPSTONE: deployed_system_secure_grounded proves the SAME composed A∧B∧C∧D∧E assurance case as deployed_system_secure, but with guarantee E's whole-history leg routed through GroundedApex.light_client_verifies_whole_history_grounded — so the assumed EngineSound is GONE (verified: appears 0×) and the deployed capstone rests on STRICTLY {crypto floor (Poseidon2SpongeCR + the §8 injectivity carriers + the FRI hrec carrier) + honest-prover realizer data (hleaves = the descriptorRefines family deriving leaf_sound; hbindExtract = the binding-AIR trace discharging binding_sound)}. hrec NOW DERIVED: deployed_system_secure_grounded_v2 DROPS the carried hrec and threads (PTree + NodeCarrier + wrapping inputs) into engineSound_grounded_v2, so guarantee E's recursion leg rests on the PER-NODE FriExtract floor (NodeCarrier) — every EngineSound leg derived, no whole-tree FRI-composition hypothesis. A-D pass through unchanged (orthogonal to the four carriers). Before: trusted Poseidon-CR + FRI + an assumed EngineSound bundling the per-leaf executor binding + chain-ordering tooth as opaque hyps. After: those legs are DERIVED. Fires on the honest teethGenesis chain both ways (grounded_capstone_engine_fires / grounded_capstone_engine_fires_v2 — the _v2 uses the concrete honest PTree + per-node carrier, binding extraction concrete). #print axioms = {propext, Classical.choice, Quot.sound}. The residual is now the PER-NODE FriExtract floor (NodeCarrier), the whole-tree recursive_sound DERIVED not carried.
import Dregg2.Circuit.CustomCarrierAttack -- ADVERSARIAL: refutes StarkSoundCustom as a DEPLOYED carrier + reduces EngineBinding to the floor. deployed_admits_unbacked = the forged custom trace (commit=999, no backing sub-proof) that SATISFIES the deployed True-gate AIR but fails Satisfied2Staged; starkSoundCustom_unsound_over_deployed = the binding lightclient_unfoolable_custom_binds claims is carried ENTIRELY by the Satisfied2Staged hypothesis, ungrounded over the deployed True-gate AIR (clean-but-VACUOUS-as-deployed — the custom companion proves nothing about the deployed system). ⚑ STALE 2026-07-28 (9fba3cd10): `engineBinding_of_floor` is DELETED. It was not merely resting on the refuted `Poseidon2SpongeCR` — its CONCLUSION was false too: `EngineBinding (floorEngine hash)` asserts `hash [a,b] = hash [c,d] → a = c`, and the diagonal `n ↦ (n,0)` injects an infinite set into a bounded codomain, so it is pigeonhole-refuted at deployed BabyBear exactly like the floor. ANY global `∀ p q, ¬Coll` side condition reproduces it, which is why the honest object is per-PAIR: `EncColl hash enc p q` at the two public-input lists the sponge actually absorbs (all three poles proved), plus `vk_determined_or_encColl` with NO hypothesis, and nine `*_binding_from_fold` / nine floor-free `*_or_collides`. Nine `honest_companion_fires` were themselves conditional on the refuted floor — the POSITIVE non-vacuity witnesses were vacuous — and are now unconditional at `Reference.refSponge`, whose CR is proved. CONSEQUENCE: the deployed custom binding must be rebuilt from the FOLD (AggAirSound + the connect + Poseidon2-CR), never StarkSoundCustom. #assert_axioms-clean, no sorry.
import Dregg2.Circuit.CustomBindingFromFold -- THE REPAIR (replaces the refuted StarkSoundCustom): custom_binding_from_fold proves the DEPLOYED custom binding from a TRUE-as-deployed premise — a verifying aggregate (the per-turn fold incl. the custom leaf) FORCES the effect-vm leg's exposed custom-commitment PI to be backed by a verifying sub-proof (binding) with a uniquely-determined VK (anti-ghost), resting ONLY on {FRI floor via AggAirSound.FriExtract + Poseidon2SpongeCR + the connect (f.leafCommit = f.c)} — StarkSoundCustom GONE, EngineBinding derived internally via engineBinding_of_floor — ⚑ STALE 2026-07-28 (9fba3cd10): `engineBinding_of_floor` is DELETED. It was not merely resting on the refuted `Poseidon2SpongeCR` — its CONCLUSION was false too: `EngineBinding (floorEngine hash)` asserts `hash [a,b] = hash [c,d] → a = c`, and the diagonal `n ↦ (n,0)` injects an infinite set into a bounded codomain, so it is pigeonhole-refuted at deployed BabyBear exactly like the floor. ANY global `∀ p q, ¬Coll` side condition reproduces it, which is why the honest object is per-PAIR: `EncColl hash enc p q` at the two public-input lists the sponge actually absorbs (all three poles proved), plus `vk_determined_or_encColl` with NO hypothesis, and nine `*_binding_from_fold` / nine floor-free `*_or_collides`. Nine `honest_companion_fires` were themselves conditional on the refuted floor — the POSITIVE non-vacuity witnesses were vacuous — and are now unconditional at `Reference.refSponge`, whose CR is proved. custom_companion_grounded = the grounded analog of CustomApex.lightclient_unfoolable_custom_binds on the same floor as every other effect. Non-vacuous both ways (honest_companion_fires; forged_unsat = the forged-999 commitment makes the aggregate UNSAT, the circuit twin of the Rust fold tooth). #assert_axioms-clean. The custom guarantee is now REAL-as-deployed; CustomApex's StarkSoundCustom path is SUPERSEDED (retire follow-up).
import Dregg2.Circuit.CustomDeployedBytePin -- THE (c) RUNG for Effect::Custom: the deployed custom descriptor pinned AT ITS COMMITTED BYTES. #guard emitVmJson2 (withDfaRcPins (gentianDeployedBareRefuse customV3)) == the exact `circuit/descriptors/rotation-v3-staged-registry.tsv` row-19 JSON Rust include_str!s (V3_STAGED_REGISTRY_TSV) — the FIRST byte pin for any rotation-v3 registry member (EffectVmEmitRotationV3:6096 declined them for elab cost), plus the same-bytes pin through the emitter's own v3RegistryCapOpenDep lookup, so `deployedCustomV3` IS the shipped member and not a look-alike. FALSIFIED: flipping ONE column index (72->73) inside the golden turns three guards red. What the bytes then SAY, checked not asserted: exactly ONE proof_bind op, spelled {"t":"proof_bind","guard":var 8,"commit":var 72,"vk":var 68}, and — constrainsBindingCol over EVERY shipped constraint — NO gate / boundary / transition / lookup / window-gate reads ANY of the sixteen custom binding columns (68..71, 72..75, 1663..1666, 1667..1670); only the 16 piBinding pins publish them and the proofBind declares them. Theorems at those bytes: deployed_custom_proofBind_vacuous (the shipped row gate is True for EVERY env — the deployed AIR constrains the commitment and VK nowhere), and CustomCarrierAttack's forged trace LIFTED off the toy demoC columns onto the SHIPPED ones — deployed_forged_row_admitted (commit=999 passes the deployed gate) vs deployed_forged_row_rejected_by_staged (the staged in-AIR verifier bites), with deployed_honest_row_accepted_by_staged (commit=123) as the positive polarity so the staged gate is not refusing everything. SCOPE: this pins what the AIR does NOT do; the binding itself is the per-turn FOLD (CustomBindingFromFold + joint_turn_recursive::prove_custom_binding_node_state_segmented), reached only when the prover sets carrier_witness = Some(CarrierWitness::Custom _) — a prover-chosen field no in-AIR gate and no light-client verify tooth forces. #assert_axioms-clean, no sorry, no native_decide, no floor carrier.
import Dregg2.Circuit.BridgeBackingAttack -- ADVERSARIAL (G1 analog of CustomCarrierAttack): refutes the deployed inbound-bridge-mint's foreign-spend backing. deployed_admits_unbacked_bridge = a bridge-mint row that SATISFIES the deployed descriptor's row intent (credit value_lo + frame freeze + nonce tick) while its published mint_hash (prmCol 0) is backed by NO verifying foreign note-spend — the deployed bridgeMintVmDescriptor has NO proof-binding op and reads mint_hash in no constraint, so a pure light client sees the credit with no witnessed backing (the off-AIR verify_note_spend_dsl_full in apply_bridge_mint is the only enforcer). deployed_intent_does_not_force_backing = no uniform "deployed-accepts ⟹ backed". deployed_admits_consumed_nullifier = the consume-once guard (bridge_ledger.rs::bridge_mint_against_lock, atomic contains-then-insert over committed note_nullifiers) is a RE-EXEC tooth, not a light-client one. Same vacuity CLASS as custom: real-as-re-executed, vacuous-as-deployed-LC. REPAIR pointer (mirrors CustomBindingFromFold): the backing must come from the per-turn FOLD over the re-proved bridge-action/note-spend leaf (circuit-prove::bridge_leaf_adapter) connected to the published mint_hash PI. #assert_axioms-clean, no sorry.
import Dregg2.Circuit.SovereignBackingAttack -- ADVERSARIAL (sovereign analog of CustomCarrierAttack/BridgeBackingAttack): refutes the deployed sovereign-cell turn's AUTHORITY backing. deployed_admits_unbacked_sovereign = a sovereign leg row that SATISFIES the deployed rotated transition intent (new_commit = old+δ) while its published key_commit (aux WITNESS_KEY_COMMIT_0=23) is attested by NO verifying owner Ed25519 signature — a WRONG-OWNER/injected-key forgery the deployed effect-vm AIR cannot detect (the teeth IS_SOVEREIGN_CELL/KEY_COMMIT/SEQUENCE are dead-zero, no producer sets is_sovereign_cell=1, no constraint reads them; the off-AIR verify_strict + anchor + chain-walk in execute.rs:811/879/888 is the only enforcer). deployed_admits_injected_anchor = the pre-state-anchor leg (a): a genuine old→new whose old is not anchored to the federation's stored commitment. deployed_intent_does_not_force_authority = no uniform "deployed-accepts ⟹ authorized". deployed_admits_replayed_sequence = the monotonic per-cell chain-walk (execute.rs:888) is a RE-EXEC tooth, not a light-client one (leg (c) replay). Same vacuity CLASS as custom/bridge: real-as-re-executed, vacuous-as-deployed-LC. REPAIR pointer (mirrors CustomBindingFromFold): authority must come from the per-turn FOLD over the re-proved sovereign-authority leaf (circuit-prove::sovereign_leaf_adapter::prove_sovereign_leaf) connected to the teeth via prove_sovereign_binding_node_segmented; the leaf binds the authority DIGEST in-circuit, full in-AIR Ed25519 stays the named off-AIR cost. #assert_axioms-clean, no sorry.
import Dregg2.Circuit.FactoryBackingAttack -- ADVERSARIAL (factory analog of Sovereign/Bridge/CustomCarrierAttack): refutes the deployed Effect::CreateCellFromFactory leg's creation backing. deployed_admits_forged_child_vk = a leg row that SATISFIES the deployed EFFECT_CREATE_CELL transition intent while its published child_vk (CHILD_VK teeth) is derived by NO validating creation — an arbitrary-program forgery the generic selector (no factory AIR/STARK) cannot detect; the off-AIR validate_and_record (factory.rs:917, apply.rs:2360) is the only enforcer. deployed_admits_overbudget_factory = the per-epoch creation_budget counter is a RE-EXEC tooth, not a light-client one. deployed_admits_outside_cap_factory = an out-of-template capability. deployed_intent_does_not_force_backing = no uniform "deployed-accepts ⟹ factory-backed". honest_factory_backed pins NON-VACUITY. Same vacuity CLASS: real-as-re-executed, vacuous-as-deployed-LC. REPAIR (mirrors the sovereign fold-wire): backing comes from the per-turn FOLD over the re-proved factory leaf (circuit-prove::factory_leaf_adapter::prove_factory_leaf) connected to the CHILD_VK teeth via prove_factory_binding_node_segmented; the leaf binds (factory_vk, child_vk, derivation_digest) in-circuit, full in-AIR Poseidon2 re-derivation + cap-template Merkle-membership stay the named off-AIR cost. #assert_axioms-clean, no sorry.
import Dregg2.Circuit.MembershipBackingAttack -- ADVERSARIAL (membership analog of CustomCarrierAttack/BridgeBackingAttack/SovereignBackingAttack): refutes the deployed SenderAuthorized{AuthorizedSet::PublicRoot} turn's set-MEMBERSHIP backing. deployed_admits_unbacked_membership = a leg row that SATISFIES the deployed rotated transition intent while its published sender_leaf (an aux teeth slot, compress(sender_pk)) is proved by NO verifying Poseidon2 Merkle path — a NOT-IN-THE-SET forgery the deployed effect-vm AIR cannot detect (there is NO set-membership leg; the effect-vm "membership" column is the UNRELATED cap_root, no row publishes (sender_leaf, authorized_root) as PIs; the off-AIR MerkleMembershipStarkVerifier / verify_membership_dsl in membership_verifier.rs:143/158 is the only enforcer, so the AttestedHistory is byte-identical to a fail-closed-default validator's — RE-EXEC-ONLY). deployed_admits_injected_root = the wrong-set/injected-root leg (b): the sender is a genuine leaf of a DIFFERENT tree presented against the authorized root. deployed_intent_does_not_force_membership = no uniform "deployed-accepts ⟹ authorized member". Same vacuity CLASS as custom/bridge/sovereign: real-as-re-executed, vacuous-as-deployed-LC. REPAIR pointer (mirrors CustomBindingFromFold): membership must come from the per-turn FOLD over the re-proved membership leaf (circuit-prove::membership_leaf_adapter::prove_membership_leaf) connected to the published teeth via prove_membership_binding_node_segmented; the leaf binds the (sender_leaf, authorized_root) tuple in-circuit, full in-AIR Poseidon2 Merkle-path (the MerkleHash chip-table TID_P2 lookup) stays the named off-AIR cost. #assert_axioms-clean, no sorry.
import Dregg2.Circuit.HatcheryBackingAttack -- ADVERSARIAL (hatchery analog of CustomCarrierAttack/SovereignBackingAttack): refutes the deployed hatchery-mint's CellContract forever-crown backing. deployed_admits_unbacked_hatchery = a mint leg row that SATISFIES the deployed cell-birth transition intent (new = born+δ, installing the kind's state_constraints) while its published contract_hash (an aux Attested teeth slot) is attested by NO verifying CellContract proof — a FABRICATED forever-crown the deployed effect-vm AIR cannot detect (the contract_hash is only STORED in HpresProof::Attested{contract_hash}, hatchery_mint.rs:262, read by no constraint; the off-VK executor attestation / Hatchery.lean attested_enforces_forever EXECUTOR-image carry is the only enforcer). deployed_admits_wrong_contract = the invariant-binding leg (b), the LC-image of Hatchery.lean::forged_attestation_rejected: the contract_hash matches a verifying proof but that proof certifies a DIFFERENT invariant than the kind's claimed invariant_digest. deployed_intent_does_not_force_backing = no uniform "deployed-accepts ⟹ attested". Same vacuity CLASS as custom/sovereign: real-as-re-executed (the executor runs the program gate forever; a real Attested carries a real step_ob), vacuous-as-deployed-LC. REPAIR (BUILT, mirrors SovereignBackingAttack→sovereign_leaf_adapter): the crown comes from the per-turn FOLD over the re-proved contract-attestation leaf (circuit-prove::hatchery_leaf_adapter::prove_hatchery_leaf) connected to the published contract_hash teeth via prove_hatchery_binding_node_segmented; the leaf binds the (contract_hash, invariant_digest) tuple in-circuit, full in-AIR CellContract re-proof stays the named off-AIR digest-of-attestation cost. #assert_axioms-clean, no sorry.
import Dregg2.Circuit.FactoryBindingFromFold -- THE FLIP (factory: BackingAttack → BindingFromFold, tooth-gated): the v12 big-bang regen landed the committed factoryV3Carriers row (child_vk8 octet PIs 47..54) + the Factory fold arm, and the deployed-path tooth BIT both poles (factory_binding_deployed_tooth.rs: honest FOLDED+LC-VERIFIED, forged child_vk REJECTED). factory_binding_from_fold = the deployed factory binding from TRUE-as-deployed premises (FRI floor via AggAirSound.FriExtract + Poseidon2SpongeCR + the connect) — (binding) the published child_vk8 claim is backed by a verifying factory sub-proof, (anti-ghost) its VK is determined; the EXACT mirror of custom_binding_from_fold over the universal sub-proof-folding primitive. authorized_from_fold = the §B close: grounding onto FactoryBackingAttack.Authorized (the fold forces the backing the deployed AIR alone provably cannot — deployed_intent_does_not_force_backing stays TRUE of the bare AIR; the aggregate adds the third edge). Non-vacuous both ways (honest_companion_fires; forged_childvk_unsat_demo = the §A forged-999 claim makes the aggregate UNSAT). #assert_axioms-clean.
import Dregg2.Circuit.HatcheryBindingFromFold -- THE FLIP (hatchery: BackingAttack → BindingFromFold, tooth-gated): the regen landed the contract_hash8 octet PIs 55..62 on the SAME factoryV3Carriers row (the hatchery carrier rides factory's leg) + the Hatchery fold arm; the deployed-path tooth BIT both poles (hatchery_binding_deployed_tooth.rs: honest FOLDED+LC-VERIFIED, forged contract_hash REJECTED). hatchery_binding_from_fold = the deployed hatchery-contract binding on the same FRI+Poseidon2-CR+connect floor; backed_from_fold = the §B close grounding onto HatcheryBackingAttack.Backed. Non-vacuous both ways (honest_companion_fires; forged_contract_unsat_demo). #assert_axioms-clean.
import Dregg2.Circuit.SovereignBindingFromFold -- THE FLIP (sovereign: BackingAttack → BindingFromFold, tooth-gated): the v12 big-bang regen landed the committed makeSovereignV3DeployedWide row (KEY_COMMIT teeth PIs 58..61 PLUS the in-AIR KEY_COMMIT chip gate welding the teeth to the committed B_PUBKEY8 octet — makeSovereignV3DeployedWide_publishes_key_commit; apex Rfix 38 = makeSovereignV3Deployed) + the Sovereign fold arm, and the deployed-path tooth BIT both poles on the NATIVE row (sovereign_binding_deployed_tooth.rs: honest promotion FOLDED+LC-VERIFIED, forged key_commit REJECTED). sovereign_binding_from_fold = the deployed sovereign binding from TRUE-as-deployed premises (FRI floor via AggAirSound.FriExtract + Poseidon2SpongeCR + the connect) — (binding) the published KEY_COMMIT claim is backed by a verifying authority sub-proof, (anti-ghost) determined; the mirror of custom/factory over the universal sub-proof-folding primitive. authorized_from_fold = the §C close: grounding onto SovereignBackingAttack.Authorized (the fold forces the owner authority the deployed AIR alone provably cannot — deployed_intent_does_not_force_authority stays TRUE of the bare AIR; the aggregate adds the fold edge, and the chip gate adds the in-AIR committed-octet weld). Non-vacuous both ways (honest_companion_fires; forged_keycommit_unsat_demo = the §A forged-0 claim makes the aggregate UNSAT). #assert_axioms-clean.
import Dregg2.Circuit.MembershipBindingFromFold -- THE FLIP (membership: BackingAttack → BindingFromFold, tooth-gated, EXPOSURE-honest): the regen landed the committed transferV3MembershipWide row ((sender_leaf, authorized_root) claim PIs 50..51, teeth columns past the carriers — transferV3MembershipWide_publishes_teeth; apex Rfix 0 = transferV3Membership) + the Membership fold arm, and the deployed-path tooth BIT both poles on the NATIVE row (membership_binding_deployed_tooth.rs: honest sender-authorized transfer FOLDED+LC-VERIFIED, forged authorized_root REJECTED). ⚑ HONEST SCOPE: the deployed-AIR leg is PI exposure ONLY — the in-AIR sender-compress/fields-read welds stay the named MembershipAuthRootEdge seams, so MembershipBackingAttack §A/§A′ STAND as deployed-AIR facts; what flips is the AGGREGATE: membership_binding_from_fold (a published tuple claim must be backed by a verifying membership sub-proof — the real dsl::membership STARK, executor-aligned 687601953) + authorized_from_fold grounding onto MembershipBackingAttack.Authorized. Non-vacuous both ways (honest_companion_fires; forged_tuple_unsat_demo). #assert_axioms-clean.
import Dregg2.Circuit.CaveatBignumCompare -- THE BIGNUM `≤`/`<` COMPARISON, PROVEN IN LEAN (the source of truth the Rust caveat gadget realizes): each caveat `≤`/`<` atom is a limbwise schoolbook BORROW-SUBTRACTION over u128-scale bignum operands (NOT a miniscule ~2^24 single-felt atom). bignumVal reconstructs the integer a limb list denotes; BSValid is the denotation of the per-limb gate system (`dᵢ = yᵢ − xᵢ − brᵢ + br_{i+1}·B`, `dᵢ ∈ [0,B)` range-checked, borrows boolean, top borrow pinned 0). KEYSTONE borrowSub_iff: a valid witness EXISTS iff `bignumVal xs + init ≤ bignumVal ys` — soundness+completeness at ANY width, proved by induction with the two schoolbook-digit pivots (le_mul_of_nonpos/le_mul_of_pos). Corollaries bignumLe_iff (init 0 ⇒ `≤`, validUntil/budget) / bignumLt_iff (init 1 ⇒ `<`, heightLt STRICT — strictness IS the incoming borrow of 1); bignum_admits_iff composes the three atoms into inCircuitAdmits's body. Non-vacuous both poles at the deployed B=2^26/5-limb params + a u128-scale example. bsLimbEqn_is_gate: the per-limb equation IS the emittable `Dregg2.Circuit.Constraint` IR (range clauses = `Lookup.rangeCheck`), the same IR lean_descriptor_air ingests. #assert_axioms-clean, no sorry.
import Dregg2.Bignum -- THE UNIFIED, PROVEN, EMITTABLE BIGNUM LIBRARY every circuit weld builds on (the numerics bedrock). Over the SHARED denotation (CaveatBignum's bignumVal/Ranged), the schoolbook ops each carry (i) a witness gate system, (ii) an `_iff` soundness+completeness keystone, (iii) the emittable-Constraint tie. compare = le_iff/lt_iff (lift borrowSub_iff). SUB (SubValid): subVal (diff = minuend−subtrahend over ℤ) + sub_iff + THE ANTI-UNDERFLOW-WRAP tooth sub_underflow_unsat (a below-zero sub is UNSAT — the burn/debit drain class). ADD (AddValid, carry chain, top carry pinned 0): addValid_val (sum = x+y+cin) + add_iff (a witness EXISTS iff the sum FITS `< Bⁿ`) + THE NO-OVERFLOW tooth add_overflow_unsat (an overflowing sum is UNSAT — the field cannot wrap). MUL: bignumVal_mul_bound (general overflow-safe width bound `< B^(m+n)`) + the deployed 2×2→4 schoolbook Mul2Valid with mul2_val (product exact over ℤ) + mul2_fits. MOD/REDUCE (ModValid = Euclidean x=q·m+r, 0≤r<m): modValid_iff pins (q,r) to (x/m,x%m) + CANONICALITY modValid_unique + mod_noncanonical_unsat (a non-canonical rep is UNSAT). RANGE: range_bits_bound (a bit-list ∈[0,2ⁿ)) + THE ANTI-WRAPAROUND KEYSTONE rangeBound_field_faithful / legs_noWrap_conservation (RealCrypto.twoLeg generalized to k legs: under the range bound a mod-p FIELD equation IS the INTEGER equation — the mint-by-wraparound class). The FOUR #1-protocol-killer classes (overflow/underflow-wrap/field-vs-integer/non-canonical) are each PROVEN unconstructable (antiExploit_* summary). Emittable ties addLimbEqn/mul2LimbEqn/modEqn/subLimbEqn_is_gate = real Dregg2.Circuit.Constraint gates; range clauses = Lookup.rangeCheck. Non-vacuous BOTH poles at deployed params (Base=2^26, 2^15 vault limbs, p=2013265921 BabyBear). #assert_axioms-clean, no sorry, no warnings.
import Dregg2.Bignum.LedgerBalance -- ⚠ CARRIES ONE NAMED `sorry` (biasedLimbs_valid, the completeness pole) — the SIGNED ledger balance read at the width it is actually carried. `CellState::balance` is an i64 (the issuer well legitimately holds −supply) and reaches the circuit + light client through ONE encoder: `balance_lo_felt/hi_felt = split_u64(balance as u64)` (turn/src/rotation_witness.rs:106-117, consumed :408,410), inverse `lo | (hi<<30)` (proof_verify.rs:92). TWO wounds, priced APART because only one has a tooth: (a) THE SIGN — the cast is two's-complement, so `sign_wound_survives_the_range_check` shows −1 encodes to limbs that BOTH satisfy the deployed 30-bit in-circuit range proof (the high limb clears it by NINE) and the documented inverse reads >10^18; the low limb alone — what CircuitCompleteness:280-283 welds `kernel.bal` to — reads 1,073,741,823. NO tooth reaches this. (b) THE ALIASING — the ">>30" is a 34-bit quantity in ONE felt, so `deployedEnc_not_injective` exhibits two distinct balances with identical limbs and identical commitment; but `aliasing_dominated_in_circuit` proves the 30-bit range proof caps the admitted join at 2^60 < 2^30·p, so (b) is DOMINATED wherever that proof runs and ranks BELOW (a). `deployedJoin_faithful_on_range` is the other pole (faithful on [0,2^60)) so neither is a claim that the encoder is broken everywhere. §4b: the note accumulator leaf (commitment_set.rs:263-272, nullifier_set.rs:458, trace_rotated.rs:1568/1805) commits `split_u64(value).0` ALONE with NOTE_VALUE_HI written-and-never-read, so `accumulatorLeaf_aliases_at_2_30` — same projection, no companion limb, no ceiling, and a SOUNDNESS shape. THE ADOPTION (all instantiations of Dregg2.Bignum, nothing re-derived): balVal = bignumVal at the deployed 2^30 base; balLimbs_injective (PROVED injective, not merely wider — Ranged is load-bearing); debit_exact/debit_iff/debit_underflow_unsat + credit_exact/credit_overflow_unsat. THE KEYSTONE `deployedDebit_is_bignum_sub`: the DEPLOYED frozen-bal_hi gate ALREADY IS a SubValid witness at two limbs with the amount's high limb pinned 0 and the inter-limb borrow structurally forced 0 — so the repair is UN-PINNING ONE COLUMN, not a new construction. Its price: `deployedDebit_cannot_cross_limb` + `cross_limb_spend_is_honest` (a cell holding exactly 2^30 spending ONE unit has NO witness — every balance ≥ 2^30 is unspendable down through the boundary, and the deployed *Avail borrow chain does NOT reach it: that chain runs on the LOW limb) vs `bignum_admits_cross_limb_spend` (the limbed repair witnesses exactly that spend) — strictly better in BOTH directions. §7 names what Bignum does NOT cover: it is a NON-NEGATIVE positional system, so a signed ledger needs the bias `balance_biased` (cell/src/state.rs:1186) that the BYTE path already uses and the FELT path does not — biased_injective/biased_lt/biased_range proved, biasedLimbs_unique PROVED (soundness pole), biasedLimbs_valid `sorry` (completeness pole, discharge written at the site). MODEL ONLY: no descriptor registered, no emit path, no VK, no Rust constraint — and it deliberately does NOT claim the Rust producer computes deployedLo/deployedHi (a differential test, not a theorem) nor weld to any emitted gate set.
import Dregg2.Circuit.CaveatAdmissionRefines -- THE CAVEAT-IN-CIRCUIT WELD (Lean half of the DREGGFI §7 honest edge): the in-circuit caveat-admission gadget (circuit-prove::caveat_admission_leaf_adapter) REFINES the reified CaveatPred metatheory. inCircuit_iff_tokenAdmits = the gadget's slack-nonneg admission (0 ≤ ceiling − request, the AIR range-checks it) ⟺ Token.admits of the mandate's reified caveat chain (validUntil expiry / heightLt strict / budget spend / asset scope, each a Caveat.pred over its own request view) — so the AIR FAITHFULLY EVALUATES the caveat predicate, not a trusted caveatBit. Non-vacuous both poles (inCircuit_bridge_nonvacuous: within-caveat admits; past-expiry/over-budget/over-height/wrong-asset REFUSED). inCircuit_admitted_narrows chains attenuate_narrows (the admitted request is IN the less-attenuated parent's admissible set); inCircuit_admitted_discharges reuses token_discharges (the range-checked admission IS a Laws.Discharged verify witness). HONEST SCOPE: the DECIDABLE atoms only — Caveat.opaque + Caveat.thirdParty stay executor-trusted (not reified, not forced in-circuit). #assert_axioms-clean, no sorry.
import Dregg2.Circuit.DslBackingAttack -- ADVERSARIAL (DSL/Dfa analog of CustomCarrierAttack/BridgeBackingAttack/SovereignBackingAttack/MembershipBackingAttack): refutes that a pure light client witnesses the `Witnessed{Dfa}` predicate transition. The Dfa caveat is verified OFF-AIR by turn::executor::membership_verifier::DslCircuitDfaVerifier (CellProgram::verify_transition → stark::verify) — a PRECONDITION caveat with NO op on the deployed effect-vm at all (even more invisible than custom's vacuous True-gated proofBind). deployed_admits_unwitnessed = ∃ a Dfa-gated leg the deployed AIR ACCEPTS whose published route-commitment is exposed by NO verifying DSL sub-proof; deployed_admits_both = NON-VACUOUS (the deployed leg accepts BOTH a witnessed and an unwitnessed rc — the predicate column rides free); deployed_does_not_force_witnessed = no uniform "deployed-accepts ⟹ DSL-witnessed", RE-EXEC-ONLY as a class. REPAIR (§C, sound on the floor): dslEngineBinding_of_floor — the fold-leaf binding (circuit-prove::dsl_leaf_adapter::prove_dsl_leaf_with_commitment, a REUSE of custom_leaf_adapter, connected via prove_dsl_binding_node_segmented = prove_custom_binding_node_segmented) REDUCED to Poseidon2SpongeCR via engineBinding_of_floor (the route-commitment IS a Poseidon2 sponge of the DSL PIs) — ⚑ STALE 2026-07-28 (9fba3cd10): `engineBinding_of_floor` is DELETED. It was not merely resting on the refuted `Poseidon2SpongeCR` — its CONCLUSION was false too: `EngineBinding (floorEngine hash)` asserts `hash [a,b] = hash [c,d] → a = c`, and the diagonal `n ↦ (n,0)` injects an infinite set into a bounded codomain, so it is pigeonhole-refuted at deployed BabyBear exactly like the floor. ANY global `∀ p q, ¬Coll` side condition reproduces it, which is why the honest object is per-PAIR: `EncColl hash enc p q` at the two public-input lists the sponge actually absorbs (all three poles proved), plus `vk_determined_or_encColl` with NO hypothesis, and nine `*_binding_from_fold` / nine floor-free `*_or_collides`. Nine `honest_companion_fires` were themselves conditional on the refuted floor — the POSITIVE non-vacuity witnesses were vacuous — and are now unconditional at `Reference.refSponge`, whose CR is proved. The named big-bang piece = the deployed turn/precondition descriptor must EMIT the Dfa caveat's PI-commitment at fixed PI slots (the Witnessed{Dfa} twin of customPiExposure) — rides the VK regen. The algebraic DSL fragment reuses the leaf DIRECTLY; the production dregg-dfa-routing-v1 hits the named Poseidon2(Hash4to1/ChainedHash2to1/SeedHash2to1)+TableFunction gap (shared with custom). #assert_axioms-clean, no sorry.
import Dregg2.Circuit.DecoBackingAttack -- ADVERSARIAL (DECO/Stripe money-in analog of BridgeBackingAttack, the 8th carrier): refutes the deployed Stripe/DECO mint's PAYMENT backing. deployed_admits_unbacked_deco = a stripeMint row (Effect::Mint) that SATISFIES the deployed row intent (a positive balance credit) while its published payment_hash (the withPaymentHashPin PI) is backed by NO verifying DECO/zkTLS attestation — the deployed AIR has no commitment-binding op and reads payment_hash in no constraint, so a pure light client sees the credit with no witnessed payment (the off-AIR StripeWebhookEvent::verify HMAC-SHA256 is the only enforcer). deployed_intent_does_not_force_backing = no uniform "deployed-accepts ⟹ backed". deployed_admits_consumed_payment = the consume-once guard (bridge_ledger.rs::bridge_mint_against_lock over committed note_nullifiers) is a RE-EXEC tooth. Same vacuity CLASS as bridge: real-as-executor-verified, vacuous-as-deployed-LC. Grounds onto Crypto.Deco (paymentDigest models deco_payment_hash_felt; deco_authenticates_payment authenticates a verifying attestation). REPAIR: DecoBindingFromFold. #assert_axioms-clean, no sorry.
import Dregg2.Circuit.DslBindingFromFold -- THE FLIP (dsl: BackingAttack → BindingFromFold, tooth-gated — the 6th carrier): the rc-EMIT landed cohort-wide (withDfaRcPins: the caveat-region 4-felt DFA route-commitment carrier published as member PIs at FIXED columns / PER-MEMBER indices — transfer 46..49, sovereign 54..57 — so the fold arm DERIVES the slots from the committed row, ivc_turn_chain::dsl_rc_claim_pi_lo) + the Dsl fold arm (dual-expose at the derived rc slots → prove_dsl_leaf_with_commitment (custom reuse) → prove_dsl_binding_node_segmented; fail-closed BOTH poles: pin-less descriptor refused AND the ZERO rc sentinel refused host-side — a no-Dfa turn never folds a vacuous claim, it takes the re-exec rung), and the deployed-path tooth BIT (dsl_binding_deployed_tooth.rs: honest Dfa-gated transfer FOLDED+LC-VERIFIED, forged rc REJECTED, zero-sentinel witness REFUSED). dsl_binding_from_fold = the deployed dsl binding from TRUE-as-deployed premises (FRI floor via AggAirSound.FriExtract + Poseidon2SpongeCR + the connect), consuming DslBackingAttack's §C repair dslEngineBinding_of_route_commitment_factoring for the engine binding. dslWitnessed_from_fold = the §B close at the AGGREGATE: grounding onto DslBackingAttack.DslWitnessed (deployed_does_not_force_witnessed stays TRUE of the bare AIR — the Dfa caveat still has NO deployed op; the aggregate adds the fold edge). Non-vacuous both ways (honest_companion_fires + honest_dslWitnessed; forged_rc_unsat_demo = the §A forged-999 rc makes the aggregate UNSAT). DslBackingAttack STANDS beside it. #assert_axioms-clean.
import Dregg2.Circuit.BridgeBindingFromFold -- THE FLIP (bridge: BackingAttack → BindingFromFold, tooth-gated — the 7th, LAST carrier): the felt-domain mint_hash thread landed end-to-end — STEP 1 the executor re-align (the membership-687601953 precedent: the projector mint_hash IS note_spend_mint_hash_felt = hash_fact(hash_fact(nullifier,[root,dest_fed,asset]),[value_lo,value_hi]) over EXACTLY the six compressed felts apply_bridge_mint enforces the REAL note-spend STARK against — dsl::note_spending::bridge_mint_hash_felt, ONE canonical function, all three projector twins re-aligned; every bridge mint_hash VALUE changed), STEPS 2-4 the pin-emit (mintVmDescriptor2R24 = mintV3BridgeHash = withMintHashPin (withSelectorGate selM.MINT mintV3): the mint row's param0 published at PI 46 FIRST-row, rc 47..50; producer-filled, regen landed, drift PASS — only the mint-family rows moved), STEP 5 the fold arm (dual-expose at PI 46 → prove_note_spend_leaf_with_claim: the RE-PROVEN REAL note-spend STARK — key knowledge + Merkle membership + full-width commitment, the mint identity recomputed IN-AIR at lane 6; folding the binding-only bridge_action_air stays REFUSED as backing → prove_note_spend_mint_binding_node_segmented; ONE connected lane binds the whole spend tuple under CR; fail-closed admission: FIRST-row prmCol-0 pin only), and the deployed-path tooth BIT (bridge_binding_deployed_tooth.rs: honest bridge mint FOLDED+LC-VERIFIED on the committed row, forged mint identity REJECTED, the regen-tie geometry + nullifier-linkage cheap teeth). bridge_binding_from_fold = the deployed bridge backing from TRUE-as-deployed premises (FRI floor via AggAirSound.FriExtract + Poseidon2SpongeCR + the identity factoring + the connect), with the anti-double-mint LINKAGE (the folded identity DETERMINES the consumed nullifier). backedAt_from_fold = the §A/§B close at the AGGREGATE: grounds onto BridgeBackingAttack.BackedAt (deployed_intent_does_not_force_backing stays TRUE of the bare AIR; the aggregate adds the fold edge; the ¬consumed half rides the NAMED re-exec hfresh — BridgedNullifierSet, §C deployed_admits_consumed_nullifier STANDS). Non-vacuous both ways (honest_companion_fires + honest_backedAt; forged_mint_hash_unsat_demo = the §A goodBridgeMintRow forged-0 identity makes the aggregate UNSAT). BridgeBackingAttack STANDS beside it. #assert_axioms-clean.
import Dregg2.Circuit.DecoBindingFromFold -- THE FLIP (deco/Stripe money-in: BackingAttack → BindingFromFold — the 8th carrier, fiat money-in made light-client-witnessable): the felt-domain payment_hash thread. THE ANCHOR (dsl::deco_payment::deco_payment_hash_felt = hash_fact(hash_fact(amountCents,[currency,recipient]),[paymentIntentId]) over the PaymentFacts felts, NEVER the executor byte-domain BLAKE3 payment_nullifier — the anti-vacuity law). THE LEAF (circuit-prove::deco_leaf_adapter::prove_deco_leaf_with_claim: a Poseidon2-only commitment AIR recomputing the identity IN-AIR from PI-pinned PaymentFacts columns — gates 3/4/5 of Deco.lean::DecoRelation; the leaf tooth forged_payment_hash_does_not_fold refuses a forged identity AT THE LEAF, REAL recursion, GREEN). THE FOLD (prove_deco_payment_binding_node_segmented: the in-circuit connect ties the leaf's exposed identity to the deployed leg's published payment_hash PI). ed25519/HMAC/SHA-256/Web-PKI/the DECO handshake/Stripe schema stay OFF-AIR (the named §8 carriers, Deco.lean::deco_binds_payment — exactly bridge's ed25519+nullifier-set-off-fold posture). deco_binding_from_fold = the deployed backing from TRUE-as-deployed premises (FRI floor via AggAirSound.FriExtract + Poseidon2SpongeCR + the identity factoring + the connect), with the anti-double-mint LINKAGE (the folded identity DETERMINES the paymentIntentId). backedAt_from_fold = the §A/§B close at the AGGREGATE grounding onto DecoBackingAttack.BackedAt (the ¬consumed half rides the NAMED re-exec hfresh; §C deployed_admits_consumed_payment STANDS). deco_authenticates_from_fold = the GROUNDING onto Crypto.Deco: over decoRelEngine (verifying proofs ARE DecoRelation witnesses) the fold forces deco_binds_payment's genuine Signed/Tagged/opening facts — the light-client witness of deco_authenticates_payment. Non-vacuous both ways (honest_companion_fires + honest_backedAt; forged_payment_hash_unsat_demo = the §A forgedDecoMintRow payment_hash=0 makes the aggregate UNSAT). The deployed stripeMint descriptor emit rides the coordinated big-bang regen. DecoBackingAttack STANDS beside it. #assert_axioms-clean.
import Dregg2.Circuit.ClosureSurface -- the CLOSURE KEYSTONE: instantiates the apex CommitSurface at the FULL Lean commitment recStateCommit (S_live; .commit = recStateCommit definitionally, S_live.commit_binds = recStateCommit_binds_kernel over the CR carriers), REMOVING the wireCommit↔recStateCommit surface seam (TransferDecodeBridge's LedgerSurfaceReadout/TransferEncodeResidual are GONE). closedBridge_of_step: StateDecode S_live pc pre post + a per-effect fullActionStep pre fa post (actionTag fa = e) ⟹ kstepAll e pre post — NO surface-seam hypothesis. THREE closed rungs, one per grade: transfer_descriptorRefines_closed (VALUE_FORCED, tag 0, via transfer_descriptorRefines_fullActionStep), cellSeal_descriptorRefines_closed (principled-fix committed-root-limb, tag 52, via cellSeal_descriptorRefines), revoke_descriptorRefines_closed (phase-D cap family, tag 2, via revoke_descriptorRefines). HONEST FINDING (the keystone's point): instantiating at recStateCommit DISCHARGES FAITHFULNESS (the published root determines the full kernel) — that is what removes the surface seam — but does NOT discharge EffectDecodeBridge outright. Two residuals remain, named precisely: (1) the .log advance — recStateCommit is KERNEL-ONLY (RestHashIffFrame lists the 16 kernel fields, no log; the RecChainedState.log field is not a commitment input), so the receipt chain is STRUCTURALLY outside the kernel surface (carried inside <effect>Encodes.logAdv); (2) the transition/16-field-frame/guard — circuit-FORCED via the per-effect <effect>Encodes, NOT commitment-forced (binding two kernels does not make a relation between them true). These are the SAME residual the landed rung already carried — recStateCommit relieves none of them; it relieves the faithfulness/surface-seam obligation only. The richer EffectCommit.CommitSurface has an LH log-hash field that DOES bind the log, but the apex StateDecode/descriptorRefines layer is built on the kernel-only CircuitSoundness.CommitSurface. #assert_axioms-clean.
import Dregg2.Circuit.ClosureLog -- CLOSES the .log conjunct ADDITIVELY over ClosureSurface (the one residual the kernel-only recStateCommit surface STRUCTURALLY cannot bind). StateDecodeLog: StateDecode + published log commitments pubLogPre/pubLogPost + logBinds (pubLogPre = LH pre.log, pubLogPost = LH post.log) through the REALIZABLE logHashInjective LH carrier (the log-CR floor, same class as Poseidon2SpongeCR — a HYPOTHESIS, what EffectCommit.CommitSurface.LH / AssuranceCase.integrity_guarantee realizes; never an axiom). logAdvance_forced: from StateDecodeLog + the PUBLISHED effect's deterministic receipt-prepend (pubLogPost = LH (receipt :: pre.log), receipt = pipelinedSendReceipt/authReceipt/cellLifecycleReceipt — a fn of the effect's actor/cell), logHashInjective FORCES post.log = receipt :: pre.log. THREE full closed-with-log rungs (one per grade), each discharging the COMPLETE <effect>Spec INCLUDING its .log conjunct, the advance DERIVED from logBinds (no longer a carried <effect>Encodes.logAdv): transfer_descriptorRefines_closedLog (tag 0, BalanceMovementSpec, receipt=tr), cellSeal_descriptorRefines_closedLog (tag 52, CellSealSpec, receipt=cellLifecycleReceipt actor cell), revoke_descriptorRefines_closedLog (tag 2, RevokeSpec, receipt=authReceipt holder). Each takes the effect-encode MINUS logAdv (modeled as the encode parameterized over the single log-advance field) + the published receipt-prepend, derives the advance, reconstitutes the encode, calls the landed <effect>_descriptorRefines, then closedBridge_of_step → kstepAll over the PUBLISHED endpoints. FINAL FLOOR each rung carries: {StarkSound (apex), the Poseidon/Merkle CR set (in S_live), logHashInjective LH (now CONSUMED to force the advance)} + the per-effect circuit encode-minus-log (the CIRCUIT, not a floor) + the published receipt-prepend (the PUBLISHED effect, not a floor). The .log obstruction is now CLOSED for all three grades — the advance rides the realizable log-CR carrier, no free logAdv. #assert_axioms-clean.
import Dregg2.Circuit.ClosureAll -- FANS OUT the closed-with-log rung to EVERY effect, then ASSEMBLES the closed apex lightclient_unfoolable_closed. closedLog_of_encode: the ClosureLog template factored into ONE generic combinator (StateDecodeLog + receipt + published receipt-prepend + logNeeds encode-minus-log + actionTag fa = e ⟹ kstepAll e, the .log advance DERIVED via logAdvance_forced inside logHashInjective, never a free logAdv). 33 per-effect <effect>_closedLog rungs cloning it (transfer/cellSeal/revoke re-exported from ClosureLog; delegate/introduce/attenuate/delegateAtten/revokeDelegation/refreshDelegation cap-family; cellUnseal/cellDestroy/refusal/receiptArchive/setPermissions/setVK/makeSovereign/setFieldDyn/createCell/createCellFromFactory/spawn/noteSpend/noteCreate compressN-style; emitEvent/incrementNonce/mint/burn/bridgeMint/setField×8/pipelinedSend/heapWrite). ONE structural holdout NAMED: exercise (tag 16) — ExerciseSpec has NO outer .log receipt-prepend (the log advances in the INNER fold, faithfully); exercise_closedLog bridges directly via closedBridge_of_step, does NOT consume logHashInjective, NOT a residual. §C: ClosedLogExtract S LH hash R e = the per-effect circuit-witness + log-floor extraction bundle (Satisfied2 (R e) + StateDecodeLog ⟹ kstepAll e); effectDecodeBridge_of_closedLogExtract discharges EffectDecodeBridge from it given the realizable mkLog log-enrichment (the logHashInjective floor binding the published log commitments); hrefinesAllClosed assembles the apex ∀; closedLogExtract_transfer PROVES the transfer slot non-vacuous from transfer_closedLog modulo the genuine Satisfied2⟹encode circuit residual. lightclient_unfoolable_closed: the apex at S_live/Rfix/kstepAll, carried floors EXACTLY {StarkSound, Poseidon2SpongeCR + CommitSurface CR, WitnessDecodes, ClosedLogExtract-carrying-logHashInjective} — all realizable; NO per-effect EffectDecodeBridge/LedgerSurfaceReadout/decode residual remains beyond the circuit witness + the four floors. #assert_axioms-clean.
import Dregg2.Circuit.TransferDecodeBridge -- DEMONSTRATES the EffectDecodeBridge/WitnessDecodes floor REALIZABLE for transfer (the LEDGER-committed part). transfer_decodeBridge: StateDecode S pc pre post + the transfer witness + the named surface seam (LedgerSurfaceReadout — the wireCommit↔recStateCommit reconciliation at the 4 moved coords, REALIZABLE for the deployed rotated-recStateCommit surface) + the non-ledger residual (TransferEncodeResidual: the rows/RowEncodes decodes/IsTransferRow + dir/amt tags + guardAuth-cap-tree + 16 non-bal frame fields + log) ⟹ rotatedEncodes. DISCHARGED from StateDecode (the realizable win): the 4 bal-boundary fields hsrcPre/hsrcPost/hdstPre/hdstPost + the cross-cell hledgerFrame (the decoded balance limbs ARE the StateDecode-bound kernels' ledger entries, via the surface seam under Poseidon2SpongeCR). NAMED residual (NOT discharged — the part the ledger ROOT cannot carry): the per-row column reads + the cap-tree authority (= the RotatedKernelRefinementFacet.TransferAuthoritySource residual) + the 16-field frame/log. Compose: transfer_descriptorRefines_fromStateDecode + transfer_fullActionStep_fromStateDecode = the clean EffectDecodeBridge-shaped transfer rung taking StateDecode instead of a raw rotatedEncodes. Tooth: decodeBridge_rejects_wrong_readout (the discharged ledger field has teeth — a seam mis-reading the ledger is refused by the circuit-forced debit). The floor is now demonstrated REALIZABLE for transfer's ledger part; the other 35 effects follow the same column-readout. #assert_axioms-clean.
import Dregg2.Circuit.ClosureTransfer -- DISCHARGES the transfer slot of ClosedLogExtract to its ASYMPTOTIC FLOOR: {the four realizable crypto floors + the cap-open authority witness}, NO carried extract/raw-rotatedEncodes/column residual. SPLITS the opaque extract of closedLogExtract_transfer into its genuine sources: (1) TransferTraceReadout — the circuit-witness COLUMN+FRAME extraction (the two designated rows di/ci + RowEncodes decodes + IsTransferRow + dir/amt tags + RotTableSide + the 16 non-bal frame fields + side guards + the receipt-log advance) — pure functions of the SATISFIED trace t + the prover's row designation, the genuine WitnessDecodes-class limb-level reads the LEDGER root cannot certify, supplied by StarkSound; (2) LedgerSurfaceReadout (reused from TransferDecodeBridge) — the 4 bal-boundary limbs + hledgerFrame FROM the S_live StateDecode (recStateCommit full-kernel binding) under the Poseidon/Merkle CR floor; (3) TransferAuthorityWitness := RotatedKernelRefinementFacet.TransferAuthoritySource — the SOLE irreducible residual: the cap-open rides a SEPARATE descriptor (the LIVE transferCapOpenEffV3) whose Satisfied2 the transfer trace does NOT contain, carried as the realizable in-circuit depth-16 cap-membership opening (authWitness_forces_faithful: it FORCES the deployed two-axis authorizedFacetB). rotatedEncodes_of_floors assembles the full rotatedEncodes from the three floors (no opaque extract); closedLogExtract_transfer_closed produces ClosedLogExtract S_live LH hash Rfix 0 from {readout + hpub-receipt + ledger-seam + authWitness + its toy-tier projection}. HONEST FINDING: the column reads do NOT unpack from Satisfied2 ALONE — they need the prover's row-designation/turn-binding (which row is debit/credit, amount = tr.amt), exactly the WitnessDecodes-class witness StarkSound extracts; named as the TransferTraceReadout floor, not faked. The asymptotic shape the other 35 effects replicate (column reads from the trace, ledger/frame from the surface decode, authority from the per-effect cap-open). #assert_axioms-clean.
import Dregg2.Circuit.ClosureTransferAvail -- the transfer + burn closure slots ON THE HARDENED (DEPLOYED) PATH: the availOf residual DISCHARGED, not deleted. ClosureTransfer's slot is stated at Rfix 0 = the BARE transferV3Membership, whose mod-p gate does NOT force amt ≤ bal, so it must carry availOf — a REGISTRY-LAG artifact: the wire registries route the hardened *Avail faces (borrow-limb decomposition + range teeth + no-final-borrow gate, VK epoch 887b95e76) and RotatedKernelRefinementAvailWide PROVES availability + the EXACT ℤ debit on the deployed welded members. This module carries that discharge up to the CLOSURE: TransferTraceReadoutAvail (the same WitnessDecodes-class column+frame floor — .toReadout projects onto ClosureTransfer's TransferTraceReadout — over the multi-width RotTableSideW + the debit row's WIDTH-only canonicality envelope; NO availability leg), rotatedEncodesAvail_of_floors (the assembly with the havail argument GONE), transfer_descriptorRefinesAvail_closedLog (the closed-with-log rung on weldedTransferAvailWide), and closedLogExtract_transfer_closed_avail : ClosedLogExtract S_live LH hash R 0 for any registry with R 0 = weldedTransferAvailWide — floors EXACTLY {TransferTraceReadoutAvail, hpub, LedgerSurfaceReadout, TransferAuthorityWitness + toyAuthOf}, availability supplied by availability_forced_weldedWide. §6 closes the SIBLING slot of the same class: burn (tag 4), whose availability was carried INSIDE rotatedEncodesBurn.guardAvail (well-supply inflation, audit forgery 2) — over weldedBurnAvailWide the readout floor supplies rotatedEncodesBurnAvail, which has NO guardAvail field. RfixAvail = Rfix with exactly the two debiting tags flipped to the deployed hardened members (RfixAvail_off: every other tag is Rfix verbatim). TEETH: closure_rejects_overdebit_avail / closure_audit_forgery_unsat (pre.bal = 0, amt = 10⁹) / closure_rejects_overburn_avail — UNSAT at the closure slot, so the discharge is a PROOF, not a deletion. The cap-open authority residual is untouched (a separate seam). Remaining: the v3RegistryHeap flip so Rfix 0 IS the deployed member. #assert_axioms-clean.
import Dregg2.Circuit.ClosureFanoutGenuine -- the GENUINE (anti-laundering) per-effect dischargers: replaces each laundered closedLogExtract_<e>_closed (= closedLogExtract_of_rung over a bare ClosedLogRung hypothesis, i.e. the whole Satisfied2→kstep refinement carried) with one that (1) TAKES a NAMED decode-extraction floor <e>TraceReadout = Satisfied2 (Rfix e) ⟹ <e>EncodesMinusLog (the WitnessDecodes-class limb-level readout, Satisfied2→ENCODE not Satisfied2→kstep), and (2) ACTUALLY CALLS the proven <e>_closedLog rung (the landed RotatedKernelRefinement* …EncodesMinusLog → kstepAll e) on the extracted encode + StateDecodeLog + published receipt-prepend. 29 genuine dischargers, EACH naming its proven *_closedLog rung in the proof term (grep: mint/burn/bridgeMint/incrementNonce/setField/heapWrite Satisfied2-style; delegate/introduce/attenuate/delegateAtten/revokeDelegation/revoke/refreshDelegation cap-family threading the deployed Scap CapHashScheme; cellSeal/cellUnseal/cellDestroy/refusal/receiptArchive/setPermissions/setVK/makeSovereign/createCell/createCellFromFactory/spawn/noteSpend/noteCreate compressN-family threading cnFamily+hN; emitEvent/pipelinedSend value-forced; exercise the structural holdout = Satisfied2→exerciseEncodes with NO receipt-prepend, still calling exercise_closedLog). ClosureReadouts bundles the per-effect NAMED readouts + shared carriers (Scap, the six cnFamily compressNs + their injectivity, the transfer slot = ClosureTransfer.closedLogExtract_transfer_closed, the off-cohort `other` fallback). closedLogExtract_all_genuine discharges ∀ e, ClosedLogExtract via the 36-way actionTag case-split — every cohort slot CALLING its proven rung so the RotatedKernelRefinement* soundness rungs are LOAD-BEARING. lightclient_unfoolable_closed_final_genuine feeds it to lightclient_unfoolable_closed: carried set = the per-effect decode-extraction (WitnessDecodes class) + the crypto floors {StarkSound, Poseidon2SpongeCR + S_live CR, logHashInjective, Scap, the compressNs}, NOT the whole refinement. #assert_axioms-clean.
import Dregg2.Circuit.ClosureFinal -- THE FOLD + headline: collapses the per-effect <e>TraceReadout family into ONE witness floor parametric in pi.effect (ClosedWitness = WitnessDecodes + the SINGLE ClosedLogExtract pi.effect + the single mkLog), exploiting that lightclient_unfoolable applies its per-effect rung at EXACTLY pi.effect (the ∀ e family was over-asked). lightclient_unfoolable_one = the parametric apex consuming only the published-effect rung. lightclient_unfoolable_circuit_sound = THE HEADLINE: verifyBatch accept + {StarkSound, Poseidon2SpongeCR + S_live CR, logHashInjective (inside ClosedWitness.mkLog), ClosedWitness} ⟹ ∃ pre post, StateDecode ∧ kstepAll pi.effect ∧ endpoints commit — the four standard crypto foundations + ONE witness floor, NO 36-way family. Non-vacuity: closedWitness_of_readouts BUILDS ClosedWitness from the genuine ClosureReadouts bundle, its ext = closedLogExtract_all_genuine rds pi.effect routing through every proven <e>_closedLog rung (load-bearing); lightclient_unfoolable_circuit_sound_of_readouts composes the two. #assert_axioms-clean.
import Dregg2.Circuit.ClosureFinalAvail -- the ClosureFinal apex REPOINTED to the DEPLOYED hardened registry RfixAvail (tags 0/4 = weldedTransferAvailWide/weldedBurnAvailWide): threads ClosureTransferAvail's proven-but-untethered _availFix slots up to the apex. closedLogExtract_transport lifts the 34 UNCHANGED tags of closedLogExtract_all_genuine (Rfix e) to RfixAvail e via RfixAvail_off (the descriptors are literally identical); closedLogExtract_all_genuine_avail = ∀ e, ClosedLogExtract … RfixAvail e (debiting tags from the _availFix slots, availability DISCHARGED; every other tag transported). ClosedWitnessAvail = the ClosedWitness mirror over RfixAvail; lightclient_unfoolable_closed_final_avail = THE HARDENED APEX (verifyBatch (vkOfRegistry RfixAvail) accept + [StarkSound hash RfixAvail] + crypto floors + ClosedWitnessAvail ⟹ ∃ pre post, StateDecode ∧ kstepAll pi.effect ∧ endpoints commit) — at pi.effect ∈ {0,4} the transition's amt ≤ bal is FORCED by the deployed borrow chain, availOf DISCHARGED at the descriptor the light client verifies, NOT carried. closedWitnessAvail_of_readouts BUILDS it from the genuine ClosureReadouts + the two _availFix slots (non-vacuous). Rfix/vkOfRegistry deliberately UNCHANGED (in-place flip unsound: the welded .umemOp fails MapShape); this is the parallel additive deployed-path capstone. The STARK floor [StarkSound hash RfixAvail] is realized by AlgoStarkSoundKernelAvail.algoStarkSound_kernelAvail (targeted, the umem memory-legs route). #assert_axioms-clean.
import Dregg2.Circuit.CircuitCompleteness -- THE CONVERSE of ClosureFinal: a kernel-VALID turn HAS an accepting proof (liveness / no-false-rejection; the circuit never spuriously REJECTS a genuine transition). Dual to lightclient_unfoolable_circuit_sound, rung-for-rung. (1) descriptorComplete S hash d kstep = the per-effect SATISFIABILITY rung (dual of descriptorRefines): every kstep pre post (AccountsWF boundary) admits a Satisfied2 witness of d publishing the kernel's own commitment commitOf S pre post turn, decoding faithfully to (pre,post). (2) StarkComplete hash R = the dual audited p3 batch-STARK COMPLETENESS floor (dual of StarkSound): a Satisfied2 witness publishing pi ⟹ ∃ π, verifyBatch accept (the prover CAN prove what it satisfies — FRI/p3 completeness; REALIZABLE, named, not provable in Lean). (3) lightclient_complete = THE APEX: kstep pre post + descriptorComplete + [StarkComplete] + the named hash CR ⟹ ∃ pi π, verifyBatch (vkOfRegistry R) pi π = accept ∧ pi commits to (pre,post) — a VALID turn HAS an accepting proof. The COMMITMENT direction is CONSTRUCTIVE (stateDecode_construct: the prover HOLDS the kernels and computes S.commit — the EASY dual of WitnessDecodes, no carried floor). (4) transfer_descriptorComplete = the transfer BEACHHEAD (template, dual of transfer_descriptorRefines): from BalanceMovementSpec the spec DETERMINES the whole rotatedEncodes decode — transfer_rotatedEncodes_construct CONSTRUCTS it (boundary limbs = kernel ledger, frame = unchanged fields, guards = admitGuardA, all spec-discharged); what is NOT constructed is the satisfying VmTrace itself (the chip/range row assignment a real circuit run produces) = the realizable PROVER floor TransferTraceProver (dual of soundness's TransferTraceReadout — soundness READS the trace, completeness BUILDS one) + buildWitness (the prover's circuit run producing the Satisfied2-publishing-pi witness, the construction-dual of StarkSound's extraction). NON-VACUITY tooth: transfer_descriptorComplete_genuine + _credit_genuine prove the constructed decode realizes the GENUINE debit/credit (post.bal src a = pre.bal src a − amt, forced from BalanceMovementSpec via recTransferBal_correct) — not a degenerate no-move witness. Per-effect fan-out shape (dual to the 36 soundness *_descriptorRefines rungs): each effect's <e>Spec DETERMINES its <e>Encodes, so each is a transfer_rotatedEncodes_construct-shaped CONSTRUCT + its own <e>TraceProver realizable floor. #assert_axioms-clean.
import Dregg2.Circuit.CircuitCompletenessValue -- the COMPLETENESS rungs for the economic VALUE-FORCED effects: burn / mint / bridgeMint / setField (dual of the soundness refinements in RotatedKernelRefinementMintBurn / RotatedKernelRefinementSetField, mirroring the transfer template CircuitCompleteness.transfer_descriptorComplete EXACTLY). For each effect: (1) <e>TraceProver = the realizable trace-row construction floor (NAMED, Type-valued) — the designated circuit row + its IsXRow/RowEncodes decode + the decoded CellStates + the limb/value equalities tying them to the kernel ledger; the construction-dual of the soundness row readout. (2) <e>_rotatedEncodes*_construct = CONSTRUCT the rotatedEncodes* witness FROM the kernel <e>Spec: the ledger frame (bal = recTransferBal … for burn/mint, cell = setFieldCellMap … for setField), the admissibility guard, the 16-field frame, the receipt log are ALL discharged from the spec's conjuncts; only the row + limb-ties come from the prover floor. (3) <e>_descriptorComplete = <e>Spec + a realizable buildWitness ⟹ a Satisfied2 witness of <e>V3 publishing the kernel's own commitment (commitOf), decoding to (pre,post) via stateDecode_construct — the per-effect satisfiability the apex lightclient_complete consumes. NON-VACUITY teeth (the constructed decode is the REAL move): burn_descriptorComplete_genuine + _well_genuine (post.bal cell a = pre.bal cell a − amt AND post.bal a a = pre.bal a a + amt via recBurn_ledger_correct), mint_descriptorComplete_genuine + _well_genuine (+amt credit / −amt well-debit via recTransferBal_mint_correct), setField_descriptorComplete_genuine (fieldOf (slotName slot) (post.cell cell) = v via writeFieldCellMap_correct). bridgeMint_descriptorComplete = mint rung re-exported (same recCMintAsset / MintASpec / mintV3). #assert_axioms-clean on every theorem.
import Dregg2.Circuit.CircuitCompletenessRecord -- the COMPLETENESS rungs (wave 2) for the per-cell VALUE/RECORD effects whose soundness rungs are realized: incrementNonce / emitEvent / pipelinedSend / makeSovereign / setFieldDyn / setPermissions / setVK (dual of the soundness refinements in RotatedKernelRefinement{IncNonce,Misc,PermsVK}, mirroring CircuitCompletenessValue EXACTLY). For each effect: (1) a realizable prover floor (NAMED, Type-valued) — IncNonceTraceProver = the designated active nonce-tick row + its IsIncNonceRow/RowEncodesIncNonce decode + the cellPre/cellPost CellStates + hnVal (n = cellPre.nonce+1); MakeSovereignRootProver/SetFieldDynRootProver/SetPermsRootProver/SetVKRootProver = the committed FIX-root limb (postRoot/hpost/gate, + hRebindRoot for makeSovereign) the honest prover supplies; emitEvent/pipelinedSend carry NO prover floor (LIVE whole-state-row passthrough, every leg spec-determined). (2) <e>_<encode>_construct = CONSTRUCT the soundness <e>Encodes FROM the kernel <e>Spec: the whole-cell-map move (incNonceCellMap/sovereignRebind/setFieldCellMap/setPermsCellMap/setVKCellMap), the admissibility guard, the 16/17-field frame, the receipt log are ALL discharged from the spec's conjuncts; only the row/FIX-root comes from the prover floor. (3) <e>_descriptorComplete = <e>Spec + a realizable buildWitness ⟹ a Satisfied2 witness publishing the kernel's own commitment (commitOf), decoding to (pre,post) via stateDecode_construct — the per-effect satisfiability the apex lightclient_complete consumes. NON-VACUITY teeth (the constructed decode is the REAL move, via the SAME move-correctness lemma soundness uses): incrementNonce_descriptorComplete_genuine (fieldOf nonceField (post.cell cell) = n via incrementNonce_cellWrite_correct), emitEvent/pipelinedSend_descriptorComplete_genuine (the receipt advance off the spec's log clause), makeSovereign_descriptorComplete_genuine (post.cell cell = .record [(commitmentField,.dig (stateCommitment (pre.cell cell)))] via makeSovereignSpec_commitment_value), setFieldDyn_descriptorComplete_genuine (fieldOf f (post.cell cell) = v via writeFieldCellMap_correct), setPermissions/setVK_descriptorComplete_genuine (fieldOf perms/vkField (post.cell cell) = p/vk via the cellWrite_correct lemmas). #assert_axioms-clean on every theorem.
import Dregg2.Circuit.CircuitCompletenessLifecycle -- the COMPLETENESS rungs (wave 3) for the cell LIFECYCLE / audit-record family whose soundness rungs are realized in RotatedKernelRefinement{CellSeal,Lifecycle}: cellSeal / cellUnseal / cellDestroy / refusal / receiptArchive (dual of those soundness refinements, mirroring CircuitCompletenessRecord's lifecycle-limb *RootProver template EXACTLY). For each effect: (1) a realizable FIX-root prover floor (NAMED, Type-valued) — LifecycleRootProver / UnsealRootProver = the two committed lifecycle-root columns + the FIX gate (target lcSealed / lcLive) + the off-cell lifecycle freeze; DestroyRootProver = BOTH the lifecycle-root limb (target lcDestroyed) AND the death-cert-root limb (target certHash) + both freezes; AuditRootProver = the committed audit-slot-root column + the FIX gate pinning the slot to the one-shot flag 1 (parameterized by f, serving both refusal/receiptArchive). (2) <e>_<encode>_construct = CONSTRUCT the soundness <e>Encodes FROM the kernel <e>Spec: the whole-lifecycle-map move (sealLifecycleMap/unsealLifecycleMap/destroyKernelMap+destroyDeathCertMap) or whole-cell-map audit move (auditCellMap), the admissibility guard, the 15/16-field frame, the receipt log are ALL discharged from the spec's conjuncts; only the FIX root(s) come from the prover floor. (3) <e>_descriptorComplete = <e>Spec + a realizable buildWitness ⟹ a Satisfied2 witness publishing the kernel's own commitment (commitOf), decoding to (pre,post) via stateDecode_construct — the per-effect satisfiability the apex lightclient_complete consumes. NON-VACUITY teeth (the constructed decode is the REAL lifecycle/record move, off the spec's own clause): cellSeal_descriptorComplete_genuine (post.lifecycle cell = lcSealed), cellUnseal_…_genuine (= lcLive), cellDestroy_…_genuine (= lcDestroyed) AND _deathCert_genuine (post.deathCert cell = certHash — the death-certificate fold is genuinely written), refusal/receiptArchive_…_genuine (fieldOf refusal/lifecycleField (post.cell cell) = 1 via auditCellWrite_correct — the audit record is genuinely written). #assert_axioms-clean on every theorem. ASYMMETRY: none — every soundness column reconstructs from the Spec alone (the FIX roots are realizable prover floors, dual to the soundness readouts, never reconstructed FROM the spec).
import Dregg2.Circuit.CircuitCompletenessAuthority -- the COMPLETENESS rungs (cap-token / authority wave) for the CAP-AUTHORIZED effects whose soundness AUTHORITY leg is realized in RotatedKernelRefinementFacet (EffAuthoritySource / effAuthoritySource_authorizes, via Emit/CapOpenEmit.effCapOpenV3_authorizes): attenuate / exercise / delegate / introduce / grantCap / revoke / revokeDelegation / refreshDelegation / revokeCapability. The CONVERSE of effCapOpenV3_authorizes (cap-open Satisfied2 + DeployedFaithfulEff + edge ⟹ authorizedFacetEffB): from a cap-authorized turn CONSTRUCT a cap-open authority source so an accepting proof EXISTS. THE LOAD-BEARING ASYMMETRY (resolved HONESTLY, the prompt's case (a)): the kernel gate authorizedFacetEffB caps provided effectBit turn = decide (actor = src) || (caps actor).any (capAuthorizesFacetEff …) is a DISJUNCTION (owner OR cap), and the soundness keystone effCapOpenV3_authorizes discharges authority STRICTLY through the CAP disjunct (via authorizedFacetEffB_holds_cap). So completeness of the cap-OPEN descriptor is NECESSARILY about CAP-authorized turns — the honest statement is CONDITIONAL on cap-authority (the AuthorizedByCap hypothesis: a concrete witnessing FacetCap c ∈ caps actor over src permitting the effect bit under a satisfied tier). The OWNER-authorized turn (actor = src, no cap) proves through a SEPARATE owner path (owner_authorityComplete: decide(actor=src)=true, NO cap-open) — landed here too; authorityComplete_dichotomy pins the gate is EXACTLY (owner OR cap), so authority-completeness is EXACTLY the two rungs (no third path, neither subsumes the other). NOT a stub: both disjuncts proven. CapOpenWitness = the realizable cap-open CONSTRUCTION floor (NAMED, Type 1, dual of the soundness EffAuthoritySource carried trace data — soundness EXTRACTS it off a proof, the honest prover BUILDS it): the cap-open Satisfied2 of the live effCapOpenV3 base name n + chip-soundness + opened row + deployed leafAt + DeployedFaithfulEff over the ACTUAL effect bit 1<<<n + (actor⇒src) edge + committed-tier. capOpenWitness_to_source repackages it as EffAuthoritySource (pure field-for-field). authorityComplete_generic = THE RUNG (CapOpenWitness ⟹ authorizedFacetEffB caps provided (1<<<n) tr = true, FORCED via effAuthoritySource_authorizes). Per-effect rungs at the deployed facet.rs bits (attenuate/exercise EFF_TRANSFER=1, grantCap EFF_GRANT_CAPABILITY=2, revokeCapability EFF_REVOKE_CAPABILITY=3, introduce EFF_INTRODUCE=13, delegate/revoke/refreshDelegation EFF_DELEGATION_OPS=16). NON-VACUITY: authorizedByCap_nonvacuous (a concrete cap-table inhabits AuthorizedByCap) + authorityComplete_fires + unauthorized_no_authority (the BOTH-POLARITY tooth — a non-owner with empty caps is NOT authorized, the gate bites). #assert_axioms-clean. DO NOT assemble the ∀ e premise here.
import Dregg2.Circuit.CircuitCompletenessTransferConstruct -- DE-LAUNDERS the VALUE leg (transfer): REDUCES the fat per-move buildWitness floor (which bundled the satisfying trace PLUS nine spec-determined conjuncts — the four boundary CellState.balLo limbs, the four TransferParams direction/amount tags, the TransferTraceProver rows) to the MINIMAL TransferSatFloor: ONLY the trace realizability Satisfied2 hash transferV3 … t + tracePublishedCommit t = commitOf S pre post turn (the StarkComplete-class floor — by Stage 0, Satisfied2 is PURE constraint-satisfaction over a concrete VmTrace, no FRI/AIR-prover content; the FRI gap is the SEPARATE StarkComplete bridge). transfer_descriptorComplete_reduced re-proves the satisfiability rung from TransferSatFloor ALONE (the commitment decode CONSTRUCTED via stateDecode_construct; the spec-determined data DROPPED — it is fixed by the kernel move, not assumed). buildWitness_to_satFloor pins the reduction is FAITHFUL (the fat floor's output reaches the minimal one). #assert_axioms-clean. The value-leg analog of CapOpenTraceFloor.
import Dregg2.Circuit.CircuitCompletenessAuthorityConstruct -- DE-LAUNDERS the authority leg: CONSTRUCTS the faithful cap-membership (DeployedFaithfulEff + MembersAt + leaf/edge/tier decode) from AuthorizedByCap rather than ASSUMING it as CapOpenWitness fields. The genuine INVERSE of the soundness bridge deployedCapOpen_implies_authorizedEffB (MembersAt ∧ confersLeaf ⟹ ∃ cap): from ∃ cap EXHIBIT the leaf (authLeafAt: on-edge leaf decodes to the cap's facet/tier, deny-all off-edge), the root (authConstructedRoot = the leaf digest, depth-0 opening), the membership (authLeaf_membersAt: empty path), the conferral (authLeaf_confers), and the faithfulness (authConstructed_faithful: every conferring edge IS the authorizing edge, backed by the cap). tierOfTag_tierByte = the tier decode inverts its own byte under vkOfTier. The ONLY residual carried is the cap-open TRACE realizability (CapOpenTraceFloor: VmTrace t + Satisfied2 of effCapOpenV3 + chip-soundness + row-identification pinning the committed cap_root/src/leaf to the CONSTRUCTED root/edge/leaf) — the StarkComplete dual. authConstructs_source ASSEMBLES the soundness EffAuthoritySource from AuthorizedByCap + the trace floor (hfaith/htier CONSTRUCTED, not assumed); authComplete_constructed / _from_hypothesis = the DE-LAUNDERED rung forcing authorizedFacetEffB. #assert_axioms-clean.
import Dregg2.Circuit.CircuitCompletenessSetInsert -- the COMPLETENESS rungs (wave 5, the LAST per-effect cluster) for the SET-INSERT / BIRTH family whose soundness rungs are realized in RotatedKernelRefinement{Notes,NotesFresh,Birth,SpawnHandoff} (consuming the phase-D gadgets SortedTreeNonMembership + CapTreeUpdate): noteSpend / noteCreate / createCell / createCellFromFactory / spawn. The CONVERSE of those soundness set-insert / non-membership refinements: from the kernel <e>Spec CONSTRUCT the <e>Encodes witness — the committed FIX set-root columns + the set-insert gate come from a realizable PROVER floor, the guard/freshness/frame/log legs are discharged FROM the spec. For each effect: (1) a realizable set-root prover floor (NAMED, Type-valued, dual of the soundness committed set-root readout) — SetInsertRootProver = the two published note-list-root columns (NoteRootRow) + the gNoteGrow FIX gate pinning the post column to the grown-list digest x::preList (serves noteSpend nullifiers + noteCreate commitments); AccountsInsertRootProver = the two accounts-root columns (AccountsRootRow) + the gAccountsGrow FIX gate pinning the post column to insert newCell preK.accounts (serves createCell / createCellFromFactory / spawn); SpawnHandoffInsertProver = the child cap_root sorted-tree INSERT data (old/new root + childKey + spine + SpineCommits old/new bindings + childKey-fresh non-membership) from which capInsert_sound forces the cap-key-set growth. (2) <e>_<encode>_construct = CONSTRUCT the soundness <e>Encodes FROM the kernel <e>Spec: the set-insert is in the spec, the admissibility guard / no-double-spend FRESHNESS + spend-proof gate (BOTH noteSpendGuard conjuncts) / born-empty records / factory install / cap-handoff Caps-moves / frame / receipt are ALL discharged from the spec's conjuncts; only the set-root columns + insert gate (+ the cap-tree spine for spawn) come from the prover floor. (3) <e>_descriptorComplete = <e>Spec + a realizable buildWitness ⟹ a Satisfied2 witness publishing the kernel's own commitment (commitOf), decoding to (pre,post) via stateDecode_construct — the per-effect satisfiability the apex lightclient_complete consumes. NON-VACUITY teeth (the constructed decode is the REAL set move, via the SAME insert/non-membership lemma soundness uses): noteSpend_descriptorComplete_genuine (post.nullifiers = nf :: pre.nullifiers) AND _fresh_genuine (nf ∉ pre.nullifiers — the inserted key present post AND absent pre, the non-membership leg, off noteSpendGuard); noteCreate_…_genuine (post.commitments = cm :: pre.commitments); createCell / createCellFromFactory_…_genuine (post.accounts = insert newCell pre.accounts); spawn_…_genuine (accounts insert) AND spawn_descriptorComplete_handoff_genuine (keysOf S newRoot = insert childKey (keysOf S oldRoot) with childKey present post AND fresh pre, via spawn_handoff_forces_insert / capInsert_sound). ASYMMETRY WATCH (the wave-4 owner-vs-cap analog) — surfaced and DISMISSED honestly, not stubbed: the noteSpend non-membership freshness is NOT an asymmetry — NoteSpendSpec's guard noteSpendGuard IS spendProof = true ∧ nf ∉ pre.nullifiers, so the freshness the soundness phase-D non-membership open FORCES is EXACTLY the spec's own guard conjunct (spec-determined in the completeness direction; no prover-floor non-membership open needed for the encode, no owner-vs-cap disjunction). Each spec INHABITABLE (the spec files' #guard witnesses st0/stN0/sAG0/facS). #assert_axioms-clean on every theorem. DO NOT assemble the ∀ e premise here.
import Dregg2.Circuit.CircuitCompletenessSatFloor -- DE-LAUNDERS the value leg UNIFORMLY: folds the per-effect Satisfied2-publication carriers into ONE descriptor-AGNOSTIC realizability SatFloor S hash d kstep (∀ kstep pre post, AccountsWF ⟹ ∃ a satisfying trace of d publishing commitOf — the StarkComplete-class dual of the soundness StarkSound extraction, with NO per-effect trace/root prover data). descriptorComplete_of_satFloor re-proves descriptorComplete from SatFloor ALONE for ALL 21 value-leg effects (the decode CONSTRUCTED via stateDecode_construct; the spec-determined <e>TraceProver/<e>RootProver — which fed only an unused _henc — GONE). #assert_axioms-clean.
import Dregg2.Circuit.CircuitCompletenessAssembled -- THE COMPLETENESS CAPSTONE (converse of CircuitSoundnessAssembled / ClosureFanoutGenuine): discharges lightclient_complete's carried per-effect descriptorComplete premise — value-leg UNCONDITIONALLY over the live tags — and composes the authority dichotomy honestly. (1) dispatchArm_<e> = LOWER kstepAll e (= dispatchArm e = ∃ fa, actionTag fa = e ∧ fullActionStep) to the leaf <e>Spec (the converse of soundness's <spec>_to_dispatchArm; the actionTag constraint pins the constructor, fullActionStep IS the leaf spec definitionally). (2) CompletenessWitnesses S hash compressN = the per-effect prover-floor bundle (Type 1, DUAL of ClosureReadouts): for each VALUE/RECORD/LIFECYCLE/SET-INSERT live effect the realizable buildWitness construction (the honest prover BUILDS a satisfying trace of Rfix e publishing the kernel's own commitOf — the construction-dual of StarkSound's extraction). (3) descriptorComplete_value = the ∀ e (over the landed-value-rung tags) value-leg discharge, dispatching each tag to its proven <e>_descriptorComplete rung over the bundle (DUAL of closedLogExtract_all_genuine's 36-way actionTag case-split) — UNCONDITIONAL in authority (descriptorComplete is the value/state satisfiability; authority lives in kstepAll e). (4) the AUTHORITY leg = authorityComplete_dichotomy re-composed (owner ∨ cap covers every authorized turn; NOT folded into a single clean ∀ e — that would be FALSE). (5) lightclient_complete_assembled = THE HEADLINE: a genuine kernel transition kstepAll e pre post (AccountsWF) ⟹ ∃ accepting batch proof committing to (pre,post), value-leg premise GONE, standing on {StarkComplete, Poseidon2SpongeCR, the CompletenessWitnesses prover bundle}. (6) the BIDIRECTIONAL pairing with lightclient_unfoolable_assembled = verifyBatch-acceptable ⟺ kernel-valid (mod the asymmetric named floors: soundness carries StarkSound/WitnessDecodes/decode-bridges, completeness carries StarkComplete/the prover bundle). HONEST verdict: value-leg-UNCONDITIONAL ∀ e + the two-disjunct authority dichotomy — the conditional is in exactly one place (which authority disjunct a turn rides), the right answer not a forced green. #assert_axioms-clean.
import Dregg2.Circuit.CircuitCompletenessNonVacuity -- REFUTES completeness VACUITY: EXHIBITS a concrete Satisfied2 inhabitant (a constructed term, not the carried floor hypothesis) for the LIVE transferV3 — exactly the Satisfied2 the assembled completeness capstone consumes. transferTrace0 = the empty (zero-main-row) trace; satisfied2_transferV3_empty proves Satisfied2 hash transferV3 minit mfin [] transferTrace0 for ANY hash/minit/mfin (the per-row legs VACUOUS at rows=[], the memory legs collapse to the empty log/boundary: Disciplined []=True, MemCheck minit mfin [] []=rfl, table faithfulness rfl). completeness_satisfiability_nonvacuous states it plainly: ∃ minit mfin maddrs t, Satisfied2 hash transferV3 … — the carried Satisfied2 antecedent is INHABITED, so completeness is not vacuously true through an uninhabitable satisfiability claim. transferSatFloor_of_publication builds the FULL TransferSatFloor from the constructed Satisfied2 + ONLY the opaque tracePublishedCommit publication leg (the named FRI/p3 readout realizability, the SAME abstraction boundary soundness carries as StarkSound — not a Lean-internal vacuity). #assert_axioms-clean: the inhabitant is CONSTRUCTED, no open hole / native_decide / fresh axiom.
import Dregg2.Circuit.CircuitCompletenessNonVacuityReal -- UPGRADES the above from type-inhabitance to MEANINGFUL satisfiability by a NATURAL transfer: a NON-EMPTY two-row trace whose ACTIVE row 0 is a genuine debit (bal_lo 100→90 via amount 10·direction 1, a real nonce tick 0→1) satisfies Satisfied2 hash transferV3 — no nonce −1, no full drain. The Lean per-row `transition` is UNCONDITIONAL on row 0 (`nxt.before[i] = loc.after[i]`); the WRAP row (row 1, isLast — gates/welds vacuous) CARRIES row 0's REAL after-state in its BEFORE block (sbCol BALANCE_LO=90, sbCol NONCE=1), so the transition lands on the GENUINE after-state, not zero. The rotated BEFORE/AFTER welds tie r0/r1 to the moved limbs (before 100, after 90/nonce 1); the boundary-LAST pins are VACUOUS on the active row; row 1's boundary-LAST pins read its own (zero) after-block. The lookup leg (chip + range) is PURE membership (NO ChipTableSound needed for satisfiability), so the table is the rows' own evaluated lookup tuples (tfOf2). The memory/map legs collapse as in the empty case (graduateV1 emits no mem/map ops). completeness_genuinely_nonvacuous: ∃ a NON-EMPTY (rows ≠ []) trace satisfying Satisfied2 — a REAL transfer is inhabitable. #assert_axioms-clean.
import Dregg2.Circuit.DecideSatisfied2 -- THE KERNEL BRIDGE for the deployed v2 denotation (the v2 analog of Argus/InterpCore.decideVm_iff_satisfiedVm): an executable decideSatisfied2 mapDec hash d minit mfin maddrs t : Bool + the kernel-proven decideSatisfied2_iff_Satisfied2 (decideSatisfied2 … = true ↔ Satisfied2 …) — the tiny verified core a Rust ENUMERATOR runs against to decide whether a CONCRETE multi-table witness is in the deployed verify_vm_descriptor2/Ir2Air/Satisfied2 accept-set. ALL legs are genuinely Decidable EXCEPT the mapOp row leg (the opensTo/writesTo heap-opening EXISTENTIAL, the spike finding), which is ORACLE-PARAMETERIZED by a supplied mapDec : VmRowEnv → MapOp → Bool with its faithfulness hypothesis hmapDec (∀ env m, mapDec env m = true ↔ m.holdsAt hash env — the named heap-opening floor, the v1 reference's totality analog). Per-row .base/.lookup/.window/range reuse the verified decideConstraint/decideLookup/decideWindow/decideRanges; .memOp/.umemOp/.proofBind are row-locally True; the memory legs (Nodup/closed/Disciplined/MemCheck) carry proved Decidable instances; the table-faithfulness legs are list equalities. instDecidableSatisfied2 packages the Decidable instance under the oracle. Both-polarity non-vacuity: decideSatisfied2_empty_accepts (the empty descriptor on a one-row witness) + decideSatisfied2_badRange_rejects (a 2^30 wire fails a 30-bit range tooth). #assert_axioms-clean.
import Dregg2.Circuit.DecideMapMerkle -- DISCHARGES the kernel-bridge mapDec oracle: a CONCRETE decidable mapDecMerkle hash wit env m (no ∃ heap — the prover's opening supply wit : VmRowEnv → MapOp → FeltHeap IS the witness heap, checked by the decidable decOpensTo/decWritesTo against the Merkle model) + mapDecMerkle_faithful (the hmapDec shape PROVEN, not assumed: soundness unconditional via the supplied witness, completeness via the schema's FLOOR-FREE padImtRoot_binds_or_ghost_or_collides + the prover's well-formed WitnessOpens supply). ⛑ 2026-07-30 THE DECIDER DECIDES THE DEPLOYED COMMITMENT AND CARRIES NO FLOOR: decOpensTo/decWritesTo now check padImtSchema MAP_SENTINEL's conjuncts — sorted, every key < MAP_SENTINEL, length ≤ 2^MAP_TREE_DEPTH (SPARSE, not dense), padImtRoot as the commitment — and all four of mapDecMerkle_complete / _faithful / decideSatisfied2'_iff_Satisfied2 / instDecidableSatisfied2' dropped their Poseidon2SpongeCR route entirely (it rode mapRoot_injective, and that floor is PROVED FALSE at deployed BabyBear). The failure mode is now named per-instance as SupplyResid — some admissible heap commits to THIS op's own root column and carries the schema's residual against the supplied heap (PadGhost3, a deployed wound since the padding is literal BabyBear::ZERO, or a genuine sponge collision at the ONE pair padImtRootFind returns) — with BOTH poles pinned and no floor in either type: supplyResid_refuted_at_oddSponge (refuted at the in-tree inhabited instance, stated AT oddSponge rather than under a Good hash binder precisely to keep the refuted Function.Injective out of consumers' types) and mapResid_reachable (not accidentally empty). decWritesTo deliberately does NOT check HeapOk of the POST heap, because writesToMerkleS does not either — strengthening it would make the decider refuse rows the denotation accepts. Every structural step is proved at a GENERIC depth and transported to MAP_TREE_DEPTH by application; no maxHeartbeats bump. The Lean half of the faithfulness bridge is now assumption-free, full stop. #assert_axioms-clean (9).
import Dregg2.Circuit.Satisfied2Faithful -- THE FAITHFUL RESHAPE of the deployed accept-set + the v1→v2 COLLAPSE RECIPE (additive — leaves the shared Satisfied2/opensTo untouched so the 76-file rung tree + the parallel V3-emit work are not disturbed). Satisfied2Faithful permOut hash d minit mfin maddrs t EXTENDS Satisfied2 with the deployed circuit's OWN table-faithfulness as STRUCTURAL conjuncts (NOT free levers): chipTableFaithful = ChipTableSoundN permOut (t.tf .poseidon2) (the WIDE genuine-permutation chip soundness — the deployed Ir2Air::Chip, descriptor_ir2.rs:2039), permWidth (CHIP_OUT_LANES lanes), chipHashIsLane0 (the v1 digest IS lane 0 of the genuine permutation), rangeTableFaithful (t.tf .range = rangeRows BAL_LIMB_BITS, the deployed range height). chipSoundN_implies_chipSound: the WIDE soundness forces the legacy ChipTableSound head (the lever graduateV1_sound needs), so Satisfied2Faithful.chipSound DISCHARGES hchip from the structure. THE COLLAPSE RECIPE satisfied2Faithful_satisfiedVm: from a Satisfied2Faithful witness of graduateV1 d, the v1 satisfiedVm holds on every row with NO free hchip/hrange lever (the structure CARRIES them) — every satisfiedVm-shaped per-effect rung composes through this two-line pattern, dropping the graduateV1-lever discharge. transfer_satisfiedVm_faithful BEACHHEADS it on the live transfer rung (rotateV3FrozenAuthority transferVmDescriptor); the same two lines fan out per effect group. The Merkle map-root leg (depth-16 binary, nodeOf/recomposeUp/recomposeUp_inj_of_path against nodeOf_injective) is ALREADY landed in DeployedCapTree; chipTableFaithful shares its genuine-permutation carrier. #assert_axioms-clean.
import Dregg2.Circuit.FloorsNonVacuous -- ASSURANCE: the apex's named floors/carriers are provably NON-VACUOUS (none is a silently-uninhabitable hypothesis making its theorems vacuously true). Per carrier: an inhabitation witness AND, where the predicate should constrain, a separation (TRUE for some input, FALSE for another). (1) Poseidon2SpongeCR (the CR floor = injectivity): INHABITED by encodeSponge (Encodable.encode→ℤ, injective ⇒ encodeSponge_cr) + SEPARATED by a constant sponge (poseidon2SpongeCR_separates: collapses [0]/[1]) — the predicate is meaningful (not True), realizable by a hypothetical injective hash. (2) ChipTableSoundN (the wide genuine-permutation chip soundness): genuineChipTbl (one real chipRowN permOutZ [] row) SATISFIES it (genuineChipTbl_sound); forgedChipTbl ([[0]], length 1 ≠ 20) VIOLATES it (forgedChipTbl_unsound) — so Satisfied2Faithful's chip conjunct genuinely separates honest from forged. (3) THE KEYSTONE satisfied2Faithful_transferV3: the FULL Satisfied2Faithful object is inhabited for the live transferV3 by faithfulTrace (rows=[] so per-row legs vacuous; tf .poseidon2 = genuineChipTbl is genuinely ChipTableSoundN, tf .range = rangeRows BAL_LIMB_BITS genuinely, tf .memory/.mapOps = [] matching graduateV1's empty log) — chip/range faithfulness discharged as STRUCTURAL conjuncts, not levers; the collapse recipe satisfied2Faithful_satisfiedVm FIRES on it (keystone_collapse_recipe). So the apex's faithful hypothesis is provably non-vacuous. (4) EffAuthoritySource: its authority CONCLUSION authorizedFacetEffB SEPARATES (TRUE for an owner turn, FALSE for a non-owner with no caps — authorizedFacetEffB_separates), so the cap-open carrier is not a degenerate always-authorized gate (full Type-1 inhabitation needs the named CapOpenTraceFloor realizability). (5) StarkComplete: its body ∃π, verifyBatch…=accept is non-degenerate — Verdict.accept ≠ reject (verdict_accept_ne_reject), so accepting is neither tautology nor contradiction (inhabiting the class is the named FRI/p3 realizability against opaque verifyBatch). #assert_axioms-clean.
import Dregg2.Circuit.HashFloorHonesty -- ⚠ HONESTY: the hash floors are INJECTIVITY, which is FALSE for a real hash (collisions EXIST by cardinality; CR = hard to FIND, not absent) — so every theorem conditioned on Poseidon2SpongeCR/compressNInjective/compressInjective/HashCR is VACUOUSLY TRUE at real params. (§1 FALSIFIABILITY TEETH) not_injective_of_finite_range (an injective map from an infinite domain has infinite range — the counting core) specializes to: poseidon2SpongeCR_false_babyBear (the CR floor is FALSE for any BabyBear-bounded sponge, i.e. every real Poseidon2 hash_many), compressNInjective_false_of_finite_range, compressInjective_false_of_finite_range (a 2-to-1 compress into one field element), hashCR_false_of_compressing (pigeonhole: |C|<|W| ⇒ ¬HashCR). Nobody had tried to prove the floor FALSE — that is why the vacuity survived; the old encodeSponge/refSponge "non-vacuity" witnesses give FALSE COMFORT (toy injective sponge into all of ℤ, refuted by the real compressing sponge). (§2 THE KEYED COLLISION GAME; ⚑ 07-28 its FLOOR — CollisionResistant F := ∀ finder, Negl (collisionAdv F finder) — is DELETED, refuted for every compressing family, and its refutation/satisfiability/injectivity carriers moved to FloorGames §6 with the ⊤ spelled out; what remains here is the GAME) over a KEYED hash family F (winProb of finding a genuine collision over a uniform key) — a REAL assumption on the tree's ProbCrypto/Negl machinery: REFUTABLE (FloorGames.brokenFamily_not_hashCRHardQuant_top), SATISFIABLE (FloorGames.idFamily_hashCRHardQuant_top), and genuinely computational (mod2_dumb_negligible: collisions EXIST yet a finder that misses them has advantage 0 — the fix for the "existence⇒advantage 1" conflation that also makes FloorBridge's canonical family vacuous). FloorGames.hashCRHardQuant_top_of_injective: the old injective floor is STRICTLY STRONGER (⟹ new floor) but FALSE. (§3 ADVANTAGE-BOUNDED RESTATEMENT) equivocation_advantage_negligible ("two openings ⟹ equal EXCEPT w/ negligible prob" — the concrete-security form of commitment_binding/oracle_binding) + friFold_advantage_negligible (a multi-round FRI/STARK binding error = a finite SUM of per-round collision advantages, negligible by negl_finset_sum — the template every HashCR/Poseidon2SpongeCR consumer re-derives through). #assert_axioms-clean; old defs KEPT (doc-marked BROKEN) — the record matters.
import Dregg2.Circuit.VacuitySweepTeeth -- ⚠ VACUITY SWEEP (07-16), two PROVED findings, neither a soundness hole. §1 THE INJECTIVE-HASH FLOOR CLASS IS BIGGER THAN THE FOUR HashFloorHonesty FLAGGED: a census finds ~20 more carriers with the identical `∀ xs ys, h xs = h ys → xs = ys` shape, still doc-marked 'REALIZABLE', none pointing at the teeth and none re-grounded (verified: Poseidon2WideCR/Compress8CR/compress4Injective/logHashInjective/HashInjective/Compress1CR appear in NO honesty or regrounding file). poseidon2WideCR_false_babyBear refutes the most load-bearing unflagged one (7 hypothesis uses) at real params via the SAME counting core (not_injective_of_finite_range + finite_width8_bounded: the width-8 squeeze into bounded lanes has FINITE range) — sharpest because its own docstring calls it 'the EXACT analogue of Poseidon2SpongeCR', which HashFloorHonesty had ALREADY proved false; the analogy was exact, the conclusion did not travel. compress8CR_false_babyBear does the same for Compress8CR — which WAS a FIELD of the Cap8Scheme structure (chip8CR), so EVERY 8-felt cap-tree theorem carried it and no deployed Cap8Scheme VALUE could exist, and its 'Reference8 exhibits one / badChip8 falsifies one' non-vacuity argument was exactly the FALSE COMFORT HashFloorHonesty's header already named (toy injective witness satisfiable, real compressing Poseidon2 false). ⚑ REPAIRED 07-20: the field is DELETED, DeployedCapTree.deployedCap8Scheme is a REAL INHABITANT whose own chip this very tooth refutes (deployedCap8Scheme_chip_not_Compress8CR) — the vacuity is measured gone by a constructed value, not by a green build. ⛑ CLASS CLOSED 07-20: the sibling Heap8Scheme/Fields8Scheme carried the IDENTICAL field; both are DELETED, both have constructed inhabitants (deployedHeap8Scheme/deployedFields8Scheme), and §1‴-HF fires this tooth at each (deployedHeap8Scheme_chip_not_Compress8CR / deployedFields8Scheme_chip_not_Compress8CR). Compress8CR is now a field of NOTHING — it survives only as the injective special case in the three trees' strength bridges and as the refutability canaries' hypothesis. §2 MembersAt/MembersAt8 — THE DEPTH IS IN THE DOCSTRING, NOT THE Prop: cap_root.rs pins CAP_TREE_DEPTH=16 (DeployedCapOpen.DEPTH:=16) but the carrier is `∃ path, recomposeUp8 S8 (capLeafDigest8 S8 leaf) path = root` with NO length clause, and recomposeUp8 S8 cur [] = cur — so membersAt8_at_own_digest is ⟨[], rfl⟩, a DEPTH-0 'membership' (the witness CircuitCompletenessAuthorityConstruct.lean:108 already uses against a root defined AS the leaf digest). ⚑ HONEST SCOPE, and the counter-tooth proves it: MembersAt8 is NOT vacuous — root is a PARAMETER, not chosen by the existential (membersAt8_not_vacuous_general: it is refutable). The defect is FAITHFULNESS and is polarity-dependent — in NEGATIVE position (DeployedFaithfulEff8/DeployedFaithful8.backed) an over-broad MembersAt8 makes the ASSUMPTION STRONGER (demands backing for depth-0 openings the circuit never emits); in POSITIVE position (deployedCapOpen_implies_authorizedEffB's hopen) the degenerate witness is available. No theorem is WRONG; the model is coarser than the circuit. Repair (named, not applied — 153 refs/20 files, and it re-opens CircuitCompletenessAuthorityConstruct's minimal opening): carry path.length = DEPTH, or carry MembershipDepthGeneralRung2.LeafNodeSep and prove the cross-depth leg as that file already does at depth 4. #assert_axioms-clean, sorry-free.
import Dregg2.Circuit.InjectiveFloorRegrounded -- ⚠ VACUITY-SWEEP FINDING-2, CLUSTER 1 RE-GROUNDED (07-17): the three highest-value injective-hash floor carriers moved off their free `FooCR` hypotheses onto REAL collision GAMES with an explicit `Eff`. Poseidon2WideCR (7 hypothesis uses; its docstring called it "the EXACT analogue of Poseidon2SpongeCR" — which was ALREADY proved false, and the conclusion did not travel), Compress8CR (a FIELD of Cap8Scheme, so a real deployed Cap8Scheme VALUE cannot exist), compress4Injective (§2.1 proves it FALSE at BabyBear — the class's third refutation, mechanical as the sweep predicted). ⛑ NOT the `CollisionResistant` treatment: FloorGames.hashCRHardQuant_top_false_of_compressing proves THAT pattern false too (it IS the floor at ⊤, and the def is DELETED 07-28), and hard_top_iff_solvableFrac_negl proves no restatement of the win relation escapes — the only honest escape is `Eff`. So each carrier gets: the deployed hash as a KEYED family carrying NO CR field (INHABITED by the real hash), the consumer's break as a first-class Game, a CONSTRUCTIVE extractor as a MAP OF ADVERSARIES (node8BreakToFinder=pack8; commit4Find=the h4 tree trace; wireCommit8Find=the chain walk chainCollFind — deliberately NOT Classical.choose of an existential, because CommitFaithfulRegrounded's `∃ collision` props are unconditionally TRUE at deployed params and so bind nothing), win-preservation, an advantage inequality, and the keystone bound whose hEff is UNDISCHARGED and in the open. BOTH poles proved per carrier (⊤ FALSE at deployed BabyBear, routed through the sweep's OWN teeth; ⊥ vacuous). Canaried 4 ways: each extractor MUTATED ⇒ its reduction REDS, and the fail_if_success gate fed the term it SHOULD accept ⇒ fails, so it discriminates. OLD consumers KEPT UNTOUCHED (additive); the three carriers now doc-mark BROKEN and point here. #assert_all_clean: 36 keystones, sorry-free.
import Dregg2.Tactics.ThreadAdvantageBound -- the `thread_advantage_bound` tactic: the MECHANICAL SWEEP re-threading old-floor-conditioned lemmas (HashCR/Poseidon2SpongeCR/compressNInjective/MSISHard — proved VACUOUS 07-13) onto the PROPER computational floors (CollisionResistant / the adversary-indexed *HardQuant). Each old-floor use becomes an ADDITIVE negligible advantage term; the tactic closes the resulting `Negl e` goal by structural recursion over the ConcreteSecurity closure algebra (negl_zero/negl_add/negl_const_mul/negl_mul_monomial/negl_finset_sum/negl_two_pow), pulling the floor from context at the leaves (CollisionResistant F applied to the equivocating opener; MSISHardQuantShape adv applied to the solver index). Covers the two commonest consumer SHAPES — (1) single-use equivocation binding (commitment_binding / commitmentOpening_binds_of_poseidon2CR / FinBindsKernel: "opens ⟹ equal" ↦ "equivocation advantage Negl") and (2) the summed multi-round FRI/STARK fold (friFold: total binding-failure advantage = ∑_{r∈rounds} per-round collision advantage, Negl by negl_finset_sum) — plus the MIXED tower shape (debatch-scale + round-sum + zero-leg). Prototyped on 5 restatements, all #assert_axioms-clean; two fail_if_success TEETH prove it REFUSES a non-negligible goal (constant 1) and a floor leaf with no floor in context — a real discharger, not sorry-in-costume. Does NOT synthesize the restatement STATEMENT (per-theorem-shaped), nor discharge negl_of_eventually_le domination / PolyBounded side-goals (real content, left to the caller).
import Dregg2.Circuit.FloorRegroundedConsumers -- the SECOND half of the 07-13 floor-fix, CIRCUIT side: re-grounds the STARK/FRI/commitment-binding consumers (OodCommitmentBinding, FriSoundness.oracle_binding, AirSoundness.committed_trace_pinned, FinBindsKernel root, the FRI/StarkSound fold, the AlgoStarkSound* tower, and the deployed apex lightclient_unfoolable_deployed_transferV3) off the VACUOUS injective floors (Poseidon2SpongeCR/HashCR, proved false by HashFloorHonesty) onto the keyed collision floor — which as of 07-28 is HashCRHardQuant F Eff with the class NAMED, the rung-1 def CollisionResistant (= that floor at ⊤, refuted for every compressing family) having been DELETED and its teeth restated at (fun _ => True) written out. Each consumer's Boolean "opens ⟹ equal" (which NEEDS injectivity, hence empty at real params) gains an ADVANTAGE-BOUNDED sibling: an equivocating opener IS a CollisionFinder (exactly the reduction each consumer's own *_breaks_cr tooth witnesses), so under CollisionResistant F its equivocation advantage is Negl — "opens ⟹ equal EXCEPT with negligible probability". Three SHAPES threaded by thread_advantage_bound: single-use Merkle opening (one collisionAdv leaf), the multi-round FRI/STARK fold (∑_{r∈rounds} per-round collisionAdv, Negl by negl_finset_sum/union bound), and the apex TWO-hash sum (trace-commitment + OOD-commitment equivocation, negl_add) plus the mixed AlgoStarkSound* tower (query-count-scaled de-batch + round-sum + hash-free zero leg). Old injective-floor consumers KEPT untouched; siblings ADDED. Non-vacuity: FloorGames.idFamily_hashCRHardQuant_top instantiates every sibling at a real floor witness, FloorGames.brokenFamily_not_hashCRHardQuant_top refutes the floor. #assert_all_clean (8 keystones). Does NOT touch the allocator-Phase-2 Emit/EffectVmEmitRotationV3 churn; the MSIS/DL/HashCR-Boolean floors are the FloorBridge sibling lane.
import Dregg2.Circuit.Poseidon2KeyedBridge -- the "bridge to the real object": connects the DEPLOYED UNKEYED Poseidon2 sponge (Poseidon2Binding's List ℤ → ℤ, the p3 babyBearD4W16 PaddingFreeSponge) to the KEYED CollisionResistant floor the FloorRegroundedConsumers rest on. Keying is LOAD-BEARING (an unkeyed fixed hash lets an adversary hardcode a known collision, advantage 1), but the deployed hash is a fixed unkeyed function — so the consumers rested on CollisionResistant F for an ABSTRACT F. THE STANDARD BRIDGE: the deployed sponge's effective key is its DOMAIN-SEPARATION tag (the per-use prefix absorbed ahead of the message that separates the Merkle-node/leaf/log/OOD hashes). DomainSeparatedSponge bundles the deployed sponge + its finite tag space + tag-absorption + the specific deployedTag; poseidon2KeyedFamily lifts it to HashFloorHonesty.KeyedHashFamily (H n t xs = sponge (tagCode t ++ xs)). FAITHFULNESS: deployed_hash_is_family_instance (the deployed fixed function IS the family instance at deployedTag, rfl) + wins_iff_deployed_collision (a keyed-game win at any tag is DEFINITIONALLY a collision of the deployed unkeyed function xs ↦ sponge(tagCode t ++ xs) — finding, not existence). NAMED FLOOR DomainSeparatedCR D := CollisionResistant (poseidon2KeyedFamily D), a GENUINE floor per prove-the-floor-false: SATISFIABLE (domainSeparatedCR_of_injective_sponge — a domain-separated family over an injective sponge is CR, since prefix-absorption is injective by List.append_right_injective; witnessed concretely by Reference.refSponge at the REAL babyBearD4W16 params, refDomainSep_CR) and REFUTABLE (brokenDomainSep_not_CR — a tag-ignoring constant sponge has advantage 1). Linked to the Poseidon2Binding object: domainSeparatedCR_of_poseidon2SpongeCR (the old injective Poseidon2SpongeCR — false at real params — is STRICTLY STRONGER, discharges the honest floor). CONNECTION: under DomainSeparatedCR D the FloorRegroundedConsumers advantage bounds fire at F := poseidon2KeyedFamily D — deployed_recStateCommit_advantage_bound (FinBindsKernel's full-state root binder), deployed_oodCommitmentOpening/friOracle/committedTrace/friStark_fold, and the apex deployed_lightclientUnfoolable_advantage_bound (two-hash forgery advantage) — so the deployed circuit's keyed-CR floor is BRIDGED to the real deployed hash, not an abstract F. #assert_axioms-clean (9 keystones).
import Dregg2.Circuit.LightClientFusion
import Dregg2.Circuit.S5Closure -- FUSES the two mature tracks into ONE deployed computational-soundness theorem (additive, new file). Instantiates the abstract carrier-generic reduction Crypto.LightClientUC.unfoolable_of_floor at the DEPLOYED circuit-soundness model: State := BatchPublicInputs, Proof := BatchProof, Witness := (ℤ→ℤ)×(ℤ→ℤ×ℕ)×List ℤ×VmTrace, verify := dVerify R (= decide (verifyBatch (vkOfRegistry R) pi π = accept)), Sat := dSat hash R (Satisfied2 ∧ tracePublishedCommit t = pi.toPublished), Produced := dProduced S kstep (the apex ∃-body StateDecode ∧ kstep ∧ commit-eqs). deployed_ExtractsTo ⟸ StarkSound.extract; deployed_SatBindsProduced ⟸ WitnessDecodes + descriptorRefines; deployed_unfoolable := unfoolable_of_floor … IS definitionally CircuitSoundness.lightclient_unfoolable, now factored through the abstract UC halves. deployed_fooling_breaks_floor/deployed_not_foolable = the fooling_breaks_floor contrapositive at the deployed types. Negl connection: foolingRootEquivocator D xs xs' (a state-equivocation packaged as a CollisionFinder (poseidon2KeyedFamily D)) has Negl advantage under DomainSeparatedCR D (deployed_recStateCommit_advantage_bound), with foolingRootEquivocator_wins_iff (wins ↔ deployed-sponge collision). NON-VACUITY: dProduced_not_vacuous — dProduced at kstep := False is refutable, so the kstep conjunct is load-bearing (not a laundered True). RESIDUAL (named, not forced): the finder-construction FROM an actual p2Commit root-equivocation (unwinding spongeCompress/sponge into the distinct preimage pair (xs, xs')) is a prose seam, not a Lean term — FinBindsKernel.recStateCommit_binds_kernel_fin proves only the positive forward direction; plus the definitional obligation to instantiate the commitment's bare sponge at D.deployedHash (deployed_hash_is_family_instance, rfl). #assert_axioms-clean (11 keystones).
import Dregg2.Crypto.FoQromRegrounded -- the FO/QROM half of the floor sweep: lifts the ML-KEM FO transform's IND-CCA consumers to the ASYMPTOTIC Negl floor with the QROM reprogramming leg grounded on the PROVED O2H bound (NOT the opaque 1/2^λ of KemSoundnessQuant). FoQrom already re-grounds the RO step onto OneWayToHiding.o2h_bound (a real q-query quantum adversary over the Mathlib QROM model) as a PER-INSTANCE absolute bound; this ADDS the concrete-security ensemble form. foQromIndCcaAdv = the four telescoped FO legs (simFail+corrSpread+reprogAdv+cpaAdv) as a λ-indexed ensemble; foQromIndCca_negl_of_legs: each leg Negl ⟹ composite Negl by thread_advantage_bound (the negl_add spine). reprogTermEnsemble q b = FoQrom.reprog_term_bound's 2·√(q·(q·b λ)) as an ensemble; o2h_advantage_le_reprogTerm PROVES it upper-bounds the actual quantum-adversary reprogramming advantage |amp_H−amp_{H'}| (via reprog_term_bound) — so it IS the O2H bound, dominating the real advantage. reprogTermEnsemble_negl_decay: at message-guessing bound b λ=(1/2^λ)² (growing min-entropy) and q=1 the reprogramming ensemble is exactly 2·(1/2^λ), Negl by negl_const_mul/negl_two_pow — the RO leg vanishes FROM the min-entropy. HEADLINE foQromIndCca_negl: the full FO IND-CCA advantage with the O2H-grounded reprogramming leg is Negl whenever the classical FO legs are. Load-bearing: foQrom_cpa_leg_load_bearing (a constant 2/5 IND-CPA leg makes the composite non-negligible — the O2H term cannot rescue a broken lattice floor). Old FoQrom/FoBookkeeping absolute-bound defs KEPT untouched; only the asymptotic Negl siblings ADDED. #assert_all_clean (6 keystones).
import Dregg2.Crypto.HermineHashCRRegrounded -- the COMMIT-REVEAL half of the floor sweep: re-grounds the Hermine HashCR binding consumers (commitment_binding, concurrent_forgery_breaks_hashcr_or_msis / concurrent_unforgeable_reduces, and the DEPLOYED-reachable RevocationSoundness/IdentityCommitment reuses of the SAME CommitReveal/HashCR) off the VACUOUS injective HashCR (proved FALSE for a compressing commitment by HashFloorHonesty.hashCR_false_of_compressing) onto the keyed collision floor — which as of 07-28 is HashCRHardQuant F Eff with the class NAMED, the rung-1 def CollisionResistant (= that floor at ⊤, refuted for every compressing family) having been DELETED and its teeth restated at (fun _ => True) written out. commitRevealFamily: a Hermine CommitReveal at a fixed index becomes a KeyedHashFamily (reveals=Input, commitment=Out, H _ _ w = cr.H i w); commitRevealFamily_CR_of_hashcr is the OLD⟹NEW bridge (injective HashCR discharges the ⊤-class collision floor via FloorGames.hashCRHardQuant_top_of_injective — strictly stronger, and empty for a compressing commit). hermine_commitment_binding_advantage_bound: the equivocating opener (two distinct reveals colliding — the reduction equivocation_breaks_hashcr witnesses) IS a CollisionFinder, Negl advantage under the floor (thread_advantage_bound) — "opens ⟹ equal" becomes "⟹ equal EXCEPT w/ negligible probability". hermine_concurrent_forgery_advantage_bound: the rushing composition's Boolean dichotomy ¬HashCR ∨ MSIS-solution becomes the ADDITIVE advantage collisionAdv+adv s, Negl under CollisionResistant ∧ MSISHardQuantShape (negl_add, a floor leaf each). Non-vacuity: exCR_family_CR satisfies the floor on the binding exCR; badCR_family_not_CR REFUTES it on the colliding badCR (the crEquivocator 7 8 wins on every key, advantage 1). Old injective-floor consumers KEPT untouched; siblings ADDED. Coordination: commit-reveal binding lane only — STARK/FRI is FloorRegroundedConsumers, MSIS/DL-Boolean is FloorBridge. #assert_all_clean (6 keystones).
import Dregg2.Crypto.HermineRomBinding -- ⚑⚑ THE HERMINE COMMIT-REVEAL D-DROP (07-24): HermineHintMLWE.commitment_binding and concurrent_unforgeable_reduces are DELETED (they consumed HashCR — pure injectivity, PROVED FALSE for every compressing commitment by hashCR_false_of_compressing — and the Boolean existence floor MSISHard); this module is their keyed-ROM successor. §1 crRomFamily Idx W = flatFamily (signer index = the domain-separation KEY, reveals = messages, ideal λ-bit digest) + crRomCarrier (encoding = identity in the payload, injective on the nose); commitReveal_binds_rom: every Q-query-bounded equivocator of one commitment has NEGLIGIBLE advantage — romCarrier_binds, hence keyedRom_hard (birthday bound, PROVED), NO floor hypothesis. Non-vacuity: the 0-query hardcoded-pair answerer (the exact shortCollAdv shape that refutes the IsPolyTime floor) is IN the class (crRom_class_inhabited_pos, POSITIVE advantage) and DEFANGED (crRom_constAnswer_defanged — negligible against the SAMPLED oracle where it wins with probability 1 against the fixed hash); a non-negligible forger is OUTSIDE the class (crRom_nonNegl_forger_excluded). §2 the CONCURRENT rushing composition at the sampled oracle: cfRomFamily instantiates HermineHashCRRegrounded.ConcurrentForgeryFamily with instance space = the oracle space and commit map = the oracle at the fixed signer tag; cfEquivRom_hard PROVES the equivocation horn's floor (Hard (cfEquivGame _) _ from keyedRom_hard — where the deleted export ASSUMED HashCR for the same horn); hermine_concurrent_forgery_rom = the rushing bound with the equivocation horn on the PROVED floor and the MSIS horn honestly parametric at MSISHardQuant. §3 canary (the floor at ANOTHER adversary does not close the bound, fail_if_success) + crRom_fires (closed instance Fin 3 × Fin 5, end-to-end). ⚑ The ROM idealisation is the labelled modelling step of RomCarrierSites' header. NB the HermineHashCRRegrounded line above predates this drop: its "old injective-floor consumers KEPT untouched" no longer holds — the two D exports are gone, equivocation_breaks_hashcr survives as the extractor witness only. #assert_all_clean (9 keystones).
import Dregg2.Circuit.StateCommitFloorRegrounded -- FINDING-2 sweep, COMMIT-STATE lane (07-17): the two StateCommit injective floors the 07-13 fix left behind — cellLeafInjective (:230) and logHashInjective (:238), in the SAME FILE as two already-flagged siblings — proved FALSE at deployed params and their consumers RE-GROUNDED onto a real collision-game reduction with an explicit Eff. (§1 TEETH) Value is INFINITE (Value.int injects ℤ) and List Turn is INFINITE, so a BabyBear-bounded leaf/log hash refutes both by the counting core: cellLeafInjective_false_babyBear, logHashInjective_false_babyBear. ⚑ WORSE THAN A HYPOTHESIS: Poseidon2Binding.LeafRealization/LogRealization carry Poseidon2SpongeCR as a FIELD, already proved false at BabyBear — so leafRealization_uninhabitable_babyBear / logRealization_uninhabitable_babyBear prove a deployed realization VALUE CANNOT EXIST (the Compress8CR-in-Cap8Scheme shape the sweep called the priority); every discharge through them (incl. Verify/KeystoneAuditArgusReceipt.LH₀_inj) is satisfied only by the TOY Encodable.encode sponge — the FALSE COMFORT HashFloorHonesty's own header named. ⚑ THE RE-GROUNDING DOES NOT GO THROUGH CollisionResistant: FloorGames.hashCRHardQuant_top_false_of_compressing proves that floor is ITSELF false at deployed params (it IS HashCRHardQuant _ ⊤; the def is DELETED 07-28), so the only honest escape is the Eff parameter (FloorGames §8) and this file takes it, as HermineHashCRRegrounded does. (§3-§5 THE REDUCTION) scEquivGame = the deployed break of StateCommit.cellDigest_binds_cells (two AccountsWF kernels, same accounts, DIFFERENT cell maps, EQUAL cellDigest — read off the real cellDigest, scCellDigest_eq by rfl); four maps of adversaries into three collision games over ONE sampled instance space — scToOuterNodeAdv (root node), scToMovedNodeAdv (movedDigest node), scToSpongeAdv (frame sponge), scToLeafAdv (the LEAF, at diffCell — the horn cellLeafInjective carried for free); sc_claim_wins_imp is cellDigest_binds_cells CONTRAPOSED into an extractor over the exhaustive {src}∪{dst}∪(accounts\{src,dst})∪dead partition (dead closed by AccountsWF on BOTH states); sc_adv_le is the 4-horn union bound (winProb_le_add4_of_imp, off Hermine's winProb_le_add_of_imp — REUSED not duplicated); stateCommit_equivocation_advantage_bound is the keystone under HashCRHardQuant _ Eff. §6 CANARY: the bound does NOT follow from the floors at OTHER finders (fail_if_success) + the positive pole fires — MUTATION-TESTED (deleting sc_adv_le from the keystone REDS it). §5b the LOG side, honestly SMALLER: its break IS definitionally a collision (no tree, no dichotomy to build), so the content is existence→advantage — log_equivocation_advantage_bound (⚑ scLogFamily_CR_of_logHashInjective, "strictly stronger AND unsatisfiable", DELETED 07-28 — refuted floor to refuted floor). §7 both poles PROVED per floor (⊤ false at compressing params, ⊥ vacuous). ⚑ §8 the usual 'old floor ⟹ new floor' note is FALSE for the leaf: cellLeafInjective is PER-CELL and does not cover its family's cross-cell collisions — the reduction stays on the diagonal (scToLeafAdv_diagonal), stated rather than papered over. valueDecEq is COMPUTABLE (off the proven-injective Poseidon2Binding.Reference.encV) — no Classical.decEq in the counting. Old consumers KEPT untouched; siblings ADDED. #assert_all_clean (31 keystones).
import Dregg2.Circuit.LogCommitRegrounded -- ⚑ VACUITY CAMPAIGN, `logHashInjective` LANE (07-25): the CORE of the log-family port — the TOTAL EXTRACTOR (`logHashFind` + `logHashFind_spec`) that a re-grounding of the receipt-chain floor actually needs, plus the S3 form (`logHash_binds_of_noLogColl`) the whole 329-carrier cone re-points onto. WHY AN EXTRACTOR IS THE IRREDUCIBLE WORK: `hLog xs ys h : xs = ys` ELIMINATES the injectivity — it DISCARDS the equivocation branch — so the old proof cannot be replayed; a reduction must CONSTRUCT the colliding pair. `logHashFind` does, totally and decidably, and TAGS which deployed object broke: `.inl` = a genuine Poseidon2 sponge collision, `.inr` = a genuine collision of the canonical serialization (a SERIALIZATION BUG, not a crypto assumption). ⚑ Its spec is stated over the BARE FACTORING `∀ zs, LH zs = sponge (enc zs)` — an equation the real deployed hash satisfies — and deliberately NOT over `Poseidon2Binding.LogRealization`, whose `spongeCR` field is already refuted at BabyBear (`logRealization_uninhabitable_babyBear`): a spec quantified over an uninhabitable bundle would BE the vacuity this campaign removes. Deliberately LIGHT (`StateCommit` + `Tactics` only) because `EffectCommit`/`SetFieldCommit`/`CircuitSoundness` import it. Teeth: the side condition is REFUTABLE (fires at a constant accumulator), SATISFIABLE (does not fire at deployed-shaped named points), LOAD-BEARING (`logHash_binds_unconditional_false` — the S3 statement with `hno` deleted is REFUTED), the port BITES (`canary_tamper_moves_logDigest` forces a tampered chain to move the digest with no injectivity floor in the argument), and the extractor reaches BOTH horns (`logHashFind_hits_sponge_horn`). #assert_all_clean (12 keystones).
import Dregg2.Circuit.LogCommitRegroundedRom -- the GAME-THEORETIC half of the same lane, split out so the core stays importable by the tree's spine: the hypothesis-free S2 disjunction (`logHash_binds_or_collides`), its advantage bound, and the keyed-ROM headline `logHash_binds_rom` (birthday bound via `flatSite_binds` — a THEOREM, not a carried floor; ⚑ the digest space is the λ-growing `Fin (2 ^ l)`, which is NEVER the deployed felt, and that modelling step is said out loud rather than smuggled). Both poles proved: the ROM game is WINNABLE (`logRom_pole_pos`) and the winning shape is DEFANGED (`logRom_pole_defanged`), not excluded. ⚑ `someLogColl_of_babyBear` states the port's honest scope as a theorem — at the deployed width SOME pair of chains DOES collide, which is exactly why the universal floor is refuted; what the port buys is that each theorem now names the ONE pair its conclusion depends on. `logColl_width_note` is the 8-FELT tripwire (`card R = p^8 ≈ 2^247` → birthday 2^123.5 is the TARGET; one felt → 2^15.5 is a BREAK). #assert_all_clean (9 keystones).
import Dregg2.Circuit.LogCommitCutoverCheck -- the SEMANTIC TEETH of the `logHashInjective` CUTOVER (16 endpoints cut over IN PLACE — same names, same conclusions, floor binder GONE, per-instance `¬ LogColl` at the EXACT pair the old proof fed the floor): the v1 generic pair (`EffectCommit`), the v2/dual/triple/quad/quint pairs, the concrete `SetFieldCommit` pair, and the two log-seam theorems in `CircuitSoundness`. Two independent teeth per theorem: an INDEPENDENTLY WRITTEN `example : ⟨intended S3 type⟩ := @original` accepted only by KERNEL DEFEQ (a silent weakening, a dropped hypothesis, a binder reorder, or a floor resurrection goes RED here), and `#assert_not_depends_on … [logHashInjective]` (the refuted floor is UNREACHABLE from the proof). Plus NO-STRENGTH-LOST examples deriving each OLD statement through the `noLogColl_of_inj` bridge — so the bar holds in both directions: the new hypothesis is IMPLIED BY the old one (each ported theorem is logically STRONGER) and the conclusions are unchanged. `CommitmentCrossBind.setFieldCommit_binds_all` is deliberately ABSENT: a multi-floor site under live co-tenant edit, refused rather than swept.
import Dregg2.Tools.ConePortLogHashRun -- ⚙ THE PINNED MEASUREMENT that decides the next wave of the `logHashInjective` lane: ConePort re-runs the ELABORATED-TERM analysis of every direct consumer of the 16 cut-over endpoints on EVERY BUILD and checks each site against its pinned outcome. MEASURED 2026-07-25: **48 PORTED / 48 eligible, 0 refusals, 0 unexplained** — each port one bridge application (`sites=1, sideConds=1`), surgered on the elaborated term, `Meta.check`ed, kernel-`addDecl`ed and delaboration-ROUND-TRIPPED before it counts. So the remaining 313 `logHashInjective` carriers are a CODEMOD problem, not 313 re-proofs — and that is a machine-checked fact, not an estimate. ONE `AppRule` covers the whole level because the endpoint cutover routed every still-floored consumer's floor through the single bridge `noLogColl_of_inj`; that uniformity was the point of the bridge. The 2 pinned REFUSALS were `CircuitSoundness.turnDecodeChainLog_seam_log_derived`/`_rejects_forged_log`, which carried the floor as a STRUCTURE FIELD (`TurnDecodeChainLog.hLog`), not a telescope binder — the field-bundle class, where a field has no argument position to be per-instance AT, so the port unit is the BUNDLE (a design decision, not a rewrite). ⚑ THAT BUNDLE IS NOW CUT OVER (07-25, `Circuit/TurnDecodeChainLogBundleCutoverCheck`): the field is DELETED, both theorems take `NoLogSeamColl` at exactly the chain adjacencies, and both pins STAY `.refuse "not a carrier"` — now because the floor is gone from their types AND out of their proof closure, not because it sat where the tool could not reach. ⚑ NOT a cutover and NOT additive: the ports live only in this module's environment as the measurement's evidence; no successor theorem is added beside a surviving vacuous original. Drift in either direction (a pinned-portable site refusing, a pinned refusal porting, or a refusal for the wrong REASON) is a BUILD ERROR.
import Dregg2.Circuit.StateCommitLeafRegrounded -- ⚙ THE `cellLeafInjective`/`compressInjective` ENDPOINT PORT (07-25, vacuity-elimination campaign): the S2/S3 re-grounding of StateCommit's two leaf/node binding endpoints, in the ListCommitRegrounded shape. Both floors are FALSE at deployed BabyBear params (cellLeafInjective_false_babyBear: Value is INFINITE and a real Poseidon2 leaf lands in ONE felt; compressInjective_false_of_finite_range: two field elements do not fit in one), so every theorem threading them was conditional on a refuted premise. S2 (movedDigest_binds_or_collides / frameDigest_binds_or_collides / compress_binds_or_collides / cellLeaf_binds_or_collides) is HYPOTHESIS-FREE: equal digests force equal cells OR a NAMED collision, and the extractors are TOTAL — CompressColl.extracts / CellLeafColl.extracts / MovedColl.extracts / FrameColl.extracts RETURN the specific colliding pair on an equivocation, which is exactly what the old proofs never built (injectivity DISCARDS a case; a reduction must PRODUCE the witness). S3 (*_binds_of_no*Coll) restores each old CONCLUSION byte-identically with the refuted universal swapped for a per-instance, refutable ¬Coll at the EXACT arguments the proof passed. NO STRENGTH LOST: *_of_carriers re-derives the old endpoints through the port (old floor ⟹ every side condition, via the no*Coll_of_inj bridges). ⚑ HONEST-BUT-CONDITIONAL, never universal: at the deployed ~31-bit felt the side conditions are ~2^15.5-breakable claims about NAMED points (the felt-width wound), and they get STRONGER as the digest is repaired toward the 8-felt ~2^123.5 target — nothing here instantiates a width. TEETH: the predicates FIRE at a constant hash (compressColl_refutable / cellLeafColl_refutable / movedColl_refutable) AND are satisfiable at a deployed-shaped instance (noCompressColl_satisfiable), the side conditions are LOAD-BEARING (compress_binds_unconditional_false / movedDigest_unconditional_false — the S3 statements with hno DELETED are REFUTED), and the left disjunct BITES (canary_moved_tamper_moves_digest_or_collides: at named points with no collision available S2 forces the digests apart, no injectivity floor in the argument). #assert_all_clean (31 keystones).
import Dregg2.Circuit.StateCommitLeafCutoverCheck -- ⚙ THE CUTOVER TEETH for the 07-25 cellLeafInjective cutover (13 theorems: 6 in CommitmentCrossBind, 7 in Argus/Receipt, floor binder REMOVED IN PLACE, per-instance side conditions in its place). Two independent build-time gates. (1) NO STRENGTH LOST: seven *_of_injective bridges whose types are INFERRED from the term, so the type each states IS the pre-cutover statement — old carrier telescope in, old conclusion out, routed through the port; a silently WEAKENED conclusion stops elaborating. (2) NO FLOOR RESURRECTION: #assert_not_depends_on pins every one of the 13 off cellLeafInjective AND off both floor-carrying endpoints (StateCommit.FrameDigestBindsCells / MovedDigestBindsCells) — re-adding the binder, or routing back through an old endpoint, is a BUILD ERROR here. RULER: tier-A cellLeafInjective 316 → 300 on this cutover (measured, own runs, same tree).
import Dregg2.Crypto.FactoryBindingFloorRegrounded -- FINDING-2 sweep, FACTORY/BINDING lane (07-17): three more injective floors the 07-13 fix left behind — CommitmentBinding.Compress1CR (:52), Exec.Factory.HashInjective (:75), Authority.MacaroonDischarge.BindingHashCR (:171). (§1a) compress1CR_false_babyBear (List ℤ infinite, one bounded squeeze) and ⚑ THE 2026-07-25 BUNDLE CUTOVER: Compress1CR WAS a FIELD of Compress2, so a deployed Compress2 VALUE COULD NOT EXIST — CommitmentBinding's own headline ('the StateCommit root-binding portal thereby stands on the permutation CR, NOT a separate assumption') stood on nothing at BabyBear, satisfied only by Reference.refCompress2 (compress1 = Encodable.encode, range NOT bounded). The field is now DELETED; compress2_uninhabitable_babyBear (which projected it) is REPLACED by deployedCompress2_compress1_not_Compress1CR — the constructed deployed inhabitant's OWN compression refutes the deleted field, i.e. the very function the teeth refute now INHABITS the structure — plus compress1CR_field_uninhabitable_babyBear, the general falsifier kept alive for the next bundle in this shape. Teeth: Circuit.BundleCutoverCheck. (§1b) hashInjective_false_blake3: the published content Schema × RecordProgram is INFINITE while factory_vk is BLAKE3 (256-bit), so Factory.vk_determines_invariants / vk_determines_program — the formal content of CONSTRUCTOR TRANSPARENCY — are VACUOUSLY TRUE at deployed params. ⚑ (§1c) BindingHashCR is the ONE carrier here that is NOT refuted at deployed params, and the sweep's blanket 'FALSE-AS-NAMED' is WRONG about it: crypto::binding_hash is SHA-256 of a 32-byte tail into a 32-byte body, |Tag|=|Bytes|, the map does NOT compress and pigeonhole has nothing to say — injectivity there is CONSISTENT. So the teeth are stated at their real scope (bindingHashCR_false_of_compressing / _of_infinite_tag, the latter firing at the model's own unbounded generic Tag) and NOT rounded up to a refutation; the defect that IS present is the wrong SHAPE (adversary-independent EXISTENCE where the consumer needs computational FINDING — which MacaroonDischarge's §5 PORTAL note already CLAIMS it carries). (§2-§4 THE REDUCTIONS, each contraposing the deployed theorem into an extractor, each canaried + MUTATION-TESTED) nodeToCompress1Adv: a 2-to-1 node collision becomes a compress1 collision by the INJECTIVE packing (compressInjective_of_compress2 contraposed) → compress2_node_advantage_bound, NO new crypto (still the ONE permutation call, primitive #4, now stated as something to FIND). forgerToFactoryHashAdv: two WELL-FORMED descriptors sharing a vk with different contracts — well-formedness turns the vk equality into a HASH equality, which IS the reduction (vk_determines_invariants contraposed) → vk_determines_invariants_advantage_bound. rebinderToBindingAdv: one discharge, two DISTINCT parent tails, ONE replay; hmacInj (the deployed theorem's OWN carried premise) peels the final MAC step leaving bindingHash p = bindingHash p' (rebinding_changes_replay contraposed) → rebinding_advantage_bound. All three condition on FloorGames.HashCRHardQuant _ Eff at an EXPLICIT class, NOT HashFloorHonesty.CollisionResistant (= that floor at ⊤, itself false — FloorGames.hashCRHardQuant_top_false_of_compressing). §5 both poles PROVED per floor (⊤ false at compressing params, ⊥ vacuous), with the binding floor's ⊤ pole recorded as CONDITIONAL per §1c. Old consumers KEPT untouched; siblings ADDED. #assert_all_clean (27 keystones).
import Dregg2.Crypto.IdentityCommitmentRegrounded -- WIRES the DEPLOYED id-re-basing gate onto the proper floor: HermineHashCRRegrounded's prose names IdentityCommitment as a "DEPLOYED-reachable reuse" but ships only the GENERIC commit-reveal keystone — no theorem instantiated it for the id-commitment's own cr, so the deployed verify_committed_ml_dsa binding (IdentityCommitment.id_commitment_binds, cell-crypto/captp/wire) STILL rode the injective HashCR (VACUOUS: hybrid_id_commitment = BLAKE3(tag, ed ‖ len(ml) ‖ ml) is compressing, so HashCR is false by hashCR_false_of_compressing). This file closes that wiring gap. idCommitFamily cr = commitRevealFamily cr () (the deployed id-commit hash as a KeyedHashFamily); id_commitment_binds_advantage_bound: an enrollment-equivocation adversary (two distinct key pairs ⟹ two distinct length-framed preimages by commit_collision_is_hash_collision ⟹ a hash collision) IS a CollisionFinder, Negl advantage under HashCRHardQuant (idCommitFamily cr) ⊤ via the generic keystone + thread_advantage_bound — "one id ⟹ one key pair" becomes "⟹ one key pair EXCEPT w/ negligible probability". Non-vacuity on the id-commitment itself: idCommit_exCR_CR satisfies the floor on the injective exCR; idCommit_badCR_not_CR REFUTES it on the colliding badCR (crEquivocator [1] [2] wins on every key, advantage 1 — mirrors IdentityCommitment.binding_needs_hashcr). Old injective-floor gate KEPT untouched; sibling ADDED. #assert_all_clean (4 keystones).
import Dregg2.Crypto.WireAkeRegrounded -- the forward-scaffolding floor sweep, CLOSEST-TO-DEPLOYED leg: re-grounds the wire-handshake CHANNEL BINDING (WireAke.channel_binding/channel_binding_transcript, the deployed peer-auth no-UKS/no-key-reuse floor, wire/src/server.rs PeerAuthResponse) off the VACUOUS injective HashCR (the concat-KDF session key sessionKey cr frameK = cr.H () (frameK (ss_x,ss_pq,tr)) is compressing, so HashCR is FALSE by hashCR_false_of_compressing) onto the keyed collision floor — which as of 07-28 is HashCRHardQuant F Eff with the class NAMED, the rung-1 def CollisionResistant (= that floor at ⊤, refuted for every compressing family) having been DELETED and its teeth restated at (fun _ => True) written out. channelKeyFamily cr = commitRevealFamily cr () (the session-key hash as a KeyedHashFamily; sessionKey_eq_family pins it definitionally to WireAke.sessionKey); channel_binding_advantage_bound: a UKS/key-reuse adversary (two distinct framed inputs ⟹ two distinct (ss_x,ss_pq,tr) triples by uks_breaks_hashcr ⟹ a hash collision) IS a CollisionFinder, Negl advantage under CollisionResistant (channelKeyFamily cr) via the generic keystone + thread_advantage_bound — "equal session key ⟹ equal transcript" becomes "⟹ equal transcript EXCEPT w/ negligible probability". Non-vacuity: channelKey_crK_CR satisfies the floor on the injective transcript-binding crK; channelKey_badCR_not_CR REFUTES it on a colliding transcript-blind session-key hash (crEquivocator 7 8 wins on every key, advantage 1). ake_authentication/ake_session_key_secure NOT touched (already on the standard floor). Old injective-floor theorems KEPT; sibling ADDED. #assert_all_clean (5 keystones).
import Dregg2.Crypto.RandomnessBeaconRegrounded -- the beacon leg: re-grounds the post-quantum randomness-beacon safety floors — the ABSTRACT model (RandomnessBeacon.unbiasable_of_hashcr / prediction_matching_two_reveals_breaks_hashcr / commit_binds_contribution) AND the DEPLOYED crypto-hashrand refinement (HashRandRefinement.hashrand_unbiasable / hashrand_commit_binds) — off the VACUOUS injective HashCR (the combine H("output", sorted[(i,cᵢ)]) and commit H(i,cᵢ) are compressing) onto the keyed collision floor — which as of 07-28 is HashCRHardQuant F Eff with the class NAMED, the rung-1 def CollisionResistant (= that floor at ⊤, refuted for every compressing family) having been DELETED and its teeth restated at (fun _ => True) written out. beaconHashFamily cr i / hashRandCommitFamily X / hashRandOutputFamily X = commitRevealFamily at the beacon's combine/commit/output hashes; beacon_binding_advantage_bound + hashrand_commit_binding_advantage_bound + hashrand_output_binding_advantage_bound: a bias/early-prediction/equivocating-committer adversary (two distinct reveals colliding to one output/leaf, a hash collision by bias_breaks_honest_slot_cr) IS a CollisionFinder, Negl advantage under the floor (thread_advantage_bound) — "distinct honest reveal ⟹ distinct output" becomes "⟹ distinct output EXCEPT w/ negligible probability" (UNBIASABILITY) and "opens ⟹ equal EXCEPT w/ negligible probability" (UNPREDICTABILITY). Non-vacuity: satisfiable on the injective exBeaconHash/goodCR (both roles), load-bearing on the colliding badBeaconOut/badX.cr (crEquivocator wins on every key, advantage 1). Old injective-floor consumers KEPT; siblings ADDED. #assert_all_clean (9 keystones).
import Dregg2.Crypto.XmVrfRefinementRegrounded -- the PQ sortition-VRF leg: re-grounds the DEPLOYED crypto-xmvrf leader-sortition UNIQUENESS (XmVrfRefinement.xm_unique / merkle_leaf_binding / leaf_unique, at most one output verifies per (pk,epoch) — no double-claimed committee seat) off the VACUOUS injective HashCR (the BLAKE3 leaf/node hashes H(Role.leaf/node, ·) are compressing) onto the keyed collision floor — which as of 07-28 is HashCRHardQuant F Eff with the class NAMED, the rung-1 def CollisionResistant (= that floor at ⊤, refuted for every compressing family) having been DELETED and its teeth restated at (fun _ => True) written out. xmVrfLeafFamily X / xmVrfNodeFamily X = commitRevealFamily X.cr Role.leaf/node; xm_leaf_uniqueness_advantage_bound + xm_node_binding_advantage_bound: a uniqueness-breaking adversary (two distinct verifying outputs colliding at one leaf, or two child pairs at one node — a hash collision by distinct_outputs_break_hashcr) IS a CollisionFinder, Negl advantage under the floor (thread_advantage_bound) — "two verifying outputs ⟹ equal" becomes "⟹ equal EXCEPT w/ negligible probability" (a validator double-claims a seat only with negligible advantage). Non-vacuity: satisfiable on the injective goodX.cr (both roles), load-bearing on a colliding node hash (crEquivocator [1] [2] wins on every key, advantage 1 — mirrors naive_not_merkle_backed). Pseudorandom sortition-fairness NOT touched (floor = the PRG). Old injective-floor consumers KEPT; siblings ADDED. #assert_all_clean (6 keystones).
import Dregg2.Crypto.HashSigMerkleRegrounded -- the hash-signature sponge leg (SAME abstract-family shape as FloorRegroundedConsumers): re-grounds the SLH-DSA/SPHINCS+ MANY-TIME hash-signature key-swap BINDING (HashSigMerkle.merkle_ots_binds_index — a verifying signature at index i carries EXACTLY the OTS pubkey committed at i, so a forgery is a genuine Lamport forgery on the committed key) off the VACUOUS injective Poseidon2SpongeCR (a compressing p3 Poseidon2 sponge has collisions by pigeonhole, FALSE by poseidon2SpongeCR_false_babyBear) onto the keyed collision floor — which as of 07-28 is HashCRHardQuant F Eff with the class NAMED, the rung-1 def CollisionResistant (= that floor at ⊤, refuted for every compressing family) having been DELETED and its teeth restated at (fun _ => True) written out. merkle_ots_binds_advantage_bound: a leaf-swap-equivocation adversary (two distinct OTS pubkeys hashing through pkEncode/pkLeaf to one committed Merkle leaf — a sponge collision, exactly the collision merkle_ots_binds_index rules out) IS a CollisionFinder on the keyed sponge family F, Negl advantage under CollisionResistant F (thread_advantage_bound) — "verifying signature ⟹ committed key" becomes "⟹ committed key EXCEPT w/ negligible probability". Non-vacuity: FloorGames.idFamily_hashCRHardQuant_top instantiates the sibling (merkle_ots_binds_fires), FloorGames.brokenFamily_not_hashCRHardQuant_top refutes the floor (a broken sponge admits a key swap). Old injective-floor consumer KEPT; sibling ADDED. #assert_all_clean (2 keystones).
import Dregg2.Crypto.VrfRegrounded -- the lattice-VRF UNIQUENESS keystone, CARRYING its reduction (REWRITTEN 07-16 by the *HardQuant repair; see docs/deos/VACUITY-SWEEP.md Finding 1). A validator double-claiming a committee seat = an adversary producing two DISTINCT short outputs y₁≠y₂ with short proofs both verifying against one public key and one commitment: vrfUniqGame makes that a first-class Adversary over a λ-indexed sampled LatticeVrfFamily, its win relation IS VRF.latticeVerify twice over. THE REDUCTION IS NOW IN THE STATEMENT — uniqBreakToMsisSolver is the extractor (z₁−z₂, −(y₁−y₂)) as a FUNCTION; uniqBreak_reduces_to_msis is VRF.lattice_vrf_uniqueness_reduces_to_msis APPLIED (win-preservation); uniqBreak_adv_le_msis_adv is the advantage inequality by winProb_le_of_imp over the shared instance space; lattice_vrf_uniqueness_advantage_bound applies FloorGames.MSISHardQuant (vrfMsisFamily F) Eff — the Module-SIS game at the AUGMENTED map [A|t], with IsMSISSolution in the win relation — to the EXTRACTED solver and dominates down. WHAT IT REPLACES: the old statement was (adv)(s)(hfloor : MSISHardQuant adv) : Negl (adv s) = `hfloor s`, a P→P instantiation that the sweep discharged by passing the POSEIDON2 COLLISION floor into the MSIS slot — the reduction was cited in the header and absent from the theorem. §2 CANARY: fail_if_success proves the keystone does NOT follow from the floor alone (hypothesis and conclusion are now about DIFFERENT games) — a tooth unwritable under the old statement, where both were the same Negl (adv s). THE COST, NOT HIDDEN: Eff is a PARAMETER (no cost model in-tree, FloorGames §8) with both poles PROVED — lattice_vrf_floor_top_false_of_compressing (Eff:=⊤ ⇒ FALSE at compressing params, which is WHY MSIS is a hard search problem) and lattice_vrf_floor_satisfiable_vacuously (Eff:=⊥ ⇒ vacuous); the keystone's hEff side condition (`the extracted solver is in the class` = the standard `the reduction is efficient`) is UNDISCHARGED and named at the use site. Boolean-floor consumer VRF.lattice_vrf_unique_under_msis KEPT untouched. #assert_all_clean (6 keystones).
import Dregg2.Apps.PreRotationKeySetRegrounded -- ⚑ VACUITY-SWEEP FINDING 2, cluster 3/3 (07-17) — the KeySetCR site. §1 PROVES the carrier FALSE AS NAMED: PreRotation.KeySetCR hash := ∀ a b : List Key, hash a = hash b → a = b IS injectivity on the INFINITE List Key, while the deployed key-set digest lands in a BOUNDED range — keySetCR_false_blake3 (256-bit) / keySetCR_false_babyBear refute it by the HashFloorHonesty counting core, so all SIX consumers (rotate_compromise_resistant, rotate_install_unique, rotChain_pinned_by_commitments, rotateStepCooled_compromise_resistant, preimage_blocks_cooled_rotation, rotateWrite_compromise_resistant) are VACUOUSLY TRUE at real parameters; the file's own demoHash_CR "non-vacuity" witness is a TOY INJECTIVE Encodable pairing into all of ℤ — toy satisfiable, real hash false, exactly the FALSE COMFORT HashFloorHonesty's header names. §2 keySetFamily lifts the deployed digest to a KeyedHashFamily keyed by its domain-separation tag (deployed_hash_is_family_instance = rfl; ⚑ keySetFamily_CR_of_keySetCR, the old⟹new bridge, is DELETED 07-28 — it implied a refuted floor from a refuted floor, so it was true and empty; §7's priced poles and §8's keyed-ROM successor carry the weight). §3-§4 THE REDUCTION IS IN THE STATEMENT: rotationForgeryGame's win relation is the REAL deployed verb (rotateStep … = some _, i.e. ADMITTED) exhibiting a NON-committed key set; forgeryToCollisionFinder is the extractor; forgery_wins_imp routes through PreRotation.rotate_exhibits_preimage (the verb's own guard — an admitted forgery EXHIBITS hash newKeys = hash committed with newKeys ≠ committed, a genuine collision); forgery_adv_le is the advantage inequality by winProb_le_of_imp. §5 rotate_compromise_resistant_advantage_bound / rotate_install_unique_advantage_bound: the Boolean "any other key set is REFUSED" becomes "refused EXCEPT with negligible probability" — from Hard (keySetCollisionGame D) Eff VIA the reduction. §6 CANARY (fail_if_success: the keystone does NOT follow from the floor at some OTHER finder — MUTATION-TESTED: feeding it the real bound goes RED, so it discriminates) + the positive pole. §7 ⚑ THE Eff IS THE WHOLE HONESTY AND IS UNDISCHARGED (no cost model — FloorGames §8), PRICED at both poles: keySet_floor_top_false_of_compressing / _blake3 (⊤ ⇒ FALSE at the deployed digest, via FloorGames.not_hard_top_of_always_solvable fed by the counting core) and keySet_floor_bot_vacuous (⊥ ⇒ vacuous); brokenKeySet_floor_top_false refutes it on a constant digest. Old consumers KEPT untouched; siblings ADDED. #assert_all_clean (19 keystones).
import Dregg2.Apps.QueueRootFloorRegrounded -- ⚑ VACUITY-SWEEP FINDING 2, cluster 3/3 (07-17) — the Apps/QueueRoot cluster (RootCR/LeafCR/PairCR/LenBindCR, the biggest by consumer count). All four are injectivity of a compressing map into a bounded 256-bit BLAKE3 digest (blake3_binary_root over hash_entry), so all are FALSE at deployed parameters and every consumer (dequeue_proof_pins, dequeue_forgery_refused, stale_proof_refused, dequeue_proof_unique, queueDequeueProven_pins_root_transition/_refuses_forgery/_refuses_stale, tagged_dequeue_proof_pins) is VACUOUS there. ⚑ THE ZERO-FREE RESTRICTION IS RESPECTED, NOT DODGED — this is the delicate part: RootCR does NOT claim full injectivity (QueueRoot itself proves that false via refRoot_pad_alias — padding is indistinguishable from a zero leaf), so a false-proof that re-exhibited the padding alias would MISS the carrier. §1a refutes RootCR INSIDE its own restricted domain: zfRep n := List.replicate (n+1) 1 is an INFINITE family of pairwise-distinct ZERO-FREE lists (zfRep_zeroFree/zfRep_injective), so a range-bounded root collides on two of THEM (exists_zeroFree_collision_of_finite_range → rootCR_false_of_finite_range / rootCR_false_blake3). The restriction buys nothing against counting. §1b LeafCR's falsity has a DIFFERENT shape and the file says so: its domain is the FINITE 88-byte entry preimage, so the counting core does not apply and the honest refutation is pigeonhole on cardinalities (leafCR_false_of_compressing, 2²⁵⁶ < 2⁷⁰⁴ — the hashCR_false_of_compressing shape); leafCR_false_of_finite_range is the infinite-Entry variant. §3-§4 A GENUINE DICHOTOMY WITH A UNION BOUND (the HermineHashCRRegrounded concurrent-forgery pattern, not a single-leaf bound): dequeueForgeryGame's win relation is the REAL Rust-verbatim verifyDequeue accepting against the LIVE pre-root while claiming a different entry/remaining; forgery_wins_imp proves EITHER the two committed windows are DISTINCT zero-free lists with one root (a real ROOT collision, inside RootCR's own domain — both zero-free DERIVED, the live one because it is a leaf image and leaves are nonzero) OR they are equal, forcing remaining = rest by cons-injectivity, so the claim differs in the ENTRY while the leaves collide (a real LEAF collision); forgery_adv_le is the UNION BOUND over the shared instance space (winProb_le_add_of_imp, reused from HermineHashCRRegrounded rather than duplicated). §5 dequeue_proof_pins_advantage_bound / queueDequeueProven_pins_root_transition_advantage_bound / dequeue_proof_unique_advantage_bound: the Boolean pins become the additive negligible advantage from BOTH floors VIA the reduction. §6 CANARY (MUTATION-TESTED RED on the real bound) + positive pole. §7 both Eff poles PRICED per floor (root_floor_top_false_of_compressing/_blake3, leaf_floor_top_false_of_compressing, *_bot_vacuous, brokenQueue_root_floor_top_false). §8 PairCR/LenBindCR FALSE-PROVED (pairCR_false_blake3, lenBindCR_false_blake3) — they carry the PROPOSED §7 level-tagged hardening, which Rust has NOT adopted (a wire-affecting root-format migration), so they are refuted honestly and their game-re-grounding path is stated rather than built; ⚑ tagged_carriers_false_at_bounded_root proves THE HARDENING DOES NOT ESCAPE THE FINDING — via QueueRoot.taggedRoot_RootCR, at a bounded digest the hardening's carrier conjunction is itself FALSE. The upgrade fixes the PADDING ALIAS (structural, and it really does); it cannot fix the counting bug. Only Eff does. Old consumers KEPT; siblings ADDED. #assert_all_clean (32 keystones).
import Dregg2.Circuit.CouncilRosterRegrounded -- ⚑ VACUITY-SWEEP FINDING 2, cluster 3/3 (07-17) — the RosterCR site (CouncilCommit:161, the PreRotation.KeySetCR shape). §1 rosterCR_false_of_finite_range / _blake3 / _babyBear: injectivity of the guardian-roster digest on the INFINITE List Guardian into a bounded range is FALSE by the counting core, so recStateCommit_recovers_council_roster — the LIGHT-CLIENT PAYOFF that a verified root recovers the LITERAL K-of-N guardian set — is VACUOUS at deployed parameters. §2 rosterFamily (keyed by the domain-separation tag) + deployed_hash_is_family_instance (rfl) ⚑ rosterFamily_CR_of_rosterCR (old⟹new) DELETED 07-28: both ends refuted, so it was true and empty. §3 ⚑ THE GAME IS ANCHORED ON THE REAL DEPLOYED READER, not a re-labelled collision game: rosterSubstitutionGame's answers are two REAL RecordKernelStates + two rosters, and the win relation mentions CouncilCommit.councilCommitOf D.idCell itself — a dishonest host whose two identity cells' council_commit registers AGREE while committing to DIFFERENT guardian sets (the sub-threshold/attacker-quorum swap). §4 substitution_wins_imp genuinely TRANSPORTS (rw [← hcommit, hcc, hcommit'] through the shared commitment) rather than re-pairing its own hypothesis; substitution_adv_le is the advantage inequality by winProb_le_of_imp. A ROOT-level attacker instantiates the game: the shared-commitment conjunct is discharged by the REAL CouncilCommit.recStateCommit_recovers_council. §5 recStateCommit_recovers_council_roster_advantage_bound carries an EXPLICIT Eff. §6 CANARY (MUTATION-TESTED RED) + positive pole. §7 both poles PRICED (roster_floor_top_false_of_compressing/_blake3, roster_floor_bot_vacuous, brokenRoster_floor_top_false + brokenRoster_substitution_floor_top_false — the named attack is a REAL attack on a broken digest). ⚑ SCOPE, NAMED NOT HIDDEN: the reduction covers the roster-recovery step from the shared council commitment onward; the root⟹shared-commitment step rides the StateCommit CR set (compressInjective/compressNInjective/cellLeafInjective — ANOTHER cluster's carriers), carried as a named separate hypothesis, untouched here. #assert_all_clean (20 keystones).
import Dregg2.Spike.CommitTreeRegrounded -- ⚑ VACUITY-SWEEP FINDING 2, cluster 3/3 (07-17) — the CommitTreeInjective site (Spike/EffectVmConstraints2:373, the air.rs:2546-2594 group-4 STATE_COMMIT portal). §1 commitTreeInjective_false_babyBear / _blake3: the carrier is injectivity of commitTree H on the INFINITE List ℤ while the root is ONE felt — commitTree_bounded proves the tree inherits H's window (the 12-column branch is an H output; the malformed-tuple fallback is 0, in-window), which is what lets the refutation be stated on H rather than on the tree. So state_commitment_binds_state and state_commitment_no_silent_change — the theorems that make the published PI NEW_COMMIT bind the actual post-state — are VACUOUS at deployed parameters. §3 stateEquivocationGame: the adversary exhibits TWO DISTINCT post-state tuples behind ONE published root, which IS the attack the consumers rule out. §4 equivocationToCollisionFinder + equivocation_wins_imp + equivocation_adv_le. §5 state_commitment_binds_state_advantage_bound / state_commitment_no_silent_change_advantage_bound, each carrying an EXPLICIT undischarged Eff. §6 CANARY (MUTATION-TESTED RED on the real bound) + positive pole. §7 both poles PRICED + refutability on a broken deployment. Old consumers KEPT; siblings ADDED. #assert_all_clean (20 keystones).
import Dregg2.Circuit.FriCompressRegrounded -- ⚑ VACUITY-SWEEP FINDING 2, cluster 3/3 (07-17) — the CompressInjective site (FriVerifier:214, the Poseidon2 MerkleTreeMmcs TruncatedPermutation). §1 compressInjective_false_of_finite_range (the 2-to-1 pair-flattening shape, mirroring HashFloorHonesty.compressInjective_false_of_finite_range) and ⚑ compressInjective_false_of_digest_width [Finite F] (w) (hw : ∀ a b, (compress a b).length = w) — the DEPLOYED form, whose WHOLE hypothesis is a finite field + a constant output length; nothing about Poseidon2's algebra is used (compressInjective_false_poseidon2_digest fixes w = 8 = DIGEST_ELEMS). So merkleRecompute_binds — the FRI anti-forgery tooth — is VACUOUS at deployed parameters. §3 merkleForgeryGame: two DISTINCT leaves recomputing the SAME root at the same query index over the same sibling path; forgery_win_fools_merkleVerify ties the win to the DEPLOYED merkleVerify Boolean accepting BOTH leaves against one root, so the game is anchored on the verifier's own accept condition rather than an abstract root. §4 ⚑ THE FULL PATH-PEEL IS CLOSED, NOT DOWNGRADED: peelPath is a CONSTRUCTIVE extractor that walks the forged opening and returns the FIRST genuine compress collision, proved by peelPath_wins over the SAME path recursion merkleRecompute_binds uses (both leaves open at the same index, so at each level they hit compress against the same sibling on the same side; either the digests already agree on distinct inputs — collision, returned — or they stay distinct and the walk descends with the invariant restored; the nil case is where the two proofs meet). So merkleRecompute_binds's injectivity STEP is replaced by the extraction it was standing in for, with NO assumption on compress at all. Non-vacuity verified EMPIRICALLY (#guard-executed): a compress injective at level 0 but colliding at level 1 makes peelPath return exactly the level-1 collision — the DESCENT branch is exercised, not only the immediate-hit branch. §5 merkleRecompute_binds_advantage_bound carries an EXPLICIT undischarged Eff. §6 CANARY (MUTATION-TESTED RED) + positive pole. §7 both poles PRICED + brokenCompress_forgery_solvable (the attack is real and a broken hash performs it). #assert_all_clean (21 keystones).
import Dregg2.Crypto.SpongeCompressionRegrounded -- ⚑ VACUITY-SWEEP FINDING 2, cluster 3/3 (07-17) — the CompressionCR site (SpongeReduction:146), THE HIGHEST-VALUE CARRIER OF THE CLUSTER: SpongeReduction's headline spongeCR_of_reduction is what demotes the tower's spongeCR carrier from "an unbounded list-hash is injective" to "ONE permutation call is CR (primitive #4)". §1 ⚑ compressionCR_false_of_finite_state [Nonempty State] [Finite State] — the deployed form needs NO numeric bound at all: Finite State is the WHOLE hypothesis, and it is the only thing SpongeReduction's own abstraction says about a fixed-width permutation state. Uncurried step : State × List ℤ → State has an INFINITE domain (Infinite (List ℤ) × Nonempty State ⇒ Prod.infinite_of_right) and a FINITE codomain, so the counting core fires. Therefore spongeCR_of_reduction, foldl_step_eq, finalState_inj and realizedSpongeOfReduction are ALL vacuous at deployed parameters — the headline reduction transports nothing at a real sponge. §3-§4 ⚑ A REAL CONSTRUCTIVE EXTRACTOR, NOT A RELABEL: the obvious "two (state, block) pairs colliding under step" game would be definitionally the compression-collision game and its extractor the identity — decoration. Instead the attack games are the events the OLD consumers actually rule out (mdPeelGame, finalStateCollisionGame, spongeCollisionGame — the headline), and peel walks the two foldl step chains from the LAST block inward and RETURNS the first divergence as an explicit (state, block) collision: foldl_step_eq's induction run BACKWARDS — where that theorem CONSUMES CompressionCR, peel PRODUCES the collision a disagreement forces. Structurally recursive, uses the adversary's actual output, appeals to NO choice principle; the init-vs-step boundary is discharged by the STRUCTURAL InitStepSeparated — exactly where the length prefix earns its keep (peel_spec is the correctness proof). Extractors mdPeelToCompressionFinder / finalStateToCompressionFinder / spongeToFinalStateAdv (the truncation leg, carrying hSq) / spongeToCompressionFinder = the composition, each with _wins_imp + _adv_le. §5 foldl_step_eq_advantage_bound / finalState_inj_advantage_bound / spongeCR_of_reduction_advantage_bound (headline), each carrying an EXPLICIT undischarged Eff. §6 CANARY (MUTATION-TESTED RED) + positive pole. §7 both poles PRICED (compression_floor_top_false_of_finite_state — ⊤ FALSE at ANY real sponge state, no parameters needed — and compression_floor_bot_vacuous) + brokenSponge_floor_top_false / brokenMachine_not_compressionCR. Old consumers KEPT; siblings ADDED. #assert_all_clean (30 keystones).
import Dregg2.Crypto.BeaconSlotRegrounded -- ⚑ VACUITY-SWEEP FINDING 2, cluster 3/3 (07-17) — the HonestSlotCR site (RandomnessBeacon:79). ⚑ ITS FALSITY HAS A GENUINELY DIFFERENT SHAPE FROM ITS SIBLINGS, and the file proves and explains rather than forcing the counting core onto it: with rest FIXED the honest slot is Ct → O, a map between two FIXED-WIDTH types — nothing makes Ct infinite (a contribution is one value, not a list), so there is no infinite domain for the counting core to bite on and assuming one would model a deployment nobody runs. The honest refutation is by COMPRESSION: honestSlotCR_false_of_compressing via Fintype.exists_ne_map_eq_of_card_lt under |O| < |Ct| (which IS the definition of a hash-combine, not an extra hypothesis) — the hashCR_false_of_compressing shape. Contrast KeySetCR/Poseidon2SpongeCR/CompressionCR, whose domains (List Key, List ℤ, State × List ℤ) are structurally infinite. honestSlotCR_false_of_infinite_ct is the unbounded/aggregated-contribution variant. ⚑ AND THE FILE REFUTES RandomnessBeacon's OWN DEFENCE: that header argues HonestSlotCR "is NOT bespoke — it is DISCHARGED by the imported standard HashCR (honestSlotCR_of_hashcr)". True, and not a defence: HashCR is ONE OF THE FOUR FLOORS HashFloorHonesty ALREADY PROVED FALSE (its TOOTH 3). honestSlotCR_discharge_hypothesis_is_false / _of_finite_out prove it at the exact cr the beacon is built from ([Finite O] is the whole deployed hypothesis), so honestSlotCR_of_hashcr transports NOTHING at deployed parameters — false hypothesis, false conclusion, both vacuous. Bottoming out at a standard floor is grounding only if the standard floor is SATISFIABLE. The asymmetry is recorded: the SLOT floor needs compression to refute, the underlying MULTISET-HASH family floor needs only a bounded output (the multiset domain IS infinite) — two different arguments for two levels of the same carrier. §3-§4 biasToCollisionFinder is data-dependent (the biaser chooses rest per tag; the extractor conses each honest contribution onto ITS rest) and bias_wins_imp runs through Multiset.cons_inj_left — the same pure cancellation fact honestSlotCR_of_hashcr uses, now CARRYING the reduction instead of a false hypothesis. §5 honest_makes_unbiasable_advantage_bound / bias_breaks_honest_slot_cr_advantage_bound, each carrying an EXPLICIT undischarged Eff. §6 CANARY (MUTATION-TESTED RED) + positive pole. §7 both poles PRICED + brokenBeacon_not_honestSlotCR, fired THROUGH the old bias_breaks_honest_slot_cr (the old theorem is the tooth). ⚠ NEARBY SITE, NAMED: Crypto/HashRandRefinement.lean:140 consumes the identical Multiset.cons_inj_left-against-an-injective-frameOutput shape and wants the same treatment. Complementary to (not overlapping) RandomnessBeaconRegrounded, which re-grounds the beacon CONSUMERS and never touches this carrier. #assert_all_clean (22 keystones).
import Dregg2.Circuit.DomainSeparatedCREffRegrounded -- ⚑⚑⚑ VACUITY-SWEEP FINDING 2, cluster 3/3 (07-17) — THE SHARPEST ONE: the BRIDGE TO THE REAL OBJECT DOES NOT REACH THE REAL OBJECT. Poseidon2KeyedBridge is the file written to close the "abstract F" gap and connect the DEPLOYED unkeyed Poseidon2 (babyBearD4W16) to the keyed floor the whole STARK/FRI/apex chain rests on; its named floor DomainSeparatedCR D := CollisionResistant (poseidon2KeyedFamily D) is doc-marked "a GENUINE floor … NOT an injectivity/existence-refutation". But FloorGames' load-bearing result lands squarely on it: that floor IS HashCRHardQuant F ⊤ (⚑ 07-28 DomainSeparatedCR's body SAYS so now — CollisionResistant and the iff that used to reveal it are both deleted, and domainSeparatedCREff_top_iff_old is Iff.rfl) and Hard G ⊤ ↔ Negl (solvableFrac G) (hard_top_iff_solvableFrac_negl) — so it IS a floor at the UNRESTRICTED class, hence IS the probabilistic EXISTENCE floor, hence FALSE wherever collisions merely EXIST. §1 domainSeparatedCR_false_babyBear PROVES it: at a BabyBear-bounded sponge collisions exist at every domain-separation tag (exists_collision_at_tag — prefixing is injective but the prefixed map still lands in one bounded field), so the DEPLOYED apex chain (deployed_lightclientUnfoolable_advantage_bound and siblings) is VACUOUS at deployed parameters. Same pigeonhole that killed Poseidon2SpongeCR, one Classical.choice later. ⚑ AND THE SATISFIABILITY WITNESS IS THE TELL: domainSeparatedCR_forces_unbounded_sponge proves anything discharging the floor has an INFINITE-RANGE sponge — i.e. is not a field hash at all — which explains BOTH poles of the bridge's own non-vacuity argument at once: refDomainSep_CR succeeds precisely because Reference.refSponge is an injective Encodable-style map into ALL of ℤ, and the deployed sponge fails precisely because its range is one bounded field. Toy witness satisfiable, real hash false — HashFloorHonesty's own diagnosis of its own predecessor, recurring at the file written to cure it. ⚑ HONEST SCOPE: no theorem is WRONG and nothing deployed becomes unsafe today — the consumers are true, vacuously, so they transport no security; the bridge's REAL contributions stand untouched (the keyed family and the faithfulness lemmas deployed_hash_is_family_instance / wins_iff_deployed_collision — the game genuinely IS about the deployed function). What fails is only the adversary CLASS. §2 THE REPAIR: DomainSeparatedCREff D Eff := HashCRHardQuant (poseidon2KeyedFamily D) Eff — the SAME game over the SAME deployed family (so every faithfulness lemma applies verbatim; the game was never the problem, the CLASS was), at an EXPLICIT class; domainSeparatedCREff_top_iff_old is the bridge that makes §1 a statement ABOUT the deployed floor. §3 re-grounds every deployed consumer the bridge ships (recStateCommit root binder, OOD/Merkle/trace binders, the multi-round FRI/STARK fold via negl_finset_sum with a per-round hEff, and the APEX two-hash light-client forgery bound), each keeping its exact conclusion and carrying its hEff in the open. §4 CANARY (MUTATION-TESTED RED) + positive pole + both Eff poles PRICED (effFloor_top_false_babyBear, effFloor_bot_vacuous). §5 ⚑ THE LANDING SITE, NAMED — unlike the lattice residual this one has an ADDRESS: RomQueryFloor.RomEff (query-bounded ROM adversaries, with romCollision_hard proving the UNCONDITIONAL birthday bound and BOTH escapes proved — choiceAdv_not_romEff excludes Classical.choice, twoPointAdv_in_romEff exhibits a real member) is the honest class for a HASH. What it needs is stated precisely rather than hand-waved: RomQueryFloor samples a uniformly random ORACLE whereas poseidon2KeyedFamily is a FIXED tag-keyed function, so landing the deployed sponge there is a MODELLING step — the standard random-oracle idealization of Poseidon2 — NOT a derivation, and it must be taken deliberately and labelled, not smuggled. Not taken here; taking it silently would be exactly the laundering this sweep exists to stop. Poseidon2KeyedBridge NOT edited; consumers KEPT, siblings ADDED. #assert_all_clean (17 keystones).
import Dregg2.Circuit.Blake3FloorEffRegrounded -- ⚑⚑ VACUITY-SWEEP FINDING 2, cluster 3/3 (07-17) — THE FOURTH COSTUME, COMPILED. Blake3FloorReduce gets §1 RIGHT (Blake3NoCollision proved FALSE at any finite digest by the counting core — correct, valuable, reused here, not disturbed). Its §2 is the problem: the prescribed fix is the OrBreak twin — replace the false hypothesis with a disjunct NAMING the break — advertised as "the same conclusion; the unsatisfiable collisionHard hypothesis is gone", blake3_floor_cr "DE-VACUATED". But OrBreak Break P := Break ∨ P and Blake3Collision hash := ∃ x y, x ≠ y ∧ hash x = hash y is an EXISTENCE claim that the file's OWN blake3Collision_of_finite_digest PROVES for every finite-digest hash — i.e. for the real 32-byte BLAKE3. So orBreak_twin_trivial_at_finite_digest compiles the twin's exact conclusion via OrBreak.broke (blake3Collision_of_finite_digest hash), WITHOUT hCommitted and WITHOUT hOpened; and orBreak_trivial_for_any_conclusion sharpens it as far as it goes — at a finite digest OrBreak (Blake3Collision hash) P holds for ANY P WHATSOEVER, INCLUDING A FALSE ONE, so a twin's truth is independent of its conclusion and cannot be evidence for it. A P ∨ True is as empty as a False → P; swapping one for the other is not a repair. HashFloorHonesty's mod2_dumb_negligible named this exact conflation (existence of a collision does NOT by itself break computational CR); the OrBreak shape reproduces it on the OTHER SIDE OF THE TURNSTILE. ⚑ AND THE FILE STATES THE REFUTATION AS A FEATURE: blake3Collision_of_finite_digest's docstring reads "the Break side is a THEOREM for the real (finite-digest) hash. The twin's break branch is not decorative." — backwards: a break branch that is a THEOREM is an escape hatch the file proved is always open, and §3's "FIRE: both branches exercised" fires it on TOY hashes (toyHash2/collidingMac) while at the deployed hash the break branch is the ONLY branch. The ¬Blake3Collision-taking consumers (blake3_binds_of_no_collision, share_mac_detects_tamper_of_no_collision) inherit the plain defect — no_collision_hypothesis_false_at_finite_digest proves their hypothesis FALSE, since blake3NoCollision_iff_no_break makes it exactly the falsified carrier. THE REPAIR: the break must be about FINDING, not EXISTENCE. §2 Blake3Deployment carries the deployed tag-keyed BLAKE3, the honestly committed preimage, the PUBLISHED commitment, the dealer's committedOpens tie, and digestFinite as a FIELD (the real 32-byte output is a FACT about the deployment, not an assumption); blake3Family lifts it (deployed_hash_is_family_instance = rfl). §3 commitOpeningGame is anchored on the DEPLOYED published commitment (wins := x ≠ committed t ∧ hash t x = commitment t) rather than being the collision game renamed. §4 opening_wins_imp genuinely TRANSPORTS through committedOpens (hopen.trans (D.committedOpens t).symm) to reach a real collision; opening_adv_le by winProb_le_of_imp. §5 blake3_commit_opens_advantage_bound: binding EXCEPT with negligible probability, with an explicit undischarged Eff. §6 TWO canaries — the ordinary one (MUTATION-TESTED RED on the real bound) and ⚑ a second that refuses the fourth costume itself: naming the break buys nothing here, because the conclusion is an ADVANTAGE, not a disjunct. §7 both poles PRICED — blake3_floor_top_false_at_finite_digest uses the SAME fact that makes the twin trivial, to PRICE the floor instead of to discharge a consumer, and the difference between those two uses IS the finding — plus blake3_floor_bot_vacuous and brokenBlake3_floor_top_false / brokenBlake3_opening_top_false (the named attack is a REAL attack on a broken hash). ⚑ HONEST SCOPE: nothing in Blake3FloorReduce is WRONG — every theorem in it is TRUE; the twins are true for a reason that has nothing to do with the reduction they are named for, so they bank no security. Blake3FloorReduce NOT edited beyond a doc-marker; defs/twins KEPT. #assert_all_clean (16 keystones).
import Dregg2.Deos.InAirAuthorityFloorRegrounded -- ⚑ VACUITY-SWEEP FINDING 2, cluster 3/3 (07-17) — the FloorDigestBinds site (Deos/InAirAuthorityDigestGadget:128). §1 floorDigestBinds_false_babyBear: the carrier is injectivity on the INFINITE List ℤ while the Option-B digest is ONE BabyBear felt — literally the StateCommit.compressNInjective shape HashFloorHonesty had ALREADY proved false — so the CR leg of all four consumers (gentian_selector_forced_discharged, gentian_settle_forced_discharged, gentian_partial_unsat_discharged, gentian_phantom_unsat_discharged) is VACUOUS at deployed parameters. ⚑ THE SHARPENING: the felt bound that REFUTES the floor is a hypothesis the consumers THEMSELVES assume, IN THE SAME SIGNATURE, about the very felts they digest — hcanon : ∀ c, 0 ≤ (envAt t i).loc c ∧ (envAt t i).loc c < 2013265921. The pigeon was always in the signature. §2 floorDigestFamily (keyed by the domain-separation tag) + deployed_hash_is_family_instance (rfl) + the old⟹new bridge. §3 ⚑ THE ATTACK GAME IS ANCHORED ON THE DEPLOYED GADGET, not a re-labelled collision game: alternateFloorGame's win relation reads the attacked value out of a REAL VmTrace — digestLimb t is literally (envAt (D.trace t) (D.row t)).loc gentianAuthDigestCol, the real column through the real envAt — and wins iff an ALTERNATE floor hits that PUBLISHED limb while differing from the committed one, which is exactly the dodge gentian_selector_forced_discharged's docstring says the forger "can dodge NEITHER by an alternate witnessed floor NOR by sel = 0". §4 alternate_wins_imp genuinely TRANSPORTS (hlimb.trans (D.hcommitLimb t)) — a limb-hit becomes a real digest collision — rather than re-pairing its own hypothesis; alternate_adv_le by winProb_le_of_imp. §5 gentian_alternate_floor_advantage_bound / gentian_settle_alternate_floor_advantage_bound carry an EXPLICIT undischarged Eff. §6 CANARY (MUTATION-TESTED RED) + positive pole. §7 both poles PRICED (floorDigest_floor_top_false_of_compressing/_babyBear — whose docstring records that the consumers' own hcanon supplies exactly the refuting bound — and floorDigest_floor_bot_vacuous) + brokenFloorDigest_floor_top_false / _alternate_top_false (the named attack is a REAL attack on a broken digest). ⚑ SCOPE, NAMED NOT HIDDEN — ONE OF THREE LEGS: this re-grounds the CR leg only. The chip-table faithfulness leg (ChipTableSound hash (t.tf .poseidon2) — a different-shaped floor, "does the poseidon2 chip table compute hash?") and the wide-commit limb binding (hcommitLimb, carried here as a structure FIELD and ASSERTED, not discharged — the file says so in its own § RESIDUAL) are SEPARATE hypotheses with their own repair paths, untouched. The gadget keystone is NOT claimed re-grounded; one of its three legs is. #assert_all_clean (19 keystones).
import Dregg2.Circuit.ChipTableReduction -- REDUCES ChipTableSoundN off the opaque-floor list (shrinking kernelConfigSound's residual). AcceptanceDischarge left the WitnessDecodes/<e>TraceReadout residual at ChipTableSoundN permOut (t.tf .poseidon2). This FACTORS it: (RANGE half) rangeTableFaithful (t.tf .range = rangeRows bits) is STRUCTURAL, not a lever — range_table_faithful_of_structural reads the honest interval [0,2^bits) off it via range_row_mem_iff (symbolic; never enumerates 2^30; fires on BAL_LIMB_BITS=30 — in_range_row_mem/boundary_row_not_mem). (POSEIDON2 half) ChipTableSoundN per row = STRUCTURAL shape (ChipRowShaped; the input/output decode is UNIQUE — chipRowShaped_decode_unique, padTo_inj + append_inj, no crypto; already rejects malformed rows — forged_not_shaped) AND the residual outBlock = permOut ins. That residual is the deployed Ir2Air::Chip round gates (poseidon2_permute_expr, descriptor_ir2.rs:1919, NOT VmConstraint2s of any Lean descriptor so Satisfied2 cannot deliver them), NAMED as Poseidon2ChipArithSound (round-gate arithmetization soundness — a SEPARATE primitive from Poseidon2SpongeCR: output-correctness, not injectivity; arithSound_not_CR proves the separation via the all-zero permutation whose lane-0 digest is constant hence non-CR). chipTableSoundN_of_arith_and_accept DISCHARGES ChipTableSoundN = {Poseidon2ChipArithSound + ChipRoundGatesAccepted (extraction-side, same class as the bus/FRI facts)}; tableFaithfulness_of_arith_and_range assembles BOTH Satisfied2Faithful conjuncts (chip+range). Non-vacuous: chipTableSoundN_fires + roundGatesZ_separates. So ChipTableSoundN is DERIVED, sole crypto residual = the named Poseidon2 chip-arithmetization soundness; no fresh opaque floor. #assert_axioms-clean.
import Dregg2.Circuit.FloorsNonVacuousWave -- EXTENDS FloorsNonVacuous to the per-effect <E>TraceReadout carriers: each <e>_descriptorRefines_sat rung takes a <E>TraceReadout PREMISE, and an uninhabitable premise would make its refinement rung VACUOUSLY satisfiable. Per readout a CONCRETE inhabiting term (Nonempty) is constructed: a two-row trace whose designated active row carries the selector hot + the committed decode-seam limb, a RotTableSide table half over the faithful chip/range tables (readoutTrace_side, reusing FloorsNonVacuous.faithfulTf), pre=post so every frame field is rfl, the guard discharged at a self-targeted live cell (actor=cell ⇒ self-authority troa_lrefl; lifecycle 0 ⇒ Live ⇒ acceptsEffects), and the decode seam realized by setting the kernel field to the chosen column's value (the trace-fill identity by construction). So each per-effect readout is non-vacuous (the premise is satisfiable, not secretly empty). #assert_axioms-clean.
import Dregg2.Circuit.FloorsNonVacuousWaveLifecycle -- EXTENDS the readout non-vacuity discipline to the LIFECYCLE/AUDIT family: CellUnsealTraceReadout / CellDestroyTraceReadout / RefusalTraceReadout / ReceiptArchiveTraceReadout (RotatedKernelRefinementLifecycle) each PREMISE their <e>_descriptorRefines_sat rung — an uninhabitable premise would make the rung VACUOUS. Per readout a CONCRETE inhabiting term (Nonempty): the readoutTrace (active row hot at the effect selector SEL_CELLUNSEAL/SEL_CELLDESTROY/SEL_RECEIPT_ARCHIVE_RT with AFTER disc limb = the post lifecycle discriminant, all-zero wrap LAST row), the committed-root seams realized by compressN := cZero (every listDigest collapses to 0, matching the zero record limb + zero pub: deathCertRoot/auditSlotRoot/piAnchored all 0), a near-pre=post boundary (cellUnseal: lifecycle Sealed=1 for the guard; destroy/audit: Live=0 with accounts={0}; refusal differs only at the cell-map via auditCellMap), guards discharged by self-authority (actor=cell=0) + membership + liveness via decide. So each lifecycle/audit readout is non-vacuous. #assert_axioms-clean.
import Dregg2.Circuit.FloorsNonVacuousWaveBirth -- EXTENDS the readout non-vacuity discipline to the CELL-BIRTH family: CreateCellTraceReadout / CreateFromFactoryTraceReadout / SpawnTraceReadout (RotatedKernelRefinementBirth) each PREMISE their <e>_descriptorRefines_sat rung — an uninhabitable premise would make the rung VACUOUS. Per readout a CONCRETE inhabiting term (Nonempty): the active-row trace hot at the effect selector (SEL_CREATE_CELL_RT/SEL_FACTORY_RT/SEL_SPAWN_RT), a pre→post boundary that GROWS the account set (post.accounts = insert newCell pre.accounts, so the growthDecodes implication's conclusion is rfl — the WitnessDecodes seam realized by construction), the guard discharged by caps actor := [node newCell …] (mintAuthorizedB; + confersEdgeTo target for spawn) with freshness newCell ∉ pre.accounts, the born-empty per-cell post maps matching bornEmptyAt / the spec's if-c=newCell forms, a trivial CONFORMING FactoryEntry registered at vk 0 (factory), the spawn cap-handoff maps (the PHASE-D residual, carried), and the receipt+frame (rfl, post built from pre). So each birth readout is non-vacuous. #assert_axioms-clean.
import Dregg2.Circuit.FloorsNonVacuousWavePermsProgram -- EXTENDS the readout non-vacuity discipline to the PERMS/VK/PROGRAM/REVOKE family: SetPermsTraceReadout / SetVKTraceReadout (RotatedKernelRefinementPermsVK) · SetProgramTraceReadout (RotatedKernelRefinementProgram) · RevokeCapabilityTraceReadout (RotatedKernelRefinementCapFamily) each PREMISE their <e>_descriptorRefines_sat rung — an uninhabitable premise would make the rung VACUOUS. Per readout a CONCRETE inhabiting term (Nonempty): setPerms/setVK = the active row hot at SEL_SET_PERMS/SEL_SET_VK with the AFTER perms/VK limb + declared-param column both 0, the boundary's cell-map the declarative setPermsCellMap/setVKCellMap …0 slot write so fieldOf permsField/vkField (post.cell 0) = 0 by the set*_cellWrite_correct write/read law (limb/param decode match; cellStructResidual the slot-write reconstruction), guard self-authority+membership+liveness at account 0; setProgram = LAST-row only with compressN := cZero collapsing auditSlotRoot + listDigest auditLeaf cZero [0] to 0 (matching the zero record limb + zero pub), cellMapMove the program:=0 slot write; revokeCapability = active row hot at sel.REVOKE_CAPABILITY with capsMoveDecodes an IMPLICATION discharged fun _ => rfl by post.caps := removeEdgeCaps pre.caps 0 0, KernelFrameExceptCaps all-rfl, no guard leg (RevokeSpec admissibility is trivial). So each perms/program/revoke readout is non-vacuous. #assert_axioms-clean.
import Dregg2.Circuit.FloorsNonVacuousWaveTransfer -- EXTENDS the readout non-vacuity discipline to the TRANSFER beachhead: TransferTraceReadout (ClosureTransfer) is the most structured per-effect readout (two designated rows with full RowEncodes decodes, dir/amt tags, RotTableSide faithfulness, the side guards non-neg/distinct/both-live/accepts, the 16-field frame, the receipt-log advance) — its <e>_descriptorRefines_sat rung would be VACUOUS if uninhabitable. A CONCRETE inhabiting term (Nonempty): a three-row txTrace (row 0 debit direction=1, row 1 credit direction=0, row 2 wrap; both designated rows transition-not-last) whose RowEncodes decodes are rfl by construction (each CellState/param field DEFINED as the row's column read via cellBefore/cellAfter/paramsOf; the published OLD/NEW commits = 0 = the zero state-commit columns), RotTableSide riding the faithful chip/range tables, a pre=post boundary over accounts={0,1} both Live with tr=⟨0,0,1,0⟩ (src 0 ≠ dst 1, both live, amount 0 ≥ 0) so every guard discharges by decide and every frame is rfl. So the transfer readout is non-vacuous. #assert_axioms-clean.
import Dregg2.Circuit.FloorsNonVacuousWaveMiscNotes -- EXTENDS the readout non-vacuity discipline to the MISC/NOTES/HEAP family: MakeSovereignTraceReadout / SetFieldDynTraceReadout (RotatedKernelRefinementMisc) · NoteSpendTraceReadout / NoteCreateTraceReadout (RotatedKernelRefinementNotes) · HeapWriteTraceReadout (RotatedKernelRefinementExercise) each PREMISE their <e>_descriptorRefines_sat rung — an uninhabitable premise would make the rung VACUOUS. Per readout a CONCRETE inhabiting term (Nonempty): the active-row trace hot at the effect selector (SEL_MAKE_SOVEREIGN_RT/SEL_SET_FIELD/SEL_NOTE_SPEND/SEL_NOTE_CREATE; heapWrite reads HEAP_ROOT_AFTER=0), the decode seam realized by construction (makeSovereign: AFTER mode limb=1 with post.cell:=sovereignRebind ⇒ the if-indicator=1; setFieldDyn: AFTER fields_root limb & declared-param both 0 with post.cell:=setFieldCellMap …"x" 0 reading slot back 0; noteSpend/noteCreate: growthDecodes is an IMPLICATION whose conclusion post.nullifiers/commitments:=nf/cm::pre holds rfl; heapWrite: newRoot:=0=the AFTER register), the guards discharged at the self-targeted live cell (self-authority actor=cell=0; empty caveats admit; membership {0}; lifecycle Live) by decide, noteSpend freshness nf∉[] + spendProof=true. So each misc/notes/heap readout is non-vacuous. #assert_axioms-clean.
import Dregg2.Circuit.Satisfied2FaithfulActive -- THE KEYSTONE MADE WHOLE: ONE Satisfied2Faithful term that is BOTH active per-row AND faithful-tabled (the two FloorsNonVacuous + CircuitCompletenessNonVacuityReal half-witnesses unified). satisfied2Faithful_transferV3_active : Satisfied2Faithful permOutZ permOut0 transferV3 … activeFaithfulTrace, where activeFaithfulTrace carries the SAME two REAL active rows as realTransferTrace (row 0 the genuine debit bal_lo 100→90, nonce 0→1, amount 10·dir 1 — the per-row Gate/Transition gates GENUINELY bite; row 1 the wrap carrying row 0's after-state) AND a tf laying down the GENUINE deployed faithful tables: .poseidon2 = the active union chip table tfOf2 transferV3 realRow lastRow, proven ChipTableSoundN permOutZ (every one of the 40+40 evaluated chip tuples has an ALL-ZERO 8-lane squeeze ⇒ IS a real chipRowN permOutZ ins row — a kernel-decide enumeration, no native_decide); .range = rangeRows BAL_LIMB_BITS (the active after-balance limb lookups land at [90]/[0],[0]/[0] ⊂ [0,2^30), so the GENUINE deployed limb table CONTAINS them via lookup_range_complete — the faithful range table, not a membership stand-in); .memory/.mapOps = [] (graduateV1 emits no mem/map log). The Satisfied2 core reuses the CircuitCompletenessNonVacuityReal per-row discharge (base_constraints_hold on the active row; chip lookups by membership in the union; the mem/map collapse); the four faithful fields are permOutZ_width, permOutZ_lane0, activeChipTbl_sound (the decide-checked ChipTableSoundN), and rfl for the range. So the full deployed accept-set is inhabited by a REAL active turn — one object, not two composed. satisfied2Faithful_satisfiedVm FIRES on it (active_collapse_recipe), flowing to the rung layer with NO free chip/range lever. #assert_axioms-clean.
import Dregg2.Circuit.ClosureFanout -- (SUPERSEDED by ClosureFanoutGenuine for the anti-laundering bar; kept for the ClosureFloors structure) REPLICATES the closure shape for the remaining 35 effect slots + assembles the FINAL apex lightclient_unfoolable_closed_final standing on ONLY the realizable crypto floors. ClosedLogRung S LH hash R e = the per-effect circuit-witness rung floor (Satisfied2 (R e) + StateDecodeLog ⟹ kstepAll e — the §B *_closedLog tail as a NAMED carrier, = the WitnessDecodes-class limb-level extraction StarkSound supplies); closedLogExtract_of_rung is the SHARED generic combinator (threads the Poseidon2SpongeCR/pc context — no 35× boilerplate) discharging ClosedLogExtract from ClosedLogRung. 30 cohort-tag dischargers closedLogExtract_<e>_closed (one line each) + exercise (16) via the same floor (its log advances in the inner fold, NOT a receipt-prepend — still discharged to its realizable floor). ClosureFloors S LH hash bundles the per-effect ClosedLogRung family (one per actionTag + a uniform `other` for off-cohort indices riding the transfer fallback). closedLogExtract_all discharges ∀ e, ClosedLogExtract by the 36-way actionTag case-split over ClosureFloors. lightclient_unfoolable_closed_final feeds it to lightclient_unfoolable_closed: carried floor set EXACTLY {StarkSound hash Rfix, Poseidon2SpongeCR + the S_live CR fields, logHashInjective LH (inside mkLog), WitnessDecodes, ClosureFloors (the per-effect circuit-witness extraction family)} — all realizable; ALL 36 actionTag slots discharged to their realizable floors, NO per-effect EffectDecodeBridge/decode residual remains. HONEST: the per-effect rung's logical CORE (encode ⟹ Spec ⟹ dispatchArm) is landed in RotatedKernelRefinement* and consumed inside the ClosedLogRung floor; the floor IS the realizable Satisfied2⟹(params,receipt,hpub,encode) circuit-witness extraction, named not faked. #assert_axioms-clean.
import Dregg2.Circuit.ClosureColumnDischarge -- the GENERIC column-read discharge (soundness floor loop #4): the RowEncodes-class limb decodes are env.loc-DETERMINED, so they LEAVE the carried floor; only the row designation (StarkSound-class) is the genuine prover witness. decodeCellPre/decodeParams/decodeCellPost READ env.loc at the named state_before/param/state_after columns into a CellState/TransferParams; rowEncodes_of_designation (THE combinator): given ONLY the two publish-commitment ties (env.pub OLD/NEW_COMMIT = state_commit column — the piBinding hash-CR facts), the canonical decoders satisfy RowEncodes — the 17 column equalities by rfl, only the 2 publish ties carried. rowEncodes_decode_determined: any RowEncodes decode of the same env AGREES with the canonical reads (the decode was never witness). TransferRowDesignation = the SLIM transfer readout (rows di/ci + in-bounds + publish ties + dir tags, all read from the trace); transferRowDesignation_decodes RECONSTRUCTS the two RowEncodes decodes from it. The uniform pattern (the other 35 effects' *Encodes column blocks are column=field, rfl). #assert_axioms-clean.
import Dregg2.Circuit.ClosureFloorReduce -- APPLIES the column-read discharge to ClosureTransfer's carried readout floor (soundness floor loop #4, made load-bearing): TransferReadoutResidual = the genuine COLUMN-FREE residual of TransferTraceReadout (the slim TransferRowDesignation + RotTableSide + the LEDGER ties of the canonical decoded limbs (the LedgerSurfaceReadout surface seam) + the amount/receipt ties + the 16-field frame + side guards + log advance) — NO srcPre/…/srcParams records, NO RowEncodes decodes, NO dir-tag fields (those are env.loc-determined via the designation). transferTraceReadout_of_residual REBUILDS the full TransferTraceReadout from the residual: the decoded records are the canonical decodeCellPre/Params/Post (envAt t ·), the RowEncodes decodes come from rowEncodes_of_designation, the dir tags from the designation. So ClosureTransfer's carried readout = {row designation + publish ties + the named ledger/frame/guard residuals}, the mechanical column reads DISCHARGED. The minimal floor: uniformly {StarkSound row designation + hash/Merkle CR publish ties + LedgerSurfaceReadout} with the RowEncodes limb decodes GONE. #assert_axioms-clean.
import Dregg2.Circuit.ClosureDesignationDischarge -- derive_designation_discharge: the metaprogram AUTO-GENERATING the per-effect-family RowEncodes-of-designation discharge lemma from the family's RowEncodes def (the P1 readout reduction, one macro line each). classify introspects the argument telescope + ∧-body: (1cell,0int)=post-only, (2cell,0int)=pre-post, (2cell,1int)=value-carrying (finds the param value column), emitting <e>_rowEncodes_of_designation (17 column reads rfl, ∀i blocks intro i;rfl, publish ties by assumption) + its #assert_axioms tripwire. 21 CLEAN families auto-derived across the three shape-classes (mint/burn/bridgeMint/noteSpend/noteCreate value; incrementNonce/revokeDelegation/exercise/pipelinedSend/cellDestroy/setVK/cellUnseal/refusal/cellSeal/emitEvent/setPermissions/refreshDelegation/receiptArchive pre-post — the last two ties-FREE; createCell/createCellFromFactory/makeSovereign post-only) + 4 HAND variants the macro REFUSES (setField's leading slot + written-value tie; spawn's (1cell,1int) profile; attenuateA's cap-digest param; bridge's RowEncodes structure over RecordKernelState — only its two param column reads discharged, the ledger/freeze residual named). #assert_axioms-clean.
import Dregg2.Circuit.ClosureFloorReduceFanout -- THREADS the per-effect designation discharge into the fanout readouts (mirror of ClosureFloorReduce for the non-transfer effects): the rotatedEncodes<E> decode structures ClosureFanoutGenuine's readouts produce are REBUILT from a column-free residual, their carried RowEncodes decode supplied by the <e>_rowEncodes_of_designation lemma — NOT floor. IncNonceReadoutResidual/rotatedEncodesIncNonce_of_residual (pre-post exemplar): drops cellPre/cellPost + hwiEnc, keeps the row designation wi + publish ties + the canonicality envelope + the forced-tick/whole-map/guard/log/18-frame residual; hwiEnc := incNonce_rowEncodes_of_designation. MintReadoutResidual/rotatedEncodesMint_of_residual (value exemplar): drops recipPre/recipPost + hciEnc, keeps ci + publish ties + the value-column tie hAmt + the ledger-surface/frame/mintAdmit-guard residual; hciEnc := mint_rowEncodes_of_designation rewritten by hAmt. So the fanout readouts' column-decode content SHRINKS to {designation + publish ties (+ value tie)}; the ledger-seam/guard/authority/frame residuals SURVIVE BY DESIGN. #assert_axioms-clean.
import Dregg2.Circuit.ClosureForest -- THE WHOLE-TURN CLOSED APEX over HETEROGENEOUS effects (soundness loop #2): a verified TURN — a LIST of mixed effects threaded as a TurnDecodeChain (each step Satisfied2 + decoded + seam-published; per-step effect identified by the GENERIC hidx, ANY of the 36) — ⟹ a genuine whole-turn executor run execFullTurnA start acts = some fin, standing on the SAME realizable floors as the single-step ClosureFinal.lightclient_unfoolable_circuit_sound, now PER-STEP, with NO transfer-only hidx0 residual. (1) hrefines_forest_closed — BUILDS the whole-turn fold's carried family ∀ e, descriptorRefines S_live hash (Rfix e) (dispatchArm e) from the genuine ClosureReadouts bundle via hrefinesAllClosed ∘ closedLogExtract_all_genuine (kstepAll = dispatchArm defeq), so every proven <e>_closedLog rung is LOAD-BEARING behind the turn. (2) lightclient_unfoolable_circuit_sound_turn — THE HEADLINE: TurnDecodeChain over heterogeneous effects + TurnEndpoints + {Poseidon2SpongeCR + S_live CR, logHashInjective (inside mkLog), StarkSound (via the fold), the per-step ClosureReadouts prover-witness family} ⟹ ∃ acts s s', execFullTurnA s acts = some s' ∧ endpoints commit to the published turn-level (pre,post). NON-VACUITY: closedLogExtract_family_covers_mixed inhabits the rung at the NON-transfer cohort cellSeal(52)+revoke(2)+mint(3) simultaneously (the mixed-effect tooth, each routing through its proven *_closedLog); lightclient_unfoolable_circuit_sound_turn_empty instantiates the apex on the degenerate empty chain (joint-satisfiability). #assert_axioms-clean.
import Dregg2.Circuit.RotatedKernelForestCohortChain -- THE DEPLOYED PER-COHORT PROOF-CHAIN forcing (foolable gap #2: the verifier forces only the LEAD effect). The deployed prove_cohort_run_chain emits a LIST of legs (one per maximal homogeneous cohort run), each with its own rotated PI vector carrying its OLD/NEW 8-felt commit, threaded so leg[i].NEW = leg[i+1].OLD. CohortProofChain is that leg list at the published-commitment level (a TurnDecodeChain re-presented per-cohort, via toTurnDecodeChain); the gap is purely verifier-side (turn/executor/proof_verify.rs resolves ONE descriptor by vm_effects.first() + verifies ONE proof). (1) chainForcesEveryCohort — the FOREST-FORCING statement: the per-effect descriptorRefines family forces dispatchArm e legᵢ.pre legᵢ.post for EVERY leg (StepsRefine over the cohort chain), strictly stronger than the lead-only deployed check. (2) cohort_chain_forces_tail — the SHARP tooth: a TAIL leg ([Transfer,SetPermissions]'s SetPermissions) is forced as strongly as the lead. (3) lightclient_cohort_chain_forces_full_turn — folds to a genuine execFullTurnA run forcing every cohort (the ClosureForest headline reached through the per-cohort leg list). (4) chainBroken_rejects / unchained_tail_rejects — THE CHAIN-REJECTION TOOTH: a leg list whose published commits do NOT chain (legᵢ.pubPost ≠ legᵢ₊₁.pubPre) cannot inhabit pubChain ⇒ REJECTED (the deployed adjacency check this_old==prev_new, full_turn_proof.rs:2448); cohort_chain_accepts_honest builds the honest chained [lead,tail] that ACCEPTS. #assert_axioms-clean.
import Dregg2.Circuit.CouncilCommit -- RECOVERY circuit-binding: the guardian COUNCIL commitment bound into the state commitment (light-client-unfoolable recovery; StaticThresholdSigPolicy host-trust retired in-circuit).
import Dregg2.Circuit.SettlementSoundness -- THE SETTLEMENT SOUNDNESS KEYSTONE (compose): verifyBatch accept ⟹ genuine kstepAll transition (apex) AND authority-live-AT-SETTLEMENT (Revocation at the settlement coordinate, NOT branch time). Teeth: settlement_gap_real; n=1 collapses the window. #assert_axioms-clean.
import Dregg2.Circuit.EngineSoundOfApex -- THE ENGINESOUND WELD: discharge RecursiveAggregation.EngineSound.leaf_sound BY the single-turn apex (lightclient_unfoolable_circuit_sound), so the multi-turn / finalized-history / distributed stack rests on circuit soundness, not on leaf_sound as a free sibling. ApexLeafBundle = per-leaf datum (batch pi/π + accepting verdict + the single ClosedWitness floor ⟹ apex FIRES) + the NAMED lowering apexLowers (the apex's kstepAll pi.effect, per-asset bal ledger, single FullActionA, ANY effect → the step's recCexec, legacy balOf-cell ledger, transfer Turn). engineSound_of_apex BUILDS EngineSound: leaf_sound DISCHARGED via leafStep_of_bundle∘the apex; recursive_sound/binding_sound = the two FRI legs (outside Lean) passed through. PAYOFF: multiTurn_rests_on_apex / finalized_rests_on_apex. THE RESIDUAL apexLowers is the THREE reconciliation mismatches NAMED (endpoint-binding · transfer-arm-only pi.effect=0 · cross-ledger bal-vs-balOf-cell, RingFFI projAsset agreement is column-only); realizable on honestStep (transfer arm) — honestStep_lowers. #assert_axioms-clean.
import Dregg2.Deos.BranchStitch -- BRANCH-AND-STITCH: nesting=firmament-capability-confinement (a Virtual branch holds no cap to main ⇒ side-effects imaginary) + stitch=pushout-correctness; the operable protocol of distributed time-travel.
import Dregg2.Deos.DocPatch -- THE PATCH-COMMUTATION law (Pijul heart, DOCUMENT-LANGUAGE.md §2.2): the three additive op effects (addAtom/addEdge/tombstone, faithful to dregg-doc graph.rs) COMMUTE on disjoint parts (the addAtom/tombstone SAME-id non-commutation is the load-bearing §2.2 boundary, witnessed), are each INFLATIONARY in the DocMerge ⊑ order (even tombstone, since alive≤dead), and APPLY-IS-MERGE-with-a-singleton (addEdge_is_merge / tombstone_is_merge_on_present) so commutation is merge_comm specialised. Built on Dregg2.Deos.DocMerge. #assert_axioms-clean.

-- ── ENMESHMENT: formerly-orphan modules pulled into the root build graph so their
-- #assert_axioms / #assert_namespace_axioms hygiene pins run by default under `lake build Dregg2`.
-- (Excluded: Dregg2.Claims (imports root = build cycle) — a separately-buildable top-of-graph
--  target. BridgeMint, Exec.ConcreteKernel, CreateCellFromFactory, NoteSpend, and the
--  Dregg2.Circuit.Argus aggregator are all repaired and re-enmeshed here — their hygiene pins are
--  GUARDED by the root build.)
import Dregg2.Agent.Mandate
import Dregg2.Authority.CSpace
import Dregg2.Authority.CredentialAttenuation
import Dregg2.Circuit.Argus.Coeffect
import Dregg2.Circuit.Argus.CompileE
import Dregg2.Circuit.Argus.CompileFold
import Dregg2.Circuit.Argus.Disclose
import Dregg2.Circuit.Argus.Effects.Attenuate
import Dregg2.Circuit.Argus.Effects.BalanceA
import Dregg2.Circuit.Argus.Effects.BridgeMint
import Dregg2.Circuit.Argus.Effects.Burn
import Dregg2.Circuit.Argus.Effects.CellDestroy
import Dregg2.Circuit.Argus.Effects.CellSeal
import Dregg2.Circuit.Argus.Effects.CellUnseal
import Dregg2.Circuit.Argus.Effects.CreateCell
import Dregg2.Circuit.Argus.Effects.CreateCellFromFactory
import Dregg2.Circuit.Argus.Effects.Delegate
import Dregg2.Circuit.Argus.Effects.DelegateAtten
import Dregg2.Circuit.Argus.Effects.EmitEvent
import Dregg2.Circuit.Argus.Effects.ExerciseViaCapability
import Dregg2.Circuit.Argus.Effects.IncrementNonce
import Dregg2.Circuit.Argus.Effects.Introduce
import Dregg2.Circuit.Argus.Effects.MakeSovereign
import Dregg2.Circuit.Argus.Effects.Mint
import Dregg2.Circuit.Argus.Effects.Noop
import Dregg2.Circuit.Argus.Effects.NoteCreate
import Dregg2.Circuit.Argus.Effects.NoteSpend
import Dregg2.Circuit.Argus.Effects.NoteSpendCompose
import Dregg2.Circuit.Argus.Effects.PipelinedSend
import Dregg2.Circuit.Argus.Effects.ReceiptArchive
import Dregg2.Circuit.Argus.Effects.RefreshDelegation
import Dregg2.Circuit.Argus.Effects.Refusal
import Dregg2.Circuit.Argus.Effects.RevokeDelegation
import Dregg2.Circuit.Argus.Effects.SetField
import Dregg2.Circuit.Argus.Effects.SetPermissions
import Dregg2.Circuit.Argus.Effects.SetVerificationKey
import Dregg2.Circuit.Argus.EmitRoundtrip
import Dregg2.Circuit.Argus.InterpGolden
import Dregg2.Circuit.Argus.Joint
import Dregg2.Circuit.Argus -- THE ARGUS AGGREGATOR: re-enmeshed (its CreateCellFromFactory + NoteSpend Effects are repaired; the heaps-field extensionality drift fixed). Pulls every per-effect Argus weld + its hygiene pins under the root build.
import Dregg2.Circuit.DecideSatisfied2Golden
import Dregg2.Circuit.Emit.EffectVmEmitBridge
import Dregg2.Circuit.Emit.EffectVmEmitBridgeMint
import Dregg2.Circuit.Emit.EffectVmEmitBurnRunnable
import Dregg2.Circuit.Emit.EffectVmEmitCellDestroyFullState
import Dregg2.Circuit.Emit.EffectVmEmitCellSealFullState
import Dregg2.Circuit.Emit.EffectVmEmitCreateCellFromFactoryFullState
import Dregg2.Circuit.Emit.EffectVmEmitCreateCellFullState
import Dregg2.Circuit.Emit.EffectVmEmitDelegate
import Dregg2.Circuit.Emit.EffectVmEmitDelegateAtten
import Dregg2.Circuit.Emit.EffectVmEmitEmitEventWide
import Dregg2.Circuit.Emit.EffectVmEmitExerciseWide
import Dregg2.Circuit.Emit.EffectVmEmitIncrementNonceFullState
import Dregg2.Circuit.Emit.EffectVmEmitMakeSovereignFullState
import Dregg2.Circuit.Emit.EffectVmEmitMintRunnable
import Dregg2.Circuit.Emit.EffectVmEmitNoopWide
import Dregg2.Circuit.Emit.EffectVmEmitPipelinedSendWide
import Dregg2.Circuit.Emit.EffectVmEmitReceiptArchiveWide
import Dregg2.Circuit.Emit.EffectVmEmitRecordRoot
import Dregg2.Circuit.Emit.EffectVmEmitRefusalFullState
import Dregg2.Circuit.Emit.EffectVmEmitSetFieldFullState
import Dregg2.Circuit.Emit.EffectVmEmitSetPermissionsFullState
import Dregg2.Circuit.Emit.EffectVmEmitSetVKFullState
import Dregg2.Circuit.Emit.FieldDeltaRangeEmit -- LAW #1: FieldDelta range algebra + whole-descriptor range soundness emitted from Lean, byte-pinned
import Dregg2.Circuit.Emit.MerkleMembership4aryRefine -- LAW #1: deployed membership algebra emitted from Lean; whole-descriptor SAT ⇒ positional Poseidon2 fold, byte-pinned artifact
import Dregg2.Circuit.Emit.Poseidon2HashRefine -- LAW #1: standalone Poseidon2 hash descriptor emitted from Lean; SAT ⇒ genuine chip hash, concrete non-vacuity
import Dregg2.Circuit.PqIdentityAuthority -- cell-owned ML-DSA identity rotation semantics over lossless 16×u16 byte images + exact four-limb u64 epoch; ML-DSA authorization/possession remain explicit outer predicates
import Dregg2.Circuit.Emit.PqIdentityAuthorityEmit -- LAW #1: additive one-row RotatePqIdentity authority descriptor; exact target + target-Ed25519 + old/new key/evidence images, expected=old, overflow-free +1, key-change Bezout tooth; runtime refusal stays until the ML-DSA boundary is composed
import Dregg2.Crypto.BinaryTripleSacrifice -- exact GF(2) Beaver sacrifice identity; honest-pair completeness, two-challenge malformed detection, and the one-bit-per-round soundness ceiling are all theorem-pinned
import Dregg2.Crypto.AuthenticatedBitMac -- SPDZ-style authenticated-opening algebra: the party-major batch check is exactly alpha times the random linear combination of opening errors; trusted key setup and commit-before-reveal remain explicit premises
import Dregg2.Crypto.DealerlessPreprocessing -- information-theoretic dealer-removal core: GF(2)-word MAC shares fold to global alpha; one missing share is bijective onto global keys; accepted commit/reveal receipts bind session+manifest+complete roster; successful one-time consumption cannot relabel correlation context
import Dregg2.Crypto.DealerlessPartyLocalComposition -- FHTRI005 party-local composition: exact context-bound ordered complete-roster opening, commitment-list uniqueness under explicit injectivity, XOR-share reconstruction into BinaryTripleSacrifice.Valid, and executable omission/reorder/context/cross-term teeth
import Dregg2.Crypto.DealerlessChosenVoleMac -- dealerless chosen-input VOLE algebra: full ordered cross-term reconstruction over commutative modules, characteristic-two endpoint-addition bridge, party-local mask cancellation, exact AuthenticatedBitMac composition, and executable omission/diagonal-only teeth
import Dregg2.Crypto.Arith.ArithmetizeMVP -- MVP-0 of the PROOF-PRODUCING ARITHMETIZER: `#arithmetizeDfa foo := spec` is a real elaborator that ADDS BOTH `def foo_air` AND a kernel-checked `theorem foo_sound : Refines foo_air` (the REAL refinement, proof term GENERATED from dfaAccepts_reduces) in one command. Negative canary ARMED at build time (#guard_msgs): proving aplusb's statement from toggle's spec FAILS to typecheck — the generated proof provably DEPENDS on its spec. Layer A (abstract Cert) only — no felts.
import Dregg2.Crypto.Arith.ArithmetizeTypedPredicate -- THE ARITHMETIZER AT LIVE ALTITUDE: `#arithmetizeTypedPredicate` / `#arithmetizeTypedRefinement` generate a LIVE EffectVmDescriptor2 + its kernel-checked soundness (LiveRefinesSpec quantifying over ARBITRARY nonempty traces of the EMITTED descriptor) by CITING TypedLinearPredicateDescriptorIR2's existing sound/canonical_iff — the MVP mechanism on the EXISTING Layer-B compiler, not a duplicate stack. 3 armed #guard_msgs canaries (generating the AIR from ALPHA then claiming BETA's soundness is REJECTED); alpha_json != beta_json proves the emitted BYTES are a function of the program. ⚠ CAPS: Satisfied2-level (inherits the FRI/witness-gen floor); the Holds↔Spec bridge is caller-supplied, NOT synthesized; affine+Boolean fragment.
import Dregg2.Crypto.Arith.FieldDeltaRangePilot -- THE RE-DERIVATION PILOT + MEASUREMENT. Re-derives the shipped field-delta-result-range descriptor as a `Program` (using the new AffineTerm `.scale` for the 2^j recomposition coefficients) and MEASURES the generated-vs-hand cost from the PROVEN descriptor_exact_resources: traceWidth 280 vs 33 (8.485x), constraints 374 vs 35 (10.686x) — confirming the survey to 3 sig figs. The 10x is the zeroTest-per-equality reification (3 gates + 2 witness cols each) with NO optimizer between Program and descriptor — the baseline the peephole pass (docs/DESIGN-peephole-layout-optimizer.md) exists to close back to hand-parity.
import Dregg2.Crypto.Arith.BridgeSynthesis -- MVP-2 step A: the reflection bridge SYNTHESIZER. holds_iff_canonForm : ∀ input, p.Holds input ↔ canonForm p input, generated per-Program by REFLECTING OVER SYNTAX (Formula/AffineTerm ASTs at elaboration, leaves closed by linear_combination — NOT decide-over-ZMod, the 64GB trap the design explicitly rejects). Auto-derives the Holds-canonicalization half of the arithmetizer's bridge (the arbitrary-Spec link stays manual by design). Armed #guard_msgs canary bites. noncomputable Props, no ZMod kernel reduction.
import Dregg2.Verify.RangeFamilyFaithfulRoute -- closes the carrier-level range-table-faithfulness MODELING GAP (the Gabbay falsifier §0 residual): Satisfied2 carries mem/mapTableFaithful but no range-table faithfulness, so a forged tf .range could pass every gate. Adds a per-width-family conjunct RangeFamilyFaithful (b∈{15,30}) and routes the Gabbay soundness THROUGH it WITHOUT touching base Satisfied2 (no red-umbrella — 62 producers unchanged); a forged range table is now inadmissible (statement_sound_family). ⚠ MODELING GAP not live vuln: deployed descriptor_ir2.rs verify_vm_descriptor2 already refuses oversized byte-tables (test ir2_oversized_byte_table_refuses) — this catches Lean up to the wire. 43 lookup-bearing consumers' routes + the 15-bit deployed pin are named residuals.
import Dregg2.Verify.RangeFamilyFaithfulBatch -- extends the range-table-faithfulness route to 6 representative lookup-bearing by-name descriptors (the 29-bit predicate-arith family gt/ge/le/lt/inRange + a presentation range), spanning range-width families beyond the committed [15,30]. Each _sound_family routes through RangeFamilyFaithful so a forged range table is inadmissible (_forgedRange_inadmissible). ⚠ MODELING-GAP not vuln (deployed verify_vm_descriptor2 already refuses oversized byte-tables). ~37 of 43 lookup descriptors remain (named).
import Dregg2.Verify.RangeFamilyFaithfulBatch2 -- continues the range-faithfulness route into FIVE width families the first batch never reached ({2,4,27,graduated,generic}), base Satisfied2 UNTOUCHED. 10 of 46 lookup/range-bearing by-name descriptors now routed. ⚠ A docstring claiming the family CARRIER refuses the wrap-mint canary was PROVED FALSE by the verify and is corrected in-file (the refusal is the range predicate's, not the carrier's); a §5 'sharpest connection' over-billing is corrected to a naming identity. Honest finding: the BFV butterflies are NOT a range-table hole (their lookups go to declared chip tables; their 48 bounds sit in `ranges`).
import Dregg2.Crypto.Arith.RowLibraryExperiment -- THE LAYER-B GATE, verdict FEASIBLE: 7 of DyckStackRefine's bespoke felt combinators externalized into descriptor-PARAMETRIC lemmas, and a SECOND independent descriptor's SAT⇒RowValid assembled PURELY by composing them (no primality split, no gate_modEq_iff, no omega over field values below the library line). So auto-generating the concrete felt AIR refinement is a PROJECT, not a research bet. Honest cap: the two hardest Dyck combinators (kindPartition case-split, occupancy tooth) are NOT yet in the library — factorability asserted, not demonstrated.
import Dregg2.Circuit.Emit.HomomorphicDigestEmit -- LAW #1: SIS homomorphic-digest FOLD-STEP descriptor emitted from Lean, byte-pinned. Constrains the fluid scan-state digest update dig'=dig+A·enc at q=BabyBear (windowGate accumulate + A·enc gates + last-row-vacuity repair). step_refines: the fold-step constraints vanish mod p IFF the step is exact (field-faithful, BOTH directions); descriptor-level teeth via real constraint membership. n=4 POC (scales to ~9-48 felts per SIS-DIGEST-PARAMS); registry/golden DEPLOY deferred (placeholder A, not yet transparently sampled).
import Dregg2.Circuit.Emit.HomomorphicDigestEmitProd -- LAW #1: PRODUCTION-width (n=9, birthday-tight) digest fold-step, TRANSPARENT A (splitmix64 of the "dregg" seed, published constants, cross-checked). step_refines_prod: same field-faithful BOTH-directions iff at n=9. Deploy-ready; SIS-hardness at these dims is the estimator's job (SIS-DIGEST-PARAMS: ~9 felts ℓ₂/birthday, 36-40 ℓ∞).
import Dregg2.Circuit.Emit.HomomorphicDigestChainRefine -- LAW #1: closes the digest soundness CHAIN — chain_refines_model proves the emitted fold-step trace's final PI ≡ HomomorphicDigest.digest over the WHOLE history (descriptor Satisfied2 row-leg ⟹ the model, over ZMod q, via induction on step_refines). Honest perimeter: turn-identity/witness-gen + MSIS + FRI floors named, not discharged.
import Dregg2.Circuit.Emit.MerkleMembership4aryWideEmit -- LAW #1 · felt-width E2 #9: WIDE 8-lane-node-fold membership, ADDITIVE beside the deployed 1-felt descriptor (staged-additive-then-cutover). Genuine node-fold widening (node8W = A16(A16(c0‖c1)‖A16(c2‖c3)), 8 lane-wise continuity, leaf+root both 8-felt — no projection masquerade). interior_forge_narrow_admits_wide_refuses: the narrow lane-0 fold ADMITS a lane-0-colliding interior forge (equal parents, free); the wide fold refuses OR hands back a NAMED Coll8 chip collision (extraction-as-data, not a vacuous ∃). Rust repoint + golden regen + vk rotation = deferred coordinated deploy.
import Dregg2.Circuit.Emit.FaithfulNoteSpendDescriptorPlan -- exact dark note spend: hidden owner/nonce/randomness/key, FNO2 derives the shielded owner address from the spending key, exact FNC2 commitment→faithful depth-16 note-root membership, exact FNF2 public nullifier, public value+asset and host-planned successor-root binding. Uses the full 16-state Poseidon2 bus (50 permutations/row), canonical digest8↔16-u16 packing, in-AIR 16-row depth counter; no public leaf/commitment.
import Dregg2.Circuit.ExactNullifierAafiPlan -- exact tagged BOT/REAL/TOP + raw-16×u16 indexed-nullifier semantics: full FNF2 raw domain, pointer bracket, sorted insert/double-spend refusal, append-order physical update, two fixed depth-16 arity-4 root rewrites, and authenticated base-4 physical indices
import Dregg2.Circuit.Emit.ExactNullifierAafiDescriptorPlan -- Lean-authored FNSP-v3 plan: concrete domain-separated state16 tagged linked-leaf/node schedules, canonical endpoint payloads, tag-dispatched u16 lex bracket, shared predecessor prefix, two-path atomic weld, count↔append-path radix chain, exact terminal-full count, BOT-only genesis, and prior/post FNS3(root8,count4) commitments; artifact/runtime cutover remains gated
import Dregg2.Circuit.Emit.ExactNullifierAafiRotatedStateWeld -- staged exact state-boundary weld: FNS3 replaces the rotated faithful-8 nullifier group, wide BEFORE/AFTER commits replace caller checkpoint PIs, the 179-cell input payload is carried, and the other 171 output cells are preserved; runtime/VK remains gated on witness/refinement cutover
import Dregg2.Circuit.Emit.ExactNullifierAafiRotatedStateRefine -- independent state16/wide refinement: genuine per-row FNS3 lookup executions + sound prover-built state16 table, assembled into the adversarial PI60..75 rotated-boundary apex with all 171 stable payload cells preserved; artifact/VK remains deliberately un-emitted
import Dregg2.Circuit.Emit.ExactNullifierAafiRotatedStateHonest -- first genuine 16-row exact-AAFI witness slice: BOT < REAL(ff..ff) < TOP, count 1→2, append slot/base4 quotient, raw/tagged 16-limb lex gadgets, bit/radix/count/depth continuity, and 64 real range-table lookups; remaining hash/path/FNS3/rotated-wide composition stays named
import Dregg2.Circuit.Emit.ExactNullifierAafiRotatedStateHashPathHonest -- exact-AAFI honest hash/path continuation: fills the genuine state16 schedules and authenticated sparse path rows beyond the first algebraic witness slice
-- Dregg2.Circuit.Emit.EmitAllJson / EmitGraduate EXCLUDED: pure `def main` JSON-emitter
-- executables (zero theorems / zero hygiene pins); their top-level `main` collides with
-- Dregg2.Apps.AgentOrchestration.main already in root. Nothing to enmesh.
-- Dregg2.Claims EXCLUDED: it imports `Dregg2` (the root) as a corpus-wide CI pin-net, so
-- enmeshing it here is a build cycle. It remains a separately-buildable top-of-graph target.
import Dregg2.Distributed.CrashRecovery
import Dregg2.Distributed.FinalityGate
import Dregg2.Distributed.MembershipSafety
import Dregg2.Exec.CapTPConsentLace
import Dregg2.Exec.CapTPHandoffSound
import Dregg2.Exec.CapTPSettlement
import Dregg2.Exec.CapTPStoreForward
import Dregg2.Exec.CellCarryG
import Dregg2.Exec.CellGated
import Dregg2.Exec.ConcreteKernel
import Dregg2.Paco.UpTo.Guarded
import Dregg2.Substrate.IssuerLedger
import Dregg2.Verify.ContractG
import Dregg2.Verify.FramesG
import Dregg2.Verify.QueueFactoryProbe
import Dregg2.Verify.TacticsG
import Market -- DrEX (Dragon's Egg Exchange): the multilateral MARKET layer crossBid_needs_market demands — MarketClearing (fair + pool-Converts-balanced allocation over Intent books); clearing_conserves_per_asset (Σin=Σout per asset via KernelBridge.toBal) + exact_clears_iff (an exact book clears IFF pools balance) + ringClearing (the 3-party ring containing the bilaterally-stuck crossBid CLEARS) w/ teeth mint_refused (a Σ-violating book admits NO clearing) + unfair_refused (balanced-but-misrouted allocation refused); n=1 MarketClearing.ofFill recovers the bilateral settle. Market/Fairness adds the FAIRNESS half over Intent/Ring's cycles (conserve+atomic already proven THERE, composed not re-proved): clearing_respects_limits — every participant of a CycleValid cycle stays within its declaration BOTH sides (debited only offerAsset ≤ offerAmount [new] + credited wantAsset ≥ wantMin [Ring's cycle_individuallyRational]) w/ teeth overdebit_refused/wrongAsset_refused (a limit-breaching cycle never FORMS)
import Market.PrivateBookBfvBindingAir -- fixed live N4K4/n4096/three-RNS exact-opening relation: 98,304 canonical coefficient equations, bounded shared noise, full private root/public identity statement, NTT→negacyclic refinement, and Satisfied→ExactPrivateBookBfvOpening.
import Market.PrivateBookBfvSliceDescriptor -- first executable native-field exact-opening slice: all 4096 terms of one production negacyclic coefficient equation, same hidden Dark Bazaar order/root, wide committed public-key row, Lean-emitted HidingFRI descriptor; 98,303 mechanically indexed equations remain.
import Market.PrivateBookBfvNttFamily -- next exact scaling cut: one 2^20×48 per-book NTT family (98,304 equations; 384× padded-row compression) + reusable 2^18 key certificate, staged odd-NTT semantics, full refinement bridge, and executable q0/q1 multi-coefficient teeth. Production OddNttRefines/butterfly AIR/wgpu lowering remain explicitly named, not assumed away.
import Market.PrivateBookBfvRootOrder -- the three deployed q0/q1/q2 odd-NTT roots are proved to have exact primitive order 8192, not merely the necessary half-power sign; a canonical typed RootTable discharges root identity while the general Fourier orthogonality/refinement theorem remains explicit.
import Market.PrivateBookBfvButterflyAir -- first actual 48-column radix-2^14 butterfly IR2 slice: exact product/add/sub carry chains, canonical q0/N8 schedule, concrete 12-row witness, and conjoined cross-stage multiset/permutation teeth. Production faithful-table LogUp, ingress/normalization, terminal rows, and recursive join remain.
import Market.PrivateBookBfvButterflyFaithful -- committed LogUp/multiset closure for the 48-column butterfly: every boundary table is an exact permutation of both preceding writes and following reads; generic n/logN theorem instantiates production 4096/12, while q0/N8 teeth prove membership-only duplicate-table attacks now refuse. Rust table-faithfulness carrier remains the deployment seam.
import Market.PrivateBookBfvTerminalBinding -- the q0 threshold terminal's product is definitionally welded to one typed (order,ciphertext,modulus,coefficient) projection of the exact spectral trace; changed product/domain attacks make the fused relation uninhabited; odd-NTT and negacyclic consequences are proved without a hash-injectivity hypothesis.
import Dregg2.ForMathlib.BerryEsseen        -- upstreamable: exponential-tilt + exact tilted-tail identity + char-fn Gaussian decay (Berry–Esseen ingredient)
import Dregg2.ForMathlib.GuruswamiSudan     -- upstreamable: GS list-size half (card ≤ y-degree via card_roots') + the multiplicity-m interpolation obstruction for multiset words
import Dregg2.ForMathlib.CertifiedInterval  -- upstreamable: signed rational intervals w/ the FOUR-CORNER product (Mathlib's is cone-only, unsound over ℚ) + outward dyadic rounding
import Dregg2.ForMathlib.IntervalConvolution -- upstreamable: certified interval convolution/power ENCLOSE the exact (icvPow 2304 = the δ R1 object) + tail-bound capstone
import Dregg2.ForMathlib.IntervalFFT          -- upstreamable: certified COMPLEX interval arithmetic + interval DFT/IDFT; fftConvI_contains BRACKETS the exact convolution via the kernel-clean complex DFT convolution theorem (the frequency-domain δ-R1 acceleration)
import Dregg2.ForMathlib.PolishchukSpielman -- upstreamable: L3.2 THE CORRELATED-AGREEMENT CRUX — the Polishchuk–Spielman bivariate divisibility lemma in its Cramer–Nardi-FIXED form (BCIKS20 rev.3 Lemma 4.3), PROVED: gcd-split + Sylvester-resultant count where the Cramer quotient-degree bound legalizes the row operation; + L3.1 leading-coeff degree comparison; ZMod-11 instance fires every hypothesis; kernel-clean
import Dregg2.Games.PrivatePreferenceDescriptor -- fixed N4/K4 private score aggregation: private two-bit ballots + wide blinded commitment -> exact lowest-index aggregate winner; Lean-authored reusable AIR, with the modular-to-integer descriptor lift named honestly
import Dregg2.Games.PrivatePreferenceCellDescriptor -- additive custom-VK door for private preference: exact 27-PI `[old8||new8||session||rule||root8||winner]` ABI; state/app-root welds are fold-owned
import Dregg2.Games.DescentCensusDescriptor -- fixed-eight Descent custody census: eight native fields-root openings + six AIR-counted zone totals, direct custom-VK/fold ABI
import Dregg2.Games.PrivateShuffleDescriptor -- fixed N=8 private shuffle/deal: eight individually blinded arity-16 leaves -> faithful node8 tree root; exact bijection proves no duplicate/no omission, selective depth-3 openings; correctness explicitly does NOT claim unbiased randomness
import Dregg2.Games.PrivateShuffleFairDescriptor -- rejection-sampled commit/reveal entropy -> unbiased rank in S8, with exact permutation and deal-root binding plus explicit restart-bias boundary
import Dregg2.Games.PrivateRaidAssignmentDescriptor -- private N=4 raid-role assignment optimizer: exact admissibility, score maximum, deterministic lexicographic tie break, and Lean-emitted AIR soundness
import Dregg2.Games.AutomataflBraid -- proof-native Automatafl multi-leg braid: reveal/resolve/step seams, functional join, no spurious winner, and explicit collision branch for swapped histories
import Dregg2.Games.AutomataflRules -- ⚑ THE RULES-FAITHFUL AUTOMATAFL SPEC (07-20): Automatafl.lean was mirrored off logic/src/game.rs and never read against the Creator-Approved ruleset; the audit (docs/reference/AUTOMATAFL-RULES-CONFORMANCE-AUDIT.md) found 9 divergences, 6 outcome-changing, 3 that DESTROY PIECES — the smallest firing with ONE move on a 3x3 board. This is the replacement, authored against README §Game Rules + old_python_prototype/model.py. THE TYPE OF A TURN CHANGES: a turn is N ROUNDS over a RoundState (board + conflict markers + locked moves + re-entrants), not `Board -> List Move -> Board`; conflicted coordinates are MARKED and become illegal as source AND destination for ALL, the involved players re-enter, everyone else is LOCKED, and the round RECURSES. The path check is INCLUSIVE OF THE DESTINATION (model.py::CanMove scans the inclusive rectangle) so a non-moving piece on the destination BLOCKS and the mover is replaced at its origin; 2-cycles STAY PUT; empty cycles cannot capture; the automaton square is banned as a SOURCE only; the column rule is SELECTABLE (Column|Row|Freeze). A vacuum CONFLUENCE — which the old spec awarded by MOVE-LIST ORDER, deleting the loser — is now a CONFLICT (DetectAndConflict), which is what makes resolve_perm provable. TWO KEYSTONES, both UNCONDITIONAL: resolve_conserves (piece conservation as a bijection on occupied squares — the old applyMoves_conserves_pieces had to ASSUME rules clause 3.2 as hlandA/hlandB) and resolve_perm (permutation-invariance — the old FairnessObligation was not merely unproven, it was FALSE). SETUP/WIN added (2P = two corners in the same row; 4P = one each; the win fires on the automaton MOVING INTO a corner, not sitting on one). 81 #guards, one per rules clause, carrying the audit's D1/D2/D3/D4a/D4b/D5/D6 witnesses now behaving per the rules; mutation-canaried 3 ways. Automatafl.lean is NOT deleted — the ~24k-line proof tower still imports it; the migration is the next stage.
import Dregg2.Games.AutomataflTermination -- ⚑ M3 OF THE MULTI-ROUND CONFLICT BRAID — THE TERMINATION BOUND (07-23, docs/reference/AUTOMATAFL-MULTIROUND-BRAID-DESIGN.md §5): the conflict re-entry protocol RECURSES (a clash MARKS the conflicted coordinate(s), the conflicted seats re-enter, repeat until clean), and the braid fold needs a BOUND to be a fixed-depth verifiable object. PURE SPEC-SIDE (no descriptor, no AIR — the Leg C AIR stays Lean-authored under Circuit/Emit). roundStep_marks_strict_growth: every .again round STRICTLY grows marks, because cs is disjoint from marks — cs lands inside `candidates all` (move ENDPOINTS) and every move of the round is marks-legal (fresh by roundStep's own moveLegalB filter, LOCKED by the RoundWF invariant, which survives because roundStep DROPS every locked move a new mark touches). The MERGE/confluence clause the design doc flagged as owed is PROVEN, not scoped: unresolved_subset_candidates via stopWalk_mem (a contested LANDING is reached by walking edgeMap, and every edge head is some move's .to). Then marks_length_le_board (marks ⊆ the n² board coordinates) gives againDepth_le — THE FUEL LEMMA M7 CONSUMES: againDepth + |marks| ≤ n² (121 at n=11) — plus runTurn_fuel_bound (a trace longer than n² ALWAYS resolves) and runTurn_none_length_le. NO STUCK STATE: runTurn returns none for exactly one reason, the trace ran out; even an all-illegal round is CLEAN, not wedged. 27 keystones #assert_all_clean; canaries on the audit's own D4b fork and D5 merge witnesses (marks 0→1→2 across two clash rounds, and the strict-growth theorem APPLIED at a real board).
import Dregg2.Circuit.Emit.AutomataflBraidFold -- ⚑ M7 OF THE MULTI-ROUND CONFLICT BRAID — THE `runTurn` FOLD (07-23, docs/reference/AUTOMATAFL-MULTIROUND-BRAID-DESIGN.md §4.3, M7). Authors NO AIR: it COMPOSES M2 (legC_sat_imp_roundAgainN11) and M6 (turn_sat_imp_roundStepMarks_pi) into braid_sat_imp_runTurn at n=11, P=2. The ENGINE is roundStep_agree: roundStep is RoundStateAgrees-congruent (M1's honest relation — literal `=` on marks is FALSE, marks compared up to List.Perm), because MoveLegal reads marks only through `∉` (Perm-invariant) and RoundWF only through Nodup, and every board read bottoms out in cellAt/size/automaton (BoardAgrees-congruent: carAt/blockedB/edgeMap/landMap/movers/landBad/unresolved/clashCoords LITERALLY equal, resolveMoves/automatonStepCfg/raycastFuel cell-agreeing boards). The fold (runTurn_conflict_fold) threads M2 through the congruence + roundStep_again_wf over the FoldSeam chain of decoded RoundState windows; the clean round (rm_clean_resolves) resolves and M6 pins the final board cell-wise. TERMINATION is M3's againDepth_le (≤ n²=121); the trace is an explicit hypothesis (the light-client fold supplies the k conflict subs + clean subs), never a smuggled fuel bound. Board equality is CELL-WISE (Board carries function fields). Scope P=2 (LEGC_SEATS=2); the clean round's only residual is hseat0 (the Leg-S who-routing residual, §9.2). 15 keystones #assert_all_clean {propext, Classical.choice, Quot.sound}; no sorry/native_decide/assumed-hypothesis/weakened capstone.
import Dregg2.Circuit.CollaborativeTensorCode -- Dregg-native collaborative tensor-code foundation: separable finite linear encoding, order-independent tensor axes, exact encode/fold commutation, and sharp Shamir-row observation boundary (<t rows support every secret vector; t rows pin it). Information-theoretic support only; randomized-view equality, malicious collaboration, and FS/PCS soundness remain above this layer.
import Dregg2.Circuit.ExactFnspV3DurableAuthority -- durable exact-nullifier authority laws over the existing FNS3(root8,count4) semantics: dense append replay, stale-CAS/forged-successor refusal, exact generation/count advance, successor FNS3 recomputation, and deterministic unique replay. Root replay remains the explicit Rust↔Lean correspondence seam; no second hash implementation.
import Dregg2.Circuit.ExactFnspV3ReceiptFrame -- typed exact-FNSP receipt architecture: separate full-turn and proof-local commitment/link domains, three-way continuation weld, canonical FNS3 points, and globally keyed exactly-once consequence consumption
import Dregg2.Circuit.ExactFnspV4ConsensusEnvelope -- signer-independent activation/frame core identity, local-envelope noninterference, fixed-core envelope soundness, and atomic first-activation+accepted-frame laws; collision resistance/signature verification are explicit assumptions and no quorum construction is claimed
import Dregg2.Circuit.ShieldedExactApexV4 -- one hidden opening feeds the full nullifier, wide value/asset binding, per-asset conservation, fixed market rule, exact outputs, FNI4 append, and FXC4 consequence; pinned-VK/HidingFRI knowledge soundness remains an explicit uninhabited seam
import Dregg2.Circuit.Emit.ShieldedWholeNoteSwapSubstrateDescriptor -- explicitly NON-APEX fixed-100-PI barter substrate: one opening drives canonical full-nullifier packing, wide binding, whole-note swap, output root, and binary AAFI32/FNS4; FWS1 is schema-separated from semantic FXC4, whose 19 Dark-AMM + 27 ring lanes/rule are absent, alongside the four explicit witness welds
import Dregg2.Circuit.ShieldedV4Finalization -- one atomic shielded-v4 finalization joins the shared-witness apex, signer-free receipt core, authenticated causal time, FNS4/output-note installation, and terminal ACK; local envelope/clock noninterference + rejection/retry/fatal no-install teeth; #assert_axioms-clean
import Dregg2.Metatheory.DirectLogicArithmetization -- corrected Gabbay matrix compiler + field projection + private interpolation/public direct-table live instances; finite-signature FOL IR-v2 and four production DREGG public decision ledgers; live degree-2 BoolGraph optimizer certificates; four-mode indexed presentation compiler with explicit conversion/glue cost; finite HOL, family-polynomial Yoneda, the finite-set uniform-width no-go, fibred evidence CCC, and intensional strong-CCC compilation; stored-opening-bound proof/BFV plans with exact zero-observation accounting plus the B=8 symmetric-root/CSE schedule (11 rather than 15 ciphertext multiplications at unchanged depth); live public interaction-receipt IR through depth 14 with arbitrary-trace soundness and typed-syntax-erasure obstruction; categorical coverage/query factorization; robust transcript descent; and effective assembly covers/regular images. Boundaries remain explicit: finite grounding may expand exponentially; workload atoms remain outside their Boolean skeletons; Gabbay public direct tables reveal every cell and omit interpolation; family Yoneda is hom-set-indexed, not one uniform succinct backend; representation optimality is finite-list-relative and glue is not free; the B=8 specialization has exact plaintext semantics and real BFV execution on the depth-oriented parameter set, but no general BFV noise theorem or distributed-key execution; query crypto/FS/FRI reductions remain external; assembly coverage is not the exact completion or query soundness; no general FHE security/noise, runtime-decoder refinement, prover-throughput, or finality theorem.
-- ─── Rooted orphans (built green but previously reachable from NO default lake target, so
--      their #assert_axioms / #guard checks never ran in CI). Registered here so `lake build`
--      compiles them and a downstream regression goes red instead of silently rotting. ───
import Dregg2.Circuit.Emit.AdjacencyMembershipEmit
import Dregg2.Circuit.Emit.BlindedMembershipWideEmit
import Dregg2.Circuit.Emit.BoundPresentationWideEmit
import Dregg2.Circuit.Emit.HeapLeafWideEmit -- the WIDE (17-felt) heap-leaf absorb schema addr8‖value‖nextAddr8 on the shared chip + Lex bracket. ⚑ §RomSuccessor (07-24): EXPORTED binding = wideLeaf_binds_rom (identity carrier over the truncated triple, romCarrier_binds/keyedRom_hard) + wideKeysTamper_binds_rom (sort-key/value/pointer tamper priced) + wideTwins_defanged; wideKeys_bound_or_collide DELETED (zero consumers); heapLeafWide/wideOpen8 `_or_collides` DEMOTED to skeletons (wideOpen8's successor = leaf carrier ⊕ RomMerkleOpening union bound, named in-file, not yet landed).
import Dregg2.Circuit.Emit.LexCompare8Emit
import Dregg2.Circuit.Emit.ShieldedSpendDescriptor
import Dregg2.Circuit.Emit.ShieldedValueLinkDescriptor
import Dregg2.Circuit.ShieldedSpendPortDischarge -- spend_circuit→Lean port, security core: the two NAMED hypotheses of the shielded falsifier proofs DISCHARGED against the emitted AIR objects. §A #15: emitted_leaf_isCommittedNote / emitted_accept_is_committed prove Satisfied2 shieldedSpendDesc ⟹ (accepted leaf IS a genuine committed note — the C6 value-theft tooth, IsCommittedNote — ∧ chain folds to the COMMITTED-accumulator PI ∧ no wire-root decouple), WITH NO AccumulatorSound hyp: the emitted C6a/C6b deliver directly what ShieldedMerkleRootPin.pinned_accept_is_committed could only assume. Hair realizes the abstract per-level fold as the emitted lkParent site (emitted_fold_step_row0); NoteAccumulatorCR names the residual ∀-leaf CR floor (deferred fold-walk + Poseidon2 CR); pin_accept_is_note_committed composes the abstract keystone at the faithful realization, floor NAMED. §B #16: emitted_conserved_is_leaf_bound proves the conserved value (valAt = rowAt·cVAL) IS the cell hashed into leaf_commit & value_binding on every row (leg=leaf=ONE in-AIR cell ⇒ genuine_note_inflates unrepresentable); emitted_minted_equals_bound_spent = conservation_holds at that cell; emitted_linkHolds proves ShieldedValueLinkPin.linkHolds (the deployed-omitted legValue=leafValue) by construction. Non-vacuous: discharge_fires_on_witness / link_discharge_fires_on_witness fire on the emit modules' explicit satisfying traces. Residual: nullifier end-to-end, VALUE_BITS ℤ-lift, PQ/wide #17, Rust producer+VK. #assert_axioms-clean.
import Dregg2.Circuit.ShieldedSpendPortResidual -- spend_circuit→Lean port, RESIDUAL soundness downstream of the security core. §A nullifier double-spend: emitted_nullifier_is_committed_note_nullifier (the published nullifier PI ≡ hash_fact(leaf_commit,key) with leaf_commit a genuine IsCommittedNote + current≡leaf_commit — bound to the ACTUAL spent committed note, not a free value), emitted_nullifier_determined (replay ⇒ same nullifier cell, the detection precondition), emitted_nullifier_double_spend_refused = THE COMPOSITION with the landed Exec.NullifierAccumulator (8-felt sorted-tree): a second spend of the same note publishes a nullifier with NO NfAccWitness (present_no_witness) — in-AIR determinism ∘ set non-membership, double-spend unrepresentable (accumulator COMPOSED, not re-proved). §B NoteAccumulatorCR fold-walk: emitted_fold_step_all_rows (every parent = Hair hash current level, generalizing emitted_fold_step_row0) + emitted_chain_continuity (current[i+1]≡parent[i] mod p) ⇒ emitted_membership_chain (the per-row C3/C5 chain IS a foldPath: leaf IsCommittedNote, each step ≡ Hair, final ≡ committed-root PI); noteAccumulatorCR_of_hashFloor REDUCES the ∀-leaf floor to the SINGLE Poseidon2-CR floor FoldReachIsMember (bounded, SpineCommits8/Compress8CR shape) + honest members_are_notes — no longer an unbounded per-leaf assumption. §C mod-p→ℤ range-lift: emitted_conservation_int lifts conservation_holds (Σvalue≡Σout mod p) to ℤ equality under the NAMED no-wraparound bound (VALUE_BITS range gadget = named next lane). §D #17 VERDICT: BOTH a structural port (widen value_binding to 8-lane via chipLookupTupleN/ChipTableSoundN + multi-limb conservation — Lean-authorable, named next lane) AND the deploy (retire Ristretto commit_hidden_asset DLog → ring-clearing apex, Rust+VK regen — the PQ cutover). Non-vacuous (fires on the emit modules' witnesses). #assert_axioms-clean; crypto floors NAMED never axiomatized.
import Dregg2.Circuit.ShieldedSpendFoldReachRealized -- spend_circuit→Lean port, the §B FoldReachIsMember residual REALIZED over a REAL committed accumulator. CommittedChain = the inserted note's full preimage + committed path, chainRoot COMPUTED by folding the data (never assumed); fold_meet = the unconditional walk (two folds meeting either thread through one another or hand back HairBreak — a NAMED collision of the emitted arity-5 hash_fact site, the Compress8CR family shape); foldReach_trichotomy ⇒ foldReachIsMember_realized: FoldReachIsMember holds over chainMember ∪ {raw value} from TWO standard named floors (HairCR fact-site collision-freeness + BaseAtomPreimageFree — both 2^15.5/2^31-priced at the 1-felt width, the #17 residual); noteAccumulatorCR_realized lands NoteAccumulatorCR through the landed reduction. TWO PROVED FINDINGS: (1) NO leaf/node domain separation — the C6 note shape IS the fold shape, so every committed chain extends DOWN through the note's own preimage: foldReachIsMember_forces_raw_atom / noteAccumulatorCR_forces_raw_atom_note prove the ∀-path floor FORCES the raw value field into the member set (the ∀-path form overreaches; the bounded break-game is the true crypto content); (2) the Hair convention (current-first, pos-absorbed) is a CHAIN commitment — two leaves under one 1-felt root is itself a break; the MULTI-note set composes at the landed 8-felt accumulator instead (emitted_accept_spends_note_in_committed_set: capstone ∘ SpineCommits8/keysOf8). FIELD-REDUCTION IDENTIFICATION: emitted_chain_is_exact_foldPath — on the canonical-BabyBear-rep trace (CanonicalCells/CanonicalHash, denotation facts) the emitted mod-p C3/C5 chain COLLAPSES to the exact-ℤ foldPath = pi[committed_root] the accumulator consumes. CAPSTONE emitted_accept_is_committed_member_or_break: a satisfying trace against a real CommittedChain spends a committed chain value, or exhibits a named base-atom preimage, or a HairBreak. Non-vacuous (fires on the emit module's zero witness); #assert_axioms-clean; floors NAMED never axiomatized.
import Dregg2.Circuit.ShieldedValueRangeDischarge -- spend_circuit→Lean port, §C VALUE_BITS range gadget DISCHARGED: shieldedValueLinkRangedDesc = the value-link descriptor + two per-row 27-bit range LOOKUPS (value/out_val ∈ [0,2^27)) against the EXISTING LexCompare8 .custom LEX_RANGE_TID table (rangeRows 27 content contract) — zero new columns, range-by-lookup reused, no bit-decomposition re-authored (Rust twin: shielded_ring_clearing_nleg_air.rs VALUE_BITS=27, ring_no_wrap_ok). ranged_satisfies_base (constraint-superset transport ⇒ all value-link theorems apply) + emitted_values_in_range (every row 27-bit-bounded) + emitted_prefix_sums_bounded (≤15 rows ⇒ both prefix sums in [0,p): 15·2^27 = p−1) ⇒ emitted_conservation_int_ranged = THE DISCHARGE: Σvalue = Σout_val in ℤ with emitted_conservation_int's named no-wraparound bound now PROVED from the emitted object; remaining hypotheses STRUCTURAL not crypto (range-table content contract — rangeRows definitionally [0,2^27), ChipTableReduction — + build-time clearing width ≤15). LOAD-BEARING CANARY: wrapMintTrace mints p units disguised as 0 (field-conserving, p≡0) — wraparound_mint_accepted_by_base proves the PLAIN descriptor ACCEPTS it, wraparound_mint_refused_by_ranged proves the ranged descriptor REFUSES it; the gadget is provably the thing standing between field conservation and an over-mint. Non-vacuous (honest 5+3/4+4 witness satisfies ranged; ℤ-lift fires 8=8); byte-pinned golden. Residual still open: §D wide value_binding + Ristretto-retire deploy, §B FoldReachIsMember floor, Rust producer/VK regen. #assert_axioms-clean.
import Dregg2.Circuit.Emit.ShieldedWideValueLinkDescriptor -- residual #17 STRUCTURAL half: value_binding widened 1-felt→8-felt, Lean-authored AIR (shieldedWideValueLinkDesc, "dregg-shielded-wide-value-link-conserve::v1", width 30, 9 PIs). Faithful multi-limb inputs: value/asset each 4×16-bit limbs RANGE-CHECKED in the descriptor + recomposition gates pin the compatibility felts (u64_recompose shape, wide_value_binding.rs); the 8-lane wide binding is a chipLookupTupleN site (chip_lookup_sound_N lever) over [DOMAIN "VBN0", limbs, randomness, 0] (arity-11 chip discipline), ALL 8 lanes PI-pinned; leaf_commit stays the narrow fact site over the SAME recomposed cells with hash = headHash permOut (lane 0 of the SAME squeeze — chipTableSound_of_soundN bridges wide→narrow table soundness). KEYSTONE wide_value_link_bound = value_link_bound re-derived at 8 felts (lanes forced + leaf forced + recomps + 16-bit-canonical limbs + all pins); wide_conservation_holds/wide_mint_unbalanced_unsat (Σ over the recomposed multi-limb value); wide_decoupled_unsat. THE CR STORY, vacuity-doctrine-honest: narrow_binding_collision_exists PROVES (pigeonhole, zero crypto) EVERY BabyBear-bounded 1-felt binding has two distinct canonical openings colliding (the ~2^15.5 birthday surface — the 1-felt floor is UNSATISFIABLE, not weak); the NAMED wide floor WideBindingCR quantifies over CANONICAL openings only (~2^159 domain vs ~2^248 range — NOT counting-refutable, contrast deleted-FALSE Poseidon2WideCR) and is INHABITED (wideBindingCR_satisfiable via packPerm); wide_binding_binds = the payoff (equal published 8-felt binding ⇒ SAME u64 opening ⇒ equal conserved value); birthday_collision_no_longer_forges composes both (the collision pair exists for every legacy binding, the wide binding separates it). Non-vacuous: honest balanced 2-row witness satisfies (honest_balanced_satisfies, genuine chipRowN table); mint_refused/decoupled_refused/birthday_tooth_fires bite concrete forgeries. Residual NAMED: Ristretto-retire deploy (#17's other half, Rust+VK), wide-leaf-tree precommit, VALUE_BITS ℤ-lift, Rust producer/emit wiring + golden regen. #assert_axioms-clean.
import Dregg2.Circuit.ShieldedWideJoinPin -- C5 DARK-VALUE v4 (ring↔wide sidecar decouple): the exact-spend path ties the wide full-u64 carrier (wide_value_binding.rs, 16-lane node8) to the ring/conservation proof (RING_LEG_CLAIM_LEN=3 claim [nullifier,merkle_root,value_binding]) ONLY by felt-equality on the ~31-bit legacy_binding (apply.rs:1723-1743 both reconstruct from input.legacy_value_binding) — NOT a same-opening (HORIZONLOG 07-22). FALSIFIER narrow_join_admits_dark_value_decouple / dark_value_decouples: whenever the squeeze has a value-distinct collision (demo_narrow_collides: the modular legacy_binding DOES — 1%5=6%5), the ring conserves o₁.value while the wide carrier advertises a DIFFERENT dark value o₂.value and the narrow join ACCEPTS. LAUNDER exposed join_still_decouples: even with the wide carrier internally faithful (WideCarrierFaithful = Injective, the honest reading of wide_value_binding.rs) the NARROW join still decouples — the ring never consults the 16 wide lanes. FIX A shared_witness_forbids_decouple (one opening cell feeds both ⇒ same value, decouple unrepresentable — "share one witness"). FIX B wide_join_forces_same_opening / wide_join_same_value / wide_join_rejects_the_falsifier (join on the injective WIDE bind ⇒ same opening, under the wide-hash CR floor NAMED as Function.Injective — "faithful full-width commitment"). HONEST OBLIGATION CrossSchemeSameOpening / cross_scheme_join_needs_argument: keeping two schemes (Ristretto vs node8) leaves a genuine same-opening ARGUMENT, a NAMED crypto obligation dispatched by Fix A collapse or Fix B injectivity, never wiring. Binds abstract (holds for real Poseidon2/BabyBear squeeze); non-vacuous #guards; byte-safe (touches no deployed code/descriptor). C5 twin of ShieldedValueLinkPin lifted to the ring↔wide join; InterfaceIdWidth narrow_conflates/wideId_injective shape. #assert_axioms-clean.
import Dregg2.Circuit.ShieldedOnRampPin -- ⚑ THE SHIELDED VALUE ON-RAMP boundary (DESIGN-shielded-value-on-ramp §4's named next byte-safe lane): what Shield/Deshield must enforce, why today's append cannot, and the §5.1 value-aliasing tension the design left open. §1 BOUNDARY MODEL: Shield-A moves one cleartext note of u64 value v out of note_commitments and mints the Poseidon2 noteLeaf = hash_fact(v,[asset,owner,rand]) into note_shielded with v PUBLIC at entry; shield_leaving_eq_entering states the obligation exactly (leaving cleartext = entering shielded = the public v, as ℤ), step_conserves_supply / run_conserves_supply prove the ℤ supply invariant over any shield/deshield run, and modp_identity_is_not_the_obligation shows why ℤ and not mod-p (0 and p are congruent, both u64, not equal ⇒ a mod-p boundary identity admits a p-unit mint). §2 THE FALSIFIER (premise CONFIRMED at source, apply.rs:1849 appends ShieldedNoteCommitment(leg.commitment_bytes) verbatim; GATE 1 constrains only the INPUTS vs the wire merkle_root and GATE 2 only the Pedersen/Bulletproof shape of the output legs — nothing puts them in any note-commitment image): deployed_append_is_free (the prover can place ANY value in the set) ⇒ deployed_append_unsound (AccumulatorSound is FALSE for the post-append root, witness = that very leaf with a genuine path) and ★ deployed_append_unsound_today with ZERO hypotheses at deployed params (no Shield effect exists ⇒ the ledger-authorized set is EMPTY ⇒ every element of note_shielded is unauthorized). ★ l4_pin_over_deployed_append_relocates_15 / l4_pin_relocates_15_today: an L4 pin to THAT root is SATISFIED (acceptsPinned holds) by a spend of a note no gate authorized, with accumulator soundness false for the root — the pin holds and the theft still lands, so #15 RELOCATES one level up rather than closing (DESIGN §3.3, machine-checked). §2.1 SHARPENING of the design's own wording (a partial refutation, reported): noteAccumulatorCR_vacuous_of_c6Surjective proves that if the C6 site is mod-p surjective — expected of a real Poseidon2 with 4 free field inputs onto ~2^31 — then NoteAccumulatorCR holds for EVERY root INCLUDING today's prover-written one, so the ∃-image IsCommittedNote CANNOT price the wound (it asks that some opening exist, never that the LEDGER authorized one); the content lives on IsLedgerNote (leaf = commitment of a preimage the boundary gate approved), where the falsifier is unconditional; the literal form deployed_append_breaks_noteAccumulatorCR is also proved under the named non-surjectivity hypothesis it genuinely needs, and falsifier_survives_c6_surjectivity holds both facts side by side. §3 THE FIX SOUND: shieldAppend_is_ledger_note is by construction (no crypto), so shieldA_noteAccumulatorCR discharges NoteAccumulatorCR via the LANDED ShieldedSpendPortResidual.noteAccumulatorCR_of_hashFloor under the SINGLE named Poseidon2 floor FoldReachIsMember (itself realized under HairCR by ShieldedSpendFoldReachRealized) — the pin genuinely closes #15; shieldA_pin_spends_authorized gives the strictly stronger LEDGER-AUTHORIZED conclusion; shieldA_pin_closes_15 composes with the landed ShieldedSpendPortDischarge.pin_accept_is_note_committed; deshield_publishes_an_entered_value is the exit (a Deshield can only publish the value of a note a Shield entered — what makes L4's pin non-vacuous). §4 THE VALUE-ALIASING TOOTH (DESIGN §5.1, "the first thing the Lean lane should model"): the committed cleartext leaf carries split_u64(value).0 = the LOW 30 BITS (effect_vm/helpers.rs:13-17, commitment_set.rs:262) while the shielded C6 opening reduces mod p<2^31 — boundary_images_alias_both_ways exhibits u64 pairs conflated by each image and separated by the other (2^30 vs 0; p vs 0), and ★ aliased_shield_inflates is the concrete inflation: a shield checked on the cleartext leaf's own value column mints 2^30 units from a note worth 0 and the ℤ supply invariant breaks by exactly 2^30. TWO CLOSURES PROVED: cap_closes_clear_alias / cap_closes_felt_alias / cap_closes_aliased_shield (an amount cap v<2^30 makes BOTH images injective, so the aliased check IS the exact check) and wide4_injective_on_u64 (the deployed 4×16-bit-limb u64 encoding, wide_value_binding.rs:37-40, is injective on ALL of [0,2^64) — a complete closure where the cap is partial), with wide4_separates_the_alias biting the concrete pair. Non-vacuous #guards (10); crypto floors NAMED never axiomatized; no sorry/admit/native_decide. BYTE-SAFE: metatheory only — no descriptor, no emit wiring, no deployed byte; does NOT implement Effect::Shield/Deshield (wire+effects_hash+descriptor rung = VK-affecting, ember-gated per DESIGN §4) nor fix the GATE-4 append. #assert_axioms-clean (32).
import Dregg2.Circuit.Emit.PredicatesCompoundWeakCountermodel -- countermodel: CompoundClassified is strictly too weak to imply genuine AND (a Satisfied2-accepted trace refuting the AND functional law)
import Dregg2.Circuit.FriArityForking
import Dregg2.Circuit.FriDeployedRateInstance
import Dregg2.Circuit.FriCloseNUniqueDecode -- ⚑ FRI LADDER L5·R2 — the per-layer RS unique decoder WELDED to the FriSetupK/closeN vocabulary the L5 target (PositiveRadiusTraceDecode) consumes. Two near-definitional bridges: mem_rsCode ↔ (∃ p, natDegree < D ∧ f = evalVec pts p) (0 < D; false at D = 0 where rsCode = {0}) and (disagree f g).card = hammingDist f g (rfl). The weld closeN_rsCode_decode: closeN (rsCode pts D) e f + 2e < N−D+1 ⟹ ∃! codeword within e (uniqueness via near_word_decodes_existsUnique; e = 0 / D = 0 corners handled directly, hypotheses minimal). FIRED at friSetupDeployed (2^24-pt domain, rate 1/8) at the TRUE UD radius e = ⌊(N−D)/2⌋ = 7340032 > 0: all-ones codeword corrupted at coord 0 decodes uniquely (deployed_positive_radius_decode_fires) and the decode is PINNED to the honest word (deployed_decode_pins). SHARP CANARY at e+1 = 7340033: the min-weight geometric-sum codeword (Σ_{s<2^21} X^s, vanishing on the nonidentity 2^21-th roots) split by residue classes gives a word with TWO codewords (0 and geomWord) inside e+1 — deployed_radius_plus_one_not_unique. So the deployed UD radius is EXACTLY 7340032, both polarities on one instance (deployed_udr_radius_sharp). Consumes closeN (link A) — produces no proximity; the n²-loss arity cap (FriDeployedRateInstance §6) untouched. 22 keystones #assert_all_clean.
import Dregg2.Circuit.CorrelatedAgreement -- ⚑⚑ THE CORRELATED-AGREEMENT FORMALIZATION (07-24), aggregator root for the seven-file subtree. UD-regime correlated agreement for RS codes (BCIKS20 Thm 4.1 / Kopparty 2025) — the mathematical keystone of the FRI extractor's DecodedLdtLink residual. L0 scaffolding + unique decoding; L1 Berlekamp-Welch (with the NAIVE solvable-iff-close REFUTED, bw_naive_converse_false); L2 bivariate interpolation via a Cramer engine over F[Z] itself (no fraction field — a bare rank-over-F(Z) argument cannot give the degree bound PS needs) composing end to end through Dregg2.PS.polishchuk_spielman; L4 the collinearity double count, bound proven ATTAINED and the lossless threshold (m-1)(e+1) < |Z| proven SHARP; L5/L6 the target Props proven in the exact shape Interface consumes, classical regime, with the PS regime covering the full UD radius at a higher threshold and the residual band machine-checked to cost under 1.2 bits. tower_far_survival_ca_proven discharges the tower survival with a PROVEN CA at POSITIVE radius (previously only the degenerate r = 0 corner, where CA buys nothing). ⚑ Rooted through ONE aggregator because individual import lines were dropped by contending commits, leaving the subtree outside the build target and outside CI. HONEST SCOPE: theorems about RS codes and the HONEST fold chain — Strategy is instantiated at honestStrategy throughout, so this does NOT make the apex adversary-quantified.
import Dregg2.Circuit.FriDecodedTraceReadout -- ⚑ FRI LADDER L5·R4a — from R2's unique decoded codeword, the TRACE COLUMNS, by low-degree restriction to the trace subdomain. Trace row i pinned at LDE index 8·i (traceIdx/rowPt = ω₂₄^(8i); injective via orderOf(ω₂₄^8)=2^21; traceIdx_range = exactly the 8∣x class; rowPt_pow_traceRows = the order-2^21 subgroup). DIMENSION GATE CONFIRMED: degree bound 2^18·8 = 2^21 = trace length EXACTLY (trace_dims_confirmed), so the 2^21 trace points PIN the interpolant (trace_readout_pins_interpolant) and the readout loses nothing (trace_pins_codeword). decodedTrace (per-column codeword family → MatrixOracle over trace rows) + decodedTrace_col_eq_interpolant (∃! natDegree<2^21 interpolant serving BOTH the full-domain codeword and the trace column via rowPt). Assembly: closeN_unique_trace / colsClose_unique_trace — inside the UD radius 7340032 the (matrix) trace readout is UNIQUE, consuming exactly the ColsClose shape L6's dichotomy delivers. FIRED on R2's corrupted fireWord24 (unique trace = all-ones, interpolant pinned to C 1, corruption REPAIRED in the readout); CANARY at 7340033 on canaryWord: 0 vs geomWord read DIFFERENT traces at row 0 (2^21 ≠ 0 via CharP), so no ∃! — the decode is load-bearing for the readout (trace_readout_radius_sharp = both polarities). R4b still owes: trace columns → TraceWitnessed (VmTrace identification + MainAirAcceptF + legs). 28 keystones #assert_all_clean.
import Dregg2.Circuit.FriDecodedTraceWitness -- ⚑⚑ FRI LADDER L5·R4b — L5 ASSEMBLED: positiveRadiusTraceDecode_decoded / _transferV3 at the deployed friSetupDeployed instance (2^24 domain, rate 1/8, UD radius 7340032). VmTrace identity built unconditional (decodedVmTrace, faithful; hcap now a THEOREM). TraceWitnessed legs: MainAirAcceptF DERIVED via the LANDED ALI/OOD chain (verifyAlgo_accept_forces_table_identity → ood_forces_mainAirAccept_field_of_residuals); memory/range/hash legs DISCHARGED; LogUp arm from BusModelOk. The DEEP-ALI half is the NAMED residual DecodedLdtLink (contains NO hood/MainAirAcceptF/hbus/Skolemized trace — a genuine load-bearing hypothesis: dropping its layout eq breaks the hAir derivation): the accept ⟹ AIR-constraints-on-the-DECODED-trace linkage, whose research core is correlated-agreement quotient consistency (FriCorrelatedAgreementSharp.CorrelatedAgreementLine — reduction proven, the LINE itself unproven — the BCIKS / HyperFRI frontier). Residuals EXACTLY {Poseidon2SpongeCR, DecodedLdtLink, DecodedBusLink}. FIRE+BITE; #print axioms clean (verified). 24 #assert_axioms.
import Dregg2.Circuit.FriDeepQuotientRlc -- ⛑ FRI LADDER L5·R4b SUB-PIECE 1 — the DEPLOYED DEEP-quotient α-RLC input reduction, MODELED (the seam FriBatchedOracle.lean:50-57 and DeployedTraceExtract.lean:414-419 both name as deliberately unmodeled). Models fri/src/verifier.rs:617-640 at rev 82cfad73 LITERALLY: roStep/reduceOpening IS the imperative (alpha_pow, ro) fold and reduceOpening_eq proves it equals (α^len, Σ α^i·t_i). Typed as the deployed verifier types it — committed matrix over the BASE field F, reduced word over the degree-4 EXTENSION E ([Algebra F E]), since p_at_x : Val becomes Challenge at the subtraction. PROVEN: deepQuot p z := p /ₘ (X - C z) with the factor theorem (deepQuot_spec), natDegree drop (deepQuot_natDegree), and deepQuot_eval_deployed — the evaluation IS (p(z)-p(x))·(z-x)⁻¹, the deployed .inverse(); rlcQuotPoly degree < k-1; friInputWord_eq_evalVec + friInputWord_mem_rsCode (honest OOD values + codeword columns ⇒ the FRI input word IS a codeword); friInputWord_closeN (ColsClose form: numCols·d-close, union bound); deepQuotWord_agree_card_le + oodValue_correct_of_close (THE per-column DEEP soundness fact — a wrong opened value caps agreement at m, so inside the radius the claimed OOD value is FORCED true). NAMED RESIDUAL (hypothesis, never axiom): RlcDistributes — BCIKS20 correlated agreement for the power-RLC family, stated in the shape of FriCorrelatedAgreementSharp.CorrelatedAgreementLineAt; oodValues_correct_of_rlcDistributes derives sub-piece 2's conclusion from it ALONE, and rlcDistributes_one_column PROVES the shape at numCols=1. FIRE on ZMod 17 (4th roots of unity, out-of-domain z=3): deepQuot (X²+2X+3) 3 = X+5 as a genuine polynomial, eval and deployed division both 9, RLC and loop both 11. TEETH: fire_wrong_ood_not_close (§4 bites) + rlc_single_challenge_no_distribution (the single-α converse is FALSE — why CA is needed). NOT wired to friSetupDeployed: that in-tree code is BASE-field while the deployed word is extension-valued (named gap). FIRE also shows the §5 keystone's whole hypothesis bundle is inhabited (fire_oodValues_correct). sorry-free, no axiom, 42 keystones #assert_all_clean.
import Dregg2.Circuit.FriDecodedDomainRebase -- ⛑ FRI LADDER L5·R4b SUB-PIECE 3 — the ω₂₇-vs-ω₂₄ DOMAIN RE-BASE, closed. VERIFIED mismatch: the OOD modeler reads trace row i at ω₂₇^i (TraceColumnInterp.rowPt:37, domainSize 2^27) while the FRI decode reads it at ω₂₄^(8i) with ω₂₄ = ω₂₇^8 (FriDecodedTraceReadout.rowPt:115, FriDeployedRateInstance:412) — i.e. ω₂₇^(64i), a genuinely different point (decodePt_ne_interpPt_one), with modeler row 1 in NO decode row (interpPt_one_ne_decodePt) and overlap exactly at 64∣i (interpPt_mem_decodeRange_iff, 2^15 of 2^21 rows), and DIFFERENT row-shift multipliers ω₂₇ vs ω₂₇^64. IT RECONCILES: stride 64, 64·2^21 = 2^27 = domainSize with ZERO slack (stride_fills_domain), the decode subdomain is the stride-64 sub-family of the modeler's domain (decodePt_eq_interpPt), and the change of variable is X ↦ X^64 — (p(X^64)).eval (ω₂₇^i) = p.eval (ω₂₄^(8i)) exact and hypothesis-free (rebase_eval_basePt / rebase64_eval_interpPt), degree transporting natDegree p < 2^21 ⟹ natDegree p(X^64) < 2^27 (rebase64_natDegree_lt), faithful on the decode's low-degree witnesses. CONSUMER DIRECTION (decode ⟶ modeler, what DecodedLdtLink needs): colPoly of the decoded VmTrace IS the Lagrange interpolant, on the modeler's node family, of the re-based decode interpolant (colPoly_decodedVmTrace_eq_interpolate), pinned as the unique natDegree<2^21 such polynomial, and stated at the extraction object itself (decodedTr_colPoly_rebase). EXACT ALTERNATIVE: re-basing the modeler's NODE FAMILY to stride 64 makes the two column polynomials literally EQUAL (interpolate_stridePt_eq_self / interpolate_decodePt_eq_self) — a live definition change for TraceColumnInterp.rowPt, since that development uses rowPt only through injectivity on the row range. HONEST LIMIT: with the current rowPt the agreement is exact ON THE ROWS only; the difference is Z-divisible (rowVanishing_dvd_of_rows_zero) giving p(ζ^64) = colPoly(ζ) + Z(ζ)h(ζ) (eval_correspondence_of_factor), and is NONZERO for natDegree p ≥ 2^15 (rebase64_ne_colPoly) — so sub-piece 2 must take its OOD point in the decode coordinate or adopt the stride-64 family. Does NOT close DecodedLdtLink: sub-pieces 1 and 2 remain. FIRED in ZMod 17 (ω=3 order 16, stride 4, X²+1: points 3 vs 13 DIFFER, both readings compute to 0 / 2). 39 #assert_axioms clean.
import Dregg2.Circuit.FriEpsFriComposedAdversary
import Dregg2.Circuit.PassiveDescriptorOptimization
import Dregg2.Metatheory.EffectVmDescriptor2PassiveOptimization
import Dregg2.Metatheory.TypedLinearPredicateDescriptorIR2
import Dregg2.Metatheory.TypedLinearPredicateOptimizerCost -- CASHES the cost check the deep read found uncashed: optimize_descriptor_cost_nonincrease is the ∀-program descriptor-level corollary (descriptor_exact_resources ×2 + optimize_cost_nonincrease + the two graphCost bridges), with the SEMANTIC companions optimized_sound/optimized_canonical_iff so the bound cannot be bought by weakening the circuit (an all-.bot 'optimizer' would pass a bare cost bound). Covers the bilinear QuadProgram stack too. STRICT result: descriptor_shareAnd_exact_saving — the factorAnd rewrite's EXACT symbolic saving (graphCost.equations+2 constraints, witnesses+1 columns), mutation-canaried. ⚠ On BOTH production programs the proposer fires NOTHING (#guard (optimize src).1 == src), so §6 gives theorem-backed BOUNDS, not a demonstrated shrink — corrected in-file after a verify caught the overstatement. 20 keystones #assert_all_clean.
import Dregg2.Metatheory.TypedLinearPredicateOptimizedWiring -- WIRES the certified optimizer to the live typed compiler (the deep read found `descriptor` never called `optimize`): Program.optimized + the Holds-agreement via optimize_holds_iff, with sound/canonical_iff/public_sound TRANSPORTED to the optimized descriptor — the payoff of having proven semantic preservation. `descriptor`'s lowering geometry is UNCHANGED (the peephole recognizers depend on it); the optimized path is added alongside.
import Dregg2.Metatheory.RealizabilityLocalRewriteOracle
import Dregg2.Metatheory.DirectLogicDreggProductionPredicates
import Dregg2.Calculus.IntensionalCCCInteractionMultiBlockDescriptorIR2
import Dregg2.Verify.DirectLogicAdversarialFalsifierV2
import Dregg2.Bridge.ConditionalInterchainAdapter
import Dregg2.Crypto.EncodedBindingReduction -- THE REUSABLE `digest binds the object` REDUCTION SPINE: for any KeyedHashFamily and any encoder (the ordered limb list the deployed prover absorbs), the forgery is a first-class Game (encBreakGame), the extractor is a MAP OF ADVERSARIES into that family's collision game (encBreakToFinder), win-preservation is definitional, enc_adv_le is UNCONDITIONAL over ALL adversaries (the class does NOT appear in it), and the conclusion is domination under HashCRHardQuant. the IsPolyTime discharge (encBinding_from_polyTime / _fixedWidth) is DELETED (07-24, floor refuted); the DISCHARGED successor is IN-FILE on the PROVED keyed-ROM floor: encRomCarrier (RomCarrierSites.keyedEncCarrier at the site's key-dependent encoder) + encBinding_rom via flatSite_binds — nothing refutable carried, the fixed-width side condition dissolved into the finiteness of the truncated message shape. §6 adds the UNION BOUND for CHAINED bindings (commit ⟸ digest ⟸ sub-block), turning a three-way `binds ∨ outer-collision ∨ inner-collision` into two extracted finders, two advantages, one floor, one negligible sum. Both poles proved (⊤ FALSE for any compressing primitive, ⊥ vacuous), class inhabited, fail_if_success canary at the wrong finder.
import Dregg2.Exec.SystemRootsBindingReduction -- THE EIGHT CONSENSUS SIDE-TABLE ROOTS, as a SECURITY REDUCTION, now DISCHARGED on the keyed-ROM floor (07-23): the five `SystemRoots` disjunctions (systemRootsDigest_binds{,_fn,_pointwise}_or_collides, cellCommitS_binds_systemRoots_or_collides, cellCommitS_binds_roots_pointwise_or_collides) quantified over SOLUTIONS (a sponge collision EXISTS by pigeonhole, so the collides branch was always available). §1–§5: the forgery as a Game over the deployed domain-separated sponge (sysRootsBreakGame/cellCommitSBreakGame), the three digest-layer disjunctions ONE game projected three ways, the anti-ghost violation IS a win (sysRoots_forgery_is_break), the _advantage_bound forms honest with hEff in the open. §6 PROVES the IsPolyTime floor FALSE at the deployed sponge (sysRoots_floor_polyTime_false_babyBear) — so the old _from_polyTime headlines were VACUOUS and are DELETED, not kept beside their successors. §7 is the successor: sysRoots_binds_rom / cellCommitS_binds_rom / cellRoots_binds_rom on RomCarrierSites' kit — query-bounded oracle-program forgers, extractors mapOut (budget Q) and bindComp (budget Q+2, the chain re-queries the two inner digests), union bound over ONE sampled oracle, floor = keyedRom_hard (PROVED birthday bound), NO floor hypothesis carried. ⚑ The ROM idealisation is a labelled MODELLING step (§7 header): ideal Fin (2^l) digest vs deployed fixed ~31-bit felt (the felt-width wound, carried visibly as the chain's Fin (2^l) digest limb); truncation lossless on deployed in-range payloads (truncRoots_inj, truncRoots_rootList). Teeth: class inhabited with positive advantage, constant answerer admitted-and-defanged, non-negligible forger excluded.
import Dregg2.Circuit.Emit.EffectVmWideCommitReduction -- THE WIDE `state_commit` ANTI-GHOST, as a SECURITY REDUCTION on the grounded floor: the five GENERIC ROOTS of EffectVmFullStateRunnable (wide_binds_or_collides, wide_binds_systemRoots_or_collides, runnable_full_commit_binds_or_collides, wide_rejects_state_tamper_or_collides, wide_rejects_root_tamper_or_collides) were BARE DISJUNCTIONS quantifying over SOLUTIONS — at deployed BabyBear params a sponge collision EXISTS by pigeonhole, so `binds or collides` is satisfiable through the collides branch with binding never holding, and the two _rejects_ teeth have NOTHING BUT the collision disjuncts as their conclusion. Rebuilt on the Crypto.SpongeCarrierReduction spine: TWO deployed carriers — wideAbsorbCarrier (the H4-of-H4 over the 12 absorbed state-block columns + the sysRootsDigestCol felt) and wideFullCarrier (carrier slot resolved to the systemRootsDigest of the 8-root sub-block) — whose extractors REUSE the file's own peels (Poseidon2Binding.group4Find, Exec.SystemRoots.rootsCollFind) composed into ONE total function (wideFullFind), so the THREE-way disjunction becomes ONE extracted finder rather than a union bound (no tightness lost). Faithfulness by rfl (wideAbsorbEnc_eq_wideCommitOf, wideAbsorbCarrier_find_eq_wideCollFind: the reduction's internal witness IS wideCollFind); the deployed anti-ghost violations ARE wins (wideAbsorb_forgery_is_break, wideFull_forgery_is_break, wide_state_tamper_is_break, wide_root_tamper_is_break, and runnable_full_forgery_is_break generic in St and the RunnableFullStateSpec — the highest-fanout site); structural injectivity WideCols.eq_of_blocks is about the LIMB LAYOUT, never the hash. Headlines wideCommit_binds_from_polyTime / wideFullState_binds_from_polyTime discharge hEff at Eff := IsPolyTime via CostTactics.poly_time, the only per-site input being the 4-felt block arity / 8-root width (PROVED). Both poles priced (top FALSE at BabyBear, bot vacuous), class inhabited, fail_if_success canary + positive pole, reflexive-answer teeth, and a broken-sponge reachability tooth so the game is two-valued. The five disjunctions are DEMOTED in place to the exact-Prop skeleton the win relations rest on (~40 per-effect wide keystones instantiate them).
import Dregg2.Circuit.PeepholePassCertified -- the CERTIFIED peephole pass (R1 asserted-frontier zero-test elision ∘ E1 dead-column deletion): rwDescriptor is a record-UPDATE of the REAL emitted EffectVmDescriptor2 (stand-in trap avoided), with SecurityRefinement/CompletenessPreservation/SatisfiabilityPreservation PROVEN (rw_security/rw_completeness/rw_preservation) and composed with the already-certified E1 via preservation_comp (peephole_preservation, conditional on E1's cheap transitionCeilingOk cert). Satisfied2-preservation is MACHINE-CHECKED, not prose. ⚠ R1∘E1 recovers only the equality atoms (~3% on the field-delta pilot); R2 (booleanity→quadratic, reaches hand-parity) is proven locally correct (pairGate_iff_or) but its lift to a certified pass + the parity-on-certified-output measurement is the in-flight integration.
import Dregg2.Circuit.PeepholeR2Certified -- R2 (booleanity → quadratic) as a CERTIFIED pass, single fired site: rwR2Descriptor is a record-update of the real emitted descriptor; rwR2_security (SecurityRefinement, via a canonical r2RebuildRow witness rebuild of the elided or-node+zeroTests) + rwR2_completeness + rwR2_preservation PROVEN, composed with E1 (peepholeR2_preservation via preservation_comp). Local gate-correctness pairGate_iff_or / mul_eq_zero over BabyBear (field-ness load-bearing). ⚠ SINGLE-SITE: the full pilot needs 32 fires composed into one object (the iteration/batch wall) — that composed-output-IS-35/33/30 result is the in-flight batch reformulation, NOT this module.
import Dregg2.Circuit.PeepholeBatchCertified -- THE FINALE: certifiedPeephole is the OUTPUT of a certified batch pass (R1+R2 fired all-at-once ∘ E1), and it MEASURES 33/35/30 = EXACT hand-parity with FieldDeltaRangeEmit — measured on the SAME object certifiedPeephole_preservation proves SatisfiabilityPreservation for. Proven object == measured object: a shipped circuit re-derived from spec, optimized by a CERTIFIED pass to hand-parity, carrying a machine-checked preservation proof the hand circuit never had. Cost-parity (not byte-identity: (x+0)(x-1) vs (x)(x-1), E1 column renumber). 12 keystones #assert_all_clean, no sorryAx. Residual: general EffectVmDescriptor2→_ recognizer (this batch is specialized to the pilot via the proven pilot_holds_iff)
import Dregg2.Circuit.PeepholeGeneralPass -- GENERALIZES the peephole recognizer off the pilot: recognizePeephole reads the constraint SHAPES (zeroTest .mul(.loc x)(.loc out) / or-node WindowExpr tree) in ANY descriptor, with ∀-bridges recognize_sees_r1_site/r2_site over the whole program family. FIRES + CERTIFIES on a SECOND independent descriptor (second_r1_roundtrip, non-vacuous) as well as the pilot (92 sites). Remaining leg (named): the one-pass-all-sites program-INDEPENDENT SecurityRefinement (recognizer + per-site certified rule = the general half). 9 keystones #assert_all_clean
import Dregg2.Circuit.PeepholeGeneralBatch -- the PROGRAM-INDEPENDENT batch peephole: generalBatchPeephole : EffectVmDescriptor2 → EffectVmDescriptor2 elides the redundant booleanity bit-gate at EVERY recognized zero-test site of an ARBITRARY descriptor; general_batch_preservation is ∀-d (SecurityRefinement+Completeness, composed with E1 = certifiedGeneralBatch_preservation), and pilot_batch_preservation/second_batch_preservation are INSTANTIATIONS (one theorem, two descriptors — 374→312 on the pilot, fires on the independent secondDescriptor). ⚠ Redundant-elision only, NOT full parity: the full x=0-at-accept-frontier collapse is program-DEPENDENT (needs a syntactic accept-reachability scan) and stays pilot-specific in PeepholeBatchCertified — that scan is the named next leg. 16 keystones #assert_all_clean, no sorryAx
import Dregg2.Circuit.PeepholeFrontierScan -- the PROGRAM-INDEPENDENT frontier soundness engine: forced_field_one proves accept-reachability (accept→out=1, and-node→children=1, product→x=0) as PURE FIELD ALGEBRA over an inductive `Forced` certificate; frontier_security is ∀-d (FrontierCertified d → SecurityRefinement), verified to route through NO pilot_holds_iff. The frontier zero-test→raw-affine collapse, certified end-to-end on a real emitted descriptor (atomDesc, depth-0). ⚠ NOT yet general parity: auto-discharging FrontierCertified for AND-node descriptors needs the scan→Forced synthesis bridge (out∈acceptReachable ⟹ Forced), and full 33/35/30 also needs the R2 disjunction batch — both named next legs. 17 keystones #assert_all_clean, no sorryAx.
import Dregg2.Circuit.PeepholeFrontierScanSound -- the scan→Forced SYNTHESIS BRIDGE: acceptReachable_sound proves the syntactic reachability scan is sound w.r.t. the inductive `Forced` certificate (structural recursion on the reach iterate; each step reconstructs a genuine .andL/.andR from real and-edge + sibling-bit-gate membership — sound at EVERY index, never needs the fixpoint reached). Plus ForcedS/forcedS_field_one: the STRENGTHENED certificate (an own-bit-gated child of a forced product node is 1) whose scan reaches BOTH pilot sites where the committed one reaches only 95. ⚠ Concrete instantiation still open: discharging `out ∈ acceptReachableS (kept d)` as a kernel Prop without decide (the 374-constraint scan is the OOM class) is not done for any descriptor — #guards show the mechanism fires, they prove no Prop. 17 keystones #assert_all_clean.
import Dregg2.Circuit.PeepholeEmitMembership -- ⚑ THE WALL CAME DOWN: the first CERTIFIED SatisfiabilityPreservation on a REAL descriptor. pilot_frontier_preservation / second_frontier_preservation are PROVEN Props (not #guards) for the frontier collapse on the field-delta pilot and secondDescriptor, obtained WITHOUT kernel-scanning the 374-constraint list (decide is the 64GB OOM class). The move: forcedS_kept_of_forced — acceptReachable_sound already yields a Forced certificate over the FULL list, and re-running it over the KEPT list can only break on a dropped BIT gate (accept/and gates are never dropped, by rfl), so one side condition (H) suffices; (H) is discharged BY THE LOWERING via emission-inversion + a Formula induction (zeroTest columns are disjoint from op-node columns by allocation range). Falsifier landed as a theorem: andRightOp_atom_and_atom_false — the side condition is refutable exactly where the collapse is genuinely UNSOUND. 32 keystones #assert_all_clean
import Dregg2.Circuit.ListCommitRegrounded -- ⚑ the ListDigestBindsList ENDPOINT re-grounded (the vacuity-elimination port exemplar): S2 listDigest_binds_or_collides is HYPOTHESIS-FREE (equal digests ⇒ equal lists OR a NAMED ListColl = CNColl ∨ LeafColl with distinctness intrinsic + .extracts specs); S3 _of_noColl/_of_noCNColl/_of_noLeafColl restore the OLD conclusion under a per-instance refutable ¬Coll (HONEST-BUT-CONDITIONAL, ~2^15.5 at 1-felt); no strength lost (listDigest_binds_of_carriers re-derives the old statement THROUGH the port); S1 listDigestRom_binds = the keyed-ROM headline over length-bounded felt-lists (keyedRom_hard birthday bound, labelled Fin(2^l) modelling caveat, poles priced pos+defanged). TEETH: canary_drop_moves_digest_or_collides, listDigest_binds_unconditional_false, noColl_satisfiable/listColl_refutable. #assert_all_clean. NOW ALSO: the CUTOVER bridges noCNColl_of_inj/noCNColl_self/noListColl_self (every rewired consumer's floor flows through ONE bridge — the level-2 composition rule).
import Dregg2.Tools.ConePort -- ⚑ the mechanical S3 threader-port METAPROGRAM (the campaign's transform, prototyped): analysis on the ELABORATED TERM (floor binders by constant identity, floor-flow walk of the proof, per-site side conditions instantiated at the EXACT argument exprs, read off the replacement lemma's own telescope); surgery on the proof Expr; VERIFIED before emission (Meta.check + isDefEq + real kernel addDecl) with a MANDATORY delaboration round-trip; emits auditable source. FAIL-CLOSED refusal classes: ENDPOINT (floor applied — needs hand S2), unported-consumer/DAG-order, COMPILER-LIFTED AUX (_proof_N — the bundle case), LEAKY (proof-local binders in the side condition), floor-in-conclusion, partial application, residual floor, kernel-reject, round-trip failure. A port may only make the measurement MORE true.
import Dregg2.Tools.ConePortListCommitRun -- the ConePort DRIVER + pinned MEASUREMENT for the ListDigestBindsList cluster, re-run as a build-time tooth, RE-PINNED POST-CUTOVER: the 13 cut-over theorems REFUSE as not-a-carrier (their floor binders are GONE — this pin IS the standing proof the cutover holds); 3 level-2 threaders PORT through the ONE noCNColl_of_inj bridge AppRule (bridgeRules — the AppRule-from-PortResult composition gap closed by the cutover's uniform bridge; 2-level cone demonstrated in-env every build) + cellCommit_binds_tail = the DAG-order refusal control; endpoint REFUSES as ENDPOINT; accountsComponent/listComponent REFUSE at the compiler-lifted _proof_1 AUX sink; leakyExhibit REFUSES as LEAKY; listDigest_congr = not-a-carrier negative control. Any drift is a BUILD ERROR. CONE_PORT_WRITE RETIRED (the cone was cut over in place and deleted).
import Dregg2.Tools.ConeCutover -- ⚑ the CUTOVER step of the ConePort toolkit (a port only counts when the floor-carrying original is GONE — the additive-regrounding sin, mechanized away): transactional textual surgery, stage verifies EVERY edit (exact match-count, no residual old text) against the real tree before ANY byte is written, ONE failure refuses the WHOLE cutover with nothing touched; apply backs up + writes + retires the additive scaffolding; default mode = the standing SEMANTIC post-state tooth, re-checked on every build over the ENVIRONMENT (each cut-over declaration floor-free in STATEMENT and in PROOF CLOSURE, retired scaffolding absent from the environment, the check modules present, plus type- and proof-term BLINDNESS CONTROLS so a rejector cannot pass vacuously). The TEXTUAL post-state moved OUT of the build (scripts/cone_cutover_textual_check.sh): it reads the tree through IO.FS, which lake's import graph cannot see, so a build answers it from a cached olean — measured 2026-07-25, lake build EXIT=0 while a forced-fresh elaboration EXIT=1 on two real violations. In-place (same name, same conclusion, floor binder → per-instance side condition) is the only import-cycle-free cutover shape, and it keeps every #assert_axioms pin aimed at the now-successor name.
import Dregg2.Tools.ConeCutoverListCommit -- ⚑⚑ THE FIRST REALIZED SURFACE REDUCTION of the vacuity-elimination campaign: the ListDigestBindsList cluster CUT OVER — all 13 ConePort-verified threaders lost their refuted compressNInjective (+listLeafInjective) binders IN PLACE (same names, same conclusions, per-instance ¬CNColl/¬ListColl side conditions at the exact proof arguments); 17 consumer call sites rewired through the noCNColl_of_inj/noColl_of_carriers bridges (which makes level-2 uniformly portable — the composition rule); Circuit/ListCommitPortedCone.lean (the additive scaffolding) DELETED in the same transaction. Standing SEMANTIC post-state tooth on every build; the textual wiring check (the only one that reads Dregg2.lean itself, which no module can check from inside) runs FRESH from scripts/cone_cutover_textual_check.sh. (This block RESTORES the cluster wiring that commit 799b5a6e27 dropped from this file wholesale — the four imports vanished under an unrelated Peephole commit.)
import Dregg2.Circuit.ListCommitCutoverCheck -- the cutover's SEMANTIC teeth, re-proved by the KERNEL on every build: for each of the 13 cut-over theorems, example : ⟨the ConePort-kernel-checked S3 type⟩ := @original (statement identity to the verified port — a silent weakening or binder drift turns this red) + #assert_axioms + #assert_not_depends_on [compressNInjective, ListDigestBindsList] (the refuted floor AND the old endpoint UNREACHABLE from every cut-over theorem).
import Dregg2.Circuit.PeepholeDeployedShape -- ⚑ WHY the peephole cannot reach SHIPPED circuits, as theorems: every recogniser reads only `.windowGate` with onTransition=false, so NoPlainWindowGate cs → collapseSites = [] ∧ confirmedOuts = [] → frontierCollapse d = d (∀-d, discharged for the graduateV1/embedV1 families + predicateNeq/dfaRouting). Deployed emits author the SAME algebra as `.base (.gate …)` over EmittedExpr.var — dfaRoutingDesc's v·(v−1)=0 booleanity is right there, unreachable. It is a RECOGNISER-VOCABULARY limit, not a difficulty. Also lands deployedShape_preservation: a certified SatisfiabilityPreservation on a descriptor carrying REAL deployed hashSites/ranges/lookups/memOp/mapOp (2 firing sites, 378→374). ⚠ THREE claims RETRACTED in-file after an adversarial verify: (i) the UNIVERSAL 'no shipped emit has a peephole site' is FALSE — turnChainBindingDescriptor ships a plain .windowGate bit gate; (ii) the 36-member cohort's deployedRefuseGates ARE real zeroTest/or clusters (isZeroDefGateT is literally the invGate algebra), and they DO have an invertible emitter (blockGates/refuseGatesAt/decodeGates) — so widening the recogniser to `.base` is a viable route; (iii) transferGraduated_frontier_noop targets a SUPERSEDED graduation (the live registry routes to AvailWireMembers.transferV3AvailWire via graduateV1Wide). 56 keystones #assert_all_clean.
import Dregg2.Crypto.PrivateGraphRewriteAirBridge -- ⚑ CLOSES THE AIR→SPEC BRIDGE for the DEPLOYED hypergraph-rewrite circuit: privateGraphRewrite_sat_imp_accepts — for ANY nonempty VmTrace, Satisfied2 of the SHIPPED privateGraphRewriteDescriptor (310 cols / 584 constraints, served as private-graph-rewrite-4x2) ⟹ ∃ w, Accepts (the REAL, UNMODIFIED Accepts, all 9 fields discharged, over the REAL BoundedOneStep). Corollary privateGraphRewrite_sat_imp_rewriteStep yields Accepts ∧ RewriteStep ∧ Embeds — exactly what the history protocol's `sound` field consumes. Before this the chain stopped at DecodedAirFacts, a LABELED CHECKPOINT under a shipped light-client tooth. The four-row constTrace restriction was REMOVED, not inherited. 18 keystones #assert_all_clean; spec + descriptor byte-untouched. ⚠ SOUNDNESS only — completeness (a Rust-filled witness satisfies the descriptor) is the orthogonal witness-gen perimeter.
import Dregg2.Crypto.HierarchicalGraphFrame -- ⚑ THE FRAME CONVERSE, NOW CLOSED AT THE DEPLOYED RESOLUTION (§2b supplies the ROM↔object join the earlier version lacked; the previous 'NOT CLOSED' note here is superseded). objFrame_binds_rom: a query-bounded adversary producing an ACCEPTED deployed AncestorUpdate.check whose frame conclusion FAILS has negligible advantage — UNCONDITIONAL (no TreeAbsorbInjective, which this file still refutes for bounded-lane digests), closed through frame_binds_rom → romCarrier_binds → keyedRom_hard (a birthday THEOREM). The three named-missing correspondences are proved: romHash_semanticRoot, romHash_pathFor, romHash_rootFromPath (the last for an ARBITRARY adversarial Path4). Escape is a priced GAME WIN, not a free ∨ ∃ collision, and objBreakWitness_wins proves the event is REACHABLE (not laundering). ⚑ The bridge needed NOTHING the tree lacked — just the distinct absorb tags + leaf/node messages already carrying position and child-order — corroborating that the obstruction was always the HASH, never the layout. ⚠ RESIDUALS: romHash is the SAMPLED role-keyed oracle wearing the deployed Hash type, NOT fixed Poseidon2 (CR of a fixed function stays a conjecture); the 8-lane digest is collapsed to ONE λ-growing lane, so the modelled hash is strictly WEAKER than deployed on lanes 1-7 (conservative direction, but ties into the felt-width campaign); GRAPH_SLOTS = 4; and the deployed AIR still absorbs the whole graph core each step — this is the PREREQUISITE for shrinking it, not a shrunken circuit. 54 keystones #assert_all_clean.
import Dregg2.Verify.FloorPole -- ⚑ HonestHypothesis: the fix for the ratchet's PERVERSE INCENTIVE. Completing the doctrine's own check — writing the refutability pole an honest floor is REQUIRED to have — used to RED THE ROOT, so doing the right thing cost you and doing nothing was free; the 8-unrefuted-floors audit concluded that is why 5 of 8 floors lacked a pole. MEASURED both ways on the real tree: writing `¬ Lace.Canonical dupLace` alone takes the gate 21 → 34 violations (13 of them `Lace.Canonical`); the same pole PLUS a `HonestHypothesis` declaration returns it to 21 with ZERO false hits, while the other lane's 21 genuine carriers stay red. Four checks gate the declaration and every one fails CLOSED: never a named sentinel, refuted at a closed instance, NOT refuted parametrically, model present. ⚑ THIS IMPORT IS LOAD-BEARING, NOT DECORATIVE — the lane measured the machinery INERT without it (probe B2 failed `Unknown identifier` until the line existed), which is the exact class this file keeps re-teaching: a check that cannot be reached is a check that cannot fire.
import Dregg2.Verify.FloorCensus        -- `#floor_census` v2: the elaborated-term refuted-floor census (instrument of record; supersedes scripts/binding_surface_complete.py). v2 closes the four measurement blind spots: Pass 1b floor-in-Prop-def-BODY carriers (descriptorRefines-shaped, δ-unfolded to fixpoint) + higher-order floor flow (a floor hypothesis passed into ANOTHER hypothesis — the lightclient_unfoolable keystone — re-levels the DAG, RELEVEL lines) + DEAD split by kind×flags (dead-thm-clean = the honest day-one deletion wave, struct-boiler separated) + multi-floor COMBO histogram (AppRule design against real combinations). FAILS CLOSED on partial imports, missing sentinels (incl. the 3 prop-body keystone gates), or a mis-leveled ho-thread keystone. [import re-added: 6b1e156bdf gutted 1132 root imports incl. this one]
import Dregg2.Circuit.MapReconcileImtRepoint -- ⚑ THE `.absent` DISPATCH REPOINTED ONTO THE DEPLOYED SHAPE — AND THE WALL ONE LAYER ABOVE IT. §2 `ReconcileGatesImtAt` judges an `.absent` row by `MapAbsentImtGate.AbsentImtGatesAt` (the shipped `Ir2Air::MapAbsent`: ONE arity-3 low-leaf opening with the pointer INSIDE the committed digest, the pointer bracket, `new_root = root`), carrying the committed `ImtSorted` chain in the SAME extraction slot `ReconcileGatesAt`'s own `∃ h, SortedKeys h` occupies — no new floor, `ImtSorted` stays a hypothesis. Every other kind is the original body verbatim, so the adjacency arm stays available for a future two-leaf model. §4 EXHIBITS THE TOP GAP AT THE DEPLOYED DEPTH (`MAP_TREE_DEPTH = 16`, not a toy depth — the epoch's existing top-gap witnesses are at depth 2 and 1 and do NOT instantiate `MapReconcileModelOk`): on a fired `.absent` row whose key is the fresh key just above the committed maximum — the ORDINARY path — the OLD premise is FALSE by TWO independent mechanisms (the arity WALL at the deployed pre-root; the BRACKET above the max, now at depth 16) while the REPOINTED premise HOLDS, its law FIRES the absence, and the repointed denotation is delivered through the repointed arm. The witness kit (`spineC` / `spineC_add` / `spineC_open_last`) builds a `2^16`-leaf sorted spine AND a membership path at SYMBOLIC depth, so nothing is ever enumerated. §1 = THE REFUTATION, machine-checked: the deployed map tree has arity-3 IMT leaves for EVERY map-op kind (`descriptor_ir2.rs` `map_leaf_input_cols = [MAP_KEY, value_col, MAP_NEXT]`) while `opensTo`/`writesTo` commit arity-2 `Heap.leafOf` leaves, and under the single `Poseidon2SpongeCR` floor an arity-3 IMT root is NEVER an arity-2 `mapRoot` (`imtRoot_ne_mapRoot`) — so at a deployed pre-root `ReconcileGatesAt` is unsatisfiable FOR ANY KIND (the apex premise is EMPTY on every deployed row, not merely incomplete in the top gap) and `MapOp.holdsAt` ITSELF is refuted. Hence the apex cannot be de-vacuumed by repointing its premise: the DENOTATION (`opensTo`/`writesTo` → `MapOp.holdsAt` → `Satisfied2` → `AlgoStarkSound`) is what must move. The named REMAINING WIRE (digest-vector → chain-symbol) is NEITHER required NOR sufficient (`imtRootedRow_wire_does_not_reach_the_heap_slot`). Also restates the finding in `PremiseInhabitability`'s `Empties`/`AcceptsSome` vocabulary. RESIDUALS (header): the unpadded commitment form (the seam the wide `.absent` family already carries); nothing downstream consumes the repointed dispatch, and cannot until the denotation moves. Wiring this in also gives CI its first coverage of `MapAbsentImtGate` and `PremiseInhabitability`, which no umbrella target reached.
import Dregg2.Circuit.PeepholeBaseGateScan -- ⚑ THE PEEPHOLE REACHES SHIPPED CIRCUITS. Widens the recogniser to `.base (.gate : EmittedExpr)` — the vocabulary PeepholeDeployedShape proved it was blind to — with six shape-twins of the WindowExpr set and decide-free inversions. The identification is DEFINITIONAL (rfl): the deployed constructors ARE the recognised ones (isZeroForceGateT = bProdGate …, isZeroDefGateT = bInvGate …, isFirstBoolGate/stateGridGate/symbolGridGate = bBitGate …). FORCING ALGEBRA proven over EmittedExpr (ForcedB + forcedB_field_one + forcedB_prod_zero, residual-general as the deployed gadget needs; bBit_of_prod_inv shows a cluster's {product,inverse} pair already forces its bit boolean by pure field algebra). ⚑ CERTIFIED ON REAL DEPLOYED DESCRIPTORS, ∀-MEMBER: deployedRefuse_zeroSubst_{escrow,discharge,vault} (d) : SatisfiabilityPreservation (zeroSubstPass … (gentianDeployedBareRefuse d)) for EVERY bare cohort member — the pin hypothesis discharged BY THE DEPLOYED WELD itself (refuse_mem_block/block_mem_deployed already ARE the blockGates inversion), plus dfaRouting_zeroSubst_preservation. The soundness leg needs NO hypothesis (∀-c ∀-d); only completeness consumes the pin, so a misapplied fold can cost an honest witness, never admit a forgery. ⚠ A REWRITE, NOT a size reduction (emits |cs|+1; the win needs a downstream trivial-gate/degree pass). ⚠ base gates are when_transition-guarded so the algebra is conditioned on isLast=false (carried explicitly). 59 keystones #assert_all_clean.
import Dregg2.Circuit.ProofAssurance -- ⚑⚑ PROOF-ASSURANCE aggregator (07-25): the premise-vacuity instrument, its tree-wide SWEEP, and the apex repairs. A proof can be kernel-clean, sorry-free and non-vacuous in its own terms and still say NOTHING because its PREMISE IS EMPTY — and #assert_axioms is structurally BLIND to that (it checks what a proof rests on, not whether anything satisfies its hypotheses). friLdtExtractV3_makes_verifyBatch_reject_everything PROVES the landed extraction bundle forces verifyBatch to reject EVERY input, so the apex was vacuously true. The sweep (R1-R18, enumeration MECHANICAL: 39 structural candidates + 13 more found by grepping extractable : Prop) settles StarkComplete (collapses), GroundedApex.BindingExtract (free), AccumulatorSound (negative at deployment), and finds a NEW class: 13 carrier-gated *VerifierKernel classes gate extraction on a self-chosen extractable : Prop and ALL EIGHT reference instances set it to True — two independent vacuity modes, one of which the instrument structurally cannot see. PortalFloor is the same tree doing it RIGHT, so this is a default, not an accident. Rooted through ONE aggregator because individual import lines were dropped by contending commits — the second time that lesson was paid for. CEILING: nothing here proves any bundle SATISFIABLE at the opaque deployed args, and nothing can; every UNKNOWN is unsettled, not a pass.
import Dregg2.Games.Dungeon -- ⚑ THE DESCENT, NATIVELY IN LEAN — ⚑ THE MAP IS DAILY — and ⚑ THE KEY STAYS IN THE DOOR (07-26). Relics = owned objects with a per-relic CUSTODY code, and custody is NO LONGER A RATCHET: `unlock` now HANGS the key in the door it opened (`CARRIED → HUNG + depth`, on the floor you were standing on) and the new `take` verb lifts it back out (`HUNG + depth → CARRIED`, one breath, no guardian), so `custody_ratchet` is RETIRED AS FALSE and four strictly sharper facts from the same case split carry its design law: key_hangs_where_it_was_turned / custody_lowers_only_by_take (the ONLY lowering step in the rulebook, verb + both codes + the floor, pinned) / hoard_is_never_restocked (nothing ever writes a floor code) / bank_is_absorbing (BANKED is a sink); no-dupe was always STRUCTURAL (one relic = one entry of a fixed-length list), never the order. Because keys stop riding the descent, CARRYING RIGHTS TIGHTEN `CAP 8 → 7` — measured, `CAP 7` alone is UNWINNABLE on all 16 maps and `CAP 7` + key-in-the-door is winnable on all 16 (winsAt_true). ⚑ THE LUNGE WAS PROVABLY DEAD AND IS ALIVE: with the pack empty at the bottom `pack 1 + depth 4 + harm 2 = 7 = CAP` lands exactly, so TWO lunges keep the crown (driven, §10/§10b) and the trap moves from the lunge to the LOAD — hauling the three keys back down forfeits the crown at `3 + 1 + 4 = 8 > CAP`. ⚑ DOOM IS TERMINAL: `toll < BREATH` (spent + the climb home) is now a conjunct of EVERY verb's guard, so a run past the toll has NO legal move (doomed_is_terminal) instead of 28 more it cannot win with — licensed by doomed_never_banks, which was proved of the rulebook BEFORE the conjunct and never appeals to it. Descent still ATTENUATES capability (capacity_attenuates; hoard_never_leaves_whole; no_run_banks_everything), the LIGHT is still the clock (run_bounded; the_light_dies), keys are still CAPABILITIES (keyless_unlock_impossible), banking is still TERMINAL (banked_run_frozen; banked_at_the_surface). THE WORLD IS A PARAMETER drawn from the committed day-seed out of `drawFamily` (16 maps; day 0 IS the shipped map). COMPLETABILITY IS A WITNESS: `crownedRun` is GENERATED from the day's map and REPLAYED through the real `step` — same moves as before, but it now banks THE PRIZE ALONE (bank 4 → 1; the three keys hang), which is the decision the change exists to create. `costAt_tense` restated `24 → 22 ≤ costAt k`: the generated line still costs 24–30 (driven vector) but the CROWNED FLOOR is 22 via the lunge discount, so the old constant would now be a claim about the generator wearing a claim about the game. #assert_axioms-clean. ⚠ DOWNSTREAM NOT YET FOLLOWED: DungeonProgram/DungeonDeployed/DungeonCompleteness and the Rust mirror still carry CAP 8, the three-way custody alphabet, `relicFreeze` on unlockCase, the `.monotonic` custody tooth (which `take` falsifies), the 6-zone conservation sum and 7 move tags — see the header of Dungeon.lean §1 and the handoff.
import Dregg2.Games.DungeonProgram -- ⚑ the deployed descent teeth AS A LEAN VALUE over the LAW-#1 Exec algebra (emitted to dungeon-on-dregg/program/dungeon_program.json; loaded by dungeon_on_dregg::descent; regen.sh drift-gated). GUARDS are authored too — slotChangedForMethods riders retire the stapleable-slot class structurally (any depth move pays the delve law; any way flip EXHIBITS the carried key; any bank/fate move is a lawful banking; any exertion pays conservation+ratchet+capacity). Soundness INVERSIONS over ARBITRARY record values (admitted_verb_conserves/capacity/pays/alive, banked_tomb_refuses, dead_light_refuses, way2_flip_exhibits_key, unknown_method_refused). NOW PARAMETRIC IN THE DAY'S WORLD: the genesis mint pin, hoard census, per-relic provenance memberOf and guardian caps are emitted ONCE PER DRAWN MAP, so the artifact carries the whole 16-day family and the loader picks an index (it never assembles a constraint). Driven: programAdmitsRun crownedRun = true on day 0 (genesis + 17 verbs) AND `crownedRunAdmitted` over the WHOLE family (every day's generated crowned line admitted by that day's own teeth) + 9 named attacks refused (#guard). #assert_axioms-clean.
import Dregg2.Games.DungeonCompleteness -- DESCENT MODEL↔PROGRAM COMPLETENESS, now uniform over the DRAWN FAMILY: way_flip_exhibits_key is generic for every deployed keyed way 2..4 (with a key-mutation refusal tooth, driven on every day). Bare `Inv -> legal step -> program admitted` is genuinely FALSE: `wrongHomeState` is Inv + model-legal delve but program-refused because coarse Inv permits a relic at another relic's home while the authored provenance tooth is per-relic (exhibited on day 0 — a concrete counterexample needs a concrete world). The exact repair `ModelProgramInv := Inv ∧ CustodyHomeWF` is genesis/step/replay-preserved and holds for every reachable state, on whatever map the day drew; per-method capstones for delve/unlock/smite/loot/flee; `reachable_step_admitted` removes the premise for receipt-reachable play. #assert_axioms-clean.
import Dregg2.Games.DungeonDeployed -- DESCENT Int→Nat BRIDGE onto the DEPLOYED evaluator (DeployedConstraint, the @[export dregg_constraint_admits] the node routes through): reachable states are small (regs ≤ 26, custody ≤ 9 — guardHp ≤ 2 now READ OFF THE DAY'S WORLD LAW, `wf_guardHp_le`, not a hard-wired match), so signed Exec and unsigned 256-bit deployed evaluation AGREE on the marshalled image (tooth_transport). Flagship descent_step_teeth_deployed: every legal reachable step has EVERY in-subset tooth of EVERY matching case verdict .ok on the deployed evaluator, for every drawn map. Inversions RE-BASED at deployed width over ARBITRARY unsigned DInputs + refusing negative twins. Remainder: the six countFieldsEq census teeth (designed + falsified). #assert_axioms-clean. [imports re-added: 6b1e156bdf gutted 1132 root imports incl. these four]
import Dregg2.Circuit.MapPathCutoverCheck -- ⚑ CUTOVER TEETH (07-25) for the Poseidon2SpongeCR re-grounding of the DEPLOYED map/heap Merkle-opening spine. The refuted `Poseidon2SpongeCR` (∀-injectivity of a range-bounded sponge; `HashFloorHonesty.poseidon2SpongeCR_false_babyBear` disproves it at deployed BabyBear parameters, so every theorem assuming it was VACUOUS) is GONE from nine theorems IN PLACE: the crux path law `MapOpsColumnLayout.pathRecompute_binds_updates`, `leafOf_injective`, the three per-kind openers (`opensToMerkle_of_path` / `opensToMerkle_none_of_bracket` / `writesToMerkle_of_path`), and the gap-#5 IMT leg (`IndexedMerkleTree.imtLeafHash_injective` / `mem_phys_of_layout_get` / `imtVecCorr_mem` / `imtLowUpdate_binds`). What replaces it is a per-instance, REFUTABLE side condition named by a TOTAL EXTRACTOR: `pathCollFind` walks the deployed recompute and, on an equivocation, RETURNS the level at which the sponge actually collided (`pathRecompute_binds_updates_or_collides` is its UNCONDITIONAL correctness — no floor, hence true at deployed parameters, unlike the binding it replaces; `pathCollFind_len_le` bounds its output at 4 felts, so the reduction provably preserves efficiency), and `leafPre` / `imtLeafPre` / `ImtLeafCollIn` name the leaf-side events (the last BOUNDED over a named finite leaf list, never over the sponge's domain). Conclusions are byte-identical and the old hypothesis IMPLIES each new one (`noPathColl_of_CR` / `noLeafColl_of_CR` / `noImtLeafColl_of_CR` / `noImtLeafCollIn_of_CR` — the four remaining floor carriers, each a one-line bridge), so every ported theorem is strictly STRONGER. TEETH: per-theorem `example : ⟨post-cutover type⟩ := @original` (kernel defeq, every build) + `#assert_not_depends_on [Poseidon2SpongeCR]` whose walk covers BOTH type and proof term + `#assert_depends_on` positive controls; and in MapOpsColumnLayout §2b `pathBinds_unconditional_false` (dropping the side condition is REFUTED at the constant sponge), `pathColl_refutable` / `leafColl_refutable` (the events FIRE), `path_binds_fires` (the ported binding fires for EVERY hash at an honest opening — the restoration is not vacuous either). Floor RESIDUAL, said plainly: this removes a PROVABLY FALSE premise; it does not make the deployed 1-felt (~31-bit) Poseidon2 sponge collision-resistant — the side condition is a visible, refutable claim about ONE named pair, and the 1-felt width itself is the felt-width repair campaign's debt.
import Dregg2.Circuit.FriFoldConsistencyDichotomy -- ⚑⚑ THE OTHER HALF OF THE FOLD DICHOTOMY (07-25). forall-S survival proves farness survives for a prover FOLD-CONSISTENT along the walked path; this prices the COMPLEMENT, which is where a prover wins with probability 1. ⚑ The shaping finding, a theorem not a remark (exact_gate_pays_nothing): EXACT consistency is NOT SPOT-CHECKABLE — the gate is an equality, so its negation gives ONE disagreeing point, while the query bound pays only above delta*|iota| points; at the exact gate the payment is nothing. That is a fact about the PROTOCOL, so the gate had to be GRADED by a slack radius (FoldCloseAlong, definitionally the exact gate at d = 0). foldConsistent_or_query_paid is not excluded middle: the right disjunct NAMES the round and CARRIES the paid mass. strategy_soundness_compose has NO consistency gate in its statement and S is completely unconstrained — FS branch n*b/|R| plus query branch (1-delta)^k over one product space. Fires on halfDeviator, which defeats farness with probability EXACTLY 1 (so the survival theorems are silent about it) and is caught by the right branch; both events proved NON-EMPTY, so the bounds are real probabilities strictly inside (0,1). Exactly ONE named residual, ChainStepGap — which IS ChainStep at d = 0, and from which ChainStepSlack is a THEOREM. Blocked not on the tower (FriSetupTower landed it) but on its RADIUS: every landed schedule runs at d = 0. HONEST CEILING, in the module header: strategy-quantification is now MEANINGFUL rather than DECORATIVE, but this is a ROM/FS statement about an UNBOUND word-committing prover and it discharges NO part of the FRI/STARK floor — the commitment is not bound to a Merkle root, so nothing stops a prover answering different queries from different words.
import Dregg2.FFI -- ⚑ THE Lean⟷Rust BOUNDARY (07-25), and the ONLY C-compilation root: `dregg-lean-ffi/build.rs` now builds exactly one Lake target (`Dregg2.FFI`) and splices exactly this module's import closure into `libdregg_lean.a`. It replaces a hand-maintained 24-entry list of "modules outside the FFI closure" that had silently drifted from the 172 `@[export]`s across 28 files: a module missing from that list emitted no `:c`, so its symbol never reached the archive, so the Rust `#[cfg(dregg_*_present)]` bridge compiled its ABSENT arm and the node ran the un-gated path — green and wrong. `dregg_{eth,mpt,tm}_lc_verify` (all three interchain light-client gates) were dark exactly that way and are now in the archive. An import closure cannot drift from itself. Rooted here so the whole-tree `lake build` covers the boundary; the file itself declares nothing and proves nothing — it is the manifest, realized as imports, and its header says what the surface IS and what it deliberately excludes.
import Dregg2.Crypto.Fips204ChallengeHash -- ⚑⚑ THE CHEAP 11 OF THE 16-MODULE PQ ROOTING, KEPT IN THE DEFAULT BUILD (07-25 split; this line REPLACES `import Dregg2.Crypto.VerifyCoreArgAssembly`, which `ebb0b9f9ae` landed). It is the ONLY root path to the whole Keccak/FIPS-202 stack: closure = Keccak.Fips202{Spec,Round,Sponge,SpongeRefine,Refine,Lfsr} + Fips204BitPack + Fips204ChallengeHash + VerifyCore{Spec,EqSpec} + NttFaithful — MEASURED 11 modules, ~7.5 min, carrying the 132 #assert_axioms/#assert_all_clean the rooting was FOR. WHY NOT ArgAssembly ANY MORE: its closure adds the 5-module ML-DSA verify-core APEX SPINE (EqSpecW → UseHint → HashFrame → ArgRows → ArgAssembly) and `VerifyCoreEqSpecW` alone costs 2664s / 44 min INTRINSICALLY (degree-256 `AdjoinRoot` power basis, `maxRecDepth 8000`) on a LINEAR import chain, so no parallelism hides it and the four modules above it serialize behind it. MEASURED IN CI, not on a laptop: ci.yml's Zero-sorry step ran 233 min (run 30148956262) and 315.6 min (run 30146325284, whose job was then killed at 335 min) against `timeout-minutes: 330` — those 46 serial minutes are 15-20% of a step ALREADY AT ITS OWN TIMEOUT WALL. THE APEX SPINE DID NOT LOSE COVERAGE: ci.yml's `metatheory-pq-apex` job builds `Dregg2.Crypto.VerifyCoreArgAssembly` through scripts/axiom-hygiene-guard.sh on EVERY PUSH — a 34-MODULE closure, not the 1787-module corpus, so it is ~1h in its own visible lane instead of 46 min on this one's critical path. ⚠ CUTTING THIS LINE ALONE WOULD HAVE DELETED THE COVERAGE AND KEPT THE COST: `VerifyCoreEqSpecW` had a SECOND root path through `Dregg2.Crypto.CryptoVerifyAll`, whose `import Dregg2.Crypto.VerifyCoreEqSpecW` is dropped in this same commit (CryptoVerifyAll is a declaration-free aggregator that never named EqSpecW in its own docstring, and the apex lane covers it).
import Dregg2.Crypto.Keccak.Fips202Claims -- ⚑ THE KECCAK ASSERTION GAP, CLOSED (`ebb0b9f9ae`'s own named follow-up). Rooting the six `Keccak.Fips202*` modules bought them COMPILATION coverage ONLY: between them they contain ZERO #assert_axioms, ZERO #assert_all_clean and ZERO #guard, so their axiom hygiene was asserted by no file in any CI target. This module is where it is asserted — and it needs BOTH kinds of tooth, because `Fips202Refine.SpongeRefinesObligation` USED TO BE `True`, which compiles green and is kernel-clean, so an axiom net alone cannot tell a discharged obligation from a restored placebo. §1 `#assert_all_clean` over 14 keystones (the LFSR / round / sponge chain end to end). §2 FOUR STATEMENT pins: `rfl` on `SpongeRefinesObligation`'s own definition (the anti-`True` tooth, firing at the definition site) plus `example : ⟨literal ∀⟩ := ⟨theorem⟩` on the sponge discharge, `keccakF_refines_spec` and `round_constants_are_lfsr` — so statement drift (a lost quantifier, a new hypothesis, a specialized rate or length) is a kernel Type mismatch instead of a silently weaker claim that still passes every axiom pin. §3 `#assert_not_depends_on` proves the CAVP `native_decide` anchor (`spec_SHAKE256_empty_cavp` via `Keccak.shake256_empty_kat`) is NOT reachable from any refinement apex — `ebb0b9f9ae` asserted that; this CHECKS it — with the mandatory `#assert_depends_on` positive control so the rejectors cannot pass blind. FALSIFIED BOTH WAYS BEFORE LANDING: narrowing the ascribed ∀ (`8 * outLen` → `8 * outLen + 1`) reds with `Type mismatch`; adding `spec_SHAKE256_empty_cavp` to §1 reds with `axiom-hygiene FAIL … [shake256_empty_kat._native.native_decide.ax_1_1]`. Costs 20s. Residual it cannot touch: this refines the executable against a LEAN TRANSCRIPTION of FIPS 202; transcription fidelity to the published document is human-checked, not proven.
import Dregg2.Circuit.MapDenotationSchema -- `39b3a011ba`'s own follow-up #1, landed. It shipped as an ORPHAN, so its 11 #assert_axioms ran in NO CI target at all. VERIFIED GREEN at 21s (fresh elaboration, warm deps). It is the MapOp DENOTATION conservativity vehicle for `MapReconcileImtRepoint`'s refutation — the deployed map tree commits arity-3 IMT leaves for EVERY kind, so `MapOp.holdsAt` (the `.mapOp` arm of `Satisfied2`, and what every `AlgoStarkSound*` concludes about) is REFUTED at a deployed pre-root and the denotation must move. The largest open soundness item's carrier, covered by nothing.
import Dregg2.Circuit.MapPaddedDenotation -- ⚑ STAGE 2b OF THE DENOTATION MOVE: the PADDED (sparse) map-tree instance, WITH the anti-ghost teeth the landed schema had nothing of — and the REFUTATION of the obvious route. `docs/DESIGN-mapop-denotation-move.md` §10.6 measured that relaxing `opensToMerkle`'s `h.length = 2 ^ d` to `≤` kills `opensToMerkle_functional` / `_some_excludes_none` / `writesToMerkle_functional` inside their own defining module, because density IS `mapRoot_injective`'s hypothesis. §5 here shows WHY that cannot be patched: `heap_root.rs` pads with the LITERAL `BabyBear::ZERO`, so a live leaf digest that HITS the padding constant makes its entry INVISIBLE — `h ++ [e]` and `h` publish the same padded root while disagreeing on whether `e.1` is present — and COLLISION-RESISTANCE DOES NOT EXCLUDE IT (`padded_injectivity_is_refuted` / `padded_imt_injectivity_is_refuted` are proved at a hash that IS injective, at the deployed depth and the deployed arity-3 relinked leaf). Option (a) of the work order is therefore REFUTED, not merely unproved; the residual is a FIXED-TARGET PREIMAGE of a literal constant (~2^31 at this 1-felt commitment, ~2^124 at the deployed 8-felt tree), not a birthday event. What is landed instead is the `_or_collides` idiom applied to the PADDING event: `padImtRoot_binds_or_ghost_or_collides` binds the deployed padded root with NO hypothesis on `hash` at all, up to two NAMED, per-commitment, REFUTABLE residuals — `PadGhost3` (the ACTUAL committed digest vector contains the padding constant; a decidable property of committed data, deliberately not an `∃` over the hash's domain) and `SpongeColl` at the pair a TOTAL extractor returns. `MapLeafTeeth S` is the schema-level teeth bundle, carrying `binds` plus TWO anti-laundering fields (`resid_refuted` at a hash-level `Good`, and `good_inhabited`) so that `Resid := True` and `Resid := (h₁ ≠ h₂)` cannot be built; the three anti-ghost theorems are proved ONCE over it and all three instances inherit them — `narrowTeeth` (dense arity-2, which also CURES `mapRoot_injective` of its refuted floor), `padNarrowTeeth`, and `padImtTeeth` (THE DEPLOYED SHAPE: arity-3 IMT leaves over `relink_next_addrs` + zero padding). Conservativity is EQUALITY, not implication: `padMapRoot_dense` / `padImtRoot_dense` show the padded `commit` IS the dense one on a full tree, so the padded schema extends the landed pair rather than rivalling it, and the three dense theorems come back verbatim (`narrow_opensToMerkle_functional` &c.). §9 BITES at the deployed padding constant and the deployed depth: a SPARSE one-leaf tree (which the dense `opensToMerkle` has no witness for at all) PRESENTS its key and `bite_absence_is_refused` refuses the absence, while `ghost_pair_is_the_named_resid` shows the ghost pair lands on the named residual rather than escaping unnamed. Also welds the model to `heap_root.rs`'s precomputed `EMPTY_SUBTREE_ROOTS` (`perfectRoot_all_padding`), which is what makes the dense-fold-of-a-padded-vector model faithful to the deployed SPARSE fold. NAMED RESIDUAL: the deployed builder does NOT check pad-freeness, and the cheap fix is a DOMAIN-SEPARATED padding digest so the separation becomes the same named CR floor instead of a new preimage event.
import Dregg2.Circuit.MapKindImtGates -- ⛑ STAGE 2 PROPER OF THE DENOTATION MOVE: the FOUR arity-3 opener laws (`.read`/`.write`/`.insert`/`.aafiInsert`) that did not exist, GROUNDED AGAINST THE DEPLOYED AIR rather than ported from the Lean model — and three of the four DIVERGE, exactly as `.absent` did. Read this session in `circuit/src/descriptor_ir2.rs` (`Ir2Air::MapOps`): `map_leaf_input_cols = [MAP_KEY, value_col, MAP_NEXT]` is arity-3 for EVERY kind and `MAP_NEXT` is ONE column SHARED by the old- and new-leaf absorb. ① `.read`/`.write` share one sibling path and two leaf absorbs; the deployed table writes NO `new_root = root` column equality (the model ASSERTS one) — root preservation is DERIVED from `MAP_OLD_VALUE = MAP_VALUE`. ★ `readImtRow_opens_of_good` / `writeImtRow_writes_of_good` force `opensToMerkleS` / `writesToMerkleS` at `padImtSchema` (stage 2b's DEPLOYED padded arity-3 instance), through `imtChainOf_set` — which is precisely where the SHARED `MAP_NEXT` column is load-bearing: `unshared_pointer_write_is_not_a_relink` proves a row free to move the pointer leaves the image of `relink_next_addrs` altogether. ② ⛑⛑ `.insert` (op=3) has `rw_sel = 0` and `not_insert3 = 0`, so the AIR carries NO old-leaf absorb and NO fold to `MAP_ROOT` at all — the pre-root is UNCONSTRAINED. So `insertImtGates_cannot_force_the_write_denotation` is REFUTED, not unproved (the same accepting gate sits beside a pre-root where `writesToMerkleS` is FALSE, exhibited at `MAP_TREE_DEPTH = 16` by `bite_insert_preroot_is_unforced`); what IS forced is the POST side, `insertImtRow_post_opens_of_good`. And the EXISTING arity-2 `.insert` model is unsatisfiable on honest rows for the mirror reason: `reconcileGates_insert_forces_key_present` shows it demands the key ALREADY committed, so `reconcileGates_insert_unsat_at_fresh_key` — the `.absent` top-gap finding, verbatim, on a second arm nobody had looked at. (The epoch's own `.insert` exhibit, `MapOpsColumnLayout.toy_insert_gates`, wrote key 20 which `toyHeap` HOLDS: a value update wearing an insert's name — RENAMED to `toy_insert_op_value_update_gates`, see `MapInsertImtRepoint`.) ③ `.aafiInsert` (op=4): the deployed AIR PINS the free-slot digest to `ZERO8` — the PADDING constant — where the Lean `AafiGatesAt` leaves it a free variable, so the deployed AAFI grows INTO the padding stage 2b modelled. ★ `aafiImtRow_forces_absence_of_good` re-derives the double-spend tooth (`opensToMerkleS … root key none`) at the deployed padded commitment, with `ImtSorted` DERIVED from the schema's own `HeapOk` rather than assumed as it is on the `.absent` arm. ④ ⛑⛑ THE EIGHTH IMPOSSIBILITY, and it is STRUCTURAL: `heap_root.rs::insert_witness_aafi` folds `append_order_after` — the code calls it "a distinct commitment lineage from the sorted-compacted root8 (same leaf SET, different positions)" — while `MapLeafSchema.commit` is a FUNCTION of the logical sorted map. `no_schema_commits_the_append_order_layout` proves that for EVERY schema, at every depth, the claim "the padded fold of the physical layout is `S.commit` of the logical chain" is FALSE, and `aafi_post_is_not_the_sorted_commit` exhibits the separation at the deployed leaf. So the `.aafiInsert` arm of `MapOp.holdsAtS S` cannot be the deployed AAFI POST-condition at ANY instance of the landed schema — the pre-side law is what that arm can have. ⑤ THE GROWTH QUESTION ANSWERED: `denseSchema_write_forces_key_present` confirms that a DENSE `SizeOk` forces the written key already committed (so `.write`/`.insert` denote in-place UPDATE only, at `narrowSchema` AND at `imtSchema`), and `padImt_write_admits_growth` exhibits, at `MAP_TREE_DEPTH = 16`, a `writesToMerkleS` witness whose key is FRESH and whose heap GROWS by one — stage 2b's `≤` genuinely changed what these denotations can say, and `bite_aafi_grows` shows the deployed AAFI row realising it. NON-VACUITY, ALL FOUR ARMS, AT THE DEPLOYED DEPTH OVER A SPARSE TREE (one live leaf in a 2^16 commitment, the shape `CanonicalHeapTree::new` builds): an accepting row plus a refusing tooth each — forged read value, frozen write post-root, unforced insert pre-root, present-key AAFI. Nothing enumerates: the depth-16 paths are the symbolic `leftPadPath`/`slot1Path` recursions and every root is NAMED. FLOORS: `Poseidon2SpongeCR` appears in NO type; every law has an unconditional `_or_resid` form with three NAMED refutable residuals (`OpenResid`: the `pathCollFind` pair, the `chainAt` leaf pair, and "the opened digest IS the padding constant") plus a LABELLED `_of_good` bridge at `MapGood = Function.Injective ∧ PadFree3`, which is `padImtTeeth`'s own `Good` FIELD by `Iff.rfl` — no new floor, no new carrier. #assert_axioms-clean (43), no sorry/admit/native_decide.
import Dregg2.Circuit.MapInsertImtRepoint -- ⛑⛑ THE `.insert` ARM REPOINTED ONTO THE DEPLOYED SHAPE — and the pairing the DEPLOYMENT NAMES is proved INSUFFICIENT. `MapKindImtGates` found the `.absent` disease on a second host (`reconcileGates_insert_unsat_at_fresh_key`: the arity-2 `.insert` arm demands an old-leaf path to the PRE-root, hence the key ALREADY COMMITTED, while `heap_root.rs::insert_witness` (:1091-1093) REFUSES a present key — so the model is UNSATISFIABLE on every honest deployed insert row). This file does for `.insert` what `MapAbsentImtGate`→`MapReconcileImtRepoint` did for `.absent`. ① `ReconcileGatesImtInsertAt` / `MapInsertReconcileModelOk` / `mapOpsArmImtInsert_of_modeler` dispatch `.insert` to the deployed op=3 body at the row's columns and leave every other kind verbatim; ★ `insertRowImt_forces_post_opening` delivers the repointed denotation. ② ★ THE TWO SHAPES ARE INCOMPARABLE, as `.absent`'s were (`insert_models_are_incomparable`): on the honest deployed FRESH-KEY row at MAP_TREE_DEPTH=16 the old model is FALSE by TWO independent mechanisms — the COMMITMENT (`insert_old_model_false_deployed_commitment`) and the FRESH KEY over the epoch's own `spineC` spine (`insert_old_model_false_fresh_key_at_depth`) — while the repointed one HOLDS; on the epoch's own toy row the old model holds and the repointed one is EMPTY. `padImtRoot_ne_mapRoot_of_good` is the arity wall FLOOR-FREE and at the PADDED commitment, closing `MapReconcileImtRepoint`'s residual #1 for the gate law (§2 also restates the deployed `.absent` gate there, with `ImtSorted` DERIVED from `HeapOk`). ③ ⛑⛑ THE HONEST LIMIT, AND IT IS WORSE THAN `.insert`-ALONE: the deployed comment says freshness comes from “a paired `MapKind::Absent` opening against the same pre-root” (:540-542). `InsertPairRowAt` MODELS that pairing as a first-class object and `insertPair_forces_insertsFresh_of_good` shows what it buys — `insertsFreshS` (absent before, present-at-value after) — but `insertPair_cannot_force_the_write_denotation` REFUTES the rest: `insertPair_erases_the_rest_of_the_map` exhibits, at depth 16, an accepting `.absent`+`.insert` PAIR over the pre-root of [(1,7)] whose post-root commits [(5,2)] — key 5 genuinely fresh, key 5 genuinely lands, KEY 1 ERASED. So `.insert` is NOT sound in the named composition either; `insertsFreshS` is the true denotation and the gap to `writesToMerkleS` is the map-preservation nobody checks. ④ ⛑⛑ THE TENTH IMPOSSIBILITY, hunted early and forecloses the natural repair: the AAFI-style “slot was empty under the pre-root” gate would still reuse the ONE deployed sibling path, so it can only produce a ONE-CELL update of the committed vector — while `insert_witness` splices in SORTED position and RE-COMPACTS. `interior_sorted_insert_moves_more_than_one_cell` (inserting 5 into [(1,7),(9,3)] moves positions 0 AND 1) and `singlePath_postRoot_is_not_the_sorted_insert` prove op=3 cannot be repaired on its own shape at ANY depth ≥ 2: the two-path AAFI op is not an optimisation of `.insert`, it is the only shape a fresh-key insert is expressible in. ⑤ ⛑⛑ THE THIRD HOST, AND IT IS THE LIVE ONE: the `.write`/`.insert`/`.aafiInsert` arms of `ReconcileGatesAt` are the SAME BODY (:825-842), the two literal op=3 MapOps (`insertWriteOp`/`insertWriteOpRot`) are ORPHANS wired into NO descriptor (which CORRECTS `MapAbsentImtGate`:71-73), and what IS wired is `.aafiInsert` (`nullifierInsertOp`/`commitmentsInsertOp`/`revokedInsertOp`/`cellsInsertOp`) whose rows are fresh-key BY CONSTRUCTION. `reconcileGates_aafi_unsat_at_fresh_key` + `aafi_old_model_false_fresh_key_at_depth` land the same refutation on the accumulator arm that actually ships. NON-VACUITY at MAP_TREE_DEPTH=16 over a SPARSE tree: `bite_fresh_insert_row` is the epoch's FIRST fresh-key `.insert` witness (reusing `leftPadPath`/`slot1Path`/`spineC`, nothing enumerated), fires the law, and `bite_fresh_insert_forged_value_refused` is the tooth. FLOORS: NONE — `Poseidon2SpongeCR`/`Compress8CR` appear in no type and no def body; every law is the `_or_resid`/`_of_good` pair at `MapGood`, which is `padImtTeeth`'s own `Good` field. #assert_axioms-clean (28), no sorry/admit/native_decide.
import Dregg2.Circuit.MapDenotationCutoverCheck -- ⚑⚑ THE TEETH OF THE MAP-OP DENOTATION CUTOVER (2026-07-30), the instrument that makes it FALSIFIABLE on every build. `DescriptorIR2.opensTo`/`writesTo` — hence `MapOp.holdsAt`, hence the `.mapOp` arm of `Satisfied2`, hence every `AlgoStarkSound*` conclusion — were repointed from the RETIRED arity-2 dense binary-Merkle fold onto the DEPLOYED arity-3 indexed-Merkle commitment (`heap_root.rs`: `HEAP_LEAF_ARITY = 3`, `relink_next_addrs`, sparse zero padding). FOUR TEETH. ① `deployed_opensTo_inhabited` / `deployed_writesTo_inhabited_with_growth` — the denotation HAS a witness at the DEPLOYED depth (16), the DEPLOYED sparse occupancy (ONE live leaf in a 2^16 tree, what `CanonicalHeapTree::new` builds) and the DEPLOYED terminal sentinel (`SENTINEL_MAX = 2013265920`), for an ARBITRARY hash, no floor. Before the cutover no such witness existed and none could: the denotation demanded a DENSE vector of 2^16 strictly increasing keys under an arity-2 fold, so the apex CONCLUSION (not its premise) was refutable at the root the prover computes, on the ordinary path — every fresh nullifier and cell-id above the committed max. The write witness is a FRESH key that GROWS the heap, which the dense `= 2^d` schema could not express at all. ② THE SEPARATIONS SURVIVE (`imtLeafHash_ne_heapLeafOf`, `imtRoot_ne_mapRoot`, `padImtRoot_ne_mapRoot_of_good`) — if the cutover had UNIFIED the two commitments these would collapse and the move would have been a renaming. ③ THE DRIFT GUARD, which fired before this landed: `#assert_not_depends_on MapOp.holdsAt [Heap.leafOf]` / `[mapRoot]` / `[opensToMerkle]`, with POSITIVE CONTROLS (`#assert_depends_on … [imtLeafHash]` / `[padImtRoot]` / `[imtChainOf]`) because a walk over a closure the constant was never in reports clean for free. ④ THE ANTI-GHOST IS FLOOR-FREE — `opensTo_functional`/`writesTo_functional`/`opensTo_some_excludes_none` do not reach `Poseidon2SpongeCR`, which `poseidon2SpongeCR_false_babyBear` PROVES FALSE at deployed BabyBear. That is the ∃-HOIST half and it is the half that de-vacuums: the arity move ALONE would have re-based the same vacuous theorems onto a new commitment (`opensToMerkleS_functional_of_good` needs `Good ⊇ Function.Injective hash`, the refuted floor under another name), so `DeployedMapDenotation` §6 names the residual at the heaps THESE TWO OPENINGS supply (`Exists.choose`, canonical by proof irrelevance) and the binding has NO hypothesis on `hash` at all. ⚠ DOES NOT check that the deployed AIR FORCES the denotation for every kind: `.insert` (op=3, emitted by ZERO deployed descriptors) constrains only the post-root and `.aafiInsert`'s POST side folds the physical append-order layout — both Rust-side, both proved in `MapKindImtGates`.
import Dregg2.Circuit.MapAafiLiveRepoint -- ⛑⛑ THE `.aafiInsert` ARM REPOINTED — the arm that SHIPS — and the ELEVENTH IMPOSSIBILITY that fixes what its post side CAN say. `MapInsertImtRepoint` §8 showed `ReconcileGatesAt`'s `.write`/`.insert`/`.aafiInsert` arms are ONE BODY and that op=3 is an ORPHAN while op=4 ships; this repoints the live one. PREMISES RE-VERIFIED, SIX CITATIONS CORRECTED: the shared body is `MapOpsColumnLayout:840-856` (not :825-842); `insert_witness_aafi` is `heap_root.rs:1157` (not :1077-1156) with its present-key refusal at :1167-1169 and a SECOND pointer-bracket refusal at :1188-1189; `Ir2Air::MapOps` is `descriptor_ir2.rs:3253-3563` (not :3232-3541); the free-slot ZERO8 pin is :3514-3516, NOT :3493-3495 (those are inside gate (c)); and the deployed reach is one effect WIDER than reported — `noteCreate`/`commitmentsInsertOp` was missing, so it is EIGHT descriptors per registry, `aafi_insert` 24 rows / `absent` 24 / `write` 4 / `insert` ZERO across all seven committed TSVs. ① THE POST SIDE IS FORCED IN FULL. The landed pre-side law reads gates (a),(b); `aafiImtGates_force_layout_or_resid` reads all five, so an accepting op=4 row forces its post-root to be the fold of the committed PRE-vector with EXACTLY TWO cells changed — `p1` (the low leaf, ADDRESS and VALUE untouched, POINTER moved `lowNext→key`) and `p2`, which is FORCED to have held the padding constant (`h.length ≤ p2 < 2^dep` DERIVED from gate (d1)'s pinned ZERO8, not assumed). ★★ `aafiLayout_admits_no_erasure`: every OTHER cell is literally the pre-commitment's — which is exactly what `MapInsertImtRepoint.insertPair_erases_the_rest_of_the_map` proves the op=3 pairing CAN violate. The map-preservation nobody checks for op=3 IS checked for op=4. `aafiLayout_post_root_moves` is the frozen-root tooth. ② ⛑ THE ELEVENTH APPROACH, AND IT WORKS — a LAYOUT-LEVEL denotation. `aafiImtRow_forces_insertsUpToLayout_of_good`: an accepting deployed AAFI row forces `AafiInsertsUpToLayout` — an admissible sparse pre-map behind the pre-root at which the key is ABSENT, and a post-root that is the fold of a `List.Perm` of the padded commitment vector of `Heap.set h key value`. Freshness AND TOTAL map preservation, floor-free. The engine is `chain_relink_snoc_perm`: the deployed physical layout IS the relinked chain of the correctly-inserted map, permuted — so `heap_root.rs:1139-1141`'s “same leaf SET, different positions” is a theorem with the first half PROVED and the second localized to one `List.Perm`. ③ ⛑⛑ THE ELEVENTH IMPOSSIBILITY. The EIGHTH refuted a UNIFORM law; this refutes the post side at ONE deployed row for EVERY key and EVERY value: `aafi_interior_post_admits_no_opening` — the post-root an accepting op=4 row produces over `[(1,7),(9,3)]` at fresh key 5 satisfies NO `opensToMerkleS` at all, because the append-order address list is [1,9,5] and every `imtChainOf`'s is strictly increasing. Hence `aafi_post_cannot_be_insertsFreshS`: the honest limit `MapInsertImtRepoint` found for the op=3 pairing is NOT available here either, so ②'s layout statement is the ONLY shape the deployed post-condition has, not a weakened one. `aafi_interior_row_at_depth2` is a REAL accepting interior row (all five gates, every path obligation `rfl`) so the refutation bites gate data. NON-VACUITY at MAP_TREE_DEPTH=16 over a SPARSE tree: `bite_aafi_layout_fires` / `bite_aafi_both_sides_fire` reuse `MapKindImtGates.bite_aafi_row` (symbolic `leftPadPath`/`slot1Path`, roots NAMED as the schema's own commit, nothing enumerated), `bite_aafi_post_root_moves` is the tooth, and `bite_aafi_denotation_holds` delivers it through `mapOpsArmImtAafi_of_modeler` on a one-row descriptor. ④ THE WIDE TWIN: `MapOpWideKeyWeld.demoInsertGateW_accepts` ran `MapOpKind.insert` at `keyLo`, which `demoHeapW` ALREADY HOLDS — RENAMED `demoValueUpdateGateW_accepts` in place with `demoValueUpdateGateW_key_is_already_committed` promoted to a theorem. A fresh-key twin CANNOT be written and the obstruction is FLOOR-FREE: `wide_insert_never_grows_the_map` — `ReconcileGatesAtW`'s extraction premise is DENSE on both sides of the write, so growth is unrepresentable at every `LaneEnc` and every depth. That impossibility IS the deliverable. ⑤ ⚠ THE BRIEF'S “nine registered keystones” IS A CATEGORY ERROR, corrected in §10: `FloorRatchetBaseline` is the grandfathered REFUTED-FLOOR-CARRIER ledger (its own header: “the declaration is VACUOUS… this file is the RATCHET”), not a keystone registry; the keystone instrument is `KeystoneLint`'s `@[load_bearing_keystone]` + `#keystone_audit` and ZERO of these names carry it. The real count is TEN not nine, one of the nine cited lines lands on a name §9 discusses, two are the `.read` legs §9 itself calls SOUND, two are unrelated modules. And the prescribed remedy would turn the gate RED: `FloorRatchet.check` ERRORS on a carrier not in the baseline, so a live carrier's line must NOT be deleted. All ten adjudicated by name. FLOORS: NONE — `Poseidon2SpongeCR`/`Compress8CR` appear in no type and no def body; every law is the `_or_resid`/`_of_good` pair at `MapGood`, `padImtTeeth`'s own `Good` field, with all three residuals at SPECIFIC named witnesses. #assert_axioms-clean (37), no sorry/admit/native_decide, no `decide` on any depth-16 object.
import Dregg2.Circuit.AcceptanceDischarge -- ⚑ STARK-APEX ROOTING (three lines, six modules). This one pulls `AlgoStarkSoundFanoutMemory`. SIX of the NINE STARK-apex modules were ALLOWLISTED ORPHANS — AcceptanceDischarge, AlgoStarkSoundKernel, AlgoStarkSoundKernelAvail, AlgoStarkSoundFanout{Memory,MemFree,SetField} — while the other three (General, Instance, TransferV3) were already rooted. Not a decision with a price: all six VERIFIED GREEN, 177s TOTAL fresh, and de-listed from scripts/lean-orphans-allow.txt in this same commit (an allowlist entry must not outlive the exclusion it records).
import Dregg2.Circuit.AlgoStarkSoundKernelAvail -- STARK-APEX ROOTING, line 2 of 3: pulls `AlgoStarkSoundKernel` → `AlgoStarkSoundFanout{MemFree,Memory}`. Green at 28s / 29s / 18s / 29s respectively.
import Dregg2.Circuit.AlgoStarkSoundFanoutSetField -- STARK-APEX ROOTING, line 3 of 3: nothing in the tree imports it, so it needs its own root line. Green at 42s.
import Dregg2.Circuit.ApexMapOpVacuityJoin -- ⚑⚑ THE JOIN: the STARK apex's own `hrec` and its REFUTATION, in ONE build unit. The tree already had three instruments saying the apex is vacuous at the map-op tags (`#floor_ratchet`+`FloorRatchetBaseline`, `#opening_residual_cutover_check`, `PremiseInhabitabilitySweep`); what it did NOT have is anything forcing the apex's premise and its refutation into the same import cone, so nothing could go red. `MapReconcileImtRepoint` proves `reconcileGatesAt_unsat_at_imtRoot` and says in its own header that it deliberately does NOT import `AlgoStarkSoundFanoutMemory`; this file imports BOTH, and turns the hand-composition into an elaborated theorem. WHAT IT PROVES, at the DEPLOYED registry members and the DEPLOYED column numbers (selector/pre-root: noteSpend 4/214, noteCreate 5/215, createCell 31/188, refusal 52/224, heapWrite const-guard/216 — pinned by `censused_columns_are_the_deployed_ones`, which REDS if the layout moves): on a one-row trace whose FIRED map-op row carries the arity-3 indexed-Merkle root `heap_root.rs` computes (`HEAP_LEAF_ARITY = 3`, `:97,378`, moved 2→3 on `919b2b0b8d`) in its lane-0 pre-root column, `MapReconcileModelOk hash (Rfix e) t` is FALSE at e ∈ {17,27,28,39,56} (`deployed_mapOp_census`), hence on any accepting batch `MapReconcileFamily … (trImt hash c e) (Rfix e)` — LITERALLY the `hrec` argument of `AlgoStarkSoundKernel.algoStarkSound_kernel` — is FALSE (`apex_hrec_false_at_deployed_map_traces`). So at those tags the apex's conclusion is not merely unproved, its premise is REFUTED, and a prover behaving exactly as the deployed AIR requires falls OUTSIDE the apex entirely. `apex_is_dead_either_way` is the UNCONDITIONAL form: either the deployed verifier accepts nothing (apex quantifies over ∅) or `hrec` is false — no third branch. ⚑ FLOOR DISCIPLINE: the backbone carries NO refuted floor. It is stated over `RootSeparated` (the arity-3 root is not also an arity-2 `mapRoot`), which is strictly weaker than `Poseidon2SpongeCR`, is REFUTED at the constant hash (`rootSeparated_false_at_constant_hash` — so it is not a tautology), and is not known false at deployed BabyBear width, so these refutations SURVIVE the felt-width refutation of CR that the apex's own premise does not. EXACTLY TWO declarations bind the CR floor, both suffixed `_under_the_apex_floor`, and they exist only so the composition onto the apex's OWN `hCRh` is machine-checked instead of left to a reader; they are new `#floor_ratchet` names and are the honest price of saying "on your own floor, your own premise is false". TEETH: `censused_rows_declare_and_fire` (each op is in the deployed member's `mapOpsOf` AND its guard evaluates to 1 — the row is not vacuously non-firing, which is the failure mode that would make all of this measure nothing). NOT censused: tags 18/19 (same accounts grow-gate ops) — their registry members ride wrap chains with no landed `Rfix_*` `rfl` to reuse; nothing is claimed about them. ⚠ The intended 4th import `Dregg2.Circuit.AlgoStarkSoundKernel` is NOT taken: that module does not elaborate at this commit (`:341` `side_setField`, deterministic `isDefEq` heartbeat timeout — not this lane's). The refuted Prop is the apex's hypothesis verbatim from the same module the kernel consumes, so the import is a one-line change with no new proof obligation once the kernel is green. REPAIR NAMED, NOT TAKEN (file header §): either move the Lean denotation to arity-3 (blast radius = `Satisfied2` and the whole per-effect refinement ladder + both capstones) or exclude the seven map-op tags from the apex explicitly (small in Lean, large in meaning — no light-client soundness for any effect that touches a map). Elaborates in 5.5s; all three imports were already rooted, so the marginal root cost IS that 5.5s.
import Dregg2.Circuit.Emit.GuardedHidingSpanGuardWeld -- ⚑ ROOTS 6 UNLISTED ORPHANS in one line: DfaRoutingTableEmit + GuardedHidingSpan{Emit,Refine,GuardWeld,WideBlindEmit,WideBlindRefine}. They were UNLISTED (not even recorded in scripts/lean-orphans-allow.txt), i.e. the orphan gate was already RED on them — coverage, not a cost decision. VERIFIED GREEN before landing, both ways: fresh `lake env lean` 22s/23s/25s/27s/21s, and `lake build` of this target exits 0 with zero `declaration uses \`sorry\`` / sorryAx. (Test-building before recommending is exactly how `CryptoVerifyAll` was caught RED-at-HEAD while its allowlist line asserted GREEN.)
import Dregg2.Circuit.PeepholeBilinear -- PEEPHOLE ROOTING 1 of 3. The other ten Peephole* modules were already rooted; these five were UNLISTED orphans, so half the certified-peephole cluster was in CI and half was in nobody's build. Green at 24s (4 keystones #assert_all_clean, which ran nowhere).
import Dregg2.Circuit.PeepholeParityMeasurement -- PEEPHOLE ROOTING 2 of 3: also pulls `PeepholeZeroTestElision`. Green at 18s / 22s (20 keystones #assert_all_clean).
import Dregg2.Circuit.PeepholeR2GeneralAssembly -- PEEPHOLE ROOTING 3 of 3: also pulls `PeepholeR2General`. Green at 23s / 26s (30 keystones #assert_all_clean).
import Dregg2.Crypto.CryptoVerifyAll -- ⚑⚑ ROOTS THE NIST CONFORMANCE CLUSTER (07-25): 12 modules in one line (reachable Dregg2 1660→1672). The ACVP/CAVP validation-vector side of the PQ stack — KeccakCavp, MlDsaSigGenAcvp, MlDsaSigVerAcvp, MlKemEncapsAcvp, AcvpHex, CrateGeneratedKats, CodecRoundTrip, SignCoreSpec, EncapsCoreSpec, MlDsaHintCodec, MlDsaSigCodecClosed — i.e. the known-answer evidence for exactly the Keccak/ML-DSA refinement chain `VerifyCoreArgAssembly` roots above. It could NOT be rooted before `2a210a4e13`: EncapsCoreSpec did not compile at HEAD (an `Ambiguous term sampleA_size` collision introduced by DecapsCoreSpec), and being an orphan it could not go red to say so — the allowlist meanwhile asserted it was "GREEN self-checked, REGISTERABLE". Verified before rooting: `lake build Dregg2.Crypto.CryptoVerifyAll` exits 0 (8539 jobs).
import Dregg2.Crypto.BN254 -- ⚑⚑ ROOTS THE WHOLE BN254 / GROTH16 TOWER (07-25): 11 modules in one line — the aggregator plus Fq, Fp2, Fp6, Fp12, TowerField, Curves, Primality, Pairing, Bilinearity, Groth16. It LANDED UNROOTED: `53b7a247dc` ("the whole tower is genuine FIELDS") committed the Fp6/Fp12 field-ness discharge and `86bbc3d76c`-era work committed the rest, and `scripts/check-lean-orphans.sh` counted all eleven as UNLISTED orphans — reachable from nothing, in no CI target, so not one of the tower's `#assert_axioms` had ever been checked by CI. An earlier rooting sweep skipped it deliberately because it was mid-authoring; it is committed now, so the exclusion has outlived its reason. MEASURED BEFORE ROOTING (`lake env lean` per module, warm deps, this laptop): Fq 17s · Fp2 21s · Fp6 25s · Fp12 21s · TowerField 12s · Curves 15s · Primality 13s · Pairing 13s · Bilinearity 15s · Groth16 10s · aggregator 4s = 166s serial, ~127s on the import chain's critical path (Fq→Fp2→Fp6→Fp12→Curves→Pairing→Bilinearity). Every module exit 0, and NO `declaration uses 'sorry'` anywhere — Fp12/TowerField/Fp2/Fp6 emit only `Try this: ring_nf` info lines from `first`-combinator backtracking. ⚑ THE COST IS DELIBERATELY IN THE DEFAULT BUILD, unlike `Fips204ChallengeHash`'s neighbour above: that line's 44-min `VerifyCoreEqSpecW` had to be split into `metatheory-pq-apex` because it was 15-20% of a step already at its `timeout-minutes: 330` wall; ~2-3 min (even at a hosted runner's 2-3x) is not, and paying it here keeps the tower in the SAME corpus the zero-sorry guard reads. ⚠ WHAT ROOTING DOES AND DOES NOT BUY: compilation + axiom-hygiene coverage for the tower's stated theorems. The tower's OWN header names bilinearity, non-degeneracy and pairing faithfulness as OPEN goals, and `Primality`'s Pratt/Lucas certificate is the discharged part — rooting does not close any of that, it makes the tower's claims answerable to CI for the first time.
import Dregg2.Circuit.MapWideImtPadOpen -- ⛑⛑ THE WIDE ANALOGUE OF `padOpen_binds_or_resid` — the residual `MapAafiLiveRepoint` §10 named ("the wide-key versions do not exist … the same piece the whole wide file needs to leave the baseline"), CLOSED. ① THE LAW: `padOpenE_binds_or_resid` — an arity-3 padded IMT opening at a `LaneEnc`, with NO hypothesis on `hash`, up to THREE named per-row residuals (`OpenResidE`: a collision at the pair `pathCollFind` returns, a collision at the leaf pair the TOTAL `chainAtE` names, and `imtLeafHashE hash E l = padDigest` — the opened cell IS `heap_root.rs`'s padding constant). At `E := wideEnc` that is the 8-FELT-LANE instance the wide family had nothing of (`padOpen8_binds_or_resid`, arity **17** = `2·8+1` by `rfl`). ② IT IS ONE LAW AT TWO ENCODINGS, kernel-certified at both ends: `imtLeafHashE hash narrowEnc = IndexedMerkleTree.imtLeafHash hash` and `imtLeafHashE hash wideEnc = MapOpWideKeyGate.imtLeafHash8Of hash`, both `rfl`, and `padImtRootE narrowEnc = MapPaddedDenotation.padImtRoot`. The node layer never widens, so `perfectRoot_binds_or_collides`, the padding algebra, `pathRecompute_binds_updates_or_collides` and the symbolic `leftPadPath`/`slot1Path` are COMPOSED, not restated; what is new is the lane-encoded leaf `imtLeafHashE` and the relink `imtChainOfE` at an arbitrary key type. ③ TEETH: `ImtLaneTeeth` is `MapPaddedDenotation.MapLeafTeeth` at a lane encoding, carrying the SAME two anti-laundering fields (`resid_refuted`, `good_inhabited`), so `Resid := True` and `Resid := (h₁ ≠ h₂)` are unbuildable; `padImtLaneTeeth` instantiates it at every width and the three anti-ghost theorems are proved once over the bundle. ④ THE TWO DEPLOYED-SHAPED WIDE ARMS AT THE **SPARSE PADDED** COMMITMENT — which closes `MapAbsentImtGateWide`'s named residual #1 ("no padding: every law here is stated at `c.length = 2 ^ dep`"): `absentImtPadGatesW_force_absence_or_resid` (op=2, 24 emitted rows) and `aafiImtPadGatesW_force_absence_or_resid` (op=4, 24 emitted rows — the arm that SHIPS), both floor-free, with the AAFI free slot PINNED to the padding constant per `descriptor_ir2.rs:3514-3516`, and the pre-chain's `ImtSorted` DERIVED from admissibility (`chainE_imtSorted`) rather than assumed. ⑤ NON-VACUITY at `MAP_TREE_DEPTH = 16` over a SPARSE wide tree (one live leaf in a `2^16` commitment — the occupancy the DENSE wide laws have no witness for at all): `bite8_absent_row` / `bite8_aafi_row` are REAL accepting rows, every path obligation symbolic, no root ever evaluated, nothing enumerated; `bite8_absent_present_key_refused` / `bite8_aafi_present_key_refused` are the refusing teeth on a PRESENT key. ⑥ ⛑⛑ THE TWELFTH IMPOSSIBILITY — **widening the key does NOT kill the padding ghost**. Stage 2b's "~2^31 at 1 felt, ~2^124 at the deployed 8-felt tree" invites the reading that kind-D buys it away; the widening moves the KEY to 8 felts and the padding constant is a DIGEST, one felt at every key width. `padded_imt8_injectivity_is_refuted` refutes padded injectivity at `wideEnc` at the DEPLOYED depth at a hash that IS injective, and `wide_hardenings_do_not_kill_the_ghost` holds all THREE hardenings at once — 8-felt keys, `wideEnc.HeapOk` canonicity (the L3 closure), an injective sponge — with the ghost surviving all three. Only a domain-separated padding digest reaches it, which is a deployed-side change. FLOORS: NONE, none assumed, no new baseline entry; `Poseidon2SpongeCR` appears in no type and no `Prop`-def body, and the `_of_good` bridges ride `padImtLaneTeeth`'s own `Good` field. #assert_axioms-clean (47), no sorry/admit/native_decide, no `decide` on any depth-16 object.

-- ─── ⚑⚑ B5: THE RATCHET'S DARK SURFACE, ROOTED (2026-07-25) ───────────────────────────────
-- `#floor_ratchet` reads the environment of THIS ROOT and nothing else. `lakefile.toml` has five
-- default targets and only one of them is this file: `Metatheory.+` and `Polis.+` are GLOBBED
-- (every file is its own lake target), `Market`/`Bfv` are separate roots, and every allowlisted
-- `Dregg2/**` orphan is outside the closure too. So a declaration out there can take a hypothesis
-- this tree PROVES FALSE at deployed BabyBear parameters, compile green in the default build, pass
-- its own `#assert_axioms` — and be invisible to the gate built to catch exactly that.
--
-- MEASURED on a clean `git archive` export of the parent commit: 201 of 2029 in-tree modules dark
-- to this root, 75 of them still BUILT by lake. NINE dark modules mentioned a refuted floor in
-- CODE (comments stripped; 101 mentions). The five lines below root EIGHT of those nine — the
-- ninth is `scripts/floor_ratchet_canary.lean`, which must stay dark because its whole job is to
-- make the gate ERROR. Closure 1829 -> 1837 modules: every dependency was already in the build, so
-- the eight arrive at no transitive cost.
--
-- ⚑ EXPECT THE BASELINE TO RISE, AND READ THE RISE AS THE WIN. Rooting does not create vacuity;
-- it makes vacuity that was already in the tree COUNTABLE. The precedent is this same baseline's
-- existing `manual +56` raise — six dark modules rooted, 56 already-vacuous theorems surfaced.
-- `scripts/check-ratchet-darkness.sh` is the standing gate that keeps this surface from re-growing.
import Dregg2.Circuit.FinInjectivityCollapse -- B5 line 1 of 5. ROOTS TWO (pulls `Circuit.FinBindsKernel`): 24 of the 101 dark floor mentions, the two worst Dregg2 orphans in the census. Both were ALLOWLISTED in scripts/lean-orphans-allow.txt — i.e. the orphan gate was GREEN on them while they carried `Poseidon2SpongeCR`/`cellLeafInjective`/`compress{,N}Injective` where no instrument could count it. Verified before rooting: fresh `lake env lean` 13s / 10s, exit 0, no `sorry`.
import Dregg2.Circuit.KernelConfigSoundness -- B5 line 2 of 5: 10 dark floor mentions. Fresh `lake env lean` 11s, exit 0 (one `unused variable e` warning). Was allowlisted AND named in ci.yml's "Orphan gate" AXIOM_GUARD_TARGETS — so it was COMPILED by CI but still outside this root's environment, which is precisely the distinction B5 is about: compiled ≠ counted.
import Dregg2.Circuit.KernelConfigSoundnessAvail -- B5 line 3 of 5: 10 dark floor mentions. Fresh `lake env lean` 7s, exit 0 (two unused-variable warnings). Same posture as its sibling above.
import Dregg2.Circuit.Emit.MembershipDepthGeneralRung2 -- B5 line 4 of 5: 7 dark floor mentions, all `Poseidon2SpongeCR`. Fresh `lake env lean` 6s, exit 0. The last floor-mentioning module in the Emit refinement ladder's allowlisted block.
-- ─── B5b: THE 15 UNLISTED ORPHANS, RESOLVED (2026-07-25) ──────────────────────────────────
-- `scripts/check-lean-orphans.sh` was RED at the parent commit: 15 `Dregg2/**` modules reachable
-- from nothing and not even RECORDED in `scripts/lean-orphans-allow.txt`, so they were compiled by
-- no default target, named by no CI step, and their `#assert_axioms` ran nowhere. Each was fresh-
-- elaborated (`lake env lean`, exit 0, no `sorry`) BEFORE being recommended — the discipline that
-- caught `CryptoVerifyAll` red-at-HEAD while its allowlist line asserted GREEN. All 15 are green;
-- 13 are rooted below and 2 are recorded in the allowlist with their MEASURED cost. Closure
-- 1840 -> 1857 (17 modules: the 13 plus four `Circuit.Fri*` that arrive as transitive deps).
-- NONE of the 15 mentions a refuted floor, so unlike the B5 block above this adds no ratchet
-- carriers; it is pure coverage. Times are fresh `lake env lean` on this laptop, warm deps.
import Dregg2.Verify.ExistsImageVacuity -- ⚑ B5b, AND THE ONE THIS CAMPAIGN SHOULD BE MOST EMBARRASSED BY: a STANDING FALSIFIER for the ∃-IMAGE HOLE CLASS (`∃ w, x = f w` read as provenance — vacuous the moment `f` is surjective at deployed parameters, which it is), shipped as an ORPHAN. It names two LIVE instances (`ShieldedSpendPortDischarge.IsCommittedNote`, and a second from the corpus sweep) and it ran in NO target. This is the sixth time a tooth shipped dark. Green at 14s.
import Dregg2.Crypto.HandlebarsWitnessTooth -- B5b: the REAL consistency tooth for the templater, written because `HandlebarsWitness.materialized_agrees` was RETRACTED as `rfl` between two proofs of one `Prop` (true of ANY two proofs, cannot fail). It replaces it with `Type`/`Bool`-level content plus EIGHT mutation canaries and a forgery canary. A tooth whose whole point is that it BITES, compiled by nothing. Green at 13s.
import Dregg2.Crypto.BindingSurvivesSimulation -- B5b: commitment-layer SIMULATION-EXTRACTABILITY — the deployed binding extractors are transcript-free, so extraction survives an adversary who has seen arbitrarily many simulated proofs. Green at 17s. (A concurrent co-tenant build made one probe report a missing `DeployedHeapTree.olean`; re-run clean, exit 0.)
import Dregg2.Crypto.PolyTimeRomBridge -- B5b: the ROM/poly-time bridge — directly load-bearing for the "grounding efficient adversaries" line, where the whole question is whether a floor quantifies over EFFICIENT adversaries or over solutions. Green at 19s.
import Dregg2.Crypto.SpongeCarrierBridge -- B5b: the sponge-carrier bridge leaf (77 lines). Green at 15s.
import Dregg2.Circuit.FriIncidenceDesign -- B5b: the FRI correlated-agreement counterexample AS AN INCIDENCE DESIGN — the Lean half of the L3-crux work. Pulls `Circuit.Fri{CorrelatedAgreementSharp,LdtJohnsonList,ProximityGapListDecoding,ProximityGapWitness}`, four modules that were themselves named only in ci.yml's Orphan-gate list. Green at 14s.
import Dregg2.Circuit.Emit.LightClientEthAir -- B5b: the Lean-authored Ethereum light-client AIR. Green at 5s.
import Dregg2.Circuit.Emit.LightClientTendermintAir -- B5b: the Lean-authored Tendermint light-client AIR. Green at 6s.
import Dregg2.Circuit.Emit.AttestedFactsRootModel -- B5b: the attested-facts root model. Green at 4s.
import Dregg2.Circuit.Emit.AttestedFactsRootRegrounded -- THE TOOTH, rooted so it arms the gate against somebody: `Hash4Injective` is 4 field elements compressed to 1 and then ASKED to determine all four — pigeonhole-FALSE at every finite carrier, deployed BabyBear included. The tell was on its own page: its "non-vacuity witness" inhabited it at `FTree`, a FREE INDUCTIVE type — a model that must leave the deployed regime to exist is reporting the refutation. Both consumers ported to the per-instance residual `Coll4` (both poles proved) plus a floor-free bind-or-exhibit-the-collision form; carriers after port: ZERO. Landed by the 8-unrefuted-floors audit (34854bf62), whose headline is that of 8 "unrefuted" floors exactly ONE is a cryptographic hardness assumption.
import Dregg2.Games.AutomataflDifferential -- B5b: the automatafl engine differential. The automatafl arc is the campaign's one verified-and-deployed game and its Rust AIR was DELETED in favour of this Lean authority; the differential that pins the two together was in no build. Green at 43s.
import Dregg2.Crypto.MlDsaKeygenAcvp -- B5b: NIST ACVP keygen vectors for ML-DSA. The SIGGEN/SIGVER/ENCAPS halves of this conformance cluster were rooted through `CryptoVerifyAll` above; the KEYGEN half was in nobody's build. Half a conformance cluster is an oversight, not a posture. Green at 25s.
import Dregg2.Crypto.MlKemKeygenAcvp -- B5b: the ML-KEM half of the same keygen gap. Green at 9s.
import Dregg2.Crypto.MlDsaKeygenRefine -- B5b: the ML-DSA keygen REFINEMENT (executable =spec). Green at 38s. ⚠ Its ML-KEM sibling `MlKemKeygenRefine` is deliberately NOT here: MEASURED at 1882s (31 min) on one fresh elaboration and a TIMEOUT at 2400s on another, i.e. the `VerifyCoreEqSpecW` class — it rides the `metatheory-pq-apex` CI lane instead, recorded in scripts/lean-orphans-allow.txt with that number.
import Dregg2.Crypto.GuardToAutomatonCircuit -- ⚑ CLOSES THE PIPELINE: a PredRE guard → a tiny DFA → a WITNESSED circuit. The Deriv tower's ≅-class frontier IS a DFA (classes = states, Brzozowski derivatives = transitions), so GuardAut.guardDfa indexes it and guardDfa_decides proves the emitted automaton decides the SAME language as the guard (∀ Value words, with the minterm encoding VISIBLE in the statement and the alphabet abstraction justified by der_factors_over, not hidden). THE GATE: nodbRunWit_satisfies — a real Satisfied2Public witness for the Lean templater brace-ban's circuit; nodb_pipeline welds witness + symbol column + public final_state + guard verdict into ONE statement, fired on BOTH poles (the injection word [data,{,{] REJECTS, a safe word ACCEPTS). COMPOSED: brace-ban × the REFACTOR BUG guard in one circuit — pair_circuit_exposes_the_weakening shows the deployed half REJECT and the weakened half ACCEPT on the injection word, so a refactor bug is now a property of a proof-carrying circuit. MEASURED (witnessed objects): 11 states / 3 cover frames, 3 cols / 4 constraints / 0 nonlinear mults / area 9 / 616 wire bytes — and the SECOND guard costs 0 columns, 0 constraints, 0 area, only 12 wire bytes. ⚠ SCOPE: noDoubleBraceRE is the LEAN templater guard, NOT the shipped zkoracle-prove injection_free (same pattern, different object over BYTES) — see the file header.
import Dregg2.Crypto.InjectionGuardCircuit -- instantiates the guard→DFA→witnessed-circuit pipeline at the zkOracle INJECTION template: bytesRunWit_satisfies (a real Satisfied2Public witness) + bytes_both_poles (each pole carrying its own circuit — an injecting field REJECTS, a benign field ACCEPTS). ⚑ THE PREDICTION WAS HALF RIGHT: the pipeline IS genuinely alphabet-parametric (~40 lines, every obligation by kernel rfl, nothing in GuardAut/enc needed changing) — but ⚠ THE SHIPPED PREDICATE IS STILL NOT PROVEN, and that is the lane's ANSWER, not a caveat: zkoracle-prove/src/injection.rs RE-IMPLEMENTS the matcher in Rust rather than calling an @[export] of the Lean, so this circuit matches the LEAN mirror and only a differential relates it to shipped bytes — the tree's standing law (Lean model + Rust differential ≠ verified implementation). Closing it means making the node INVOKE Lean: an @[export]ed `derives`, or ship the emitted table as data and reduce the Rust to a table walk — and bytesDfa/bytesRunDesc are exactly the artifacts that second route needs.

import Metatheory.Adversary.Instances -- ⚑ B5 line 5 of 5, AND THE ONE THAT MATTERS MOST. ROOTS THREE (pulls `Metatheory.Adversary.{Schema,Model}`) carrying 49 of the 101 dark floor mentions — `Instances` alone has 41, the single worst dark carrier in the tree. It sits under the GLOBBED `Metatheory.+` lib, so `lake build` HAS been compiling it all along and CI has been green on it: this is not an unbuilt draft, it is a built, checked module whose floor hypotheses no census, no baseline and no ratchet has ever counted. That is the globbed-lib bypass in its purest form, and this line closes it. Verified before rooting: fresh `lake env lean` 8s / 7s / 7s, exit 0, no `sorry`; import closure carries no cycle back to this root and adds no module that was not already in the build.

-- ─── ⚑⚑ THE WIRING GATE (2026-07-25) ──────────────────────────────────────────────────────
-- Before any check in this tree can mean something, the check has to be IN THE BUILD.
--
-- The `Dregg2` lean_lib has no `globs` (lakefile.toml), so `lake build` compiles this file plus
-- its transitive imports and nothing else. A tooth nobody imports is not RED — it is ABSENT,
-- and the build is green because its checks never ran. This campaign has been bitten by that
-- four times: `6b1e156bdf` (1055+ imports dropped), `8a28420ec9` (1301 -> 217), `799b5a6e27`
-- (four cutover imports dropped SELECTIVELY under an unrelated Peephole commit), and the six
-- cutover teeth that shipped dark while being advertised as armed. Every one of those was
-- invisible to `lake build Dregg2`, which stayed green throughout.
--
-- Until now exactly ONE of eleven teeth modules was covered by any wiring check at all — the
-- ListDigestBindsList cutover, and only because its spec happens to pin a ROOT edit textually.
--
-- `#teeth_wired` reads `Environment.header.moduleNames` HERE, at the root, where that set is
-- exactly what this build compiled. It arrives through imports, so lake tracks it: the edit
-- that drops an import invalidates this file's own olean and re-runs the check on the spot. No
-- file is read, so no cached olean can answer for it — the repair this campaign's earlier
-- textual tooth needed.
--
-- ⚑ `Dregg2.Verify.TeethWiring` deliberately imports NONE of the modules it certifies — if it
-- did, importing it would guarantee their presence and the whole roster would be a tautology.
-- The membership test carries a negative blindness control (a module that cannot exist must be
-- reported ABSENT on the same run), because a rejector whose test has gone constantly-true
-- certifies nothing while staying green. The one failure it cannot report — its own deletion,
-- since a removed command raises no error — is covered outside the build, by
-- `scripts/cone_cutover_textual_check.sh`.
import Dregg2.Verify.TeethWiring
import Dregg2.Circuit.Emit.AirBuilder -- THE GAME-FREE POLYNOMIAL-HEAD BUILDER for Lean-authored AIRs. The Rust tree grew TWO copies of one hand-written AIR harness (`dregg-automatafl/src/builder.rs`, deleted, and its surviving clone `param-compose/src/builder.rs`, whose own header admits it is "a sibling ... duplicated rather than shared"). The automatafl replacement re-authored the `Head` vocabulary INSIDE a game module, so the second Lean consumer could not reuse it without importing a game. This is that vocabulary hoisted and game-free: `Head` + `headToExpr` + the SEMANTIC bridge `headToExpr_eval` (proven ONCE here rather than once per namespace — `AutomataflStepCoord`'s header records what NOT having it cost), the indexed fold laws every `List.range` emitter loop needs, and the gadget families a Rust builder hand-writes (boolean pin, conditional-nonzero, bit-decomposition range check, `forced_ge0`). NAMED DEBT: the two automatafl emit files still carry their own `Head` copies; migrating them is a separate mechanical lane.
import Dregg2.Circuit.Emit.Sha256Gadget -- THE FIRST CRYPTO FOLD: a Lean-GENERATED SHA-256 AIR gadget (aimed at the `FIN_OK`/`EXEC_OK` SHA-256 Merkle-branch carriers `LightClientEthAir` asserts as witnessed bits). `def`-generators over `AirBuilder.Head` emit the whole thing: a 32-bit word = 32 boolean columns LSB-first (ROTR/SHR are FREE re-indexing), 3-way XOR / Ch / Maj as local bit gates, mod-2^32 addition as one value-head equation + range-bounded carry. `sigmaWord`/`chWord`/`majWord`/`addMod32` are the gadgets; `sha256Round`/`scheduleWord`/`sha256Compress` FOLD them (the 64 rounds are a free `List.foldl` — expressibility, not hand-authoring). Both-polarity KAT: a `Nat` SHA-256 reference anchored to the published FIPS-180-4 `abc`/empty digests supplies honest witnesses the generated constraints ACCEPT and a flipped bit REJECTS (σ0/Σ0/Ch/Maj/addMod32). Semantic ℤ lemmas (`xorHead_eval`/`xor3_forces`/`chHead_forces`/`majHead_forces`) prove the gates FORCE the bit functions; `#assert_axioms`-clean. RESOLUTION (named): a KAT'd generator, NOT an end-to-end in-AIR SHA-256 replacing `FIN_OK`; the deployed gate is mod-p (the shared field-soundness residual §6 of the ETH AIR carries), the full-block ACCEPT is not `#guard`'d (kernel-intractable at ~30k gates), and the Merkle-branch fold + root PI-binding is the next slice.
import Dregg2.Circuit.Emit.Sha256MerkleFold -- the SHA-256 Merkle-branch fold generator built on `Sha256Gadget`; ETH `FIN_OK` step 1.
import Dregg2.Circuit.Emit.Sha256FoldForcing -- the ~30k-gate compression wall FORCED BY INDUCTION over hfold's inner chain — derived, not assumed.
import Dregg2.Circuit.Emit.Sha256HfoldDischarge -- the SHA hfold DISCHARGED across all four chains; the Midnight-shaped fold carries no assumption.
import Dregg2.Circuit.Emit.LightClientEthFinFold -- ETH finality `FIN_OK` DERIVED by the SHA-256 branch fold.
import Dregg2.Circuit.Emit.LightClientEthExecFold -- ETH execution `EXEC_OK` DERIVED; binds the on-chain-recorded root.
import Dregg2.Circuit.Emit.LightClientTmHashFold -- Tendermint `VSET_OK`/`EPOCH_OK`/`STAKE_TABLE_OK` DERIVED by an in-circuit SHA-256 absorb.
import Dregg2.Circuit.Emit.StakeWidthRange -- ⚑ THE ENCODER WIDTH GATE, and the BITE `AirBuilder.rangeNonneg` NEVER HAD. The generator has been emitted since it landed (`Bls12381Tower.fpLimbRange`, the automatafl coordinate decompose) and NOTHING in this tree ever proved a satisfied instance forces its term into `[0, 2^rbits)` — `AirBuilder`'s own `#assert_axioms` block records the gap by omission. **A generator with no forcing lemma is a SHAPE, not a check, and every consumer has been reading it as a check.** `rangeNonneg_forces` is that bite, with `rangeNonneg_eq` proving the hypotheses ARE the emitted constraint list. Lean-authored AIR throughout — a `def`-generator over `AirBuilder.Head` plus forcing lemmas over the emitted gates; no Rust hand-writes any of it. WHY IT WAS FOUND: `solRowLeaf` dropped a raw `Nat` stake into ONE word, so stake `v` and `v + 2^64` shared an anchor root while the anchor pinned no denominator — with the fold DEPLOYED and no gate that is a COMPLETE QUORUM FORGERY (bump your own validator by 2^64, `rooted/total → 1` with a single validator). Not exploitable today for two independent reasons, both measured: the deployed Rust encoder is `stake.to_le_bytes()` over a `u64` (`bridge/src/solana_consensus.rs:203`) and therefore injective on its own domain, and the AIR path is dead in Rust (`dregg-solana-lightclient-verify::v1` has no call site). **The wound was the Lean model being WIDER THAN THE DEPLOYED TYPE** — that gap is exactly how it got in. `solRowLeaf` now carries the stake as two fixed-width 32-bit words, the faithful model of `to_le_bytes()`; every KAT digest is UNCHANGED. The collision exhibits are KEPT (they are what makes the gate load-bearing) and each gained `*_out_of_range` + `*_unwitnessable` — there is no satisfying assignment for an emitted gate carrying them — while `solTable_binding_on` / `sol_stake_binding_on` now REQUIRE `SolTableInRange`, false three declarations up without it. Payoff: `sol_denominator_pinned_on` — two in-range tables at the same anchor root have the SAME TOTAL STAKE. Midnight twin closed too (`midRowWidthGates`); its `rowBlock` was already faithful, so its wound was purely hash-level. ⚠ MEASURED ALONGSIDE, NOT FIXED: `LightClientSolanaAir.lean:96-152`'s `ROOTED_STK`/`TOTAL_STK` are hidden witnesses with NO range lookup (only the slacks are ranged, at 128 bits) — sound over the ℤ reading its lemmas use, the shared field-soundness residual over F_p; field wraparound on the tally, not an encoder alias.
import Dregg2.Bridge.MinaChainSelection -- ⚑⚑ SAMASIKA CHAIN SELECTION — and **it is a TOURNAMENT, not an order.** `select`/`is_short_range`/relative-min-window-density rendered against `proof_of_stake.ml` with every constant cited (`k` 290, `slots_per_epoch` 7140, `sub_windows_per_window` 11, derived `grace_period_end` **2237** — the spec doc's table AND openmina both say 1440 and are stale). Tie-break chain: `blockchain_length`, then Blake2b(`last_vrf_output`) bytewise, then **the state hash as a FIELD ELEMENT** (numeric in `[0,p)`, not byte-LE, not Base58). PROVED: determinism-with-content (it factors through an eight-field key by `rfl`, which typechecks ONLY because nothing else is projected), irreflexivity, ASYMMETRY (an invariant the OCaml only QUICKCHECKS, `:4617`), ties-only-on-equal-keys, and transitivity restricted to pairwise-short-range tips. ⚑⚑ `beats_not_transitive`: genuine 3-cycles (`B` beats `A`, `C` beats `B`, `A` beats `C`), `decide`-checked at REAL mainnet constants, by TWO independent mechanisms — (i) `is_short_range` partitions pairs differently, so different PAIRS are judged by different rules; and (ii) even in the long regime alone, the relative minimum window density **is not an intrinsic scalar but a function of the PAIR** via `max_slot` — `relative_density_is_pair_dependent` shows one chain measuring **70** against a rival at its own slot and **7** projected twelve slots forward. **So "the canonical chain" is not a function of a candidate SET: folding `select` over candidates is order-dependent, and an adversarial peer can walk a node around the cycle.** Hence the light-client seam `minaBetterTip` is deliberately PAIRWISE against a fixed anchored segment, with `minaBetterTip_is_not_foldable` stating that limit as a THEOREM. Off-circuit decision procedure, said out loud: Mina never proves selection in a SNARK either (the in-circuit density code is block PRODUCTION, a different function).
import Dregg2.Bridge.MinaSelectionVectors -- the generated vectors.
import Dregg2.Bridge.MinaChainSelectionDifferential -- ⚑⚑ AND THE TWO REAL IMPLEMENTATIONS DISAGREE. Driving **openmina's own `mina_core::consensus`** over 5 real fork pairs (openmina's own test fixtures, re-asserted in Rust before emitting) plus 52 derived by moving `curr_global_slot` across sub-window and grace-period boundaries: **57/57 rows match our Lean**, and **30/57 differ from openmina in the relative density, with 8/57 differing in the FINAL VERDICT** — openmina adopts a different chain than the daemon would. Three compounding defects, each now a theorem: shift count measured in SLOTS where the daemon works in SUB-WINDOWS (and the spec's §5.4.12 contradicts its own §5.4.9); `for _ in 0..=shift_count`, one zero more than the pseudocode it transcribes; and `GRACE_PERIOD_END = 1440` against the daemon's 2237. `three_renderings_three_answers` pins ONE question at **50 (daemon) / 23 (openmina) / 28 (the spec document's own pseudocode)**. Separately, openmina's `is_short_range_fork` keys on `epoch_count` where the daemon keys on `curr_global_slot / slots_per_epoch`, which is **permanently divergent after any fully-empty epoch**, and drops the daemon's `Slot.succ`. Notably openmina's block-PRODUCTION density is a faithful port — the two paths live in the same repo and disagree. ⚠ Measured alongside: **openmina's fork fixtures no longer deserialize with openmina's own types**, so the only tests it has over its chain-selection code do not currently run; and `subWindowDensities` is NOT a field of the public GraphQL `ConsensusState`, so the long-range rule cannot be evaluated from the surface our observer uses at all — the short-range rule can. ⚑ The lane deliberately added NO `@[export]` for this, because the long-range branch needs data GraphQL cannot supply and an export would be an UN-CALLED GATE — the exact class this tree spent today closing.
import Dregg2.Bridge.MinaSlidingWindow -- ⚑ SAMASIKA'S SLIDING DENSITY WINDOW, replayed against the real chain. **849 real consecutive canonical devnet transitions, reproduced from ONE SEED and the slot sequence alone** — every block's own reported `min_window_density` AND its 11-entry `sub_window_densities`, ZERO mismatches, across two runs including an outage era where the minimum drops 34 → 14 → 8 → 3 with slot gaps up to 43. Two mutations (dropping the projection; an off-by-one ring index) are REFUTED by both runs, so the gate can go red. Off-circuit decision procedure, said out loud: pure `Nat`/`Int`/`Bool`, no AIR — Mina proves this in-circuit per block, and this is the unchecked twin a verifier runs. The slide law nobody writes down is proved exhaustively (11 ring positions × 11 shifts × 11 indices): index `i` carries sub-window `prevGsw − age i` with `age i = (prevRel + 11 − i) % 11`, and the entry survives exactly when `age i + diff ≤ 11`. ⚑ FINDING 1: **the density bound `d[i] ≤ 7` is NOT an invariant of the update function** — `step` alone yields 8 on mainnet constants, `by decide`. What preserves it is `step` COMPOSED WITH the strict slot increase that block validity enforces, so **a light client replaying the window must independently check `parent.slot < child.slot`; the density arithmetic never re-checks it.** ⚑ FINDING 2: **a backwards slot is not refused, it is MIS-COMPUTED** — the same 21-sub-window gap reads as density **77 backwards and 0 forwards**, because `overlapping` is vacuously true so stale entries are retained and the density inflated. Both proved. ⚑ FINDING 3: **`grace_period_end` is 2237, not 1440** — the written spec's constants table AND openmina's `consensus.rs:15` both say 1440; the daemon computes 2160 + 77. Two independent lanes caught this. ⚑ FINDING 4: the daemon GraphQL **cannot seed a window replay at all** — `ConsensusState` exposes `minWindowDensity` but NOT `subWindowDensities`, so this used Mina's precomputed-block archive (and getting a historical run meant walking `previous_state_hash` BACKWARDS: the archive holds orphans at every height, and a bare height lookup returned 4 linked blocks out of 301).
import Dregg2.Bridge.MinaVrfThreshold -- Samasika leader election's threshold, DERIVED rather than transcribed: the 11-term fixed-point Taylor series is re-derived in Lean from `f = 3/4` alone by Mina's own `Snarky_taylor.Exp.params` algorithm and PROVED equal to the eleven integers openmina hardcodes, with an independent Python re-derivation matching first. Monotone in stake via Bernstein. ⚑ THE 20-BIT QUANTISATION CLIFF: the stake fraction is floored to `per_term_precision = 20` bits BEFORE the series, so any delegation below `total/2^20` gets a threshold of **exactly zero** and wins only on a VRF output of exactly zero — **on devnet today that is 1510.919197082 MINA, below which block production is ARITHMETICALLY IMPOSSIBLE rather than merely unlikely.** Also: the OCaml comment says `2^256` where the code divides by `2^253`. ⚑⚑ AND THE SCOPE RESULT THAT MATTERS FOR A LIGHT CLIENT: **leader election is NOT checkable by one, at all.** Of its three inputs only `total_stake` is in the header; `my_stake` needs a Merkle path into the ~6,200-account staking epoch ledger; and **`vrf_output` cannot be recomputed by a verifier under any circumstances** — `eval sk m = H(m, sk·H₂(m))` needs the delegator's SECRET scalar, so it can only be PROVED, which Mina does inside the blockchain SNARK where the threshold constraint also lives. **Mina ships no standalone VRF verifier**; openmina's `mina_vrf` is evaluation-only. So a client verifying the Pickles proof INHERITS the threshold check, and this module is a SPECIFICATION OF WHAT THAT PROOF ASSERTS rather than an independent check. **Of Samasika's two mechanisms, the density window is checkable from two consecutive headers and nothing else; leader election is not checkable at all.**
import Dregg2.Bridge.MinaForkChoiceGate -- ⚑⚑ **FORK CHOICE IS NOW OURS, AND IT IS EXPORTED.** `@[export] dregg_mina_better_tip` / `@[export] dregg_mina_head_advance` — the Samasika `select` of `MinaChainSelection` finally reachable from the node, because the DATA-SOURCE blocker that kept it un-exported is closed: `bridge/src/mina_p2p.rs` decodes the binprot `Protocol_state.Value` off Mina's peer-to-peer RPC, where `sub_window_densities` is an ordinary positional field, instead of asking the public GraphQL (which has no resolver for it) or asking a node's `bestChain` which chain it likes. Adds the PERSISTED ROLLING HEAD a light client needs: `Head` = (tip selection fields, state hash, finalized height), `rollHead` = one pairwise step. PROVED: the finalized height is a RATCHET (`rollHead_finalized_monotone`, on ANY input), an unverified segment moves nothing (`rollHead_fails_closed_without_the_segment`), the head never replaces itself, and presentation order cannot make both sides win. ⚑ AND THE RESIDUAL EXPOSURE AS A THEOREM, not a caveat: `head_can_be_walked_in_a_cycle` executes three genuine Samasika advances that return the head to EXACTLY its starting value — while `the_cycle_moves_the_head_but_not_the_finalized_point` shows the ratchet held at 710 throughout. The head is a preference and it cycles; the finalized point is a ratchet and it does not. Constants are PINNED in the gate (`grace_period_end` 2237) and NOT carried on the wire, so a peer supplying the states cannot also supply the constants that make its fork win; the wire REQUIRES exactly 11 sub-window densities and a 32-byte VRF digest, so a source that cannot supply them gets `"ERR"` = REFUSAL rather than a verdict computed from what did arrive.import Dregg2.Bridge.LightClientMina -- ⚑ MINA BECOMES A FIRST-CLASS VERIFIED CHAIN — the fifth, and the last one that had no fold. The Samasika/Pickles rules, with `mina_no_forgery` + `mina_depth_is_witnessed`, matching the Eth/Tm template.

import Dregg2.Bridge.MinaBinprot -- ⚑⚑ **MINA'S BINPROT WIRE FORMAT, AS A LEAN FUNCTION** — and the reason it is here rather than in Rust is that **a parser IS semantics**. What a consensus state *is* (which bytes are `min_window_density` and which are `epoch_count`) is the content fork choice operates on; rendered in Rust it would be a MIRROR of openmina's `p2p-messages` whose correctness is a DIFFERENTIAL TEST. `decodeProtocolState` returns `MinaChainSelection.ConsensusState` DIRECTLY — no intermediate type, no conversion, nothing to keep in agreement. This is what unblocks the long-range branch: `sub_window_densities` is an ordinary positional field in binprot and is NOT a field of the public GraphQL `ConsensusState` at all. Canonical widths enforced (a `Length` of 3 written `0xfe 03 00` is a REFUSAL), every field element checked `< p`, `slots_per_epoch = 0` refused (Lean's Nat division by zero would silently collapse every state into epoch 0), the density count bounded HARD at 11, and the VRF digest **DERIVED** via `Blake2bGadget.Ref.compress` rather than taken as a carrier bit — anchored against `hashlib.blake2b` vectors. Carried protocol constants are decoded only to be REFUSED when they disagree with the pin, never adopted: a peer that supplies the states must not also supply the `grace_period_end` that makes its fork win.
import Dregg2.Bridge.MinaBinprotRealBlock -- ⚑⚑ **THE REALITY GATE: the Lean decoder reads a LIVE devnet block byte-exactly.** 1,544 bytes of devnet block **540186**'s `Protocol_state.Value`, captured 2026-07-30 from `seed-1.devnet.gcp.o1test.net:10003` over Mina's OWN protocol (pnet/XSalsa20 → Noise XX → yamux → `coda/rpcs/0.0.1` → `get_best_tip` v2) — not GraphQL, not a fixture anyone wrote. Decodes with EXACT FIT (openmina re-encoded the same object to 1544 bytes) and every field matches openmina's independent decode: length 540186, epoch_count 56, min_window_density **3**, densities **[2,5,6,6,4,5,3,3,3,2,5]**, slot 404098, `total_currency` 1584595834000001000. ⚑ It caught TWO real defects the hand-built vectors structurally could not, both LEAF-ENCODING rather than layout: `sok_digest : unit` is ONE `0x00` byte and not free (the shift survived twenty canonical field-element checks before surfacing as a timestamp of 0), and `body_reference` is a LENGTH-PREFIXED byte string, not a bare 32-byte digest. An encoder written by the same hand as the decoder agrees with it by construction; real bytes do not. Refutable: truncation, a flipped density-count byte and a one-byte shift are all REFUSALS.
import Dregg2.Bridge.MinaStateHashDerive -- ⛑⛑ **A MINA TIP'S IDENTITY IS NOW CHECKED, NOT SUPPLIED.** `state_hash` was re-derived NOWHERE in this tree: the peer handed over the protocol-state bytes AND the hash of the block those bytes are, and the chain gate, the header binding and the fork-choice tie-break were all anchored on the second. This re-derives it — `Poseidon_Fp(salt "MinaProtoState")[previous_state_hash; Poseidon_Fp(salt "MinaProtoStateBody")(pack_input (Body.to_input body))]` — in compiled Lean, from the bytes. The binprot wire carries EVERY field the preimage needs, so there is no gap to name; what had to change is that `MinaBinprot` stopped WALKING the `Blockchain_state` and throwing it away. ⛑ `Body.to_input` is not the binprot order in four places (constants absorbed FIRST via an OCaml `|>` that PREPENDS; `delta` second inside constants; `Epoch_data` as seed/start/length/ledger/lock; `has_ancestor` and `supercharge_coinbase` before both epoch blocks) and two leaf facts a structure-level read gets wrong: the VRF output contributes **253** bits, not 256, and the `staged_ledger_hash` SHA-256 eats the ledger hash BIG-endian (`Fold.bool_t_to_string` conses and never reverses). Both were caught by reading the daemon and openmina against each other. Anchors: the two Poseidon salts match openmina's OWN pinned regression constants (which exercises the 20-byte `'*'` padding, the little-endian prefix read, the Kimchi constants and the sponge schedule in one line), SHA-256 against `hashlib` at two blocks, Poseidon against six o1js golds. Both polarities are exercised on real devnet bytes in `MinaBinprotRealBlock`: the honest pair ACCEPTS, and a real block under a fabricated identity — including two real blocks presented under each other's real hashes — is `"ERR"`.
import Dregg2.Bridge.MinaStateHashRealBlock -- ⚑⚑ **THE REALITY GATE FOR A TIP'S IDENTITY, AND IT TAKES NO ORACLE.** `state_hash` is not on Mina's peer-to-peer wire at all — every node computes it, which is why `get_best_tip` never sends one. But `previous_state_hash` IS, in the clear, as the first 32 bytes of every `Protocol_state.Value`. So on a CONSECUTIVE pair: `deriveStateHash(block_N) = block_{N+1}.previous_state_hash`. Both sides came from the daemon; no transcription of ours is on either side, no explorer is asked, nothing is compiled but this. A comparison against a value we or openmina computed would be a differential against another implementation — this is a differential against **the chain**. MEASURED on live devnet blocks **540221 → 540222**, captured 2026-07-30 over Mina's own protocol: the equation HOLDS, in **26 s of KERNEL** `decide` (no `native_decide`, no `sorry`, zero axioms). ⚑ That is the ONLY claim in the tree that can refute the ~1,400-bit order of `Body.to_input`, and it catches precisely what `MinaBinprotRealBlock` is structurally blind to: a flipped byte INSIDE `Blockchain_state` keeps the 1,544-byte exact fit and `select` reads nothing there, so every check in that file still passes while the preimage moves — `a_flipped_byte_in_the_blockchain_state_moves_the_identity` is that case. Both refusal polarities come free as a `∀` over every wrong hash (instantiating `MinaStateHashDerive.the_guard_accepts_the_derived_and_refuses_the_rest`), not two sampled neighbours. REGENERATE with `bridge/tools/mina-consecutive-pair.py`, do not edit.
import Dregg2.Bridge.MinaAccountOpeningRealBlock -- ⚑⚑ **THE REALITY GATE FOR MINA *STATE*, AND IT IS THE WHOLE CORRECTNESS ARGUMENT FOR `MinaAccountOpening`.** Every Mina claim above this line is about the CHAIN — headers, proofs, forks. This one is about what is IN it: a live devnet account (`B62qmGudrekyaWbKzw2b4LagLvhUjVup3vUxKf1Yj96ZiiUJKpPZHjG`, leaf index 6202, balance 28305375018363953 nanomina, nonce 0) with its 35-level ledger opening, and the 2,048-byte binprot prefix of devnet block **540268** as a peer served it. The account's fields go in one end and `staged_ledger_hash.non_snark.ledger_hash` — a 255-bit value decoded out of Mina's OWN bytes, chosen by no party to this repo — comes out the other; the two halves are pinned to the same tip because the fetcher refuses unless the block's first 32 bytes equal the GraphQL tip's `previousStateHash`. ⚑ THREE ANSWERS, KEPT APART: the honest wire is `"1"`, SEVEN single-field mutations (balance +1, nonce, delegate, index, leaf sibling, transposed leaf direction, a well-formed 34-level opening) are each `"0"`, and a broken wire is `"ERR"` — if refusal and parse-failure were the same string none of the rejections would mean anything. It refutes all six layout traps `MinaAccountOpening`'s header enumerates (reversed `Fields.fold` order, `Coda*` prefixes, transposed `Merkle_path` tags, `vesting_period = 0`, `txn_version` at an end, a three-bit auth chunk); two of the six were caught HERE, after passing a two-source read. REGENERATE with `bridge/tools/mina-account-opening.py --from-tip-creator --with-block`, do not edit. The same wire is byte-identical to `dregg-lean-ffi/goldens/mina-devnet-account-opening.wire`, which the Rust wire builder is tested against.
import Dregg2.Bridge.LightClientMina -- ⚑ MINA BECOMES A FIRST-CLASS VERIFIED CHAIN — the fifth, and the last one that had no fold. The Samasika/Pickles rules, with `mina_no_forgery` + `mina_depth_is_witnessed`, matching the Eth/Tm template.
import Dregg2.Bridge.LightClientMinaGate -- the `@[export] dregg_mina_lc_verify` seam; `minaVerifyDecision_refines` is AXIOM-FREE by `rfl`. ⚑ ROOTING THIS IS LOAD-BEARING, NOT TIDINESS: until the export enters the archive the gate is absent, and because it FAILS CLOSED every Mina settlement returns `VerifiedGateUnavailable` — no fallback, no flag, no env var. `observer_fails_closed_without_the_verified_gate` asserts that refusal today and flips to asserting the accept once the export lands. (Flag day: a rebuild and a Lean-seed regeneration, not a migration.)
import Dregg2.Bridge.PicklesWrapShapeGate -- the `@[export] dregg_mina_wrap_shape_ok` seam: the PER-BLOCK Pickles Wrap-proof PREAMBLE decision, which `picklesWrapShapeOk_is_shapeOkRec` proves IS `KimchiVerify.shapeOkRec` (`verifier.rs:810-830`) conjoined with the two length agreements a RECURSIVE Wrap proof owes — accumulator commitments = challenge vectors, and `lr.len() = log2 max_poly_size`. ⚑ ROOTING THIS IS LOAD-BEARING FOR THE SAME REASON THE GATE ABOVE IS, and it retires a named fail-open: until 2026-07-29 `bridge/src/mina_observer.rs` fed the Mina light-client gate a compile-time `NEUTRAL_PICKLES_OK = true` for its Pickles conjunct, because `bestChain` never fetched `protocolStateProof`. The constant is DELETED; the observer now fetches every block's proof, decodes the binprot `Mina_base.Proof.Stable.V2` byte-exactly in Rust (a CODEC — no field or group arithmetic, both Lean-authored) and asks THIS gate for the verdict. Absent export ⇒ `ObserveError::VerifiedGateUnavailable`, no fallback. `real_block_wrap_shape_accepts` pins the accept on REAL devnet block 539508 and `real_block_wrap_shape_refused_by_freeze` pins that the retired `prevLen = 0` form REFUSES it — the same object `MinaRealBlockGate` runs on, now on the deployed path. ⚠ HONEST SCOPE: an accept means THE PREAMBLE PASSES, the first seven lines of `to_batch`. The arithmetic (C3/C5/C8, the group rungs, the IPA opening) is NOT reachable at runtime and this does not pretend otherwise — measured, two independent walls: those theorems are `by decide` over LITERAL constants of one extracted block, needing values that are not on the wire (the proof's `messages_for_next_step_proof.app_state` is literally `()`, so a Wrap proof does not carry the block it proves) from a dependency graph deliberately outside this workspace; and their cost is kernel-`decide` cost — 82s for C5/C8, 153s+75s for the opening rung, ~3.5h serial and ~28GB for the terminal MSM, PER BLOCK. Preamble = runtime-evaluable and now runs; arithmetic = fixture-bound. See docs/MINA-REAL-BLOCK-GATE.md §7.
import Dregg2.Bridge.PicklesProofChainGate -- the `@[export] dregg_mina_proof_chain_ok` seam: the PROOF↔PROOF chain decision, and the first thing in this tree that binds a served Pickles proof to anything outside its own bytes. ⚑ WHAT IT CLOSES AND WHAT IT DOES NOT. A Wrap proof is not self-binding to its BLOCK — `messages_for_next_step_proof.app_state` is literally `()` on the wire, the verifier substitutes the block's `stateHash` as `app_state`, and that enters only public-input slot 12 of 40 (the `messages_for_next_step_proof` Poseidon digest); the other 39 slots are functions of the proof alone. Reaching slot 12 as a COMPARISON needs the six wire-DROPPED words (`combined_inner_product`, `b`, `zeta_to_srs_length`, `zeta_to_domain_size`, `perm`, `xi`), i.e. `expand_deferred`, plus a 40-point MSM and two sponges — that is docs/MINA-REAL-BLOCK-GATE.md §6, NOT this. What IS closed: Pickles recursion makes block N's Step proof verify block N−1's Wrap proof, so block N's bytes carry the parent's own IPA accumulator `sg` AND the parent's 16 IPA challenges, comparable with zero arithmetic. MEASURED on 40 consecutive real devnet blocks (539761…539800): 39/39 pairs link on BOTH fingerprints, 40/40 distinct `sg`, 0 self-naming blocks, 0 non-adjacent coincidences. `chainOk_adjacent_proofs_differ` proves the payoff — an accepted segment CANNOT serve the same proof twice in a row, which is exactly what one real proof replayed under 290 fabricated headers used to do — and `chainOk_pins_every_seam` proves every adjacency is checked, not just the first, so runs cannot be spliced, shuffled or padded. ⚠ It does NOT bind a proof to a `stateHash`: an adversary holding a genuine consecutive run can still re-label the headers. And it is still not a Mina light client — fork choice is formalized nowhere and Samasika `select` is a TOURNAMENT, not an order.
import Dregg2.Circuit.Emit.LightClientMinaHashFold -- the Poseidon state-hash chain fold + a 254-bit canonicality AIR. ⚑ THE POSEIDON DEFECT IS DIFFERENT FROM SHA-256'S AND SHARPER: SHA-256/BLAKE2b read a message word mod 2^64 (hence the Solana stake and Midnight weight gates), but Poseidon's `absorbAt` enters every input through `(state + x) % p`, so it **cannot distinguish `x` from `x + p` AT ALL**. Against a light client — which is an ANCHOR SUBSTITUTION — `stateChain_anchor_shift_collides` proves a segment under anchor `A + p` reaches EXACTLY the same tip state hash as one under `A`, structurally, with no permutation evaluated. Closed by a Lean-authored 254-bit width gate built on `AirBuilder.rangeNonneg` via `StakeWidthRange.widthGate` — the same forcing lemma the Solana stake hole bought this morning — and the closure is EXACT, since `p > 2^254` means admitted ⇒ canonical, with `mina_alias_collapses_in_range` collapsing the whole `+k·p` family to a point. Exercised on real data (`devnet_anchor_shift_refused`).
import Dregg2.Circuit.Emit.LightClientSolHashFold -- the Solana half of the same SHA-256 carrier fold.
import Dregg2.Circuit.Emit.Blake2bGadget -- the Lean-GENERATED BLAKE2b compression gadget, for Midnight's `authSetOk`.
import Dregg2.Circuit.Emit.Blake2bFoldForcing -- the BLAKE2b ARX core FORCED and composed up to the compression function.
import Dregg2.Circuit.Emit.LightClientMidHashFold -- Midnight `AUTHSET_OK` DERIVED by an in-circuit BLAKE2b multi-block absorb.
import Dregg2.Circuit.Emit.Sha512Gadget -- the Lean-GENERATED SHA-512 compression gadget (the ed25519 challenge hash).
import Dregg2.Circuit.Emit.Ed25519Gadget -- the Fq (2^255-19) field + the COMPLETE twisted-Edwards point-add, generated, KAT'd and FORCED.
import Dregg2.Circuit.Emit.Bls12381Tower -- the BLS12-381 Fp/Fp2 tower, generated + KAT'd + forced.
import Dregg2.Circuit.Emit.Bls12381TowerExt -- the Fp6/Fp12 tower + G1/G2 curve layers, py_ecc-anchored.
import Dregg2.Circuit.Emit.Bls12381Forcing -- the tower and curve FORCED rather than only KAT'd: six composed forcing theorems.
import Dregg2.Circuit.Emit.KimchiRealProofGate -- THE MINA/KIMCHI LEG, ROOTED. Pulls its whole DAG into the default build: `KimchiVerify` (the assembled `kimchiVerifyDecision` — C1 shape, C4–C8 `combinedInnerProduct`/`ftEval0`/`publicEval`, the C9 IPA deferral) over `PastaIPA` → `PastaScalarMul` → `PastaCurveComplete` → `PastaCurve` → `PastaField` (the Pasta Fp/Fq moduli, on the Bls12381Tower generator machinery) and `PastaPoseidon`. Their headers each said "NOT imported by the truncated `Dregg2` root" — written while metatheory/Dregg2.lean was under the 2026-07-25 truncation — so seven modules' `#assert_axioms`/`#guard` KATs ran in NO CI target. They are `#assert_axioms`-clean and build green; rooting is the fix, not an allowlist entry. HONEST SCOPE (the files' own): this is NOT a proven-sound Kimchi verifier — `accept ⟹ valid` rests on the IPA/FRI soundness floor, the `linConstTerm` custom-gate carrier and the un-instantiated Fr-sponge; what is established is faithful source-cited transcription, assembly in verifier order, `kimchiVerifyDecision_refines` (`rfl`), per-check discrimination, and a REALITY GATE differential against a real o1-labs `proof-systems` @ `36a8b510` proof that the real `kimchi::verifier::verify` accepts.
import Dregg2.Circuit.Emit.SelfSettlementEmit -- ⚑ dregg-in-dregg's AIR, LEAN-AUTHORED (House Law #1: a `def` producing an `EffectVmDescriptor2`, byte-pinned by `emitVmJson2`, with forcing lemmas over the EMITTED gates; zero `.rs` touched — Rust's job is to CALL `SELF_SETTLEMENT_GOLDEN`). `dregg-self-settlement-v1`: 12 columns, 6 PIs, 13 constraints, a 1996-byte `#guard`-pinned golden, Rung 1 + Rung 2 over the emitted gates, a canonicity bridge forcing `SettleAccepts`'s four arithmetic legs as ℤ equalities, four UNSAT teeth, and a satisfying row whose equations are `11 = 11` / `29 = 29` rather than the constant-zero `0 = 0` the abstract non-vacuity is limited to (the audit's F-B2 lesson, applied). PERIMETER, stated in its header before any code: `engine`/`root_ok` ride a `.proofBind` whose content is `Satisfied2Custom.proofBound` — the NATIVE BabyBear boundary, never the gnark wrap whose FRI carrier is proven vacuous — and `cert_ok`, the QUORUM, is NOT IN THIS DESCRIPTOR AT ALL. ⚑ IT EXECUTES NOTHING: no turn can run it (there is no `FullActionA` constructor) and nothing in Rust resolves the descriptor name. That is the honest state, not a soft launch.
import Dregg2.Distributed.RollupAccountCell -- dregg-in-dregg's L2 ACCOUNT CELL, and the first part of the settlement that genuinely RUNS: `settleTurn` is a `List FullActionA` that `execFullTurnA` EXECUTES, with `#guard`s that run it on a registered account and read the committed root back out. `RollupAccount` occupies 5 of a `Value`'s 8 developer slots and is keyed by the child's GENESIS ROOT (cryptographic identity, never a nominal chainId). ⚑ THREE of `SettleAccepts`' seven legs stop being hypotheses and become EXECUTOR-ENFORCED slot caveats: `genesis_ok` via `.immutable child_genesis` (and `rollup_genesis_cannot_be_rehomed` is UNCONDITIONAL — it DERIVES the refusal from the installed caveat list rather than assuming it), the Nomad law via `.monotonic` plus a decode that reads a 0 flag as `none` whatever the root slot holds, and who may settle via `.senderAuthorized`. `readAccount_settle_is_applySettle` is the bridge: the DECODED CELL *is* the `RollupAccount` that `settled_root_is_child_final_fold` quantifies over, so the felt sitting in a real cell's `latest_root` slot is the child's genuine folded final root. ⚠ TWO LEGS STILL ONLY PROVE, both exhibited rather than hidden: `engine`/`root_ok` cannot be executor-enforced because a `setFieldA` carries NO PROOF ARGUMENT — the executor cannot refuse a settlement whose child proof fails to verify, and that gate is precisely what a `FullActionA` constructor would buy; and `monotone` is admitted at an already-settled height because the caveat vocabulary has `.monotonic` (≤) and `.monotonicSeq` (+1) and NO STRICT-`<` MEMBER, which `rollup_replay_at_same_height_is_admitted` states and a `#guard` RUNS.
import Dregg2.Distributed.SelfSettlement -- dregg-in-dregg, ROOTED: the `SettleChildChain` effect whose legs 1–4 are VERBATIM the hypotheses of the already-proven `light_client_accepts_finalized_history`, so a child `WholeChainProof` is verified through the NATIVE BabyBear boundary and never the gnark wrap (whose FRI carrier is proven vacuous). Binding lemmas `settled_root_is_child_final_fold` / `receipt_root_is_child_final_fold`: the root on the L1 AND in the parent turn's receipt IS the child's folded final root; the account is keyed by GENESIS ROOT (cryptographic identity), not a nominal chainId, so the Nomad law holds by construction. Five refusal teeth + non-vacuity over the node's real `tau`. ⚑ THE HEIGHT LEAK IS CLOSED (2026-07-28), and the named fix was WRONG in an instructive way: `numTurns` did NOT need pinning in the AIR — constraint 14 of `EffectVmEmitTurnChainBinding` has pinned it since it was emitted (`real_count[last] = pi[num_turns]`, inside the byte-golden, modelled exactly by `BindingAirSound.Satisfies.count`). The leak was one layer UP: `binding_air_discharges_binding_sound` took `Satisfies` — count field and all — and concluded THREE of the FOUR facts it could, so `EngineSound.binding_sound` had no slot and `numTurns` reached every client as a free register. **The circuit was right and the model threw away information it already held** — the same shape as the MapOp seam. Fix cost no crypto. `engineSound_numTurns_irrelevant` (now FALSE), `settle_accepts_inflated_height` and `inflated_height_commits_same_root` are DELETED, replaced by `settle_height_is_child_turn_count`, `inflated_height_cannot_settle`, `settlement_height_is_unique` and `replayed_settlement_cannot_advance` — a child cannot be settled twice, because advancing now requires the CHILD to advance. Formerly:  the deliberately-ABSENT conservation leg — the tree's floor ratchet refused it because it carries `compressInjective`/`cellLeafInjective`, floors this tree proves FALSE at deployed BabyBear width, so a settling L1 inherits correctness/ordering/fold/finality but NOT conservation. NOT a working L2: nothing executes, nothing is emitted, no Rust calls this.
import Dregg2.Circuit.Emit.PastaPoseidonFq -- ⚑ THE Fq-STATE SPONGE — the missing half of the transcript, and the LAST source of un-derived challenges. Constants are `mina_poseidon::pasta::fq_kimchi::static_params()` at o1-labs proof-systems `f6d958dc05`, dumped WHOLE (3x3 MDS + 55x3 round constants) by an extractor mirrored in-tree — nothing invented, which is what the prior lane refused to do rather than guess. The schedule is written ONCE, parametric over ⟨modulus, mds, rcs⟩, and `core_is_Ref_at_Fp` PROVES BY INDUCTION that the Fp instantiation IS `PastaPoseidon.Ref.hash` — so there is no second hand-copied absorb loop that could re-acquire the 2026-07-27 double-permute defect. 12 KATs at BOTH parities plus 5 double-permute anti-values, all emitted by the upstream `ArithmeticSponge` state machine. NAMING TRAP CORRECTED: `fq_kimchi` is the phase-1 sponge of a VESTA-committed (Step) proof AND the phase-2 sponge of a PALLAS-committed (Wrap) one, so one instantiation closed the missing half on both sides.
import Dregg2.Circuit.Emit.KimchiRecursionGate -- ⚑ P6 LANDED: the `prevLen = 0` freeze is RETIRED, and it was never a weak version of the upstream check — it was a DIFFERENT PREDICATE. `verifier.rs:810-813` compares the proof's prev-challenge count against the VERIFIER INDEX's, not against zero. `shapeOkRec` is that check; the old `shapeOk` survives as its instantiation at a non-recursive index by `rfl`, so no existing call site changed meaning, and `wrap_prev_challenges_refused` is superseded by `wrap_prev_challenges_admitted` — its old statement is now FALSE of the shipped decision. Backed by a REAL `prev_challenges = 2` Kimchi proof from `create_recursive`, asserted accepted by `kimchi::verifier::verify` BEFORE export, with 8 tampers flipping the composed accept. β/γ and α′/ζ′ and the phase-1 digest are now DERIVED from the index digest and real commitment coordinates rather than consumed as given. ⚑ TWO TRANSCRIPT DEFECTS FOUND ON THE WAY, both invisible at `prev_challenges = 0`: `ft_eval1` is absorbed BEFORE the public evaluations — a correction this repo's own `KIMCHI-VERIFY-SPEC.md` made on 2026-07-27 and which WAS NEVER CARRIED INTO THE LEAN, so the C3 ORDER theorem was pinning a schedule our own spec had already refuted — and `prev_challenge_digest` was missing from the listing entirely. The length `#guard` moved 72 -> 73 and is what went red.
import Dregg2.Circuit.Emit.PicklesDerivePlonkRealGate -- the DIFFERENTIAL half of P3, and a deliberate de-duplication: two lanes reached P3 within ~20 minutes and independently found that `derive_plonk`'s `perm` is character-for-character o1-labs' `ConstraintSystem::perm_scalars` AND K5's `permScalar` — one expression, three call sites. Rather than land a rival, that lane DELETED its own P3 and kept only what the survivor's header says it lacks: a differential against o1-labs' OWN values on a real proof — `perm` vs `perm_scalars`, `zkp(ζ)` vs the index's vanishing polynomial, `ζ^65536` at the real `max_poly_size` through `sqIter`, and `bEval` vs their `b_poly` at k=16, with eight per-argument tampers. It defines nothing.
import Dregg2.Circuit.Emit.KimchiTarget -- ⚑ THE COMPILER, not more circuits. Kimchi's gate vocabulary modelled as an emission TARGET beside `AirBuilder.Head`, sourced from `~/dev/proof-systems` @ `f6d958dc05` with file:line throughout. ⚑ `KimchiTarget.holds` is FAIL-CLOSED: an untranscribed gate is `False`, so a stray `Poseidon` row surfaces as VACUITY rather than as a discharged theorem — the one design choice that keeps a half-modelled vocabulary from silently proving things.
import Dregg2.Circuit.Emit.KimchiDag -- ⚑ THE DAG SOURCE LANGUAGE — it reaches ALL 901 base constraints and puts the whole AIR evaluation inside the budget. `Node` is a 1:1 image of p3's `SymbolicExpr` with each `Arc` child replaced by the INDEX of the node it became, so extraction drops from an algebraic rewrite to a renumbering. MEASURED: **2,433 DAG multiplies against 1,529,889 flat — 629×** (990× on W24 alone), and BELOW the 2,937 target because this interns on STRUCTURAL identity where p3's `base_cache` keys on the raw `Arc` POINTER, merging what pointer identity leaves separate. PROVED at arbitrary `CommRing` over the actual emitted list: `dagGens_forces` (variable `nv+k` holds node `k`'s denotation, one `Gen1` per node), `cseGo_denote` (a tree through the cache yields a DAG denoting exactly that tree), `dagFold_forces`, and the one that matters — **`dagDenote_unfold`: SHARING IS SOUND.** A node gets ONE variable read by every parent naming it, so an `m`-times-shared node is `m` reads of a single forced value, not `m` values an assignment could pull apart. `dagWf` is a HYPOTHESIS and not a lemma, because an out-of-order list reads an unforced child and the theorem is FALSE for it; it is `#guard`ed on all four tables and the Rust emitter refuses to print a DAG that fails it. ⚑ AND IT CORRECTED OUR OWN LEDGER: the ≈9.1×10⁴ figure counted MULTIPLIES ONLY, and the linear half is real — 2,433 muls×31 + 4,825 add/sub/neg×19 + 1,063 var-copies×19 = **187,295 rows for all 901**, ≈2.5×10⁵ for the whole AIR side. That is **0.85% of the ≈3.0×10⁷ budget**, where the flat form's multiplies ALONE were 47,426,559 rows = **158% of the entire budget**. ⚠ REMAINING: the 192 LogUp constraints need a named vocabulary extension (`chal`, a permutation-column leaf, and a `lift` of a base-DAG index — `compile_ext`'s shape; the LOWERING needs no change, `Gen1` is already at arbitrary `CommRing`); extraction is still a differential SEAM, narrower now; and 1,063 `.var` copy gates (20,197 rows) are NOT elided because eliding them makes `nodeVar` depend on the node list and the lane refused to land an optimisation whose forcing lemma it had not re-proved.
import Dregg2.Circuit.Emit.KimchiLower -- the lowering, with `lowerHead_sound` PROVED over the ACTUAL emitted list at arbitrary `CommRing`: the emitted rows FORCE the source `Head` to vanish. Plus `packGen_holds_iff`/`packGen_length` (2-per-row packing preserves meaning exactly at ⌈n/2⌉ cost) and the chunking lemmas. ⚑ SCOPE, stated in its own header after ember corrected the framing: this is the VERIFIER's `AIR.evalAtZeta` rung — the verifier evaluates the AIR AT THE OPENED POINTS, ONCE. Applied per trace row it would be the linear re-emission this whole route exists to avoid, and the header says so. **We emit a Kimchi VERIFIER OF DREGG PROOFS, never Kimchi versions of dregg's circuits.**
import Dregg2.Circuit.Emit.KimchiPoseidon2 -- the first GENERATED verifier object, and the differential that makes the compiler trustworthy. `sboxGen` is written ONCE and folded over the imported round constants — **141 S-boxes, none spelled out** — emitting the Poseidon2-w16-BabyBear permutation, which is ~96% of the verifier's budget and what every o1js rung is built from. DIGEST EXACT against `Poseidon2BabyBearW16.perm` on both pinned KATs, and `badOpCount = 0`: all 3,920 emitted instructions are satisfied by the generated witness, **so the digest is a claim about the CIRCUIT, not about a reference function running alongside it.** ROWS 2,668 vs o1js's 2,602 (+2.5%, accounted: range checks routed through `RangeCheck0` where o1js splits to `Lookup`, plus a conservative `qLimbCount`) ⇒ 24 permutations per 2^16 step, not 25. ⚑ TWO BUGS THE ROW-LEVEL CHECK CAUGHT THAT THE DIGEST DID NOT: quotient limbs masked to 64 bits at a 53-bit stride (overlapping, double-counting 11 bits/limb — RIGHT DIGEST, WRONG `q·p`), and a negative integer representative `Int.toNat` silently zeroes. Agreeing digests are not agreement.
import Dregg2.Circuit.Emit.KimchiPartition -- the step partitioner, so "~260 steps" is a COMPILER OUTPUT rather than a number of circuits anyone writes. Step-boundary contract: the public input is ONE field element (each costs a full row) — the Poseidon digest of `{rootCommitDigest, challengeDigest, stepIndex}` — binding every step to the SAME dregg proof, pinning the Fiat–Shamir state, and making double-count or skip in the aggregation tree impossible; carried variables cross as a digest plus one sponge per side, not as public inputs. ⚠ HONEST SCOPE: the arithmetic IS a compiler output, the INPUT is not — the partitioner currently runs on a permutation count QUOTED FROM A DOC, not on a circuit this compiler emitted. One permutation of ~11,000 is generated. Nobody is hand-writing step circuits, and nobody has generated a verifier either. Ordered remainder in the module: `permGen_forces` (satisfiability holds, SOUNDNESS does not), `renderOps_gens_sound`, `rangeCheck0_forces`, a scheduler that cuts where carried state is smallest, then the verifier rungs themselves.
import Dregg2.Circuit.Emit.KimchiEffectIncNonce -- ⚑ ROUTE B, and it is a DIFFERENT CLAIM from the verifier route. `KimchiLower` emits a Kimchi VERIFIER OF DREGG PROOFS; this file emits **dregg's own semantics** into Kimchi, so Mina can check an `incrementNonceA` state transition NATIVELY at 30 rows with no FRI proof consumed. The honest worry about that is "are these still the same semantics?", and the answer here is structural rather than differential: `incNonceHeadsBound` is ONE `def`-generated `List AirBuilder.Head`; `heads_denote_deployed_gates` + `selectorBindHead_denotes` prove it denotes, head-for-head at EVERY assignment, the gate bodies the DEPLOYED BabyBear descriptor already carries (`selectorGate_in_descriptor` shows the selector gate really is one of them); `kimchi_rows_force_heads` proves the emitted rows force those heads to vanish at arbitrary `CommRing` over the ACTUAL emitted list; and `two_emissions_one_def` lands BOTH arrows on one Lean `def`, `CellIncNonceSpec`. Lean-authored AIR on both targets — the o1js consumer transcribes `emitJson`'s coefficients into `Gates.generic` and authors nothing; MEASURED, snarky reports exactly the 30 rows `packGen_length` predicts. Satisfiable at birth (`incNonceKimchiRows_satisfiable`, the honest row) and refutable in general rather than by sample (`tamper_moved_balance_refused`/`tamper_frozen_nonce_refused`: NO assignment satisfies the circuit over a row that moves value or freezes the nonce; `kimchi_forces_selector` refuses another effect's selector). ⚑ THE ASYMMETRY IS NAMED AND IT RUNS THE OTHER WAY: the BabyBear forcing lemma is the WEAKER one — it reads gates mod 2013265921, needs `nonce_before + 1 < p`, needs that modulus PRIME to split the selector product, and is `True` on the wrap row; the Kimchi side over ℤ needs none of that and over Pallas needs `KimchiRowCanon`, proved strictly weaker (`kimchi_envelope_strictly_weaker`) and DISCHARGED in-circuit by `Gadgets.rangeCheck32`. ⚠ WHAT B DOES NOT DO, and it is 21 of the descriptor's 35 constraints: no `transitionAll` (so one valid row is NOT chained to the next) and no boundary PI pins. The 4 GROUP-4 hash sites ARE now emitted — see `Dregg2.Circuit.Emit.KimchiCellCommit`, which is what turned "relates two bare 13-tuples" into "relates a dregg CELL". It still checks a transition is VALID, never that dregg's chain CONTAINS it. Priced against route A in `docs/MINA-DREGG-SEMANTICS-NATIVE.md` (1.00e4 rows/effect row MEASURED against A's ~2.86e7, 99.7% of B's cost being the hash sites), with seven named ways B could mislead.
import Dregg2.Circuit.Emit.KimchiCellCommit -- ⚑ ROUTE B's COMMITMENT BINDING — the gap `KimchiEffectIncNonce` named above its own demo, closed. Before this the Kimchi emission had NONE of `incrementNonceVmDescriptor.hashSites`, so a verified route-B proof said only that two BARE 13-tuples stand in the `incrementNonceA` relation; it did not say they are the pre-image of any commitment, i.e. that they are a dregg CELL. This emits all four deployed GROUP-4 `hash_4_to_1` sites: each is `KimchiPoseidon2.permGen` (the Lean `def`-generator over the imported round constants; 141 S-boxes, none spelled out — NOT a hand-written permutation), and the WIRING is `def`-generated from the deployed `incNonceHashSites` by `inColsGo` rather than restated (MEASURED: sites 0/1/2 absorb cols [76,77,78,79]/[80,81,82,83]/[84,85,86,87], site 3 absorbs [98,99,100,186] — the three digest columns plus the audit-P0-2 authority residue — and lands on `saCol state.STATE_COMMIT = 88`). TWO ARROWS, ONE `def`: `babybear_forces_cellCommit` (= `transferHash_binds`) and `kimchi_forces_cellCommit` both force `cellCommitOf hash env.loc`. ⚑ THE ASYMMETRY AGAIN RUNS THE OTHER WAY: on the BabyBear side `siteHoldsAll` is ASSUMED of the deployed trace (no gate in `incrementNonceVmDescriptor.constraints` establishes it — the Poseidon2 chip is outside the constraint list); here it is DERIVED (`commit_rows_force_siteHoldsAll`), because the emitted circuit CONTAINS the permutation. ⚑ THE ONE UNDISCHARGED LEG IS NAMED, NOT FOLDED IN: `DigestCarrier` = `KimchiPoseidon2`'s own first remainder (`permGen_forces`), and §9 records that it is not merely unproved — at the emitted object `bbRange64` leaves its two copy columns UNPINNED (so the recomposition bounds by ~2^88, not 2^64) and `bbReduce` checks its remainder to 64 bits behind a LOAD-BEARING 2^31 claim (the S-box chain's Pasta safety is 35·2^31 → x^7 < 2^253.2; at 2^32 it wraps). It HOLDS at the honest cell, checked. TEETH: `tamper_non_preimage_refused` — NO assignment satisfies the emitted circuit while carrying a `state_commit` that is not `cellCommitOf hash` of its own after-state (general, not sampled); three instances of it, each a PERFECTLY VALID `incrementNonceA` transition under the arithmetic emission, were REFUSED by a real o1js `prove()`. COST RE-PRICED: 10,570 emitted rows, 10,010 in snarky (the 560 gap is exactly `renderOpsGo` flushing a pending generic half-row at every `rc0` where snarky packs across — the Lean model is a beaten upper bound, the safe direction), 99.7% of route B, 6 effect rows per Pickles step. ⚑ THE COMPUTATIONAL CHECKS ARE NOT `#guard`s: 15,604 instructions / 76,489 witness values / 10,570 rows through `whnf` took the module past 20 GB and had to be killed, so they run compiled in `CheckKimchiCellCommit.lean`, which EXITS NONZERO (mutation-proved red) and which `EmitKimchiCellCommit` runs before it will write an artifact. ⚠ AND WHAT IT STILL DOES NOT SAY, unchanged: MEMBERSHIP. B now says "a valid transition of a real, well-formed cell"; it says nothing about whether dregg's chain contains that cell. A well-formed transition of a cell that never existed passes every row — hashing a tuple you invented is as easy as inventing it.
import Dregg2.Circuit.Emit.MinaWrapGroupGate -- ⚑ THE FIRST CHECKS THAT TOUCH GROUP ELEMENTS on a real Mina block. The full Maller `ft_comm` assembly over block 539508's SEVEN REAL `t_comm` Pallas chunks: the Horner fold with ζ^(2^15), the linearization MSM, and `chunk(f_comm) − (ζⁿ−1)·chunk(t_comm)` — all of it running on K4a's unified RCB complete-add and K4b's `scMulLadderM` at FULL 255-bit scalar width, UNMODIFIED, so tonight's curve work is directly load-bearing. Ten tamper poles, two of which prove MALLER ITSELF is load-bearing (drop the subtraction; flip its sign). ⚑ THE GOLD IS NOT OUR OWN ARITHMETIC RESTATED: `kimchi::verifier::to_batch` is private, so the extractor rebuilds `f_comm`/`ft_comm` from `verifier.rs:897-963` out of o1-labs' OWN `perm_scalars`, `PolishToken::evaluate`, `multi_scalar_mul`, `chunk_commitment` and `scale`, then hands that reconstruction — inside the full 47-entry evaluations list — to **o1-labs' own `SRS::verify`**, the real verifier's final IPA opening check. It returns `true`, and displaced by `+G` returns `false`; both asserted BEFORE anything is emitted, and changing one digit of `FT_COMM_GOLD` turns the build red. ⚑ AND IT REPRICED §6: the devnet blockchain Wrap index has ZERO `linearization.index_terms` — every column the linearization could reference has its evaluation supplied by the proof, so kimchi folds it into the constant term C5 already reproduces, and only `sigma_comm[PERMUTS−1]` lacks one. The "linearization commitment MSM" §6 called a major sub-block is a ONE-TERM MSM, which is why this landed in a day.
import Dregg2.Circuit.Emit.MinaWrapAggregationGate -- the 47-term `Σ ξⁱ·Cᵢ` aggregation feeding the terminal MSM, and the WELD: `combinedComm_from_our_ftComm` fills the `ft_comm` slot with the point THE KERNEL ASSEMBLED rather than the dumped gold, so the two rungs are one object and not two agreeing coincidences. ⚑ ITS EXTRAPOLATION WAS WRONG IN BOTH DIRECTIONS AND RUNG 5h IS NOW DISCHARGED (`5d16fcd6e`): the naive route MEASURED at **8.6 h** (the 18 h figure read 2 s per TERM off 5d's Horner), and the memory prediction of ~7 TB was out by **250,000×** — actual **~28 GB**, because RSS tracks the largest single `decide` and chunking bounds it. The terminal MSM is not deferred at all. The terminal `msm == 0` is 82 non-SRS points plus `|srs.g| = 32768`; everything expensive is that single term. K4c's `sVec_eq_bPoly` deferral is now forced by arithmetic rather than chosen by taste. ⚠ THE GROUP CHECK IS NOT DONE: 5a–5d verify that OUR group arithmetic AGREES WITH KIMCHI'S on real points — **they do not open a single commitment**, so nothing yet says a commitment commits to anything, and P10 is inherited and undischarged either way. §6 is now eight ordered rungs with 5a–5d done; 5f (`check_bulletproof`) was blocked until the C3 lane landed, since `u_base`, `c` and the IPA challenges are all Fq-sponge outputs.
import Dregg2.Circuit.Emit.MinaRealBlockTranscript -- ⚑ C3 DERIVED ON THE REAL MINA BLOCK — the last CARRIED check on that object is gone. All SIX challenges plus BOTH digests are now recomputed rather than consumed, on devnet block 539508: β/γ as raw 128-bit squeezes of the phase-1 sponge over 37 `Fp` elements (index digest · 2 recursion commitments · public commitment · 15 witness commitments); α′→α and ζ′→ζ,ζω through the proven `endoMap`, with `derived_alpha_powers` tying α to C5's own α²¹/α²²/α²³; the fq digest; the prev-challenge digest as a fresh `fq_kimchi` sponge over the 30 carried challenges — an EVEN-length absorb, so yesterday's double-permute repair is load-bearing here and its anti-value is pinned; and ξ′→ξ, r′→r over the 91-element phase-2 stream. `real_block_accepts_on_derived_challenges` restates the gate's accept with EVERY challenge argument replaced by its derived expression, proved by REWRITING into `MinaRealBlockGate.real_block_accepts` rather than a second `decide`. The naming trap resolved correctly: the Wrap proof is PALLAS-committed, so phase 1 is `fp_kimchi` over `Fp` (K3's own constants — `core_is_Ref_at_Fp` proves it IS `PastaPoseidon.Ref`) and phase 2 is `fq_kimchi` over `Fq`; ONE parametric schedule runs both. Transcript order was verified against `verifier.rs`/`plonk_sponge.rs` at the tag the extractor ACTUALLY links (0.3.0 = `a73ca6ed58`, NOT the `f6d958dc05` our docs cite), and both mis-orderings MEASURABLY MOVE ξ′ on this object — asserted in Rust, pinned in Lean. The extractor now REPLAYS both sponges with the real `DefaultFqSponge`/`DefaultFrSponge` and asserts every challenge against `oracles(...)` BEFORE emitting. Refutability measured, not assumed: +1 on one of the 30 witness-commitment coordinates turns three `#guard`s red. ⚠ WHAT DID NOT MOVE: **no group element is touched.** The transcript eats `(x,y)` COORDINATES — nothing checks the pair is ON THE CURVE, that `public_comm` commits to the public input, or that a `RecursionChallenge`'s `comm` is `⟨b_poly_coefficients(chals), G⟩`. C3 removes a carrier; it does not verify Mina. ⚠ DEBT SURFACED, not fixed: `KimchiVerify.frPhase2Inputs` hard-codes the prev-challenge digest as `Ref.hash []` AND runs over `fp_kimchi`/`pN` — correct for a Vesta STEP proof and wrong on BOTH counts for Wrap; its one caller is an `nPrev = 0` Step fixture, so nothing is presently wrong.
import Dregg2.Circuit.Emit.MinaRealBlockGate -- ⚑⚑ THE REALITY GATE AT THE PICKLES LEVEL: a REAL Mina block, not a synthetic witness. Devnet block 539508 (state hash 3NLmVB6Fs3dm4kXNkgwheHXzJXNpCCwEDe76RpTVeBTNujm12zNk), fetched read-only over GraphQL, pinned verbatim at metatheory/fixtures/pickles-extractors/mina_devnet_block.json — the real WRAP shape: Pallas-committed, k=15, prev_challenges=2, public=40, domain 2^14. GROUND TRUTH FIRST, asserted before anything is emitted: openmina's devnet BlockVerifier loads, openmina's `accumulator_check` (`batch_dlog_accumulator_check` on Vesta) is true, and **o1-labs' own `kimchi::verifier::verify::<Pallas,…>` returns Ok(())** — those two are the body of `verify_block`; six further Rust asserts tie every emitted number to `proof.oracles(...)`. OUR SIDE (18 theorems, axiom-clean): the composed `kimchiVerifyDecisionFieldRec` **ACCEPTS THE REAL BLOCK** and rejects 4 tampers; `cipR` reproduces the real `combined_inner_product` across all 47 `es` entries INCLUDING the recursion prefix (4 tampers reject); `ftEval0R` reproduces the real `ft_eval0` at n=2^14 (3 tampers reject); and `prevChalFoldOk` shows the two leading `es` entries at ζ and ζω ARE K4c's b-polynomial of the 2×15 carried challenges — **`sVec_eq_bPoly`'s object, confirmed on a real Mina accumulator**, so our proven deferral really is Pickles'. ⚑ AND IT VINDICATES P6 EXACTLY: K5's frozen `shapeOk` REFUSES this block — **the `prevLen = 0` freeze was not conservative, it rejected Mina**; the pre-P6 decision refuses the real object outright. ⚠ WHAT IS *NOT* CHECKED, and it is a lot: **no group element is touched by any check**, C3 is CARRIED rather than re-derived (the Wrap side needs the Fq-state sponge), the dumped `expand_deferred` Step values are unconsumed, and P10 is unchanged. This is the ARITHMETIC core meeting reality — not a verification of Mina. Honest distance in docs/MINA-REAL-BLOCK-GATE.md §6: Fq sponge (days) → the Wrap GROUP check (weeks, the largest unbuilt block) → P3/P4, over the inherited P10 floor. ⚠ FINDINGS: openmina HEAD 82480cd468 **cannot verify a MAINNET block** — its `mainnet_*_verifier_index.json` are a stale serde format against pinned proof-systems 0.3.0 and `BlockVerifier::make()` PANICS (not feature-dependent; devnet files were regenerated and work, which is why this gate is devnet, and the reproduction ships). And the scope doc's "Wrap ⇒ 2^15 domain" is WRONG: the real Wrap VK domain is 2^14, 2^15 is `max_poly_size`, and `branch_data.domain_log2 = 16` describes the STEP side.
import Dregg2.Crypto.RomForkingExtractor -- ⚑⚑ THE REWINDING OBJECT — the one thing P10 named as missing, and it serves BOTH bridge directions. DESIGN: `RewindablePoint D R A` is a LAST-ROUND interactive prover — everything before the forked round is carried OUTSIDE the object by the ambient `RomCounting.cyl S σ` conditioning, exactly as `RomQueryFloor.birthday_cond` already conditions on what has been learned, so the object itself is one query point plus a PURE decision on the answer. No further oracle calls ⇒ acceptance is a pure function of the fork challenge, which is what makes the lemma provable rather than merely statable. `.fork` rewinds by mapping `T` fresh distinct session-tag points to `T` clones, reusing the device `IpaOpeningExtractionFloor` §C already commits to. THE LEMMA, PROVED: `Pr[all T forks accept ∧ pairwise-distinct challenges] ≥ ε^T − T²/|R|`, via a genuinely new counting fact (`condProb_fresh_pow`: `T` fresh points are JOINTLY independent-uniform, by induction through `condProb_split`) — **no Cauchy–Schwarz, no Jensen, and no `q_H` term anywhere** — union-bounded with the distinctness price already proved and reused unchanged. ⚑ AND THE GROUND-TRUTH CHECK THAT MATTERED: `FriArityForking.nfork_probability_bound` had generalized the WEAK (unknown-fork-index Bellare–Neven) bound and come out **VACUOUS at deployed FRI arity 8 for any real budget**, because `q_H^{n-1}` kills it. That vacuity is a fact about THE WRONG ENCODING, not about rewinding — this file generalizes the FIXED-POINT sibling (`HermineTSUF.ProbForger`, whose `T=2` bound already pays no `q_H` because its fork point is known) to `T`-ary, so it pays no `q_H` at all. Third time today an infeasible number turned out to be a bad encoding. DISCHARGED: `forking_discharges_item4` composes the lemma with `vandermonde_extracts` end-to-end for the IPA opening, and `fri_forking_bound` instantiates the IDENTICAL lemma at `FriAdversaryObject.fsRun`'s last round through an EXACT IDENTITY (`friLastRoundPoint_run_eq`), not an analogy — **one object really does serve both directions.** ⚠ WHAT REMAINS: only P10 item 2, computational binding — i.e. DISCRETE-LOG HARDNESS, the assumption every elliptic-curve system makes — plus the inherited session-tag convention. ⚠ NOT BUILT, and the lane corrected its own header after overclaiming it: the sharper classical "expected `T/ε` rewinds" accounting, which needs an expectation-of-a-geometric-variable argument this tree's counting-only model has never carried. Bounded effort, not research.
import Dregg2.Crypto.IpaOpeningExtractionFloor -- ⚑ P10 DECOMPOSED — the opening-soundness floor BOTH bridge directions inherit, now stated precisely with two of four pieces SETTLED, one PRICED, and two named as genuinely irreducible rather than smuggled past. (1) THE LINEAR-ALGEBRA STEP OF EXTRACTION — settled: `vandermonde_dual_basis` via `Matrix.det_vandermonde_ne_zero_iff`, at Pasta's real `qN`, under a NAMED undischarged `Fact (Nat.Prime pastaQ)` (255-bit primality by `decide` is infeasible — the same reason `KimchiVerify` carries no `Field (ZMod pN)` instance). (2) ⚑ THE BINDING IDEALIZATION IS **SETTLED FALSE** — `srsRelation_exists` proves by rank-nullity that an SRS with ≥2 generators in a group whose cardinality matches the scalar field's **ALWAYS has a nontrivial relation**, and `commitment_not_binding` cashes it out as two different witnesses opening the same commitment. So the honest content was never "no relation exists" but the COMPUTATIONAL HARDNESS OF FINDING ONE — `dlog_of_relation` reduces a 2-generator relation to a discrete log, landing on this tree's OWN `FloorGames.DLFamily`/`dlGame`, already known ⊤-false and already lacking an `Eff`. **Same disease as the injectivity floors, one level deeper: the idealized statement is false and the real one is a hardness bound.** (3) CHALLENGE-DISTINCTNESS of `T` rewound transcripts — PRICED at **≤ 4096/pastaQ ≈ 2⁻²⁴³** for `T = 64`, by a union bound over `RomCounting.condProb` REUSING `RomQueryFloor.condProb_two_fresh_eq` unchanged: the floor campaign's machinery paying off in a third place nobody planned it for. (4) OBTAINING `T` ACCEPTING TRANSCRIPTS FROM ONE PROVER — the rewinding/forking argument — **named and NOT discharged**; it needs an interactive-prover-with-cost-budget object this tree does not have. ⚑ AND THE STRUCTURAL PAYOFF: this ties to `MinaWrapOpeningGate`'s real `opening_relation_holds` on the IPA side AND to `FriLdtExtractV3`'s documented vacuity wound on the FRI side — **both bottom out in the SAME missing ingredient, not two unrelated gaps**, so the one object left to build closes both directions of the bridge at once. State of the art checked first rather than assumed: `l-adic/snarky` (public, UNLICENSED — read-only, nothing copied) proves batched IPA knowledge soundness but bridges to a real verifier by **four axioms asserting the rewinding extractor EXISTS**, with its own binding hypothesis's docstring conceding it is "information-theoretically false for a real single-curve SRS". Exactly two things remain assumed here: the forking argument, and `DLHardQuant` — already named, already ⊤-vacuous, no `Eff` yet, the same shape `HashCRHardQuant` had before its ROM escape.
import Dregg2.Circuit.Emit.PicklesTranscriptBinding -- ⚑ P4's TWO CLOSEABLE PIECES, CLOSED. The recursion crux was: without the transcript-equality binding, `finalize_other_proof` discharges a `Deferred_values` AN ATTACKER CHOSE so long as it is internally consistent. §Z named three things needed; two are now settled and the third is inherited.
-- (2) `endo` INJECTIVITY — **SETTLED TRUE** at the real constant Pickles runs (`lambdaVesta`), via two independent obstructions. COMBINATORIAL: the 128-bit→(a,b) decode (`to_field_checked_prime`) is injective, proved by a `c=a+b`/`d=a−b` decomposition reducing to "fixed-length signed-binary digit streams are injective" — a disjoint-integer-ranges induction with NO field arithmetic. FIELD-LEVEL: `(a,b) ↦ a·lambdaVesta + b` is injective on the reachable range, because the GLV lattice `{(x,y) : x ≡ y·lambdaVesta mod pN}` Gauss/Lagrange-reduces on the REAL 255-bit constants to a shortest vector of norm ≈2^127 — **~57 bits above the <2^70 range 128 bits can reach** — cross-checked against the Eisenstein identity `a²+ab+b² = pN` the shortest vector satisfies exactly. Minimality is proved in Lean by an ELEMENTARY COPRIMALITY ARGUMENT rather than by formalizing lattice-reduction theory: the right proof, not the big one.
-- (1) THE SPONGE-DIGEST BINDING — **REDUCED TO A PRICED RESIDUAL**, and this is where the floor campaign paid off somewhere nobody planned. `hashSepOn` mirrors `Sha256MerkleFold.pairSepOn`'s three-leg discipline over the REAL `PastaPoseidon.Ref.hash`: refuted at `⊤` by a mod-`pN` collision AND by a sharper ZERO-QUERY one — **absorbing nothing and absorbing a bare `0` coincide**, a real and previously-unremarked fact about this sponge — and satisfiable at an exhibited class over the deployed Poseidon. Priced by the tree's already-proved `RomQueryFloor.birthday_bound` at the real digest cardinality `pN ≈ 2^255`: a `Q`-query forger's collision odds are `≤ (Q²+1)/pN`, **effectively zero at any real budget** — unlike the BabyBear folds, where the same bound reads ≈2^15.5 and is a BREAK. Same machinery, opposite verdict, because the field is 255 bits wide.
-- WHAT REMAINS ASSUMED, EXACTLY TWO THINGS: `SpongeKeyedROFaithful` for this one experiment (IRREDUCIBLE — no hash is unconditionally a random oracle; it mirrors `Poseidon2RomInstantiation.KeyedRomFaithful`), and P10, the IPA/FRI opening floor, inherited and untouched. Nothing else. ⚠ NOT DONE and stated: these are composable standalone results at the concrete Fp instantiation — they are NOT yet wired into `PicklesFinalize.binding_and_accept_determine`'s own statement; P5 (the Step-side mirror) and P10 stand.
import Dregg2.Circuit.Emit.PicklesFinalize -- ⚑ PICKLES P3 BUILT + P4 MEASURED (NOT proved). `finalizeOtherProof` is the four-way AND (xi / b / combined_inner_product / plonk), assembled from what P0–P2 left: K5's `cipR`/`ftEval0R`/`gateLinConst`, K4c's `bEval`, P2's shifted-value bridge as a `decode` parameter. **P3's "genuinely new arithmetic" collapsed to two exponentiations** — `derivePlonk_perm_is_K5_permScalar` is `rfl`, so the `perm` arm IS K5's `permScalar` verbatim, and one exponentiation is P0's `sqIter`. The satisfying model `finalize_complete` is universally quantified over EVERY ring, domain and sampled tuple (not a KAT), with four tamper poles on the four checked slots plus a concrete `ZMod 97` pole for the challenge coordinates, which admit no generic falsifier because collisions there are improbable rather than impossible. `finalize_complete_both_bridges` runs ONE body at Type1-over-Fp and Type2-over-Fq UNCONDITIONALLY (`pasta_shift_constants` discharges `Shift1.ok` rather than assuming it), so **P5 is now genuinely a field swap.** ⚑ THE TRANSCRIPTION FINDING: `plonk_checks::checked` compares exactly ONE thing — `derive_plonk` computes `zeta_to_srs_length`, `zeta_to_domain_size` and `perm`, but the comparison list is the literal `[perm]` in BOTH implementations (`plonk_checks.rs:363-366`, `plonk_checks.ml:541-543`), so the two exposed `zeta_to_*` public-input slots DO NOT OCCUR IN THE DECISION (`finalize_ignores_zeta_powers` is `rfl`). Transcribed faithfully on purpose: a model comparing all three would be STRICTER THAN PICKLES and its tamper poles would be fiction. ⚑ P4 IS FORMALIZED AND MEASURED, NOT PROVED — `binding_and_accept_determine` pins NINE coordinates (with `binding_is_satisfiable` proving that premise set inhabited FIRST), and each of the three assert families is shown LOAD-BEARING by exhibiting the attack when it is dropped (`digest_assert_is_necessary` is the sharpest: the digest is not a `Deferred_values` field, so an accepting record exists for EVERY wrong digest). The two remaining free coordinates are LOCATED, not hand-waved — consumed as scalars in `picklesFtComm`, tied by a commitment equality the IPA opening discharges, i.e. P10 inherited, and that tie only exists from two `t_comm` chunks up. Reaching "the recursion is sound" additionally needs three things this tree does NOT have, written into §Z rather than claimed past: a random-oracle/collision assumption for the Poseidon sponge (the digest equality pins a VALUE, not a HISTORY), `endo` injectivity on 128-bit prechallenges, and the IPA floor.
import Dregg2.Circuit.Emit.PicklesRecursion -- Pickles P0–P2, ROOTED on landing (91 `#assert_namespace_axioms` pins). P0 runs ONE `kimchiVerifyDecisionGates` body at BOTH cycle sides (Step `ZMod pN` k=16 / Wrap `ZMod qN` k=15) with the wrap-domain generators pinned by EXACT multiplicative order; P1 proves the real `batch_dlog_accumulator_check` MSM SPLITS into K4c's `sVec`/`bEval` blocks (the accumulator IS our proven deferral); P2 is the Type1/Type2 shifted-value bridge (`2^255+1`/`2^255` proven to be those constants, `2⁻¹` witnessed in-ring so no `Field` instance is needed). ⚑ Carries ⚑ RESOLVED 2026-07-28 (c5ae5dbe1): `wrap_prev_challenges_refused` recorded that K5's v1 `shapeOk` REFUSED the real Wrap object's `prev_challenges = 2`. P6 has RETIRED that freeze — `shapeOkRec` is the UPSTREAM check (`verifier.rs:810-813` compares the proof's count against the VERIFIER INDEX's, not against zero, so the freeze was never a weak version of it but a DIFFERENT PREDICATE), `wrap_prev_challenges_admitted` supersedes it, and a co-lane confirmed the payoff on a REAL Mina devnet block (`MinaRealBlockGate.real_block_refused_by_freeze` / `real_block_shape_accepts`). The freeze was not a conservative simplification: it was rejecting Mina. NOT a Pickles verifier: both witnesses are SYNTHETIC (no real Wrap fixture exists yet), domains are 2^5 not 2^15/2^16, the Fq-state sponge is BUILT (`PastaPoseidonFq`, constants sourced whole from `mina_poseidon` — they were never absent, only unsourced), and P4 — the transcript-equality binding that pins the compared representative to the SAMPLED one — is untouched.
import Dregg2.Circuit.Emit.KimchiPoseidonGate -- the SECOND reality gate, ROOTED: a real o1-labs proof that FIRES the Poseidon custom gate (`poseidon_selector(ζ) ≠ 0`), which is what makes the six transcribed gate bodies and the C3 `v`/`u` challenge re-derivation NON-VACUOUS rather than a source reading. Its definitions already built via the line above; what did NOT run anywhere were THIS file's `#assert_axioms` + `#guard` + `by decide` differentials against that proof — an orphan created by a STALE instruction of mine ("do not touch the truncated Dregg2.lean"; the 2026-07-25 truncation is long healed). Rooting is the fix, not an allowlist entry; `scripts/lean-orphans-allow.txt`'s entry is deleted in the same commit.
import Dregg2.Circuit.Emit.MinaStateQuery -- the Mina account/state-query commitment leg (o1js-exact Poseidon reference KATs, packing-AGNOSTIC commitment lemmas under the CR floor). Same story as the line above: its header records "NOT imported by the truncated `Dregg2` root", so its KATs ran nowhere. Named residuals stay named — the real `to_input`/`pack_input` field packing and the `MinaAccount` prefix are not modelled, and `zkappRoot = zkappStateHash appState` simplifies `Zkapp_account.digest`.
import Dregg2.Circuit.Emit.ParamComposeEmit -- THE LEAN-AUTHORED PARAMETER-COMPOSITION AIR (`param_compose`) — the replacement route for the LARGEST remaining hand-written Rust AIR (`param-compose/src/air.rs`, 659 lines / 19 `assert_zero` sites, plus `builder.rs`, 369 lines / 5 more sites / 6 `ConstraintExpr` literals / an `air_accepts` predicate), which is LIVE on the deployed `Effect::Custom` door via `entity-compose/src/lib.rs:78`. Before this file a grep of metatheory/ for `paramCompose` returned NOTHING. A SHAPE-parametric descriptor family emits the whole AIR: prefix-flag activity, the identity range check, the `forced_ge0` canonical-ordering tooth, the pairwise role-KEY sweep, the gated one-hot `fetch` (resolution BY ROLE, degree-3 read), the degree-3 KNOT `coeff·va·vb`, THE LAW `outcome = Σ contribs`, the four `cap_node8` digest chains as WIDE arity-16 chip lookups (8 program-owned lanes, the `chip_lookup_sound_N` shape), and the 37 app PI bindings of `pi.rs`. Byte-pinned at the minimal exercised shape `pcMin`. COLUMN CENSUS CROSS-CHECK vs the Rust budget table: 186/346/770 against Rust's 219/379/803 — exactly 33 fewer at EVERY shape, the `zero` column + four 8-felt IV groups this file deliberately replaces with literal constants.
import Dregg2.Circuit.Emit.ParamComposeRefine -- THE SAT=>SEM BRIDGE the Rust AIR could never have (there is no formal semantics of Rust; `reference.rs` calling its differential "translation validation" is a misnomer, and `air_accepts` over a co-built row is a unit test). PROVEN off the emitted gates: `paramCompose_refines_law` (outcome = Sum coeff*val + Sum coeff*vA*vB mod the BabyBear prime — the DEGREE-3 knot sum is the whole reason the Custom VK exists); `fetch_resolves` (all FIVE anti-malleability conclusions: the read IS the selected subject's param, that subject's ROLE is the term's ADDRESSED role, it is ACTIVE, the param index is the ADDRESSED slot, and that slot is within `param_count`); `sel_oneHot_of_sat`/`selP_oneHot_of_sat` (the one-hot envelope is FORCED, not assumed, from the boolean pins + `Canon` + primality, via `boolSum_one_unique`); `role_key_of_sat` (two ACTIVE slots cannot share a role tag — `role -> subject` is a FUNCTION); `knot_neuter_rejected` (the Rust `Forgery::neuter_knots` canary as a THEOREM: zeroing a knot contribution with nonzero factors is UNSATISFIABLE). NAMED RESIDUALS, not laundered: the four digest chains are EMITTED but their root-is-the-fold induction is not assembled; the ordering tooth's ℤ-level non-vacuity is not assembled; there is no concrete `Satisfied2` witness yet.
import Dregg2.Circuit.Emit.ParamComposeGolden -- The byte-pinned DEPLOYED-SIZE param-compose descriptor (`n4 p8 l8 k6` at the 28-bit identity namespace; 770 columns / 53 PIs / 863 constraints / 18 Poseidon2 node8 sites) — the shape `entity-compose` actually builds. A separate leaf because the wire golden is ~307 KiB and the refinement surface should not pay for it on every build (the `AutomataflNGenGolden` posture).
import Dregg2.Circuit.Emit.ParamComposeGoldenShapes -- THE REST OF THE TEST CORPUS'S SHAPES, byte-pinned. `pcMin` + `pcRealistic` were the only two shapes Rust could reach, so `param-compose/tests/{composition,size,turn_door,prove_fold}.rs` — which builds FOURTEEN more — could not migrate off the hand-written Rust AIR, and `param-compose/src/{air.rs,builder.rs}` (1028 lines) could not be deleted. Fourteen single-leaf shapes pinned here: the workhorse `n3 p4 l3 k2` (346 cols), the fuel sweep (`n2 p4 l1 k1` at i28/i16, `n5 p6 l4 k3`), the VK-distinctness sweep (each of n/p/l/k/ib bumped off the workhorse), `size.rs`'s `n2 p2 l1 k1` census row at the DEFAULT 28-bit namespace (186 cols — NOT `pcMin`, which is the same bounds at a 2-bit namespace and 108 cols), and the realistic shape's identity-width sweep at ib = 12/16/20/24 (658/686/714/742 cols). Each carries a `traceWidth`/`piCount`/constraint-count/`hashSites`/lookup-count/PI-binding `#guard` plus an `emitVmJson2` byte-pin. STRUCTURAL CROSS-CHECK: `rust_prog(S) = leanWidth(S) + 33` at every shape — `air.rs:312`'s single `zero` column plus the four 8-felt IV groups `air.rs::wide_chain` allocates once per chain, both functions of nothing but "there are four chains", so the delta is shape-INVARIANT by construction rather than a per-shape coincidence.
import Dregg2.Circuit.Emit.ParamComposeGoldenCensus -- The same, at the two shapes `size.rs::staged_leaf_width_census` documents as EXCEEDING the single-leaf cap: `n6 p8 l12 k10` (1277 cols) and `n8 p16 l16 k16` (2462 cols). That test's SECOND assertion — "documented to SEGMENT — if it now fits, update the crate's budget scope" — is a gate that needs a real emitted width at these shapes, so they are pinned too rather than left as the one hole the Rust AIR still had to fill. Separate leaf from `GoldenShapes` because they are 2.26 MiB of wire on nobody's proving path.
import Dregg2.Circuit.Emit.WideValueBindingEmit -- THE LEAN-AUTHORED FULL-u64 VALUE/ASSET BINDING AIR — the replacement route for `circuit-prove/src/shielded/wide_value_binding.rs` (639 lines of hand-written Rust `CircuitDescriptor`: 144 `ConstraintExpr` values + 17 `BoundaryDef`s built by Rust helpers, reaching outside its crate for `cap_node8`/`hash_fact`). It is the PUREST instance of the drift the law names, because it was authored FOR THE FELT-WIDTH REPAIR: a correctness fix went into Rust because the Rust AIR crate was right there. Emitted here: the 128 boolean limb pins + 8 limb recompositions (the repair itself — canonical 16-bit limbs, so the full u64 is representable and nothing aliases mod p before hashing), the 2 compatibility reductions `value_mod_p = Sum 2^16i * limb_i`, the TWO domain-separated `cap_node8` carriers as WIDE arity-16 chip lookups (16 published lanes, the `chip_lookup_sound_N` shape), the compatibility `hash_fact` join on the NARROW bus, and all 17 first-row PI pins. COLUMN CENSUS CROSS-CHECK vs the Rust `col` module: 162 against Rust's 165, and 158 constraints against Rust's 144+17=161 — exactly 3 fewer in BOTH dimensions, the `domain_a`/`domain_b`/`zero` columns and their three `constant_gate`s this file replaces with literal constants in the chip tuples.
import Dregg2.Circuit.Emit.WideValueBindingRefine -- THE SAT=>SEM BRIDGE for that AIR. PROVEN off the emitted gates: `limb_canonical` (the felt-width repair — the sixteen boolean pins plus the recomposition gate FORCE each limb into [0, 2^16) over ℤ; canonicality is PRODUCED, not assumed); `seventeenth_bit_unsat` (the Rust canary `a_seventeenth_limb_bit_has_no_satisfying_trace` universally quantified — a 17-bit limb has NO satisfying trace, for any witness); `vmod_is_the_reduction`/`amod_is_the_reduction` (the deployed one-felt cells are DERIVED from the limbs, not a second free value); `wideA_lanes_forced`/`wideB_lanes_forced` (all eight lanes of each carrier are the genuine squeeze, via `chip_lookup_sound_N`); `published_lane_is_genuine`/`forged_lane_unsat` (the Rust FALSE-polarity splice canary as a theorem); `legacy_is_hash_fact` (the compatibility column is the deployed `hash_fact` over the SAME cells). THE BITE: `legacy_join_cannot_separate_aliases` takes NO hypotheses and NO crypto — on the Rust test's own pair (v, v+p) the one-felt join's inputs are IDENTICAL, so it is provably blind to the alias; `alias_separated_by_the_wide_carrier` closes it under the NAMED floor `WideCarrierCR` (injectivity IN THE LIMBS at fixed randomness/blinds — deliberately NOT the whole opening, which is pigeonhole-FALSE at ~345 bits into ~248; `wideCarrierCR_satisfiable` and `wideCarrierCR_refutable` show the floor is satisfiable and refutable but not provable).
import Dregg2.Circuit.Emit.TinyAutomataCompose -- TINY-AUTOMATA COMPOSITION cost law, measured over EMITTED descriptors. REAL RESULT: nonlinearMults = 0 at every k for table-driven routes vs 4/machine gate-baked (the per-automaton transition interpolant ceases to exist); plus lookupLog_length/forced_trace_length proving a declared exactPublicRows table is a unit-capacity LogUp receive that PINS THE TRACE LENGTH (so 'a table is free data' is false); plus soundness classify_prodList + productDesc_refines_components (composed acceptance implies EVERY component accepts). ⚠⚠ ITS §4 CROSSOVER IS RETRACTED — an adversarial verify showed the lanes route has ZERO satisfying traces at k≥2 (Eulerian words disjoint) and the product route is empty for k≥3; the 'exponential' was a 2-machine-alternation artifact (504/512 rows dead at k=8). See the file header.
import Dregg2.Circuit.Emit.TinyAutomataSatisfiable -- ⚑ THE COMPOSITION GATE PASSED: prodRunWit_satisfies is a REAL Satisfied2Public witness for a COMPOSED descriptor, general in k — the thing whose absence retracted TinyAutomataCompose's headline. The Eulerian obstruction is DISCHARGED BY CONSTRUCTION (runRows_triples makes the trace's triples EQUAL the declared table, so PublicLookupBalanced is Perm.refl, not a search). Four pairwise non-isomorphic machines, reachable product genuinely growing 2→6→12→25. ⚑ THE LAW, on satisfiable objects only, EXACTLY INVERTING the retracted headline: under the run-table discipline product-first is AREA 3·|w| INDEPENDENT OF k (24/24/24/24 at k=1..4, 4 constraints, 2 PIs, 0 nonlinear mults, 654→670 wire bytes) while lanes pay (1+2k)·|w| (24/40/56/72, 4k constraints, 657→2145 bytes) — composing FOUR independent automata costs the SAME CIRCUIT as one. ⚠ Lanes at k=3,4 are MEASURED not WITNESSED (lanes2Wit is 2-lane by construction); whole-graph costs are unwitnessed (Eulerian obstruction stands) and quoted only as the price of attestation. Product-first's unrepaired cost: a run table commits the edges TRAVERSED, not the automaton.
import Dregg2.Circuit.Emit.AttestedAutomatonEmit -- BINDS WHICH AUTOMATON RAN, closing the run-table discipline's named price. ⚑ THE IR FINDING: the subset check is NOT achievable by a lookup — a lookup targets a TableDef and the only content-committing RowSemantics is exactPublicRows, whose contents ARE the descriptor's bytes, so any lookup-shaped binding must declare every edge O(|Q|·|Σ|) AND inherits the unit-capacity Eulerian obstruction. There is NO RowSemantics committed by a ROOT (named IR gap: RowSemantics.committedRows). So it uses MapOp — already a root-committed sorted-map opening: attWit_satisfies (a real Satisfied2Public witness, NO crypto hypothesis — the opening is CONSTRUCTED via a genuine 2^16-leaf heap), root_binds_automaton (two automata under one root AGREE on the whole declared domain), forged_edge_refused (a non-edge is UNSATISFIABLE), attested_exposes_components. Cost: 4 cols / 6 constraints / ZERO declared rows, wire 1190 bytes CONSTANT (one object for every automaton and word length) — measured crossover vs the run-table object at |w|=60. ⚠⚠ FELT-WIDTH WOUND: the root is a ~31-bit scalarRootGroup fed into all 8 deployed lanes, so root_binds_automaton's Poseidon2SpongeCR is instantiated at a BabyBear codomain where CR is FALSE (~2^15.5 birthday). The BINDING is a Lean theorem against the modelled hash, NOT a deployed security property, until the root is 8-felt welded. The WITNESS legs are unaffected.
import Dregg2.Circuit.Emit.TinyAutomataLanesK -- closes the last scope gap in the composition law: lanesKWit_satisfies is a Satisfied2Public witness for the k-LANE composed descriptor GENERAL IN k, plus concrete lanesRunI3/I4 against the COMMITTED descriptors — so the k=3,4 lanes points are no longer measured-but-unwitnessed. Each lane's run table declares its OWN traversed edges (|w| rows), all reading one shared symbol column, so every lane's PublicLookupBalanced is Perm.refl and the lanes need no COMMON Eulerian word (which is exactly what made the whole-graph lanes route unsatisfiable). The law now stands on witnessed objects at every measured point: product-first area 3·|w| INDEPENDENT of k vs lanes (1+2k)·|w|.
import Dregg2.Circuit.Emit.AttestedAutomatonWeld8 -- 8-felt WIRE weld of the attestation root: carries 8 root columns (W8ROOT 0..7, pinned to 8 PIs + 8 constancy windows, verified on the emitted JSON), with root8_binds_automaton_or_collides re-proven over mapRoot8/opensToMerkle8 consuming NO Poseidon2SpongeCR (the hypothesis BabyBear refutes is GONE from the binding leg) and weld_collision_disjunct_refutable showing the disjunct is REFUTABLE, not a pigeonhole free pass. Witness: weldWit_satisfies_constructed, unconditional. ⚠⚠ THE WELD IS WIRE-ONLY — DescriptorIR2.MapOp.holdsAt/rowAt read (m.root 0), LANE 0 ONLY (:580-584), so the forced opening is still the ~31-bit scalar and lanes 1-7 are content-UNFORCED — weld_completion_lanes_free EXHIBITS this (same descriptor satisfied with two different completion-lane vectors). The binding is NOT yet a deployed security property; the remaining fix is a SHARED DescriptorIR2 edit moving holdsAt onto opensToMerkle8. Cost: 11 cols / 10 PIs / 20 constraints, wire 2596 bytes CONSTANT; the weld moves the byte crossover vs run-table 60 → 201. Aux rows UNCHANGED (5·|w| model, 16·|w| chip).
import Dregg2.Circuit.Emit.CommittedRowsSemantics -- ADDITIVE prototype of the named IR gap (RowSemantics.committedRows): a table whose contents are committed by a ROOT rather than by the descriptor's own bytes — the missing vocabulary that forced the automaton attestation through MapOp (16·|w| chip permutations) instead of an O(|w|) lookup. Adds NO constructor to the shared enum (the data rides as a shadow CommittedRowsDecl), so DescriptorIR2 is untouched. ⚠ The semantic break to respect when landing it for real: TableDef.publicContentsFaithful falls through to `True` for every non-exactPublicRows semantics, so a real constructor would inherit NO contents obligation and compile clean — constructor and obligation must land together, wired into Satisfied2. See docs/DESIGN-committed-rows-semantics.md.
import Dregg2.Circuit.Emit.DfaRoutingSubsetTableCost -- the subset-table cost companion for the guard→circuit pipeline (cost model over the emitted routing descriptor).
import Dregg2.Circuit.Emit.TinyAutomataPacked -- PACKS s automaton steps per trace row: packWit_satisfies is a real Satisfied2Public witness for the packed descriptor, the witnesses' OWN trace lengths measure 8/4/2/1 at s=1/2/4/8 (= n/s, so the packing genuinely happens), and s=1 is BYTE-IDENTICAL to the unpacked run-table descriptor (the family contains the deployed shape). ⚑ THE CAPABILITY UNLOCK: with ⌈n/s⌉ ≤ MAX_EXACT_PUBLIC_ROWS = 128 the deployed word-length ceiling becomes n ≤ 128·s, so s=8 is what brings n=1024 into reach at all. ⚠ THE SPEED WIN IS SUBLINEAR AND THE FILE SAYS SO: area (2s+1)·(n/s) = 24/20/18/17 falls only toward a 2n+s floor — width grows as height shrinks — which is exactly what the profile's width-sensitive first-digest layer predicts; wire bytes GROW 673→1316. ⚑ It also caught a bug in the shared cost model — NOW FIXED (2026-07-26): costOf.forcedTraceRows assumed lookupCount = 1 and was WRONG for s > 1. The MODEL was repaired (not the call sites): the field is a three-valued ForcedRows (unpinned | pinned n | contradictory) computed as declaredRows / lookupCount per table and meet-ed across tables, DescCost.area is Option Nat, and costOf_forced_sound PROVES that for any descriptor carrying a Satisfied2Public witness the published number is `pinned` at that trace's ACTUAL row count — the field can no longer silently lie.
import Dregg2.Circuit.Emit.PastaMsmAir -- ⚑⚑ THE CHECKER, NOT THE INSTANCE — the Mina opening check as LEAN-AUTHORED AIR (`def`-generators producing `List VmConstraint2` over `AirBuilder.Head`, plus `*_forces` over `acceptB <emitted list> a = true`; no Rust hand-writes a gate here). `openingCheck_forces`: any assignment satisfying the emitted constraints has `msm(terms) + delta = O` in `ZMod p` — X and Z both zero, which in the RCB projective model IS the point at infinity — i.e. `SRS::verify`'s statement (B). ⚑ AND IT DOES NOT SCALE WITH TERM COUNT: `n` and `nDigits` are universally quantified and appear nowhere in the proof but as fold bounds, one induction composing `pallasLadder_forces` (K4b) with `pallasRcbStep_forces` (K4a) — **the statement at n = 32,768 is the same theorem as at n = 1.** That is what makes this a theorem about the CHECKER rather than a 3.5-hour computation about one block. The new terminal gadget `fpIsZeroCore` forces a CONGRUENCE, not literal zero limbs — a gadget demanding literal zeros would refuse honest proofs, i.e. be true because nothing satisfies it — with seven `#guard`s exhibiting acceptance at canonical AND non-canonical zeros and refutation at non-zeros, so it is known satisfiable and refutable at birth.
import Dregg2.Circuit.Emit.PastaMsmLayouts -- ⚑ HORNER + GLV, and the answer is DEFER. `hornerGates_forces` is a two-nested-`List.range` induction with `n`/`nbits` appearing nowhere but as fold bounds, and `horner_layout_is_witness_independent` states the skip obstruction AS A THEOREM: the emitted gate count is a function of `(n, nbits)` ALONE and `Assignment` does not occur in the generator's type. ⚑ SO THE HORNER WIN IS 2×, NOT 4× — the 4,161,791 set-bit count is not the height of any emitted trace; the emitted height is `nbits·(n+1) = 8,356,095`. GLV is built and SOUND, and it buys soundness before speed: `glvBind_forces` + `bitsBind_forces` close `PastaMsmAir` §6.6, where **nothing previously said the ladder's digits were the bits of the scalar.** ⚑⚑ TWO CORRECTIONS TO THE P4 ENDO RESULT AS REUSED HERE: it reduces **Vesta's** lattice (`x ≡ y·lambdaVesta mod pN`) while this AIR is **Pallas** (scalars mod `qN`, `lambdaPallas`) — two `#guard`s show the Vesta pair norms to `pN` and NOT to `qN`, so §5b reduces the right lattice with the same three certificates; and the 2^70 minimality bound **does not transfer and is not wanted**, because at 128-bit halves there are ~2^1.5 representations per scalar and **soundness never wanted uniqueness — every representation multiplies to the same point.** Here the lattice is a COMPLETENESS certificate, not a soundness one. PRICES as theorems: (B) 17,375 → **8,773** adds and 573,377 → **315,691** gates, one segment at 13.4%; (A) best layout **4,227,200 rows = 2.016× the `2^21` ceiling**, 65 segments. Non-obvious: GLV halves the serial doubling depth **which the bit-plane scan has already amortised to nothing**, so as a separate term it is measurably WORSE (8,388,736 vs 8,356,095, guarded); the 4-entry table recovers it. ⚑ RECOMMENDATION, TAKEN: **DEFER the `sg` leg, do not optimise it.** `2^21` is BabyBear's two-adicity at `log_blowup 6` — a property of the FIELD, so no algorithm in this vocabulary reaches it; it misses `2^22` by exactly the 128 doublings, and closing that means `log_blowup 5`, which weakens a rate and is ember's call. `sVec_eq_bPoly` already proves what deferral needs, and deferral is BATCHING not skipping — N claims fold into one, amortised cost → 0. **2.016 × 2^21 against O(1) per block is not close.** Pippenger priced and REFUSED with a reason: ~10^6 rows would fit, but bucket accumulation is data-dependent routing and `VmConstraint2` has no permutation/sort kind; the only door is `MemOp` under a Blum multiset check — a new prover arm for a constant factor on an object we are deferring. ⚠ NOT CLOSED: GLV COMPLETENESS is MEASURED, not proved (5,000 scalars all decomposed, 20,000 more never needed a 129th bit) — provable at 129 via covering radius or at 128 via a `{1,λ,λ²}` 8-entry table. ⚑ And the instrument went RED on its author: it asserted `glvPallas_not_shorter` at `2^127` and the KERNEL REFUTED IT (the coordinates are ~2^126.2), which is the only reason to trust the greens.
import Dregg2.Circuit.Emit.PastaMsmWindowed -- ⚑ THE WINDOWED RE-LAYOUT, and it was an ORPHAN — reachable from nothing, in no CI target, for the whole of its life. That is the `minted-gating-defaults-to-silence` shape exactly: `PastaMsmAir.minaOpeningCheckDesc` declares `traceWidth 442` while addressing columns near `10^7`, so the DEPLOYED prover would REFUSE it and every row figure in that arc was conditional on a re-layout nothing emitted. This file emits it — `DescriptorIR2.WindowExpr` (`loc`/`nxt`), the primitive the IR had carried since the aggregation AIR and no `Pasta*` generator had used. The structural change: the straight-line emitter's list GROWS with the computation (573,377 gates for statement (B)); this one is **45 constraints, a CONSTANT at every `(n, nbits)`** (`windowedRowDesc_constraints_length` is `rfl`), because the computation moves into the TRACE, which is what an AIR is for. `windowedRowDesc_columns_in_bounds` discharges `descriptor_ir2.rs`'s own `chk` closure by `decide` BEFORE the prover sees the descriptor. `windowedRows_forces` is row-count-INDEPENDENT (`h` occurs only as an induction variable, so the statement at `h = 8,925` IS the statement at `h = 1`) and `windowed_forces_hornerRef` is the weld: at the Horner schedule the flat fold the rows perform IS `PastaMsmLayouts.hornerRefFrom`, so windowing changes the TRACE SHAPE and nothing about the relation. ⚠ NOT CLOSED, and §6 says so: the `DBL` PATTERN down the column is hypothesised (the doubling ITSELF is structural — `dblRow_forces`); the ℤ↔felt gap is the shared K1 residual; on-curve-ness is not gated and the `O`-skip encoding makes that BITE; P10 is untouched.
import Dregg2.Circuit.Emit.PastaMsmSliced -- ⚑⚑ THE FOUR-WAY CUT of the terminal `⟨s, srs.g⟩` leg, EMITTED — four AIR instances that each NAME THEIR OWN SLICE, riding in ONE dregg proof. `PastaIpaDeferral` §4b priced the discharge at 4,227,200 adds = 2.016× the `2^21` ceiling at EVERY `N` (amortisation moves the average, not the object); cut FOUR ways it divides and clears where TWO misses by 16,512 rows and THREE is ragged. The row template is REUSED, not re-authored — `slicedRowDesc_extends_windowed` proves `PastaMsmWindowed`'s 45 constraints are still a PREFIX of the emitted list, and the file adds 4 declaration gates + 29 `piBinding`s. ⚑ AND IT CORRECTS THE HOLE AS IT WAS STATED: a prior lane read "a group sum forgets its order, so wrong-paired instances still re-sum green". §3 exhibits all three cases and the framing was half wrong — `permutation_is_harmless` (re-ORDERING correct partials really is harmless, so ORDER is NOT what to pin), `cross_pairing_breaks_the_sum` (slice k's scalars against slice j's generators gives 21,430 against 43,210 — THAT is the hazard), `duplicated_slice_breaks_the_sum` (420). So PAIRING and COVERAGE are what must be pinned: `slicePartial` takes ONE index and uses it for BOTH lists, making a cross-paired claim UNSTATEABLE, and `slices_compose` (slice-count-independent, arbitrary `AddCommGroup`) consumes `∀ k < n`. The bounds are in the EMITTED OBJECT too: `lo` is a LITERAL in the gate (so the four descriptors are four distinct objects), `[lo,hi)` and the 27-limb partial are public inputs, and `sMaxVar`/`sMaxPi` re-decide the deployed checker's column AND `pi_index` predicates — `PastaMsmWindowed.cMaxVar` returns 0 on the `piBinding` arm, which is correct for a file emitting none and a SILENT HOLE for one that does. PROVED: `circuit/tests/pasta_sliced_sg_prove.rs` puts four instances through the deployed prover and verifier with 9 tampers refused. ⚠ NOT CLOSED, §7: the slice DECLARATION is bound, the slice CONTENTS are not (nothing says `SRC` carries `srs.g[lo+t]` — the exact-public generator table is the named next rung); and clearing `2^21` is not being provable — 1,056,896 rows PADS TO `2^21` exactly, so the cut lands AT the ceiling, and an EIGHT-way cut is what actually leaves headroom.
import Dregg2.Circuit.Emit.PastaMsmBound -- ⚑⚑ THE SLICE CONTENTS, BOUND — the rung between "a proof of a shape" and "a proof of the Mina claim". `PastaMsmSliced` §7.1 named its own hole exactly: the slice DECLARATION was bound and the slice CONTENTS were not, so NOTHING in the emitted constraints said a row's `SRC` columns carry `srs.g[lo+t]` and a prover could declare `[0,8192)` and fill the rows with any points. `circuit/tests/pasta_bound_sg_prove.rs::substituted_generator_before_and_after` MEASURES that: one conditional-add row rebuilt around a DIFFERENT REAL Mina SRS generator, whole trace regenerated so every RCB add and carry holds, is PROVED AND VERIFIED by the contents-unbound descriptor and REFUSED by this one (`LookupError(GlobalCumulativeMismatch): ir2_exact_public_47`). The mechanism is an EXACT-PUBLIC table of `(row key, absolute generator index, digit, 27 generator limbs)` plus a THREADED row-index column and ONE guarded per-row lookup — and `TableSem.exactPublicRows` is stronger than a subset lookup: `DescriptorIR2.PublicLookupBalanced` demands a PERMUTATION, so the manifest says THESE ROWS AND NO OTHERS. `row_tuple_is_its_manifest_row` is the deliverable and it is ROW-COUNT-INDEPENDENT (pure membership + `manifest_key_unique`, no counting, no pigeonhole; `w`/`planes`/`i` occur in no bound). It buys more than the generators: `bound_forces_doubling`/`bound_forces_dbl_off` DERIVE the `DBL` PATTERN that `PastaMsmWindowed` §6.1 left hypothesised, and `bound_forces_digit` pins the digit. `manifest_names_slice_index` + `slice_generator_is_sliceAt` join the contents forcing to `PastaMsmSliced.slices_compose`'s `sliceAt w k gens`. ⚠ NOT CLOSED, §7: the DEPLOYED exact-public tooth caps a manifest at 128 rows / 4096 cells and spends ONE batch AIR instance per row, and a PERMUTATION balance means manifest rows = TRACE rows — so a contents-bound instance is at most 128 rows tall. The real cut needs 1,048,704 manifest rows, 8,193x the cap (the eight-way cut, 4,097x). The theorems are row-count-independent; the DEMONSTRATION is 124 real generators. Also: the manifest's GENERATORS are Mina's, its SCALARS are synthetic (the s-vector binding is a different rung); the ℤ↔felt gap and P10 are untouched.
import Dregg2.Circuit.Emit.PastaMsmScalarBound -- ⚑⚑ THE SCALARS, BOUND TO THE BLOCK — `PastaMsmBound` bound the slice's GENERATORS and named the residual it left: the manifest's digits were a reproducible sequence, not the block's s-vector, so a verified proof described Mina's real generators folded over scalars nobody had tied to anything. This closes the other half WITHOUT witnessing 32,768 scalars, by using K4c's tensor: `sAt` reads the `i`-th entry of `PastaIPA.sVec` in |cs| multiplications (`sVec_getElem?_eq_sAt`), so the emitted manifest's digit column is a 15-ELEMENT DERIVATION from `MinaWrapOpeningGate.CHAL_F` — the challenges that file DERIVES by running devnet block 539508's own Fq sponge forward and lifting through `endoMap ENDO_R`. `bound_forces_block_digit` then composes the ALREADY EMITTED lookup: a satisfying trace's `BIT` column IS the plane-th bit of that block's `s_{lo+t}`, with `i`/`w`/`planes`/`N` in no bound, and `bound_row_is_block_term` states both halves at ONE index. The rung's defining test fires in-kernel: a trace whose digits are a DIFFERENT block's s-vector — same generators, same row keys, same `DBL`, internally consistent, exhibited BALANCING against its own manifest — is REFUSED. Adds no constraint (still 82), no table, no manifest column, no wire id; the per-slice `GEN_TID k` bus split is re-pinned here. ⚠ §7.1 is the honest limit: the tensor is bound at the DESCRIPTOR's manifest, not recomputed by an emitted constraint — `WindowExpr` has only `loc`/`nxt` and the Horner schedule puts same-term rows `w+1` apart, so recomposing a scalar from its per-plane digits is not expressible without a stride window arm. RCB→group-law, ℤ↔felt, P10 and the 128-row exact-public cap are inherited unchanged
import Dregg2.Circuit.Emit.PastaMsmOnCurve -- ⛑⛑ THE ON-CURVE GATE, and it closed a LIVE FORGERY rather than a bookkeeping residual. Every `Pasta*` rung up to `PastaMsmScalarBound` forces `hornerRefFrom` — a fold of the RCB FORMULA over projective triples — and RCB'15 Thm 1's hypothesis is that the operands are ON THE CURVE, which NOTHING in the emitted constraints said. `PastaMsmWindowed` §6.3 named the consequence and left it standing: RCB Alg. 7 at `Q = O` returns `Y₁·(X₁,Y₁,Z₁)`, so at `Y₁ = 0` it is `(0,0,0)`, which is ABSORBING (`origin_is_absorbing`, a `ring` identity for EVERY addend) and passes `PastaMsmAir`'s terminal `X ≡ 0 ∧ Z ≡ 0` predicate. MEASURED, not argued: `circuit/tests/pasta_oncurve_gate.rs::absorbing_state_before_and_after` takes the free initial accumulator to `(0,0,0)`, keeps the REAL Mina generators in `SRC` and the REAL declared digits in `BIT` — every RCB add, carry, quotient, thread and the whole exact-public lookup still exact — and the CONTENTS-BOUND descriptor **PROVES AND VERIFIES IT** through the deployed verifier. That is the Mina claim, forged, with every binding rung green. ⛑ THE CURVE EQUATION ALONE CANNOT CLOSE IT: `Y²Z = X³ + bZ³` is HOMOGENEOUS, so `(0,0,0)` SATISFIES it. What closes it is a NON-DEGENERACY witness — `Y·YINV ≡ 1`, i.e. `Y` a unit — which on Pallas (prime order, so no 2-torsion) is EQUIVALENT to "this triple is a point" and is the same condition RCB needs for `P + O` to return a correct representative. EMITTED: 8 constraints / 135 columns per gated point, on BOTH operands of every row's add (`ACC` and `SRC`); `OP` needs none (`condRef_is_a_curve_point`), `OUT` needs none (the emitted thread makes it the next row's `ACC`). PRICE: 82→98 constraints, 529→799 columns, manifest and row count UNCHANGED; `onCurveRowDesc_extends_bound` proves the 82 are still a PREFIX. `fold_accumulator_is_a_curve_point` composes `windowedRows_forces` unmodified: EVERY accumulator the whole fold passes through is a Pallas point with `Y` a unit. Plus `skip_is_projective_identity` (the `O`-skip is a no-op exactly because `Y` is a unit — the wound §6.3 named, as a theorem) and `terminal_is_the_identity` / `origin_is_not_the_identity` (the terminal predicate finally MEANS the identity; before the gate it accepted a non-point). SATISFIABLE AT BIRTH: the honest cut proves+verifies (837 ms / 53 ms / 495,512 bytes) and 500 of 512 honest accumulators are NON-AFFINE, so a gate accepting only `Z = 1` would have refused every honest trace. THREE TAMPERS LIVE ON ONE INSTANCE: curve (`OodEvaluationMismatch index 0`), generator (`LookupError GlobalCumulativeMismatch: ir2_exact_public_47`), digit (`OodEvaluationMismatch index 4`). ⚠ THE TRANSPORT IS STILL NOT DISCHARGED, and §5 says exactly what moved: RCB'15 Thm 1's HYPOTHESES are now FORCED; its CONCLUSION — that `rcbOutG` at on-curve inputs IS the group sum — is still an inherited citation, needing a formal Pallas group law plus a cofactor certificate for a degree-12 identity in 6 variables. So a verified proof constrains a formula fold OVER CURVE POINTS, not yet a group-law MSM. ℤ↔felt (K1), P10 and cofactor-1 are untouched.
import Dregg2.Circuit.Emit.PastaMsmScalarDerive -- ⛑⛑ THE TENSOR, RECOMPUTED IN-CIRCUIT, AND ITS VALUE PINNED CANONICAL — `PastaMsmScalarBound` §7.1 said the s-vector was bound at the DESCRIPTOR's manifest and not recomputed by an emitted constraint, and named the blocker: a row carries ONE bit of its scalar, the other 254 live at the same term index in the other 254 planes, `WindowExpr` has only `loc`/`nxt`, and the plane-outer Horner schedule puts same-term rows `w+1` apart — so "recomposing a scalar from its per-plane digits" needed a stride-`k` window arm or a column-major schedule. ⛑ THE DIRECTION WAS BACKWARDS. RE-composing from digits spread across planes is cross-row; DE-composing a scalar into digits is ROW-LOCAL. This file puts the scalar ITSELF on the row — `nb` emitted `PastaField.fqMulCore` steps from the challenges ON THE WIRE (`9·nb` new public inputs, first-row-pinned AND threaded, because a pin alone leaves them free on rows 1..) selected by the row's own `GIDX` digits — then witnesses all `planes` binary digits with one recomposition gate and reads its own plane off with a selector. Nothing crosses a row boundary, so NO IR CHANGE AND NO SCHEDULE CHANGE: `deriveRowDesc_extends_onCurve` proves `PastaMsmOnCurve`'s 98 constraints are still a PREFIX, the manifest/table/wire id are untouched, and the generator + wrong-block-scalar + absorbing-state tampers stay live on the same instance. ⛑ AND SHARING WAS NEVER WORTH AN IR CHANGE: an AIR's columns are GLOBAL, so a stride-`k` arm would still commit the chain's ~1000 columns on all `planes·(w+1)` rows and would raise the gate degree — identical committed area, which is what FRI pays for. ⛑⛑ §2.7 IS THE CANONICITY CERTIFICATE, AND IT RETIRES A SENTENCE THIS FILE ITSELF GOT WRONG: booleanity pins the chain's landing value into `[0, 2^planes)`, but `planes = 256` while `q < 2^255`, so `s, s+q, s+2q, s+3q, s+4q` ALL fit and `fqMulCore` witnesses a QUOTIENT — shifting the landing block by `q` and the last quotient block by `1` leaves the chain gate exactly zero over ℤ. Measured on the deployed instance: `s` and `s+q` differ at 58 of the 256 planes, and ONLY THE MANIFEST refused an internally consistent non-canonical row. The fix is the standard less-than-the-modulus certificate — `s + Σ_{p<255} 2^p·CBc p = q − 1` over `CBITS = 255` boolean columns, `CBITS` being the CONSTANT 255 and never `planes` (writing `planes` there refuses every honest trace at small plane counts). PRICE: +`265 + 37·nb + 2·planes` columns and +`264 + 29·nb + 2·planes` constraints — at the deployed shape (15 challenges, 256 planes) 799→2131 columns and 98→1309 constraints, of which the certificate alone is +255 columns and +256 constraints. FORCED: `mulSel_forces` (the multiplier is the selected challenge), `chain_forces` (the running product IS the tensor of the wire challenges in `ZMod q`, induction over the CHALLENGE count, never the row count), `sel_shape_forced` (⛑ the selector's indicator SHAPE is now DERIVED from `selOneGate`+`selIdxGate`+booleanity, not assumed — `sel_forces_closed` has no shape hypothesis), `canon_forces` (`0 ≤ s < q`), `sBits_are_the_digits` and `gidxBits_are_testBits` (boolean decompositions are UNIQUE), and ⛑⛑ `derived_is_sNat` — the landing value IS `PastaMsmScalarBound.sNat cs GIDX`, so `derived_row_bit_is_block_svec_bit` and `derived_row_bit_is_manifest_digit_at_a_named_plane` range over the CANONICAL s-vector rather than over a witnessed decomposition. ⚑⚑ §4e — THE WIRE GATES FORCE, THEY ARE NO LONGER ASSUMED. `pidxStartGate`, `pidxThreadGate`, `chalPinGates` and `chalThreadGates` were EMITTED and appeared in exactly ONE theorem in the file — `deriveGates_length`, a COUNTING lemma — while every deliverable took their content as raw hypotheses (`hwire`, `hpidx`); the module assumed what it emitted, and `circuit/tests/pasta_derive_prove.rs` stated the stronger reading. Discharged in `PastaMsmBound.tidxThread_forces`/`tidx_is_the_row_index`'s shape, row-count-independently: `DeriveStartAccepted`/`DerivePiAccepted`/`DeriveWindowAccepted` are three denotations over the ACTUALLY EMITTED `deriveWireGates` (they exist because `acceptB` is `List.all gateBodyEvalZero` and that returns TRUE on every constructor that is not `.base (.gate _)` — the row-local denotation §4–§4d is stated in CANNOT SEE a `.boundary`, a `.piBinding` or a `.windowGate`); `chal_is_the_wire`/`wire_forces_challenges` discharge `hwire` (every row's challenge columns are the DECLARED PUBLIC INPUTS', pin PLUS thread — a pin alone leaves rows 1.. free); `pidx_counts_doublings` needs NO `DBL` hypothesis at all and `pidx_is_the_plane_index` discharges `hpidx` (PIDX IS `PastaMsmBound.planeAt w i`); `dblPattern_of_manifest` derives the `DBL` pattern itself from `bound_forces_doubling`/`bound_forces_dbl_off`. ⚑⚑ §4f IS THE FIX TO THE REDUNDANCY CLAIM: `derived_row_bit_is_manifest_digit` held for the manifest's INDEX argument and NOT for its PLANE argument (`pl` was a caller's parameter while the manifest declares row `i`'s digit at `planeAt w i`), so the row-local statement is renamed `_at_a_named_plane` and SAYS SO, and the unqualified name is now the trace-level theorem that reproduces BOTH arguments from the emitted object. §5d MEASURES the forgery the thread kills: `katSwapRow1`'s row 1 derives completely and consistently against a DIFFERENT block's challenges, `acceptB (deriveRowGates 2 4)` ACCEPTS it (guarded, not asserted) and the emitted challenge thread REFUSES it; plus a jointly-satisfying eight-row trace so §4f is not true-because-empty, and four single-cell refusals (PI pin, origin pin, plane thread advancing on a non-doubling row, row claiming a foreign plane). BITES AT BIRTH: an honest row ACCEPTS, an honest DOUBLING row accepts (the join's guard is load-bearing — `BIT` is pinned to 1 there and an unguarded join would refuse every honest trace), the certificate accepts at `s = 0`, `s = 5` and the TIGHT top `s = q−1`, and the non-canonical representative `s+q` is REFUTED in the kernel at `planes = 256` under every certificate a prover could write — while the bound gate ALONE accepts it once the certificate may go negative, which is the measurement that the 255 boolean pins are the whole content. ⚠ NOT CLOSED, and §6 IS NOW THE LIST (this was the only module in the family without one, and its header forward-referenced a §6 that did not exist): ⚑ §6.2 THE LARGEST RESIDUAL, never named before — nothing in this descriptor says the DECLARED PUBLIC INPUTS ARE the block's IPA transcript challenges; the gates bind every trace row to whatever `pi` carries, and that `pi` is the block's is the light client's own check, a different rung. §6.1 names what is still carried: `hgidx` (`PastaMsmBound.bound_forces_gidx`'s) and `PastaMsmBound`'s own two side conditions (`htidx` from `tidx_is_the_row_index`, the `DBL` column's booleanity from the row template). ⚑ §4e.1 CLOSED the transport this list first named as missing: `deriveGates_noLookup` (the derivation emits no `.lookup`, at every `(nb, planes)`) ⟹ `derive_lookupLog` (the lookup LOG is the rung-below's, unchanged) ⟹ `derive_balance_transport` (the DEPLOYED derived descriptor's exact-public balance IS the one `bound_forces_doubling`/`bound_forces_dbl_off` consume), so the chain runs end to end in the kernel: emitted lookup ⟹ manifest row ⟹ `DBL` pattern ⟹ `PIDX = planeAt w i`. §6.3 prices the sharing rung IN THE KERNEL: the derivation block is 5,455,872 of the cut's 8,728,576 committed cells (62.5%), the bussed shape would be 21,312 — a factor of exactly `planes` — and it needs a `provide` dual of `Lookup` the IR does not have. §6.5 ⚠ `wireAcceptB`'s match ends in `_ => true`, a FAIL-OPEN shape: a fifth constructor in `deriveWireGates` would pass every §5d guard silently, so any addition must extend the match and the three predicates in the same commit. ℤ↔felt (K1) is inherited unchanged — every forcing theorem here is the ℤ reading of the emitted bodies and the deployed prover reads them in BabyBear, where a weighted boolean sum can wrap; the MANIFEST is still the SOLE binding of a generator INDEX to generator COORDINATES (no emitted gate relates them), so it is retained deliberately and only its `digit` field is now redundant; P10 and the RCB transport are inherited unchanged.
import Dregg2.Circuit.Emit.PastaBasePrime -- ⚑ `Nat.Prime pN` DISCHARGED — the missing hypothesis `KimchiVerify`, `KimchiRealProofGate`, `PicklesRecursion` and `PastaIpaDeferral` each name as "no `Field (ZMod pN)` instance in the tree, 255-bit primality by `decide` infeasible". A recursive Pratt/Lucas certificate (reusing `Dregg2.Crypto.BN254.Primality`'s generic `powModFuel`/`lucasList` combinator VERBATIM rather than re-deriving it), factoring `pN − 1 = 2³²·3·463·a·b` with `a` (69-bit), `b` (143-bit) two more Lucas layers deep — the factorization computed by `sympy.factorint` outside Lean, CHECKED inside it (a wrong factor list fails `hprod`/`hprime` in the kernel). Installs `Fact (Nat.Prime pN)`, so `ZMod pN` is now a genuine `Field` wherever this is imported.
import Dregg2.Circuit.Emit.PastaGroupLaw -- ⚑⚑ RCB'15 THEOREM 1's CONCLUSION, DISCHARGED for the two cases the deployed fold actually runs. `PastaMsmOnCurve`'s own §5.2 named exactly two objects as missing: (1) a formal Pallas group law — built here by INSTANTIATING Mathlib's `WeierstrassCurve.Projective.Point` at `a=0,b=5` over `ZMod pN` (its `AddCommGroup`, associativity included, is MATHLIB's theorem, inherited by instantiation, not re-derived) — and (2) a cofactor certificate for a degree-8-in-6-variables identity showing `rcbOutG`'s output is a nonzero projective rescale of Mathlib's OWN addition/doubling formula. Both cofactor identities (found by Gröbner-basis reduction in `sympy` outside Lean, `ring`/`linear_combination`-checked inside it) are exact: the DOUBLING case is a literal equality (`X` unconditionally, `Y`/`Z` given the curve equation, cofactors `9b·Z` and `−6·Y`); the generic-ADD case (different `X`-ratios, both operands away from the point at infinity) is a projective equivalence via the two cross-multiplied identities Mathlib's own `equiv_of_X_eq_of_Y_eq` wants. `toPoint_dbl_eq`/`toPoint_add_eq` compose these with `Equation`↔`OnCurveZ` (unconditional) and `Nonsingular` (via `W.IsElliptic`, Δ = −10800 ≠ 0 — so NO 2-torsion/cofactor argument is needed at all, unlike the informal "Pallas has prime order" citation this replaces) into: **a verified proof of `PastaMsmOnCurve`'s AIR now constrains the accumulator, at every double or generic-add row, to be the ACTUAL Mathlib group sum of its operands — a fold of the GROUP LAW, not merely a fold of the RCB formula over curve points.** ⚠ NOT covered, named precisely: `Q` a different representative of `−P` (RCB is empirically complete there too — checked numerically — but no cofactor certificate was extracted); ℤ↔felt (K1), P10, the manifest-bound tensor and the 4-generator shortfall are untouched, as before.
import Dregg2.Circuit.Emit.MinaOpeningCheckWeld -- the weld against "a true theorem about the wrong predicate": `air_predicate_is_the_real_relation` proves the AIR's reference object **IS** `MinaWrapOpeningGate.openingResidual` cast into `ZMod p` — BY IDENTITY, not by two computations agreeing — and `air_predicate_holds_on_the_real_block` then reads the existing `opening_relation_holds` to conclude the forced predicate is satisfied by devnet block 539508. The real content is `rcbAddM_cast`: RCB's five subtractions are TRUNCATED over `Nat`, so `cast_Sb` carries `y < p` and discharges it at each site by `Nat.mod_lt`. The reality gate and its ten tamper poles are untouched — the differential checks the instance, this checks the checker. ⚑ THE PRICE, against MEASURED deployed geometry (root `|D⁰| = 2^22` = trace `2^16` × blowup 6, `TWO_ADICITY 27` ⇒ `2^21` rows, `MAX_TRACE_WIDTH = 1024`, `F ≈ 0.094 ms`/row), with the counts a THEOREM (`openingCheckGates_counts`) rather than an evaluated list: statement (B) at 34 terms × 255 digits is **17,375 RCB complete adds / 573,377 core gates / 442 columns** — **inside ONE deployed root segment at 26% of its height**, 582 columns to spare, 7.68M cells, 13× a leaf descriptor that proves in 638 ms. ⚠ **THAT "FITS TODAY" CLAIM IS RETRACTED AS STATED** (`c6fea0ecf`): the `Head`/`cgH` vocabulary is STRAIGHT-LINE — every generator emits one flat single-row list — so "one complete add per row of 442 columns" is a RE-LAYOUT NOTHING EMITS. It needs `DescriptorIR2.WindowExpr` (`loc`/`nxt`), which the IR has and the emitter family does not. And `minaOpeningCheckDesc` **would be REFUSED by the deployed prover**: `traceWidth := 442` while its constraints address columns near 10^7, and `circuit/src/descriptor_ir2.rs:1555` rejects `column ≥ trace_width`. Every row figure below is CONDITIONAL on a windowed emitter that does not yet exist. (Also: `MAX_TRACE_WIDTH = 1024` is ADVISORY on the IR-v2 path, and `F ≈ 0.094 ms/row` is the FRI-fold term only.) The `⟨s, srs.g⟩` leg is the expensive one (16.7M adds, 8× past a single proof; 4.16M via Horner, still 2×) — and that leg is **exactly what Pickles itself defers into an accumulator**, so the honest move is to defer it the same way rather than compute it. ⚠ NAMED GAPS: the Horner AIR is not emitted (a data-independent layout is 8.36M rows; 4.16M is the SET-BIT count and needs a skip argument nothing builds); P10 untouched (relation holding ≠ prover knowing); `srs.g` still the largest trusted datum; no satisfying assignment materialised (the 573,377-gate witness is the prover's job — the witness-gen perimeter); canonicity not gated (`pastaLimbRange` costs ~30× columns and breaks the one-row width fit); digits are represented inputs and a built GLV selector roughly halves `nDigits = 255`.
-- ⚑ THE `MinaWrapSg*` INSTANCE FAMILY IS DELIBERATELY NOT ROOTED (2026-07-29). Rung 5h's `sg == ⟨s, srs.g⟩` KATs over 32,768 real generators are the INSTANCE
-- DIFFERENTIAL for ONE Mina block, not a theorem about the checker — and rooting them made `lake build Dregg2` IMPOSSIBLE TO COMPLETE: the root reached 10444/10448
-- and `MinaWrapSgChunk1` was killed at 1212 s with exit 143 (earlyoom SIGTERM), each chunk peaking near 75 GB. A root that cannot finish means `#floor_ratchet` — the
-- tree's primary instrument — can NEVER return an unqualified verdict, which costs more than the KATs are worth. `PastaIpaFold` STAYS ROOTED: it carries the folding
-- identity at arbitrary `AddCommGroup`/`Module`, gate-count-independent, which is the CHECKER theorem. Run the instance family on demand:
--   scripts/run-mina-sg-instance.sh    (see scripts/lean-orphans-allow.txt for the reasoned exclusion)
import Dregg2.Circuit.Emit.PastaIpaFold -- ⚑ RUNG 5h DISCHARGED — `sg == ⟨s, srs.g⟩` over ALL 32,768 real generators, with the 32,768 scalars DERIVED IN-KERNEL from the block's 15 transcript-derived challenges. `msm_sVec_eq_fold` proves the folding identity by induction on `k` over an arbitrary `AddCommGroup`/`Module`, reusing K4c's `sVec` rather than restating it, with orientation MEASURED not guessed (extractor GT9 folds the real `srs.g` and asserts equality to both `⟨s,srs.g⟩` and `opening.sg`, with three controls — reversed challenge order, swapped halves, `chal_inv` — ALL FALSE). ⚑ AND THE CORRECTION THAT MATTERS: **folding is the right STATEMENT and the wrong EVALUATOR.** Round `j` multiplies `2^(k−1−j)` DISTINCT points by `c_j`, so the fold costs `2^k − 1` scalar multiplications against the naive `2^k` — **it saves exactly one**, and re-serialises the 255-step doubling chain once per point. The "~15 ladders" estimate counted one per ROUND where it is one per OUTPUT ENTRY. What actually moved the cost is `msmHorner_eq_msmN`: ONE doubling chain shared across all `2^15` terms, one addition per set bit — 4.2M complete adds against 16.7M. **Expanding the scalars was never the expensive half**; `2^15` field multiplications are free beside a single group operation. Measured: naive **8.6 h**, this route **≈3.5 h serial** (105 min at 2-way), memory **~28 GB** — so the win is ~2.5×, not the 250× I predicted, and the honest lever was sharing the doubling chain rather than folding the generators. TWO WALLS FOUND ONLY BY BUILDING: `decide`ing a `2^15`-element list equality hits 29 GB (so the chunk partition is a PROVED GENERIC LEMMA, not a `decide`), and the kernel's `foldl` accumulator is a thunk chain of depth `nbits × terms` that ABORTS at `255 × 1024` with a stack overflow that `ulimit -s unlimited` does not fix — hence 1024-term chunks evaluated as two 512-term folds. ⚠ ASSUMES: P10 is untouched (5f states the verifier's opening check, 5h states its `sg` leg; both PASS, and that passing implies the prover KNOWS an opening is the extraction argument, which neither rung moves). And **`srs.g` is now the largest single piece of trusted data in the stack** — on-curve-checked but not DERIVED; deriving it needs in-kernel BLAKE2b + SvdW at 32,768 inputs, named as the honest next rung. 5g re-priced to `2 × ~3.5 h` with the same bounded memory — a fixture-and-scheduling item, not a wall.
import Dregg2.Bridge.MinaWrapSg -- ⚑ THE `2^15` MSM AS A COMPILED FUNCTION OF A BLOCK'S WIRE ARGUMENTS, not a kernel `decide` over pinned literals — the single move that decides whether a Mina checkpoint closes in minutes or in a day (`docs/MINA-CHECKPOINT-CADENCE.md` §5a). `sgVerdict chals gens sg` holds NO fixture: `k` challenges in, `2^k` generators in, `some true` / `some false` / `none` out, where `none` means the wire never presented a checkable shape and NOTHING was checked — a `2^14`-generator fold is a well-formed answer to a proof of a DIFFERENT index, and answering `false` to it would be indistinguishable from having checked. Rooting costs **+0 closure**: it imports `PastaIpaFold` and nothing else. ⚑ WHICH OBJECT IT BINDS: the CONTENTS (`sg` really is `⟨s, srs.g⟩` over the real generators), which is rung 5h's statement; the `5h-AIR` rungs bind a PROOF OBJECT and bind no contents, and neither subsumes the other. The evaluator it calls is `PastaIpaFold` §3c's `msmHornerM`, which the KERNEL instance `MinaWrapSgCore.ChunkOk` also calls — one definition, two consumers, no twin (they were a twin until 2026-07-30). ⚠ `#guard` is the UNTRUSTED evaluator and is NOT a proof: the kernel proves the CHECKER (`msmHorner_eq_msmN`, arbitrary `AddCommGroup`, bit budget a real hypothesis), a differential checks the INSTANCE. The instance differential on real devnet block 539508 — both polarities, plus the 32 chunk statements the kernel takes ~3.5 h to `decide` — is `Dregg2.Bridge.MinaWrapSgWeld`, NOT rooted because it carries the 32,768-generator fixture; run it with `scripts/run-mina-sg-compiled.sh`.
import Dregg2.Circuit.Emit.MinaWrapOpeningGate
import Dregg2.Circuit.Emit.MinaWrapOpeningWeld
import Dregg2.Circuit.Emit.MinaWrapPublicInputFromHeader -- ⚑ THE WELD THAT MAKES THE IN-KERNEL LADDER MEAN SOMETHING ABOUT A REAL HEADER. It ties `MinaStateHashWordGate`'s words 12 and 11 — derived from the SERVED `stateHash` plus the served proof bytes — to `MinaWrapPublicCommGate.PUBLIC_INPUT[12]`/`[11]`, so §3's whole in-kernel arithmetic arc now reads **"against the header a node served for block 539508"** rather than "against forty numbers". 104 lines, 9 pinned obligations, and it imports only the two gates it welds — no 32,768-literal SRS module, so it is light enough to live in the root, unlike the rung-5h instance family allowlisted out at `228e51de7`. ⚠ It was left unrooted by BOTH lanes that touched it ("kernel, unrooted" and then "left as found"), which meant its `#assert_axioms` and `#guard`s ran in NO CI target — the F-B1 shape, on the one module whose job is to connect the arithmetic to reality.
import Dregg2.Circuit.Emit.MinaWrapPublicCommGate
-- ─── ⚑⚑ THE ACCRUAL GATE (2026-07-25) ────────────────────────────────────────────────────
-- The vacuity campaign was measured over its own window and it was LOSING: 52 refuted-floor
-- carriers removed, 63 ADDED, net +11. Forty-six of the additions were brand-new theorems in
-- brand-new modules landed by OTHER lanes while the campaign ran, each taking
-- `Poseidon2SpongeCR` — which this tree PROVES FALSE at deployed BabyBear parameters — as a
-- hypothesis, so each is VACUOUS. Accrual was outrunning removal ~1.2 : 1. Porting faster
-- cannot win that race; only a gate can.
--
-- `#floor_ratchet` is that gate, and it is invoked HERE, at the end of the root module every
-- lane already builds, because at this point the environment holds the whole tree. It fails
-- the build when a declaration NOT in `Dregg2.Verify.FloorRatchetBaseline.grandfathered`
-- takes a refuted floor as a hypothesis — via a binder, via a `Prop`-def whose BODY carries
-- one (the ~250 `descriptorRefines`-shaped carriers no binder-keyed ruler can see), or via a
-- structure FIELD (the bundle class).
--
-- ⚑⚑ B3/B1/B2 CLOSED (2026-07-25). Two bypasses were found by adversarial probe and both are
-- now teeth. (B3) A new theorem taking an already-grandfathered floor-carrying STRUCTURE as a
-- hypothesis added ZERO carriers while being exactly as vacuous as a floor binder — the
-- fixpoint covered `Prop`-valued DEFS only and never propagated through structure FIELDS. 32
-- bundles were a standing bypass; `CommitSurface` alone (four refuted floors as fields) is
-- reached by 409 declarations. (B1/B2) The exemption fired on a conclusion SHAPE — `¬ Floor …`
-- or `False` — which is not a refutation test: a declaration could ASSUME a refuted floor and
-- walk out through it. It now requires the declaration to REFUTE floor content and ASSUME
-- none. The instrument got TRUER and the number went UP: 1600 → 2130 carriers, +530 that were
-- always vacuous and always uncounted (+425 bundle class, +113 contrapositive soundness claims
-- — `consensus_safe_under_floor`, `lightclient_no_fork`, every `*_rejects_*`).
--
-- ⚑ BOTH of its inputs arrive through IMPORTS — the environment, and the baseline (a Lean
-- `def`). That is deliberate, and it is the repair of this campaign's previous tooth:
-- `Tools/ConeCutoverListCommit` asserted its post-state by reading files through `IO.FS`,
-- which lake's import-based dependency graph cannot see, so it was replayed from a cached
-- olean forever — `lake build` EXIT=0 while a forced fresh elaboration EXIT=1 on a REAL
-- violation. Nothing here reads a file. Adding a declaration anywhere under `Dregg2/`
-- invalidates that module's olean, transitively invalidates this root, and re-runs the check.
--
-- The floor list is DERIVED from the tree's own refutation theorems, not hand-written, so
-- proving a new floor false arms the gate for it the same build. `#floor_ratchet_floors`
-- prints what it derived. The gate only GATES: a clean run means "no NEW declaration took a
-- provably-false hypothesis", NOT that the grandfathered carriers are sound.
import Dregg2.Verify.InjSpelledFloors -- ⚑ the WHOLE-FUNCTION DIGEST floors, REFUTED by pure cardinality. `Circuit/EffectCommit2.funcComponent` binds `Function.Injective D` for `D : (CellId → AssetId → ℤ) → ℤ`, `Caps → ℤ`, `BornEmptySideTables → ℤ`, … at ~600 sites, its doc-comment calling that "the same CR bar as cellLeafInjective". It is not: those domains are UNCOUNTABLE and `ℤ` is countable, so NO injection exists at any parameters, for any hash, in any field — a cardinality impossibility, not an optimistic hash assumption. These theorems are what ARM the inline half of `#floor_ratchet` (`Verify/InjSpelling` derives its gated signature set from them).
import Dregg2.Verify.ApexPremiseVacuity -- ⛑⛑⛑ THE TWO APEX THEOREMS OF THE ASSURANCE CASE HAVE UNSATISFIABLE PREMISES, PROVED. `AssuranceCase.deployed_system_secure` (the composed A∧B∧C∧D∧E capstone) and `AssuranceCase.unfoolability_guarantee` both carry the same five commitment-floor binders (`compressInjective cmb`, `compressInjective compress`, `compressNInjective compressN`, `cellLeafInjective CH`, `RestHashIffFrame RH`). `apexCommitFloor_unsatisfiable` refutes that bundle OUTRIGHT — not at deployed parameters, at EVERY parameter — because `RestFrameCardinalityFloor.restHashIffFrame_false_by_cardinality` kills `hRest` for every `RH : RecordKernelState → ℤ` by Cantor. So there is no assignment of the commitment portal, deployed or toy, under which either apex applies, and `CircuitSoundness.CommitSurface` (same five as FIELDS) is likewise uninhabited, which makes `AssuranceCaseGrounded.deployed_system_secure_grounded{,_v2}` vacuous twice over. INDEPENDENTLY, `apexCommitFloorSansRest_false_at_babyBear` shows the remaining FOUR are already contradictory at deployed BabyBear width, so repairing the rest-hash alone does not revive the apex; both must land. ANTI-VACUITY at both poles: the width hypothesis FIRES at a BabyBear-reduced compress (`bbCompress_bounded`, with an EXPLICIT collision `bbCompress_explicit_collision`, not only a pigeonhole count) and FAILS at an unbounded pairing where the floor is genuinely TRUE (`pairCompress_injective`), so the width binder is load-bearing; `RestHashIffFrame` has NO positive pole at all, so §3.3 exhibits both poles of the repair-shaped per-pair weakening instead (`restSeparatesAt_satisfiable` / `restSeparatesAt_refutable`, mutation-tested: moving the refutable pair to the diagonal reds it). WHY NO INSTRUMENT SAID SO: the two apexes are grandfathered at `FloorRatchetBaseline` lines 47–48, so `#floor_ratchet` cannot red on them; the Rust meta-gate `security_property_nonvacuity_gate.rs` has NO row for `deployed_system_secure` at all, and the row it does have for `unfoolability_guarantee` registers a `fires` companion (`light_client_fires_on_real_chain`) that fires a STRICTLY WEAKER theorem at the constant-zero portal — an instance which itself refutes the apex's `compressInjective` premises (`registered_fires_instance_refutes_apex_premises`). WHAT SURVIVES: `unfoolability_attestation_floor_free` — the whole-history ATTESTATION half takes none of the five and FIRES on the real honest chain (`unfoolability_attestation_fires`); it is the CRITICAL-3 conservation-from-verification half that consumes all five and is unavailable at any parameters. NOT A REPAIR: restoring conservation needs a rest-hash over the FINITE SUPPORT actually touched (the `accounts` rows), which is a commitment-shape decision, not a proof. #assert_axioms-clean (16 keystones), no `sorry`/`axiom`/`native_decide`.
import Dregg2.Verify.RestFrameFiniteSupportSuccessor -- F1 DE-RISKED (not ported): states `RestHashIffFrameFin`, `RestHashIffFrame`'s body UNCHANGED but gated on both states being `FiniteRepresentable` (the `denote`-image of a `FinKernelState` — the ALREADY-LIVE, previously-unconnected finite-map refinement `Circuit.FinKernelState`/`Circuit.FinFrameHash` build). Checked against the tree's own floor doctrine, which `RestHashIffFrame` itself never got (it was refuted before anyone asked): SATISFIABLE unconditionally (`restHashIffFrameFin_satisfiable`, at the unbounded reference sponge `Reference.refSponge` — genuinely `Poseidon2SpongeCR`, the same abstract-not-deployed pole `ApexPremiseVacuity`'s `pairCompress` occupies, so no hypothesis survives in the theorem's own type), REFUTABLE (`restHashIffFrameFin_refutable`, the constant rest-hash fails at the SAME reachable pair — `rootedInit`/`finInit` — the positive tooth fires on), hence NOT PROVABLE (`restHashIffFrameFin_not_provable`). One real consumer RE-DERIVED end to end: `StateCommit.stateCircuit_rejects_field_tamper` (the smallest theorem carrying `RestHashIffFrame`) reproved as `stateCircuit_rejects_field_tamper_fin`, firing on a concrete instance. PRICED, not guessed: ~230 of the 234 binder positions are mechanical (widen the binder list beside the `AccountsWF` hypotheses already there); a SECOND, uncounted family of 13 `RestIffNo*` siblings in `Circuit.EffectCommit2` shares the identical cardinality obstruction and needs the same treatment; the genuinely non-mechanical cost is discharging `FinKernelState`'s own undischarged honesty gate (`hpres`, the per-effect commuting square) for the ~17-25 real effect handlers — NOT done here, and a prerequisite for applying `FiniteRepresentable` to any real post-genesis state rather than only to this file's abstract witnesses. Independent of all of it: the width-bound legs (`compressInjective`/`compressNInjective`/`cellLeafInjective`, `ApexPremiseVacuity` LEGS 1-4) remain refuted at deployed BabyBear width regardless, so this alone does not revive `deployed_system_secure`/`unfoolability_guarantee`. No `FloorRatchetBaseline` row added (nothing here carries a false hypothesis). #assert_axioms-clean, no `sorry`/`axiom`/`native_decide`.
import Dregg2.Verify.FloorRatchetBaseline
import Dregg2.Verify.FloorRatchetBaselineInline -- the INLINE-SPELLED half of the grandfathered surface, kept in its own generated file: every name became visible on one day from one cause (the gate learned to read a floor spelled `Function.Injective f` instead of by name), and the main baseline is edited by other lanes in this same working tree.
import Dregg2.Verify.FloorRatchetSpecimens -- the gate's own SELF-TEST fixtures: five declaration TYPES with a known-correct exemption verdict, asserted on EVERY root build. An exemption predicate that degenerates in either direction is invisible from a green build — exempt everything and the gate passes forever; exempt nothing and writing a refutation becomes a build error. `surface` hard-errors if any verdict moved.
import Dregg2.Verify.FloorRatchet

#floor_ratchet
#teeth_wired
