# scripts/descriptor-anchor-inertness-baseline.txt — DECORATIVE PUBLIC INPUTS per served descriptor.
#
# A DECORATIVE anchor is a `pi_binding` column whose connected component in the
# constraint graph is a SINGLETON: it co-occurs with no other column in any gate body,
# boundary body, window-gate body, lookup tuple, hash site, transition or bus op. The
# proof carries the value; nothing in the circuit is about it.
#
# ⚑ THESE NUMBERS MAY ONLY GO DOWN. Regenerate with
#   scripts/check-descriptor-anchor-inertness.py --write-baseline
# and say in the commit WHICH anchor became load-bearing and by what gate.
#
# ⚠⚠ ONE ROW WENT UP AND THEN DOWN AGAIN, BOTH ON PURPOSE, ON THE SAME DAY — 2026-08-04.
# `dregg-solana-lightclient-verify::v1`: 11 → 19 → 10. Read the whole sequence before treating any
# single step as a regression or as a win; a bare column count is a bad summary of both moves.
#
#   * 11 → 19 (morning). FOUR columns LEFT the decorative set — the total-stake limbs became PI-bound
#     AND joined to the quorum chain, the FIRST columns in this census ever to stop being decorative.
#     TWELVE joined it: `ANCHOR_ROOT` was ONE column holding a 256-bit SHA-256 stake-table root (i.e.
#     compared at 31 bits, a colliding pair findable in 2^31) and became NINE radix-2^31 limbs binding
#     the whole root; the +8 new limbs were decorative for exactly the reason the single column was,
#     and the shifted BANK_ROOT/SLOT_COL account for the rest. **The count rose because a hash stopped
#     being truncated, not because a public input stopped being checked.**
#
#   * ⚑ 19 → 10 (this commit). The nine `.first` anchor-root limbs are DELETED. The descriptor
#     absorbed `dregg-solana-stake-table-fold::v1` — it is MULTI-ROW now, one row per stake-table
#     entry — and the light client's weak-subjectivity anchor is the fold's EIGHT `.last` output
#     lanes, which sit in the component that contains the whole trace. The denominator is the fold's
#     accumulator, `.last`-pinned, for the same reason. What remains decorative is the nine bank-root
#     limbs and the slot: carried by the proof, read by no gate.
#
# ⚑ THE METRIC IS COLUMN COUNT, AND COLUMN COUNT IS THE WRONG DENOMINATOR FOR "how much of this
# proof's public statement is unbound" — widening a root to bind it properly and publishing eight more
# unrelated values move it the same direction. It moved the RIGHT direction only when the root became
# DERIVED. Anyone tightening this gate should count BITS-OF-PUBLIC-STATEMENT-UNBOUND, or the
# decorative SHARE, not columns.
#
# ⚠ THIS FILE IS EDITED BY HAND FOR THAT ROW, NOT REGENERATED.
#
# ⚑ 2026-08-05 — THE TWO DARK BAZAAR ROWS WERE NEVER MINTED, AND NEITHER NEEDS TO BE. The note that
# stood here said `--write-baseline` would mint rows for `dark-bazaar-private-poa-n4k4::receipt8-v2`
# (8) and `dark-bazaar-private-poa-settlement-n4k4::transition-v3` (33), and that blessing them from
# here would be exactly the laundering this gate exists to refuse. It was right. Both were BOUND or
# DELETED instead, so both leave the census at zero rather than entering it:
#
#   * `::transition-v3` -> `::anchored-v4`. Its 33 split into two unlike halves and the split is the
#     whole repair. THIRTY-TWO were the lanes of the judge's input / successor / public-view /
#     receipt digests, UNREAD — no gate body, boundary body or lookup tuple named those columns —
#     while the descriptor's docblock said it "binds" them. They hash canonical JSON strings
#     (`DarkBazaarJudgeWire.digestString`), so no AIR over that four-row fixed-width trace can derive
#     them; publishing them was a statement the circuit could not make, and absorbing them as chip
#     INPUTS would have zeroed this census while leaving them free witnesses. They are DELETED, and
#     eight ANCHOR lanes are published in their place — the image, under the deployed Poseidon2 chip,
#     of the book root, the clearing, the ten settlement scalars and the visibility grade, forced by
#     `chip_lookup_sound_N` the way `solStakeFold_root_shares_a_constraint_with_the_stake_rows`
#     forces Solana's. The THIRTY-THIRD was the visibility grade, forced to the constant 1 by a
#     one-column gate: a measurement artifact of "a unary gate joins nothing", not an unbound value,
#     and now stated positively as `visibility_pi_is_forced_to_the_proven_grade`.
#     Public inputs 55 -> 31, width 496 -> 480. VK rotates.
#
#   * `::receipt8-v2` is DELETED outright. Its 8 were the same unbindable receipt digest, and the
#     anchoring mechanism does NOT transfer: v2's whole public surface beyond those eight lanes is
#     the v1 statement, already published and already chip-derived, so an anchor over it would be
#     redundancy dressed as a binding. Its own Lean docblock had said the pins "do not by themselves
#     prove that the receipt is the output of the PoA state machine". A family whose only reason to
#     exist is publishing a value nothing can check does not get a baseline row; it stops.
#
# ⚑ AND SAY WHAT THE COLUMN COUNT WOULD HAVE HIDDEN, per this file's own warning above. Counting
# columns, v4 "removed 32 and added 8" — but the eight are the first published value in this family
# that the circuit DERIVES, and the thirty-two were the entire settlement identity claim. The
# decorative SHARE of that proof's public statement went 33/55 = 60% to 0/31.
#
# ⚑ 2026-08-05 — `dregg-mina-lightclient-verify::v1` CHANGED SHAPE AND ITS ROW DID NOT MOVE. Read
# the two facts together, because the count alone hides both:
#   * the descriptor went width 30 -> 49, PIs 20 -> 29, constraints 50 -> 69. `PICKLES_OK` — a
#     witnessed bit forced `= 1` with nothing computing it — became `PICKLES_WITNESSED` (the residue,
#     still a bit, renamed so nobody reads it as checked) plus `WRAP_FS_PROVED`, whose `= 1` guards
#     NINE `proof_bind` constraints pinning the row's attested program to the semantic fingerprint of
#     `dregg-pasta-fq-wraplink::v1`, lane by lane.
#   * **NINE PUBLIC INPUTS ADDED, ZERO DECORATIVE ANCHORS ADDED.** The nine new PI columns are the
#     sub-proof's public-input commitment lanes, and a `proof_bind` relates its guard, its commitment
#     and its vk column — so all nine land JOINED. The row stays 18.
# ⚠ AND SAY WHAT DID **NOT** HAPPEN: the eighteen are the anchor and tip state lanes, and this rung
# did not bind one of them. They are still read only by arity-1 lane lookups and joined to nothing.
# Binding them is a different rung (the Poseidon fold over Pasta `Fp`); this row is unmoved and the
# honest reason is that the work that would move it was not done here.
#
# ⚑⚑⚑ 2026-08-05, SECOND PASS — THE ROW MOVED, 18 -> 9, AND THIS IS THE ONE CASE WHERE THE COLUMN
# COUNT AND THE MEANING CANNOT DISAGREE. Read it against the paragraph directly above, which is the
# same descriptor earlier the same day recording that its shape changed and its row did NOT.
#   * WHAT LANDED: `LightClientMinaAir` §2c. `LINK_OK` was a BARE `= 1` on a witnessed column — one
#     felt to set, nothing behind it. It is now the GUARD of a second nine-lane `proof_bind` whose
#     `vk_pin` is the semantic fingerprint of `dregg-mina-lightclient-link::v1` and whose `commit`
#     vector IS the row's nine PUBLISHED `TIP_STATE` columns (21..29). A `proof_bind` relates its
#     guard, its commitment and its vk lanes, so those nine tip columns are now JOINED.
#   * WHAT LEFT THE ROW: exactly the nine tip lanes. The row was the eighteen anchor+tip state
#     lanes; it is now the NINE ANCHOR lanes (cols 12..20) and nothing else.
#   * ⚑ **THE DENOMINATOR DID NOT MOVE. `public_input_count` IS STILL 30.** This file warns twice
#     that column count is the wrong summary — Solana's row ROSE from 11 to 19 while a root was
#     being bound properly. Here no public input was added or removed: the tip block was already
#     published, and what changed is that a constraint now names it. Decorative share 18/30 = 60%
#     -> 9/30 = 30%, and the count and the share move together for once. The descriptor also went
#     width 49 -> 58 and 62 -> 63 constraints; the head VK rotates.
# ⚠ AND SAY WHAT DID **NOT** HAPPEN, because the previous paragraph had to say the same thing:
#   * the NINE ANCHOR lanes are unmoved. Still PI-bound, still read by one arity-1 lane lookup each,
#     still joined to nothing. `LightClientAnchorConnectivity.minaVerify_anchor_lanes_are_read_but_never_joined`
#     is the narrower tripwire that keeps saying so.
#   * a second bind could NOT fix that, and the reason is structural rather than schedule: a
#     `proof_bind`'s `commit` is the only vector naming off-row evidence and there is one `piCommit`
#     per engine, so two binds against ONE program with DIFFERENT commitments are incoherent. The
#     anchor lanes are refused against the segment sub-proof's own anchor block AT THE CONSUMER
#     (`mina_head_verifier` REFUSAL 14), elementwise. That is an executor check, not an edge.
#   * ⚠ and connectivity is still CO-OCCURRENCE, not derivation. What these nine tip lanes co-occur
#     with is a guard and a program fingerprint; the bound sub-proof's own `OWNHASH` is a free
#     witness (`LinkHashResidual`), so what is gated is the segment's SHAPE, not its hashes. Read
#     `minaLink_decorative_anchors`' caveat before reading `9` as sound.
#
# ⚑ `dregg-pasta-fq-wraplink::v1` is NEWLY SERVED (2026-08-05) and enters at ZERO — all 224 of its
# public inputs sit in a single component spanning all 469 columns — so it gets no row, per the
# "descriptors at zero are omitted" rule above rather than by omission.
#
# ⚑⚑ 2026-08-05 — `dregg-pasta-msm-bucketed-pallas-n59b255-c2-w192::v1` ENTERS AT **192**, THE
# LARGEST ROW IN THIS FILE, AND THE LARGEST BY SHARE BY A WIDE MARGIN. Per this file's own warning
# above, say the share: **192 of 219 public inputs, 87.7%**. The next worst WAS `bridge-action-leaf` (DELETED 2026-08-07)
# at 28 columns (26 until 2026-08-06, when the amount widened from 2 non-injective 32-bit limbs to
# 4 injective 16-bit ones — the whole descriptor is inert by construction, so the row tracks its PI
# count exactly). A row this size is not a number to leave uncommented, so:
#
#   * WHAT THE 192 ARE. The descriptor is the ξ-aggregate of Mina devnet block 539508 — 8192 x 804,
#     475 constraints — and it publishes the six-value squaring basis `(ξ³², ξ¹⁶, ξ⁸, ξ⁴, ξ², ξ)` as
#     6 x 32 eight-bit felts on columns 612 … 803. Each of those columns is touched by EXACTLY TWO
#     constraints: `chalPinGates`' first-row `piBinding`, and `chalThreadGates`' `nxt CH_m − CH_m`.
#
#   * ⚠ THE THREAD IS NOT A MEASUREMENT ARTIFACT, AND IT IS NOT A BINDING EITHER. `nxt c` and
#     `loc c` are the SAME column, so a thread is a UNARY window gate and this census reads it as
#     joining nothing — correctly. Contrast the `::transition-v3` visibility grade above, where a
#     unary gate forcing `= 1` WAS an artifact of the metric. Here the unary gate does real work: it
#     makes the block one trace-global vector instead of 8192 free ones, and
#     `a_basis_that_varies_by_row_is_refused` measures it firing (p3 constraint `#311`, row 4095).
#     What it does not do is relate the basis to the MSM. It is 192 free felts, not 1 572 864.
#
#   * WHY THIS GETS A ROW WHERE `::receipt8-v2` GOT NONE. That family was stopped because its eight
#     digests hashed canonical JSON strings, so NO AIR over that trace could ever derive them —
#     publishing them was a statement the circuit could not make. The ξ basis is the opposite case:
#     the in-circuit derivation is DESIGNED and PRICED (`PastaMsmBucketed` §7.3 — the tensor chain
#     that re-derives each row's digit from the wire block, ~1 400 constraints and ~2 700 columns,
#     plus the `8 → 30` limb regroup and its range certificates that `MinaWrapXiAggregateMsm` §2-pre
#     names). And the felts are not free in the deployment: a batch verifier compares
#     `aggPIs[27 … 218]` against `dregg-mina-xi-scalar-vector::v2`'s published block ELEMENTWISE
#     (`MinaWrapXiBasisWeld`). This census cannot see that weld and should not — it is outside the
#     descriptor — but it is the difference between "unbindable" and "not yet bound here".
#
#   * ⚠ WHAT WOULD BE CHEATING THIS ROW DOWN. Recomposing the 32 limbs of each value into a value
#     column, or hashing the block into an anchor, joins all 192 columns to each other and takes
#     this row to 0 while leaving 192 free witnesses — exactly the "absorbing them as chip INPUTS"
#     laundering `::transition-v3` refused above. The row goes to zero when `T_COVER`'s declared
#     digits become the IMAGE of these felts in-circuit, and not before.
#
#   * ⚑ AND THE ROW HAS A RUNNING TEST BEHIND IT, because a documented wound is not a detected one.
#     `circuit/tests/pasta_msm_bucketed_prove.rs::the_published_basis_is_not_yet_bound_to_the_
#     declared_digits` proves and verifies the aggregate against a basis that is NOT the block's
#     own — same trace-template columns, same published `COMBINED_GOLD` — so the verdict is
#     exhibited on the deployed prover rather than read off this script's graph. That test going
#     RED is the signal to take this row to 0. It exists because this walker was measurably wrong
#     about this class the same day: the `proof_bind` widening made `commit`/`vk`/`bound` lane
#     VECTORS, the walker took only operands that were themselves an expression dict, and
#     `dregg-mina-lightclient-verify::v1` scored 18 -> 27 with no anchor having changed.
#
# The connectivity facts themselves are named theorems in
# `metatheory/Dregg2/Circuit/Emit/LightClientAnchorConnectivity.lean`
# (`sol_decorative_anchors`, `sol_pinned_denominator_is_not_decorative`).
#
# `count  descriptor-name`; descriptors at zero are omitted.

1	dark-amm-private-v1::wide-poseidon2-v2
12	dregg-automatafl-legc-n11
12	dregg-automatafl-legc-n5
18	dregg-bound-presentation::v1
16	dregg-descent-custody-census-fixed8-v2
11	dregg-eth-lightclient-verify::v1
8	dregg-guarded-hiding-span-m0::wide-blinded-commit-blind5-v1
12	dregg-midnight-lightclient-verify::v1
# ⚑⚑ 2026-08-08 — TWO MINA ROWS MOVED UP AND ONE APPEARED, WITH NO DESCRIPTOR BYTE HAVING CHANGED:
# THE INSTRUMENT CONFESSED, NOT THE DESCRIPTORS. The walker's `proof_bind` arm related the whole
# DECLARATION (`guard U commit U vk U bound`) while `descriptor_ir2.rs` emits polynomials over
# `commit` only under a `bound` and over `vk` only under a `vk_pin` — and every served seam is
# `bound := none`. So a `9` here meant "nine anchors whose ties are emitted constraints, plus
# twenty-seven whose tie was a declaration scored as a join". Emission-faithful (one relate() per
# emitted polynomial): `dregg-mina-lightclient-verify::v1` 9 -> 36 (the tip nonet and BOTH
# sub-proof-commitment nonets return — they are PORT surfaces, declared and covered/counted in
# `MinaSeams` §8b: tip+anchor by the emitted `headTipSeam`, the chainlink block by REFUSAL 4;
# ⚠ the conjunction block's REFUSAL 15 is DEAD CODE and counted uncovered), and
# `dregg-mina-lightclient-link::v1` 0 -> 17 (nine `BODYHASH` + eight `BODY_ACC`, published on the
# 08-08 flag day and welded by NOTHING; the eight acc lanes are UNREAD — not even a width lookup).
# The Lean twin moved the same day: `LightClientAnchorConnectivity` 53 -> 80 with the same split.
# These rows may only SHRINK from here, and the shrink must name the weld that landed.
36	dregg-mina-lightclient-verify::v1
17	dregg-mina-lightclient-link::v1
192	dregg-pasta-msm-bucketed-pallas-n59b255-c2-w192::v1
19	dregg-presentation-freshness::summary-v1
1	dregg-shielded-spend-pinned-root::v1
10	dregg-solana-lightclient-verify::v1
1	dregg-temporal-predicate-gte::dsl-v1
11	dregg-tm-lightclient-verify::v1
12	faithful-note-spend-v2::exact-note16-root8-hiding
4	faithful-note-spend-v3-plan::exact-aafi-fns3-rotated-wide-state
8	private-book-bfv-threshold-terminal-q0-b80::exact-limb-v1
1	private-graph-rewrite-4x2::injective-swapnet-poseidon2-v1
17	private-graph-rewrite-cell-4x2::injective-swapnet-poseidon2-v1
16	private-preference-cell-n4k4::score2-wide-poseidon2-v1
