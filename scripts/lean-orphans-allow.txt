# lean-orphans-allow.txt — modules under metatheory/Dregg2/ that are DELIBERATELY
# not reachable from any default lake target (Dregg2/Metatheory/Polis/Market/Bfv).
#
# WHY THIS FILE EXISTS
#   The `Dregg2` lean_lib has no `globs`, so `lake build` compiles only the modules
#   transitively imported from metatheory/Dregg2.lean. A new metatheory/Dregg2/**.lean
#   that nothing imports builds green under `lake env lean <file>`, passes its own
#   #assert_all_clean / #assert_axioms / #guard, and is covered by NOTHING in CI — a
#   downstream regression can silently break it and no gate goes red. That hole recurred
#   (StepBridge, AciNormal, AciComplete) and was each time caught only by a human reviewer.
#
#   scripts/check-lean-orphans.sh FAILS if any Dregg2/**.lean is neither reachable nor listed
#   here. So a NEW orphan reds CI and forces a decision: register it in Dregg2.lean, or add
#   it here WITH A REASON. Registering a listed module and NOT removing it here is ALSO a
#   failure (stale entry) — the allowlist cannot silently outlive the exclusion it records.
#
# FORMAT  one dotted module per line, optional `# reason` after it. `#`-only lines and blanks
#         are comments. Every listed module MUST exist as a file and MUST be a genuine orphan.
#
# ⚑ THIS FILE IS NOT THE WHOLE COVERAGE QUESTION (2026-07-25, B5).
#   It governs ONE thing: is a `Dregg2/**` module compiled by a default lake target. It does NOT
#   ask whether a module is in the environment `#floor_ratchet` / `#teeth_wired` read, which is
#   the closure of `metatheory/Dregg2.lean` ALONE — a strictly smaller set than "what lake
#   builds", because `Metatheory.+` and `Polis.+` are GLOBBED libs and `Market`/`Bfv` are separate
#   roots. A module can therefore be BUILT, axiom-checked, green in CI, and still carry a
#   hypothesis this tree proves FALSE with no instrument counting it. That second question is
#   `scripts/check-ratchet-darkness.sh` + `scripts/lean-ratchet-dark-allow.txt`.
#
#   FIVE entries left this file to that gate's RULE 1 (a dark module that MENTIONS a refuted floor
#   gets rooted, not listed): Circuit.{FinBindsKernel,FinInjectivityCollapse,KernelConfigSoundness,
#   KernelConfigSoundnessAvail} and Circuit.Emit.MembershipDepthGeneralRung2 — 51 floor mentions
#   between them, sitting behind lines that said "GREEN self-checked, REGISTERABLE". They are now
#   imported from `metatheory/Dregg2.lean`. When you consider adding an entry here, check the same
#   thing first: `bash scripts/check-ratchet-darkness.sh` will tell you if it is a floor carrier.
#
# These entries are the CURRENT (2026-07-18) orphan census — WIP from this and other
# terminals. As each cluster's work lands, register the finished modules in Dregg2.lean and
# delete them from here; the staleness check will remind you.

# --- in-flight Lean-authored circuit EMIT/REFINE/RUNG pipeline (AutomataflResolveEmit family); each lands in Dregg2.lean when its refinement rung is discharged ---
Dregg2.Circuit.Emit.AdjacencyMembershipRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.AdjacencyMembershipRefineAudit  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.AdjacencyMembershipRung2  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.AttestedFactMembershipEmit  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.AvailWideMembersNarrow  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.BoundPresentationRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.BoundPresentationRung2  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.BridgeActionEmit  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.BridgeActionRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.CommittedThresholdEmit  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.CommittedThresholdRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.DerivationEmit  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.DerivationRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.DerivationRung2  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.DfaRoutingGeneralEmit  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.DfaRoutingRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.DfaRoutingRung2  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.EffectActionBindingEmit  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.EffectActionBindingRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.EffectActionBindingRung2  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.EffectVmEmitIvcStateTransition  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.EffectVmEmitIvcStateTransitionRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.EffectVmEmitIvcStateTransitionRung2  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.EffectVmEmitIvcStateTransitionRung2Full  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.EmitAllJson  # GREEN, NO self-check — asserts nothing on its own — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.EmitGraduate  # GREEN, NO self-check — asserts nothing on its own — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.FoldEmit  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.FoldRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.FoldRung2  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.MembershipAuthRootEdge  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.MerkleMembershipRung2  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.MultiStepChainEmit  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.MultiStepChainRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.NoteSpendingLeafEmit  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.NoteSpendingLeafRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.NoteSpendingLeafRung2  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.PredicatesNeqRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.PredicatesRelationalCompoundRung2  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.PredicatesRelationalCompoundRung2Full  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.QuantifiedAbsenceEmit  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.QuantifiedAbsenceRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.TemporalPredicateEmit  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster
Dregg2.Circuit.Emit.TemporalPredicateRefine  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit.Emit refine/rung cluster

# --- circuit-soundness WIP (FRI/STARK proximity-gap, AlgoStark fanout, kernel-config, wrap-safety) ---
# ⚑ CORRECTED 2026-07-24: this heading used to say "several carry sorry". MEASURED FALSE —
# a comment-stripped scan finds ZERO real `sorry` tokens in ANY allowlisted orphan and ZERO
# across all 1730 Dregg2/**.lean. Every file here is self-checked GREEN. The reason they stay
# out of the default build is ci.yml's documented 28 RED-AT-HEAD Circuit.Emit modules
# (elaboration-time `sorryAx` from a Type mismatch — not a source `sorry`), not carried sorry.
Dregg2.Circuit.CustomLeafEncoding  # GREEN, NO self-check — asserts nothing on its own — Circuit-soundness (FRI/STARK/kernel) cluster
Dregg2.Circuit.DeltaProto  # GREEN, #guard-only (no axiom self-check) — Circuit-soundness (FRI/STARK/kernel) cluster
Dregg2.Circuit.EffectsAsDataProto  # GREEN, #guard-only (no axiom self-check) — Circuit-soundness (FRI/STARK/kernel) cluster
Dregg2.Circuit.FriVerifierOracle  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit-soundness (FRI/STARK/kernel) cluster
Dregg2.Circuit.RotatedKernelRefinementAvailWideNarrow  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit-soundness (FRI/STARK/kernel) cluster
Dregg2.Circuit.WrapSafetyStaticAnalyzer  # GREEN self-checked (#assert clean), REGISTERABLE — Circuit-soundness (FRI/STARK/kernel) cluster

# --- the ML-DSA verify-core APEX SPINE: NOT unchecked — built on EVERY PUSH by ci.yml's
# `metatheory-pq-apex` job (target `Dregg2.Crypto.VerifyCoreArgAssembly`, routed through
# scripts/axiom-hygiene-guard.sh). These five are the ONE case in this file where "orphan" does
# not mean "covered by nothing": they are deliberately OUT of the default `lake build` and IN a
# dedicated lane, because `VerifyCoreEqSpecW` costs 2664s / 44.4 min of INTRINSIC elaboration
# (degree-256 `AdjoinRoot` power basis, `maxRecDepth 8000`) on a LINEAR import chain that the
# other four serialize behind — 15-20% of a "Zero-sorry guard" step CI measured at 233 min and
# 315.6 min against its own 330-minute job timeout. Their apex is
# `verifyCore_eq_specVerifyB_noArg_deployed` (deployed `MlDsaVerifyReal.verifyCore` <=> FIPS-204
# `specVerifyB`), kernel-clean. Verified green 07-25 before the split: ArgAssembly 54s,
# ArgRows 496s, HashFrame 37s (fresh `lake env lean`, contended box).
# ⚠ IF YOU DELETE THE `metatheory-pq-apex` JOB, THESE FIVE LINES BECOME LIES. Re-root them
# (`import Dregg2.Crypto.VerifyCoreArgAssembly` in metatheory/Dregg2.lean) in the same change.
Dregg2.Crypto.VerifyCoreEqSpecW  # GREEN — built by ci.yml `metatheory-pq-apex` every push, NOT unchecked (44.4 min intrinsic; the reason it is out of the default build)
Dregg2.Crypto.VerifyCoreUseHint  # GREEN — built by ci.yml `metatheory-pq-apex` every push, NOT unchecked (serializes behind EqSpecW)
Dregg2.Crypto.VerifyCoreHashFrame  # GREEN — built by ci.yml `metatheory-pq-apex` every push, NOT unchecked (37s itself; behind EqSpecW)
Dregg2.Crypto.VerifyCoreArgRows  # GREEN — built by ci.yml `metatheory-pq-apex` every push, NOT unchecked (496s itself; behind EqSpecW)
Dregg2.Crypto.VerifyCoreArgAssembly  # GREEN — built by ci.yml `metatheory-pq-apex` every push, NOT unchecked; THE APEX TARGET that lane names (54s itself)

# --- PQ-crypto spec WIP (ML-DSA/ML-KEM codecs, VerifyCore/SignCore specs, ACVP KATs, NTT faithfulness); spec-in-progress, not yet CI-clean ---
Dregg2.Crypto.KemSoundnessQuant  # GREEN self-checked (#assert clean), REGISTERABLE — PQ-crypto spec cluster

# --- light-client / proof-of-holdings bridge WIP (Eth/Tendermint/MPT clients, holding-fold tally) ---
Dregg2.Bridge.HoldingWeightedTally  # GREEN self-checked (#assert clean), REGISTERABLE — light-client / proof-of-holdings cluster

# --- game AIRs authored in Lean, actively churning (Dungeon/MultiwayTug/Automatafl); carry sorry — not for the metatheory-no-sorry build ---
Dregg2.Games.AutomataflAir  # GREEN self-checked (#assert clean), REGISTERABLE — game-AIR cluster
Dregg2.Games.MultiwayTug  # GREEN self-checked (#assert clean), REGISTERABLE — game-AIR cluster
Dregg2.Games.MultiwayTugAir  # GREEN self-checked (#assert clean), REGISTERABLE — game-AIR cluster
Dregg2.Games.MultiwayTugProgram  # GREEN self-checked (#assert clean), REGISTERABLE — game-AIR cluster

# --- LoadBearing lint/audit META-tooling (not part of the theory build closure) ---
Dregg2.Verify.LoadBearingAuditBroad  # GREEN, NO self-check — asserts nothing on its own — LoadBearing lint/audit tooling
Dregg2.Verify.LoadBearingAuditKey  # GREEN, NO self-check — asserts nothing on its own — LoadBearing lint/audit tooling
Dregg2.Verify.LoadBearingLint  # GREEN, NO self-check — asserts nothing on its own — LoadBearing lint/audit tooling

# --- app-demo WIP (ColonistJob) — carries sorry ---
Dregg2.Apps.ColonistJob  # GREEN self-checked (#assert clean), REGISTERABLE — app-demo cluster

# --- direct-FFI variant (FFIDirect) outside the export closure ---

# --- the Claims axiom-net — KNOWN 'ran nowhere' (see MEMORY: CI-meaningfulness audit); being wired on its own track ---
Dregg2.Claims  # GREEN self-checked (#assert clean), REGISTERABLE — Claims axiom-net

# --- B5b (2026-07-25): the two of fifteen unlisted orphans that stay OUT, with their MEASURED cost ---
# The other thirteen were fresh-elaborated green and ROOTED into metatheory/Dregg2.lean in the same
# commit; these two are green too, and the only question was where to pay for them. Both are now
# named by ci.yml's `metatheory-pq-apex` job, so neither is in "NO CI target" any more — which is
# what they were. Neither mentions a refuted floor (measured, comments stripped), which is the ONLY
# reason a non-rooted home is acceptable: scripts/check-ratchet-darkness.sh RULE 1 reds the moment
# that changes.
Dregg2.Crypto.MlKemKeygenRefine  # MEASURED 1882s (31 min) on one fresh `lake env lean`, and a TIMEOUT at 2400s on another — the `VerifyCoreEqSpecW` (44 min) class. The default build and the `metatheory-no-sorry` Orphan-gate step share a job that was already CANCELLED once for length (315.6 min vs `timeout-minutes: 330`); 31 min there is not a rounding error. Rides `metatheory-pq-apex` instead. Its ML-DSA sibling (MlDsaKeygenRefine, 38s) and both *KeygenAcvp modules ARE rooted.
Dregg2.Circuit.Emit.AutomataflNGenGolden  # A 2.98 MB byte-pinned WIRE GOLDEN (117 "lines", one of them enormous). The "keep the golden a separate leaf so the refinement surface does not pay for it on every build" posture is this tree's own, documented at `Dregg2.lean`'s `ParamComposeGolden` import — which cites THIS module by name as the precedent. Green at 6-26s; also named by `metatheory-pq-apex` now, so the golden is compiled somewhere.

# --- co-tenant lanes' modules that landed DURING the B5 sweep (2026-07-25), recorded not rooted ---
# These four were committed by other terminals between this sweep's HEAD snapshot (16f7719341) and
# its commit, so they arrived UNLISTED and turned this gate red for a scheduling reason rather than
# a real one. They are NOT this lane's to root: each belongs to a live lane that will register it
# with the rest of its work, and adding an import for a module whose author is still editing it is
# how two lanes clobber one root file. VERIFIED before recording, so this is not a shrug: both
# Bridge modules fresh-elaborate green (`lake env lean`, 12s / 14s, exit 0); the two AIRs return
# only "object file … .olean does not exist" for a sibling a concurrent build had not written yet,
# which is build state, not a defect. ⚑ AND CHECKED FOR THE THING THAT WOULD MAKE RECORDING WRONG:
# `scripts/check-ratchet-darkness.sh` reports ZERO refuted-floor mentions in all four, so nothing
# vacuous is hiding behind these lines. If any of them ever names a floor, RULE 1 of that gate reds
# and the entry stops being enough. The landing lane should DELETE its entry here when it roots.
Dregg2.Bridge.LightClientMidnight  # co-tenant lane (landed 5b540c20df, "formalize + STARK-ify the MIDNIGHT lightclient"); green at 12s; zero floor mentions
Dregg2.Bridge.LightClientSolana  # co-tenant lane, same sweep window; green at 14s; zero floor mentions
Dregg2.Circuit.Emit.LightClientMidnightAir  # co-tenant lane; the AIR half of LightClientMidnight; zero floor mentions
Dregg2.Circuit.Emit.LightClientSolanaAir  # co-tenant lane; the AIR half of LightClientSolana; zero floor mentions
