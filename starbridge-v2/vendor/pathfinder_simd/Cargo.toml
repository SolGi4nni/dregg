# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.


# ── ADDED BY THIS TREE, NOT BY THE VENDOR ─────────────────────────────────────
# This package is a `[patch]` source for the repo-root workspace and sits inside
# a MEMBER's directory, so the root's `exclude` array cannot reach it: cargo's
# `is_excluded` is `!explicit_member && excluded`, and `explicit_member` is a path
# PREFIX test against `members` — the parent member wins and the exclude entry is
# discarded silently. Without this table cargo claims the package from the repo
# root, fails to find it in `members`, and refuses (`cargo metadata` here exited
# 101 for the whole 2026-07 window). An empty `[workspace]` is the only mechanism
# that detaches a nested package. Kept by `scripts/check-workspace-closure.py`.
[workspace]

[package]
edition = "2018"
name = "pathfinder_simd"
version = "0.5.6"
authors = ["Patrick Walton <pcwalton@mimiga.net>"]
build = "build.rs"
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A simple SIMD library"
homepage = "https://github.com/servo/pathfinder"
readme = false
license = "MIT OR Apache-2.0"
repository = "https://github.com/servo/pathfinder"

[features]
pf-no-simd = []

[lib]
name = "pathfinder_simd"
path = "src/lib.rs"

[dependencies]

[build-dependencies.rustc_version]
version = "0.4"

# VENDORED third-party (path-dep, so no cap-lints). `pf_rustc_nightly` is set by its
# build.rs via rustc_version; declare it so `unexpected_cfgs` stays quiet without a blanket allow.
[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(pf_rustc_nightly)'] }
