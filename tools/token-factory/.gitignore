# Transient per-run FV log (empty on success; the halmos.log carries the proof).
artifacts/**/*.fv.log
# The PoC operator gate state (holds the issuing root key; regenerated by
# `deploy-gate init` — the artifact .md records the run's committed evidence).
artifacts/deploy-gate.state
